# 「디지털인쇄비」 셀 단위 grid-diff 요약 (권위 ↔ 라이브 스냅샷)

생성: 결정론 배치(`scripts/digital_griddiff.py`) · 권위=엑셀 절대 · 라이브=감사대상(스냅샷 읽기전용)
스냅샷: `_foundation/live-snapshot/latest/`(2026-06-30 00:33 빌드 · 8세션 흑백 COMMIT 포함)

## 결론 — 셀 단위 gap-0 (전건 일치)

| | 값 |
|---|---|
| 권위 격자 셀 수 | **954** |
| 라이브 적재 셀 수 | **954** |
| 미적재 셀(missing_cell) | **0** |
| 정합 불일치(mismatch) | **0** |
| 라이브 잉여(extra_live) | **0** |
| 차원 누락(dim_missing) | **0** |

전 954셀이 권위 단가와 **verbatim 일치**(match 954/954). 교정 적재본 불필요(NO-OP).

## 도수별(1도/4도/별색) × 판형 × 면 커버리지 (present/total)

| 도수 | 판형 | 단면 | 양면 |
|------|------|------|------|
| 흑백(1도) | 국4절 | 53/53 | 53/53 |
| 흑백(1도) | 3절 | 53/53 | 53/53 |
| 칼라(4도/CMYK) | 국4절 | 53/53 | 53/53 |
| 칼라(4도/CMYK) | 3절 | 53/53 | 53/53 |
| 별색(5색) | 국4절 | 265/265 | 265/265 |

- 흑백(1도) 총 **212/212** · 칼라(4도) **212/212** · 별색 **530/530** (5색×국4절×단/양면×53수량밴드)
- 별색 5색 = 화이트·클리어·핑크·금색·은색 (각 106셀 = 국4절 × 단/양면 × 53밴드)

## 정상빈칸 vs 진짜 미적재 (false-positive 가드)

- **3절 별색 = 정상빈칸**: 권위 B02(3절)에는 별색 단가축 자체가 없음(흑백+칼라만). 라이브에도 없음 → 결함 아님. 파서가 권위 빈칸 셀을 격자에서 배제하므로 미적재로 오집계되지 않음.
- 권위에 단가가 있는 모든 (판형×도수×면×수량) 셀이 라이브에 존재 → 진짜 미적재 0.

## ★이전 "흑백 dim_missing" 신호 = 가짜(파싱한계) 정정

직전 결함보드(`digital-print-defects.csv`)는 `dim_missing clr=흑백(212셀)`을 보고했으나 **가짜 신호**였다:

- 흑백 1도는 8세션에 신규 print_opt 코드 **POPT_000008(단면1도)·POPT_000009(양면1도)** 로 적재됨(색수 CLR_000002=1도 구분). 활성 comp `COMP_PRINT_DIGITAL_S1` 안에 흑백 212셀 모두 존재.
- 그러나 공유 `grid_diff.live_grid_digital` 의 면(side) 코드맵이 POPT_000001/000002 만 알아서 신규 코드 셀을 통째로 스킵 → 흑백 누락으로 오판.
- **교정**(`grid_diff.py`): 면을 note(단면/양면) 1차 권위로 도출(+POPT_000008/009 코드맵 보강). 재실행 시 흑백 212셀 인식 → dim_missing 소멸, direct_hit 742→954.

## 라이브 구조 메모 (감사 근거)

- **흑백/칼라**: `COMP_PRINT_DIGITAL_S1`(활성). 면+도수가 print_opt_cd 로 인코딩 — POPT_000001 칼라단면 · POPT_000002 칼라양면 · POPT_000008 흑백단면 · POPT_000009 흑백양면. 도수 구분은 print_opt 의 색수(CLR_000002=1도/흑백, CLR_000005=풀컬러). use_dims=[proc_cd, plt_siz_cd, print_opt_cd, min_qty, proc_grp] → 도수축 살아있음(collapse 아님).
- **별색**: `COMP_PRINT_SPOT_WHITE_S1`(활성·통합 comp). 5색을 proc_cd(PROC_000008~012)로 구분. 530셀.
- **폐기분(제외 정당성)**: `COMP_PRINT_DIGITAL_S2`·`COMP_PRINT_SPOT_*_S2`·기타 색상 단독 S1 = del_yn=Y. 신규 통합 comp 로 대체됐고, formula_components 바인딩(PRF_DGP_A~F/INNER)은 전부 `COMP_PRINT_DIGITAL_S1`·`COMP_PRINT_SPOT_WHITE_S1` 만 참조 → 어떤 상품도 폐기 comp 를 쓰지 않음. 비활성 제외가 정합.

## 돈영향

없음 — 미적재/불일치 0이므로 저청구·과청구·견적불가 모두 0. 디지털인쇄비 시트는 §26 무결성 기준 **종결(GO)**.
