# 디지털인쇄 — 라이브 정정 실무진 정리표 (manifest-worksheet)

> **작성** 2026-06-14 · round-13 라이브 정정 트랙 (260610 L1 기준).
> **정답 권위:** 원본 엑셀 L1 최신 `24_master-extract-260610/digital-print-l1.csv` (E1~E5 독립검증 GO·무손실).
> **이 표는 비전문가 실무진용** — 코드값은 전부 한글 병기, 영문 분류어("MIS-LOADED" 등) 사용 안 함.
> **라이브 실측 갱신(2026-06-14):** 기존 `17_correctness` manifest의 일부 값이 stale → 직접 psql 재측정으로 확정. 주요 정정 = 엽서 봉투(1행→5행 이미 적재됨, 아래 W-04).

---

## 1. 처리구분 3분류 (정직하게)

| 처리구분 | 뜻 | 본 시트 건수 |
|----------|----|:--:|
| **즉시적용** | 사람 확인 없이 단순 UPDATE/INSERT로 바로 고칠 수 있는 것 | 4 |
| **신규적재** | 연결행을 새로 만들어야 하나, 형상 저장처·세트 모델 결정이 얽혀 설계가 필요한 것 | 5 |
| **컨펌대기** | 사람이 결정해야 하는 것 (질문 명시) | 5 |
| **고칠 것 없음** | 라이브가 이미 정답(정정 불요) | 6 |

---

## 2. 정리표

| ID | 상품명 | 무엇이 틀렸나 (쉬운 한국어) | 어떻게 고치나 | 영향 범위 | 처리구분 | 근거 |
|----|--------|---------------------------|---------------|-----------|----------|------|
| **W-09a** | 인쇄배경지(OPP봉투타입) · 인쇄배경지(투명케이스타입) · 인쇄헤더택 | 상품이 가짜 카테고리 "배경지"(어디에도 안 붙은 떠 있는 칸)에 묶여 있음. 진짜 자리인 "포장 > 인쇄배경지/헤더택"은 이미 만들어져 비어 있는데도 안 씀 | 진짜 포장 자리에 새로 연결하고, 가짜 "배경지" 칸 연결은 끔(논리정리) — 배경지OPP→"인쇄배경지(OPP봉투타입)" / 케이스→"인쇄배경지(투명케이스타입)" / 헤더택→"인쇄헤더택" | 상품 3개 카테고리 연결 | **즉시적용** | 엑셀 포장코드(012) + 라이브 진짜 노드 실재 |
| **W-14** | 라벨/택 | 위와 같이 가짜 "배경지" 칸에 묶임. 진짜 자리 "포장 > 라벨/포장스티커"는 비어 있음 | "라벨/포장스티커" 자리에 새로 연결 + 가짜 칸 연결 끔 | 상품 1개 카테고리 연결 | **즉시적용** | 엑셀 포장코드 + 라이브 진짜 노드 실재 |
| **W-05** | 스탠다드 쿠폰/상품권 · 프리미엄 쿠폰/상품권 | 가짜 카테고리 "상품권"(떠 있는 칸)에 묶임. 어느 판매 분류 밑에 둘지 미정 | 적정 상위 분류 결정 후 연결 | 상품 2개 카테고리 연결 | **컨펌대기** | 라이브 고아 노드 + 정상 상위 노드 미확정 |
| **W-07** | 인쇄배경지(OPP봉투타입) | 이 상품의 핵심인 "전용 커팅 모양"(기본형/타공형/핀고정형/북마크/스마트톡형/카드고정형/키링형/폰스트랩형 등)이 하나도 안 들어감 | "완칼(커팅)" 공정을 연결. 단 모양별 세부값을 담을 칸이 DB에 아직 없음 | 상품 1개 공정 | **신규적재** | 엑셀 커팅 칸 다수 + 라이브 공정 0행 |
| **W-10** | 인쇄배경지(투명케이스타입) | "접지(기본형/상하접지형)" 공정이 안 들어감 | "접지" 공정을 연결 | 상품 1개 공정 | **신규적재** | 엑셀 접지 칸 + 라이브 공정 0행 |
| **W-13** | 라벨/택 | "형상 커팅"(사각/라운딩/삼각/팔각/원형/사각리본/삼각리본/리본)이 안 들어감 | "완칼(커팅)" 공정 연결 (W-07과 같은 모양값 칸 문제) | 상품 1개 공정 | **신규적재** | 엑셀 형상 칸 다수 + 라이브 공정 0행 |
| **W-08** | 인쇄배경지(OPP봉투타입) | 이 상품은 "배경지 카드 + 사이즈 맞춘 봉투" 한 세트로 파는데, 봉투 세트가 전혀 안 들어감 | 봉투를 세트(또는 추가상품)로 연결. 단 세트로 묶을지/추가상품으로 묶을지 결정 필요 | 상품 1개 세트/추가상품 | **컨펌대기** | 사이트(세트 판매) + 라이브 세트 0행 |
| **W-11** | 인쇄배경지(투명케이스타입) | "배경지 + PP투명케이스" 세트인데 케이스 세트가 안 들어감. 게다가 PP투명케이스 상품 자체가 DB에 없음 | 케이스 상품을 먼저 만들고 세트 연결 (W-08과 같은 모델 결정) | 상품 1개 + 케이스 상품 신설 | **컨펌대기** | 사이트 + 라이브 케이스 template 0건 |
| **W-06** | 프리미엄 쿠폰/상품권 (+박 쓰는 전체 8상품) | "박" 공정에서 박 색깔 8종(홀로그램/금유광/은유광/먹유광/동박/적박/청박/트윙클)만 붙고, 박 본체 공정은 안 붙음. 박 쓰는 모든 상품이 같은 모양 | 박 색깔을 선택옵션으로 둘지 / 박 본체 공정을 따로 붙일지 결정 (전 상품 공통 정책) | 박 쓰는 8상품 공정 | **컨펌대기** | 라이브 전역 일관 패턴 (042만 아님) |
| **W-17** | 프리미엄엽서 등 (봉투 연결 표기 방식) | 봉투 연결 코드가 하이픈("TMPL-000005")으로 들어감. 사내 코드 규칙은 밑줄("_")로 통일 | 하이픈 유지 vs 밑줄로 통일 결정 | 봉투 연결 표기 전반 | **컨펌대기** | 사내 코드전략 충돌 |
| **W-04** | 프리미엄엽서 | (정정) 기존 분석은 "봉투 1종만 들어감"이라 했으나, **라이브 실측 결과 5종이 이미 들어가 있음**(OPP접착·OPP비접착·트레싱지·카드봉투화이트·카드봉투블랙) | 고칠 것 없음 (이미 정답) | — | **고칠 것 없음** | 라이브 실측 5행 |
| **W-01** | 프리미엄엽서 | 사이즈 7종 — 정답과 일치 (기존 "13종" 주장이 오류였음) | 고칠 것 없음 | — | **고칠 것 없음** | 엑셀 7행 = 라이브 7행 |
| **W-02** | 프리미엄엽서 | 공정 6종(모서리/오시/미싱/가변) — 엑셀에 별색·코팅·커팅이 없으므로 정답 | 고칠 것 없음 | — | **고칠 것 없음** | 엑셀 빈값 정합 |
| **W-03** | 프리미엄엽서 | 종이 용도가 "공통"으로 들어감 — 디지털인쇄 종이는 용도 칸이 비어 정상적으로 "공통" 처리된 것 | 고칠 것 없음 | — | **고칠 것 없음** | 적재규칙 정당 |
| **W-12** | 라벨/택 | 사이즈 3종 — 엑셀과 일치 | 고칠 것 없음 | — | **고칠 것 없음** | 엑셀 3행 = 라이브 3행 |
| **W-15/16** | 전 디지털 상품 | 수량단위("매")·출력용지("국전계열") 값 자체는 정답 | 고칠 것 없음 (값 정답) | — | **고칠 것 없음** | 값 정답 |
| **W-18** | 인쇄배경지(OPP봉투타입) | 종이가 스노우지250 1종만 들어감. 엑셀에 몽블랑240 등 추가 종이가 있는지 재확인 필요 | 엑셀 종이 행 재확인 후 누락분 연결 | 상품 1개 자재 | **신규적재** | 엑셀 종이 일부만 |

---

## 3. 처리구분 분포

| 처리구분 | 건수 | ID |
|----------|:--:|------|
| **즉시적용** | 4 | W-09a(상품 3개 묶음)·W-14 |
| **신규적재** | 5 | W-07·W-10·W-13·W-18 (+형상값 칸 부재로 부분) |
| **컨펌대기** | 5 | W-05·W-06·W-08·W-11·W-17 |
| **고칠 것 없음** | 6 | W-01·02·03·04·12·15/16 |

> **즉시적용 4건의 실체:** 카테고리 재연결 = 배경지OPP(043)·케이스(044)·헤더택(045) 3상품 + 라벨택(046) 1상품 = 4상품. 진짜 포장 노드(273/274/275/283)가 이미 비어 실재 → 새 연결행 INSERT + 가짜 고아 296 연결 논리정리(비파괴).

---

## 4. 6대 기준 자가 점검표

| # | 사용자 기준 | 본 산출 충족 방식 |
|---|-------------|-------------------|
| 1 | 코드 숨긴 실무진 정리표 | 본 worksheet — 코드값 전부 한글 병기, 영문 분류어 0, 처리구분 한국어 3분류 |
| 2 | 멱등·안전 기술 적재본 | `apply.sql` — BEGIN…COMMIT(인간 승인 후), WHERE 현재값 조건/ON CONFLICT, 재실행 0행 |
| 3 | 롤백 | `rollback.sql` — 적용 전 원문 복원 |
| 4 | 정규화·오염 방지 | `normalization-guard.md` — 3중 화이트리스트(table-spec 도메인·webadmin FK·260610 L1)·화이트리스트 밖 값 0 |
| 5 | CPQ option/template/constraint 반영 | `cpq-plan.md` — 봉투 세트·박색 옵션·형상 커팅을 option_groups/templates/constraints로 설계 |
| 6 | 타임스탬프·변경이력 추적 | `apply.sql` — INSERT=reg_dt=now()·UPDATE=upd_dt=now()·note(있는 테이블)에 "정정 2026-06-14: X→Y" |

---

## 5. 미해소 컨펌 (인간 결정 필요)

- **Q-ID-A** — 배경지/케이스 봉투 세트를 (a)`t_prd_product_sets` (b)`t_prd_product_addons` (c)CPQ 옵션 중 어디로? (사이트=세트 판매 → a/c 부합, 결정 필요)
- **Q-ID-B** — 배경지/헤더택/라벨택 카테고리: 정상 노드 재연결(권장·search-before-mint) vs 고아 296 부모만 012로 UPDATE. 상품권(041/042) 정상 상위 노드 미확정.
- **Q-DP-C** — 박색 8종 = CPQ 선택옵션인가, 박 본체 공정 별도 추가인가? (라이브 전역 일관 패턴 — 8상품 전부 부모 없이 자식 8종)
- **Q-DP-B** — tmpl_cd separator 하이픈 유지 vs `_` 통일.
- **Q-DP-D [신규]** — 형상 커팅 세부값(기본형/타공형/사각/원형…)을 담을 칸(`prcs_dtl_opt` 류)이 라이브에 부재. CPQ option_items 경유 vs 신규 테이블(ddl) — 형상값 적재처 결정.
