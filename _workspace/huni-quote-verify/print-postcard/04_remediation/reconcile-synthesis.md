# 프린트엽서 가격계산 검증 — Claude+Codex reconcile 종합

> Phase 3 (오케스트레이터/메인) · 2026-06-18 · huni-quote-verify
> 입력: `02_verify/verdict-claude.md`(Claude 라이브 실측) + `03_codex/reconciliation.md`(Codex gpt-5.5 독립)
> Codex 가용성: **AVAILABLE(gpt-5.5)** — preflight 통과·exit 0. 미가용 폴백 없음.

## 종합 판정: 🔴 NO-GO (양 모델 독립 합의)

**프린트엽서(엽서 group·앵커 PRD_000016 프리미엄엽서·공식 PRF_DGP_A 합산형)는 현재 라이브에서 자기 가격공식으로 올바른 가격계산이 안 된다.** Claude(라이브 evaluate_price 실호출)와 Codex(독립 2nd opinion)가 **서로 결과를 보지 않고** 둘 다 NO/NO-GO에 도달.

- Claude 실측: PC-2(100×150 칼라단면 200매) 인쇄비 권위 **20,000** vs 라이브 재계산 **78,000**(S1 26,000+S2 52,000 이중·도수 미반영·출력매수 미적용 복합).
- Codex 독립: 같은 케이스 "칼라 200매 인쇄비 11,250원 과소산출" + 도수축 드롭=의도 아닌 결함(권위 칼라열·골든·라이브 흑백값 3근거).
- 단가 "값"은 가격표 verbatim 정합(용지비 70.64 ✅) — **틀린 건 값이 아니라 산정 구조.**

## reconcile 매트릭스

| 결함 | Claude 실측 | Codex 독립 | 합의도 | 처리 |
|------|:--:|:--:|:--:|------|
| **F-1 도수축 붕괴**(칼라/별색 미반영·면축에 도수열 오매핑) | 🔴 재현 | 🔴 동의 | **강수렴(확정)** | arbiter |
| **F-5 유광코팅 단가행 0**(침묵 0원) | 🔴 재현 | 🔴 동의 | **강수렴(확정)** | arbiter |
| **F-2 인쇄비 S1/S2 이중배선**(2배) | 🔴 재현(78,000 실측) | 🟡 위험 플래그(신중) | **수렴(Claude 확정)** | arbiter |
| **F-3 출력매수 미적용**(×qty vs ×ceil(qty/판걸이)) | 🔴 재현 | (미포착) | Claude 단독 확정 | arbiter+개발자 |
| **F-4 plate↔work 사이즈 단절**(work 선택 시 no-match 0원) | 🔴 재현 | (미포착) | Claude 단독 확정 | arbiter+개발자 |
| **F-6 투명엽서 PRF 미바인딩** | 🔴 재현 | (미포착) | Claude 단독 | dbmap |

## ★ Codex 단독 발굴 (divergence·미검증 가설 — 라이브 재실측 전 채택 금지)

이중 모델의 핵심 가치 — Claude가 놓친 3건을 Codex가 제기:
- **CX-1 손지율 +5장 전파**: 손지율이 전 comp에 일괄 전파되면 오산 가능 → 엔진 코드 실측 필요.
- **CX-2 판걸이수 앱 룩업 ↔ 가격표 비동기**: 앱 계산 판수와 가격표 기준 불일치 가능 → 라이브 확인.
- **CX-3 별색 디지털인쇄비 + 별색인쇄비 중복**: work-spec은 "별색=정당 별도"인데 codex는 중복 적용 규칙 모호 지적 → 라이브 규칙 실측 + 권위 대조 필요.

→ 이 3건은 **검증가 재호출 또는 라이브 psql 직접 확인** 대상(컨펌큐). 환각 경계: 채택 전 라이브 검증.

## ★ work-spec 명제 정정 (검증이 분해를 교정)

분해가의 **D-COLOR "흑백만 적재"** → Claude 실측이 더 깊게 정정: **"면축(POPT 단/양면)에 흑백단면·칼라단면 두 열을 오매핑·도수축 자체 부재"**. PRD_000016은 칼라전용인데 단면에 흑백단가(350)가 실림 → 정답 칼라단면(800@25)이 라이브에 아예 없음. (생성≠검증의 가치 — 분해 단계 가설을 실측이 교정)

## 다음: Phase 4 개선/수정/보완

강수렴·확정 결함(F-1~F-5)을 dbm-price-arbiter 심의로 정립안 도출. F-3/F-4(출력매수·plate↔work 환산)는 엔진 런타임 계산이라 데이터 정립 + 개발자 백로그 병기. Codex 단독 3건은 라이브 재실측 후 편입.
