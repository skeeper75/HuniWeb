# 교정 명세 — 아크릴키링 고리 옵션 저청구 (실무진 값 확인용)

> 출처: hdx `--scope linkage` E1R 역방향 단서 → 근본원인 추적. 라이브 읽기전용 스냅샷 snap_20260704_1554.
> **★260704 갱신 — 엔진 실측 완료 + 라이브 정밀 대조로 진단 교정**(아래 "엔진 판정" 절).
> [HARD] 값 날조 금지 — 단가값은 라이브 실재·가격표 260527 권위와 일치. "어느 옵션 = 어느 값" 매핑은
> 현재 opt_cd 가 무관 상품에 재사용된 충돌 상태라 실무진/권위 확정 필요.
> 확정 후 연결 SQL(백업·게이트·undo·P4 재실측) 생성 → webadmin 실화면 확인 → 인간 승인 COMMIT.

## ★ 엔진 판정 (선행 실측 완료 — pricing.py verbatim)

**질문**(핸드오프 선행): 엔진이 opt_cd 단가를 **공식 배선 없이도** 옵션 경로로 가산하는가?
→ **답 = (b) 아니오. 공식 배선이 필수.**

근거(`raw/webadmin/webadmin/catalog/pricing.py`):
- `evaluate_price` → `_evaluate_formula(frm_cd,…)` 는 `TPrcFormulaComponents.filter(frm_cd=…)` 로
  **그 공식에 배선된 구성요소만** 순회·합산(pricing.py:665). 배선 안 된 고아 comp 는 절대 안 봄.
- `opt_cd` 는 NON_QTY_DIMS 매칭 차원(pricing.py:42·`_row_matches`) — **배선된 comp 내부**에서 단가행을 고를 뿐.
- ∴ opt_cd 재지정(구 1단계)만으로는 **영원히 저청구**. PRD_000146 이 `PRF_CLR_ACRYL`(base COMP_ACRYL_CLEAR3T
  1개만 배선)을 쓰는 한 고리 comp 는 합산 대상이 아님.

## 무엇이 끊겼나 (라이브 정밀 대조 — 구 진단 교정)

**아크릴키링(PRD_000146)** 은 공식으로 공용 `PRF_CLR_ACRYL`(투명 아크릴)을 씀 = **base(투명아크릴
인쇄가공비)만** 물림. 손님 고리 옵션이 가격에 전혀 안 닿아 **0원 청구(저청구)**. 원인 3중:

**① 상품이 잘못된 공식을 쓴다(핵심).** ★전용 공식 `PRF_ACRYL_KEYRING` 이 **이미 존재·완전 배선됨**
   (base + `COMP_ACRYL_KEYRING` + `COMP_ACRYL_KEYRING_BALLCHAIN`, use_yn=Y, note "본체 면적 + 고리
   가산(146 저청구 교정)", 06-27 15:36 생성). **그런데 어떤 상품도 이 공식을 안 씀** — PRD_000146 은
   같은 날 17:06 `PRF_CLR_ACRYL` 로 (재)연결되어 교정 공식이 고아가 됨(구 진단의 "comp 미배선 고아"는
   부정확 — 공식 배선은 있으나 **상품→공식 연결이 끊김**).

**② 단가행 opt_cd 가 무관 상품에 재사용됨(충돌).** `COMP_ACRYL_KEYRING` 단가행:
   - `OPV_000473`=0 · `OPV_000474`=300 (신형 코드·고리없음/은색구슬줄 값)
   - `OPV-000026`=1,100 · `OPV-000027`=1,200 (구형 코드) → **현재 이 두 코드는 카드봉투(PRD_000004)
     화이트/블랙 색상 옵션이 씀**. 손님 실제 고리 코드는 신규 `OPV_000624~627`(06-28 등록·아래 표).
   → 어느 단가행 opt_cd 도 손님 고리 옵션 코드와 안 맞음(공식만 고쳐도 opt_cd 매칭 실패로 여전히 0).

**③ option_items 0행.** PRD_000146 옵션→차원 환원 행이 아예 없음(옵션이 직접 opt_cd 로 매칭되는 구조).

**④ 볼체인 comp 부적합(review).** PRD_000146 옵션그룹은 "고리"(OPT_000157) 하나뿐(은색구슬줄 포함).
   `COMP_ACRYL_KEYRING_BALLCHAIN` 단가행 opt_cd(OPV_000475~483)는 **포토북(PRD_000100) 표지타입**
   그룹 코드 → 146 엔 매칭 안 됨(무해하나 의미상 잉여). 은색구슬줄은 고리 comp 가 이미 300원으로 커버.

## ⛔ 교정 방향 전면 수정 (260704 — webadmin 실화면이 진짜 버그 규명)

**★ 아래 "opt_cd 재키잉" SQL(sql/acryl-keyring-0*.sql)은 폐기(SUPERSEDED).** 라이브 시뮬레이터
(webadmin 백엔드) 실측이 **더 크고 다른 버그**를 드러냄:

- 라이브 sim-meta: `opt_groups=[]`, 고리는 **자재(mat_cd)로 모델링** — MAT_000051(은색고리)·
  MAT_000052(금색고리)·MAT_000456(군번줄)이 `product_materials` 에 base 투명아크릴과 **같은 USAGE.07**로 등록.
- 그런데 이 고리 자재들은 **전 시스템 단가행 0**. base comp COMP_ACRYL_CLEAR3T 단가행은 MAT_000042/043(투명)만.
- **결과**: 손님이 은색고리(MAT_000051) 선택 → base comp 매칭 실패 → **상품 전체 0원**(고리 +1100 누락 정도가
  아니라 전체가 0). 라이브 실측: 투명3mm=200,000 / 은색고리·금색고리=**0원**.
- ∴ opt_cd 재지정으론 못 고침(라이브는 mat_cd 로 선택). **근본원인 = 고리를 base 자재로 오모델링.**

**올바른 교정(확정·기존 설계 정합) = 고리를 addon 템플릿으로**(같은 상품의 볼체인이 이미 그 패턴):
1. 고리 addon 템플릿 추가: 은색고리(1,100)·금색고리(1,200)·군번줄=은색구슬줄(300)·고리없음(0) —
   볼체인 TMPL-000056~063(칼라 8색·1,000·라이브 sim 검증 +1,000 반영 확인) 미러. 삭제된
   TMPL-000015(은색고리)/016(금색고리)/018(볼체인)는 복원 후보(del_yn=Y).
2. 오모델링 자재 제거: PRD_000146 `product_materials` 에서 MAT_000051/052/456 삭제(base 대체 방지).
3. 검증: 라이브 sim 에서 투명3mm base 불변 + 고리 addon 선택 시 grand_total += 1,100/1,200/300.

**교훈**: [HARD] webadmin 실화면 확인이 잘못된 COMMIT을 막았다(값은 맞았으나 선택 메커니즘 오판).
생성(내 SQL)≠검증(라이브 sim) 분리의 실증.

## (폐기됨) ~~검증 상태 (260704 — opt_cd 재키잉 접근)~~

교정 SQL 후보 4종 생성·검증 완료. **남은 게이트 = webadmin 실화면 + 인간 승인 COMMIT**(자동 COMMIT 금지).

| 게이트 | 결과 |
|---|---|
| 엔진 실측 | **(b) 확정** — 공식 배선 필수(opt_cd 단독 불충분·pricing.py:665) |
| 라이브 드리프트 재-SELECT | **0** — 146=PRF_CLR_ACRYL·키링 구코드4행(0/300/1100/1200)·신규코드 단가행0·KEYRING공식 사용상품0 |
| P4 적대적 재실측(engine verbatim) | **GO** — 은색고리 교정 전 0/장(no_match·저청구) → 후 1,100/장·값합2600 불변·무회귀(146 전용 공식) |
| webadmin 실화면 | ⏳ 인간 — 고리 4종 선택 시 PRICE 반영 확인 |
| 인간 승인 COMMIT | ⏳ 인간 |

**SQL 후보**(`remediate/sql/`): `acryl-keyring-01-backup.sql` · `-02-dryrun.sql`(ROLLBACK 검증) ·
`-03-fix.sql`(COMMIT·webadmin 후) · `-04-undo.sql`. 재실측 스크립트 `_acryl_keyring_p4_remeasure.py`(GO).
실행 순서: 01(백업) → 02(dryrun 게이트 통과 확인) → **webadmin 실화면** → 03(fix COMMIT) → 사후 재시뮬·스냅샷 재생성·재진단.

## 권위값 확정 — 가격표 260527 · 아크릴 시트 (60~63행)

권위 엑셀에서 확정(★날조 아님·셀 출처 명시). 값이 이미 라이브 단가행에도 존재(옛 코드에 묶임):

| 손님 옵션 | 현행 옵션코드 | **권위 단가** | 출처 | 라이브 단가행(옛 코드) |
|---|---|---|---|---|
| 고리없음 | OPV_000624 | **0원** | 가격표!아크릴 60행 | COMP_ACRYL_KEYRING · OPV_000473 = 0 ✓ |
| 은색구슬줄(군번줄) | OPV_000627 | **300원** | 가격표!아크릴 61행 | COMP_ACRYL_KEYRING · OPV_000474 = 300 ✓ |
| 은색고리 | OPV_000625 | **1,100원** | 가격표!아크릴 62행 | COMP_ACRYL_KEYRING · OPV-000026 = 1,100 ✓ |
| 금색고리 | OPV_000626 | **1,200원** | 가격표!아크릴 63행 | COMP_ACRYL_KEYRING · OPV-000027 = 1,200 ✓ |

※ 상품마스터 260610 아크릴 시트도 교차확인: 고리없음 0·볼체인 1,000(색상별)·은색고리/금색고리 "*가격표참고"
   → 가격표가 권위. 상품마스터 "볼체인 1,000"은 색상 볼체인(별건)이고, DB 옵션 "은색구슬줄"=군번줄=300원(가격표 61행).

**결론: 값은 이미 라이브에 정확히 존재. 저청구 원인 = 순수 연결 끊김**(손님 옵션코드 ↔ 단가행 옛 코드 불일치).
교정 = 값 적재가 아니라 **재연결**(opt_cd 재지정) + 부품 comp 배선.

## 재연결 매핑 (권위 확정·기계적)

단가행 opt_cd 를 옛 코드 → 현행 손님 옵션코드로 재지정(값 불변·순수 연결):

| COMP_ACRYL_KEYRING 단가행 | 옛 opt_cd | 단가 | → 현행 opt_cd | 손님 옵션 |
|---|---|---|---|---|
| (행1) | OPV_000473 | 0 | OPV_000624 | 고리없음 |
| (행2) | OPV_000474 | 300 | OPV_000627 | 은색구슬줄 |
| (행3) | OPV-000026 | 1,100 | OPV_000625 | 은색고리 |
| (행4) | OPV-000027 | 1,200 | OPV_000626 | 금색고리 |

## 교정 방식 (엔진 판정 (b) 반영 · 3단계 · 신규 mint 0)

엔진 실측으로 (b) 확정 → 교정은 **① 공식 재연결 + ② 단가행 opt_cd 재키잉** 2축(신규 공식/단가 mint 0):

**1단계 — 상품→공식 재연결(구조·값 무관·auto_data 성격).**
   `t_prd_product_price_formulas`: PRD_000146 `frm_cd` = `PRF_CLR_ACRYL` → **`PRF_ACRYL_KEYRING`**
   (이미 완전 배선된 전용 공식 재사용·search-before-mint 충족). 단독으로는 아직 0(opt_cd 불일치·2단계 필요).

**2단계 — 단가행 opt_cd 재키잉(값 불변·권위 매핑).**
   `COMP_ACRYL_KEYRING` 단가행 4개의 opt_cd 를 손님 현행 코드로(아래 매핑표). 값(0/300/1100/1200) 그대로.
   충돌 해소 = 카드봉투(PRD_000004)가 쓰는 OPV-000026/027 에서 키링 단가행을 떼어내 고리 코드로 옮김.
   가격중립 **아님이 정상**(0 → 정상 청구 복원). ⚠ 권위 매핑이라 실무진 확정 게이트 필요(값 자체는 확정).

**3단계(review) — 볼체인 comp 처리.** `PRF_ACRYL_KEYRING` 에 배선된 `COMP_ACRYL_KEYRING_BALLCHAIN`
   은 146 옵션그룹에 대응 안 됨(무해 0). 은색구슬줄은 고리 comp 300원으로 커버되므로 **이 상품 공식에서
   볼체인 comp 제외 검토** 또는 그대로 둠(잉여). 실무진/설계 판단.

★ [HARD] SQL = auto_data(1단계 구조 재연결)만 즉시 P4 재실측 후보. 2단계(opt_cd 권위 매핑)는
  needs_authority(실무진 확정) → 확정 후 백업·게이트·undo·P4 재실측(은색고리 +1,100 반영 확인) →
  webadmin 실화면(A/B/C 사이즈·고리 4종 PRICE 반영) → 인간 승인 COMMIT.

## 동종 스캔 결론 (같은 단절 다른 상품에 있나)

전 상품 양방향 스캔 결과 — **이 패턴은 격리됨**(널리 안 퍼짐):
- **PET배너**: 거치대가 전용 comp('PET배너 거치대 추가가격' 10,000/23,000원) + option_items 5행으로 **정상 청구**.
- **프리미엄엽서**: 오시·미싱·용지·별색 등 전 옵션이 option_items 41행으로 차원 환원 → **정상 청구**.
- 그 외 아크릴 9상품(맥세이프·포카키링·코스터 등): 부품옵션 없음 또는 연결 정상.

→ opt_cd 는 배선된 comp 내부의 매칭 차원(엔진 확정). 진짜 저청구 신호 = **부품성 옵션인데 (상품 공식이
   부품 comp 를 배선한 공식이 아님) AND (option_items 차원환원 0) AND (부품 단가행 opt_cd 가 손님 코드와
   불일치)**. 이 3중 조건 동시충족 = 전 상품 중 **아크릴키링(PRD_000146)만 해당**(패턴 격리 확인).
