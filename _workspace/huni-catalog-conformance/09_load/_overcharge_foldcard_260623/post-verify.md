# post-verify.md — §21 접지카드 교정 사후검증 (COMMIT 후·별도 연결)

> 2026-06-23 · COMMIT 직후 별도 read-only 연결로 영속성·돈영향·멱등 비준. 전 항목 PASS.

---

## 1. 영속성 — 별도 연결 재실측 (트랜잭션 로컬 아님 증명)

| 검증 | 기대 | 실측 | 통과 |
|------|------|------|:--:|
| PROC_000106 영속 | 4단대문접지·upr=PROC_000056·seq19·Y·N | 동일 | ✅ |
| PROC_000107 영속 | 반접지·upr=PROC_000056·seq20·Y·N | 동일 | ✅ |
| 4 comp proc_cd 충전 | 060/071/106/107 | 동일 | ✅ |
| 4 use_dims 토큰 | `["proc_cd","min_qty","proc_grp:PROC_000056"]` ×4 | 동일 | ✅ |

## 2. verbatim — 단가값 불변

| comp | sum_price(기준선) | 영속 실측 | 통과 |
|------|:--:|:--:|:--:|
| 3FOLD | 31,965.00 | 31,965.00 | ✅ |
| 4ACC | 41,110.00 | 41,110.00 | ✅ |
| 4GATE | 41,110.00 | 41,110.00 | ✅ |
| HALF | 24,421.00 | 24,421.00 | ✅ |
| **총계** | **138,606.00** | **138,606.00** | ✅ |

→ 단가값 한 글자도 안 바뀜. 교정 = proc_cd·use_dims 분화만.

## 3. 돈 크리티컬 — P8-1 택일 25,000→6,000 영속 재현

| 시나리오 | 접지비 | 판정 |
|----------|:--:|:--:|
| BEFORE 의미(4개 silent 합산) | **25,000** | 과대청구 재현 |
| 택일 3단접지(PROC_000060) | **6,000** | ✅ 25,000→6,000 분리 입증 |
| 택일 4단대문접지(PROC_000106) | 7,000 | ✅ 택일 단일 |
| 택일 반접지(PROC_000107) | 5,000 | ✅ 택일 단일 |
| 택일 4단병풍접지(PROC_000071) | 7,000 | ✅ 택일 단일 |

→ 접지방식 1개 선택 시 그 1개 단가만 청구(이중계상 0). 손님 과대청구 +18,000~20,000/장 해소.

## 4. 멱등성 — apply.sql 재실행 (COMMIT 후)

```
INSERT 0 0 / INSERT 0 0  (마스터 ON CONFLICT no-op)
UPDATE 0 ×8              (proc_cd·use_dims IS DISTINCT 가드 no-op)
```
→ 재실행 = 완전 no-op. R1 멱등 PASS (COMMIT 전·후 양쪽 입증).

## 5. undo 가역성 — DRY-RUN 검증 (미실행)

`undo.sql` 기본 ROLLBACK 실행 시 UPDATE 192(단가행 NULL)·UPDATE 4(use_dims 원복)·UPDATE 2(신규코드 논리삭제)
문법·효과 정상. ROLLBACK으로 라이브 무변경(proc_cd 충전 유지) 확인. 실 undo 시만 COMMIT 분기.

## 6. 종합 판정

**GO — COMMIT 성공·전 사후검증 PASS.** 25,000→6,000 돈영향 해소·verbatim 불변·멱등·가역.
잔여: PRD_000028 옵션그룹 0행(접지방식 선택 UI 부재) = 별 CPQ 트랙(dbm-cpq-option-mapping).
가격 데이터(이 교정)는 완료 — 028 위젯 UI 노출만 후속.
