# 개발 수정 요청서 — `fn_calc_pansu` 판걸이수 산출 함수

작성 2026-07-01 · 대상=개발팀(HuniProductPrice2 레포 + Railway 라이브 DB) · 라이브 읽기전용 실측 재현. 실 함수 교체=개발팀 배포·인간 승인.

---

## 0. 한눈 요약 (비전문가)

디지털인쇄는 큰 종이(전지) 1장에 작은 제품을 여러 개 앉혀 한 번에 찍습니다. "한 장에 몇 개 앉느냐"가 **판걸이수**이고, 이 숫자로 필요한 전지 매수가 정해져 **인쇄비·용지비**가 매겨집니다. 지금 가격엔진의 `fn_calc_pansu`는 **종이 면적만 보고 기하학적으로 꽉 채워** 계산해서, 실제 인쇄의 칼선 간격·물림변·자재별 여백을 빼먹습니다. 그 결과 실무진이 엑셀에 손으로 적은 판걸이수와 어긋나 **가격이 양방향(저/과청구)으로 틀립니다(±7~80% 실측)**. 일부 큰 제품(썬캡)은 아예 0이 나와 **견적 자체가 불가**합니다. 실무진 판걸이수가 정답이므로, 엔진이 그 값을 먼저 쓰도록 고쳐 주세요.

---

## 1. 현재 함수 (고칠 대상)

`raw/webadmin/sql/32_fn_calc_pansu.sql` (라이브 동일):

```sql
-- 판 실제영역 = 판형 작업사이즈(work) − 여백
pw := plate.work_width  - margin_lft - margin_rgt;
ph := plate.work_height - margin_top - margin_bot;
-- 아이템 = 작업사이즈(여분포함)
iw := item.work_width;  ih := item.work_height;
IF (치수 NULL/0) THEN RETURN NULL;   -- 계산불가
n_normal := floor(pw/iw) * floor(ph/ih);   -- 정상 배치
n_rotate := floor(pw/ih) * floor(ph/iw);   -- 90° 회전
RETURN GREATEST(n_normal, n_rotate);       -- 더 많이 앉는 쪽
```
- 입력: `(판형 siz_cd, 아이템 siz_cd)` 2개. 시그니처에 **상품·자재 정보 없음**.
- 호출부: `pricing.py:287 _calc_pansu()`, `:215 plate_qty()`(판수=⌈주문수량÷판걸이수⌉), `price_views.py:1495`.

## 2. 한계 (왜 틀리나)

| # | 한계 | 결과 |
|---|---|---|
| L1 | **칼선 간격(gutter) 미반영** — 제품 사이 여백 0 가정 | 한 행/열 더 앉는 것으로 착각 → 판걸이수 과다 → 저청구 |
| L2 | **물림변(gripper) 미반영** — 기계가 무는 변 여백 미차감 | 동상 |
| L3 | **자재별 임포지션 차이 미반영** — 투명PET 등은 더 넓게 앉힘 | 투명엽서 전 사이즈 +43~50% 저청구 |
| L4 | **출력용지규격(전지) 매칭 미반영** — 큰 제품에 맞는 전지 선택 로직 없음 | 썬캡(작업320×404)은 등록 전지에 기하학적으로 0개 → **fn_calc_pansu=0 → 견적불가** |

핵심: **순수 기하 패킹(면적÷면적)** 이라 실무 인쇄의 물리 제약을 못 담습니다. 방향이 양쪽(과다=저청구·과소=과청구)으로 갈립니다.

## 3. 권위 (정답) — ★두 소스가 충돌, 단일화 선결

판걸이수 권위가 **두 곳에 따로** 있고 서로 다릅니다(2026-07-01 전수 재검증으로 확인):
- **(A) 인쇄상품가격표 "판걸이수" 시트**(94행): 재단·블리드·**작업사이즈**·판걸이·출력용지규격·인쇄가능영역을 모두 가진 전용 imposition 시트. **단 자재(투명 등) 구분 없음**(사이즈/카테고리 단위).
- **(B) 상품마스터 디지털인쇄 "판수" 컬럼**(27 상품×사이즈): 상품(자재)별 값. 투명엽서 등 자재 임포지션 반영.
- **충돌 실측**: 73×98 = A 18 / B 15 · 투명엽서 98×98 = A 12(generic) / B 8(투명자재) · 2단접지카드 110×170 = A 4 / B 8 · 스탠다드 148×210 = A 4 / B 6. → ★어느 게 production-truth인지 **실무진/예전사이트(goods.asp) 교차로 확정**해야 t_siz_pansu 적재 가능. (이전 ASP 실청구가 tiebreaker)

## 4. 재현 비교 — 현재 엔진 vs 권위 (qty=120·단면·기본자재·라이브 실측 2026-07-01)

> 엔진 판걸이수 = `fn_calc_pansu(판형, 사이즈)` 라이브 직접값. 교정가 = (현재 판형기준 subtotal ÷ 엔진 전지매수) × 권위 전지매수 — 전지당 단가 verbatim, 매수만 권위로 환산.

| 상품 | 사이즈 | 엔진판수 | 권위판수 | 현재가 | 교정가 | 갭 | % | 방향 |
|---|---|---|---|---|---|---|---|---|
| 투명엽서 | 98×98 | 12 | **8** | 21,000 | 31,500 | +10,500 | +50% | ▲저청구 |
| 투명엽서 | 100×150 | 9 | **6** | 29,400 | 42,000 | +12,600 | +43% | ▲저청구 |
| 투명엽서 | 135×135 | 6 | **4** | 39,000 | 58,500 | +19,500 | +50% | ▲저청구 |
| 프리미엄/코팅/스탠다드엽서 | 73×98 | 18 | **15** | 9,594 | 10,965 | +1,371 | +14% | ▲저청구 |
| 프리미엄/스탠다드엽서 | 100×150 | 9 | **8** | 14,989 | 16,060 | +1,071 | +7% | ▲저청구 |
| 스탠다드엽서 | 148×210 | 4 | **6** | 24,619 | 16,413 | −8,206 | −33% | ▼과청구 |
| 2단접지카드 | 100×150 | 9 | **8** | 23,389 | 25,060 | +1,671 | +7% | ▲저청구 |
| 2단접지카드 | 135×135 | 6 | **8** | 35,413 | 26,560 | −8,853 | −25% | ▼과청구 |
| 2단접지카드 | 110×170 | 4 | **8** | 47,119 | 23,559 | −23,560 | −50% | ▼과청구 |
| 2단접지카드 | 170×110 | 2 | **10** | 64,238 | 12,847 | −51,391 | −80% | ▼과청구 |

**전수 27케이스 중 15건 불일치(저청구 11·과청구 4·일치 12).** 양방향 결함 확인. 투명(자재종속)·접지카드(큰 사이즈)에서 최대.
일치 사이즈(98×98·135×135·95×210·110×170 등)는 우연히 기하=실무 일치.
> ★주의: 위 §4 표는 권위로 **상품마스터(B)** 만 썼다. 아래 §4b 재검증 결과 일부 케이스는 두 권위가 충돌하므로 **확정 결함 vs 충돌(미확정)** 로 재분류해야 한다(B 단독 신뢰 금지).

## 4b. 전수 재검증 — 여백 적용 + 두 권위 대조 (2026-07-01·★중요 보정)

사용자 지적("여백 모두 적용 상태인지 전수확인")에 따라 라이브 work치수·여백을 전수 점검하고 엔진 기하 vs **두 권위 모두** 대조. 데이터=`pansu-repro-bothauth-260701.csv`.

**① 여백(작업사이즈) 적용 상태** — 라이브 `work` vs 판걸이수 시트 `작업사이즈`:
- 엽서·명함·쿠폰·헤더택·라벨택·인쇄배경지: 라이브 work = 시트 작업사이즈 ✓(블리드 정확 적용·예 73×98 cut→work 75×100).
- **하드커버 내지·소량전단지·레더 내지: 블리드 미적용**(work=재단, 예 148×210 work=148×210 vs 시트 작업 150×212). 단 큰 사이즈라 floor 결과 동일(현재 무해)·정합상 보정 권장.

**② 기하공식 비재현(작업사이즈+여백 적용해도 안 맞음)** — 전수 입증:
- 판걸이수 시트 판걸이 vs 순수 기하(작업사이즈,인쇄가능영역): 77건 중 **18건 불일치, 양방향**. 100×150: 기하 9 vs 시트 8(칼선간격↓)·투명케이스 74×74: 기하 15 vs 시트 24(중첩배치↑). → **단순 floor 나눗셈으로 판걸이수 재현 불가**(룩업 테이블 필수의 결정적 근거).

**③ 확정 vs 충돌 재분류**:
- **확정 결함(양 권위 합치)**: 100×150 = 엔진 9 vs **시트 8 = 마스터 8** → 전 엽서·접지카드 공통 저청구(+7%). 인쇄배경지 86×120/76×120 엔진 10 vs 시트 9.
- **자재종속(마스터 채택)**: 투명엽서 — 마스터(8/6/4) < 시트 generic(12/8/6)=엔진. PET 임포지션 → 마스터가 옳을 개연 → +43~50% 저청구.
- **★권위 충돌(미확정·실무진/goods.asp 확정 필요)**: 73×98(시트 18=엔진 vs 마스터 15)·2단접지 110×170(시트 4=엔진 vs 마스터 8)·135×135(시트 6 vs 마스터 8)·스탠다드 148×210(시트 4 vs 마스터 6). 시트가 엔진과 일치하는 경우가 많아 **마스터 단독으로 결함 단정 불가**.
- 라벨택 40×80(엔진 30 vs 시트 24)·투명케이스(엔진 < 시트) = 시트 단독 기준 불일치.

## 5. 극단 사례 — 썬캡 (PRD_000051·견적 0)

- 권위(판걸이수 시트): 썬캡 출력용지규격 **3절 330×540**, **판걸이수 1**, 재단 320×404.
- 현재: 판형이 국4절(SIZ_000499 316×467)로 바인딩 → 작업 320×404가 국4절 재단영역(306×457)보다 커서 `fn_calc_pansu=0` → 인쇄·용지·커팅 전부 합산 제외 → **견적 0(불가)**.
- L4(출력용지규격 매칭 미반영)의 극단: 기하계산이 오차가 아니라 **0**이 되어 상품이 안 팔림.

## 6. 제안 수정

### 6-1. lookup 우선 (핵심)
권위 판걸이수 저장소를 먼저 조회, 없을 때만 기존 기하 폴백:
```sql
SELECT pansu INTO v FROM t_siz_pansu WHERE ...키...;
IF v IS NOT NULL THEN RETURN v; END IF;
-- (폴백) 기존 기하 계산 — 권위 미등록 사이즈 회귀 방지
... RETURN GREATEST(n_normal, n_rotate);
```
폴백 유지 → 권위 미등록분은 기존 동작 보존(회귀 격리).

### 6-2. ★키 설계 — 자재/상품 종속 주의 (설계 결정 필요)
투명엽서(98×98)=판수 **8** vs 일반엽서(98×98)=**12** — **같은 (판형,사이즈)인데 자재(상품)별로 판수가 다름**. 따라서 `(plate, item)` 키만으로는 투명을 표현 못 함.
- **(권장) `(prd_cd, item_siz_cd) → pansu`** — 상품 단위. 호출부 `pricing.py`가 prd_cd 보유 → `fn_calc_pansu`에 prd_cd 인자 추가(`fn_calc_pansu(plate, item, prd_cd DEFAULT NULL)`) 또는 pricing.py에서 lookup 후 함수 폴백.
- (대안) `(plate, item, mat_typ)` — 자재 임포지션 군으로.
- 시그니처 변경 시 호출부(`pricing.py:287`·`price_views.py:1495,1888`)도 prd_cd 전달하도록 소폭 수정.

### 6-3. 적재 데이터 — ★권위 단일화 선결
- **충돌 해소 전 적재 금지**: §3·§4b의 두 소스 충돌(73×98·접지카드·투명·스탠다드)을 실무진/goods.asp로 확정한 뒤 단일 값으로 적재.
- 후보: 판걸이수 시트 94행(`auth-pansu-all-260701.json`은 상품마스터 27행 — B소스) + 판걸이수 시트(A소스). **A=사이즈/카테고리·자재무관 / B=상품(자재)별** → 권장 = **자재 구분이 필요한 상품(투명 등)은 B, 그 외는 A** 를 병합한 (prd_cd, siz_cd) 단일표.
- ★썬캡=3절 330×540·판수1 → 3절 전지 등록+썬캡 판형 재바인딩 동반 필요.

## 7. 검증 케이스 (적용 후 회귀)

| 케이스 | 기대 판수 | 기대 가격(qty120) | 분류 |
|---|---|---|---|
| 100×150 엽서/접지(전상품) | 9→**8** | +7% | ✅확정(시트=마스터 합치) |
| 투명엽서 100×150 | 9→**6**(마스터) | 29,400→42,000 | 자재종속(마스터 채택 시) |
| 썬캡 | 0→**1**(3절 바인딩+등록 후) | 0→정상 | ✅확정(견적불가 해소) |
| 73×98 엽서 | 18 vs 15 | — | ⚠충돌(시트18=엔진 vs 마스터15)·확정 후 |
| 2단접지 110×170·135×135 | 4/6 vs 8 | — | ⚠충돌·확정 후 |
| 스탠다드 148×210 | 4 vs 6 | — | ⚠충돌·확정 후 |
| 무회귀(98×98·135×135 등 합치분) | 변동 없음 | 변동 없음 | — |

검증 도구: `cd _workspace/_foundation/batch && python3 -c "import lib_huni as H; H.load_env(); sim=H.HuniSim(); print(sim.simulate('PRD_000019',{'siz_cd':'SIZ_000003','mat_cd':'MAT_000178','print_opt_cd':'POPT_000001'},120,procs=[{'proc_cd':'PROC_000004'}]))"` → `base.components[].pansu` 확인.

## 8. 라우팅·범위 (순서)
0. **★선결: 권위 단일화**(실무진/예전사이트 goods.asp 교차) — §3·§4b 충돌 케이스를 production-truth로 확정. 이게 안 되면 적재값을 못 정함.
1. **개발(C트랙)**: `fn_calc_pansu` lookup 우선 + 기하 폴백 + `t_siz_pansu` 신설 + 호출부 prd_cd 전달. 라이브 함수 교체=인간 승인 배포(레포 HuniProductPrice2 반영 동반·드리프트 주의).
2. **데이터(하네스·dbmap)**: 확정 권위 판수 (prd_cd,siz_cd) 적재(인간 승인) + 썬캡 3절 전지 등록·재바인딩.
3. **검증**: 시뮬레이터 재실측으로 판수=확정권위 일치(§7).
- 무날조: 본 문서 수치는 라이브 시뮬레이터·`fn_calc_pansu` 직접·두 권위 엑셀 실측 재현(추정 아님). ★전수 재검증으로 "마스터 단독=결함" 과진단을 확정/충돌로 보정함(2026-07-01).

---

## 8. 검증 추가 (2026-07-01 후속 · 작업사이즈 재확인 — 사용자 지적 반영)

**질의:** 상품 사이즈는 재단사이즈이고 판형에 들어가는 것은 작업사이즈다. 보고서가 진짜 작업사이즈로 계산했는가?

**확인 결과 (라이브 실측):**
- `fn_calc_pansu` 는 **작업사이즈(`work_width/work_height`)** 를 씁니다(판형=작업−여백, 아이템=작업). 재단(`cut_*`) 미사용 — 코드 확인.
- **정상 엽서**는 재단·작업이 **둘 다 정확히 등록**(작업=재단+2mm 여분): 73×98(재단73×98/작업75×100)·98×98(98×98/100×100)·100×150(100×150/102×152)·148×210(148×210/150×212). → **보고서의 엔진판수는 진짜 작업사이즈 기반이 맞습니다**(§4 비교표 유효).
- **★썬캡(051) 정밀 재진단:**
  - 아이템 `SIZ_000195` = 재단 **비어있음** / 작업 313×400 (impos_yn=N). ★재단/작업 구분 미입력(데이터 갭).
  - 판형 `SIZ_000499` = 작업 316×467, 여백 5mm 사방 → 인쇄영역 306×457.
  - `fn_calc_pansu(SIZ_000499, SIZ_000195)=0`: 아이템 폭 **313 > 인쇄영역 폭 306**(7mm 초과)라 정상·회전 모두 0.
  - **정답 판수 = 1**(대형 1-up 제품·1장에 1개). 여백 없으면 316/313×467/400=1×1=1. **여백 차감이 near-miss(7mm)로 0 을 만든 것** — 순수 기하 함수의 한계 실증(§2 L4 확증).
- **데이터 갭 별건(교정 후보):** 썬캡 재단 미입력 / 일부 사이즈(150×298·298×150) 작업 비어있음(→NULL→0) / SIZ_000523 이름"100×150"인데 재단 200×150(명칭≠치수).

**결론(썬캡):** 보고서 진단 유효. 썬캡 견적불가는 **기하 함수의 near-miss(여백 차감)** + 재단/작업 데이터 갭 복합. **정답 판수 1** 은 기하로 안 나오므로, ③의 권위 판걸이수 lookup(`t_siz_pansu`)에 썬캡=1 을 실어야 해소(데이터만으론 불가·공유 판형 SIZ_000499 변경은 타 상품 영향 위험). → **DEV 트랙 유지**(fn_calc_pansu lookup 우선 로직 + t_siz_pansu 적재). 데이터 갭(재단/작업 미입력)은 별도 §26 교정.

### 8.1 ★정정 (권위=판걸이수 시트 기준 — 앞 §8은 라이브 판형 오사용)

앞 §8은 라이브 등록 판형(`SIZ_000499` 316×467)으로 계산했으나, **권위는 판걸이수 시트**(`huni-dbmap/06_extract/pangeori-l1.csv` 76행)다:

| 사이즈옵션 | 재단 | 블리드 | 작업 | 판걸이 | 판형(3절·출력용지) | 여백 제외 |
|---|---|---|---|---|---|---|
| 썬캡 | 320×404 | 0 | **320×404** | **1** | **330×540** | 좌우 6mm·상 13mm·하 16mm |

- 라이브 데이터가 **권위와 불일치**: 썬캡 아이템=`SIZ_000195`(313×400) → 정답은 `SIZ_000056`(320×404·실재) / 판형=`SIZ_000499`(316×467) → 정답 330×540(3절).
- **권위값으로 fn_calc_pansu 수기 계산해도 = 0**: 인쇄영역 = 330−6−6 × 540−13−16 = **318×511**, 아이템 320×404 → floor(318/320)=0 → **0**. 아이템 폭 320 > 인쇄영역 318(**2mm 초과**). 권위 판걸이=**1**.
- **★결정적: 권위 데이터로도 순수 기하는 0(2mm 물리 초과), 실무 판걸이는 1**(블리드 트림·물림 허용). **어떤 기하 함수도 이 1을 재현 불가** → 유일 해법 = **판걸이수 시트를 `t_siz_pansu` lookup 으로 적재하고 엔진이 그 값을 우선 사용**(썬캡=1). §3 권고와 동일, 근거 강화.
- 부수 데이터 교정(§26): 썬캡 상품사이즈 `SIZ_000195`→`SIZ_000056` 정정 + 판형 330×540 등록/연결(단, 이것만으론 여전히 0 → lookup 필수).
