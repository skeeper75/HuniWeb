# 포토북 — 라이브 정정 실무진 정리표 (manifest-worksheet)

> **작성** 2026-06-14 · round-13 라이브 정정 트랙 (260610 L1 기준).
> **정답 권위:** 원본 엑셀 L1 최신 `24_master-extract-260610/photobook-l1.csv` (byte동일·유효·E1~E5 GO).
> **이 표는 비전문가 실무진용** — 코드값은 전부 한글 병기, 영문 분류어("MIS-LOADED" 등) 사용 안 함.
> **라이브 실측 갱신(2026-06-14):** 기존 `17_correctness/photobook` manifest(2026-06-11)의 일부 값이 stale → 직접 psql 재측정으로 확정. **가장 큰 정정 2건:**
> - ① **레더 자재유형이 이미 고쳐져 있음**(레더 BATCH-4 적용분 — MAT_000006 종이→가죽·MAT_000186 실사소재→가죽 둘 다 라이브 `.06 가죽`. 종전 "MIS-LOADED"는 이미 해소 → 고칠 것 없음).
> - ② **소프트커버 페이지 정책이 엑셀에 있음**(종전 F-PB-1은 "엑셀 공란"이라 했으나 L1 `책등` 칸에 소프트=4/6/8/10/12/14, 하드/레더=10/12/14/16 명시 → 표지타입별 차등이 엑셀 권위. 라이브 단일 24/150/2는 하드 기준만 반영 = 차등 미반영).

---

## 1. 포토북이 어떤 상품인가 (정체 — 칼럼 해석 이전)

- **범주:** 일반 인쇄물 (06 책자 > 포토북). 포장재/굿즈/액세서리 아님 — 카테고리 고아 없음(라이브 CAT_000108→상위 CAT_000006 책자, 정상).
- **구성:** **책자類 세트(B 셋트)** — 완제품 1(에디터 디자인) + 반제품 7(내지1·표지5·면지1).
- **생산방식:** 디지털인쇄(내지·하드/소프트 표지) + 특수출력(레더 표지) → 무광코팅(종이표지) → 면지부착 → **PUR제본**(책등=페이지수 가변) → 재단 → 포장.
- **레이플랫 vs PUR 컨펌(entity-semantic §144):** L1 `제본사양_제본` = 전 행 **PUR** 단일. 레이플랫 행 없음 → 라이브 PROC_000020 PUR 단일이 엑셀 정합. (레이플랫 미운영 = 정답, 별도 컨펌 불요.)

---

## 2. 정리표

| ID | 상품/속성 | 무엇이 틀렸나 (쉬운 한국어) | 어떻게 고치나 | 영향 범위 | 처리구분 | 근거 |
|----|-----------|---------------------------|---------------|-----------|----------|------|
| **W-01** | 포토북 · 표지(무광코팅) 공정 | 종이표지(하드/소프트)는 "무광코팅"을 입히는데, 그 코팅이 공정으로 안 들어가고 자재 이름 안에만 묻혀 있음("아트250+무광코팅"). **다른 책자 6종은 코팅을 공정으로 제대로 분리**했는데 포토북만 빠짐 | "무광(코팅)" 공정을 포토북에 연결 | 상품 1개 공정 1건 | **즉시적용** | 책자 6종 분리 정합 + 코팅=공정(실무진 확정 Q9) |
| **W-02** | 포토북 · PUR제본 필수표시 | "PUR제본"이 **선택공정(필수 아님)** 으로 들어가 있음. 포토북은 제본이 빠지면 책이 안 되므로 필수여야 함 | 제본을 "필수공정"으로 표시(켜기) | 상품 1개 공정 1건 | **즉시적용** | 제본=본질 필수·PUR 단일(택일 불요) |
| **W-03** | 포토북 · 페이지(책등) 정책 | 페이지 규칙이 **하드커버 기준 한 줄(24~150·2씩)만** 들어가 있음. 엑셀을 보면 **소프트커버는 페이지(책등)가 달라**(소프트=4/6/8/10/12/14단, 하드·레더=10/12/14/16단). 표지타입별 차등이 안 들어감 | 표지타입별 페이지(책등) 차등을 반영 (단 라이브 페이지룰은 상품당 한 줄 구조 → 표지타입별로 나누려면 옵션/sub_prd 설계 필요) | 상품 1개 페이지룰 | **컨펌대기** | L1 `책등` 칸 표지타입별 명시(하드 10/12/14/16·소프트 4/6/8/10/12/14) |
| **W-04** | 포토북 · 아트250 자재 정리 | 같은 "아트250+무광코팅" 종이가 **공백 차이로 두 개**(MAT_000250·MAT_000260)로 갈라져 있고, 안 쓰는 "하드커버전용지+무광코팅"(MAT_000172)도 떠 있음. 게다가 코팅이 자재 이름에 붙어 있어 위 W-01과 얽힘 | 자재 이름에서 코팅을 떼고(아트250만 남김), 중복 슬러그 통합 검토 | 자재 마스터 정리(횡단 8상품) | **컨펌대기** | 같은 자재 250/260 중복·172 미사용(v03 정규화 결함) |
| **W-05** | 포토북 · 가격 전체 | **가격이 한 줄도 안 들어가 있음**. 포토북은 "가격포함" 상품인데 라이브에 가격 0건 → 견적 불가 | 가격을 적재(사이즈 4 × 표지타입 = 활성 11조합 기본가 + 페이지 추가 2P당). 단 어느 가격 테이블에 넣을지 결정 필요 | 상품 1개 가격 다수 | **컨펌대기** | L1 `가격_기본(24P)`·`가격_추가(2P)당` 명시·라이브 0행 |
| **W-06** | 포토북 · 표지 펼침 작업사이즈 | 표지 펼침(책등 포함) 작업사이즈 7종이 마스터엔 있는데 상품엔 안 붙음 | 미연결 유지(생산 작업용·고객 미선택) vs 표지 반제품에 연결 결정 | 상품 1개 size(생산용) | **컨펌대기** | 펼침 siz 마스터 실재·상품 미연결 |
| **W-OK-1** | 포토북 · 레더 자재유형 | **(정정) 종전 "레더가 종이/실사소재로 잘못 분류됨"이라 했으나, 라이브 실측 결과 이미 가죽(.06)으로 고쳐져 있음**(레더 BATCH-4 적용분) | 고칠 것 없음 (이미 정답) | — | **고칠 것 없음** | 라이브 MAT_000006·186 둘 다 .06 |
| **W-OK-2** | 포토북 · 카테고리 | 정상 노드(포토북→책자)에 연결됨. 디지털인쇄처럼 가짜 카테고리 고아 없음 | 고칠 것 없음 | — | **고칠 것 없음** | 라이브 CAT_000108→CAT_000006 |
| **W-OK-3** | 포토북 · 사이즈 | 완성품 4종(8x8·10x10·A5·A4) — 엑셀과 일치 | 고칠 것 없음 | — | **고칠 것 없음** | L1 4종 = 라이브 4종 |
| **W-OK-4** | 포토북 · 도수/인쇄옵션 | 내지=양면 CMYK4, 표지=단면(앞 CMYK4·뒤 인쇄안함) — 엑셀과 일치 | 고칠 것 없음 | — | **고칠 것 없음** | L1 내지/표지인쇄 정합 |
| **W-OK-5** | 포토북 · 세트(반제품 7) | 완제품 1 + 반제품 7(내지·표지5·면지) — B 셋트 구조 충실 | 고칠 것 없음 | — | **고칠 것 없음** | 라이브 sets 7행 = 엑셀 |
| **W-OK-6** | 포토북 · 반제품 빈 껍데기 | 반제품 7개는 사이즈/자재/공정이 비어 있는데, 이건 정상(자재 권위는 부모 상품에 있음) | 고칠 것 없음 | — | **고칠 것 없음** | 설계의도(자재=부모+용도) |
| **W-OK-7** | 포토북 · 책등 미저장 | 책등(mm)은 페이지수 따라 앱이 계산(DB 미저장) | 고칠 것 없음 | — | **고칠 것 없음** | 앱 계산 원리 |
| **W-OK-8** | 포토북 · 수량/주문채널 | 수량 1~1000·1씩, 편집기=켜짐·업로드=꺼짐, 단위=권 — 전부 정답 | 고칠 것 없음 | — | **고칠 것 없음** | L1 정합 |

---

## 3. 처리구분 분포

| 처리구분 | 뜻 | 건수 | ID |
|----------|----|:--:|----|
| **즉시적용** | 사람 확인 없이 단순 INSERT/UPDATE로 바로 고침 | 2 | W-01(코팅 공정 연결)·W-02(PUR 필수 켜기) |
| **컨펌대기** | 사람이 결정해야 함 (질문 명시) | 4 | W-03(페이지 차등)·W-04(아트250 정리)·W-05(가격)·W-06(펼침 siz) |
| **고칠 것 없음** | 라이브가 이미 정답(정정 불요) | 8 | W-OK-1~8 |

> **즉시적용 2건의 실체:**
> - W-01 = `INSERT t_prd_product_processes (PRD_000100, PROC_000015 무광, mand=N)` — 무광 공정 마스터 실재(search-before-mint·신규 mint 0).
> - W-02 = `UPDATE … mand_proc_yn='Y' WHERE prd_cd='PRD_000100' AND proc_cd='PROC_000020'` — 멱등(이미 Y면 0행).
> 둘 다 비파괴·재실행 0행·롤백 가능.

---

## 4. 6대 기준 자가 점검표

| # | 사용자 기준 | 본 산출 충족 방식 |
|---|-------------|-------------------|
| 1 | 코드 숨긴 실무진 정리표 | 본 worksheet — 코드값 전부 한글 병기, 영문 분류어 0, 처리구분 한국어 3분류 |
| 2 | 멱등·안전 기술 적재본 | `apply.sql` — BEGIN…COMMIT(인간 승인 후), WHERE 현재값 가드/ON CONFLICT, 재실행 0행 |
| 3 | 롤백 | `rollback.sql` — 적용 전 원문 복원(무광 공정 DELETE·PUR mand=N 복귀) |
| 4 | 정규화·오염 방지 | `normalization-guard.md` — 3중 화이트리스트(table-spec·webadmin FK·260610 L1)·화이트리스트 밖 값 0 |
| 5 | CPQ option/template/constraint 반영 | `cpq-plan.md` — 표지타입(페이지차등)·표지 자재선택을 option_group/constraint로 설계 |
| 6 | 타임스탬프·변경이력 추적 | `apply.sql` — INSERT=reg_dt=now()·UPDATE=upd_dt=now()·note(있는 테이블) "정정 2026-06-14: X→Y" |

> **포토북 특수성 — note 칸 부재 주의:** `t_prd_product_processes`에는 note 컬럼이 **없음**(reg_dt/upd_dt/del_yn만). 즉시적용 2건의 변경이력은 reg_dt(INSERT)·upd_dt(UPDATE) **타임스탬프로만** 추적(디지털인쇄 apply.sql §컨펌블록 주석과 동일 제약). 페이지룰(W-03)은 note 칸 있음.

---

## 5. 미해소 컨펌 (인간 결정 필요)

- **Q-PB-1 (페이지·책등 표지타입별 차등):** L1 `책등` 칸이 하드/레더=10/12/14/16, **소프트=4/6/8/10/12/14**로 다름. 라이브 페이지룰은 **상품당 1행 구조**(PK=prd_cd)라 표지타입별 차등을 한 테이블에 못 담음. (가) 현행 하드 기준 1행 유지 (나) 표지타입을 CPQ 옵션으로 두고 옵션별 페이지 범위 차등 (다) 표지 sub_prd별 page_rule — 어느 모델로?
- **Q-PB-2 (아트250 자재 정리):** 코팅을 자재명에서 떼고(아트250만) + 중복 슬러그 250/260/172 통합. 7상품 쓰는 260로 통일 검토. 이름 변경/통합은 횡단 영향 → 별도 자재정리 트랙(ddl-proposer).
- **Q-PB-3 (가격 목표 테이블):** 포토북 가격(사이즈×표지타입 매트릭스 base + per-page)을 `t_prd_product_prices`(직접단가·고정가형) vs `t_prd_template_prices`(SKU별·Phase11 신설) 어디에? size×표지타입 SKU 성격 → round-2/16 가격 트랙 책임.
- **Q-PB-4 (표지 펼침 siz 귀속):** 표지 작업사이즈(펼침 7종)를 미연결 유지(생산용·견적밖) vs 표지 sub_prd에 연결(생산 BOM)?

---

## 6. 라이브 stale 정정 요약 (종전 17_correctness 대비)

| 항목 | 종전 분류(2026-06-11) | 라이브 실측(2026-06-14) | 변경 |
|------|----------------------|--------------------------|------|
| 레더 MAT_000006 자재유형 | MIS-LOADED(.01 종이→.06) | **.06 가죽**(이미 적용) | **해소 → 고칠 것 없음** |
| 레더 MAT_000186 자재유형 | MIS-LOADED(.08 실사→.06) | **.06 가죽**(이미 적용·6상품 횡단) | **해소 → 고칠 것 없음** |
| 소프트커버 page 정책 | AMBIGUOUS(엑셀 공란 GAP) | **엑셀 `책등` 칸에 차등 명시**(소프트 4~14) | **재분류: 컨펌대기**(엑셀 권위 존재) |
| 무광코팅 공정 | MISSING(컨펌대기) | 라이브 0건·마스터 실재 | **즉시적용 격상**(search-before-mint 가능) |
| PUR mand | MIS-LOADED | mand=N 확인 | 유지(즉시적용) |
| 가격 | MISSING | 0행 확인 | 유지(컨펌대기·가격 트랙) |

> **결산:** 레더 BATCH-4 적용으로 핵심 결함 2건 자동 해소(MIS-LOADED 4→2). 소프트 page는 엑셀 권위 발견으로 GAP→컨펌. 즉시적용 가능분 = 무광코팅 연결 + PUR 필수화 2건.
