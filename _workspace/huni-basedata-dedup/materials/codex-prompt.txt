You are an independent adversarial 2nd-opinion verifier for a data-deduplication decision in a print-shop's base-data (t_mat_materials = materials catalog). Your default stance is SUSPICION. The Claude analyst already made a verdict; your job is to attack it, not agree with it. Cite only facts present in the data below; do not invent table contents. Read-only — no DB access.

# CONTEXT — what this harness does (materials axis, display-duplicate dedup)
Goal: find rows whose USER-FACING DISPLAY NAME (mat_nm) is duplicated across different surrogate codes, or where display ≠ actual (mis-load), and safely consolidate. NOT a general mapping fix.
4 inspection axes [HARD]:
  ① authority + display↔actual consistency (mat_nm label ↔ internal type/parent/BOM ↔ authority excel). Mismatch = mis-load.
  ② display duplicate (same mat_nm, different mat_cd).
  ③ internal-value duplicate (same actual values, different mat_cd).
  ④ semantic-distinction preservation (work/cut/plate/unit/product-specific are NOT duplicates = false-positive guard).
Verdict types: A = surface-dedup (soft-delete stale, keep canonical), B = keep (legitimate semantic split), C = BLOCKED (escalate, do not touch live this round). Price-dependent (component_prices>0) = always BLOCKED hold.

# LIVE FACTS (independently re-measured by Claude from read-only SELECT; treat as the ground truth data, but you may challenge interpretation)
The candidate group is the calendar "tripod stand" (삼각대) parts.

mat_cd | mat_nm | mat_typ_cd | parent(upr_mat_cd) | dims(w/h/d/bdl) | BOM links | component_prices(cp) | del_yn | authority match
-------|--------|-----------|--------------------|-----------------|----------|---------------------|--------|----------------
MAT_000032 | 삼각대(그레이) | MAT_TYPE.02 (링제본부자재 / ring-binding part) | MAT_000031 (캘린더부자재 / calendar-part) | all NULL | 0 | 0 | N | NONE (no matching authority row)
MAT_000252 | 삼각대(그레이) | MAT_TYPE.07 (포장부자재 / packaging part) | none (root) | all NULL | 1 (PRD_000108 탁상형캘린더 / desk-calendar, USAGE.07) | 0 | N | YES — master-260610 calendar-l1: 탁상형캘린더 option "캘린더가공(필수)_삼각대컬러" value = 삼각대(그레이) → MAT_000252
MAT_000033 | 캘린더부자재 삼각대블랙 | MAT_TYPE.07 (포장부자재) | MAT_000031 | all NULL | 0 | 0 | N | NONE
MAT_000254 | 삼각대(블랙) | MAT_TYPE.07 (포장부자재) | none (root) | all NULL | 1 (PRD_000109 미니탁상형캘린더 / mini-desk-calendar, USAGE.07) | 0 | N | YES — master-260610 calendar-l1: 미니탁상형 option "삼각대컬러" value = 삼각대(블랙) → MAT_000254

Parent context: MAT_000031 (캘린더부자재, type .07, root) has children: MAT_000032 (삼각대 grey, type .02 — note the type mismatch vs siblings), MAT_000033 (삼각대 black, .07), MAT_000034 (우드거치대 wood-stand, .07, already soft-deleted del_yn=Y on 2026-06-16), MAT_000035 (끈 string, .07).
Note: MAT_000032's upd_dt = 2026-06-19 (touched recently) but still type .02, parent MAT_000031, BOM 0.

# UPSTREAM DIAGNOSIS (from a separate dbmap track, C-CAL-01) — TREAT AS A CLAIM, not gospel; judge if it actually applies
A separate "axis-staged-load" track (round-22/round-13) diagnosed that the tripod-stand SHOULD be a PROCESS (mounting/거치 process), not a material — i.e. MAT_000252/254 are themselves "material→process mis-loads". Its plan: mint a "tripod mounting process" master (currently 0 process rows exist), move 삼각대컬러 into option_items as a process param, then soft-delete the material rows MAT_000252/254. That process master does NOT yet exist (0 rows). So that axis-move is BLOCKED on a missing process mint.

# CLAUDE'S PHASE-2 VERDICT (the thing you must adversarially check)
Overall = C (BLOCKED). 0 safe loads this round. 0 live changes.
- Display-duplicate group = 삼각대(그레이): MAT_000032 vs MAT_000252.
- Claude says: surface "Plan A" (soft-delete MAT_000032 as stale, keep MAT_000252 as canonical) LOOKS right, BUT adopting it would lock MAT_000252 in as a "canonical material", which pre-empts the upstream C-CAL-01 decision that MAT_000252/254 should leave the material axis entirely (become a process). Therefore HOLD = C (BLOCKED), hand off to the axis-move track.
- Claude rejects Plan B (keep both as legit semantic split): says MAT_000032's .02 (ring-binding) type is just a wrong-type mis-load, not a legitimate "catalog vs production" axis, so there's no basis to keep it as a permanent distinction either.
- Black pair (MAT_000033/254): NOT a display-duplicate (names differ), but Claude bundles it into BLOCKED as a structural parallel + C-CAL-01 co-target.
- All 4 codes cp=0 → no price impact.

# YOUR JOB — answer each, row by row, default = suspicion
Q1. MAT_000032 "stale vs legitimate-split": Is .02 (ring-binding) on a row named "삼각대(그레이)" a legitimate semantic distinction worth keeping (B), or a stale mis-load remnant (deletable)? Authority NONE + BOM 0 + cp 0 + type-vs-name mismatch — do these signal stale? Give your independent call.
Q2. Axis-move boundary (is C justified?): Does adopting Plan A (delete 032, canonicalize 252) actually pre-empt the C-CAL-01 axis-move? OR — counter-argument — is MAT_000032 (authority-absent, BOM 0, type-mismatched orphan) STALE *independently* of the axis-move, such that soft-deleting ONLY 032 is safe even now (because 032 is junk under EITHER outcome: if 252 stays material, 032 is a dup; if 252 becomes process, 032 is still junk)? Argue both sides, then pick. THIS IS THE KEY QUESTION — Claude chose total-hold; challenge whether 032-only cleanup is safe.
Q3. Grey/black parallel over-reach: Is bundling the black pair (033/254) into BLOCKED an over-extension, given 033/254 are NOT a display-duplicate (different names)? Should the black pair be scoped out of THIS (display-dup) harness entirely?
Q4. NO-OP honesty: Is ending the materials pilot at 0 safe loads (BLOCKED handoff) an honest conclusion, or is there a safe cleanup Claude missed (e.g. 032 alone)?

Be concrete and decisive. If you disagree with Claude, say exactly what action you'd take instead and on which mat_cd. If you agree, say why the counter-arguments fail. Output a short structured verdict per Q1–Q4 plus a final one-line recommendation.
