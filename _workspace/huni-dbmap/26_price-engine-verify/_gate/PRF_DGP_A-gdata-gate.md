# PRF_DGP_A G-DATA 게이트 — 첫 클래스 실증 (round-18+)

> 실행 2026-06-14 · C-1 확정 기준([[C-1-decision]]) 첫 적용. 권위 = 라이브 실측(읽기전용) + 엽서 파일럿 `26_price-engine-verify/postcard/`.
> 클래스: **PRF_DGP_A**(디지털인쇄 원자합산형A) · 바인딩 9상품(엽서 PRD_000016 등) · 구성요소 29 · 전부 단가형(PRICE_TYPE.01).

## 0. 판정 한 줄
**G-DATA = 🔴 NO-GO (A-1 차단)** → G-CALC 진입 안 함 → **arbiter 정립(D-2 remediation·C-2) 직행**. 핸드오프 예측("D-2로 G-DATA 차단 → 정립 직행이 정상·현행 거짓 GO와 대비")대로 **정상 동작 재현**.

## 1. GATE-S (구조·배선 완전성) — ✅ PASS
postcard `chain-completeness.md` GO와 라이브 재확인 정합:
- PRF_DGP_A → formula_components 29 comp 배선 완전.
- 각 comp 단가행 완비(디지털인쇄비 단/양면 212행, 코팅 92, 용지 49, 별색 10종×53, 후가공 9~23 등).
- t_dsc(수량구간 할인) 0행은 D-3로 분리(별도).

## 2. GATE-A (의미 정합)

### A-1 (가격 구성 정합·옵션→comp 매핑) — 🔴 NO-GO
- **D-2 [최중대]**: 고객 옵션 선택(면·코팅·별색·후가공)을 **어느 comp로 합산할지** 매핑이 라이브에 부재. 순수 차원매칭 시 29 comp 무차별 → 대량 과대합산. (postcard `defects.md` D-2)
- **D-1**: 가격 단가행 siz_cd = 출력판형(SIZ_000499 등)인데 상품 작업사이즈(SIZ_000001~007)와 불일치 → 작업사이즈 입력으로는 매칭 0행. 작업사이즈→출력판형 변환 부재(엔진 매칭 규칙 갭).
- ⇒ "어느 comp를 어느 차원으로 합산"하는 매핑이 엑셀대로 라이브에 적재 안 됨 = **계산 선행 차단 사유**.

### A-2 (참조 마스터 정확성·클래스-스코프 라이브 실측) — ✅ PASS
C-1 확정 기준 적용. PRF_DGP_A 단가행 차원키가 가리키는 마스터 행 라이브 실측:
| 차원 | distinct | 고아 FK | round-13 MIS-LOADED 겹침 |
|---|--:|--:|---|
| siz_cd | 2 (SIZ_000077·SIZ_000499) | **0** | 무 |
| mat_cd | 49 (용지) | **0** | 무 |
| clr_cd | 2 (CLR_000002 1도·CLR_000005 4도) | **0** | 무 |
| proc_cd | 0 | 0 | — |
| opt_cd | 0 | 0 | — |
- 고아 FK 0. round-13 digital-print MIS-LOADED 4건(C-04 addon·C-05/C-09/C-14 카테고리)은 **가격 차원이 아님** → 엽서 가격계산 무관.
- **노트(정립 시 점검)**: siz SIZ_000077(316과 무관·note "적용=와이드벽걸이캘린더")이 공유 comp(디지털인쇄비 212행)에 혼입. 마스터 행 자체는 정상이라 A-2 통과이나, 단가행 차원 적합성은 D-1/A-1 정립에서 검토.

## 3. C-1 결정 타당성 실증 (첫 클래스에서)
엽서는 **A-2 PASS(참조 가격 마스터 정상)인데 A-1으로 차단**.
- 만약 선택지 B(전역 round-13 GO 필수)였다면 digital-print 라이브 미적용(카테고리 고아 잔존)으로 A-2도 막혔을 것. 그러나 그 고아는 가격 차원이 아니므로 엽서 가격과 무관.
- **클래스-스코프 실측(채택안 A)이 정확히 "무관 미적용은 막지 않고, 진짜 차단 사유(A-1 D-2)만 차단"** → C-1 결정의 타당성이 첫 클래스에서 확인됨.

## 4. 라우팅 (다음)
- G-DATA NO-GO(A-1) → **G-CALC·벤치마크 진입 안 함**. [HARD] 계산기 보정 하드코딩으로 거짓 GO 내지 않음(현행 recompute.py `compute_corrected`와 대비).
- 다음 단계 = **arbiter 정립**: D-2 remediation(옵션→comp 매핑) + **C-2 결정**(매핑 권위 = CPQ `option_items` vs 차원 컬럼 신설). 산출 경로 `28_price-arbitration/PRF_DGP_A/`.
- 재검증 폐루프: 매핑 적용(인간 승인) 후 G-DATA 재실행 — 보정 하드코딩 없이 활성 comp 한정 재현되면 RESOLVED.
