-- ============================================================
-- 가격테이블 무결성 교정 적재본 (생성측 산출물 · 결정론 배치)
-- [HARD] 인간 승인 전 COMMIT 금지. 게이트(골든 시뮬)+codex 스냅샷 교차 후 dbmap COMMIT.
-- 권위=인쇄상품 가격표 260527(절대). 단가 verbatim. 라이브 읽기전용·스냅샷 기준.
-- 생성: build_load.py (결정론·재실행 가능)
-- ============================================================

-- verbatim 적용 대상 0건(값 불일치/오타이핑 없음).

-- ── 설계 결정 필요(BLOCKED·자동 적재 금지) ──
-- [BLOCKED · 설계 결정 필요] dim_missing clr=흑백
--   권위: 212셀(전 판형·면·수량) | 라이브: 축 부재(use_dims collapse)
--   돈영향: 도수축 collapse: use_dims 에 도수축 없음 → 도수 구분 불가 (잘못된 단가 과/저청구 또는 견적불가) · 차원 설계 결정 필요(BLOCKED)
--   사유: 차원 축 신설/공식 재설계는 search-before-mint·인간 결정 (blind insert/swap 금지). dbmap/§18 라우팅.
