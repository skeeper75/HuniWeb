# 라이브 적재 기록 — 소형박 동판셋업비 공정코드 이관 (2026-08-30)

> 도메인룰 `huni-pricing-engine-map.md` §7.2 — 가격은 webadmin 실화면으로 넣는다(추적성).
> 대상 · 화면 경로 · 변경 전후 · 파급 범위 · 권위 근거 · 승인 사실을 1:1로 남긴다.

## 무엇을

| 항목 | 값 |
|---|---|
| 구성요소 | `COMP_FOIL_SETUP_SMALL` 박·형압 동판셋업비(소형) |
| 화면 | `/admin/price-viewer/comp/COMP_FOIL_SETUP_SMALL/edit/` (단가편집 그리드) |
| 행 수 | 8행 (변경 전후 동일) |
| 바꾼 것 | `proc_cd` 만. 단가 5,000 · 적용일 2026-06-01 · 비고 **전부 그대로** |
| 저장 응답 | `{"ok": true, "inserted": 8, "updated": 0, "deleted": 8}` (full-sync) |
| 승인 | 지니 — 「8행만 먼저 해보자」 (2026-08-30) |

## 변경 전 → 후

대형박(`PROC_000033`) 하위 → 소형박(`PROC_000153`) 하위. 공정명이 같은 것끼리 1:1.

| 색상 | 전 | 후 |
|---|---|---|
| 홀로그램박 | `PROC_000037` | `PROC_000154` |
| 금유광 | `PROC_000038` | `PROC_000155` |
| 은유광 | `PROC_000039` | `PROC_000156` |
| 먹유광 | `PROC_000040` | `PROC_000157` |
| 동박 | `PROC_000041` | `PROC_000158` |
| 적박 | `PROC_000042` | `PROC_000159` |
| 청박 | `PROC_000043` | `PROC_000160` |
| 트윙클박 | `PROC_000044` | `PROC_000161` |

변경 전 `comp_price_id` 47860~47867 (reg 2026-06-30) → 후 104620~104627 (reg 2026-08-30 01:54:43).

## 왜

2026-08-12 08:13:27 에 `PROC_000033` 이 「박」→「대형박」으로 개명되고 같은 시각
`PROC_000153` 「소형박」이 신설됐다. 프리미엄명함·펄명함(소형 명함)은 소형박 계열
(`154~161`)에 연결됐으나 **단가행은 대형박 계열에 남아** 위젯이 보내는 `PROC_000155`가
단가행 `PROC_000038`과 매칭되지 않았다. 개명·분리가 단가표까지 내려오지 않은 것이다.

## 검증 (화면)

| | 전 | 후 |
|---|---|---|
| 프리미엄 명함 `PRD_000031` · 금유광 선택 | 4,500원 (완제품가만) | **9,500원** (완제품가 4,500 + **동판셋업비 5,000**) |
| 프리미엄명함 · 박 해제 | 4,500원 | **4,500원** — 게이트 정상 |
| 펄명함 `PRD_000034` · 은유광 선택 | 박 줄 없음 | **동판셋업비 5,000 표시** |

박 선택 게이트(`use_dims=["proc_cd"]`)가 살아 있음을 해제 A/B로 확인했다.

## 파급 범위

이 구성요소를 쓰는 공식 3개(`PRF_NAMECARD_PREMIUM_FOIL` · `PEARL_FOIL` · `FIXED_FOIL`),
상품 2종(프리미엄명함 · 펄명함). `FIXED_FOIL`은 상품 미바인딩.
대형박 상품 5종은 `COMP_FOIL_SETUP_LARGE`를 써서 영향 없음.

## 아직 안 한 것

| 대상 | 행 | 상태 |
|---|---:|---|
| `COMP_FOIL_PROC_SMALL_STD` 가공비(소형·일반박) | 1,620 | 보류 — `use_dims` 스코프 `proc_grp:PROC_000033` → `PROC_000153` 변경이 함께 필요 |
| `COMP_FOIL_PROC_SMALL_SPECIAL` 가공비(소형·특수박) | 540 | 보류 — 동일 |

붙여넣기 TSV는 `out-foilfix/`에 생성돼 있다(`m6_foilfix.py`).

**미판정**: 단가행 `dim_vals`의 가로·세로가 화면 힌트와 반대로 보인다
(화면 「가로 20~80 / 세로 20~40」 ↔ 단가행 가로 3값 · 세로 5값).
권위 표의 「가로/세로」 축 라벨 해석이 먼저이므로 이번 이관에서는 손대지 않았다.

## 되돌리기

같은 화면에서 8행의 공정을 `154~161` → `037~044`로 되돌리고 저장하면 원상복구된다
(단가·적용일·비고는 바뀌지 않았으므로 공정 컬럼만 되돌리면 된다).
