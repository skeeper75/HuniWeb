-- =====================================================================
-- round-10 델타 적용본 — 상품마스터 260527 -> 260610
-- 종착점: 검증된 멱등 델타 + 롤백전용 DRY-RUN. NEVER COMMIT (인간 승인).
-- =====================================================================
-- [평결] 이번 버전쌍의 자동 적용 가능 UPSERT = 0건.
--   전 변경(527셀)이 ESCALATE(526) 또는 GAP(1)으로 분류됨:
--     - 굿즈파우치 448 = size->CPQ 옵션 재분류  (옵션레이어 미적재 -> L2 설계 escalate)
--     - 스티커 37      = 커팅옵션 클리어        (옵션레이어 escalate)
--     - 아크릴 41(LOW) = 변형행 -16 tail-shift  (논리삭제 후보 escalate)
--     - 실사 1         = 범례 텍스트            (적용 대상 t_* 없음 = GAP)
--   따라서 자동 생성 INSERT/UPDATE 문 없음. 본 파일은 단일 트랜잭션 스켈레톤이며
--   모든 escalate 처리문은 주석 처리(비파괴) — 인간 결정·후속 트랙용.
--
-- 안전(HARD): hard-delete 금지 / DDL 금지 / COMMIT 금지.
--   라이브는 읽기전용 정합만. 본 SQL은 BEGIN ... ROLLBACK 으로만 실행(DRY-RUN).
-- =====================================================================

\set ON_ERROR_STOP on
BEGIN;

-- ---------------------------------------------------------------------
-- 자동 적용 델타 (UPSERT): 없음 (0건).
--   멱등 증거: 적용문이 0이므로 DRY-RUN 1·2회차 모두 delta=0 (자명한 멱등).
-- ---------------------------------------------------------------------

-- ---------------------------------------------------------------------
-- ESCALATE 후보 (전부 주석 — 인간 승인·후속 CPQ/논리삭제 트랙에서만 적용)
--   아래는 "무엇을 할 것인가"의 명세이지 실행문이 아니다.
-- ---------------------------------------------------------------------

-- [A] 굿즈파우치 size->option 재분류 (224쌍, 58상품) — dbm-cpq-option-mapping L2
--   현재: 값들이 t_prd_product_sizes 에 size 로 적재(예 레더라벨제작 PRD_000280).
--   목표: CPQ 옵션레이어(option_groups/options/option_items)로 재구성.
--   적재된 size 행의 운명(유지 vs use_yn='N')은 L2 설계에서 결정.
-- 예시(절대 실행 금지 — 설계 후 별도 트랙):
--   INSERT INTO t_prd_product_option_groups (prd_cd, opt_grp_cd, opt_grp_nm, sel_typ_cd, ...)
--     VALUES ('PRD_000280', '...', '...', '...', ...) ON CONFLICT (prd_cd, opt_grp_cd) DO NOTHING;
--   (option_items.ref_dim_cd=OPT_REF_DIM 으로 차원행 polymorphic 참조 — 트리거 fn_chk_opt_item_ref 준수)

-- [B] 스티커 합판도무송스티커 커팅옵션 클리어 (37) — 옵션레이어/도메인 확인
--   라이브는 커팅을 size(정사각NxN(EA))로 적재. 옵션 재모델 or 제거 의도 확인 후.

-- [C] 아크릴미니파츠 변형행 -16 (LOW) — 논리삭제 후보
--   라이브 PRD_000163 size 1행. 감축행 use_yn='N' 제안은 logical-delete-and-gaps.md.

-- ---------------------------------------------------------------------
-- GAP: 실사 범례 텍스트 1셀 — 적용 대상 t_* 없음. 무처리(NO_OP).
-- ---------------------------------------------------------------------

ROLLBACK;   -- [HARD] 기본 롤백. COMMIT 은 인간 승인 후 수동 전환만.
