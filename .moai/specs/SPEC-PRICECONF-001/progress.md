---
id: SPEC-PRICECONF-001
doc: progress
version: "0.2.0"
updated: 2026-08-28
status: in-progress
tier: M
---

# 진행 원장 — SPEC-PRICECONF-001

> [HARD] 이 파일은 **현세대 기록만** 보유한다(REQ-PC-025). 마감된 세대는 `progress-history.md` 로 즉시 분리한다.
> 상한: 800줄 / 64 KB. 초과 시 분리가 선행하고 그 전에는 새 기록을 쓰지 않는다(REQ-PC-026).

## 상태

| 단계 | 상태 | 비고 |
|---|---|---|
| plan (SPEC 작성) | 완료 | Tier M · 3 아티팩트 + 원장 |
| plan-audit | 미실행 | 사용자 직접 지시로 run 진입 |
| run — M0 기준선 | **완료** | 2026-08-28 · §E.2 증거 |
| run — M4 포스터·사인 | **진행 중** | 세대 4 마감(M4-21~28) · 축③ 완주(UNVERIFIED 0) · **확정 결함 5종 교정 대기** · 오판 5건 철회 |
| run — M8·M3·M5·M6·M10·M7·M9·M2·M1 | 대기 | 밀도 순 |
| run — M11 종료 스윕 + 트랙 B | 대기 | |
| sync | 대기 | |

---

## §E.1 Plan-phase Audit-Ready Signal

plan_status: audit-ready
plan_complete_at: 2026-08-28

M0 실측으로 `plan.md §D` 를 개정(v0.1.0 → 마일스톤 분할 축 변경). 개정 근거는 §E.2 D-1.

## §E.2 Run-phase Evidence

### M0 — 기준선 (마감 2026-08-28 · 상세는 `progress-history.md` 세대 1)

M0 의 증거 9항(E-1~E-9)과 개정 결정(D-1)은 `progress-history.md` 로 이월했다.
현세대가 계속 참조하는 값만 남긴다:

| 항목 | 값 | 실측 |
|---|---|---|
| 게시 분모 | **192** | 2026-08-29 재실측(M4-26). 세대 3까지의 **193 은 stale** — 1건 감소, 어느 상품인지는 미추적 |
| 「가격 미검토」 게시 | **131** | 동일 |
| 마일스톤 배정 | M1 36 · M2 28 · M3 18 · M4 28 · M5 17 · M6 16 · M7 14 · M8 13 · M9 11 · M10 12 | 중복 0 · 누락 0 |
| 산출물 | `.moai/state/verify/PRICECONF-M0/` (워크리스트 · 마일스톤 배정 · 시트 커버리지) | |

### M4 — 포스터 · 사인 (진행 중)

> 세대 3(M4-13 ~ M4-20)은 `progress-history.md` 로 이월했다. 아래는 **세대 4(M4-21 ~ M4-28)** 다.

#### 세대 4 요약 — 무엇이 확정되고 무엇이 철회됐나

| # | 작업 | 결과 |
|---|---|---|
| M4-21 | 축③ UNVERIFIED 5건 해소 | **29셀 전건 일치** → 축③ UNVERIFIED **0** · 종단 PASS 18→**23** |
| M4-22 | 축④ 조합 분모 전수 재산정 | 사이즈·자재 축 **28/28 OK · 113=113**. 옵션 가격축 미측정 5상품 발견 |
| M4-23 | 추가상품 템플릿 단가 전수 | 템플릿 99종 · **단가 불일치 0**. 결함 2건은 고아(도달 불가) |
| M4-24 | 후속 3단계 | 미판정 13→2 · 옵션 단가 **27/27 일치** · 이중합산 없음 실증 · 봉투 **연결 누락 9종** |
| M4-25 | 옵션 실호출 + 축④ 재판정 | 70호출 OK 64. **결함 A/B/C** 확정. **축④ FAIL 5건은 결함 0** |
| M4-26 | 게시 위젯 0원·음수 점검 | **0원 0 · 음수 0** (192위젯 × 2방식). 0원 단가 12건은 전부 정당 |
| M4-27 | 공식↔구성요소 배선 검증 | 「과소청구 20상품」 주장 → **M4-28에서 전면 철회** |
| M4-28 | **[정정]** M4-27 오판 철회 | meta 기반 재검증 **OK 174 · ERR 18 · ZERO 0 · NEGATIVE 0** |

**이 세대의 성격**: 조사·판정·정정이 전부다. **라이브 쓰기 0건**, 코드 구현 0건.
`/moai run` 슬래시 커맨드는 호출하지 않았고 `manager-develop` 도 쓰지 않았다 —
resume 지시에 따라 오케스트레이터가 직접 조사·기록·커밋했다(run 단계의 작업이나 run 워크플로는 아님).

#### 확정 결함 (교정 대기)

| # | 결함 | 대상 | 성격 | 근거 |
|---|---|---|---|---|
| **A** | 마감 **필수** 옵션에 단가행 없음 → **주문 차단** | `PRD_000133` 캔버스행잉 · `PRD_000134` 린넨우드봉족자 | 고객이 반드시 골라야 하는데 고르면 「주문할 수 없습니다」 | M4-25 · 3사이즈 전부 재현. 대칭 비교군: `PRD_000124`·`PRD_000139` 는 `0.00` 행이 있어 통과 |
| **B** | 사이즈 수량규칙 오적재 | `PRD_000145` 미니배너 `150x300mm` | 권위 1/10000/1 vs 라이브 **5/1000/5** — 1~4개 불가·5배수만·10000구간 도달불가 | M4-25 · 권위 상품마스터 실사 r142 |
| **C** | 상품 수량규칙 NULL → 상한 미적용 | `PRD_000141` 홀로그램시트커팅 · `PRD_000142` 유광아크릴스티커 | 권위 1/10000/1 인데 **qty=999,999 통과**(실증) | M4-25 · 짝 중 하나만 비었다(무광 ✓/홀로그램 ✗, 미러 ✓/유광 ✗) |
| **D** | 폼보드 A3 화이트 단가 | `PRD_000129` | `supply 6,000` vs 권위 `7,000` — 1행 | 세대 3부터 미교정 |
| **E** | 추가상품 **연결 누락 9종** | OPP접착봉투 4 · OPP비접착봉투 1 · 트레싱지봉투 4 | 템플릿·단가는 권위대로 있는데 `t_prd_product_addons` 배선만 없어 고객이 못 산다 | M4-24 · 의도적 미출시인지 누락인지는 **실무진 확인 사항** |

#### 철회된 주장 (다시 제기하지 말 것)

| 주장 | 철회 근거 |
|---|---|
| 「무광시트커팅 조합 6→3」 | **6이 맞다.** 3사이즈 × 자재 2. `axis4.py` 는 M4-14에서 이미 `del_yn` 필터를 걸었다(M4-22) |
| 「축④ FAIL 5건 = 결함」 | 폼보드·포맥스 8 = **성립 불가 조합**(자재명에 사이즈 박힘) · 138/139 = 재현 불가 · 145 = 도구 정규식 한계(M4-25) |
| 「실내용배너거치대 3,000원 과소청구」 | 상품마스터 「실사」 c26(10,000)이 **STALE**. 권위는 가격표 「포스터사인」 c11(**7,000**), 라이브도 7,000(M4-23) |
| 「부자재 선택안함 주문 차단」 | 그 옵션은 `del_yn='Y'` **삭제 옵션** — 고객 도달 불가(M4-25) |
| **「디지털인쇄 과소청구 20상품」** | **전면 오판.** `cfg`(레이아웃)를 `meta.prod_dims`(데이터)로 착각. 실제로는 정상 청구(M4-28) |


## §E.3 Run-phase Audit-Ready Signal

_<M11 종료 스윕 후 기재>_

## §E.4 Sync-phase Audit-Ready Signal

_<pending sync-phase>_

---

## §F Phase 4 Mode Selection

**입력 파라미터** — tier: M · 범위: SPEC 아티팩트 2 + 하네스 1 + 원장 산출 3 · 도메인: 1(가격 정합) ·
파일 언어: markdown + python + SQL · 병렬 이득: LOW(라이브 화면 조작은 병렬화 불가, plan.md §B-4)

| 모드 | 선택 | 사유 |
|---|---|---|
| `direct` | **선택** | M0 은 읽기 전용 실측 + 문서 산출. 코드 변경 없음, 다중 도메인 아님 |
| `serial` | 미선택 | 서브에이전트 위임 이득 없음 — 단일 세션 실측으로 완결 |
| `fanout` | 미선택 | 단일 도메인(가격 정합). 다중 도메인 리서치 아님 |
| `sweep` | 미선택 | 기계적 대량 변환 아님(파일 30건 미만, 균일 변환 규칙 없음) |

**Decision: direct**

**근거** M0 의 작업은 라이브 SELECT 5회 + 엑셀 결정론 스캔 1회 + 문서 3건 갱신이다. 코딩 중심 작업이
아니고 다중 도메인도 아니므로 Anthropic 의 coding-task 병렬성 유보 조항과 무관하게 `direct` 가 최소 모드다.
M4 이후는 화면 조작이 개입하므로 `serial` 재평가 대상.

---

## §G 원장 세대 분리 규약

> 세대 목록·분리 트리거·현재 계측은 **§G 다음 세션 재개 지점 §7 원장 규약**이 정본이다(중복 제거).

---

## §H M0 산출물 위치

| 산출물 | 경로 | 행수 |
|---|---|---|
| 게시 상품 워크리스트 | `.moai/state/verify/PRICECONF-M0/worklist-260828.csv` | 193 |
| 마일스톤 배정 | `.moai/state/verify/PRICECONF-M0/milestone-assignment-260828.csv` | 193 |
| 권위 시트 커버리지 | `.moai/state/verify/PRICECONF-M0/sheet-coverage-260828.csv` | 시트×마일스톤 |
| 커버리지 스캐너 | `_workspace/postersign-audit/coverage_scan.py` |
| 전 시트 구조 조사 | `_workspace/postersign-audit/scan_all_sheets.py` — 전치 가능 블록 전수 | — |

---
## §G 다음 세션 재개 지점 (2026-08-29 마감 · 세대 4)

### 0. 이 세션이 무엇이었나 — 한 문단

M4 잔여를 닫으러 들어가 **축③을 완주**하고(UNVERIFIED 0), 그 뒤 지니의 연속 지적에 따라
**추가상품 템플릿 · 옵션 가격축 · 0원/음수 · 공식 배선**까지 검증 범위를 넓혔다.
확정 결함 5종(A~E)을 남겼고, **오판 5건을 철회**했다. 마지막 M4-27은 통째로 뒤집혔다.
**라이브 쓰기 0건 · 코드 구현 0건.**

### 1. [HARD] 다음 세션이 반드시 지킬 것 — 순서가 곧 정확도다

> **매뉴얼 → 실화면 → 코드 → DB.** DB 역산이 첫 단계면 안 된다.
> 세대 4의 오판 5건 중 4건이 이 순서를 어겨서 났다.

| # | 지침 | 왜 |
|---|---|---|
| 1 | **가격 검증 입력은 `meta.prod_dims` + `mand:true`** 로 만든다 | `GET /api/w/v1/widgets/{wgt}?site_key=` 응답의 **최상위 `meta`**(← `widget` 안 아님). `cfg.items[].ref_key` 는 **레이아웃**이지 데이터가 아니다. DB로 조합을 지어내면 위젯이 실제 보내는 것과 달라지고 그 결과를 결함으로 읽는다 |
| 2 | **「고객이 안 고르면 적용 안 된다」고 가정하지 않는다** | 고객은 최소 옵션만 고르고 **자재·공정은 내부에서 정해진다**. `mand:true`/`mand_proc_yn='Y'` 는 위젯이 **항상 켠 채 보낸다**(`catalog/models.py:701` 계약). 시뮬레이터도 자동으로 채운다 |
| 3 | **상품뷰어가 세팅됐으면 「가격 빼고 다 연결」로 신뢰한다** | 실무진이 관리하는 화면이다. 여기가 완비인데 가격이 안 나오면 **가격 쪽(공식·구성요소·단가)** 을 의심한다 |
| 4 | **결함 주장 전 3관문** | ① `use_dims`(가격축인가) ② `del_yn`/`use_yn`(살아있는가) ③ **위젯 도달**(고객이 가는가). 셋째는 DB로 못 답한다 |
| 5 | **권위는 여러 곳에 있고 값이 다르다** | 가격 사안의 권위는 **가격표**다. 상품마스터 「실사」 c25/c26 은 **STALE**(실측: 배너거치대 10,000 vs 가격표·라이브 7,000). 한 시트 안에서도 **A열 블록만 훑으면 우측(c10~) 가격 블록을 통째로 놓친다** |
| 6 | **가격 설계 질문 전 「계산공식집초안」**(상품마스터 **첫 시트**) 을 읽는다 | 상품군별 유형(원자합산형/고정가형)과 **판매가 전개식**이 정본이다. 전개식에 없는 축(예: 공정 선택)은 고객 선택 사항이 아니라는 뜻 |
| 7 | 낮은 `total` 은 **`components` 배열을 열어** 무엇이 빠졌는지 본다 | 엔진은 필수 구성요소 미매칭을 **에러로 막지 않고** 그 항목만 뺀 채 `ok:true` 를 낸다 |

### 2. webadmin 화면 지도 (매뉴얼 = `raw/webadmin/tools/manual_content.py`)

| 화면 | 경로 | 쓰임 |
|---|---|---|
| 상품 뷰어 | `/admin/product-viewer/` → `pvSelect('PRD_xxx')` | 사이즈·도수/인쇄옵션·**판형**·자재·**공정**·묶음수·추가상품·제약 전 섹션 |
| 가격 뷰어 | `/admin/price-viewer/` → `qvSelect('PRD_xxx')` | 상품별 가격 원천(직접단가/공식/할인) 시계열 연결 |
| **가격 구조 보기** | `/admin/price-viewer/{PRD}/diagram/` | **공식 → 구성요소 → 단가표** 한눈에. 구성요소별 단가행까지 표시 |
| **가격 시뮬레이터** | `/admin/price-simulator/` → `.pitem[data-prd]` 클릭 | 차원·옵션·**공정**·수량 입력 → **구성요소별 계산 상세**(매칭 차원·판수 환산까지) |
| 가격공식 MD | `/admin/price-formula-md/` | 공식별 구성요소 편집 |
| 가격구성요소 MD | `/admin/price-component-md/` | 구성요소 속성 + 단가표 그리드 진입 |

로그인: `.env.local` 의 `HUNI_ADMIN_URL` / `HUNI_ADMIN_ID` / `HUNI_ADMIN_PW`.
gstack: `$B goto <url>` → `$B snapshot -i` → `$B fill/click` → `$B js "..."`.

### 3. 착수 후보 (지니 선택 대기)

1. **결함 A 교정 — 마감 필수 옵션 단가행 추가** (`PRD_000133`·`PRD_000134`)
   0원 단가행 1건씩만 넣으면 주문 차단이 풀린다. 비교군(`PRD_000124`·`PRD_000139`)이 정답 형태를 보여준다.
   **주문이 막혀 있으므로 파급이 가장 직접적이다.** Web UI 경유 · 건별 승인.
2. **결함 B·C 교정 — 수량규칙** (`PRD_000145` 사이즈 배선 · `PRD_000141`·`PRD_000142` 상품 레벨)
   권위 1/10000/1 로 맞춘다. B는 10000 구간 단가(2,800원) 도달 불가까지 딸려 있다.
3. **결함 D 교정 — 폼보드 A3 화이트 6,000→7,000** (1행). 권위 재확인 후 Web UI 그리드.
4. **결함 E 판단 — 봉투 연결 누락 9종** 실무진 확인 사항. 의도적 미출시인지 배선 누락인지 물어야 한다.
5. **미판정 18건 좁히기** — `meta.prod_dims` 로도 가격이 안 나온 건. 페이지수 축 5(캘린더) ·
   내지/묶음수 축 9(책자·포토카드·엽서북) · 최소수량 2 · **단가 미등록 후보 3**(투명케이스·고무판·아크릴쉐이커).
   앞의 15건은 도구가 축을 못 채운 것이므로 **축을 채우는 프로브 보강**이 선행.
6. **화이트인쇄엽서(`PRD_000020`) 단독 확인** — `PROC_000008 화이트인쇄` 필수인데 meta 기반에서도
   용지비만 나왔다. 별색인쇄비가 `spot_side_cnt` 를 더 요구하는 것으로 보이나 **미판정**.
7. **M8 스티커(13건)로 이동** — `PRD_000052`~`PRD_000067`. 밀도 100%.
8. **박 면적격자 값 대조** — `COMP_FOIL_PROC_*` **8,816행**. 축은 완전 직교 확인됨
   (`dim_vals` JSON 에 가로·세로 실재). 남은 건 값 정합 하나. M6 소관이나 파급 최대.

### 4. 재개 전 반드시 재실측할 것

- **게시 분모** — 2026-08-29 실측 **192**. 세대 3까지의 **193은 stale**. 매번 다시 잰다(AC-PC-025)
- **권위 엑셀 최신본** — `ls -la /Users/innojini/Dev/HuniWeb/docs/huni/*.xlsx` **절대경로로**
  (워크트리에서 상대경로로 보면 260702가 최신으로 나온다). 현재 최신 = **260822_1**
- **`git log --oneline -8`** — 세대 4 커밋 8건(`ea68db8c` … `28e2cc96`)

### 5. 산출물 위치 (세대 4)

| 산출물 | 경로 |
|---|---|
| **★ M4-27 정정** | `_workspace/postersign-audit/widget-meta-correction-260829.md` |
| **★ 올바른 프로브** | `_workspace/postersign-audit/widget_meta_probe.py` — `meta.prod_dims` + `mand` |
| 축③ 5건 해소 | `axis3-unverified5-260829.md` |
| 축④ 분모 재산정 | `axis4-recount-260829.md` · `axis4_recount.py` · `axis4_optaxis.py` |
| 추가상품 템플릿 | `addon-tmpl-audit-260829.md` · `addon_tmpl_audit.py` |
| 후속 3단계 | `addon-followup-260829.md` |
| 옵션 실호출 | `optaxis-probe-260829.md` · `optaxis_probe.py` |
| 0원·음수 | `zero-price-260829.md` · `zero_price_scan.py` · `zero_price_probe2.py` |
| 공식 배선(오판 포함) | `formula-wiring-260829.md` · `axis_gap_scan.py` · `mandatory_proc_gap.py` |
| 4축 판정표 | `axis-verdict-28.csv` |

> 위 경로는 전부 `_workspace/postersign-audit/` 아래다.

### 6. 도구 함정 (누적 — 세대 4에서 6줄 추가)

세대 3까지의 함정표는 `progress-history.md` 세대 3에 있다. 세대 4가 새로 밟은 것:

| 함정 | 증상 | 회피 |
|---|---|---|
| **[HARD] 위젯 `cfg` 를 「노출 축」으로 읽음** | `proc_cd` 가 없다고 「과소청구 20상품」 오판 | **`cfg`=레이아웃, `meta.prod_dims`=데이터**. `meta` 는 응답 **최상위** |
| **[HARD] 「고객이 안 고르면 적용 안 된다」 가정** | 필수공정이 가격에 안 붙는다고 판단 | `mand:true` 는 위젯이 항상 켠 채 보낸다(`models.py:701`) |
| **[HARD] 매뉴얼·실화면 없이 DB 역산** | 상품뷰어 완비인데 「배선 결함」이라 함 | **매뉴얼 → 실화면 → 코드 → DB** |
| **[HARD] 값 대조를 배선 검증보다 먼저** | 배선이 어긋나면 값이 맞아도 가격이 안 나온다 | ①배선 ②축 충족 ③값 대조 |
| **권위 시트를 하나로 가정** | 「실사」 c26(10,000)으로 없는 결함을 만듦 | 가격 권위는 **가격표**. 한 시트 **우측(c10~)** 도 훑는다 |
| **도구 매칭 완화로 1건 더 잡기** | OK 46→45 · 미판정 10→12 로 나빠짐 | 완화는 **판정 지표로 검증**하고 나빠지면 되돌린다. 도구 한계 ≠ 미검증 |
| **0원을 곧 결함으로** | 0원 12건 전부 정당(직각모서리 무료) | 권위가 0원인지 먼저 본다. 한 구성요소가 `proc_cd` 로 유료/무료를 함께 담는다 |
| **`del_yn` 미필터(재발)** | 삭제 옵션 「선택안함」을 결함으로 봄 | 라이브 행을 셀 때 `del_yn`/`use_yn` 을 **먼저** 건다 |

### 7. 원장 규약

- `progress.md` — **현세대만**. 현재 세대 = **세대 4(M4-21 ~ M4-28)**
- 세대 1 = M0 · 세대 2 = M4-1~M4-12 · 세대 3 = M4-13~M4-20 → 전부 `progress-history.md`
- 분리 트리거: 마일스톤 마감 또는 **800줄 / 64KB 초과** 중 먼저 오는 것
- **분리가 선행하고 그 전에는 새 기록을 쓰지 않는다**(REQ-PC-026)
- 현재 계측: 세대 3 분리 직후 기준 **상한 이내**(마감 시 `wc -l`·`wc -c` 재계측)
