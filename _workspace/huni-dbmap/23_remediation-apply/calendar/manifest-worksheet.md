# 캘린더 + 디자인캘린더 — 라이브 정정 실무진 정리표 (manifest-worksheet)

> **작성** 2026-06-14 · round-13 라이브 정정 트랙 (260610 L1 기준).
> **정답 권위:** 원본 엑셀 L1 최신 `24_master-extract-260610/calendar-l1.csv` · `design-calendar-l1.csv` (E1~E5 독립검증 GO·무손실, 헤더 타입 artifact 0→0.0만 무관).
> **이 표는 비전문가 실무진용** — 코드값은 전부 한글 병기, 영문 분류어("MIS-LOADED" 등) 사용 안 함.
> **라이브 실측 갱신(2026-06-14):** `17_correctness/calendar` 분석을 직접 psql 재측정으로 확정. 주요 확정 = MES 007-0001~5 전역 미사용(즉시적용 가능)·삼각대/링 자재 오적재 6행·우드거치대 자재 마스터 실재(MAT_000223/MAT_000034).
> **양식 표준:** 디지털인쇄 GO 산출(`23_remediation-apply/digital-print/`) 5종 양식 동일.

---

## 1. 처리구분 3분류 (정직하게)

| 처리구분 | 뜻 | 본 시트 건수 |
|----------|----|:--:|
| **즉시적용** | 사람 확인 없이 단순 UPDATE/INSERT로 바로 고칠 수 있는 것 | 1 |
| **신규적재** | 연결행을 새로 만들어야 하나, 공정 신설·옵션 설계·가격 트랙이 얽혀 설계가 필요한 것 | 8 |
| **컨펌대기** | 사람이 결정해야 하는 것 (질문 명시) | 5 |
| **고칠 것 없음** | 라이브가 이미 정답(정정 불요) | 7 |

> **캘린더가 디지털인쇄와 다른 점:** 디지털인쇄의 즉시적용은 "가짜 카테고리에서 진짜 자리로 재연결"이었다. **캘린더는 카테고리 고아가 0건**(전부 007 하위 정상 노드) → 그 즉시적용 대상이 없다. 대신 캘린더의 깨끗한 즉시적용 = **MES 코드 채움**(007-0001~5, 전역에서 아무도 안 쓰는 번호·중복 0·제약 위반 0 확인). 나머지(삼각대/링 공정 전환·장수·가공 택일·봉투·가격)는 공정 신설·CPQ 설계·가격 트랙이 얽혀 신규적재/컨펌이다.

---

## 2. 정리표

| ID | 상품명 | 무엇이 틀렸나 (쉬운 한국어) | 어떻게 고치나 | 영향 범위 | 처리구분 | 근거 |
|----|--------|---------------------------|---------------|-----------|----------|------|
| **C-13** | 탁상·미니·엽서·벽걸이·와이드 캘린더 5종 | 생산관리 코드(MES) 칸이 전부 비어 있음(NULL). 엑셀 원본엔 007-0001~5로 5상품 1:1로 적혀 있음 | 5상품에 각자 MES 번호(007-0001~5)를 채움. 이 번호를 쓰는 다른 상품이 전혀 없어(중복 0) 안전하게 채울 수 있음 | 상품 5개 코드칸 | **즉시적용** | 엑셀 L1 MES칸 + 라이브 전역 007번호 미사용 실측 |
| **C-01** | 탁상·미니 캘린더 | "삼각대(그레이/블랙)"가 "종이 같은 자재"로 잘못 들어가 있음. 삼각대는 종이가 아니라 "삼각대에 끼워 세우는 가공(공정)"임 | 삼각대 거치를 공정으로 옮김. 단 "삼각대 거치"라는 공정 자체가 아직 DB에 없어 먼저 만들어야 함(삼각대 색은 그 공정의 선택값) | 상품 2개 자재→공정 | **신규적재** | 거치=공정(생산 Case)·삼각대 거치 공정 마스터 0행 |
| **C-02** | 벽걸이·와이드 캘린더 | "링 블랙"이 자재로 들어가 있는데, 링은 이미 들어가 있는 "트윈링제본 공정"의 색(선택값)이어야 함 → 자재행이 중복 | 링 색을 트윈링제본 공정의 선택값으로 옮기고, 자재로 들어간 "링 블랙"행은 끔(논리삭제·완전삭제 아님) | 상품 2개 자재 중복 | **신규적재** | 트윈링제본 공정 이미 적재·링=그 공정 색 |
| **C-03** | 탁상·미니 캘린더 | 탁상·미니는 트윈링제본을 안 하는데 "링 블랙" 자재가 붙어 있음 = 완전히 잘못 붙은 잉여 | "링 블랙" 자재 연결을 끔(논리삭제) | 상품 2개 잉여 자재 | **신규적재** | 탁상/미니=삼각대 거치(트윈링 아님) |
| **C-05** | 캘린더 5종 | 핵심인 "장수(4장/8장/12장/16장…)"가 어디에도 안 들어감. 장수는 고객이 고르는 옵션이고 장수마다 가격이 다름 | "장수 선택 옵션"을 새로 만들고 가격공식과 연결. (주의: 장수는 책자 페이지룰이 아님 — 캘린더 고유 옵션) | 상품 5개 옵션+가격 | **신규적재** | 실무진 확정 Q12(장수=고객선택+가격공식)·옵션 0행 |
| **C-06** | 캘린더 5종 | "캘린더가공"이 여러 방식(가공없음/우드거치대/타공+끈/트윈링제본) 중 하나를 고르는 택일인데, 택일 묶음이 안 들어감(공정만 따로 떠 있음) | "캘린더가공 택1 묶음"을 만들고 가공별 추가가격을 연결 | 상품 5개 택일그룹 | **신규적재** | 실무진 확정 Q12·가공 택일 6멤버·택일그룹 0행 |
| **C-08** | 엽서·디자인 캘린더 | 추가상품 "캘린더봉투"가 안 연결됨(봉투는 사이즈에 맞춰 골라야 함) | 캘린더봉투(PRD_000005)를 추가상품으로 연결. 단 사이즈 맞춤 봉투 묶음(template)을 먼저 만들어야 함 | 상품 봉투 연결 | **신규적재** | 추가상품 칸·캘린더봉투 template 0건 |
| **C-09** | 엽서·디자인 캘린더 | 추가부속 "우드거치대"가 안 들어감 | 우드거치대를 자재로 연결. 우드거치대 자재(MAT_000223·MAT_000034)가 이미 DB에 있어 새로 안 만들어도 됨 | 상품 자재 연결 | **신규적재** | 실무진 확정 Q13(우드거치대=자재)·자재 마스터 실재 |
| **C-11** | 디자인 캘린더 5종 | 디자인캘린더는 가격이 정해진 고정가(4000~24000원)인데 가격이 하나도 안 들어감 | 사이즈/페이지별 고정가를 가격칸에 채움(별 가격 트랙) | 상품 가격 | **신규적재** | 실무진 확정 Q15(디자인=고정가)·가격 0행 |
| **C-10** | 디자인 캘린더 5종 | 디자인캘린더는 "에디터로 편집해 주문"하는데 에디터 켜짐 표시가 꺼져 있음(N) | 에디터 켜짐(Y)으로 바꿈. 디자인캘린더는 캘린더와 같은 상품의 다른 주문방식이라 같은 상품칸을 켬 | 상품 5개 에디터 플래그 | **컨펌대기** | 디자인=에디터 surface·같은 상품 |
| **C-04** | 캘린더 5종 | 출력용지 종류값(국전계열/기타)은 정답이나, 적재 프로그램(load_master)을 다시 돌리면 이 값이 "기타"로 되돌아가는 위험이 있음 | 값은 그대로 두고, 적재 프로그램에 "치수→계열" 매핑을 보강(재적재 시 되돌림 방지) | 적재 코드 정합 | **컨펌대기** | 적재 코드 결함 F-1(코드↔라이브 불일치) |
| **C-12** | 디자인 캘린더 | 같은 상품인데 종이가 캘린더판은 "별도설정"(고르기), 디자인판은 "몽블랑190g"(딱 정함)으로 적힘. 어느 쪽이 진짜 자재 기준인지 미정 | 두 표기 중 자재 권위를 결정 | 종이 자재 권위 | **컨펌대기** | 같은 상품 두 surface 종이 확정도 다름 |
| **C-14** | 미니탁상형 캘린더 | 미니가 "탁상형캘린더" 분류에 묶여 있음. "미니탁상형캘린더" 전용 분류칸(CAT_000113)이 따로 있는데 안 씀 | 전용칸으로 옮길지(전용 노드) 지금 분류 유지할지(엑셀 구분=탁상형) 결정 | 상품 1개 분류 | **컨펌대기** | 엑셀 구분=탁상형 / 전용 노드 실재·미연결 |
| **C-CL-A** | 삼각대 거치 공정 | (위 C-01 부속) "삼각대 거치" 공정을 새로 만들지, 부속 자재로 둘지 정책 결정 | 공정 신설 vs 자재 유지 결정 | 정책 | **컨펌대기** | 거치=공정(설계의도) vs 현 자재 적재 충돌 |
| **OK-1~7** | 캘린더 5종 | 사이즈(엽서 6종 포함)·도수(단/양면)·출력용지 값·카테고리 고아 0·tr03 디자인상품·디자인=같은 상품·트윈링/타공 공정 = 전부 정답 | 고칠 것 없음 | — | **고칠 것 없음** | 엑셀=라이브 정합·고아 0 |

---

## 3. 처리구분 분포

| 처리구분 | 건수 | ID |
|----------|:--:|------|
| **즉시적용** | 1 | C-13 (MES 채움·상품 5개) |
| **신규적재** | 8 | C-01·C-02·C-03·C-05·C-06·C-08·C-09·C-11 |
| **컨펌대기** | 5 | C-04·C-10·C-12·C-14·C-CL-A |
| **고칠 것 없음** | 7 | OK-1~7 (size·도수·plate값·고아0·prd_typ·디자인=같은상품·트윈링/타공) |

> **즉시적용 1건의 실체:** MES 채움 = 탁상(007-0001)·미니(007-0002)·엽서(007-0003)·벽걸이(007-0004)·와이드(007-0005). 5상품 1:1·전역 007번호 미사용(중복 0 실측)·UNIQUE 제약 부재(위반 0)·L1 엑셀 명시값. `t_prd_products`엔 note 컬럼이 없어 변경이력은 `upd_dt=now()`로만 추적.

---

## 4. 6대 기준 자가 점검표

| # | 사용자 기준 | 본 산출 충족 방식 |
|---|-------------|-------------------|
| 1 | 코드 숨긴 실무진 정리표 | 본 worksheet — 코드값 전부 한글 병기, 영문 분류어 0, 처리구분 한국어 3분류 |
| 2 | 멱등·안전 기술 적재본 | `apply.sql` — BEGIN…COMMIT(인간 승인 후), `WHERE 값이 실제 다를 때만` 가드(디지털 MINOR 교훈 반영·완전 무동작 멱등), 재실행 0행 |
| 3 | 롤백 | `rollback.sql` — 적용 전 원문(MES NULL) 복원 |
| 4 | 정규화·오염 방지 | `normalization-guard.md` — 3중 화이트리스트(table-spec 도메인·webadmin FK·260610 L1)·화이트리스트 밖 값 0 |
| 5 | CPQ option/template/constraint 반영 | `cpq-plan.md` — 장수 옵션·가공 택일그룹·삼각대색/링칼라 공정 param·봉투 사이즈매칭을 option_groups/templates/constraints로 설계 |
| 6 | 타임스탬프·변경이력 추적 | `apply.sql` — UPDATE=upd_dt=now()(MES). note 없는 테이블은 upd_dt만. 삼각대/링 논리삭제는 del_yn='Y'+upd_dt |

---

## 5. 미해소 컨펌 (인간 결정 필요)

- **CL-A** — 삼각대 거치를 신규 공정으로 만들까요(현 자재 MAT_000252/254 논리삭제), 아니면 부속 자재 유지할까요? (거치=공정 설계의도 vs 현 자재 적재 충돌) — C-01·C-CL-A
- **CL-B** — 트윈링 링(MAT_000253)을 공정 색값(param)으로 옮기고 자재행 논리삭제할까요? 탁상/미니의 링 부착(잉여)은 논리삭제 확정인가요? — C-02·C-03
- **CL-C** — 출력용지 계열(.01 국전계열) 적재경로 정합화 — load_master에 치수→계열 매핑 추가(재적재 퇴행 방지)에 동의하시나요? — C-04
- **CL-D** — 장수(C17)를 CPQ 옵션그룹으로 적재 — 택1(단일)인가요? 가격공식 바인딩은 round-2 디지털 가격엔진과 어떻게 합산하나요? — C-05
- **CL-E** — 디자인캘린더 종이 권위 — 명시 `몽블랑190g`(디자인판) vs `*별도설정`(업로드판), 같은 상품의 자재 권위는? — C-12
- **CL-F** — 미니탁상(109) 카테고리 — CAT_000113(전용 노드) 재연결 vs CAT_000112(엑셀 구분=탁상형) 유지? — C-14
- **CL-G** — 디자인캘린더 editor_yn=Y 일괄 UPDATE 진행? (file_upload_yn=Y 유지·2채널) — C-10

> **참고:** MES 채움(CL-G와 별개의 C-13)은 중복 0이 실측으로 입증돼 즉시적용으로 분류했으나, 디지털 산출처럼 "인간 승인 후 COMMIT" 원칙은 동일(apply.sql은 보유만, 자동 실행 금지).
