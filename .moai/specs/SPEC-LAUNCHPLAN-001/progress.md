# SPEC-LAUNCHPLAN-001 — 진행 기록

## §E.1 Plan-phase Audit-Ready Signal

```yaml
spec_id: SPEC-LAUNCHPLAN-001
version: "0.4.0"
plan_status: audit-ready
plan_complete_at: 2026-09-17
author: manager-spec
tier: M
req_count: 18
ac_count: 16
budget_exception: "REQ 한 축 +2 (AC 축은 예산 준수) — 리드 판정 ③ 승인, 기록 plan.md §A-3"
```

- 산출 5종: `spec.md`(GEARS 18항) · `plan.md`(마일스톤 5 · M0 3분할) ·
  `acceptance.md`(16 — §A D3 판정 001~014 · §B run 판정 015 · §C 사람 판정 016 ·
  추적 매트릭스 고아 0) · `research.md`(입력 5/5 · 원본 9종 · 미해결 입력 통합표 35건) ·
  `progress.md`(본 파일).
- SPEC ID 사전 검사: `[[ "SPEC-LAUNCHPLAN-001" =~ ^SPEC(-[A-Z][A-Z0-9]*)+-[0-9]{3}$ ]]` → `PASS`
  (Bash 실행 · 출력 인용).
- 미해소 `[NEEDS CLARIFICATION]` 마커: **0건**.
- 입력 **5/5 완결** — S5(`S/S5-live/`, 2026-09-17 01:30~01:33 KST · p2/p1 · 쓰기 0) 반영 완료.

### 계획감사 이력 (3/3 소진)

| 회차 | 점수 | 판정 | 처리 |
|---|---|---|---|
| review-1 | 64/100 | FAIL | 치명 4(C-1~C-4) · 주요 16 · 경미 7 → 26/29 해소 · 회귀 0 |
| review-2 | 79/100 | FAIL(1점 미달) | 차단 N-1(갭 상수) + N-2·N-3·N-4 + AC 병합 3건 + §6-1 + S5 반영 + 이월 3건 선반영 |
| review-3 | **88/100** | **PASS-WITH-NOTES** | R-1(AC-007(d) 범위) + R-4·R-5·R-6 텍스트 정정 |

추이 64 → 79 → 88 · 회귀 0건 · 신규 치명 0건.

### 잔존 기술부채 (드러내 놓고 이월 — 재감사 회차 소진)

| # | 내용 | 왜 지금 안 고치는가 | 닫는 지점 |
|---|---|---|---|
| R-2 | 선언 갭 수 == 실제 갭 표기 수 대조가 AC-LP-014 에 없음(현재는 「수 + 기준 + 원천 선언」만 검사) | AC 계약 변경이고 재감사 회차가 없음 | **run 단계** — D3 구현 중 자연히 손대는 자리. **sync-auditor 확인 대상** |
| R-3 | §6 앵커 순서 — 도식(`diag-option-price`)이 37행 메뉴 표보다 **뒤**에 옴. 「그림 한 장」이 목적인데 표가 먼저 보임 | 동일(AC 계약 변경) | **run 단계** — D1 조립 시 순서 조정 + D3 앵커 순서 검사 추가. **sync-auditor 확인 대상** |
| m-8 | `REQ-LP-016` 이 Go/No-Go·컷오버·하이퍼케어·RACI 4섹션을 한 조항에 과묶음 | **보류가 옳다고 감사 판정** — 분해 3안이 전부 REQ 예외를 +2 → +5 로 키우거나 문제를 절반만 품. `AC-LP-013` 이 소조건 (a)(b)(c)(d) 로 네 섹션을 각각 검사하므로 **진단 해상도는 이미 보존** | 이 SPEC 이 아님 — **다음 유사 SPEC 을 처음부터 4 REQ 로 쪼개서** 해소 |

### 미검증 사항 (run 단계 첫 확인 대상)

3회차 내내 닫지 못한 것들이다. 전부 **설계 검토이지 실행 관측이 아니다.**

1. **D1·D2·D3 미생성** — 모든 게이트 판정이 설계 검토다. 특히 `AC-LP-014(c)`(선언 문장 파싱) ·
   `AC-LP-005(a)`(요약 700자·6요소 라벨 추출) · `AC-LP-006(d)`(체크 방법 형태소 검사)는
   **jsdom 구현 난이도가 있고, 구현 불가로 판명되면 그 검사는 대리 지표로 퇴화**한다.
   → run M4 에서 구현 가능성을 먼저 확인하고, 불가하면 대리 지표로 낮추지 말고 **리드에
   보고**한다.
2. `R/R5/build_xlsx_v4.py` **미열람** — `AC-LP-015(c)`(654행 시트 보존) 실현 가능성 미확인.
   → run M3 착수 전 열람.
3. **`huni-product-lifecycle.md` · `huni-pricing-engine-map.md` 내용 미검토** —
   `AC-LP-014(d)` 의 「5단계」와 「`use_dims` 12축」이 두 정본의 실제 서술과 일치하는지
   **미검증**. → **run M0 에서 대조할 것.** 불일치 시 §6 도식 계약이 흔들린다.
4. `option-price-trace.md` 의 mermaid **렌더 가능성** 미확인(파싱 존재만 확인) ·
   §A-2 LOC 추정 미검증 · **교차모델 2차 의견 미호출**(Claude 단독 앵커).

### 다음 단계

Implementation Kickoff Approval(plan→run 인간 게이트) 대기. 승인 후 run 레인은
**M0(28구간 통합 상태판 · 3분할)** 부터 착수하며, 위 미검증 3번을 M0 안에서 먼저 닫는다.

## §E.2 Run-phase Evidence

### M0 — 28구간 통합 상태판 (2026-09-17 · run 레인)

#### Claim (주장)

1. SPEC 폴더 안 런타임 캐시 2종을 삭제했다.
2. `S/S5-plan/` 산출 디렉터리를 신설하고 `status-28.csv`(32행)를 생성했다.
3. plan 단계 「미검증 사항」 4건 중 **3건을 닫았다**(#1 #2 #3). #4 는 산출물 미생성으로 미개봉.

#### Evidence (증거 · 실행 명령 + 관측 출력)

**(1) 캐시 삭제** — 삭제 전 미추적 확인:
```
$ git ls-files .moai/specs/SPEC-LAUNCHPLAN-001/.moai .moai/specs/SPEC-LAUNCHPLAN-001/.claude
(출력 없음 = 전건 미추적)
$ rm -rf <두 디렉터리>
$ git status --short --branch
## WT-open-plan-rewrite
?? .moai/state/config-cache.json
?? .moai/state/context-usage.json
?? .moai/state/github/
```
`.moai/` 하위는 런타임 캐시 3파일(`config-cache.json`·`context-usage.json`·`github/counts.json`),
`.claude/` 하위는 빈 디렉터리 2개(`agent-memory/manager-spec`·`agent-memory/plan-auditor`)뿐이었다
— 빈 디렉터리는 git 이 표시하지 않으므로 `git status` 에 안 보였던 것이고, 미추적 판정과 모순되지 않는다.
워크트리 루트 `.moai/state/` 는 지시대로 **보존**(위 status 3행이 그 증거).

**(2) 상태판** — `S/S5-plan/status-28.csv` 생성. 생성기 자기검사 출력:
```
rows(데이터) = 32
분할 분포 : {'①': 10, '②': 11, '③': 11}
status 분포: {'작동': 2, '부분': 12, '구현-미검증': 5, '없음': 6, '미실측': 2, '미착수': 5}
작동 표기 행: ['A1', 'B2']
근거 빈칸 행: []
```
열 = `구간 · 구간명 · 분할 · 시스템 · 담당 · status · 10/6_필수여부 · 근거 · 실측일시 · 남은_일`
(plan §M0-6 의 7열 + `구간명`·`분할`·`실측일시` 3열. `분할` 은 plan §M0 이 요구한 ①②③ 표기,
`실측일시` 는 `CARDS-S.md` §2 공통 스키마의 `measured_at` 필수 규정을 승계).

**`작동` 표기 2행뿐**(A1 상품등록 · B2 옵션선택·실시간견적) — 둘 다 오늘 화면 관측 근거를 갖는다
(A1: 상품뷰어 296항목 렌더 @01:30:17 · B2: `<huni-widget>` 2건 Shadow DOM 렌더 + 부가세 포함가
7,700 / 44,130 @01:40:39~01:40:52). REQ-LP-018(화면 없이 `작동` 없음) 준수.

**(3) 미검증 #1 — D3 세 검사의 jsdom 구현 가능성 = 구현 가능. 대리 지표 불필요.**
프로토타입 실행 출력(Node v25.2.1 + jsdom 설치본):
```
005(a) 본문 글자수 = 25 · 700 이하 = true
006(d) 동사 = true · 화면지시어 = true · 길이 = 29 → true
014(c) 수 = 3 · 기준선언 = true · 원천경로 = true → true
```
세 검사 모두 DOM 조회 + 정규식으로 성립한다 — AC-LP-006(d) 의 「형태소 검사」는 실제로는
SPEC 이 토큰 목록(`열|누르|접속|조회|실행` · `화면|메뉴|페이지|콘솔|목록`)을 명시해 둔
**부분문자열 매칭**이므로 형태소 분석기가 필요 없다.

**(4) 미검증 #2 — `R/R5/build_xlsx_v4.py` 열람 완료. 654행 시트 보존 실현 가능.**
```
$ wc -l .../R/R5/build_xlsx_v4.py
     914
$ sed -n '3,5p'
R5 · 엑셀 9시트 생성기 (D-20 재기준선 · 2026-09-16)
원장 v4 CSV(654행 · 28열) → docs/huni/후니프린팅_통합IA_일정_역할분담_260916.xlsx
$ wc -l .../R/R2/unified-ledger-v4.csv
     655   (헤더 1 + 데이터 654)
```
D2(`build_xlsx_v4_plan.py`)는 이 스크립트의 포크로 성립한다. 차단 요인 없음.

**(5) 미검증 #3 — 두 정본 대조 완료. §6 도식 계약 흔들림 없음.**
```
$ grep -nE 'use_dims|12종' .claude/rules/moai/domains/huni-pricing-engine-map.md
203:**12종 선언값 전부 사용 중, 미사용 0.** 추가로 선언에 없는 토큰 2계열: opt_grp: 22건 · proc_grp: 31건
196:- use_dims 의 opt_grp:* 는 매칭 차원이 아니라 **스코프 선언**이며 판별차원 계산에서 제외된다(:778-781)
205:물리 컬럼 clr_cd(도수)가 unique 제약에는 있으나 _USE_DIM_CHOICES 12종과 NON_QTY_DIMS 9종에는
$ grep -nE '단계' .claude/rules/moai/domains/huni-product-lifecycle.md | head -1
24:## 1. 상품 하나가 팔리기까지 — 6단계
```
- `use_dims` **12축**은 정본과 일치(REQ-LP-017 · AC-LP-014(d)).
- 「5단계 vs 6단계」는 **모순이 아니다** — 6단계는 상품 생애주기(정본 §1), 5단계는 옵션↔가격 도식
  자체의 단계이고, `spec.md:201` 이 정본을 「§1 6단계」로 정확히 인용하고 있다.
- **D1 도식에 반영할 단서 2건**(정본 `:196`·`:205`): ① `opt_grp:*` 는 판별차원이 아니므로 도식이
  「12축 전부가 가격을 가른다」로 읽히면 안 된다 ② `clr_cd`(도수)는 12종·9종 어디에도 없다.

#### Baseline-attribution (baseline 귀속)

- 워크트리 `t47` HEAD = `13dffba8 feat(SPEC-LAUNCHPLAN-001): plan-phase artifacts (Tier M)`
  (5파일 1,194행) · `git rev-list --count --left-right origin/main...HEAD` = `0	1`
  (origin/main 대비 1 앞섬 · 뒤처짐 0) · 브랜치 `WT-open-plan-rewrite` · 미푸시.
- 상태판 입력 = S1~S5 카드 산출물 실측 문장. 모든 행의 `근거` 열은 그 원문 위치를 가리킨다
  (빈칸 0건 — 위 자기검사 출력).
- 라이브 관측 시각은 전부 S5(2026-09-17 01:29~01:42 KST) 승계 — run 레인이 새로 브라우저를
  열지 않았다(§Gaps 참조).

#### Gaps (미검증 — 이번에 관측하지 **않은** 것)

1. **ego-browser 재실측을 run 레인이 새로 하지 않았다.** plan §M0-2 가 「① 즉시 가능(p1·p2)은
   S5 로 사실상 완료 — 아래 4에서 소비만 한다」로 규정하므로 **소비**했다. 리드 dispatch 의
   「space 20 재실측」을 새 관측으로 읽으면 이 항은 미이행이다 — **리드 판정 필요**.
2. **p3 셀러어드민 · p4 구 사이트 미관측** → `D1`·`E1` 두 행은 `없음` 이 아니라 **`미실측`**.
   4관문(`CARDS-S.md` §3-2)을 통과하지 않은 상태에서 `없음` 을 적으면 근거 없는 결함 주장이 된다.
   plan §M0-4 의 문자 그대로는 ③ 에 `없음`/`미착수` 를 쓰라고 하지만, 두 행은 부재가 확인된 것이
   아니라 **보지 못한 것**이므로 상태 어휘를 하나 늘렸다 — **리드 판정 필요**(어휘 확장 승인 여부).
3. **회원 화면 24행 재실측 대기** — 이메일형 테스트 계정 미확보(S1·S5 공통 Missing Input).
   `B5`·`B6`·`B7`·`C7`·`D2` 가 이 계정 하나에 함께 걸려 있다(분할 ②의 대표 묶음).
4. **미검증 #4 미개봉** — `option-price-trace.md` mermaid 렌더 가능성 · §A-2 LOC 추정 ·
   교차모델 2차 의견. 전부 D1 산출물이 있어야 닫히므로 **M2/M4 로 이월**.
5. **jsdom 이 이 저장소에 없다** — 리포지토리에 `package.json` 이 없고 전역 설치도 없다
   (`npm ls -g --depth=0` 출력에 jsdom 부재). 프로토타입은 스크래치패드에 임시 설치해 돌렸다.
   D3 를 실제로 두려면 `S/S5-plan/` 에 `package.json` + `node_modules` 가 생긴다 —
   **커밋 대상인지 `.gitignore` 대상인지 리드 결정 필요**.

#### Residual-risk (잔여 위험)

- **R-α (신규)**: jsdom 의 HTML 파서는 `<table>` 밖의 `<td>` 를 **조용히 버린다**(프로토타입에서
  실제로 겪음 — `querySelector('td.check')` 가 `null`). D1 이 표를 온전한 `<table><tr><td>` 로
  내지 않으면 AC-LP-006 의 6칸 검사가 **0행을 읽고 조용히 통과**할 수 있다.
  → D3 에 「검사 대상 행 수가 0 이면 FAIL」 가드를 넣어야 한다. M4 착수 시 필수.
- **R-β**: 상태판의 `status` 판정은 카드 산출물의 문장을 승계한 것이고, run 레인이 코드를 다시
  읽어 교차검증하지는 않았다. 카드가 틀렸으면 상태판도 같이 틀린다.
- 기존 이월 R-2·R-3(plan §잔존 기술부채)은 **D1 조립(M2)·D3 작성(M4)** 에서 닫는다 — 미해소.

### M0-보정 — R 카드 근거 누락 교정 (리드 지적 수용)

#### Claim (주장)

최초 M0 는 **S1~S5 만 소비하고 R1a~R1e 를 읽지 않았다.** 그 결과 p3 셀러어드민·p4 구
사이트를 「미실측」으로 판정했으나, 두 화면은 **2026-09-16 에 ego-browser 로 이미 전수
관측되어 있었다.** 리드 지적을 수용해 R 근거가 닿는 **11행을 보정**했다.

#### Evidence (증거)

```
$ sed -n '/^## 2. 화면별 관측/,/^## 3/p' R/R1d/findings.md
| 10 | 전자 결제(PG) 설정 | /pg | 21:28 | 「전자결제(PG) 서비스를 신청해주세요」 … |
| 24 | 쇼핑몰 수정 | /mall/modification/81683 | 21:31 | 결제수단 노출설정 10항목 전부 「사용 안 함」 … |
| 30 | SMS 관리>사용설정 | … | 21:31 | SMS 사용설정 「사용 안 함」 · 발신번호 02-3409-8853 등록(비활성) |
| 31 | 알림톡 사용설정(회원) | … | 21:31 | 알림톡 사용 여부 「사용 안 함」 |
| 35 | 알림톡 템플릿 관리 | … | 21:35 | 등록 템플릿 0건 |
| 52 | 운영자 관리 | /admin/list | 21:33 | 1건: huniprinting·채훈희·마스터 |
| 53 | 권한그룹 관리 | /authorityGroup/list | 21:33 | 0건 |
| 72 | 기초정보 관리 | /service | 21:33 | 회사명·사업자등록번호·통신판매업신고번호·주소·업종/업태·개인정보책임자 빈칸 |
$ sed -n '/^## 4. 구 사이트/,/^## 5/p' R/R1a/findings.md
| 증빙서류발급 | /mypage/tax01.asp | 신청 가능 내역표 + 결제방법별 발행 가능표 + 안내 7항 | … |
```
관측 범위: R1a = p1 신규몰 + p4 구 사이트(9/16 21:26~21:34) · R1b = p2 webadmin(9/16) ·
R1d = p3 셀러어드민(9/16 21:26~21:41) · R1e = 스킨 저장소(9/16 21:50~22:05).

보정 11행: `D1` `E1`(리드 직접 지적) + `A4` `B5` `B6` `C6` `C7` `D2` `D3` `D4` `D5` `E3`.

보정 후 분포 — 분할 ①10 / ②13 / ③9 · status 작동 2 · 부분 14 · 구현-미검증 5 · 없음 6 ·
미착수 5 · **미실측 0** · 근거 빈칸 0 · 실측일시 빈칸 0.

#### 새로 드러난 사실 (보정이 낳은 것)

1. **B6 의 병목은 코드가 아니라 계약이다.** 스킨 stub 4행보다 앞서는 것은 셀러어드민의
   **전자결제(PG) 미신청**이다(R1d #10). 결제수단 노출 10항목도 전부 off(#24).
2. **E3 는 코드 결함 + 미입력 결함 두 겹이다.** 가입 약관동의 미전달(스킨)에 더해
   셀러어드민 기초정보 6항목이 전부 빈칸 — 전자상거래법 표시 의무 미충족(#72).
3. **C6 는 「없음」이되 수동 경로는 있다.** 자동 배선은 0이지만 셀러어드민에
   송장 저장·스캐닝 출고·묶음배송 화면이 실재한다(#61). 오픈 초기 수동 운영 판단이 가능하다.
4. **상품 수 분모가 셋으로 갈린다** — webadmin 296 / 셀러어드민 판매중 297 / 검색결과·대시보드 291.
5. **C7 은 구 사이트 대비 후퇴다** — 구 사이트는 12단계 공정 범례를 고객에게 노출했고
   신규몰은 샵바이 6상태다(R1a §4).

#### 어휘 확장 선언 (plan §M0-4 이탈 — 리드 승인)

plan §M0-4 는 분할 ③ 에 `없음`/`미착수` 를 쓰라고 규정한다. 상태 어휘에 **`미실측`** 을
추가했고 리드가 승인했다. 승인된 정의는 **「어느 시점의 관측도 없는 행」** 으로 좁혀진다
(R1/S5 어느 근거도 없는 경우만). 4관문(`CARDS-S.md` §3-2) 미통과 `없음` 표기 금지 원칙은
유지된다.

**보정 후 이 값을 쓰는 행은 0건이다** — `미실측` 은 예약 어휘로만 남는다. 최초판이 이
값을 쓴 것 자체가 R 카드 누락의 증상이었다.

#### 리드 판정 반영 (4건)

| # | 판정 | 반영 |
|---|---|---|
| 1 | S5 소비 인정 · D1·E1 「미실측」은 오판 | 위 보정 11행. 새 관측 의무는 **M4 전 1회** — D1 의 체크방법 문장을 p1·p2 에서 실제로 따라가 `URL@일시` 채우기(H절 AC) |
| 2 | `미실측` 어휘 승인(정의 축소) + 이탈 선언 기록 | 본 절에 기록 · 현재 사용 0건 |
| 3 | jsdom | `S/S5-plan/package.json` + lock 커밋 · `node_modules` 는 `S/S5-plan/.gitignore` 제외 · 설치 명령 1줄은 review-notes 에 — **M4 에서 실행** |
| 4 | R-α 가드 승인 | D3 에 「검사 대상 행 수 0 이면 FAIL」 + **검사마다 읽은 행 수 출력** — M4 필수 |

#### Gaps (보정 후 남은 미검증)

1. **p3 재실측은 여전히 대기** — 지니 로그인 필요(리드가 요청 중). D1·E1 은 9/16 근거로
   판정했고 분할 ③(관측 불가) → **②(재실측 대기)** 로 재분류했다.
2. **M4 전 새 관측 1회 미이행** — D1 체크방법 문장 따라가기(H절 AC)는 D1 HTML 이 있어야 한다.
3. 회원 화면 24행 · 미검증 #4 는 앞 절과 동일하게 미해소.

#### Residual-risk

- **R-β 강화**: 최초판이 R 카드를 통째로 놓친 사실은, 입력 원본 목록을 카드가 아니라
  **디렉터리 전수**로 확인해야 한다는 뜻이다. M1 착수 전 `R/`·`S/`·`L5/`·`L6/` 를 훑어
  소비 대상 원본 목록을 먼저 고정한다.

#### 다음 단계

M1(데이터 조립) 착수 가능. 단 위 Gaps 1·2·5 는 **리드 판정 대기** 항목이라 보고 후 진행한다.

### M1.5 — 매뉴얼 요소 대조 (진행 중 · ①③ 완료 · ② 착수 전)

#### Claim

1. **① 분모 산출 완료** — 159요소 / 50 화면단위. 산출 규칙을 문장으로 고정하고 재현 명령을 남겼다(상수 아님).
2. **③ p3 셀러어드민(NHN) 재실측 완료** — D1·E1 을 오늘 근거로 교체.
3. ② p2 webadmin 실클릭(116건)은 **착수 전**.

#### Evidence

**① 분모** — `manual-element-matrix.csv` 159행 생성(커밋 `d602ff2c`).
```
manual_content.py      SCREENS 17 · captures 28 · 콜아웃 97 · MODEL_ADMIN_SCREENS 10
widget_manual_content  SCREENS 24 · captures 32 · 콜아웃 52
합계 159 · 경로 종류 읽기 91 / 쓰기 43 / 읽기+쓰기 25 · 원문 위치(파일:줄) 미해결 0
```
산출 규칙: **요소 1건 = 매뉴얼이 `selector` 로 화면 위 한 지점을 지목하고 `label` 로 설명한 콜아웃 하나.**
콜아웃이 없는 화면은 화면 1건 = 요소 1건. `steps` 는 도달 조작이므로 분모 제외.
재현 명령은 커밋 `d602ff2c` 메시지에 전문 수록 — D1 이 선언하고 D3 가 4번째 인자로 받아 실행한다.

**③ p3 재실측** — `SHOPBY_ADMIN_*` 로그인(07:05~07:07 · 캡차·OTP 없음). 읽기 전용 · 저장 0.
- **진입 기법(신규 확인)**: 셀러어드민 본문은 `service.shopby.co.kr` 가 아니라
  **`enterprise-remote.shopby.co.kr` iframe** 안에 있다. 직접 URL(`/service`·`/pg`)로는 좌측 메뉴만
  렌더되고 폼이 뜨지 않는다 — **메가메뉴를 열어 항목을 클릭**해야 iframe 이 이동한다.
  메뉴 항목은 접힌 상태에서 `x = -105`(화면 밖)이라 `전체메뉴 열기` 선행이 필수다.
- **E1 기초정보**(@07:40:49 · `…/configuration/basic/info`): 회사명 **빈칸**(0/45) · 사업자등록번호 **빈칸**(0/10) ·
  전자상거래업자 부호 **빈칸** · 통신판매업 신고번호 **빈칸**(0/20) · 업종·팩스 빈칸 · 대표자명 「대표 운영자」.
  **채워진 2건이 오히려 문제** — 대표전화번호 `010-****-3762`(개인 휴대폰 · 푸터 `02-3409-8853` 과 불일치) ·
  대표이메일 `****@gmail.com`(**S6 §4 가 목업 더미 계열로 지목한 주소가 라이브 설정에도 존재**).
- **D1 SMS**(@07:41:20): SMS 사용설정 **「사용 안 함」** · 발신번호 `0234098853` 등록(비활성) · 잔여 1,000P.
- **D1 알림톡**(@07:42:09): 알림톡 사용 여부 **「사용 안 함」** · 채널 `@huniprinting`·발신프로필키 등록됨 ·
  회원 관련 자동 알림톡 중 「사용함」 4건뿐 — 9/16 R1d 와 동일.
  화면 명시 의존 2건: 「SMS 발신번호가 등록되어야 카카오톡 채널 등록·알림톡 발송 가능」 ·
  **「템플릿은 카카오 검수 승인 후에만 발송 가능」** ← 심사 리드타임이 임계경로인 근거.

#### Baseline-attribution

워크트리 `t47` HEAD = `d602ff2c`(직전 `1779bf67` plan 레인 SPEC 수정 착지 · 리드 검산 완료).
상태판 오늘 관측 반영 행 28 / 32. 라이브 쓰기 0(저장·토글·발송 0).

#### Gaps

1. **② p2 webadmin 실클릭 116건 미착수** — M1.5 의 본체다. 리드 판정: 159 전건 대상, 실클릭은
   읽기 91 + 읽기+쓰기 25 의 읽기 부분 = 116, 쓰기 43 은 `쓰기경로-dev환경필요`.
   순서는 webadmin 107 → 위젯빌더 52.
2. **알림톡 「주문배송 관련」 탭 미열람** — 회원 관련 탭만 봤다. 15항목 전부 「사용 안 함」은 9/16 R1d 승계.
3. **레드프린팅 회원 흐름 미검증** — 자격증명 로그인 실패(잠김 위험으로 재시도 안 함).

#### Residual-risk

- **iframe 진입 기법이 M1.5-② 전체의 전제**다. webadmin(`huni-admin.printly.co.kr`)은 iframe 구조가
  아닐 가능성이 높지만 확인 전이며, 만약 같은 구조라면 화면당 조작 수가 늘어난다.
- 매트릭스 159행의 `경로 종류`는 콜아웃 **label 의 어휘**로 분류했다(저장·등록·삭제 → 쓰기).
  실제 화면에서 읽기 전용인데 쓰기로 분류된 행이 있을 수 있다 — ② 실측 중 교정한다.

### 푸시 전 체크 (sync 레인 인계 · 리드 판정 260917)

| # | 항목 | 왜 | 처리 주체 |
|---|---|---|---|
| 1 | 커밋 `7abdcdba` 히스토리에 **평문 연락처 2건**(대표전화·대표이메일)이 남아 있다 | 작업 파일은 마스킹했으나 이력은 그대로다. 지금 재작성하면 두 레인이 공유한 브랜치를 흔들어 금지 | **푸시 결정 시 sync 레인**이 squash / filter 로 제거. 그 전까지 브랜치는 **내부 워크트리 전용 · 푸시 0** |

### M1.5-② webadmin 실클릭 — 중단 지점 (지니 /clear 지시 · 2026-09-17 07:58)

#### 진행 결과

| 축 | 값 |
|---|---|
| 매트릭스 전체 | 159 요소 |
| **동작확인(URL@일시)** | **37** |
| **쓰기경로-dev환경필요** | **43**(클릭하지 않음 — 규정대로) |
| **미실측** | **79** (49.7% · AC-LP-014(e)(vi) 상한 30% **미달성 — 재개 필요**) |
| webadmin 107 중 | 동작확인 37 · 쓰기경로 32 · 미실측 38 |
| 위젯빌더 52 | 전건 미착수 |
| run 레인 실제 클릭 | webadmin step 9회 + p3 메뉴 4회 + p1 13회 |

#### [사고 보고] category-master 에서 삭제 확인 다이얼로그를 띄웠다 — 피해 없음

**무슨 일**: 매뉴얼이 규정한 선행 조작 `click .row` 를 `.row >> nth=0` 으로 실행했는데,
`/admin/category-master/` 에서 **「엽서/카드 카테고리를 삭제할까요? (논리삭제)」 확인 창**이 떴다.
스크립트가 다이얼로그를 **자동 수락(acceptDialog)** 하도록 짜여 있었다.

**원인**: 그 화면의 `.row` 는 내부에 삭제 컨트롤을 품고 있다 —
`<div class="row"> … <span class="del" onclick="delRow(event,'CAT_000001')">삭제</span> …`.
행 중앙 클릭이 그 자식에 닿았다. **매뉴얼의 step 자체가 이 화면에서는 쓰기 경로를 건드린다.**

**피해 확인(즉시 실측 · 2026-09-17 07:58:16~07:58:48)**:
- webadmin `/admin/category-master/` — **엽서/카드 `CAT_000001` 존재** · 하위 4 · 상품 18 · 전체 16행 유지.
- 고객 화면 `shopby.huniprinting.co.kr/` — **GNB 12 카테고리 전부 정상**(엽서/카드 포함).
→ **삭제되지 않았다.** 다만 「수락이 발화하지 않았음」을 증명한 것이 아니라
  **「두 화면 모두에 그대로 존재함」을 관측**한 것이다(VCI §1 — 관측한 것만 적는다).

**재개 시 의무**: 선행 조작을 `.row` 중앙이 아니라 **`.row .nm` 등 안전한 자식**으로 좁히고,
클릭 전에 `.del`·`.edit` 자식 유무를 먼저 검사한다. **다이얼로그 자동 수락 코드를 제거**하고,
다이얼로그가 뜨면 그 요소는 `미실측(사유)` 로 둔다. 매트릭스 해당 행 note 에 경고를 남겼다.

#### [손실] 첫 6화면 16요소의 증거가 저장되지 않았다

`product-viewer` · `…/options/OPT-000010/?_popup=1` · `sku-catalog` · `set-products` ·
`tmpl-combo-md` · `paper-management` — 콘솔에는 **16/16 확인**으로 찍혔으나, 그 직후
다이얼로그로 프로세스가 종료돼 결과 JSON 이 **기록되지 않았다**. URL@시각 증거가 없으므로
**기억으로 채우지 않고 `미실측` 으로 둔다.** 재개 시 이 6화면을 먼저 다시 돈다.

#### M1.5-② 재개 지점

| 항목 | 값 |
|---|---|
| 매트릭스 | `_workspace/huni-launch-runway/07_rebaseline/S/S5-plan/manual-element-matrix.csv`(159행 · 10열) |
| 읽는 법 | 같은 폴더 `matrix-README.md` — 분모 규칙·재현 명령·두 분모 분리·결과 4종·iframe 기법 |
| **다음 화면(1순위)** | 위 6화면 재실측(16요소) → 이어서 `{code}` 치환이 필요한 product-viewer 하위 3화면(options·constraints·templates) + `price-viewer/comp/{code}/edit` + `price-viewer/{code}/diagram` |
| 그다음 | `MODEL_ADMIN_SCREENS` 10건(경로 없음 — Django 표준화면) |
| 마지막 | 위젯빌더 52요소 — 대표 상품 1개(프리미엄명함)로 캔버스 상태를 만든 뒤 속성 패널 순회 |
| p2 로그인 | `huni-admin.printly.co.kr` **유지 중**(iframe 아님 · 직접 URL 로 바로 열림) |
| p3 로그인 | `service.shopby.co.kr` **유지 중**(`SHOPBY_ADMIN_*`) · 본문은 `enterprise-remote` iframe · 메가메뉴 클릭 필요 |
| p1 로그인 | 회원 세션 유지 중(이메일형 계정) |
| 남은 지시 | 알림톡 **「주문배송 관련」 탭** 15항목을 ② 도는 김에 오늘 시각으로 갱신(리드 판정 3) |
| **M2 주의** | `8dc0a618` 기준 `acceptance.md` 를 **다시 읽고** `data-owner ∈ {nhn,huni,ext}` · `data-work ∈ {config,dev,wait}` 적용. nhn·ext 는 `dev` 금지 · `wait` 은 `ext` 전용 · `ext×wait` 집합 = §8 선행/외부 대기 항목 |
| 브랜치 | `WT-open-plan-rewrite` · 워크트리 `.claude/worktrees/t47` · **유일 사본 · 푸시 0** |

### M1.5-② webadmin·위젯빌더 실클릭 — 완료 (2026-09-17 08:12~08:27 KST · run 레인)

#### Claim

1. **매트릭스 159 요소가 닫혔다** — `미실측` **0건**(0.0%). AC-LP-014(e)(vi) 상한 30% 충족.
2. 유실됐던 첫 6화면 16요소를 **재실측**했고, `{code}` 치환 하위 화면·`MODEL_ADMIN` 10건·위젯빌더 52를 전부 돌았다.
3. **라이브 쓰기 0.** 저장·삭제·게시·토글 0회. 확인창은 **한 번도 수락하지 않았다.**
4. 남은 지시 1건(알림톡 「주문배송 관련」 15항목 갱신)은 **미완** — p3 렌더러 정지 + 브라우저 제어권 사용자 이관으로 중단.

#### Evidence

`manual-element-matrix.csv` 159행 재집계 — **동작확인 116 · 쓰기경로-dev환경필요 43 · 미실측 0**.
동작확인 116행 전부 `2026-09-17` 시각을 달고 있다(오늘 근거 100%).

| 분모 | 전체 | 동작확인 | 쓰기경로-dev | 미실측 |
|---|---|---|---|---|
| webadmin | 107 | 75 | 32 | **0** |
| 위젯빌더 | 52 | 41 | 11 | **0** |
| **합계** | **159** | **116** | **43** | **0** |

화면단위 50/50 실측. 실클릭 대상 116 = 읽기 91 + 「읽기+쓰기」 25 의 읽기 부분 — 리드 판정과 일치.

재실측한 구간과 근거 URL@시각:

- **유실 6화면 16요소**(08:12:18~08:12:34) — `/admin/product-viewer/` 4 · `…/PRD_000046/options/OPT-000010/?_popup=1` 1 ·
  `/admin/sku-catalog/` 1 · `/admin/set-products/` 4 · `/admin/tmpl-combo-md/` 4 · `/admin/paper-management/` 5. 전건 실재.
- **`{code}` 치환 하위 화면**(08:13:48~08:15:52) — `product-viewer/{code}/{options,constraints,templates}` ·
  `price-viewer/comp/{code}/edit` · `price-viewer/{code}/diagram` · `category-master`.
- **`MODEL_ADMIN` 10건**(08:16:47~08:16:59, 고객·가격공식 2건 08:28 재판정) — Django 표준 목록 10화면 전부 렌더.
- **위젯빌더 41요소**(08:24:19~08:27:13) — `widget-manager` 목록·상세 · `widget-templates` · `widget-builder`
  (넓은 화면 32 + 좁은 화면 6). 빌더는 `?prd=PRD_000031`(프리미엄명함 · `WGT_000249` · 캔버스 13항목)로 상태를 만들었다.

**[HARD] 3항 준수 증거** — 프로브 스크립트에 셋 다 코드로 박았다(`scratchpad/m15/probe.mjs`):
① `acceptDialog` 를 **코드에서 제거**했다. 확인창이 뜨면 `dismiss` 후 그 화면을 `미실측(사유)` 로 둔다.
② 선행 클릭은 `.row` 중앙이 아니라 `.nm`·`.name`·`.tit` 등 안전 자식으로 좁힌다.
③ 클릭 전 대상 안에 `.del`·`.edit`·`[onclick*=del]`·`[onclick*=save]` 가 있으면 **클릭하지 않는다.**
이번 순회에서 ②③ 가드가 발동해 클릭을 막은 화면은 없었다(대상 selector 가 전부 쓰기 컨트롤을 품지 않았다).
`category-master` 는 **선행 클릭 없이** `.row` 존재만 확인해 지나갔다 — 07:58 사고 재발 0.

**07:58 사고 대상 재확인**(08:14:00) — `/admin/category-master/` 에 `엽서/카드 CAT_000001` 존재 · 전체 16행 유지.

#### Baseline-attribution

워크트리 `t47` 시작 HEAD = `9ae7b8fb`. 이 블록의 모든 수치는 **갱신 후** `manual-element-matrix.csv` 를
재집계해 얻었다(상수 전사 아님). 재현:

```bash
python3 -c "import csv;from collections import Counter;\
rows=list(csv.DictReader(open('_workspace/huni-launch-runway/07_rebaseline/S/S5-plan/manual-element-matrix.csv')));\
print(len(rows), Counter(r['실측 결과'].split('(')[0] for r in rows))"
```

브라우저는 기존 ego-browser space 20 을 이어받았다(p2 webadmin 세션 유지 · 신규 로그인 0).

#### Gaps

1. **알림톡 「주문배송 관련」 15항목 미갱신.** p3 에서 「주문배송 관련」 탭 앵커를 클릭하자 **확인창이 떴고
   수락하지 않았다**([HARD] 1). 그 직후 p3 렌더러가 응답 불능이 됐고(`Runtime.evaluate` 타임아웃),
   새 탭으로 복구하려던 차에 **브라우저 제어권이 사용자에게 이관**(하드 스톱)됐다. 9/16 R1d 의
   「15항목 전부 사용 안 함」은 **승계값이며 오늘 시각이 아니다.**
2. **`동작확인` 의 뜻은 「selector 실재 + 가시」까지다.** 콜아웃 `label` 이 설명하는 **동작 자체**(눌렀을 때
   무엇이 열리는지)는 쓰기 위험 때문에 검증하지 않았다. 「전수 실측」이라 부르지 않는다.
3. **`{code}` 치환 화면은 대표 1건 실측**이다 — `PRD_000046`(옵션·제약) · `PRD_000001`(템플릿) ·
   `PRD_000145`(가격공식) · `COMP_ACC_ENV_OPP_NADH`(단가표). 전 상품 순회가 아니다.
4. **레드프린팅 회원 흐름** 여전히 미검증(자격증명).

#### Residual-risk

- **`{code}` 자리에 데이터 없는 상품을 넣으면 없는 결함이 보인다.** `PRD_000046/templates` 는 `.lnk.edit` 0건,
  `PRD_000223/diagram` 은 `단가표 편집` 0건이었으나, **둘 다 화면 결함이 아니라 그 상품에 데이터가 없어서**였다
  (템플릿 보유 `PRD_000001` → 11건 · 가격공식 보유 `PRD_000145` → 1건). 「선행 조작을 빼면 없는 결함이
  보인다」의 쌍둥이다 — `matrix-README.md` 에 교훈으로 올렸다.
- **느슨한 `text=` 매처가 거짓양성을 낸다.** 첫 순회에서 상위 컨테이너가 자식 텍스트를 품어 `＋ 템플릿 지정`·
  `템플릿 목록에서 빼기` 가 「있음」으로 잡혔다. 리프 노드 전용으로 고친 뒤 재실측했고, 그 결과
  `템플릿 목록에서 빼기`·`이름 저장` 은 **`.tm-row` 선행 클릭 뒤에만** 뜬다는 것이 드러났다.
  **첫 판정은 폐기하고 두 번째 값을 채택했다.**
- **`#wb-pal-pin2` 는 고정 상태에서 0×0** 이다. 매뉴얼이 규정한 「겹쳐 열림」을 만들려면 `#wb-pal-pin`(고정 해제)
  → `panelOpen('pal')` 가 선행돼야 한다. 뷰 상태 변경이라 실행했고 **`#wb-pal-pin2` 로 원상복구**했다
  (`aria-pressed=true` 재확인). DB 쓰기 아님.
- p3 정지 원인을 단정하지 않는다. 확인창을 수락하지 않았으므로 **저장·이동은 일어나지 않았다** — 관측한 것은
  「렌더러가 응답하지 않는다」뿐이다.

### M1.5-② 잔여 — 알림톡 「주문배송 관련」 재실측 (2026-09-17 08:47 KST · 지니 제어권 반환 후)

#### Claim

1. 「주문배송 관련」 탭의 자동 알림톡 항목은 **33건**이며 **전건 「사용 안 함」**이다(오늘 시각).
2. **지시서·9/16 R1d 가 말한 「15항목」과 개수가 다르다.** 15 가 어디서 나온 수인지는 관측하지 않았다.
3. 라이브 쓰기 0 — 라디오·저장 버튼을 누르지 않았다.

#### Evidence

`service.shopby.co.kr/pro/premium/kakao/notification/config?…&templateType=ORDER_DELIVERY`
@ 2026-09-17 08:47 KST. 33건 전부 `사용 안 함`:

| 계열 | 건수 | 항목 |
|---|---:|---|
| 주문·결제·배송 | 19 | 입금대기안내(무통장입금/가상계좌) · 주문접수완료(신용카드 등) · 입금지연취소안내 · 상품발송완료 · 구매확정요청(예정)안내 · 품절취소안내 · 취소승인완료 · 반품신청완료 · 반품수거완료 · 이미출고(취소/교환요청) 안내 · 반려처리(취소/반품/교환요청) 안내 · 배송지 입력 안내 · 반품승인완료 · 취소신청완료 · 교환신청완료 · 교환승인완료 · 입금확인안내 · 배송지미입력취소안내 · 배송보류 안내 |
| 정기배송 | 14 | 정기배송 상품 결제 예정/결제 실패/신청/주문 접수완료/발송완료/일시정지/일시정지 해제/회차 건너뛰기/일정 변경/해지/배송지입력/선물수락완료/자동 해지/변경 동의 안내 |

같은 화면에서 **「알림톡 사용 여부」 자체가 「사용 안 함」**임을 함께 확인했다(07:42 관측과 동일).
⇒ 개별 항목을 켜도 상위 스위치가 꺼져 있으면 발송되지 않는다.

**판독 방법**(README § iframe 기법 보강): 라디오 선택 상태는 스냅샷에 안 나온다. 본문이
교차 출처 iframe 이라 `page.evaluate` 로도 못 읽는다. **`Page.createIsolatedWorld` 로 그 프레임에
격리 월드를 만들고 `Runtime.evaluate` 로 `input[type=radio].checked` 를 읽었다.**
스크린샷으로 육안 교차 확인했다(보이는 12건 전부 「사용 안 함」 — DOM 판독과 일치).

#### Baseline-attribution

앞 시도(08:3x)는 `service.shopby.co.kr` 의 **기존 탭(p3)에서 앵커를 클릭**해 확인창을 띄웠고,
[HARD] 1 대로 수락하지 않았다. 이번에는 **새 탭(p10)에 `templateType=ORDER_DELIVERY` 를 직접 열어**
확인창 없이 도달했다 — README 의 「직접 URL 은 좌측 메뉴만 뜬다」는 **셀러어드민 메가메뉴 계열에 해당하고,
알림톡 탭 전환에는 해당하지 않는다.**

#### Gaps

1. **「15항목」의 출처를 확인하지 못했다.** 9/16 R1d 원문을 다시 읽지 않았고, 33 vs 15 의 차이가
   ① 그때는 정기배송 계열이 없었는지 ② 다른 탭을 센 것인지 ③ 부분만 센 것인지 **관측하지 않았다.**
   **33 을 오늘 값으로 쓰고, 15 는 인용하지 않는다.**
2. 「문의 관련」 탭은 이번에도 열지 않았다.

#### Residual-risk

- 상위 스위치(알림톡 사용 여부)가 꺼져 있어 **개별 항목 33건의 on/off 는 현재 발송에 영향이 없다.**
  오픈 시 켜는 순간 이 33건 설정이 그대로 발효되므로, 켜기 전에 항목별 의사결정이 필요하다.
- 카카오 검수 승인 전에는 템플릿을 켜도 발송되지 않는다(07:42 화면 명시) — 임계경로는 검수 리드타임이다.

### M1 — 데이터 조립 (착수 · 중단 · 2026-09-17 09:0x KST · 지니 세션 종료 지시)

#### Claim

1. **654행 원장 → 28구간 매핑 규칙을 완성하고 검증했다** — `UNMAPPED 0`.
2. `plan-rows.csv` 본체는 **미작성**. M1 의 1~4 항 중 **1항의 절반**(구간 귀속)까지다.
3. 「15항목」 의문은 리드가 닫았다 — 9/16 스크린샷 **하단 잘림**에 따른 부분 집계였다.

#### Evidence

`S/S5-plan/stepmap.py` — `(대분류, 중분류) → 구간` 규칙 88개 + `구간 → 트랙` 표(plan.md §D).
654행 전건 매핑, 규칙에 없는 조합 0:

```
행 654 · UNMAPPED 0
구간: A1 33 · A2 23 · A3 19 · A4 6 · A5 41 · B1 3 · B2 33 · B3 18 · B4 8 · B5 24 ·
      B6 38 · B7 12 · C2 2 · C3 17 · C4 48 · C5 78 · C6 25 · C7 18 · D1 12 · D2 40 ·
      D3 28 · D4 17 · D5 31 · E1 56 · E3 5 · E4 19
트랙: T2 132 · T3 194 · T4 188 · T5 90 · T6 31 · T7 19
```

규칙은 **파일 한 곳에만** 둔다. 추측으로 매달지 않고, 규칙에 없는 조합은 `UNMAPPED` 로 남겨
사람이 보게 했다 — 이번 실행에서는 0이었다.

읽은 입력(전건 읽기 전용 · 메인 절대경로): `R/R2/unified-ledger-v4.csv`(654) ·
`R/R3/decisions-for-pm.md`(결정 30건 · A 7 / B 13 / C 4 / D 6) ·
`S/S1-skin/api-wiring.csv`(84) · `S/S2-pipeline/pipeline-status.csv`(30) ·
`S/S5-live/menu-map-260917.csv`(37) · `S/S5-plan/status-28.csv`(32) ·
`research.md` §4(미해결 35 · 차단 Y 24 · T1 인프라가 9건으로 최다).

#### Baseline-attribution

HEAD `e7b79677`(직전 `e1b99e22` M1.5-② 완료). `8dc0a618` 이후 `spec/plan/acceptance.md`
변경 **0건**을 `git log 8dc0a618..HEAD -- <3파일>` 로 확인하고 시작했다 — 재독 기준이 드리프트하지 않았다.

#### Gaps

1. **`plan-rows.csv` 미작성.** 남은 일은 M1 의 1항 나머지(트랙별 최상위 3~5행 선정) ·
   2항(나머지 `detail` 강등 · `std_id` 다중값 매달기) · 3항(결정 30건 구간 귀속) ·
   4항(차단 24건 §8 배치)이다.
2. **T1 은 원장에 행이 없다** — 원장은 기능 원장이고 T1(E2·F1~F4)은 인프라라 `S4-infra` 에서 온다.
   E2 로 갈 실제 `std_id` 후보 4건을 찾아 뒀으나 **`stepmap.py` 에 반영하지 않았다**:
   `STD-SYS-024`(보호 라우트가 vercel.app 으로 튐) · `STD-SYS-036`(도메인·SNS 콜백 URL 정합) ·
   `STD-SYS-052`(환경변수 정합) · `STD-SYS-040`(발송전용 메일 도메인).
   현재 규칙은 이 넷을 분류상 `E1`/`B5` 로 보낸다 — **std_id 단위 override 를 넣어야 한다.**
   넣지 않으면 T1 최상위 행이 전부 `NEW` 가 되고, AC-LP-007(c) 의 「NEW ≤ 최상위 30%」를 압박한다.
3. 최상위 행의 **증거·담당·목표일**을 아직 하나도 채우지 않았다. AC-LP-007(a)(b) 는 증거 실재를
   검사하므로 **경로가 실제로 있는지 확인하며** 채워야 한다.

#### Residual-risk

- **매핑은 분류 기반이라 개별 행에서 어긋날 수 있다.** 88개 규칙이 654행을 덮었다는 것은
  「빠진 조합이 없다」는 뜻이지 「행마다 옳다」는 뜻이 아니다. 최상위 행에 `std_id` 를 매달 때
  그 행 몇 개를 표본으로 열어 보는 편이 안전하다.
- `C5` 78건 · `E1` 56건 · `C4` 48건이 크다. 이 구간들은 `detail` 로 접히는 양이 많아
  AC-LP-007(d)(전 행 `작동` 규율)의 사정권이 넓다 — `작동` 을 적을 때 화면 근거를 반드시 단다.

### M1 — 데이터 조립 (완료 · 2026-09-17 10:54 KST · run 레인 · 리드 dispatch 재개)

#### Claim (주장)

1. `plan-rows.csv` 본체 완성 — **735행 = 최상위 28 + detail 707**(원장 654 + S4 런북 27 + 차단 입력 26). 트랙별 최상위 T1 4 · T2 4 · T3 5 · T4 5 · T5 4 · T6 3 · T7 3(전 트랙 3~5).
2. T1 std_id override 4건(`STD-SYS-024·036·052·040` → E2)을 `stepmap.py` `STD_OVERRIDE` 로 반영 — 원장 매핑 `UNMAPPED 0` 유지.
3. 결정 30건 → 구간 귀속(`decisions-by-step.csv`) · 오픈 전 필수 17.
4. 차단 입력 → §8 재료: 외부 대기(`data-owner=ext × data-work=wait`) 46행을 `wait-items.csv` 로 분리. 내부 결정·확인 차단은 wait 가 될 수 없어 detail 행(`huni`/`nhn` × `config`)으로 두고 D1 §8 「내부 선행 결정」 목록 재료로 쓴다(리드 승인).
5. 최상위 행 28건 전부 증거 실재(`파일:줄` 존재 + 줄 번호 ≤ 총 줄 수) · 담당 7실명/`외부(…)` · 체크 방법 형식 · NEW 5/28(18%) · `nhn/ext×dev` 0 · `wait×비ext` 0 · `작동` 115행 전부 `URL@일시`(≥2026-09-16) — 생성 스크립트 자체 점검 통과(판정은 M4 D3 가 따로 한다).
6. 새 축(지니 260917 · 리드 정정): `api-path ∈ {server-api, shop-api, admin-manual, none}` + `api-evidence`(`<파일명> <METHOD> <path>`) + `forced_by_impl` + `api_basis` 열.
7. 목표일 = 「가장 이른 완료일(하한)」(리드 조건부 승인 (가)) — 근무일 달력은 한국천문연구원 특일정보 응답 원문으로 코드 계산.

#### Evidence (실행 명령 + 관측 출력)

```
$ cd _workspace/huni-launch-runway/07_rebaseline/S/S5-plan && python3 -B build_plan_rows.py; echo exit=$?
exit=0
행 735 = 최상위 28 + detail 707 (원장 654 + 런북 27 + 차단입력 26)
트랙별 최상위: {'T1': 4, 'T2': 4, 'T3': 5, 'T4': 5, 'T5': 4, 'T6': 3, 'T7': 3} · NEW 최상위 5/28
차단 Y(research §4 표 직접 집계): 26 Counter({'S4': 9, 'S3': 7, 'S1': 5, 'S2': 5})
상태: {'미착수': 413, '부분': 138, '없음': 6, '구현-미검증': 17, '대기': 16, '작동': 115, '미실측': 30}
소유×분류: {('huni', 'dev'): 579, ('huni', 'config'): 67, ('nhn', 'config'): 43, ('ext', 'wait'): 46}
api-path: {'none': 610, 'server-api': 14, 'shop-api': 66, 'admin-manual': 45}
admin-manual forced_by_impl: {'N': 31, 'Y': 14}
외부 대기(ext×wait): 46
  T1-1  서희항          하한     0일 · 1주+ 0 · 미판정 0 · 미완 0 → 미산정(런북 단계 수 5 · 원장 미완 행 없음) (임계경로 미산정 구간)
  T1-2  서희항          하한     0일 · 1주+ 0 · 미판정 0 · 미완 0 → 미산정(런북 단계 수 4 · 원장 미완 행 없음) (임계경로 미산정 구간)
  T1-3  서희항          하한     0일 · 1주+ 0 · 미판정 0 · 미완 0 → 미산정(런북 단계 수 10 · 원장 미완 행 없음) (임계경로 미산정 구간)
  T1-4  김동학          하한   3.0일 · 1주+ 0 · 미판정 0 · 미완 4 → 2026-09-21 (가장 이른 완료일(하한) · 런북 8단계 미산정 제외)
  T2-1  최숙진          하한  21.0일 · 1주+ 0 · 미판정 0 · 미완 20 → 2026-10-21 (가장 이른 완료일(하한))
  T2-2  최숙진          하한   5.0일 · 1주+ 0 · 미판정 0 · 미완 8 → 2026-10-28 (가장 이른 완료일(하한))
  T2-3  서희항          하한   6.5일 · 1주+ 0 · 미판정 0 · 미완 5 → 2026-09-29 (가장 이른 완료일(하한))
  T2-4  서희항          하한  35.0일 · 1주+ 1 · 미판정 0 · 미완 29 → 2026-11-11 (가장 이른 완료일(하한))
  T3-1  김동학          하한     1일 · 1주+ 0 · 미판정 0 · 미완 1 → 2026-09-17 (가장 이른 완료일(하한))
  T3-2  김동학          하한  17.0일 · 1주+ 1 · 미판정 0 · 미완 21 → 2026-10-15 (가장 이른 완료일(하한))
  T3-3  김동학          하한  56.5일 · 1주+ 6 · 미판정 2 · 미완 39 → 2027-04-05 (가장 이른 완료일(하한))
  T3-4  김동학          하한  79.5일 · 1주+ 2 · 미판정 2 · 미완 66 → 2027-01-15 (가장 이른 완료일(하한))
  T3-5  신우진          하한  74.0일 · 1주+ 11 · 미판정 0 · 미완 27 → 2027-01-06 (가장 이른 완료일(하한))
  T4-1  김동학          하한     0일 · 1주+ 0 · 미판정 0 · 미완 0 → 미산정(원장 미완 행 없음) (임계경로 미산정 구간)
  T4-2  서희항          하한  15.0일 · 1주+ 1 · 미판정 2 · 미완 12 → 2027-04-26 (가장 이른 완료일(하한) · 선행 T4-1 미산정/회신 대기분 0 으로 계산)
  T4-3  서희항          하한   4.0일 · 1주+ 0 · 미판정 0 · 미완 5 → 2026-09-22 (가장 이른 완료일(하한))
  T4-4  서희항          하한  12.5일 · 1주+ 1 · 미판정 0 · 미완 8 → 2026-10-15 (가장 이른 완료일(하한))
  T4-5  최숙진          하한   6.5일 · 1주+ 0 · 미판정 0 · 미완 7 → 2026-10-23 (가장 이른 완료일(하한))
  T5-1  최숙진          하한  77.5일 · 1주+ 12 · 미판정 0 · 미완 33 → 2027-01-12 (가장 이른 완료일(하한))
  T5-2  최숙진          하한   6.5일 · 1주+ 0 · 미판정 0 · 미완 4 → 2026-09-29 (가장 이른 완료일(하한))
  T5-3  김동학          하한  24.5일 · 1주+ 1 · 미판정 1 · 미완 19 → 2026-10-27 (가장 이른 완료일(하한))
  T5-4  신우진          하한  13.0일 · 1주+ 1 · 미판정 1 · 미완 8 → 2027-01-29 (가장 이른 완료일(하한))
  T6-1  채훈희          하한  12.0일 · 1주+ 2 · 미판정 0 · 미완 5 → 2026-10-07 (가장 이른 완료일(하한))
  T6-2  외부(NHN커머스)   하한   1.5일 · 1주+ 0 · 미판정 0 · 미완 2 → 회신 요청 2026-09-18 · 회신 시점 미정 (외부 대기 — 소요 0)
  T6-3  외부(토스페이먼츠)   하한  22.0일 · 1주+ 2 · 미판정 0 · 미완 13 → 회신 요청 2026-09-18 · 회신 시점 미정 (외부 대기 — 소요 0)
  T7-1  신우진          하한  17.5일 · 1주+ 1 · 미판정 0 · 미완 11 → 2026-10-21 (가장 이른 완료일(하한) · 선행 T1-1·T1-2·T1-3 미산정/회신 대기분 0 으로 계산)
  T7-2  신우진          하한     1일 · 1주+ 0 · 미판정 0 · 미완 1 → 2027-04-06 (가장 이른 완료일(하한))
  T7-3  채훈희          하한     0일 · 1주+ 0 · 미판정 0 · 미완 0 → 미산정(원장 미완 행 없음) (임계경로 미산정 구간)
  담당자 직렬(가정) 김동학: 하한 294.0일 → 2027-11-29
  담당자 직렬(가정) 최숙진: 하한 45.5일 → 2026-11-25
  담당자 직렬(가정) 신우진: 하한 41.5일 → 2026-11-19
  담당자 직렬(가정) 서희항: 하한 34.5일 → 2026-11-10
  담당자 직렬(가정) 미정: 하한 24.0일 → 2026-10-26
  담당자 직렬(가정) 지니: 하한 2.0일 → 2026-09-18
  담당자 직렬(가정) 채훈희: 하한 1.0일 → 2026-09-17
결정 30 → 구간: {'E4': 3, 'B6': 2, 'C5': 1, 'A1': 1, 'D5': 4, 'E1': 3, 'B5': 2, 'E2': 5, 'A3': 2, 'C3': 1, 'D4': 1, 'A4': 2, 'A5': 1, 'D3': 1, 'E3': 1} · 오픈 전 필수 17
```

`ruff check build_plan_rows.py` → `All checks passed!` · `wc -l plan-rows.csv` → 736(헤더 + 735, 필드 내 줄바꿈 0).

근무일 달력: `holidays-kasi-2026.xml`·`holidays-kasi-2027.xml` = `k-skill-proxy /v1/korean-holiday/calendar?operation=rest`
(data.go.kr 15012690 `getRestDeInfo`) 응답 원문(resultCode 00 · totalCount 22/24). 계산 결과 `workdays.csv` 앞 11일:
`2026-09-17 · 09-18 · 09-21 · 09-22 · 09-23 · 09-28 · 09-29 · 09-30 · 10-01 · 10-02 · 10-06`.

#### Baseline-attribution (baseline 귀속)

HEAD `b30f0cfc` 에서 시작. 원장 `R/R2/unified-ledger-v4.csv` 654행(스크립트 `assert`) · parsed endpoints 22파일 635건(엔드포인트
인용은 전부 실재 집합 대조 — 없으면 스크립트가 종료) · research.md §4 표 직접 파싱 · S4 런북 표 직접 파싱. 결정 번호 1~30 연속 확인(누락 시 종료).

#### Baseline-attribution 보강 — 담당자 직렬 하한 294.0일의 재현 (리드 검산 요청 · 2026-09-17 11:13 KST)

리드가 같은 규칙으로 294.0 을 재현하지 못했다. 원인은 **정규화와 필터 두 단계가 문서에 없었던 것**이다.

**(1) 담당실명 정규화** — `build_plan_rows.pick_name`: 원 표기에서 7실명(김동학·서희항·최숙진·김용기·신우진·지니·채훈희) 중
**문자열에서 가장 앞에 나오는 이름 하나**로 묶는다. 다인 표기는 **첫 이름에 소요 전량**(배분하지 않음). 실명이 없으면 `외부(원문)`, 빈칸·「미정」은 `미정`.
전체 맵(원 표기 → 결과 · 원장 행수):

```
  '김동학 대표(기본배정)' → 김동학 · 206
  '서희항 대표 (기본배정)' → 서희항 · 172
  '최숙진 실장 (기본배정)' → 최숙진 · 110
  '김동학 대표 (기본배정)' → 김동학 · 22
  '신우진 (기본배정)' → 신우진 · 21
  '신우진(기본배정)' → 신우진 · 20
  '최숙진 실장·김용기 부장(기본배정)' → 최숙진 · 18
  '미정' → 미정 · 18
  '최숙진 실장(기본배정)' → 최숙진 · 9
  '신우진' → 신우진 · 6
  '서희항 대표' → 서희항 · 5
  '신우진(작성)·김동학 대표(게시)' → 신우진 · 5
  '상대측 회신 대기' → 외부(상대측 회신 대기) · 5
  '' → 미정 · 4
  '지니' → 지니 · 4
  '서희항 대표(기본배정)' → 서희항 · 3
  '토스페이먼츠' → 외부(토스페이먼츠) · 3
  '최숙진 실장' → 최숙진 · 2
  'KG이니시스' → 외부(KG이니시스) · 2
  '신우진·김동학 대표·서희항 대표' → 신우진 · 2
  '최숙진 실장 · 채훈희 대표(추가 여부 결정)' → 최숙진 · 1
  '김동학 대표' → 김동학 · 1
  '김동학 대표 · 콘텐츠 총괄 최숙진 실장' → 김동학 · 1
  '최숙진 실장 주관 · 김용기 부장 분담(권장)' → 최숙진 · 1
  '채훈희 대표(형태·메뉴명 결정) · 최숙진 실장(제안)' → 채훈희 · 1
  '이니시스(확인 신우진·최숙진 실장)' → 신우진 · 1
  '네이버페이' → 외부(네이버페이) · 1
  '카카오페이' → 외부(카카오페이) · 1
  '이니시스·카카오페이' → 외부(이니시스·카카오페이) · 1
  '최숙진 실장·김용기 부장 (기본배정)' → 최숙진 · 1
  '샵바이(NHN커머스)' → 외부(샵바이(NHN커머스)) · 1
  '김동학 대표 · 규칙/문구 최숙진 실장' → 김동학 · 1
  '최숙진 실장 (협조 김용기 부장)' → 최숙진 · 1
  '최숙진 실장(1차) · 김용기 부장(2차) · 채훈희 대표(3차 승인)' → 최숙진 · 1
  '김용기 부장(논의 주관) · 최숙진 실장' → 김용기 · 1
  '채훈희 대표·최숙진 실장(날짜 확정) · 개발(시나리오 초안)' → 채훈희 · 1
  '김동학 대표 (기본배정) / 신우진(콜백 재전송)' → 김동학 · 1
```

알려진 한계: 「이니시스(확인 신우진·최숙진 실장)」 처럼 벤더 표기 안의 확인자 이름이 잡히는 경우가 있다 — 이 행은
`owner_work` 가 담당역할 외부로 먼저 `ext` 를 붙여 직렬 합산에서 빠진다(아래 B).

**(2) 필터와 제외 — 김동학 기준**

```
$ cd _workspace/huni-launch-runway/07_rebaseline/S/S5-plan && python3 -B lane_audit.py 김동학
정규화 = 담당실명 문자열에서 7실명 중 가장 앞에 나오는 이름 하나(다인 표기는 첫 이름에 전량) · 실명 없으면 외부(원문)/미정
A 원장 654 → 상태∈{todo,new,partial,미판정} · 오픈차단여부≠오픈 무관: 348행 · 김동학 (행,일,1주+) (170, 349.5, 36)
B 제외: data-owner=ext(외부 대기 · 소요 0 규칙): 19행 · 김동학 (8, 35.5, 7)
C 제외: 어느 최상위 행 묶음(구간+filt)에도 안 들어감: 10행 · 김동학 (4, 20, 4) · 구간 {'D5': 10}
= lanes.csv 입력: 319행 · 김동학 (158, 294.0, 25)
```

리드 재현값(「김동학 대표(기본배정)」 한 표기 153행 · 331.5일)과의 차이는 (가) 표기 변형 4종을 합친 것(A 170행 · 349.5일)과
(나) B·C 제외(12행 · 55.5일)로 설명된다. C 의 10행은 D5 행 중 T6-1/2/3 의 `filt`(이관·잔액 / 엔터프라이즈·외부포인트 / 토스·가상계좌·충전)
어디에도 걸리지 않는 행이다 — 최상위 행 묶음 밖이라 직렬 합산에서 빠졌다. **이 제외가 옳은지는 판단 사항**이다(D5 는 오픈 범위이므로 넣는 편이 하한에 더 가깝다).

**(3) 재현 명령 한 줄** — `python3 -B _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/lane_audit.py 김동학` (다른 실명도 인자로).

**[주의 — 리드 판단 요청 사유]** A 단계 모집단이 정확히 **348행**이다. 지니가 반려한 「348행」·「565일」과 **같은 필터 계열**(미완 + 오픈 무관 제외)이며,
「구 IA·검증·설정·오픈후 행까지 일수로 합산」했다는 반려 사유가 이 하한에도 일부 적용된다(분류=검증·설정 행이 소요로 합산됨).
따라서 담당자 직렬 하한은 **헤드라인 금지 · §8 산식 설명과 함께 표 안에만**(리드 판정 보류) 두고, 오픈 범위 결정(결정 1) 전의 **범위 미조정 하한**이라고 명시한다.

#### 판단 기록 (리드 판정 3건 반영)

| # | 판정 | 반영 |
|---|---|---|
| 1 | api-evidence = `<파일명> <METHOD> <path>` — operationId 원천 0/635 | 전 열 반영 |
| 2 | api-path 4값(`none` = 샵바이 비경유) — 리드 해석 정정 | `none` 행 evidence 「샵바이 비경유」 |
| 3 | 목표일 (가) 조건부 승인 · 차단 26 확정 · CARDS-S 달력 줄 폐기 | 환산 반일 .5·1일 1·2-3일 2·1주+ 5 · 최상위 행마다 1주+ 건수·미판정 제외 건수 동반 · 외부 대기=회신 요청일+「회신 시점 미정」 · 원장 미완 0 = 「미산정」 |

**리드 조건에서 한 걸음 더 간 것 1건(보고)**: 담당자 직렬 가정을 **행 날짜에 걸지 않았다**. 걸면 같은 사람의 행들을
어떤 순서로 줄 세우느냐에 따라 날짜가 바뀌어 「하한」이 아니게 된다. 그래서 행 날짜 = 선행(T 행) + 자기 소요만,
담당자 직렬은 **담당자별 「전부 끝나는 날」**(`lanes.csv`)에만 적용했다. 담당자 묶음은 최상위 행 담당(결정자)이 아니라
**원장 담당 열의 실명**이다 — 최상위 행 담당으로 묶으면 T3-5(신우진·결정) 아래 김동학 개발 행 74일이 신우진에게 얹혔다(1차 실행에서 관측 후 교정).

**api-path 분류 방식**: 규칙(기능명+확인처 · 구간 제한 · 제외어) → 비-`none` 전건(admin-manual 35 · shop 87 · server 19 = 1차) 열람 →
행 단위 사람 판정 38건(`REVIEW` 표 · `api_basis` 「사람 판정 260917」). 최종 admin-manual 45(Server API 있음 = 우리 선택 14 · 화면만 제공 31) · shop 66 · server 14 · none 610.

#### Gaps (미검증)

1. **api-path `none` 중 샵바이 관련 구간인데 규칙 미매칭 232행은 개별 검토하지 않았다**(E1 43 · A5 27 · D5 24 · C6 20 · B5 19 · D2 18 …).
   `none` 이 「비경유 확정」이 아니라 「규칙이 못 잡음」일 수 있다 — D1 요약 층에 이 수를 그대로 싣는다. 재현: `api_basis` 에 「매칭 규칙 없음」.
2. **research.md §4-5 「차단 Y 24건」은 표 직접 집계 26 과 불일치**(research.md:354·357) — plan 레인 소유라 고치지 않았다(리드 판정).
3. **CARDS-S.md:6 근무일 나열은 요일이 틀렸다**(9/17 은 목요일 · 9/19 는 토요일 · 9/21 누락 · 나열 9 ≠ 「12」) — 쓰지 않음(리드 판정).
   또 공식 원천상 **2026-09-28(월)은 공휴일이 아니다** — CARDS-S 「연휴 9/24~28」 표현과 다르다.
4. 최상위 행 증거는 **존재·줄 범위**만 확인했다. 그 줄이 행 주장을 뒷받침하는지 내용 대조는 M4 D3 범위 밖 — 사람 판정(AC-LP-016) 몫.
5. 목표일 하한은 **원장 작업량구간이 맞다는 전제** 위에 있다. 1주+ 는 상한이 없어 최상위 행마다 건수를 같이 싣는다.
6. 원장 `담당실명` 이 「미정」인 미완 행 합 24.0일은 담당자 직렬에서 「미정」으로 남는다.

#### Residual-risk (잔여 위험)

- 김동학 담당자 직렬 하한 **294.0일 → 2027-11-29**. 오픈 범위·인원 결정(결정 1·2) 없이는 D1 §8 임계경로가 이 날짜를 가리킨다 — 숫자를 그대로 싣고 §9 3레버 근거로 쓴다(「565일」 과 다른 산식임을 본문에 명시해야 오독을 막는다).
- T1 F1~F3 는 원장 행이 없어 **미산정 구간**이다. 임계경로의 가장 앞이 비어 있어 도출 날짜는 T1 을 0 으로 본 하한이다.
- 결정 30 → 구간 귀속과 차단 26 내부/외부 판정은 **사람 판단 표**(`DECISION_STEP` · `BLOCKED_MAP`)다. 리드 이견 시 표 한 줄만 고치고 재실행.

### M1-보정 — 소요 합산 규칙 변경(지니 결정 260917 · 리드 전달) · 2026-09-17 11:57 KST

#### Claim

1. 소요 하한(최상위 행 날짜와 담당자 직렬 모두)은 **원장 분류 ∈ {개발, 수정} 행만** 합산한다. 검증·설정·이슈·미판정 분류 행은 체크리스트 행으로 남고 `note` 에 「소요 미합산(분류=…)」.
2. C 단계(어느 최상위 행 묶음에도 안 들어감) D5 10행 제외를 **해제** — T6-1 의 `filt` 를 `REST`(같은 구간 형제 최상위 행 filt 에 안 걸린 행 전부)로 바꿔 흡수. C 제외 = 0행.
3. 다인 표기 첫 이름 전량 배정은 유지하고 D1 §8 에 가정 한 줄로 명시.
4. 재현값이 리드 계산(김동학 개발+수정 126행 · 293일)과 **일치**.

#### Evidence

```
$ python3 -B _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/lane_audit.py 김동학
정규화 = 담당실명 문자열에서 7실명 중 가장 앞에 나오는 이름 하나(다인 표기는 첫 이름에 전량) · 실명 없으면 외부(원문)/미정
A 원장 654 → 상태∈{todo,new,partial,미판정} · 오픈차단여부≠오픈 무관: 348행 · 김동학 (행,일,1주+) (170, 349.5, 36)
B 제외: data-owner=ext(외부 대기 · 소요 0 규칙): 19행 · 김동학 (8, 35.5, 7)
C 제외: 어느 최상위 행 묶음(구간+filt)에도 안 들어감: 0행 · 김동학 (0, 0, 0) · 구간 {}
= 최상위 행 묶음 안: 329행 · 김동학 (162, 314.0, 29)
D 제외: 분류 ∉ {개발, 수정}(지니 결정 260917 — 체크리스트엔 남고 소요만 미합산): 181행 · 김동학 (36, 21.0, 0) · 분류 {'검증': 58, '이슈': 57, '설정': 62, '미판정': 4}
= lanes.csv 입력: 148행 · 김동학 (126, 293.0, 29)
```

`python3 -B build_plan_rows.py` → exit 0 · 자체 점검 통과(구조 불변: 735 = 최상위 28 + detail 707 · NEW 5/28).

#### 전후 비교 (전 = `1ed6bd15` 산출 · 후 = 이번 재생성)

| 행 | 담당 | 소요 하한 전→후(일) | 1주+ 전→후 | 소요 미합산 행(후) | 가장 이른 완료일 전 → 후 |
|---|---|---|---|---|---|
| T1-1 | 서희항 | 0 → 0 | 0 → 0 | 0 | 미산정(런북 단계 수 5 · 원장 미완 행 없음) → 미산정(런북 단계 수 5 · 원장 미완 행 없음) |
| T1-2 | 서희항 | 0 → 0 | 0 → 0 | 0 | 미산정(런북 단계 수 4 · 원장 미완 행 없음) → 미산정(런북 단계 수 4 · 원장 미완 행 없음) |
| T1-3 | 서희항 | 0 → 0 | 0 → 0 | 0 | 미산정(런북 단계 수 10 · 원장 미완 행 없음) → 미산정(런북 단계 수 10 · 원장 미완 행 없음) |
| T1-4 | 김동학 | 3.0 → 1 | 0 → 0 | 3 | 2026-09-21 → 2026-09-17 |
| T2-1 | 최숙진 | 21.0 → 8 | 0 → 0 | 15 | 2026-10-21 → 2026-09-30 |
| T2-2 | 최숙진 | 5.0 → 1 | 0 → 0 | 7 | 2026-10-28 → 2026-10-01 |
| T2-3 | 서희항 | 6.5 → 6 | 0 → 0 | 1 | 2026-09-29 → 2026-09-28 |
| T2-4 | 서희항 | 35.0 → 14 | 1 → 1 | 23 | 2026-11-11 → 2026-10-13 |
| T3-1 | 김동학 | 1 → 1 | 0 → 0 | 0 | 2026-09-17 → 2026-09-17 |
| T3-2 | 김동학 | 17.0 → 3.5 | 1 → 0 | 17 | 2026-10-15 → 2026-09-22 |
| T3-3 | 김동학 | 56.5 → 34.5 | 6 → 4 | 24 | 2027-04-05 → 2027-01-25 |
| T3-4 | 김동학 | 79.5 → 53.5 | 2 → 1 | 35 | 2027-01-15 → 2026-12-08 |
| T3-5 | 신우진 | 74.0 → 69 | 11 → 11 | 8 | 2027-01-06 → 2026-12-29 |
| T4-1 | 김동학 | 0 → 0 | 0 → 0 | 0 | 미산정(원장 미완 행 없음) → 미산정(원장 미완 행 없음) |
| T4-2 | 서희항 | 15.0 → 13.5 | 1 → 1 | 5 | 2027-04-26 → 2027-02-15 |
| T4-3 | 서희항 | 4.0 → 2 | 0 → 0 | 3 | 2026-09-22 → 2026-09-18 |
| T4-4 | 서희항 | 12.5 → 11 | 1 → 1 | 3 | 2026-10-15 → 2026-10-08 |
| T4-5 | 최숙진 | 6.5 → 4 | 0 → 0 | 5 | 2026-10-23 → 2026-10-15 |
| T5-1 | 최숙진 | 77.5 → 52 | 12 → 9 | 19 | 2027-01-12 → 2026-12-03 |
| T5-2 | 최숙진 | 6.5 → 6 | 0 → 0 | 1 | 2026-09-29 → 2026-09-28 |
| T5-3 | 김동학 | 24.5 → 20.5 | 1 → 1 | 9 | 2026-10-27 → 2026-10-21 |
| T5-4 | 신우진 | 13.0 → 12 | 1 → 1 | 3 | 2027-01-29 → 2026-12-21 |
| T6-1 | 채훈희 | 12.0 → 32 | 2 → 6 | 8 | 2026-10-07 → 2026-11-05 |
| T6-2 | 외부(NHN커머스) | 1.5 → 0 | 0 → 0 | 2 | 회신 요청 2026-09-18 · 회신 시점 미정 → 회신 요청 2026-09-18 · 회신 시점 미정 |
| T6-3 | 외부(토스페이먼츠) | 22.0 → 13.5 | 2 → 1 | 7 | 회신 요청 2026-09-18 · 회신 시점 미정 → 회신 요청 2026-09-18 · 회신 시점 미정 |
| T7-1 | 신우진 | 17.5 → 10.5 | 1 → 0 | 3 | 2026-10-21 → 2026-10-07 |
| T7-2 | 신우진 | 1 → 0 | 0 → 0 | 1 | 2027-04-06 → 2027-01-25 |
| T7-3 | 채훈희 | 0 → 0 | 0 → 0 | 0 | 미산정(원장 미완 행 없음) → 미산정(원장 미완 행 없음) |

| 담당자 직렬(가정) | 소요 하한 전→후(일) | 전부 끝나는 날(하한) 전 → 후 |
|---|---|---|
| 김동학 | 294.0 → 293.0 | 2027-11-29 → 2027-11-26 |
| 최숙진 | 45.5 → — (개발·수정 행 0) | 2026-11-25 → — |
| 신우진 | 41.5 → 1.0 | 2026-11-19 → 2026-09-17 |
| 서희항 | 34.5 → 20.0 | 2026-11-10 → 2026-10-20 |
| 미정 | 24.0 → 27.5 | 2026-10-26 → 2026-10-30 |
| 지니 | 2.0 → — (개발·수정 행 0) | 2026-09-18 → — |
| 채훈희 | 1.0 → — (개발·수정 행 0) | 2026-09-17 → — |

#### Gaps

- 분류 「미판정」 4행(김동학 기준)은 개발인지 모르는 채 소요 0 이 됐다 — 하한 쪽으로 기우는 오차.
- T3-5(신우진·결정 행) 아래 D3 개발 행 69일은 **행 날짜**에는 들어가고 담당자 직렬에서는 원장 담당(김동학)에 합산된다 — 최상위 행 담당과 소요 주체가 다른 행이 있다는 뜻이다.
- 최숙진·지니·채훈희는 개발·수정 미완 행이 0 이라 담당자 직렬 표에서 사라졌다. 설정·검증 일이 없다는 뜻이 **아니다**(소요 미합산일 뿐).

#### Residual-risk

- 규칙 변경으로 도출 날짜(가장 늦은 최상위 행 하한)가 `2027-04-26`(T4-2) → `2027-02-15`(T4-2)로 당겨졌다. 앞당겨진 것은 검증·설정 일을 0 으로 본 효과다 — 「그만큼 빨리 끝난다」로 읽으면 오독이다. D1 §8 에 합산 대상을 굵게 명시했다.

### M2 — D1 오픈 계획서 HTML (완료 · 2026-09-17 11:58 KST · run 레인)

#### Claim

1. `docs/huni/후니프린팅_오픈계획서_260917.html` 생성 — 생성기 `S5-plan/build_d1.py`(표·집계 수 전부 CSV 계산 · 손으로 적은 수 없음). 에이전트용 요약 `.md` 동반.
2. 13섹션 `sec-01`~`sec-13` · §6 블록 순서 = 메뉴 지도 → 옵션↔가격 도식 → 매뉴얼 요소 대조 요약 층(plan §M2-2 순서) · 전건 매트릭스는 부록 `<details id="manual-element-matrix">`.
3. 새 축 요약 층 `id="api-admin-manual"` 을 §7 맨 앞에 — 「API 가 있는데 수동으로 두고 있는 항목」 14건(목록·근거·담당) + 「셀러어드민 화면으로만」 31건(접힘) + 4값 정의·분포 + 규칙 미매칭 `none` 232행 미검토 고지.
4. 체크리스트 행 = 여섯 칸(`owner`·`duedate`·`evidence`·`check`·`prereq`·`status` 클래스) + 속성 `data-role/step/std/owner/work` + **추가 속성** `data-api-path`·`data-api-evidence`(D3 는 추가 속성을 거부하지 않게 만든다 — 리드 판정). 최상위 28행은 §7 트랙 블록, detail 707행은 부록(`id="appendix"`) 트랙별 접힘.
5. 담당자 직렬 하한은 헤드라인·§1 에 쓰지 않고 §8 표 안에만 「오픈 범위 조정 전 · 1인 직렬 가정 · 하한」 + 반려 합산과 다른 점 한 문장(리드 판정).
6. 브라우저 렌더 확인: ego-browser space 20 · p12(자기 탭 · 확인 후 닫음) — mermaid 3개 전부 `data-processed=true` + SVG · 문법 오류 0 · 본문 폰트 Pretendard.

#### Evidence

```
$ python3 -B _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/build_d1.py
D1 /Users/innojini/Dev/HuniWeb/.claude/worktrees/t47/docs/huni/후니프린팅_오픈계획서_260917.html 731,883 bytes
top 28 · detail 707 · wait 46 · derived 2027-02-15 (T4-2)
menu 37 (사이드바 33 + 비사이드바 4) · 갭★ 5 · matrix 159 {'동작확인': 116, '쓰기경로-dev환경필요': 43} · 화면 50
api {'none': 610, 'server-api': 14, 'shop-api': 66, 'admin-manual': 45} · forced 14 · admin-only 31 · 규칙 미매칭 none 232
exit=0
```

생성 측 자체 점검(판정은 M4 D3 몫):

```
자체점검(생성 측 · D3 아님) — bytes 731883
코드명 토큰: []
반려 수치 565일/348행: 0 0
조판+imposition 출현: 1
기준일+판정 술어 같은 문장(전 문서): 0
§1 요약 산문 글자 수: 419
외부 <script src>: 0 · stylesheet: ['https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css']
다크 흔적 prefers-color-scheme/data-theme/toggle: 0 0 0
섹션 앵커 13: True · top 28 · detail 707
```

렌더 확인(ego-browser · file:// · 2026-09-17 11:4x KST):
`{"mermaid":[{"processed":"true","svg":true,"err":false}×3],"font":"Pretendard, system-ui, -apple-system, sans-serif"}` · 스크린샷 sec-01·04·06·07 (scratchpad, 커밋 안 함).

#### Baseline-attribution

입력 = 커밋 `8800f98f` 의 `plan-rows.csv`·`lanes.csv`·`decisions-by-step.csv`·`workdays.csv` + `manual-element-matrix.csv`(`e1b99e22`) + 메인 체크아웃 `S/S5-live/menu-map-260917.csv`(읽기 전용) + `matrix-README.md` 분모 명령(정규식 추출 — 문서에 옮겨 적지 않고 README 원문을 그대로 실음).

#### 판단 기록 (계약 해석 — M4 D3 에서 이견 시 조정)

| 계약 | 해석·선택 |
|---|---|
| AC-LP-002 제외 목록 | detail 707행(원장 원문 · 카드 코드명·반려 수치가 섞임)을 **부록 안**에 둬서 산문 검사 제외 범위로 보냄. 본문 데이터 문자열은 `clean()` 으로 코드명 제거 · 「조판」→「판 배치」 · 기준일+판정 술어 문장의 날짜를 「기준일」로 |
| `\bS3\b` 가 AWS S3 제품명에도 걸림 | 본문 표기를 「원고 저장소(AWS)」로(의미 보존). 증거 칸·부록 경로는 원문 유지 |
| AC-LP-014(c) 갭 판정 기준 | 「갭 열 ★ 표기 행」 = 5건으로 선언 + 원천 경로 `class="source"` |
| AC-LP-012(b) §8 | 외부 대기 목록 `id="wait-items"` = `ext×wait` 46행 그대로 · 내부 선행 결정 `id="internal-prereq"` 별도 · dev 환경 선행 `id="devenv-prereq"` 28화면 + T1 진입 조건 상호 참조 문장 |
| AC-LP-012(a) 도출된 오픈일 | `id="derived-open-date"` = 최상위 행 가장 이른 완료일(하한) 최댓값 `2027-02-15`(T4-2) · 미산정 구간·외부 회신·범위 미조정 병기 |
| AC-LP-013 | Go/No-Go 결정권자 채훈희 1인 · 회의 `2026-10-02`(기준일 직전 근무일 · **제안** 표기) · 롤백 트리거 수치 = 행수 지문 불일치 · 잔액 1원 · 가격 API 중앙값 300ms(런북 F3-10·P-G1 원천) · 시한 48시간(베스트프랙티스 리서치) · 하이퍼케어 종료 지표 날짜 리터럴 0 |
| AC-LP-011 | 6단계 = spec REQ-LP-014 원문 · 외부 의존 T3(1·3단계) / T6(1·5단계) 각 2 · T6 밖 대기 2 · 담당 「미확정」 · P-6 번복 문장 |
| AC-LP-010(c) | 표 `id="t4-status"` 에 C1·C2·C3 수신 = 구현-미검증 · C3 해석·C4~C7 = 없음(REQ-LP-013 원문 값) |

#### Gaps (미검증)

1. **D3 미작성** — 위 해석이 AC-LP-001~014 를 통과하는지는 아직 관측하지 않았다(M4).
2. **파일 크기 731,883 바이트** — detail 707행을 부록에 전부 실은 결과. html-report 스킬 권고(≤120KB)를 넘는다. SPEC 계약(REQ-LP-004·AC-LP-015(c) 행 수 일치)이 우선이라 줄이지 않았다.
3. **한국어 윤문(humanize) 최종 패스 미실시** — 네이티브 원문으로 썼으나 `moai-domain-humanize` 패스는 돌리지 않았다(규칙상 무거운 한국어 산출물 대상). M4 사람 판정 전에 돌릴지 리드 판단.
4. 운영 섹션의 **컷오버 순서·RACI 배정·의사소통 채널은 원천에 확정값이 없어 제안값**이다(표 안에 제안임을 적지 않은 칸이 있다 — RACI·채널). 사람 판정에서 확인 필요.
5. 역할 진입점은 최상위 행 담당 기준 — 김용기 담당 최상위 행이 없어 `role-ops` 는 최숙진 행으로만 채워진다.
6. 매뉴얼 결함 1건(category-master 선행 조작)은 부록에 서술로만 올렸다(행 ID 없음).

#### Residual-risk

- mermaid CDN(jsdelivr ESM) 이 막힌 환경에서는 `<noscript>` 가 아니라 **원문 `pre` 텍스트**가 보인다(JS 는 켜져 있고 네트워크만 막힌 경우) — 인쇄·오프라인 판독은 원문 텍스트로 떨어진다.
- `clean()` 은 원장 원문을 본문에 옮길 때 코드명을 **지운다** — 최상위 행 제목·결정 제목 일부가 원문과 글자 단위로 다르다(부록·CSV 는 원문 유지).

### M3 — D2 엑셀 재생성 (완료 · 2026-09-17 11:59 KST · run 레인)

#### Claim

1. `S5-plan/build_xlsx_v4_plan.py` = R5 `build_xlsx_v4.py` 확장 복제. 기존 9시트는 그대로 두고 `09_구간상태판`(status-28.csv) · `10_트랙체크리스트`(plan-rows.csv 735행 · api-path 열 포함) 두 시트를 추가.
2. 산출 `docs/huni/후니프린팅_오픈계획서_260917.xlsx` · 빌더 종료 코드 0 · 검산 11건 ALL PASS — 원장 시트 654행 · 트랙 체크리스트 735행 == D1 `data-role` top+detail 735.
3. 원본 R5 빌더는 수정하지 않았다(복제본만). 입력은 메인 체크아웃 절대경로 읽기 전용, 출력만 워크트리.

#### Evidence

```
$ python3 -B _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/build_xlsx_v4_plan.py; echo exit=$?
[OK] /Users/innojini/Dev/HuniWeb/.claude/worktrees/t47/docs/huni/후니프린팅_오픈계획서_260917.xlsx
  원장 654행 · 게이트 5개(_workspace/huni-launch-runway/07_rebaseline/R/R3/gates-d20.md) · 회의 89 · 시나리오 48 · 주차 배치 R3 확정

== VERIFY ==
  [PASS] 02 행 수 654 == 원장 654
  [PASS] 02 「분류」 빈칸 0건 (미판정은 허용)
  [PASS] 02 「난이도」 빈칸 0건 (미판정은 허용)
  [PASS] 02 「담당역할」 빈칸 0건 (미판정은 허용)
  [PASS] 02 std_id 유일
  [PASS] 07 ①블록 남은 일 348행 == 원장 남은 일 348 · 중복 0 · 누락 0 · 여분 0
  [PASS] 06 행 수 89 == 회의 추적 89
  [PASS] 시트명 ['00_읽는법', '01_역할정의', '02_IA마스터', '03_페이즈일정', '04_진행현황', '05_주차별실행판', '06_회의결정추적', '07_체크리스트', '08_변경이력', '09_구간상태판', '10_트랙체크리스트']
  [PASS] 02 원장 시트 데이터 행 654 == 654
  [PASS] 10 트랙체크리스트 735행 == D1 data-role top+detail 735
  [PASS] 09 구간상태판 32행 == status-28.csv

== 시트 · 데이터 행 수 ==
  00_읽는법: max_row=51 · max_col=3
  01_역할정의: max_row=15 · max_col=18
  02_IA마스터: max_row=655 · max_col=20
  03_페이즈일정: max_row=11 · max_col=7
  04_진행현황: max_row=109 · max_col=8
  05_주차별실행판: max_row=372 · max_col=13
  06_회의결정추적: max_row=93 · max_col=12
  07_체크리스트: max_row=405 · max_col=9
  08_변경이력: max_row=659 · max_col=12
  09_구간상태판: max_row=36 · max_col=10
  10_트랙체크리스트: max_row=739 · max_col=20

[참조 · 읽기만] 260902 시트: ['00_읽는법', '01_역할정의', '02_IA마스터', '03_페이즈일정', '04_진행현황'] (스키마가 의도적으로 바뀌어 구조 대조는 하지 않는다)

== RESULT: ALL PASS ==
exit=0
```

#### Baseline-attribution

HEAD `aedd541c`(M2) · `plan-rows.csv` 는 `8800f98f` 규칙 · D1 HTML 은 `aedd541c` 산출을 검산 시점에 직접 읽음(행 수 문자열 대조).

#### Gaps

1. AC-LP-015(c) 의 두 번째 조건(트랙·체크리스트 시트 행 수 == D1 행 수)은 빌더 **자체 검산**으로 관측했다 — §B 판정 주체는 run 레인이므로 M4 에서 D3 와 별개로 한 번 더 인용한다.
2. 기존 `07_체크리스트` 시트 안내 문구에 9/16 반려 산식(남은 일 행 수)이 그대로 남아 있다 — 원본 보존 원칙상 손대지 않았다(엑셀은 AC-LP-002 산문 검사 대상 아님). 사람 판정에서 혼동을 부르면 시트 설명 한 줄 보강 여부를 리드가 판단.
3. 엑셀 서식(열 너비·굵게)은 열어서 눈으로 보지 않았다.

#### Residual-risk

- 윤문 패스로 D1 산문이 바뀌어도 행 수는 불변이라 10 시트 검산은 유지된다. 단 윤문 뒤 D1 을 재생성하면 이 검산을 다시 돌린다.

### M3-보정 · M2-보정 — 제안값 표지 · D-P 결정자 원문 · 엑셀 구 산식 안내 (리드 판정 · 2026-09-17 12:29 KST)

#### Claim

1. D1 운영 섹션의 원천 없는 값(Go/No-Go 결정권자·회의일 · 3레버 결정자 · 컷오버 7행 · 롤백 트리거 · 하이퍼케어 CS 기준 · RACI 8행 · 소통 3행)에 눈에 보이는 표지 「제안(확정 전)」 26곳 + §12 끝 `id="jini-decisions"` 「지니 결정 필요」 표 7행(항목·제안값·근거·결정자).
2. T4 설계 결정 D-P1~D-P9 결정자를 S2 findings §4 원문대로 **복수 실명**으로(Go/No-Go 결정권자 1인 조건과 별개 — 섞지 않음).
3. D2 엑셀 `07_체크리스트` A3 · `00_읽는법` 끝에 강조 안내 행(9/16 판 구 산식 · 오픈 계획 기준은 10·09 시트). 원본 행·수치는 보존. VERIFY 검산 2건 추가.

#### Evidence

`grep -o '제안(확정 전)' docs/huni/후니프린팅_오픈계획서_260917.html | wc -l` → `26`

```
$ python3 -B _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/build_xlsx_v4_plan.py; echo exit=$?
  [PASS] 02 행 수 654 == 원장 654
  [PASS] 02 「분류」 빈칸 0건 (미판정은 허용)
  [PASS] 02 「난이도」 빈칸 0건 (미판정은 허용)
  [PASS] 02 「담당역할」 빈칸 0건 (미판정은 허용)
  [PASS] 02 std_id 유일
  [PASS] 07 ①블록 남은 일 348행 == 원장 남은 일 348 · 중복 0 · 누락 0 · 여분 0
  [PASS] 06 행 수 89 == 회의 추적 89
  [PASS] 시트명 ['00_읽는법', '01_역할정의', '02_IA마스터', '03_페이즈일정', '04_진행현황', '05_주차별실행판', '06_회의결정추적', '07_체크리스트', '08_변경이력', '09_구간상태판', '10_트랙체크리스트']
  [PASS] 02 원장 시트 데이터 행 654 == 654
  [PASS] 10 트랙체크리스트 735행 == D1 data-role top+detail 735
  [PASS] 09 구간상태판 32행 == status-28.csv
  [PASS] 07 안내 행(A3) 실재 — 9/16 판 표기 · 10 시트 안내
  [PASS] 00 안내 행 실재
== RESULT: ALL PASS ==
exit=0
```

#### Gaps

- 제안값 표지는 **생성기가 붙인 위치**만 확인했다 — 렌더 화면에서 눈에 띄는지는 사람 판정 때 본다.

### M1.5 분모 보강 (리드 판정 260917) · 외부 대기 대상 원천화 · M4 D3 1회차(윤문 전) — 2026-09-17 14:03 KST

#### Claim

1. **분모 규칙 보강**: 매뉴얼 콜아웃이 0 이거나 매뉴얼 섹션이 없는 **사이드바 관리 화면**도 화면 1건 = 요소 1건. 원고 분모 명령에 메뉴 지도 CSV 항을 더해 **159 → 162**. 문서 본문 메뉴 6개(퀵스타트 가이드·메뉴얼·위젯빌더 메뉴얼·테이블 명세서·SDK 개발가이드·임베드 라이브 데모)는 제외를 **선언**(README · D1 `#manual-exclusions`) — D3 가 계산한 제외 집합과 선언 목록을 대조하고 매번 출력.
2. **라이브 읽기 실측 3화면**(ego-browser space 20 · p2 · 클릭 0 · 저장 0 · 다이얼로그 0 · [HARD] 3항 준수): basecode-master = 동작확인 · guide-file-md·main-img-md = 불일치(매뉴얼 섹션 없음 · 화면은 렌더).
3. **외부 대기 회신 요청 대상**을 키워드 배정에서 **행별 원천 인용**(`wait_target_basis`)으로 교체 — 원천에 상대 이름이 없거나 원천이 충돌하는 3행은 「회신 요청 대상 미확정」.
4. **D3 1회차(윤문 전) = 13/14 PASS · FAIL AC-LP-012(b)** — 위 미확정 3행. 리드 판정대로 FAIL 로 보고한다(지니 확인 상신 대상).

#### Evidence — 분모

```
$ cd /Users/innojini/Dev/HuniWeb && cd raw/webadmin/tools && python3 -c 'import importlib.util as u
def L(n,p):
    s=u.spec_from_file_location(n,p); m=u.module_from_spec(s); s.loader.exec_module(m); return m
mc=L("mc","manual_content.py"); wc=L("wc","widget_manual_content.py")
n=sum(len(c.get("callouts",[])) for s in mc.SCREENS for c in s.get("captures",[]))
n+=sum(len(c.get("callouts",[])) for s in wc.SCREENS for c in s.get("captures",[]))
n+=len(mc.MODEL_ADMIN_SCREENS); import csv; M=list(csv.DictReader(open("../../../_workspace/huni-launch-runway/07_rebaseline/S/S5-live/menu-map-260917.csv",encoding="utf-8"))); k="매뉴얼 화면 섹션(SCREENS/MODEL_ADMIN) 유무"
n+=sum(1 for r in M if r["사이드바그룹"]!="(사이드바 없음)" and (r[k].startswith("없음") or "콜아웃 0" in r[k]))
print(n)'
162
```

라이브 실측 원시 기록 `S5-plan/m15-2b-results.jsonl`(화면 1건마다 즉시 append):

```
{"path":"/admin/basecode-master/","startedAt":"2026-09-17 13:43:09","url":"/admin/basecode-master/","title":"기초코드정보 | 후니 DB Admin","h1":"기초코드정보?\n\n  #hh-help-link{display:inline-flex;align-items:center;justify-content:","login":false,"rows":23,"textLen":1478,"measuredAt":"2026-09-17 13:43:11"}
{"path":"/admin/guide-file-md/","startedAt":"2026-09-17 13:43:11","url":"/admin/guide-file-md/","title":"상품별 작업가이드 파일 | 후니 DB Admin","h1":"상품별 작업가이드 파일?\n\n  #hh-help-link{display:inline-flex;align-items:center;justify-co","login":false,"rows":248,"textLen":6094,"measuredAt":"2026-09-17 13:43:11"}
{"path":"/admin/main-img-md/","startedAt":"2026-09-17 13:43:11","url":"/admin/main-img-md/","title":"상품별 메인이미지 | 후니 DB Admin","h1":"상품별 메인이미지?\n\n  #hh-help-link{display:inline-flex;align-items:center;justify-conte","login":false,"rows":248,"textLen":6745,"measuredAt":"2026-09-17 13:43:40"}
```

| | 보강 전(`e1b99e22`) | 보강 후 |
|---|---|---|
| 요소(분모 = CSV 행) | 159 | 162 |
| 화면단위 | 50 | 53 |
| 결과 분포 | {'동작확인': 116, '쓰기경로-dev환경필요': 43} | {'동작확인': 117, '쓰기경로-dev환경필요': 43, '불일치': 2} |
| 미실측 비율 | 0/159 (0.0%) | 0/162 (0.0%) |
| (iv) 불일치 목록 | 0 | 2(작업가이드 파일 · 메인이미지 — 매뉴얼 섹션 없음 · 개발자 전달) |

#### Evidence — 외부 대기 12행(원장 담당 칸이 우리 쪽 사람이던 행)

| 행 | 제목 | 회신 요청 대상 | 근거 |
|---|---|---|---|
| STD-PAY-001 | 신용카드 결제(PG) | 외부(이니시스) | 원장 담당실명 「이니시스(확인 신우진·최숙진 실장)」 · 확인처 「이니시스」 |
| STD-PAY-007 | 적립금(프린팅머니) 결제 | 외부(NHN커머스) | 원장 비고 「엔터프라이즈 플랜 확인(V9) 필요」 · _workspace/huni-launch-runway/07_rebaseline/S/S3-migration/migration-status.md:58 「Shopby 엔터프라이즈 플랜·외부포인트 정식 적용(V9) → NHN 1:1 세팅」 |
| STD-PAY-008 | 복합결제(적립금+PG 병용) | 외부(NHN커머스 · 이니시스) | 원장 선행의존 「STD-PAY-007;STD-PAY-001」 — 두 선행 행의 대상 |
| STD-PAY-011 | PG 리다이렉트·결제 확정 콜백 | 외부(이니시스) | 원장 선행의존 「EXT-PG」 · 같은 EXT-PG 행 STD-PAY-001 확인처 「이니시스」 |
| STD-MEM-020 | 기존 회원 데이터 이관(구 사이트) | 외부(구 사이트 운영사/IDC) | 원장 선행의존 「EXT-OLDDB」 · _workspace/huni-launch-runway/07_rebaseline/S/S3-migration/migration-status.md:49 「ⓔ 구 사이트 운영사/IDC 협조」 |
| STD-MYP-007 | 프린팅머니 충전(PG 결제→적립 전환) | 외부(회신 요청 대상 미확정) | 원천 충돌 — 원장 선행의존 「EXT-PG」(이니시스) vs 9/15 회의 확정 「충전 = 토스페이먼츠 가상계좌」(STD-MYP-028) |
| STD-MYP-009 | 기존 프린트머니 잔액 이관 | 외부(구 사이트 운영사/IDC) | 원장 선행의존 「EXT-OLDDB」 · _workspace/huni-launch-runway/07_rebaseline/S/S3-migration/migration-status.md:65 「구 사이트 운영사/IDC(C-1-2·C-2-4)」 |
| STD-MYP-026 | 구 ASP 코드·DB 미제공 전제 — 이관 원천은 잔액 스냅샷만 | 외부(구 사이트 운영측) | 원장 확인처 「구 사이트 운영측」 |
| STD-MYP-028 | 충전 입금 확인 = 토스페이먼츠 가상계좌 API 직접 발급 | 외부(토스페이먼츠) | 원장 기능 「토스페이먼츠 가상계좌 API 직접 발급」 · .moai/specs/SPEC-LAUNCHPLAN-001/research.md:331 「토스 계약 4건 회신」 |
| STD-FIN-008 | PG 정산 대사 | 외부(이니시스) | 원장 선행의존 「EXT-PG」 · 같은 EXT-PG 행 STD-PAY-001 확인처 「이니시스」 |
| STD-SYS-004 | PG 연동 설정 | 외부(이니시스) | 원장 선행의존 「EXT-PG」 · 같은 EXT-PG 행 STD-PAY-001 확인처 「이니시스」 |
| STD-SYS-039 | 웹훅 등록 화면 위치 확인(셀러어드민 내 미발견) 및 등록 여부 | 외부(NHN) | 원장 비고 「앱 개발자센터 또는 NHN 1:1 문의로 확인」 |

미확정 3행(전체 46 중):

| 행 | 제목 | 근거 |
|---|---|---|
| STD-MYP-007 | 프린팅머니 충전(PG 결제→적립 전환) | 원천 충돌 — 원장 선행의존 「EXT-PG」(이니시스) vs 9/15 회의 확정 「충전 = 토스페이먼츠 가상계좌」(STD-MYP-028) |
| BLK-S2-4 | [선행 입력] PitStop 구매 진행 상태(견적·라이선스·인스턴스 계획) | .moai/specs/SPEC-LAUNCHPLAN-001/research.md:317 「PitStop 구매 진행 상태」 — 회신할 상대 이름이 원천에 없다 |
| BLK-S4-6 | [선행 입력] Vercel 프로젝트 접근 · 현재 `DATABASE_URL`·`NEXTAUTH_URL` 소재 · 리다이렉트 설정 유무 | .moai/specs/SPEC-LAUNCHPLAN-001/research.md:345 「Vercel 프로젝트 접근」 — 접근 권한을 가진 상대 이름이 원천에 없다 |

인용한 `파일:줄` 근거는 생성 시 줄 수 범위를 스크립트로 확인했다(실재 OK).

#### Evidence — D3 1회차(윤문 전 · D1 md5 `5476498a5ef527db23763de6a2ae6653`)

```
$ node _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/verify_plan.mjs docs/huni/후니프린팅_오픈계획서_260917.html /Users/innojini/Dev/HuniWeb/_workspace/huni-launch-runway/07_rebaseline/R/R2/unified-ledger-v4.csv _workspace/…/S5-plan/manual-element-matrix.csv "<README 분모 명령>"
[PASS] AC-LP-001 자기완결 + 라이트 테마 전용
    (① 허용 밖 외부 참조 0) 읽은 대상 2
    (② prefers-color-scheme 0) 읽은 대상 1
    (③ 다크 선택자 0) 읽은 대상 1
    (④ 테마 토글 0) 읽은 대상 1
[PASS] AC-LP-002 Out of Scope 금지 4종
    ((a) 내부 코드명 0) 읽은 대상 1804
    ((b) 반려 수치 0) 읽은 대상 1804
    ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1430
    ((d) 조판 ≤ 1) 읽은 대상 1804
[PASS] AC-LP-003 13섹션 골격 + 범위 두 표
    ((a) sec-01~13 순서) 읽은 대상 13
    ((b) 범위 두 표·동결 규칙) 읽은 대상 2
[PASS] AC-LP-004 스윔레인 3~7 + CTO 10구간 매핑
    ((a) 파싱·레인 수·소유 라벨) 읽은 대상 4
    ((b) 상위 10 · 구간 참조) 읽은 대상 10
    ((c) 영향 문장) 읽은 대상 10
[PASS] AC-LP-005 요약·마일스톤·배치도
    ((a) 요약 6요소·700자·범례) 읽은 대상 6
    ((b) 마일스톤 4~6·오너·상태·T1 첫째) 읽은 대상 5
    ((c) 배치도 ≤12·실명·소유 라벨) 읽은 대상 10
[PASS] AC-LP-006 체크리스트 7블록·3~5·6칸·체크 방법·소유 분류
    ((a) 7블록·최상위 3~5) 읽은 대상 7
    ((b) 6칸 빈칸 0) 읽은 대상 28
    ((c) 담당 실명) 읽은 대상 28
    ((d) 체크 방법 실질) 읽은 대상 28
    ((e) 소유·분류 제약(전 행)) 읽은 대상 735
[PASS] AC-LP-007 증거 실재 + 작동 판정 규율
    ((a) 파일:줄 실재(최상위 · 파일 증거 28)) 읽은 대상 28
    ((b) URL@일시 형식·호스트(최상위 · URL 증거 0)) 읽은 대상 28
    ((c) data-std 원장 실재 · NEW 5 ≤ 30%) 읽은 대상 28
    ((d) 작동 행 전부 URL@일시 ≥ 2026-09-16(전 행)) 읽은 대상 115
[PASS] AC-LP-008 구간 귀속 + 역할별 진입점
    (data-step 허용 집합) 읽은 대상 735
    (역할 앵커 5·각 1건 이상) 읽은 대상 5
[PASS] AC-LP-009 T1 인프라
    ((a) F1~F4) 읽은 대상 4
    ((b) 진입 조건 정확히 9) 읽은 대상 9
    ((c) F3-11·F4-8 금지 표기) 읽은 대상 2
    ((d) 리다이렉트 재검증) 읽은 대상 1
[PASS] AC-LP-010 T4 3단 + D-P1~9 + C1~C7 값 대조
    ((a) 3단계 순서) 읽은 대상 3
    ((b) D-P1~9 결정자) 읽은 대상 9
    ((c) 상태 값 대조) 읽은 대상 8
[PASS] AC-LP-011 T3·T6 이관 6단계 + 외부 의존 계층
    ((a) 6단계 순서) 읽은 대상 2
    ((b) 하위 외부 의존 정확히 2) 읽은 대상 2
    ((c) T6 밖 대기 2종) 읽은 대상 1
    ((d) 미확정·P-6) 읽은 대상 2
[FAIL] AC-LP-012 임계경로 전진 구축 + 3레버
    ((a) 순서·도출 날짜) 읽은 대상 4
    ((b) 외부 대기 46 · dev 선행 28) 읽은 대상 46 · 위반 3
        - 회신 요청 대상 부적격 「외부(회신 요청 대상 미확정)」: 프린팅머니 충전(PG 결제→적립 전환) — 회신 요청 대상 외부(회신 요청 대상 미확정) 
        - 회신 요청 대상 부적격 「외부(회신 요청 대상 미확정)」: [선행 입력] PitStop 구매 진행 상태(견적·라이선스·인스턴스 계획) — 회신 요청 
        - 회신 요청 대상 부적격 「외부(회신 요청 대상 미확정)」: [선행 입력] Vercel 프로젝트 접근 · 현재 `DATABASE_URL`·`NEXTAU
    ((c) 3레버 결정자) 읽은 대상 3
[PASS] AC-LP-013 Go/No-Go · 컷오버 · 하이퍼케어 · RACI
    ((a) 4상태·결정권자 1인·회의일) 읽은 대상 4
    ((b) 컷오버 5칸·롤백 트리거) 읽은 대상 7
    ((c) 하이퍼케어 지표·날짜 없음) 읽은 대상 5
    ((d) RACI A 1개·소통 5요소) 읽은 대상 8
    [(v) 대조 제외 6] 퀵스타트 가이드 · 메뉴얼 · 위젯빌더 메뉴얼 · 테이블 명세서 · SDK 개발가이드 · 임베드 라이브 데모
[PASS] AC-LP-014 판매 준비 프로세스 3블록
    ((a) 메뉴 37·내역) 읽은 대상 37
    ((b) 갭 열·갭 5건) 읽은 대상 37
    ((c) 갭 수·기준·원천) 읽은 대상 1
    ((d) 옵션↔가격 5단계·고치는 화면) 읽은 대상 5
    ((e) 매트릭스 사슬·값·하한(원고 분모 162 · 부록 162 · 요약 합 162 · 화면 대상 31 · 미실측 0)) 읽은 대상 162

== D3 RESULT: 13/14 PASS · FAIL AC-LP-012 ==
exit=1
```

#### D3 작성 중 스스로 고친 것 (보고)

| # | 발견 | 조치 |
|---|---|---|
| 1 | D3 가 「외부(회신 요청 대상 미확정)」을 통과시켰다 — 정규식이 「미정」만 찾았는데 「미확정」에는 그 글자 연속이 없다 | 정규식에 「미확정」 추가 → AC-LP-012 가 제대로 FAIL |
| 2 | AC-LP-002 가 분모 명령 코드 블록 안의 경로 조각 「S5」를 내부 코드명으로 잡았다 | 산문 추출에서 `pre`·`code`(코드 블록)를 뺐다 — AC 가 「산문 노드만」이라 계약 문구에 맞춘 해석. **판정 대상** |
| 3 | 원장 제목의 「[차단 입력 S2-4]」가 정리 함수에서 코드만 지워져 「[차단 입력 -4]」로 보였다 | 제목을 「[선행 입력]」으로, 식별자는 행 ID 속성에만 |

#### Gaps

1. (v) 커버 판정은 **경로 정규화 일치 또는 메뉴명↔매트릭스 화면명 포함**이라는 D3 규칙이다 — 이름 포함 판정은 느슨할 수 있다(예: 짧은 메뉴명이 긴 화면명에 우연히 포함).
2. 3화면 실측은 **렌더 확인**(제목·행 수·로그인 폼 부재)까지다 — 화면 안 개별 버튼은 매뉴얼 콜아웃이 없어 대조하지 않았다.
3. D3 는 이 레인이 썼다 — 생성과 검증 주체가 같다. 리드 재실행·재집계가 독립 판정이다.

#### Residual-risk

- 미확정 3행은 D1 §8 외부 대기 목록에 「회신 요청 대상 미확정」으로 그대로 보인다 — 지니 확인 전까지 AC-LP-012 는 FAIL 이 유지된다.

### M4 — 윤문 · 리드 정정 반영 · D3 최종 · 음성 대조 · AC-LP-015 (2026-09-17 14:09 KST · run 레인)

#### Claim

1. **윤문(moai-domain-humanize · 한국어 · Fast · 산문 모드)** — 생성기 `build_d1.py` 의 손으로 쓴 산문 23곳만 수정(E-7 존대 통일 10 · J-3 줄글 속 줄표 곁가지 8 · A-23 직역 비유 「손잡이」 1 · 어색한 서술 4). 숫자·날짜·ID·경로·앵커·data 속성·실명·검사 토큰 불변. 산문 줄 문자 유사도 0.994 → **변경률 추정 0.6%**(30% 경고선 한참 아래 · LLM 추정이 아니라 difflib 계산). 등급 A(잔여 S1 0).
2. **윤문 전후 D3 판정 차이 0** — PASS/FAIL 줄·위반 목록·RESULT·종료 코드 동일. 전체 출력 차이는 AC-LP-002(c) 가 읽은 문장 수 1줄(1430→1432 · 줄표를 마침표로 바꿔 문장이 둘로 나뉜 결과)뿐.
3. **리드 정정 반영** — ① STD-MYP-007 충전 → 외부(토스페이먼츠)(설계문서:312·:314 9/16 재확정이 원장 EXT-PG 보다 우선 · :304 무통장 복귀 대안 병기) ② S2-4 PitStop 구매 = 내부(대표(구매) · S2 findings:48 · 실명 원천 미기재) ③ S4-6 Vercel 접근 = 내부(김동학·PM · S4 런북:131·:84) — 둘 다 `huni×config` 로 외부 대기에서 §8 「내부 선행 결정」으로 이동 → 외부 대기 **46→44**, 체크리스트 `ext×wait` 집합도 44(D3 가 두 집합 일치 확인).
   경위(리드 요청 한 줄): 레인이 원천에 상대 이름이 없다고 「미확정」으로 둔 2건을 리드도 원천을 열지 않고 받아들였다가, 리드가 원천을 열어 내부 담당을 찾아 정정했다.
4. **D3 가드(리드 승인)** — AC-LP-002 산문 추출에서 뺀 `pre`·`code` 블록을 매번 출력하고, 허용(mermaid 원문 3 · 분모 명령 1) 밖 블록이 생기면 FAIL. (b) 반려 수치는 코드 블록까지 포함해 센다.
5. **D3 최종 = 14/14 PASS · 종료 코드 0.** 음성 대조 2건 = 종료 코드 1(다크 규칙 1줄 주입 → AC-LP-001 위반 목록에 「prefers-color-scheme 출현 1」 · 분모 명령 선언 1글자 변경 → AC-LP-014 「(i) D1 선언 명령 ≠ 4번째 인자」).
6. **AC-LP-015 (a)(b)(c)(d) 충족** — 아래 Evidence.

#### Evidence — 윤문 전후 D3 출력 전체 diff

```
$ diff d3-run1-prehumanize.txt d3-run2-posthumanize.txt
9c9
<     ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1430
---
>     ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1432
```

판정 줄만 추린 diff(`grep -E "^\[(PASS|FAIL)\]|        - |RESULT|exit="`) → 출력 없음 · diff 종료 코드 0.
D1 md5: 윤문 전 `5476498a…` → 윤문 후 `8b16483e…` → 정정 반영 최종 `6a3f1b86b6d92eded930acb2f709b2ae`.

#### Evidence — D3 최종(정상 D1)

```
$ node _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/verify_plan.mjs docs/huni/후니프린팅_오픈계획서_260917.html /Users/innojini/Dev/HuniWeb/_workspace/huni-launch-runway/07_rebaseline/R/R2/unified-ledger-v4.csv _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/manual-element-matrix.csv "<matrix-README.md 분모 명령>"
[PASS] AC-LP-001 자기완결 + 라이트 테마 전용
    (① 허용 밖 외부 참조 0) 읽은 대상 2
    (② prefers-color-scheme 0) 읽은 대상 1
    (③ 다크 선택자 0) 읽은 대상 1
    (④ 테마 토글 0) 읽은 대상 1
    [산문 제외 코드 블록 4] pre.mermaid 「flowchart LR subgraph L1["고객 화면·자사몰 스킨」 | pre.mermaid 「flowchart LR n1["자사몰 스킨 컨테이너 — Lightsa」 | pre.mermaid 「flowchart LR s1["1. 상품 구성요소<br/>고치는 화면」 | pre#denominator-command.cmd 「cd raw/webadmin/tools && python3 -c 'imp」
[PASS] AC-LP-002 Out of Scope 금지 4종
    ((a) 내부 코드명 0) 읽은 대상 1800
    ((가드) 산문 제외 코드 블록 = mermaid 원문·분모 명령뿐) 읽은 대상 4
    ((b) 반려 수치 0) 읽은 대상 1800
    ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1432
    ((d) 조판 ≤ 1) 읽은 대상 1800
[PASS] AC-LP-003 13섹션 골격 + 범위 두 표
    ((a) sec-01~13 순서) 읽은 대상 13
    ((b) 범위 두 표·동결 규칙) 읽은 대상 2
[PASS] AC-LP-004 스윔레인 3~7 + CTO 10구간 매핑
    ((a) 파싱·레인 수·소유 라벨) 읽은 대상 4
    ((b) 상위 10 · 구간 참조) 읽은 대상 10
    ((c) 영향 문장) 읽은 대상 10
[PASS] AC-LP-005 요약·마일스톤·배치도
    ((a) 요약 6요소·700자·범례) 읽은 대상 6
    ((b) 마일스톤 4~6·오너·상태·T1 첫째) 읽은 대상 5
    ((c) 배치도 ≤12·실명·소유 라벨) 읽은 대상 10
[PASS] AC-LP-006 체크리스트 7블록·3~5·6칸·체크 방법·소유 분류
    ((a) 7블록·최상위 3~5) 읽은 대상 7
    ((b) 6칸 빈칸 0) 읽은 대상 28
    ((c) 담당 실명) 읽은 대상 28
    ((d) 체크 방법 실질) 읽은 대상 28
    ((e) 소유·분류 제약(전 행)) 읽은 대상 735
[PASS] AC-LP-007 증거 실재 + 작동 판정 규율
    ((a) 파일:줄 실재(최상위 · 파일 증거 28)) 읽은 대상 28
    ((b) URL@일시 형식·호스트(최상위 · URL 증거 0)) 읽은 대상 28
    ((c) data-std 원장 실재 · NEW 5 ≤ 30%) 읽은 대상 28
    ((d) 작동 행 전부 URL@일시 ≥ 2026-09-16(전 행)) 읽은 대상 115
[PASS] AC-LP-008 구간 귀속 + 역할별 진입점
    (data-step 허용 집합) 읽은 대상 735
    (역할 앵커 5·각 1건 이상) 읽은 대상 5
[PASS] AC-LP-009 T1 인프라
    ((a) F1~F4) 읽은 대상 4
    ((b) 진입 조건 정확히 9) 읽은 대상 9
    ((c) F3-11·F4-8 금지 표기) 읽은 대상 2
    ((d) 리다이렉트 재검증) 읽은 대상 1
[PASS] AC-LP-010 T4 3단 + D-P1~9 + C1~C7 값 대조
    ((a) 3단계 순서) 읽은 대상 3
    ((b) D-P1~9 결정자) 읽은 대상 9
    ((c) 상태 값 대조) 읽은 대상 8
[PASS] AC-LP-011 T3·T6 이관 6단계 + 외부 의존 계층
    ((a) 6단계 순서) 읽은 대상 2
    ((b) 하위 외부 의존 정확히 2) 읽은 대상 2
    ((c) T6 밖 대기 2종) 읽은 대상 1
    ((d) 미확정·P-6) 읽은 대상 2
[PASS] AC-LP-012 임계경로 전진 구축 + 3레버
    ((a) 순서·도출 날짜) 읽은 대상 4
    ((b) 외부 대기 44 · dev 선행 28) 읽은 대상 44
    ((c) 3레버 결정자) 읽은 대상 3
[PASS] AC-LP-013 Go/No-Go · 컷오버 · 하이퍼케어 · RACI
    ((a) 4상태·결정권자 1인·회의일) 읽은 대상 4
    ((b) 컷오버 5칸·롤백 트리거) 읽은 대상 7
    ((c) 하이퍼케어 지표·날짜 없음) 읽은 대상 5
    ((d) RACI A 1개·소통 5요소) 읽은 대상 8
    [(v) 대조 제외 6] 퀵스타트 가이드 · 메뉴얼 · 위젯빌더 메뉴얼 · 테이블 명세서 · SDK 개발가이드 · 임베드 라이브 데모
[PASS] AC-LP-014 판매 준비 프로세스 3블록
    ((a) 메뉴 37·내역) 읽은 대상 37
    ((b) 갭 열·갭 5건) 읽은 대상 37
    ((c) 갭 수·기준·원천) 읽은 대상 1
    ((d) 옵션↔가격 5단계·고치는 화면) 읽은 대상 5
    ((e) 매트릭스 사슬·값·하한(원고 분모 162 · 부록 162 · 요약 합 162 · 화면 대상 31 · 미실측 0)) 읽은 대상 162

== D3 RESULT: 14/14 PASS ==
exit=0
```

#### Evidence — 음성 대조(AC-LP-015(d))

다크 규칙 1줄 주입 복사본(`</style>` 앞에 `@media (prefers-color-scheme: dark){…}` · 검사 후 삭제):

```
[FAIL] AC-LP-001 자기완결 + 라이트 테마 전용
    (① 허용 밖 외부 참조 0) 읽은 대상 2
    (② prefers-color-scheme 0) 읽은 대상 1 · 위반 1
        - prefers-color-scheme 출현 1
    (③ 다크 선택자 0) 읽은 대상 1
    (④ 테마 토글 0) 읽은 대상 1
    [산문 제외 코드 블록 4] pre.mermaid 「flowchart LR subgraph L1["고객 화면·자사몰 스킨」 | pre.mermaid 「flowchart LR n1["자사몰 스킨 컨테이너 — Lightsa」 | pre.mermaid 「flowchart LR s1["1. 상품 구성요소<br/>고치는 화면」 | pre#denominator-command.cmd 「cd raw/webadmin/tools && python3 -c 'imp」
[PASS] AC-LP-002 Out of Scope 금지 4종
    ((a) 내부 코드명 0) 읽은 대상 1800
    ((가드) 산문 제외 코드 블록 = mermaid 원문·분모 명령뿐) 읽은 대상 4
    ((b) 반려 수치 0) 읽은 대상 1800
    ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1432
    ((d) 조판 ≤ 1) 읽은 대상 1800
[PASS] AC-LP-003 13섹션 골격 + 범위 두 표
    ((a) sec-01~13 순서) 읽은 대상 13
    ((b) 범위 두 표·동결 규칙) 읽은 대상 2
[PASS] AC-LP-004 스윔레인 3~7 + CTO 10구간 매핑
    ((a) 파싱·레인 수·소유 라벨) 읽은 대상 4
    ((b) 상위 10 · 구간 참조) 읽은 대상 10
    ((c) 영향 문장) 읽은 대상 10
[PASS] AC-LP-005 요약·마일스톤·배치도
    ((a) 요약 6요소·700자·범례) 읽은 대상 6
    ((b) 마일스톤 4~6·오너·상태·T1 첫째) 읽은 대상 5
    ((c) 배치도 ≤12·실명·소유 라벨) 읽은 대상 10
[PASS] AC-LP-006 체크리스트 7블록·3~5·6칸·체크 방법·소유 분류
    ((a) 7블록·최상위 3~5) 읽은 대상 7
    ((b) 6칸 빈칸 0) 읽은 대상 28
    ((c) 담당 실명) 읽은 대상 28
    ((d) 체크 방법 실질) 읽은 대상 28
    ((e) 소유·분류 제약(전 행)) 읽은 대상 735
[PASS] AC-LP-007 증거 실재 + 작동 판정 규율
    ((a) 파일:줄 실재(최상위 · 파일 증거 28)) 읽은 대상 28
    ((b) URL@일시 형식·호스트(최상위 · URL 증거 0)) 읽은 대상 28
    ((c) data-std 원장 실재 · NEW 5 ≤ 30%) 읽은 대상 28
    ((d) 작동 행 전부 URL@일시 ≥ 2026-09-16(전 행)) 읽은 대상 115
[PASS] AC-LP-008 구간 귀속 + 역할별 진입점
    (data-step 허용 집합) 읽은 대상 735
    (역할 앵커 5·각 1건 이상) 읽은 대상 5
[PASS] AC-LP-009 T1 인프라
    ((a) F1~F4) 읽은 대상 4
    ((b) 진입 조건 정확히 9) 읽은 대상 9
    ((c) F3-11·F4-8 금지 표기) 읽은 대상 2
    ((d) 리다이렉트 재검증) 읽은 대상 1
[PASS] AC-LP-010 T4 3단 + D-P1~9 + C1~C7 값 대조
    ((a) 3단계 순서) 읽은 대상 3
    ((b) D-P1~9 결정자) 읽은 대상 9
    ((c) 상태 값 대조) 읽은 대상 8
[PASS] AC-LP-011 T3·T6 이관 6단계 + 외부 의존 계층
    ((a) 6단계 순서) 읽은 대상 2
    ((b) 하위 외부 의존 정확히 2) 읽은 대상 2
    ((c) T6 밖 대기 2종) 읽은 대상 1
    ((d) 미확정·P-6) 읽은 대상 2
[PASS] AC-LP-012 임계경로 전진 구축 + 3레버
    ((a) 순서·도출 날짜) 읽은 대상 4
    ((b) 외부 대기 44 · dev 선행 28) 읽은 대상 44
    ((c) 3레버 결정자) 읽은 대상 3
[PASS] AC-LP-013 Go/No-Go · 컷오버 · 하이퍼케어 · RACI
    ((a) 4상태·결정권자 1인·회의일) 읽은 대상 4
    ((b) 컷오버 5칸·롤백 트리거) 읽은 대상 7
    ((c) 하이퍼케어 지표·날짜 없음) 읽은 대상 5
    ((d) RACI A 1개·소통 5요소) 읽은 대상 8
    [(v) 대조 제외 6] 퀵스타트 가이드 · 메뉴얼 · 위젯빌더 메뉴얼 · 테이블 명세서 · SDK 개발가이드 · 임베드 라이브 데모
[PASS] AC-LP-014 판매 준비 프로세스 3블록
    ((a) 메뉴 37·내역) 읽은 대상 37
    ((b) 갭 열·갭 5건) 읽은 대상 37
    ((c) 갭 수·기준·원천) 읽은 대상 1
    ((d) 옵션↔가격 5단계·고치는 화면) 읽은 대상 5
    ((e) 매트릭스 사슬·값·하한(원고 분모 162 · 부록 162 · 요약 합 162 · 화면 대상 31 · 미실측 0)) 읽은 대상 162

== D3 RESULT: 13/14 PASS · FAIL AC-LP-001 ==
exit=1
```

분모 명령 선언 1글자 변경 복사본(`print(n)` → `print(n+1)` · 검사 후 삭제):

```
[PASS] AC-LP-001 자기완결 + 라이트 테마 전용
    (① 허용 밖 외부 참조 0) 읽은 대상 2
    (② prefers-color-scheme 0) 읽은 대상 1
    (③ 다크 선택자 0) 읽은 대상 1
    (④ 테마 토글 0) 읽은 대상 1
    [산문 제외 코드 블록 4] pre.mermaid 「flowchart LR subgraph L1["고객 화면·자사몰 스킨」 | pre.mermaid 「flowchart LR n1["자사몰 스킨 컨테이너 — Lightsa」 | pre.mermaid 「flowchart LR s1["1. 상품 구성요소<br/>고치는 화면」 | pre#denominator-command.cmd 「cd raw/webadmin/tools && python3 -c 'imp」
[PASS] AC-LP-002 Out of Scope 금지 4종
    ((a) 내부 코드명 0) 읽은 대상 1800
    ((가드) 산문 제외 코드 블록 = mermaid 원문·분모 명령뿐) 읽은 대상 4
    ((b) 반려 수치 0) 읽은 대상 1800
    ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1432
    ((d) 조판 ≤ 1) 읽은 대상 1800
[PASS] AC-LP-003 13섹션 골격 + 범위 두 표
    ((a) sec-01~13 순서) 읽은 대상 13
    ((b) 범위 두 표·동결 규칙) 읽은 대상 2
[PASS] AC-LP-004 스윔레인 3~7 + CTO 10구간 매핑
    ((a) 파싱·레인 수·소유 라벨) 읽은 대상 4
    ((b) 상위 10 · 구간 참조) 읽은 대상 10
    ((c) 영향 문장) 읽은 대상 10
[PASS] AC-LP-005 요약·마일스톤·배치도
    ((a) 요약 6요소·700자·범례) 읽은 대상 6
    ((b) 마일스톤 4~6·오너·상태·T1 첫째) 읽은 대상 5
    ((c) 배치도 ≤12·실명·소유 라벨) 읽은 대상 10
[PASS] AC-LP-006 체크리스트 7블록·3~5·6칸·체크 방법·소유 분류
    ((a) 7블록·최상위 3~5) 읽은 대상 7
    ((b) 6칸 빈칸 0) 읽은 대상 28
    ((c) 담당 실명) 읽은 대상 28
    ((d) 체크 방법 실질) 읽은 대상 28
    ((e) 소유·분류 제약(전 행)) 읽은 대상 735
[PASS] AC-LP-007 증거 실재 + 작동 판정 규율
    ((a) 파일:줄 실재(최상위 · 파일 증거 28)) 읽은 대상 28
    ((b) URL@일시 형식·호스트(최상위 · URL 증거 0)) 읽은 대상 28
    ((c) data-std 원장 실재 · NEW 5 ≤ 30%) 읽은 대상 28
    ((d) 작동 행 전부 URL@일시 ≥ 2026-09-16(전 행)) 읽은 대상 115
[PASS] AC-LP-008 구간 귀속 + 역할별 진입점
    (data-step 허용 집합) 읽은 대상 735
    (역할 앵커 5·각 1건 이상) 읽은 대상 5
[PASS] AC-LP-009 T1 인프라
    ((a) F1~F4) 읽은 대상 4
    ((b) 진입 조건 정확히 9) 읽은 대상 9
    ((c) F3-11·F4-8 금지 표기) 읽은 대상 2
    ((d) 리다이렉트 재검증) 읽은 대상 1
[PASS] AC-LP-010 T4 3단 + D-P1~9 + C1~C7 값 대조
    ((a) 3단계 순서) 읽은 대상 3
    ((b) D-P1~9 결정자) 읽은 대상 9
    ((c) 상태 값 대조) 읽은 대상 8
[PASS] AC-LP-011 T3·T6 이관 6단계 + 외부 의존 계층
    ((a) 6단계 순서) 읽은 대상 2
    ((b) 하위 외부 의존 정확히 2) 읽은 대상 2
    ((c) T6 밖 대기 2종) 읽은 대상 1
    ((d) 미확정·P-6) 읽은 대상 2
[PASS] AC-LP-012 임계경로 전진 구축 + 3레버
    ((a) 순서·도출 날짜) 읽은 대상 4
    ((b) 외부 대기 44 · dev 선행 28) 읽은 대상 44
    ((c) 3레버 결정자) 읽은 대상 3
[PASS] AC-LP-013 Go/No-Go · 컷오버 · 하이퍼케어 · RACI
    ((a) 4상태·결정권자 1인·회의일) 읽은 대상 4
    ((b) 컷오버 5칸·롤백 트리거) 읽은 대상 7
    ((c) 하이퍼케어 지표·날짜 없음) 읽은 대상 5
    ((d) RACI A 1개·소통 5요소) 읽은 대상 8
    [(v) 대조 제외 6] 퀵스타트 가이드 · 메뉴얼 · 위젯빌더 메뉴얼 · 테이블 명세서 · SDK 개발가이드 · 임베드 라이브 데모
[FAIL] AC-LP-014 판매 준비 프로세스 3블록
    ((a) 메뉴 37·내역) 읽은 대상 37
    ((b) 갭 열·갭 5건) 읽은 대상 37
    ((c) 갭 수·기준·원천) 읽은 대상 1
    ((d) 옵션↔가격 5단계·고치는 화면) 읽은 대상 5
    ((e) 매트릭스 사슬·값·하한(원고 분모 null · 부록 162 · 요약 합 162 · 화면 대상 31 · 미실측 0)) 읽은 대상 162 · 위반 1
        - (i) D1 선언 명령 ≠ 4번째 인자

== D3 RESULT: 13/14 PASS · FAIL AC-LP-014 ==
exit=1
```

#### Evidence — AC-LP-015 (a)(b)(c)

(a) 산출 3종 존재·0바이트 아님: `docs/huni/후니프린팅_오픈계획서_260917.html` 766,250B · `S5-plan/build_xlsx_v4_plan.py` 56,590B + `docs/huni/후니프린팅_오픈계획서_260917.xlsx` 571,684B · `S5-plan/verify_plan.mjs` 34,672B.
(b)(c) D2 빌더:

```
$ python3 -B _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/build_xlsx_v4_plan.py; echo exit=$?
[OK] /Users/innojini/Dev/HuniWeb/.claude/worktrees/t47/docs/huni/후니프린팅_오픈계획서_260917.xlsx
  [PASS] 02 행 수 654 == 원장 654
  [PASS] 02 「분류」 빈칸 0건 (미판정은 허용)
  [PASS] 02 「난이도」 빈칸 0건 (미판정은 허용)
  [PASS] 02 「담당역할」 빈칸 0건 (미판정은 허용)
  [PASS] 02 std_id 유일
  [PASS] 07 ①블록 남은 일 348행 == 원장 남은 일 348 · 중복 0 · 누락 0 · 여분 0
  [PASS] 06 행 수 89 == 회의 추적 89
  [PASS] 시트명 ['00_읽는법', '01_역할정의', '02_IA마스터', '03_페이즈일정', '04_진행현황', '05_주차별실행판', '06_회의결정추적', '07_체크리스트', '08_변경이력', '09_구간상태판', '10_트랙체크리스트']
  [PASS] 02 원장 시트 데이터 행 654 == 654
  [PASS] 10 트랙체크리스트 735행 == D1 data-role top+detail 735
  [PASS] 09 구간상태판 32행 == status-28.csv
  [PASS] 07 안내 행(A3) 실재 — 9/16 판 표기 · 10 시트 안내
  [PASS] 00 안내 행 실재
== RESULT: ALL PASS ==
exit=0
```

#### AC 판정 요약(run 레인 관측 — 최종 판정은 리드)

| AC | 결과 | 근거 |
|---|---|---|
| AC-LP-001~014 | PASS 14/14 | D3 최종 종료 코드 0 |
| AC-LP-015 (a)(b)(c)(d) | PASS | 위 Evidence · 음성 대조 종료 코드 1 |
| **AC-LP-016** | **미판정** | 사람 2인(CTO 시점 · 실무운영 시점) 5문항 판정은 레인이 대신할 수 없다 — `review-notes.md` 미작성(가짜 판정을 만들지 않는다). 리드 계획: M4 후 CTO·실무진 시점 독립 리뷰 |

#### Gaps

1. **AC-LP-016 사람 판정 미실시** — DoD 3항 미충족.
2. D3 는 이 레인이 작성했다 — 검사 해석(산문 코드 블록 제외 · (v) 커버 판정 규칙 · 외부 대기 대상 부적격 판정)은 리드 재실행·재집계가 독립 판정이다.
3. 윤문은 Fast 모드 1패스 — 생성기 산문만 대상. 원장·런북에서 옮겨 온 행 제목·체크 방법 문장(부록 detail 707행 포함)은 원천 문장이라 손대지 않았다.
4. D1 크기 766KB(부록 detail 707행) — 사람 판정에서 읽기 부담이 나오면 부록 분리 여부는 리드 판단.
5. 운영 섹션 제안값(「제안(확정 전)」 26곳 · 지니 결정 목록 7행)은 확정 전이다.
6. 푸시 전 체크(`7abdcdba` 이력 평문 연락처 squash)는 sync 레인 몫 — 미실시.

#### Residual-risk

- 가장 이른 완료일(하한)과 담당자 직렬 하한은 원장 작업량구간이 맞다는 전제 위에 있다. 1주+ 는 상한이 없다.
- 원장·런북·결정 문서가 바뀌면 `build_plan_rows.py → build_d1.py → build_xlsx_v4_plan.py → verify_plan.mjs` 순서로 다시 돌려야 한다(손으로 고친 수치는 없다).

### M4-보정 — AC-LP-016 대리 리뷰 반영 (2026-09-17 14:20 KST · 리드 dispatch)

#### Claim

리드가 전달한 AI 대리 리뷰 2건(**사람 판정 아님** · CTO 시점 (c) FAIL · 실무운영 시점 (b)(c) FAIL)의 지적과 리드가 D1 에서 확인한 잔존 결함 F1~F9 를 **생성기에서** 고치고 재생성했다(HTML 직접 수정 0). D3 = **14/14 PASS · exit 0** · 음성 대조 exit 1 유지. 기준 D1 md5 `6a3f1b86…` → 보정 후 `e25e4a3f93da64f02d5780bf4368fb74`.

#### 지적 · 수정 · 재검

| # | 지적(리드 확인) | 수정 | 재검 |
|---|---|---|---|
| F1 | T1-1·T1-2 체크 방법에 호스트·서비스명·리전·로그 위치 없음 | 원천(S4 migration-plan.md:14·:42 · :47)에서 서비스명 `dev-huni-admin-apne2-cs` · 리전 서울(apne2) · `{"ok":true}` · 검증 4종 명령명을 넣음. 원천에 없는 **전체 호스트 주소·리허설 로그 보관 위치는 「원천 미기재 · 확인처: 인프라팀」 명시**(지어내지 않음) | 아래 전후 |
| F2 | T2-1 조합 없음 · 기본사양가 화면 누락 | 원천(S5-live option-price-trace.md §2·§3 · status-28.csv:3)의 조합: 아크릴키링 PRD_000146 사이즈 7종 중 하나(사이즈 이름은 원천 미기재)·고리 「고리없음」·수량 1 / 무선책자 PRD_000069 A5세로·백색모조지 120g·페이지 24·부수 2 + `/admin/start-price/` 「없음」 2행 | 아래 전후 |
| F3 | 최상위 행에 T 번호 13/28 | 최상위 28행 전부 행 머리에 T 번호 | `class="rid"` 28 |
| F4 | 이번 주 할 일을 못 답함 | 진입점 머리에 규칙 한 줄(상태≠완료 · 선행 칸 「—」) + 행마다 「이번 주 착수 가능 / 선행 대기」 · 목표일 칸 머리 「하한 — 마감일 아님」 · 날짜 신설 0 | 착수 가능 1(T2-1) · 선행 대기 25 · 외부 행 2 제외 |
| F5 | T1 마일스톤 날짜가 오늘로 보임 | 미산정이 과반이면 「미산정 N/M — 계산 불가」를 앞에, 계산값은 괄호 뒤 | 「계산 불가」 1(T1 마일스톤 · 미산정 3/4) |
| F6 | 수동 등록 목록에 트랙 연결 없음 · 블록 진입 가로막음 | 항목마다 「연결 T 번호」 또는 「참고(트랙 행 밖)」 · 본문은 요약 5줄, 전체 목록 2개는 접힘 | `#api-summary` 5줄 · `#api-forced`·`#api-admin-only` 둘 다 `<details>` 안 |
| F7 | §8 「외부(MES 공급사)」 잔존 | T4-4 선행·S2-3 담당을 원천대로 「외부(MES 담당)」 · 같은 성격의 T4-3 「외부(PitStop 공급사)」→「대표(구매) PitStop 구매·라이선스 결정(D-P1)」 · T1-4 「외부(Vercel 계정 소유자)」→「김동학·PM Vercel 프로젝트 접근」 · T6-1 「구 사이트 운영사」→「구 사이트 운영사/IDC」 | HTML 「공급사」 0 · 「계정 소유자」 0 |
| F8 | 「차단 입력 -5(」 잘린 표시 | T4-1·T3-5 선행 문구에서 식별자 제거 → 「선행 입력 「order/register 계약」」·「선행 입력 「클레임·문의 범위」」 | 전 문서 정규식 ` -\d\(` 0 · 「차단 입력 -」 0 |
| F9 | §8 날짜와 §9 결정1이 판정처럼 읽힘 | 도출 날짜 바로 뒤 「범위·인원 조정 전 계산값이며 오픈 가능 여부 판정이 아니다. 판정은 Go/No-Go 회의에서 한다.」 · 결정1 표시 제목 「오픈 범위를 어디까지로 할 것인가」(원문은 CSV·원천 유지) | D3 AC-LP-002(c) PASS(새 문장은 critical-path 안) |

#### 전후 — 체크 방법(F1·F2)

- T1-1 전: Lightsail 콘솔을 열어 admin 서비스가 healthy 이고 /healthz 페이지에 접속하면 ok 가 나오는지 조회한다
  후: AWS Lightsail 콘솔(서울 리전 · apne2)을 열어 컨테이너 서비스 dev-huni-admin-apne2-cs 의 admin 이 healthy · cron 이 running 인지 조회하고, 그 서비스의 /healthz 페이지에 접속해 {"ok":true} 가 나오는지 본다 — 서비스 전체 호스트 주소는 원천 미기재 · 확인처: 인프라팀
- T1-2 전: 리허설 결과 로그를 열어 검증 4종이 exit 0 이고 행수 지문 목록이 Railway 와 같은지 조회한다
  후: 리허설 실행 화면에서 검증 4종(audit_data_integrity --exposure --strict · audit_published_prices · audit_set_qty_mode · audit_engine_dims)이 모두 exit 0 인지 조회하고, 테이블별 행수 지문 목록이 Railway 와 같은지 본다 — 리허설 로그 보관 위치는 원천 미기재 · 확인처: 인프라팀
- T2-1 전: webadmin 가격 시뮬레이터 화면을 열어 아크릴키링·무선책자를 조회하고 0원 구성요소가 목록에 없는지 확인한다
  후: webadmin 가격 시뮬레이터 화면을 열어 ① 아크릴키링(PRD_000146)에 사이즈 7종 중 하나 · 고리 「고리없음」 · 수량 1 을 넣고 ② 무선책자(PRD_000069)에 본품 사이즈 A5세로 · 내지종이 백색모조지 120g · 페이지 24 · 부수 2 를 넣어 두 상품 모두 제외 구성요소 0 · 합계 0원 아님을 조회한다. 이어 상품별 기본사양가 메뉴(/admin/start-price/) 목록을 열어 「없음」 2행이 0행이 됐는지 본다

#### Evidence — D3

```
$ node _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/verify_plan.mjs docs/huni/후니프린팅_오픈계획서_260917.html <원장 CSV> <매트릭스 CSV> "<README 분모 명령>"
[PASS] AC-LP-001 자기완결 + 라이트 테마 전용
    (① 허용 밖 외부 참조 0) 읽은 대상 2
    (② prefers-color-scheme 0) 읽은 대상 1
    (③ 다크 선택자 0) 읽은 대상 1
    (④ 테마 토글 0) 읽은 대상 1
    [산문 제외 코드 블록 4] pre.mermaid 「flowchart LR subgraph L1["고객 화면·자사몰 스킨」 | pre.mermaid 「flowchart LR n1["자사몰 스킨 컨테이너 — Lightsa」 | pre.mermaid 「flowchart LR s1["1. 상품 구성요소<br/>고치는 화면」 | pre#denominator-command.cmd 「cd raw/webadmin/tools && python3 -c 'imp」
[PASS] AC-LP-002 Out of Scope 금지 4종
    ((a) 내부 코드명 0) 읽은 대상 1968
    ((가드) 산문 제외 코드 블록 = mermaid 원문·분모 명령뿐) 읽은 대상 4
    ((b) 반려 수치 0) 읽은 대상 1968
    ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1600
    ((d) 조판 ≤ 1) 읽은 대상 1968
[PASS] AC-LP-003 13섹션 골격 + 범위 두 표
    ((a) sec-01~13 순서) 읽은 대상 13
    ((b) 범위 두 표·동결 규칙) 읽은 대상 2
[PASS] AC-LP-004 스윔레인 3~7 + CTO 10구간 매핑
    ((a) 파싱·레인 수·소유 라벨) 읽은 대상 4
    ((b) 상위 10 · 구간 참조) 읽은 대상 10
    ((c) 영향 문장) 읽은 대상 10
[PASS] AC-LP-005 요약·마일스톤·배치도
    ((a) 요약 6요소·700자·범례) 읽은 대상 6
    ((b) 마일스톤 4~6·오너·상태·T1 첫째) 읽은 대상 5
    ((c) 배치도 ≤12·실명·소유 라벨) 읽은 대상 10
[PASS] AC-LP-006 체크리스트 7블록·3~5·6칸·체크 방법·소유 분류
    ((a) 7블록·최상위 3~5) 읽은 대상 7
    ((b) 6칸 빈칸 0) 읽은 대상 28
    ((c) 담당 실명) 읽은 대상 28
    ((d) 체크 방법 실질) 읽은 대상 28
    ((e) 소유·분류 제약(전 행)) 읽은 대상 735
[PASS] AC-LP-007 증거 실재 + 작동 판정 규율
    ((a) 파일:줄 실재(최상위 · 파일 증거 28)) 읽은 대상 28
    ((b) URL@일시 형식·호스트(최상위 · URL 증거 0)) 읽은 대상 28
    ((c) data-std 원장 실재 · NEW 5 ≤ 30%) 읽은 대상 28
    ((d) 작동 행 전부 URL@일시 ≥ 2026-09-16(전 행)) 읽은 대상 115
[PASS] AC-LP-008 구간 귀속 + 역할별 진입점
    (data-step 허용 집합) 읽은 대상 735
    (역할 앵커 5·각 1건 이상) 읽은 대상 5
[PASS] AC-LP-009 T1 인프라
    ((a) F1~F4) 읽은 대상 4
    ((b) 진입 조건 정확히 9) 읽은 대상 9
    ((c) F3-11·F4-8 금지 표기) 읽은 대상 2
    ((d) 리다이렉트 재검증) 읽은 대상 1
[PASS] AC-LP-010 T4 3단 + D-P1~9 + C1~C7 값 대조
    ((a) 3단계 순서) 읽은 대상 3
    ((b) D-P1~9 결정자) 읽은 대상 9
    ((c) 상태 값 대조) 읽은 대상 8
[PASS] AC-LP-011 T3·T6 이관 6단계 + 외부 의존 계층
    ((a) 6단계 순서) 읽은 대상 2
    ((b) 하위 외부 의존 정확히 2) 읽은 대상 2
    ((c) T6 밖 대기 2종) 읽은 대상 1
    ((d) 미확정·P-6) 읽은 대상 2
[PASS] AC-LP-012 임계경로 전진 구축 + 3레버
    ((a) 순서·도출 날짜) 읽은 대상 4
    ((b) 외부 대기 44 · dev 선행 28) 읽은 대상 44
    ((c) 3레버 결정자) 읽은 대상 3
[PASS] AC-LP-013 Go/No-Go · 컷오버 · 하이퍼케어 · RACI
    ((a) 4상태·결정권자 1인·회의일) 읽은 대상 4
    ((b) 컷오버 5칸·롤백 트리거) 읽은 대상 7
    ((c) 하이퍼케어 지표·날짜 없음) 읽은 대상 5
    ((d) RACI A 1개·소통 5요소) 읽은 대상 8
    [(v) 대조 제외 6] 퀵스타트 가이드 · 메뉴얼 · 위젯빌더 메뉴얼 · 테이블 명세서 · SDK 개발가이드 · 임베드 라이브 데모
[PASS] AC-LP-014 판매 준비 프로세스 3블록
    ((a) 메뉴 37·내역) 읽은 대상 37
    ((b) 갭 열·갭 5건) 읽은 대상 37
    ((c) 갭 수·기준·원천) 읽은 대상 1
    ((d) 옵션↔가격 5단계·고치는 화면) 읽은 대상 5
    ((e) 매트릭스 사슬·값·하한(원고 분모 162 · 부록 162 · 요약 합 162 · 화면 대상 31 · 미실측 0)) 읽은 대상 162

== D3 RESULT: 14/14 PASS ==
exit=0
```

음성 대조(다크 규칙 1줄 주입 · 검사 후 삭제):

```
[FAIL] AC-LP-001 자기완결 + 라이트 테마 전용
    (① 허용 밖 외부 참조 0) 읽은 대상 2
    (② prefers-color-scheme 0) 읽은 대상 1 · 위반 1
        - prefers-color-scheme 출현 1
    (③ 다크 선택자 0) 읽은 대상 1
    (④ 테마 토글 0) 읽은 대상 1
    [산문 제외 코드 블록 4] pre.mermaid 「flowchart LR subgraph L1["고객 화면·자사몰 스킨」 | pre.mermaid 「flowchart LR n1["자사몰 스킨 컨테이너 — Lightsa」 | pre.mermaid 「flowchart LR s1["1. 상품 구성요소<br/>고치는 화면」 | pre#denominator-command.cmd 「cd raw/webadmin/tools && python3 -c 'imp」
[PASS] AC-LP-002 Out of Scope 금지 4종
    ((a) 내부 코드명 0) 읽은 대상 1968
    ((가드) 산문 제외 코드 블록 = mermaid 원문·분모 명령뿐) 읽은 대상 4
    ((b) 반려 수치 0) 읽은 대상 1968
    ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1600
    ((d) 조판 ≤ 1) 읽은 대상 1968
[PASS] AC-LP-003 13섹션 골격 + 범위 두 표
    ((a) sec-01~13 순서) 읽은 대상 13
    ((b) 범위 두 표·동결 규칙) 읽은 대상 2
[PASS] AC-LP-004 스윔레인 3~7 + CTO 10구간 매핑
    ((a) 파싱·레인 수·소유 라벨) 읽은 대상 4
    ((b) 상위 10 · 구간 참조) 읽은 대상 10
    ((c) 영향 문장) 읽은 대상 10
[PASS] AC-LP-005 요약·마일스톤·배치도
    ((a) 요약 6요소·700자·범례) 읽은 대상 6
    ((b) 마일스톤 4~6·오너·상태·T1 첫째) 읽은 대상 5
    ((c) 배치도 ≤12·실명·소유 라벨) 읽은 대상 10
[PASS] AC-LP-006 체크리스트 7블록·3~5·6칸·체크 방법·소유 분류
    ((a) 7블록·최상위 3~5) 읽은 대상 7
    ((b) 6칸 빈칸 0) 읽은 대상 28
    ((c) 담당 실명) 읽은 대상 28
    ((d) 체크 방법 실질) 읽은 대상 28
    ((e) 소유·분류 제약(전 행)) 읽은 대상 735
[PASS] AC-LP-007 증거 실재 + 작동 판정 규율
    ((a) 파일:줄 실재(최상위 · 파일 증거 28)) 읽은 대상 28
    ((b) URL@일시 형식·호스트(최상위 · URL 증거 0)) 읽은 대상 28
    ((c) data-std 원장 실재 · NEW 5 ≤ 30%) 읽은 대상 28
    ((d) 작동 행 전부 URL@일시 ≥ 2026-09-16(전 행)) 읽은 대상 115
[PASS] AC-LP-008 구간 귀속 + 역할별 진입점
    (data-step 허용 집합) 읽은 대상 735
    (역할 앵커 5·각 1건 이상) 읽은 대상 5
[PASS] AC-LP-009 T1 인프라
    ((a) F1~F4) 읽은 대상 4
    ((b) 진입 조건 정확히 9) 읽은 대상 9
    ((c) F3-11·F4-8 금지 표기) 읽은 대상 2
    ((d) 리다이렉트 재검증) 읽은 대상 1
[PASS] AC-LP-010 T4 3단 + D-P1~9 + C1~C7 값 대조
    ((a) 3단계 순서) 읽은 대상 3
    ((b) D-P1~9 결정자) 읽은 대상 9
    ((c) 상태 값 대조) 읽은 대상 8
[PASS] AC-LP-011 T3·T6 이관 6단계 + 외부 의존 계층
    ((a) 6단계 순서) 읽은 대상 2
    ((b) 하위 외부 의존 정확히 2) 읽은 대상 2
    ((c) T6 밖 대기 2종) 읽은 대상 1
    ((d) 미확정·P-6) 읽은 대상 2
[PASS] AC-LP-012 임계경로 전진 구축 + 3레버
    ((a) 순서·도출 날짜) 읽은 대상 4
    ((b) 외부 대기 44 · dev 선행 28) 읽은 대상 44
    ((c) 3레버 결정자) 읽은 대상 3
[PASS] AC-LP-013 Go/No-Go · 컷오버 · 하이퍼케어 · RACI
    ((a) 4상태·결정권자 1인·회의일) 읽은 대상 4
    ((b) 컷오버 5칸·롤백 트리거) 읽은 대상 7
    ((c) 하이퍼케어 지표·날짜 없음) 읽은 대상 5
    ((d) RACI A 1개·소통 5요소) 읽은 대상 8
    [(v) 대조 제외 6] 퀵스타트 가이드 · 메뉴얼 · 위젯빌더 메뉴얼 · 테이블 명세서 · SDK 개발가이드 · 임베드 라이브 데모
[PASS] AC-LP-014 판매 준비 프로세스 3블록
    ((a) 메뉴 37·내역) 읽은 대상 37
    ((b) 갭 열·갭 5건) 읽은 대상 37
    ((c) 갭 수·기준·원천) 읽은 대상 1
    ((d) 옵션↔가격 5단계·고치는 화면) 읽은 대상 5
    ((e) 매트릭스 사슬·값·하한(원고 분모 162 · 부록 162 · 요약 합 162 · 화면 대상 31 · 미실측 0)) 읽은 대상 162

== D3 RESULT: 13/14 PASS · FAIL AC-LP-001 ==
exit=1
```

D2 빌더 재실행 `== RESULT: ALL PASS ==`(10 트랙체크리스트 735 == D1 735).

#### 변경량 기록

difflib 산문 줄(한글 12자 이상) 기준 변경률: `build_d1.py` 3.1% · `build_plan_rows.py` 2.1% — 윤문이 아니라 내용 보정(값·표시 추가)이라 윤문 변경률 0.6% 와 따로 적는다.

#### Gaps

1. **AC-LP-016 은 여전히 사람 판정 전** — 리드가 같은 두 리뷰를 새 인스턴스로 재실행한다(대리 리뷰도 사람 판정이 아니다).
2. F4 「이번 주 착수 가능」은 **선행 해소 기록이 문서에 없어** 선행이 적힌 행을 전부 대기로 본 보수 규칙이다 — 실제로는 착수할 수 있는 행이 더 있을 수 있다.
3. F1 T1-1 의 전체 호스트 주소 · T1-2 의 로그 위치 · F2 아크릴키링 사이즈 이름은 원천 미기재로 남았다.
4. 렌더는 다시 열어 보지 않았다 — mermaid 원문은 바뀌지 않았고 D3 가 파싱 PASS.

### M4-보정2 — AC-LP-016 대리 재판정 실무운영 (b) 보강 (2026-09-17 · 리드 dispatch · 지니 결정 260917)

#### Claim

대리 재판정(D1 `e25e4a3f…`) 실무운영 (b) FAIL 과 공통 마찰 3건을 **생성기 `build_d1.py` 에서만** 고치고 재생성했다(HTML 직접 수정 0 · `plan-rows.csv` 무변경 → D2 입력 무변경이라 D2 재생성 안 함). D3 = **14/14 PASS · exit 0**. 기준 D1 md5 `e25e4a3f93da64f02d5780bf4368fb74` → 보정2 후 **`96ce574ce6407c39947fefed154bac60`**.

#### 지시 · 수정

| # | 지시 | 수정 |
|---|---|---|
| 1 | 진입점에 「이번 주 선행 풀기」 · 규칙 한 줄 · 신설 0 · 여러 행을 막으면 1건 | `unblock_items()` — 최상위 28행의 **선행 칸만** ` · ` 로 나눠 T 행은 제외, `외부(`→회신 받기 · `결정 N`→결정 요청(`결정 23·29` 는 2건) · `선행 입력`→입력 받기를 행 담당 역할에, 이름(지니·신우진·PM·김동학·서희항·최숙진·김용기·채훈희·대표)이 적힌 선행은 그 사람 역할에 입력 제공/결정 내리기 + 행 담당 역할에 입력 받기/결정 요청. 키(선행 문구 · 결정은 번호) 기준 dedupe. 규칙 문장 `#unblock-rule` 진입점 머리. 표시 문구는 선행 칸 원문 그대로 |
| 2 | §7 진입점을 수동 등록 요약보다 앞으로 | `tracks_html` 순서 `진입점 → api_block → 트랙` |
| 3 | §1 T1-4 하한 9/17 vs §7 선행 대기 불일치 | `due_with_state()` — §1 다음 마일스톤 카드 하한 옆에 선행 대기 배지(하한 계산식 무변경). `startable()` 은 위로 옮겨 한 곳에서 정의 |
| 4 | 본문에 기준일 10/6 사실 문장 · 판정 술어 금지 · §10 과 연결 | §1 「무엇·왜」 카드 `#base-date`: 「기준일(역산 기준점)은 2026-10-06 이다. Go/No-Go 판정 회의 제안일 2026-10-02 는 기준일 직전 근무일이다.」 · §10 「(기준일 2026-10-06 직전 근무일)」 → `#base-date` 링크 |

#### Evidence — D3 (최종 D1 `96ce574c…`)

```
$ node _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/verify_plan.mjs docs/huni/후니프린팅_오픈계획서_260917.html /Users/innojini/Dev/HuniWeb/_workspace/huni-launch-runway/07_rebaseline/R/R2/unified-ledger-v4.csv _workspace/huni-launch-runway/07_rebaseline/S/S5-plan/manual-element-matrix.csv "<D1 #denominator-command 엔티티 해제>"
[PASS] AC-LP-001 ~ [PASS] AC-LP-014 (14줄 전부 PASS)
    ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1695
    (역할 앵커 5·각 1건 이상) 읽은 대상 5
== D3 RESULT: 14/14 PASS ==
exit=0
```

#### Evidence — 음성 대조(기준일 문장)

| 사본 | 바꾼 것 | 결과 |
|---|---|---|
| neg-a | `은 <b>2026-10-06</b> 이고 오픈 가능하다.` | **14/14 PASS · exit 0 — D3 가 못 잡음** |
| neg-b · neg-c | `은 2026-10-06 이고 오픈 가능하다.`(태그 없음) | **13/14 · FAIL AC-LP-002 (c) 위반 1 · exit 1** |

⇒ D3 (c) 는 텍스트 노드를 줄바꿈으로 이어 문장을 자르므로 **날짜가 인라인 태그 안에 있으면 날짜와 술어가 다른 문장으로 갈려 검사를 빠져나간다**. 그래서 기준일 문장에서 `<b>` 를 뺐다(최종 D1). 사본은 D1 과 같은 `docs/huni/` 에 두고 실행 후 삭제(첫 시도는 scratchpad 에 둬서 AC-LP-007 증거 경로가 깨져 판정 무효).

#### before / after — 역할별 「이번 주 착수 가능 + 선행 풀기」

| 역할 | 최상위 행 | before 착수 가능 | after 착수 가능 | after 선행 풀기 | after 합 |
|---|---|---|---|---|---|
| 쇼핑개발 — 김동학 | 7 | 0 | 0 | 8 | 8 |
| 인쇄개발 — 서희항 | 8 | 0 | 0 | 7 | 7 |
| **실무운영 — 최숙진·김용기** | 5 | **1** (T2-1) | 1 | **3** | **4** |
| PM — 신우진·지니 | 4 | 0 | 0 | 6 | 6 |
| 대표 결정 — 채훈희 | 2 | 0 | 0 | 3 | 3 |

실무운영 선행 풀기 3 = 결정 요청 「결정 10(기본값 지정 주체)」(T2-2) · 입력 받기 「사업자등록증 기준 번호 확정(지니)」(T5-1) · 회신 받기 「외부(카카오) 템플릿 검수」(T5-2). T4-5 의 선행은 T4-4(T 행)뿐이라 규칙상 빠진다. 규칙으로 3건 이상이 나왔고 억지로 채운 항목은 없다.

#### 불변 · 변경량

- D1 전후 대조(스크립트): `tr[data-row-id]` 수 · `STD-*` 집합 · `data-row-id` 집합 · `#derived-open-date` **전부 동일**. 실명 7명 집합 동일 — 출현 횟수만 김동학 279→283 · 지니 23→35(기존 선행 문구를 진입점에 재인용).
- HTML 텍스트 diff = 의도한 4곳뿐(§1 기준일 문장 · §1 T1-4 배지 · §7 진입점 이동+선행 풀기 5블록+규칙 1줄 · §10 링크).
- 변경률: `build_d1.py` 산문 줄(한글 12자 이상) 기준 12/222 = **5.4%** · `git diff --stat` 2 files, +72 −13. `build_plan_rows.py`·CSV 무변경.
- 라이브 쓰기 0 · 푸시 0.

#### Gaps

1. **AC-LP-016 은 여전히 사람 판정 전** — 리드 새 대리 리뷰로 재판정.
2. 선행 풀기는 **최상위 행 선행 칸만** 읽는다. 세부 707행의 선행·D-P1~P9 결정자 표(§7 T4)는 규칙에 넣지 않았다 — 넣으면 역할별 건수가 늘 수 있다(예: 최숙진은 D-P3·D-P5·D-P7·D-P8·D-P9 결정자).
3. 「사업자등록증 기준 번호 확정(지니)」(T5-1)과 「사업자등록증 원본 확인(지니)」(T5-3)은 문구가 달라 PM 에서 2건으로 셌다 — 같은 한 번의 확인으로 풀릴 수 있다.
4. §1 외에 §8 임계경로 표·트랙 표 하한 칸에는 배지를 넣지 않았다(두 표 모두 선행 칸이 바로 옆에 있음).
5. D3 (c) 의 인라인 태그 누락은 **검사기 결함 후보**로 기록만 했다 — `verify_plan.mjs` 는 고치지 않았다(범위 밖 · 리드 판정 대상).
6. 렌더는 다시 열어 보지 않았다(CSS 클래스 `.waiting`·`.rule` 재사용, `h5`·`.unblock-list` 신규 스타일 없음).

### M4-보정3 — D3 인라인 태그 사각지대 (2026-09-17 · 리드 dispatch · 리드 검산 2342064c PASS 후속)

#### Claim

리드가 검사기 결함으로 판정한 D3 AC-LP-002(c) 사각지대를 **`verify_plan.mjs` 에서** 고쳤고, PM 「이번 주 선행 풀기」의 사업자등록증 2건 계수를 생성기에서 1건으로 묶었다. 실제 D1 = **14/14 · exit 0** · 음성 대조 2건 모두 **FAIL · exit 1**. D1 md5 `96ce574c…` → **`566407914fc06ec501efd67c91f76434`**.

#### 수정

| # | 지시 | 수정 |
|---|---|---|
| 1 | (c) 문장 추출을 블록 요소 단위로 | `outside` 텍스트 노드(제외 목록 = `proseNodes` 그대로: script·style·pre·code·td.evidence·.source·#appendix · #critical-path·#decisions 밖)를 가장 가까운 블록 요소(`p, li, td, th, h1~h6, dd, dt, figcaption, caption, summary, blockquote, div, section, header, footer, body`)가 같으면 이어 붙이고, 블록 안 소스 줄바꿈은 공백으로 접은 뒤 블록 사이만 줄바꿈 → 기존과 같은 `(?<=[.!?。])\s+|\n` 분할 · 같은 OPEN_DATE·PRED 판정. (a)(b)(d)·가드는 무변경 |
| 2 | PM 사업자등록증 2건 → 1건 | `target_key()` — 사람이 적힌 선행은 「사람 표시(이름·괄호)를 뺀 첫 낱말 + 적힌 사람 역할 집합」이 같으면 같은 선행. 문구가 다르면 원문을 ` / ` 로 나란히 · 막는 행 T 번호 나열. 규칙 문장 `#unblock-rule` 에 한 구절 추가 |

#### Evidence — D3 · 음성 대조 (복사본은 `docs/huni/` 안에 두고 실행 후 삭제 · 주입 위치 `<section id="sec-02">` 바로 뒤)

```
[real] exit=0                                   (최종 D1 566407914fc06ec501efd67c91f76434)
    ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1190
== D3 RESULT: 14/14 PASS ==
[nega] exit=1                                   <p>기준일 <b>2026-10-06</b> 오픈은 가능하다.</p>
[FAIL] AC-LP-002 Out of Scope 금지 4종
    ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1191 · 위반 1
        - 기준일 2026-10-06 오픈은 가능하다.
== D3 RESULT: 13/14 PASS · FAIL AC-LP-002 ==
[negb] exit=1                                   <p>기준일 2026-10-06 오픈은 가능하다.</p>
[FAIL] AC-LP-002 Out of Scope 금지 4종
    ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1191 · 위반 1
        - 기준일 2026-10-06 오픈은 가능하다.
== D3 RESULT: 13/14 PASS · FAIL AC-LP-002 ==
[posb] exit=0                                   기준일 문장에 <b>2026-10-06</b> 되돌린 복사본
== D3 RESULT: 14/14 PASS ==
```

수정 전 같은 주입 재현: nega **exit 0 · 14/14(못 잡음)** · negb exit 1 — 리드 재현과 일치.

D3 출력 전후 diff(보정2 최종 vs 보정3 최종): (c) 읽은 대상 1695 → 1190(단위가 텍스트 노드 조각 → 블록 문장) · (a)(b)(d) 읽은 대상 2061 → 2060(PM 목록 1줄 감소). 나머지 줄 동일.

#### 역할별 건수 재집계 (착수 가능 + 선행 풀기)

| 역할 | 보정2 | 보정3 |
|---|---|---|
| 쇼핑개발 — 김동학 | 0 + 8 = 8 | 0 + 8 = 8 |
| 인쇄개발 — 서희항 | 0 + 7 = 7 | 0 + 7 = 7 |
| 실무운영 — 최숙진·김용기 | 1 + 3 = 4 | 1 + 3 = 4 |
| **PM — 신우진·지니** | 0 + 6 = 6 | **0 + 5 = 5** (「사업자등록증 기준 번호 확정(지니) / 사업자등록증 원본 확인(지니)」 막는 행 T5-1 · T5-3) |
| 대표 결정 — 채훈희 | 0 + 3 = 3 | 0 + 3 = 3 |

쇼핑개발(T5-3)·실무운영(T5-1)은 각자 한 행만 막혀 있어 원래 1건 — 변화 없음.

#### 변경량 · 가드

`git diff --stat`: build_d1.py +15/−4 · verify_plan.mjs +13/−1 · D1 HTML 1줄(PM 블록). 줄 기준 변경률 build_d1.py 1.9% · verify_plan.mjs 2.4%. HTML 직접 수정 0 · CSV 무변경(D2 재생성 불요) · 라이브 쓰기 0 · 푸시 0.

#### Gaps

1. 「첫 낱말」 정규화는 현재 선행 칸 28행에서만 검증했다 — 첫 낱말이 같지만 다른 대상인 선행이 나중에 생기면 잘못 묶인다(적힌 사람 역할 집합까지 같아야 묶이므로 범위는 좁다).
2. 블록 목록에 없는 요소(`dl` 직속 텍스트 등)는 가장 가까운 상위 블록으로 묶인다 — 현 D1 에서 그런 산문은 확인하지 않았다.
3. t47 워크트리 잠금(plan-huniweb pid 58222)은 기록만 — 계속 `git -C`·경로 지정.

### M4-보정4 — 대리 재판정 권고 표현 (2026-09-17 · 리드 dispatch · AC-LP-016 대리 재판정 CTO 5/5 · 실무운영 5/5 PASS 후 지니 결정)

#### Claim

권고 표현 3건을 **`build_d1.py` 에서만** 고치고 재생성했다(HTML 직접 수정 0 · CSV 무변경 → D2 재생성 불요 · 검사기 무변경). D3 = **14/14 PASS · exit 0**. D1 md5 `566407914fc06ec501efd67c91f76434` → **`7dc9f2c61cadf87cd380e360a3d49886`**.

#### before / after

| # | 위치 | before | after |
|---|---|---|---|
| 1 | §8 `p.derived` | 실제 날짜는 여기에 소요 미산정 구간과 외부 회신 시점이 더해져 늦어진다. | 실제 날짜는 여기에 소요 미산정 구간과 외부 회신 시점이 더해지면 이보다 늦어질 수 있다. |
| 2 | §7 진입점 머리 `#cto-entry` | (없음) | CTO — 의사소통 표의 「대표·CTO」 행(제안(확정 전))이 받는 것은 RAG·임계경로 날짜·결정 요청(주 1회 · 주간 회의 · 의사소통 주기)이다. 시스템 배치·파이프라인은 시스템 배치도·주문이 흐르는 길에서, 개발 구간은 아래 인쇄개발(서희항)·쇼핑개발(김동학) 블록에서 본다. |
| 3 | §7 `#role-ops` 머리 `p.zero-top` | (없음) | 김용기: 최상위 할 일 담당 행 없음(세부 1행은 부록 T5) |

- 1 — 판정 술어 확인: §8 은 `#critical-path` 안이라 D3 AC-LP-002(c) 의 **검사 대상 밖**이다. 그래서 같은 정규식(`가능|불가|어렵|무리|힘들|충분|맞출 수`)을 새 문장에 직접 돌려 **0건**을 확인했다(D3 가 확인한 것이 아니다).
- 2 — 원천 대조: §12 `#comms` 「대표·CTO」 행은 **메시지 수준 「RAG·임계경로 날짜·결정 요청」 · 주 1회 · 주간 회의 · 담당 지니 · 제안(확정 전)** 이다. 「시스템 배치·파이프라인」이라는 말은 그 행에 **없다** — 그래서 행 값은 그대로 인용하고, 시스템 배치·파이프라인은 실재 섹션(§5 시스템 배치도 · §4 주문이 흐르는 길)으로, 개발 구간은 기존 두 역할 블록으로 링크했다. 인용값이 표와 다르면 생성이 멈추도록 `assert` 를 넣었다. 새 역할·담당 0.
- 3 — 실측: `plan-rows.csv` 최상위 담당에 김용기 **0행** · 세부 담당에 **1행**(`STD-FIN-021` 「적립금·증빙 데이터의 MS·이카운트 연동 방식」 · T5) · D1 부록 `data-row-id="STD-FIN-021"` 1건 실재. 수는 CSV 에서 계산해 적는다.

#### Evidence — D3 (최종 D1 `7dc9f2c6…` · 워크트리 루트 절대경로로 실행)

```
[real] exit=0
    ((c) 오픈일 판정 문장 0(critical-path·decisions 밖)) 읽은 대상 1193
== D3 RESULT: 14/14 PASS ==
```

(c) 읽은 대상 1190 → 1193 = 새 블록 문장(CTO 안내 2 · 김용기 1).

#### 불변 · 변경량 · 가드

- D1 전후: `tr[data-row-id]` 수 · `STD-*` 집합 · `#derived-open-date` · 실명 7명 집합 **동일**. HTML diff = 위 3곳뿐.
- `build_d1.py` 줄 기준 변경 21/795 = **2.6%** · `git diff --stat` +23/−5(생성기) · D1 4줄.
- `S5-plan/.moai/` 재발생 없음(명령을 워크트리 루트 절대경로로 실행) · 라이브 쓰기 0 · 푸시 0.

#### Gaps

1. §8 문장은 D3 검사 범위 밖이라 **정규식 직접 확인만** 했다.
2. PM 역할의 지니도 최상위 담당 행이 0이다 — 지시 범위(김용기)만 적었고 지니 줄은 넣지 않았다.
3. 렌더는 다시 열어 보지 않았다(`.rule`·`.proposal` 클래스 재사용 · `.zero-top` 은 스타일 없음).

## §E.3 Run-phase Audit-Ready Signal

- **상태**: run 단계 산출 완료 · **AC-LP-001~015 관측 PASS** · **AC-LP-016 대리 재판정 CTO 5/5 · 실무운영 5/5 PASS**(D1 `566407914…` · 대리 리뷰는 사람 판정 대체 아님) → 권고 표현 3건 반영(M4-보정4) 후 리드 검산 → sync.
- **최종 D1**: md5 `7dc9f2c61cadf87cd380e360a3d49886` · **D3 14/14 PASS · exit 0**(M4-보정4) · 음성 대조 nega·negb FAIL exit 1(M4-보정3 · 검사기 이후 무변경).
- **최종 커밋**: 이 블록을 담은 M4-보정4 커밋(카드 t47 · 직전 `81987b0a`) · 브랜치 `WT-open-plan-rewrite` · **미푸시**.
- **산출**: D1 `docs/huni/후니프린팅_오픈계획서_260917.html`(+요약 .md) · D2 `docs/huni/후니프린팅_오픈계획서_260917.xlsx` + `S5-plan/build_xlsx_v4_plan.py` · D3 `S5-plan/verify_plan.mjs`(+package.json·lock · node_modules 제외) · 생성기 `build_plan_rows.py`·`build_d1.py`·`lane_audit.py` · 데이터 `plan-rows.csv`·`decisions-by-step.csv`·`wait-items.csv`·`lanes.csv`·`workdays.csv`·`holidays-kasi-2026/2027.xml`·`manual-element-matrix.csv`(162)·`m15-2b-results.jsonl`.
- **라이브 쓰기 0** — 이번 run 의 라이브 접촉은 webadmin 읽기 3화면(ego-browser space 20 · p2 · 클릭 0 · 저장 0 · 다이얼로그 0)과 로컬 파일 렌더 확인(p12 · 닫음)뿐.
- **재현 순서**: `python3 -B build_plan_rows.py && python3 -B build_d1.py && python3 -B build_xlsx_v4_plan.py && node verify_plan.mjs <D1> <원장 CSV> <매트릭스 CSV> "<README 분모 명령>"` (S5-plan 디렉터리 · `npm ci` 선행).
- **sync 레인 인계**: 푸시 전 `7abdcdba` 이력 평문 연락처 squash(리드 판정 260917) · 리뷰 렌즈에 「api-path — 우리 구현이 셀러어드민 수동 등록을 강제하는 항목」 포함.


## §E.4 Sync-phase Audit-Ready Signal

- **상태**: `spec.md` status `draft` → **`implemented`**(sync 레인 · 260917). **`completed` 아님** — 아래 Gap 1.
- **이력 정리(보안 렌즈)**: 평문 연락처가 들어간 `7abdcdba` 와 바로 뒤 마스킹 커밋 `d2e6ee2a` 를 한 커밋으로 합치고 뒤 17커밋을 재적용했다. 방법 = `git commit-tree` 로 원래 트리·작성자·일시·메시지를 그대로 옮겨 붙이고 `git update-ref` 로 브랜치를 옮김(대화형 rebase·작업 트리 조작 0). 합친 커밋 메시지 본문의 연락처도 마스킹 형식으로 바꿨다.
  - 재작성 전 HEAD `99fb3566` → 재작성 후 HEAD `3fa5fd2d` · `git diff 99fb3566 3fa5fd2d` **0바이트 · exit 0** · `8dc0a618..HEAD` 19커밋(병합 0).
  - 연락처형 정규식(휴대폰 `01[016789]…` · 이메일) 검사, 값은 출력하지 않고 줄 수만: 새 이력 전 커밋 **diff 줄 0 · 커밋 메시지 줄 0**.
  - **원래 SHA 는 백업 브랜치 없이 reflog 로만 보존**된다(`WT-open-plan-rewrite@{1}` = `99fb3566`). reflog 가 만료·정리되면 옛 커밋은 복구할 수 없다.
- **api-path 렌즈(요약 층 정합)**: `plan-rows.csv` 735행 중 `admin-manual` 45 = 「API 가 있는데 수동」 14 + 「화면으로만」 31. D1 `#api-admin-manual` 의 두 목록 행 ID 집합이 CSV 와 **일치**(중복 0) · 요약 줄 14 = 트랙 연결 10 + 참고 4 · 31 = 25 + 6 · 표 `data-api-path="admin-manual"` 45행 전부 요약에 있음(누락 0).
- **산출물**: D1 md5 `7dc9f2c61cadf87cd380e360a3d49886` — §E.3 이후 **변경 없음**(이력 정리는 트리를 바꾸지 않음).
- **라이브 쓰기 0 · 푸시 0 · main 병합 0**.

#### Gaps

1. **AC-LP-016 사람 판정 대기** — AI 대리 리뷰 2건은 PASS 이지만 사람 5분 이해 테스트는 아직 안 했다. 그래서 `completed` 로 올리지 않았다.
2. 운영 제안값(Go/No-Go 10/2 · RACI · 채널 · 컷오버)은 **지니 결정 대기** — 문서의 「제안(확정 전)」 표지 유지.
3. 연락처 검사는 이 브랜치가 추가한 커밋(`8dc0a618..HEAD`)만 대상이다. `8dc0a618` 이전부터 있던 파일(`07_rebaseline/L0/N1/payment-approval-flow.md`)에 결제 벤더 도메인 이메일 2줄이 있으나 이번 카드 범위 밖이라 손대지 않았다.
4. 푸시 전 원격 이력 대조(`git fetch` 후 divergence)는 하지 않았다 — 푸시·병합 단계에서 할 일.

