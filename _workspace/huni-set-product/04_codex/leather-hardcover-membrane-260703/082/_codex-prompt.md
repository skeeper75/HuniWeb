# 독립 교차검증 요청 — 082 하드커버 링책자 셋트 "면지 통합(4택1·인쇄면지 포함)" 재설계

당신은 후니프린팅 셋트상품(부품조립형) 재설계의 **독립 2차 검증자**다. 아래 설계를 **처음 보는 눈으로 냉철히 반박**하라. 다른 검증자(Claude/게이트)의 판정은 당신에게 제공되지 않는다(독립 판정). 환각 금지 — 코드/파일에서 실증되는 것만 근거로 삼아라.

## 읽어야 할 파일 (읽기전용, 이미 리포 안에 있음)
- 설계 명세: `_workspace/huni-set-product/03_design/leather-hardcover-membrane-redesign-260703/082/redesign-spec-082.md`
- 적재본: `.../082/apply-082.sql` (8스텝) · `.../082/undo-082.sql`
- 골든 재현: `.../082/golden-reproduce-082.md` · `.../082/t_prd_product_sets-082.csv` · `.../082/blocked-board-082.csv`
- 라이브 가격엔진 코드(권위): `raw/webadmin/webadmin/catalog/pricing.py` (evaluate_price / evaluate_set_price)
- 셋트 시뮬레이터 메타: `raw/webadmin/webadmin/catalog/price_views.py` (함수 `_set_members_meta`)
- 시뮬레이터 렌더: `raw/webadmin/webadmin/catalog/templates/catalog/price_simulator.html` (renderInsts / memberMulti)
- (참고·072 선례) `_workspace/huni-set-product/04_codex/leather-hardcover-membrane-260703/reconcile.md` — 072는 이미 COMMIT 완료. 082는 그 동형이나 특이점 2건이 다르다.

## 재설계 요지 (당신이 반박할 대상)
082 셋트 = 표지(083) + 내지(286) + 면지. 현재 면지는 빈 멤버 **4개**(084화/085블/086그/**087인쇄**)로 이중표현. 재설계:
- 면지 1멤버(084)로 통합. 085/086/087 셋트링크 논리삭제(del_yn=Y) + 상품마스터 use_yn=N.
- 부모 082의 면지자재(MAT_382/383/384/**385 인쇄면지** USAGE.03)와 옵션그룹(OPT_066·OPV_440~**443**)을 면지 멤버 084로 이관.
- 색/인쇄 택1(화/블/그/**인쇄**)은 면지 멤버 084의 "자재 드롭다운"으로 발현한다고 주장.
- 신규 mint 0. 하네스 골든 **818,438**(set_eval 800,000 + 내지286 18,438) 재설계 전후 동일하다고 주장.

## ★082 특이점 (072와 다른 점 — 여기 집중)
1. **인쇄면지 MAT_385/멤버 087** — 072는 색 3택, 082는 4번째로 "인쇄"(OPV_443→MAT_385) 존재. 설계는 084 무공식이라 인쇄 선택 시에도 기여 0이라 주장(D-3 후속·무손상 보존만).
2. **★★USAGE.07 링자재 MAT_013/014/015(화이트링·블랙링·링메탈링)** — 면지가 아닌 표지/바인딩(링) 자재. apply-082.sql `[7]`은 `usage_cd='USAGE.03'` 가드로 링자재를 절대 미참조한다고 주장(불가침).

## 당신이 독립 판정할 5개 핵심 질문
아래 각 항목에 **AGREE / DISAGREE / UNCERTAIN**과 근거(파일:라인 또는 코드 로직)를 명시하라.

1. **골든 818,438 무손상 주장이 안일한가.** 면지 자재4(인쇄면지 포함)+옵션을 부모→멤버 084로 이관하고 부모 082에서 은퇴한 뒤 evaluate_set_price가 정말 818,438 그대로인가. pricing.py상 evaluate_price는 공식 전에 **직접단가 TPrdProductPrices를 먼저** 보는데, 084/085/086/087에 직접단가 행이 없다는 게 코드상 "기여 0"을 보장하는가? 이중합산·누락 여지는? (골든 딕셔너리의 44123 스테일 엔트리 주석이 은폐하는 결함은 없는가?)

2. **★★USAGE.07 링자재(013/014/015)가 정말 미터치인가.** apply-082.sql `[7]`의 `usage_cd='USAGE.03'` 가드가 링자재(USAGE.07)를 안전히 배제하는가? mat_cd IN (382,383,384,385) 목록에 링자재 코드가 섞일 여지는 0인가? 링 가격 기여(set_eval=800,000·COMP_BIND_HC_TWINRING)가 이 재설계로 손상될 코드 경로가 있는가?

3. **인쇄면지 MAT_385/OPV_443 — 통합 후 기여 0 맞나.** 084에 MAT_385 자재를 붙이고 "인쇄"(OPV_443) 옵션으로 택1 가능하게 만든 뒤, 인쇄(OPV) 선택 시 evaluate_price 기여가 정말 0인가? 아니면 인쇄면지가 실제로는 인쇄비를 발현해야 하는데 무공식이라 **누락(저청구)**되는 건가? 이 재설계가 그 누락을 은폐/고착하는가?

4. **085/086/087 은퇴 무결성·트리거 순서.** 087(인쇄면지 멤버)을 포함한 3멤버 은퇴가 복합PK·FK·전역 역참조에 안전한가(설계가 역참조 0을 근거로 확인했는가)? apply 순서(자재[2]→옵션[3~5]→부모은퇴[6~7]→멤버은퇴[8]) 및 undo 복원 순서가 fn_chk_opt_item_ref 트리거를 위반하지 않는가? OPV_443→MAT_385 참조는 트리거 통과하는가?

5. **색/인쇄 택1 자재 드롭다운 발현·오차단(선택지 손실).** `_set_members_meta`가 멤버별로 materials를 동봉하고 opt_groups는 미동봉한다면, 이관한 OPT_066은 휴면이 되는데 — 손님이 화/블/그/**인쇄**를 084 자재 드롭다운으로 정말 4개 다 고를 수 있는가, 아니면 선택지가 사라지는가(오차단)? 인쇄면지가 자재 드롭다운에 뜨는 게 손님에게 "인쇄비 0"을 오도하지 않는가?

## 출력 형식
- 각 질문 1~5: 판정(AGREE/DISAGREE/UNCERTAIN) + 근거 1~3줄(파일:라인).
- 마지막에 종합: 이 재설계를 S1~S8 게이트로 넘겨도 되는가? 반드시 라이브 실측/보강해야 할 잔여 리스크 Top 3.
- 당신 판정은 "가설"임을 인지하라 — 라이브 DB 미접근·코드/파일 근거만.
