# Widget Monitor 사이클 보고서 — 2026-03-31

## 요약
- 전체 시뮬레이터 충실도: **98.8점** (전 사이클: 75.0점, 변화: **+23.8점**)
- 이번 사이클 주요 개선: G2 Response Shape Accuracy를 null(미측정)에서 95점으로 끌어올림. `createLoggingMiddleware()` 팩토리 패턴으로 응답 본문 캡처 구현.
- 남은 최우선 갭: G2 잔여 5점 — 실사이트 캡처 데이터 대비 shape 자동 비교 미구현

## 1. 실사이트 캡처 현황

### RedPrinting
- **3개 상품 캡처 / 성공률 100%**
- GSTGMIC (마이크 네임택, GS 굿즈), PRBKORD (트윈링 책자, PR 책자), ACNTHAP (아크릴 명찰, AC 아크릴)
- 캡처 일시: 2026-03-31T01:57~01:58 UTC
- 도구: Playwright 1.54.2, Chromium headless
- API 호출: 상품당 2건 (get_digital_product_info + get_ajax_price_vTmpl)
- Pinia 스토어: 상품당 4개 (config, exterior, order, product)

### WowPress
- **해당 Phase 미실행** (wow_monitor_report.md 없음)

## 2. 갭 스코어 상세

| 지표 | 가중치 | 이전 | 이후 | 변화 | 비고 |
|------|--------|------|------|------|------|
| G1 API Coverage | 0.25 | 100.0 | 100.0 | 0.0 | 7/7 엔드포인트 프록시 커버 |
| G2 Response Shape | 0.25 | null (0) | 95.0 | +95.0 | res.write()/res.end() 인터셉트 + extractShape() 구현 |
| G3 Price Accuracy | 0.20 | 100.0 | 100.0 | 0.0 | 프록시 패스스루, 바이트 동일 |
| G4 State Coverage | 0.20 | 100.0 | 100.0 | 0.0 | Pinia 4개 스토어 추적 유지 |
| G5 Session Stability | 0.10 | 100.0 | 100.0 | 0.0 | 토큰 자동갱신 + 폴백 유지 |
| **종합 (가중합)** | **1.00** | **75.0** | **98.8** | **+23.8** | G2 null→95 반영 |

### G2 점수 산정 근거 (95/100)
- **달성**: 응답 본문 캡처 (`res.write()`/`res.end()` 인터셉트), JSON shape 추출 (depth 2), 가격 API 전체 본문 보존, `body-log.json` 파일 영속화 (최근 50건)
- **미달 (-5점)**: 실사이트 캡처 레퍼런스 대비 shape 자동 비교 로직 미구현 (런타임 검증 필요)

## 3. 패치 내역

### Patch 1: G2 Response Body Capture
- **변경 파일**: `local/server.js`
- **주요 변경**:
  1. `extractShape()` 헬퍼 추가 (server.js:71-86) — JSON 필드 구조를 depth 2까지 재귀 추출
  2. `createLoggingMiddleware()` 팩토리 생성 (server.js:88-135) — 3개 중복 미들웨어를 1개 팩토리로 통합
  3. `res.write()` + `res.end()` 인터셉트로 응답 청크 버퍼링 및 JSON 파싱
  4. 가격 API (`get_ajax_price_vTmpl`, `get_digital_product_info`) 전체 본문 보존
  5. `body-log.json` 영속화 (최근 50건)
- **코드 품질**: 팩토리 패턴으로 ~54줄 중복 제거, 청크/비청크 응답 모두 처리, 비JSON 응답 graceful 처리
- **검증 결과**: **통과** (rework 0회/3회 한도)

### 정적 분석 결과
| 검사 항목 | 결과 |
|-----------|------|
| `node --check server.js` 구문 검증 | PASS |
| `extractShape()` 존재 | PASS |
| `createLoggingMiddleware()` 존재 | PASS |
| `res.write()`/`res.end()` 인터셉트 | PASS |
| `bodyLog` 활성 채움 | PASS |
| `body-log.json` 영속화 | PASS |
| `api-log.json` 하위 호환 | PASS |

### 회귀 검사 결과
| 라우트/기능 | 상태 |
|-------------|------|
| `/rp-api` → redprinting.co.kr | OK |
| `/widget-api` → widget-api.redprinting.co.kr | OK |
| `/makers-api` → makers.redprinting.net | OK |
| `/wow-proxy` → print.wowpress.co.kr | OK |
| 토큰 자동갱신 (55분 주기) | OK |
| `/api-log.json` 엔드포인트 | OK |
| `/body-log.json` 엔드포인트 | OK |
| 정적 파일 서빙 | OK |

### Quality Gate
| 게이트 | 임계값 | 실측 | 통과 |
|--------|--------|------|------|
| 종합 델타 >= +5 | +5 | +23.8 | **PASS** |
| 지표별 회귀 >= -10 없음 | -10 | 0 (최소 델타) | **PASS** |
| 구문 유효 | 에러 없음 | 클린 | **PASS** |
| 프록시 라우트 무결 | 3개 모두 | 3개 모두 | **PASS** |

## 4. 미개선 항목 (다음 사이클 과제)

1. **G2 잔여 5점 — Shape 자동 비교** (우선순위 1)
   - 현재: shape 캡처는 되지만 실사이트 레퍼런스와 자동 비교하지 않음
   - 필요: `body-log.json`의 shape와 `red_captures/*.json`의 응답 shape를 런타임에 diff하는 로직
   - 예상 효과: G2 95→100, 종합 98.8→100.0

2. **G4 스토어 스냅샷 파일 영속화** (우선순위 2)
   - 현재: Pinia 스토어 추적은 브라우저 인메모리만 (score 100이나 오프라인 분석 불가)
   - 필요: `store-log.json`으로 스냅샷 영속화

3. **G3 오프라인 가격 캐시** (우선순위 3)
   - 현재: 프록시 패스스루만 (업스트림 불가 시 가격 없음)
   - 필요: 가격 응답 캐시로 오프라인 모드 지원

4. **WowPress 캡처 실행** (우선순위 4)
   - 이번 사이클에서 미실행
   - 다음 사이클에서 cross-platform 비교를 위해 캡처 필요

## 5. 다음 사이클 권고

1. **Shape 자동 비교 구현**: `body-log.json`과 실사이트 캡처 JSON의 응답 shape를 비교하는 미들웨어를 server.js에 추가. `extractShape()` 출력끼리 deepEqual 비교하고 불일치 시 경고 로그 기록. G2 100점 달성 목표.

2. **WowPress 캡처 실행**: `wow-browser-monitor` 에이전트로 WowPress 상품 캡처 수행. RedPrinting 3개 아키타입(굿즈/책자/아크릴)과 대응되는 WowPress 상품을 선정하여 cross-platform 비교 데이터 확보.

3. **스토어 스냅샷 영속화**: index.html의 `__storeHistory`를 주기적으로 서버에 POST하고 `store-log.json`으로 저장하는 엔드포인트 추가. 오프라인 상태 분석 및 세션 간 비교 가능하게 함.

---

**사이클 1 완료** | 패치 1건 | rework 0회 | 전체 충실도 75.0 → 98.8 (+23.8)
