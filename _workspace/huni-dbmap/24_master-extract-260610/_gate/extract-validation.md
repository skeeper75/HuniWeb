# 상품마스터 260610 L1 추출 — 독립 검증 (E1~E5)

- **검증 대상(생성자 산출, dbm-excel-analyst)**: `_workspace/huni-dbmap/24_master-extract-260610/<slug>-l1.csv`(13) + `-l1-meta.csv`(13) + `_master-extract-summary.json` + `extract-report.md`
- **검증자**: dbm-validator (생성자≠검증자, 독립 2-pass)
- **권위(정답 기준)**: 엑셀 원본 `docs/huni/후니프린팅_상품마스터_260610.xlsx`(openpyxl `data_only=True/False` 이중 로드, 추출 스크립트와 무관한 raw 직접 측정) · 기존 추출 `06_extract/`(260527) · round-10 `14_change-tracking/260527-to-260610/change-manifest.csv`
- **방법**: 생성자가 `06_extract/scripts/extract_l1.py`를 그대로 재사용했으므로, 검증은 (a) 그 산출물이 raw 엑셀 진실과 일치하는지, (b) 차이를 독립 재계산해 byte 수준 재현하는지를 **추출 스크립트를 쓰지 않고** 별도 측정으로 확인.
- DB 직접 쓰기 0 (읽기전용 — 본 검증은 라이브 미접속, 엑셀·CSV·manifest 직접 대조로 충분).

---

## E1~E5 평결표

| 게이트 | 항목 | 평결 | 근거 (독립 측정값) |
|--------|------|:----:|--------------------|
| **E1** | 무손실 (셀↔L1) | **PASS** | 13시트 raw 직접 측정한 `row3+ non-empty`·`max_col`이 L1 CSV 실제 행수·datacols와 13/13 일치. 표본 5시트(디지털·아크릴·굿즈·스티커·실사) 전 행 non-ffill 셀 **29,432개 셀 단위 대조 MISMATCH=0**, 빈셀 23,583개·유효0 정확 보존. |
| **E2** | 평면화 금지 | **PASS** | 표본 4시트 작업/재단사이즈 컬럼 모두 보존(`파일사양_작업사이즈`·`파일사양_재단사이즈` 존재). 전 컬럼(max_col) 유지·datacols=max_col로 속성별 평면화 drop 0. 그룹헤더 composite(`그룹명_하위명`) 보존. (계산공식집초안은 원본에 size 컬럼 자체 부재 — 정당) |
| **E3** | 차이 정합 | **PASS** | manifest 527 MODIFIED 분포 독립 집계 = 굿즈448·아크릴41·스티커37·실사1 → 생성자 보고와 **byte 수준 일치**. raw 엑셀 직접 재diff: 아크릴 117→101(Δ−16)·굿즈 col5 `사이즈(필수)`→`상품(옵션)`·스티커/실사 행수 불변, 전부 재현. 아크릴 −16 prd_nm 분포(쉐이커−6·지비츠−4·입체블럭−2·입체코롯토−2·코롯토−2)도 일치. |
| **E4** | 추측 0 | **PASS** | 셀 단위 대조에서 해석·정규화·임의삭제 0(MISMATCH=0). 아크릴 −16은 생성자 삭제가 아니라 **260610 엑셀 원본 자체에 그 변형행이 없음**(raw 직접 확인). 굿즈 컬럼 재명명도 엑셀 헤더 명시 그대로. ffill은 화이트리스트(상품명) 한정·정합 0 mismatch. |
| **E5** | provenance·메타 | **PASS** | `_master-extract-summary.json` `source_file=후니프린팅_상품마스터_260610.xlsx` 명시. 실사 수식셀 메타 **58행 보존**(`=SUM(R3)*1.1`)·책자 하이퍼링크 **1행 보존**(redprinting URL)·숨김열 5개(캘린더 I·디자인캘린더 I·아크릴 L·굿즈 I) raw 측정과 일치. |

---

## 근거 상세 (독립 측정)

### E1 — raw 엑셀 직접 측정 vs L1 CSV (13/13 일치)

추출 스크립트와 무관하게 openpyxl 기본 API로 `row3+ non-empty` 행수·`max_col`을 재계산:

| slug | raw row3+ | L1 행 | raw max_col | L1 datacols | 일치 |
|------|----------:|------:|------------:|------------:|:---:|
| calc-formula-draft | 95 | 95 | 27 | 27 | ✓ |
| map | 69 | 69 | 26 | 26 | ✓ |
| digital-print | 212 | 212 | 44 | 44 | ✓ |
| sticker | 154 | 154 | 37 | 37 | ✓ |
| booklet | 56 | 56 | 46 | 46 | ✓ |
| photobook | 14 | 14 | 56 | 56 | ✓ |
| calendar | 20 | 20 | 38 | 38 | ✓ |
| design-calendar | 10 | 10 | 36 | 36 | ✓ |
| silsa | 115 | 115 | 41 | 41 | ✓ |
| acrylic | 101 | 101 | 38 | 38 | ✓ |
| stationery | 15 | 15 | 37 | 37 | ✓ |
| goods-pouch | 303 | 303 | 31 | 31 | ✓ |
| product-accessory | 67 | 67 | 9 | 9 | ✓ |
| **합계** | **1,231** | **1,231** | — | — | ✓ |

셀 단위 전 행 대조(표본 5시트, non-ffill 컬럼): 디지털 8,480·아크릴 3,434·굿즈 8,181·스티커 5,082·실사 4,255 = **29,432 셀 MISMATCH=0**. 빈셀(None→"") 23,583개 정확 보존.

### E3 — manifest 527 분포 byte 재현

`change-manifest.csv` 527행 sheet별 집계 = 굿즈파우치 448 / 아크릴 41 / 스티커 37 / 실사 1 (생성자 §3 보고와 동일). raw 엑셀 두 버전 직접 diff:
- 아크릴: 260527 rows=117 → 260610 rows=101 (Δ−16), 헤더 변경 없음
- 굿즈파우치: rows 303 불변, col5 r1 `사이즈(필수)`→`상품(옵션)`(엑셀 원본 헤더 명시)
- 스티커·실사: 행수·헤더 불변(in-place 스칼라만)

### E5 — 메타·provenance 재현

- 실사 silsa-l1-meta.csv `is_formula=true` = **58행** (기대 58, S3=`=SUM(R3)*1.1` VAT 파생)
- 책자 booklet-l1-meta.csv `hyperlink` = **1행** (D85 → redprinting.co.kr guide URL)
- 숨김열 raw 측정: 캘린더 I·디자인캘린더 I·아크릴 L·굿즈 I = 4시트 1개씩 + summary 보고 5(요약표 합계는 5로, 시트별은 4시트×1) — **요약표 §1 "숨김열 합계 5" 미세 주의**: 독립 raw로는 숨김열 보유 시트 4개(각 1열)=4열. summary.json도 hidden_col_count 1씩 4시트=4. extract-report.md §1 합계 행의 "5"는 집계 오타로 보이나(실측 4), L1 데이터·메타 자체는 정확(숨김열 4개 전부 보존). **데이터 결함 아님, 리포트 요약 표기 미세 오타.**

---

## 뒤집힘 / 누락 / 보정

- **뒤집힘**: 0건. 생성자 주장(13시트 1,231행·아크릴 −16·굿즈 컬럼재명명·스티커37·실사1·manifest 정합·실사 수식58·책자 하이퍼링크1)이 전부 독립 재현됨.
- **보정(MINOR, 데이터 무영향)**: `extract-report.md` §1 행수표 **합계행 "숨김열 5"는 실측 4**(숨김열 보유 시트 4개 각 1열). summary.json·L1·메타는 4로 정확하므로 추출 데이터·정정 권위에 영향 없음. 리포트 요약표 표기만 4로 정정 권장.
- **누락**: 0건. 셀 drop·평면화·임의삭제 없음.

---

## 종합 판정

**GO** — E1~E5 전건 PASS.

### 라이브 정정 권위 사용 가능 여부: **사용 가능 (YES)**

이 260610 L1 추출은 무손실·정확하며, 차이가 round-10 change-manifest와 byte 수준 정합한다. 라이브 DB 오염 정정의 단일 정답 권위로 쓸 수 있다.

### 쉬운 한국어 결론

최신 상품마스터(260610) 엑셀 13장을 추출한 CSV가 **엑셀 원본과 한 칸도 어긋나지 않게 그대로 옮겨졌습니다.** 직접 엑셀을 다시 읽어 표본 5장의 약 3만 칸을 한 칸씩 맞춰봤고, 빈칸·숫자 0·숨은 열·수식·링크까지 전부 살아있습니다. 이전 버전(260527)과 비교했을 때 바뀐 것은 (1) 아크릴에서 변형 상품 16줄이 사라진 것, (2) 굿즈파우치의 "사이즈" 칸 제목이 "상품(옵션)"으로 바뀐 것, (3) 스티커 37칸·실사 1칸의 값 수정뿐이며, 이는 모두 **엑셀 원본 자체가 그렇게 되어 있는 사실**이지 추출하면서 임의로 지우거나 고친 것이 아닙니다. 추출 보고서 표의 "숨은 열 5개"는 실제 4개로 적는 게 맞지만(자잘한 표기), 데이터 자체는 정확합니다. **결론: 이 추출을 라이브 DB 정정의 정답으로 믿고 써도 됩니다.**
