# L4b 진행 — 카드 t31-b (고객 계정·부가 · 표준 84행)

> lane `run-huniweb` · 2026-09-02 · 담당 대분류: 회원·인증20 / 마이페이지16 / 프로모션14 / B2B12 / 시스템·플랫폼22 = **84**

## 완료조건 충족 근거

| # | 완료조건 | 검산 | 판정 |
|---|---|---|---|
| ① | 자기 도메인 std_id **전건**이 mapping.csv 에 있다 (누락 0) | L1 canon 에서 5개 대분류 필터 → 84행. mapping.csv 84행. 빌더가 `missing`/`extra` 양방향 assert 로 강제(`_build.py`) | ✅ |
| ② | 상태 `done` 은 전건 L2 file:line 인용 | done 15행 전건 근거 보유. 「근거 없는 done」 = **0건** (기계 검산) | ✅ |
| ③ | `new` 는 전건 사유 1줄 + ia.md 오염 여부 표시 | new 12행 전건 사유 기재. 오염 3건(STD-PRM-013·STD-SYS-003·STD-SYS-015) 명시 → `newly-found.md` | ✅ |
| ④ | 무작위 15건 재대조 불일치 0 | 시드 고정 표본 15건 재대조 → FAIL 0. 추가로 **인용된 file:line 45개 전건** 실재성 검증 → FAIL 0 | ✅ |
| ⑤ | 미귀속 legacy 목록 존재 | `unassigned-candidates.csv` 543행. 귀속 173 + 미귀속 543 = **716** (유실 0) | ✅ |

### ④ 재대조 실행 기록

```
표본15 → 근거검증 OK 8 · FAIL 0 · 파일근거없음 9 (todo/new 정상)
전건 근거검증: OK 45 · FAIL 0
근거 없는 done: 없음
```
재현: `python3 L4/b/_recheck.py` (시드 20260902 고정)

## 판정 분포

| 상태 | 건수 | 비율 |
|---|---:|---:|
| `done` | 15 | 17.9% |
| `partial` | 14 | 16.7% |
| `todo` | 43 | 51.2% |
| `new` | 12 | 14.3% |
| **합** | **84** | |

대분류별 `todo` 밀도: **B2B 11/12 (92%)** · 회원·인증 10/20 · 시스템·플랫폼 8/22 · 마이페이지 7/16 · 프로모션 7/14.
→ B2B 는 사실상 전부 미착수다. 10/6 범위 판정(L5)에서 가장 먼저 걸릴 축.

## 산출

```
L4/b/mapping.csv               84행
L4/b/newly-found.md            new 12건 (신뢰 9 · ia.md 오염 3)
L4/b/status-flip.md            정방향 15행 · 역방향 12행 + 판정규칙 공시
L4/b/unassigned-candidates.csv 543행
L4/b/progress.md               이 파일
L4/b/_build.py / _recheck.py / _q.py  재현 스크립트
L4/b/_mine.csv / _legacy-slim.json    중간 캐시(1.4MB→486KB, raw 필드 제거)
```

## lead 가 알아야 할 사항

1. **★ 호출 지점이 아예 없는 서비스 2건을 상태 판정에 반영했다.**
   - **D-17 본인인증** (`endpoint="(호출 지점 없음)"`) → STD-MEM-003(휴대전화 본인인증) · STD-SYS-012(본인인증 연동) 둘 다 `todo`. SB-043 `absent` 와 일치. `X-AUTH-CONTRACT-01` 계약 미체결이 선행 블로커라 **코드 작업으로 풀리는 항목이 아니다**.
   - **D-18 세금계산서/현금영수증** (`endpoint="(호출 지점 없음)"`) → STD-MYP-014 · STD-MYP-015 · STD-B2B-012 셋 다 `todo`. SB-053 은 `document-section.tsx:1-6` 정적 표 UI 뿐. 셋 다 **돈이 걸린다**.
   - 두 건 모두 legacy 는 `PARTIAL` 로 세어뒀지만 실측 근거는 「호출 자체가 없음」이다. **PARTIAL 을 근거로 잔여 공수를 축소 추정하면 안 된다.**

2. **★ Prisma absent (SB-063) 는 시스템·플랫폼 축의 사문(死文)이다.**
   `prisma/schema.prisma:165-637` 에 23모델이 정의돼 있으나 `src` 전역 import 0건. 즉 **스토어프론트에 자체 영속 계층이 없다** — 모든 상태가 shopby API 와 localStorage 에 얹혀 있다. 표준 84행 중 대응 std 가 없어 mapping.csv 에는 행이 생기지 않았고, 여기에 기록으로 남긴다.
   L5 영향: STD-SYS-017(백업·복구)·STD-SYS-008(상품 동기화) 설계 시 「스킨에 DB 가 있다」는 전제를 쓰면 안 된다. 스키마 파일의 존재를 구현 근거로 오독할 위험이 크다(§4-5 문서≠코드).

3. **stub → `todo` 로 내렸다** (규칙 공시: `status-flip.md` §C). 화면 껍데기만 있고 배선 0건인 것을 `partial` 로 세면 낙관 판정이라 판단했다. 원 L2 상태는 사유 칸에 `stub` 으로 남겨 lead 가 되돌릴 수 있다.

4. **L2 가 침묵한 9개 항목에 한해 코드 보강 확인을 했고, 근거 앞에 `[L4b보강]` 을 붙여 L2 인용과 구분했다.** (약관 전달·휴면·회원등급·마케팅 수신동의·옵션보관함·기획전·SEO·분석도구) L2 를 덮어쓴 것이 아니라 L2 무근거 구간만 메웠다. lead 가 원하면 이 9건은 배제하고 전부 `todo` 로 읽어도 판정 방향은 바뀌지 않는다(전부 `todo`/`partial` 쪽).

5. **미귀속 543건의 성격**: `P-*` 115건(상품·가격 데이터)은 전량 내 도메인 밖이다. 나머지는 인쇄상품 옵션·주문·결제·배송(L4a) 과 운영자·통계(L4c) 축. 내 판단으로 고아 선언한 것은 **0건** — 3자 교집합은 lead 몫.

6. **경계 걸침 주의 2건** (lead 합침 단계에서 재판정 필요)
   - `F-051`(견적서 생성·다운로드) 을 STD-B2B-011(견적서 발행)에 물렸다. L4a 의 「옵션·견적」 축도 같은 legacy 를 물 가능성이 높다 — §4-2 상 1:N 정상이지만 **중복 계상은 아닌지** 확인 필요.
   - `SCOPE-126`/`F-126`(관리자 쿠폰관리) 을 프로모션 3행에 물렸다. L4c 「운영자·회원CS」 도 물 수 있다.

7. **§4-7 준수**: `scope`/`ia` 의 `돈여부`/`주문여부` `null` 은 「미상」으로 다뤘고, 돈 표시는 `runway` 실값에서만 가져왔다. `false` 로 오독한 곳 없음.

8. **§4-8 준수**: runway conflict 7건 중 내 도메인에 물린 것은 없다(전부 상품·가격 축). 병합 과정에서 지운 legacy_id 0건.

## 한계 (정직 표기)

- **§4-4 그대로 승계**: 여기의 `done` 은 정적 코드 판독이다. 서버를 띄워 관찰하지 않았다. 특히 **STD-SYS-009(가격 브리지)** 는 돈이 걸리는데 배선 존재만 확인했다 — 금액 무손실 여부는 L5 P0 시나리오가 관측해야 한다.
- STD-SYS-019(반응형)는 L2 가 판정 축으로 세우지 않아 실측 상태가 **미상**이다. `new` 로만 세웠고 상태는 주장하지 않았다.
- webadmin `views.py`(4773줄)·`widget_api.py`(4666줄) 미독(§4-6) 승계 — WA-* 근거는 URL 라우팅표 수준이다. STD-SYS-001(관리자 계정)·STD-B2B-001(거래처) 판정은 이 해상도에 묶인다.

---

**상태: 완료** — 완료조건 5개 전부 충족. 커밋·푸시 없음. `L4/b/` 밖 파일 수정 0건.
