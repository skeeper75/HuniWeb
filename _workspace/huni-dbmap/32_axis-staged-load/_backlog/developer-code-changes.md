# 개발자 협업 백로그 — webadmin(HuniProductPrice2) (round-22 v2)

> **작성** 2026-06-16. webadmin은 개발자가 GitHub로 배포하는 라이브 코드 — **우리가 수정 불가(read-only oracle)**. 아래는 개발자에게 요청할 항목이다. 권위=상품마스터 260610·인쇄상품가격표. v03 엑셀=오염원.

## A. 경로 Y — 교정 입력 엑셀 재적재 (근본 교정·회귀 방지)

6축 매핑 오류 진원은 전부 **입력 v03 엑셀**이고 `load_master.py`는 무변환 전파기다. 라이브 직접 교정(경로 X)은 `load_master --all` 재적재 시 `TRUNCATE … CASCADE`로 소멸하므로, **근본 교정 = v03 입력을 권위로 바로잡아 재적재**다.

### A-1. ⑥ 카테고리 (경로 X 이미 라이브 COMMIT 완료 — v03 미반영 시 회귀)
- **현황:** 2026-06-16 라이브에 고아 부카테고리 페어 111건 삭제 + 빈 고아 노드 12개 `use_yn='N'` COMMIT 완료(`_exec_category/apply-log.md`). 단 **v03 재적재 시 회귀**.
- **개발자 액션 (v03 교정):**
  - `11_상품별카테고리` 시트: 고아 카테고리(상품악세사리·명함·상품권·배경지·실사·단품형·플래너·소품·데스크/사무용품·디지털악세서리·말랑·레더파우치·에코백부자재)로 가는 **부카테고리 페어 행(주카테고리여부='N') 111건 제거**. 정상 잎노드 페어는 보존.
  - `01_카테고리` 시트: 위 고아 14노드의 **`사용여부`='N'** (행 삭제 금지 — surrogate 행순 보존). `상위카테고리코드` 채움은 불요(정답 잎노드 이미 존재).
- **상세 매핑:** `_corrected_xlsx/category-correction-spec.md` §2·§5.

### A-2. ④ 자재 — mat_typ 정정 0건 + 비소재 CPQ 축이동(B-3) 설계 완료 (라이브 적용 0)
- **mat_typ 정정 0건**(레더 이미 .06).
- **B-3 비소재 오염 축이동 설계**(`_corrected_xlsx/material-nonmaterial-cpq-spec.md`·검증 `_gate/material-cpq-gate.md` GO): .08 실사14·.09 파우치74·.10 악세사리43=**131행**·연결 **82 distinct 상품/172 link**(검증 정정·명세 84/144는 per-typ 혼용 오류 F-1)·component_prices **0참조**(가격 안전).
  - **라벨별 목표 축:** 색(본체색 2~3종=자재유지·4종+→CPQ option)·형상→siz칼틀/공정·구수→bundle·용량/사이즈→siz·인쇄면→print_side·소재유지 23.
  - **[HARD] FK 위상(80/82 상품 BOM이 .08/.09/.10에 의존·load-bearing):** siz/bundle/print_side 적재 → CPQ option(round-6·webadmin 적재경로 부재) → product_materials 82상품 재배선 → 오염행 use_yn='N'(마지막). **삭제 선행 시 80상품 본체 자재 전손.**
  - **라이브 적용 0건** — 전부 경로 Y(siz/bundle/print_side·v03 05_자재정보/14_상품별자재) + round-6 CPQ 트랙(색→option ~21행) + BLOCKED.
  - **정정 노트(검증 발견):** F-1 재배선 분모=82/172(명세 84/144 정정·실 적재 시 단일 권위 SELECT 재집계) · F-2 양면유광(316/317/318)=색 아닌 마감 공정 · F-3 .09 색 라벨합 미세 오류.
  - **도메인 컨펌 4건:** AX-1 만년스탬프 잉크색7 귀속 · AX-2 size→siz 사슬 보존 · AX-4 봉투/(3개1팩)/리필잉크 통일 · B-7 형상 칼틀 vs 공정param.

### A-3. ② 사이즈 (라이브 가역 안전분 0건 — 전부 경로 Y/BLOCKED)
- 검증 결과 ②사이즈축은 단독 가역 교정 0(component_prices가 116 siz를 2,601행 CASCADE 참조·가격모델 종속).
- **경로 Y(v03 재적재·가격모델 동반):** `04_사이즈정보`·`13_상품별사이즈` siz_nm 표기 정규화(묶음 N장 34행·색 2행·구수 1행 분리) — 단독 불가, 가격모델(product_prices) 해소 동반. 상세=`_corrected_xlsx/size-correction-spec.md`.
- **CORRECT(손대지 말 것):** EA 칼틀(cp 260)·전지 SIZ_000499(cp 880·plate 32)·형상 도무송 칼틀 — 가격사슬 핵심·정합.

### A-3b. ④ 굿즈/파우치 본체 자재 BOM (GPM-1/2 41상품 라이브 COMMIT 완료)
- **발견:** 굿즈/파우치 103상품 본체 소재 BOM 확인 **0개**(상품마스터에 "본체 소재" 컬럼 부재→소재는 상품명에만→load_master 무변환이라 미적재·진짜 소재행 .05/.06은 실사/책자에만 연결된 고아).
- **경로 X 완료:** 상품명 직결 명확분 41상품(레더23 MAT_000008·캔버스9 MAT_000185·린넨5 MAT_000184·메쉬4 MAT_000183) 본체 소재 link 라이브 COMMIT(`_exec_goods_material/apply-log.md`·신규 mint 0·기존 자재 재사용). v03 미반영 시 회귀.
- **경로 Y(근본):** v03 `14_상품별자재`에 본체 소재 컬럼이 없음 → 개발자가 41 본체 소재 행 append(또는 상품마스터 시트에 소재 컬럼 신설 = 실무진/개발자).
- **BLOCKED:** 타이벡 11(하드/소프트 GPM-D)·모호 3(레더라벨/스트랩키링/캔버스스트랩)·GPM-3 신규 mint ~25(우드/규조토/코르크/벨벳/세라믹/광목/연질PVC 자재 코드 신설·ddl-proposer)·GPM-5 폰케이스 5 미등록.
- **컨펌:** GPM-A 본체 소재 BOM 등록 정책(위젯/MES BOM 요구)·GPM-B 신규 자재 코드 범위(.12 굿즈 vs 신규 mat_typ)·GPM-C MAT_000061~067 파우치 색 합성행 고아 처리.
- **★후속 [HARD]:** GPM-4(오염 .09 link 제거)는 본체 link 선적재(완료) 이후에만.

### A-4. ⑤ 공정 (에폭시 169 라이브 COMMIT 완료 — 나머지 경로 Y/BLOCKED)
- **경로 X 완료:** PRD_000169 아크릴입체블럭 ↔ 에폭시(PROC_000083) 연결 1건 라이브 COMMIT(`_exec_process/apply-log.md`). v03 미반영 시 회귀.
- **경로 Y(v03 `15_상품별공정` 행 추가·근본):** ① 169 에폭시 행 append(회귀 방지) ② 캔버스 6상품(239/240/261/262/268/270) 부착(081)→봉제(080) 정정.
- **BLOCKED(도메인 컨펌):** 봉제 49상품 BOM(레더=봉제 vs 접착·봉제 유형 param)·PUR책자 mand·코팅스티커 Q9·PRD_000168 입체코롯토 에폭시(B-10 정체).
- **신규 mint(ddl-proposer·search-before-mint 마스터 부재 입증·PROC_000085~):** 미싱제본·보드마운팅·삼각대거치 — 전부 BLOCKED(도메인 컨펌 후).

## B. ⓑ 코드 수정 백로그 (C-1~C-6 — 교정 엑셀로 못 닫는 코드 강제 영역)

`03_webadmin-load-path-analysis.md §5` 권위. 우리가 수정 불가 — 개발자 요청.

| # | 항목 | load_master.py | 요청 |
|:--:|------|:--:|------|
| C-1 | MES_ITEM_CD NULL 강제 | :261 | 중복 해소 후 NULL 강제 해제 |
| C-2 | qty_unit_typ_cd NULL | :269 | 시트10 컬럼 + 코드 매핑 |
| C-3 | constraint_json 폐기 | :270·sql/23 | CPQ 제약 적재 경로 |
| C-4 | 입력 엑셀 경로 하드코딩 | :39·:517 | `main`에 엑셀 경로 인자 추가(권장) — 교정본 주입 용이 |
| C-5 | P-TRUNCATE 재실행 가드 | :168/185/198/214/231/253 | (경로 X 채택 시) 재실행 정책 또는 교정 보존 |
| C-6 | ENUM_ALIAS/MAT_TYP_OVERRIDE 확장 | :109-121 | 교정 라벨 추가 등록(교정 엑셀 대안) |

## C. 검증 (개발자 재적재 후)

- `_corrected_xlsx/category-correction-spec.md §1-2` SELECT 재실행 → 고아 연결 0·정상 주카테고리 273 무손상 확인.
- 멱등: `load_master --all` 2회 재적재 시 동일 결과.

## D. 미해소 컨펌 (도메인·인간 승인)

- **B-2:** PRD_000218 타이벡북커버·PRD_000229 이미지피켓(비활성)의 정상 잎노드 매칭.
- **MAT_000166** 투명접착 PVC: 정답 mat_typ(.02 필름 vs .08 실사소재) + 정체(v03 잔재 논리삭제 여부).
- **B-3(다음 단계):** ④자재 비소재 행(.09 파우치·.10 악세사리 색/형상/구수) → CPQ option·siz 축이동(가격사슬 0참조라 안전하나 product_materials 64상품 재배선 선행).
