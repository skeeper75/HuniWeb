# 수량할인 차원 전 사슬 정합 — 발견 (260706)

> ★★ 라이브 COMMIT 완료(260706·지니 승인·되돌리지 말 것):
> - **결함1** = `t_prd_product_discount_tables` 7행(아크릴 7상품 × DSC_ACR_QTY) COMMIT. 사후 webadmin
>   검증: qty100 전부 384,000(20% 적용·전 480,000). load=`acryl-discount-wire-260706-load.sql`·undo=`-undo.sql`.
> - **결함3** = `t_prd_product_processes` 5행(책자내지 5종 × PROC_000004 mand Y·seq -1) COMMIT. 사후 검증:
>   인쇄방식 디지털 단독 제약·인쇄비 20,000 정상(전 옵셋선택 0원 저청구 차단). load=`bookinner-print-proc-260706-load.sql`·undo=`-undo.sql`.
> - base 단가 무변경(할인·공정 배선만). DRY-RUN(ROLLBACK) 실증 후 COMMIT·사후 verify 통과.

> 전 사슬 원칙(상품→공식→구성요소→차원→기준정보 마스터↔시트차원)을 **수량할인 차원**에 배치화한 결과.
> grid_diff(면적 L6)가 못 보는 차원. 결정론 센서스 = `discount_conformance.py`(라이브 읽기전용·토큰0).
> 검증 2중: ① 라이브 DB 형제 불일치 ② webadmin 시뮬레이터 실화면(SOT 규칙 충족).

## 결함 1 — 🔴 아크릴 7상품 수량할인 미배선 (돈 크리티컬·확정·교정대기)

**사슬 끊김 위치 = L1(상품→할인테이블 배선)**. 같은 공식 `PRF_CLR_ACRYL`을 쓰는 9상품 중
2개만 `DSC_ACR_QTY`("아크릴 카테고리 수량별 구간할인") 배선, **7개 미배선**:

| prd_cd | 상품명 | 배선 |
|---|---|---|
| PRD_000161 | 판아크릴 | ✅ DSC_ACR_QTY |
| PRD_000162 | 아크릴포카스탠드 | ✅ DSC_ACR_QTY |
| **PRD_000152** | **아크릴명찰** | ❌ 미배선 |
| **PRD_000148** | **아크릴뱃지** | ❌ 미배선 |
| **PRD_000157** | **아크릴네임택** | ❌ 미배선 |
| **PRD_000150** | **아크릴스마트톡** | ❌ 미배선 |
| **PRD_000151** | **맥세이프 스마트톡** | ❌ 미배선 |
| **PRD_000159** | **아크릴 코스터** | ❌ 미배선 |
| **PRD_000158** | **아크릴 포카키링** | ❌ 미배선 |

- 할인테이블 `DSC_ACR_QTY` 이름 = "**아크릴 카테고리** 수량별 구간할인" → 카테고리 전체 적용 의도.
  구간(typ .01 단가형): 1~49=0% · 50~99=**10%** · 100~299=**20%** · 300~499=**30%** · 500~999=**40%** · 1000+=**50%**.

### webadmin 실화면 검증 (아크릴명찰 152 vs 판아크릴 161 · 동일 50×50 · 동일 단가 4,800/개)
| 수량 | 152 미배선 | 161 배선 | 과청구 |
|---|---|---|---|
| 30 | 144,000 (`discounts:[]`) | 144,000 (1~49=0%) | 0 (정상) |
| 100 | 480,000 (`discounts:[]`) | 384,000 (20%↓ −96,000) | **+96,000 (25%)** |
| 1000 | 4,800,000 (`discounts:[]`) | 2,400,000 (50%↓ −2,400,000) | **+2,400,000 (정가의 2배)** |

→ 미배선 7상품은 대량주문 시 수량할인이 **전혀 적용되지 않아 최대 2배 과청구**. 프레임워크 §3.1 지목과 정확히 일치.

### 교정안 (인간 승인 필요 · 라이브 COMMIT)
`t_prd_product_discount_tables` 에 7행 INSERT(prd_cd × `DSC_ACR_QTY`). 멱등 ON CONFLICT.
배선된 판아크릴/포카스탠드가 정상 작동 = 그릇·구간 검증 완료. **base 가격 무변경**(할인만 추가).
→ 실행 전 게이트: 각 상품 webadmin 실화면에서 배선 후 할인 적용 재확인.

## 결함 2 — ⚪ 굿즈 7상품 수량할인 미배선 = **밴드축 FALSE POSITIVE (권위 판정 완료·종결)**

> ★판정(260706·hpti-authority-extractor 3중 삼각검증·`GOODS7-BAND-JUDGMENT-260706.md`): 권위(상품마스터 260703
> 굿즈파우치 시트 "구간할인적용테이블" col24) 대조 결과 **7상품 전부 밴드 무명시** → 미배선이 설계상 정답. **적재 제안 없음**.
> - 만년스탬프217·레더라벨제작280 = col24 **"구간할인없음" 명시**(HIGH). 클립보드215·투명클립보드216·이미지피켓229 = 공란(MED-HIGH).
> - 키캡키링202 = 공란+use_yn=N(비활성). **아크릴쉐이커코롯토226 = BLOCKED**(공식 note "미바인딩 해소 시그널·실무진 확인"·TBD·비활성).
> - 적재규칙="명명밴드(굿즈A/B·말랑·파우치)만 배선"·배선된 형제 24개 전부 명명밴드 보유 → 7상품 미배선은 규칙 정합.
> - 돈 방향 = 정가 청구(과소청구 아님·보수적) → 긴급 아님. 향후 할인 원하면 결함교정 아닌 신규 권위 등록 사안.
> - **[교훈] 형제 불일치 센서스의 🟡(밴드 분산 공식)는 권위 판정 없이 결함 단정 금지** — 결함1(카테고리 일괄 DSC_ACR_QTY)과
>   달리 굿즈는 상품별 명명밴드라 미배선=정상일 수 있다. 실측(결함1·3)과 권위판정(결함2)로 트리아지 분리 확정.

<details><summary>원 발견 기록 (판정 전)</summary>

공식 `PRF_GOODS_FIXED_SIZ`(24 배선 / 7 미배선). 배선 24개는 4종 할인테이블 분산
(DSC_ACR_QTY·DSC_FABRIC_QTY·DSC_GOODSA_QTY·DSC_GOODSB_QTY) = **상품별 밴드 배정**(카테고리 일괄 아님).

| prd_cd | 상품명 |
|---|---|
| PRD_000215 | 클립보드 |
| PRD_000202 | 키캡키링 |
| PRD_000217 | 만년스탬프 |
| PRD_000229 | 이미지피켓 |
| PRD_000280 | 레더라벨제작 |
| PRD_000216 | 투명클립보드 |
| PRD_000226 | 아크릴쉐이커코롯토 (TBD·PRF_ACRYL_SHCOROTTO_TBD 죽은공식) |

- **준비된 goods-57 band-assign CSV(다 트랙)에 0 매칭** → 그 적재본과 별개의 갭.
- 밴드 4종 분산이라 미배선=결함인지, 어느 밴드인지 **권위(상품마스터 260703 굿즈시트) 대조 필요**.
  아크릴처럼 자동 확정 불가 → hpti-authority-extractor 또는 인간 판정. **자동 교정 금지**.
</details>

## 결함 3 — 🔴 책자 내지 5종 디지털인쇄 공정 미배선 (저청구·돈 크리티컬·확정·교정대기)

**타 차원 배치화(`sibling_conformance.py`)로 발견 — [공정] 차원.** 공식 `PRF_DGP_INNER`(7상품 중 2배선/5미배선).

| prd_cd | 상품명 | 공정 배선 |
|---|---|---|
| PRD_000284 | 하드커버책자-내지 | ✅ PROC_000004 (mand Y·seq -1) |
| PRD_000110 | 엽서캘린더 | ✅ PROC_000079,PROC_000004 |
| **PRD_000285** | **레더 하드커버책자-내지** | ❌ 미배선 |
| **PRD_000286** | **하드커버 링책자-내지** | ❌ 미배선 |
| **PRD_000287** | **중철책자-내지** | ❌ 미배선 |
| **PRD_000289** | **무선책자-내지** | ❌ 미배선 |
| **PRD_000291** | **PUR책자-내지** | ❌ 미배선 |

- **기전**: `COMP_PRINT_DIGITAL_S1`(디지털 인쇄비) 단가행은 **전부 `PROC_000004`(디지털인쇄)에만** 충전(625행).
  5종은 `t_prd_product_processes` 배선이 없어 인쇄방식 드롭다운이 **옵셋·디지털·실크 3개 전부 노출**
  (형제 284는 PROC_000004 배선이 화이트리스트 역할 → 디지털만 노출).
- **실측(qty100·SIZ_000170·MAT_000072)**: 미배선 287 옵셋(PROC_000003) 선택 시 `COMP_PRINT_DIGITAL_S1=0` final **768**
  vs 디지털(PROC_000004) 선택 시 인쇄비 **20,000** final 20,768. → **옵셋/실크 선택 시 인쇄비 0 저청구**.
- 260701 "디지털인쇄 base 공정 미바인딩→인쇄비 영구0"(18상품 교정) 결함 클래스의 **누락 5상품**(당시 284만 포함).
- **교정**: `bookinner-print-proc-260706-load.sql`(5종 PROC_000004 mand Y·seq -1 배선·멱등)·`-undo.sql`(적재전 0행 확인).
  base 단가 무변경. COMMIT 전 각 상품 webadmin 실화면(인쇄방식 디지털 제약) 재확인.

## 타 차원 배치화 센서스 결과 요약 (`sibling_conformance.py` · 전 차원)

전 사슬 형제 불일치를 5개 상품단위 배선 차원(수량할인·공정·판형·인쇄옵션·묶음수)으로 배치화. **신뢰도=배선율**:

| 차원 | 공식 | 배선/미배선 | 판정 |
|---|---|---|---|
| 수량할인 | PRF_CLR_ACRYL | 2/7 | 🔴 **결함1** (돈·확정) |
| 공정 | PRF_DGP_INNER | 2/5 | 🔴 **결함3** (돈·확정) |
| 수량할인 | PRF_GOODS_FIXED_SIZ | 24/7 | 🟡 결함2 (권위 밴드 판정 선결) |
| 공정 | PRF_CLR_ACRYL | 8/1 (코스터159) | ⚪ 후보A = **FP**(UV=단가 comp 0개·가격무영향·생산정보 갭) |
| 판형 | PRF_STK_FIXED | 12/1 (대형자유형057) | ⚪ 후보C = **FP**(고정가 by-siz·판수 미사용·260701 판정) |
| 공정/인쇄옵션/묶음수 | PRF_GOODS_FIXED_SIZ | 저배선율(3~16%) | ⚪ **FP**(굿즈=완제품 통가격·해당 배선 불필요·센서스 "약" 랭크) |

- **[HARD] 교훈**: 저배선율(소수만 배선) = 대개 설계상 정상(FP). **고배선율·소수 이상치(★강)만 진짜 결함**.
  단 PRF_DGP_INNER(2/5)는 배선율 낮아도 실측으로 진짜 결함 확인 → **비율은 트리아지 신호, 확정은 실측**.

## 원칙
- 라이브 읽기전용·결정론. 교정 = 인간 승인 + webadmin 실화면 후 dbmap COMMIT.
- 결함1·3은 확정(실측 검증완)·결함2는 권위 판정 선결. base 가격 무변경(할인/공정 배선만 추가).
