# 아크릴 — 정규화·오염 방지 가드 (normalization-guard)

> **작성** 2026-06-14 · round-13. 사용자 기준 6번 = "정정으로 들어가는 모든 값이 임의 판단 아닌 화이트리스트 안에 있음을 입증".
> **3중 화이트리스트:** ① table-spec 도메인값(`docs/huni/table-spec_260610.html`) ② webadmin sql/ FK·제약 ③ 260610 L1 엑셀 명시값.
> **목표:** 화이트리스트 밖 값 0 보장. apply.sql이 적재/변경하는 모든 코드값을 3축으로 검증.

---

## 1. 즉시적용분(AW-CAT) 적재값 화이트리스트 입증 (apply.sql 실행분)

| 적재 컬럼 | 적재값 | ① table-spec 도메인 | ② webadmin FK/제약 | ③ 260610 L1 엑셀 | 안? |
|-----------|--------|---------------------|--------------------|--------------------|:--:|
| `t_prd_product_categories.cat_cd` (146~152) | CAT_000140~146 | 카테고리 코드(아크릴키링~명찰) | FK→`t_cat_categories` (라이브 실재·upr=CAT_000009 아크릴·lvl2) | 구분=단품형·MES 009-0001~0007 | ✅ |
| `cat_cd` (153) | CAT_000147 골드실버아크릴명찰 | 카테고리 코드 | FK→t_cat_categories 실재(upr=009·lvl2) | 명찰(골드실버)·009-0007 | ✅ |
| `cat_cd` (154) | CAT_000153 아크릴머리끈 | 카테고리 코드 | FK 실재 | 머리끈·009-0011 | ✅ |
| `cat_cd` (155~159) | CAT_000148~152 | 볼펜·지비츠·네임택·포카키링·코스터 | FK 실재(upr=009·lvl2) | 009-0008/0010/0020 등 | ✅ |
| `main_cat_yn` | 'Y' / 'N' | YN 플래그(Y/N) | NOT NULL·char(1)·YN 도메인 | — (메타) | ✅ |
| `prd_cd` | PRD_000146~159 | 상품코드 | FK→`t_prd_products` 실재 | L1 prd_nm 매칭 | ✅ |
| `reg_dt`/`upd_dt` | now() | 타임스탬프 | reg_dt NOT NULL·upd_dt nullable | — | ✅ |
| `note` | "정정 2026-06-14: …" | 자유 메모(nullable) | nullable varchar | — (변경이력) | ✅ |

> **화이트리스트 밖 값 = 0.** 즉시적용분이 쓰는 카테고리 코드(CAT_000140~153)는 전부 라이브 카테고리 마스터에 이미 실재(2026-06-14 psql 실측: 14노드 모두 upr=CAT_000009 아크릴·lvl2). 신규 카테고리 코드 mint 0 — search-before-mint 준수.

**라이브 실재 입증 SELECT (재현):**
```sql
SELECT cat_cd, cat_nm, COALESCE(upr_cat_cd,'NULL'), cat_lvl
  FROM t_cat_categories
 WHERE upr_cat_cd='CAT_000009' AND cat_lvl=2 ORDER BY cat_cd;
-- → CAT_000140 아크릴키링|CAT_000009|2 · 141 아크릴마그넷 · 142 아크릴뱃지 …
--    147 골드실버아크릴명찰 · 148 아크릴볼펜 · 149 아크릴지비츠 · 150 네임택 · 151 포카키링 · 152 코스터 · 153 머리끈
-- 가짜 고아: CAT_000299 단품형|NULL|3 (부모 없음 → 정정 대상)
```

**DRY-RUN 입증(2026-06-14·BEGIN…ROLLBACK):** `INSERT 0 14`(정상노드 14연결·충돌 0) + `UPDATE 14`(고아 299 강등 14) + `ROLLBACK` → FK/NOT NULL/PK 위반 0·COMMIT 0.

---

## 2. −16 변형행 — 화이트리스트 무관(삭제 0) 입증

| 항목 | 입증 |
|------|------|
| 변형행 −16 라이브 EXTRA | **0건**(실측: 미니파츠163=1·코롯토164=6·포카코롯토165=1·입체블럭169=2·지비츠156=5 size 행, 과확장 0) |
| size 기계삭제 | **0건**(apply.sql에 t_prd_product_sizes DELETE/use_yn 강등 없음) |
| 가격사슬 파손 위험 | **0**(삭제 안 했으므로 적재된 size/price 사슬 보존) |

> [HARD] "260610에서 행이 사라졌으니 라이브 size를 지운다"는 임의 판단을 **하지 않음**. 화이트리스트 밖 값 삽입도, 화이트리스트 안 값 삭제도 0.

---

## 3. 컨펌대기/신규적재분 적재 후보값 화이트리스트 (실행 전 사전 검증)

| 적재 컬럼 | 후보값 | ① table-spec | ② webadmin FK | ③ 260610 L1 | 안? | 비고 |
|-----------|--------|--------------|---------------|--------------|:--:|------|
| `t_prd_product_processes.proc_cd` (UV) | PROC_000002 UV | 공정 코드(UV) | FK→`t_proc_processes` 실재 | 폴더=UV인쇄/루아샵 | ✅ | 변형 param 적재처(prcs_dtl_opt) 부재 → CONFIRM-AC-4 |
| `proc_cd` (완칼) | PROC_000053 완칼 | 공정 코드(완칼) | FK→t_proc_processes 실재 | 형상 굿즈 묵시(명시 컬럼 無) | ✅ | 모양 param 적재처 부재·묵시 모델링 CONFIRM-AC-1 |
| `proc_cd` (부착) | PROC_000081 부착 | 공정 코드(부착) | FK→t_proc_processes 실재 | 가공=자석/핀/끈/집게 | ✅ | 대상 enum[라벨,맥세이프,끈,테입]에 핀/자석/집게 부재 → 확장 |
| `t_prd_product_materials.mat_cd` (카라비너 고리) | 051(은)·052(금) 재사용 | 자재 코드 | FK→`t_mat_materials` 실재 | 고리 7색 | 🟡 | 5색(레드/블랙/핑크/하늘/화이트) 코드 부재 → mint 필요 |
| `mat_cd` (라미10T) | (없음) | — | **FK 대상 부재** | 소재=투명아크릴라미(10T) | ❌ | **라이브 라미10T 자재 0건 → 신설 선행 컨펌(AW-A1)** |
| `t_prd_product_addons.tmpl_cd` (볼체인) | (없음) | template 코드 | **FK 대상 부재** | 추가상품=볼체인 9색 | ❌ | **볼체인 template 0건(t_prd_templates 봉투류만) → 신설 선행** |
| prcs_dtl_opt 변형/모양값 (배면양면/사각/원형) | — | — | **적재처 테이블 부재** | 인쇄사양/형상 다수 | ❌ | **CONFIRM-AC-4/1 — CPQ option_items 경유 또는 ddl** |

> **컨펌대기 화이트리스트 밖(❌) 3건:** 라미10T 자재·볼체인 template·prcs_dtl_opt 적재처. 임의 mint 금지 — 데이터/스키마 신설이 인간 승인 대상이므로 apply.sql에서 주석 처리(실행 안 함). 카라비너 고리 5색(🟡)도 코드 신설 대기.

---

## 4. 오염 방지 — 무엇을 하지 않았나 (round-13 교훈 반영)

| 오염 패턴 | 본 정정이 회피한 방식 |
|-----------|----------------------|
| 색/형상/사이즈를 자재로 오적재 (MAT_TYPE.08~10 오염) | 형상(코스터 원형/사각·카라비너 4형상)=siz_nm 보존 + 완칼 모양=공정 옵션. 자재 테이블 미오염 |
| 별색을 도수로 오적재 | 아크릴 별색 없음(UV 풀빼다=화이트 underbase 흡수) → 해당 없음 |
| UV 변형을 인쇄면(도수)으로 오적재 | **이미 오적재된 상태(AW-M1)를 인지·정정 제안**(print_side→공정 변형). 추가 오적재 0 |
| 신규 카테고리 코드 남발 | 정상 노드 140~153 **재사용**(search-before-mint), 신규 mint 0 |
| 고아행 hard-delete | 고아 299(단품형) 연결은 **논리강등(main_cat_yn='N'+note)** — DELETE 안 함 |
| −16 변형행 기계삭제 | **라이브 EXTRA 0 실측 → 삭제 0**(가격사슬 보존·schema-design-intent-first) |
| 적재처 없는 값 억지 삽입 | 라미10T/볼체인 template/모양 param은 적재처 부재로 **주석 보류**(컨펌) — NULL 강제/엉뚱한 칸 삽입 안 함 |

---

## 5. 가드 결론

- **즉시적용분(AW-CAT 단품형 14상품 재연결): 화이트리스트 밖 값 0.** 전 코드값 3중 화이트리스트 안 + 라이브 실재 입증 + 멱등 DRY-RUN 통과(제약 위반 0·INSERT 0 14·UPDATE 14·ROLLBACK).
- **−16 변형행: 삭제 0·화이트리스트 무관**(라이브 EXTRA 0 실측).
- **컨펌대기분: 화이트리스트 밖 3건(라미10T·볼체인 template·prcs_dtl_opt 적재처)을 정직하게 보류** — 억지 삽입 0.
- 임의 판단 0: 모든 적재값이 ① 엑셀 명시(260610 L1) ② 라이브 FK 대상 실재 ③ table-spec 도메인 중 셋을 동시 충족하거나, 못 충족 시 보류.
