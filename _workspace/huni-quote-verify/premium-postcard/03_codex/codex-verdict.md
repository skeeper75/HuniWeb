# Codex(gpt-5.5) 독립 판정 — 원문 (외부 의견·가설)

> **★[HARD] 이 문서는 외부 모델(Codex gpt-5.5)의 독립 의견이다. 라이브/권위로 검증되기 전 사실로 채택 금지(환각 경계).**
> codex exec v0.140.0 · `-s read-only` · session 019eda62-afd2-7413-ab85-d1dfbf706619 · 2026-06-18
> 입력 = work-spec만(Claude 판정 비노출·독립성 유지). codex는 라이브 DB·파일 쓰기 접근 없음 — 프롬프트에 제공된 사실만으로 추론.

---

## 판정: NO-GO(교정가능)
> 프리미엄엽서 인쇄비는 현재 가격표 3D 매트릭스의 축을 잘못 접은 상태이고, S1/S2 동시 합산까지 겹쳐 골든 케이스와 불일치합니다.

## 3축 판정
- **(a) SOT 일치: PASS** — 상품마스터상 front_colrcnt_cd=CLR_000005(칼라 고정)·print_opt_cd=단면/양면 축. 권위 가격표도 도수×면×출력매수 구조 제공 → 원천 정의 자체는 해석 가능.
- **(b) 공식↔구성요소 매핑: FAIL** — PRF_DGP_A에 S1·S2가 둘 다 addtn_yn=Y로 배선 → 엔진 계약상 두 subtotal 모두 합산. 단면/양면 중 하나만 선택돼야 하는 인쇄비 구조와 불일치.
- **(c) 차원 매칭: FAIL** — 상품 정의에서 print_opt_cd는 면. 그런데 라이브 S1/S2 가격데이터에서 POPT_000001/000002 슬롯이 흑백/칼라처럼 사용됨. 가격표 [도수] 축 일부가 print_opt_cd에 들어간 상태라 충돌 실재.

## 질문별 답
1. **제대로 계산되지 않음.** 깨지는 축 = (b) 공식↔구성요소 매핑 + (c) 차원 매칭. SOT 자체는 칼라 고정+단면/양면 의미 명확.
2. **S1/S2 차원 매핑 올바르지 않음.** 손님이 "단면"(POPT_000001) 선택 시:
   - S1 → 450/400/350/130 매칭 = 가격표 B열(흑백·단면)
   - S2도 합산되면 → 900/800/700/260 = 가격표 C열(흑백·양면)
   - 따라서 단면 선택이 칼라단면 D열로 가지 않음.
3. **맞음. 칼라전용이므로 올바른 단가 = 칼라단면 D열/칼라양면 E열.** 국4절 출력매수25:
   - 칼라단면 D = 800 → 800×25 = **20,000** = 골든 단면 일치
   - 칼라양면 E = 1600 → 1600×25 = **40,000** = 골든 양면 일치
4. **예, 이중 합산 발생.** 공식은 subtotal 단순 합산·addtn_yn=Y인 S1/S2 둘 다 계산. 엔진에 "단면이면 S1만, 양면이면 S2만" 배타 선택 개념 없음.
5. **추가 리스크: 있음.**
   - **R-codex-1**: siz_cd와 plt_siz_cd가 각각 다른 구성요소에 쓰여 같은 국4절 의미라도 룩업 경로가 둘로 갈라짐 → 데이터 정합성 유지 리스크.
   - **R-codex-2**: 엔진에 판걸이수/출력매수 보정/ceil 없음 → 입력 qty가 이미 "출력매수"로 정규화돼 들어온다는 전제가 깨지면 가격 선형 왜곡.
   - **R-codex-3**: 도수 고정 상품인데 가격 룩업 차원에 front_colrcnt_cd 같은 도수 축이 반영 안 됨 → 흑백/칼라/별색 확장 시 같은 충돌 반복 가능성.

## 확신도: High
**모르는 것(codex 자진 명시)**: 실제 DB 전체 formula_components 조건식, addtn_yn의 엔진 내 추가 의미, qty가 호출 전 출력매수로 변환되는지 여부는 work-spec 밖이라 단정 안 함. 제공된 엔진 계약 기준으로는 위 판정 성립.

---
tokens used: 5,186
