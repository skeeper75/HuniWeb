# wave-3 UI 실화면 검증 (PRD_000047 소량전단지)

> §31 Phase 5 · hcr-ui-registrar · 2026-07-02 · gstack browse + HUNI_ADMIN_* 읽기 탐색(저장/삭제 클릭 0)
> 화면: `/admin/product-viewer/PRD_000047/constraints/` (로그인 후) · 규칙 `R_EXCL_COATING_THIN_PAPER`

## 완료 정의 판정 — **PASS (4/4)**

| # | 확인 항목 | 판정 | 증거 |
|---|-----------|------|------|
| ① 역파싱 | 규칙이 **폼빌더 편집 폼**으로 열림(raw JSON 폴백 아님)·그룹 2개(코팅 or-2 · 종이 or-15) | **PASS** | 편집 진입 URL `?edit=R_EXCL_COATING_THIN_PAPER` → 옵션-dim rows=2(코팅 유광/무광)·자재-dim rows=15(얇은종이 15종) JS 실측·AND 배지로 두 그룹 결합 · `047-02-edit-builder-parsed.png` |
| ② 표시 | 규칙명·차원(var)·조건·err_msg 읽히는 형태 | **PASS** | 규칙명 `[제약조건] 코팅은 두꺼운 종이(180g 이상)에서만 가능`·유형 배지 `금지`·차원 드롭다운(옵션/자재)·조건값(코팅 유광/무광·백색모조지 100g 등)·오류문구 textbox 노출 · `047-02` |
| ③ 조정가능 | 조건 값 변경이 UI에서 가능 → 저장 없이 이탈 | **PASS** | 종이 그룹 1행 값 `백색모조지 100g → 220g` 변경 성공(JS 확인) → **저장 버튼 미클릭** · 페이지 reload 로 폐기 → DB 재확인 MAT_000072 유지(미저장 확정) · `047-03-adjust-paper100to220-unsaved.png` |
| ④ 동작 | `/validate/` 미리보기로 rule-spec 막힘/통과/통과2 재현 | **PASS** | 검증 미리보기 패널 "이 조합 검사" 3케이스 재현(아래) · validate POST 3회 전부 **HTTP 200**(130ms·410B, 500 무발생) |

## validate 3케이스 (검증 미리보기 · Ajax `/validate/`)

| 케이스 | 옵션(코팅) | 자재(종이) | 기대 | 실화면 결과 | 판정 | 스크린샷 |
|--------|-----------|-----------|------|-------------|------|----------|
| 막힘 | 유광(OPV_000280) | 백색모조지 100g(MAT_000072) | 차단 | 빨강 박스 "막힘 — 코팅은 두꺼운 종이(180g 이상)에서만 가능합니다… (규칙: [제약조건]…)" | **PASS** | `047-04-validate-BLOCK-glossy-100g.png` |
| 통과 | 유광(OPV_000280) | 백색모조지 220g(MAT_000074) | 허용 | 초록 박스 "통과 — 통과" | **PASS** | `047-05-validate-PASS-glossy-220g.png` |
| 통과2 | 코팅없음(OPV_000279) | 백색모조지 100g(MAT_000072) | 허용(오차단 방지) | 초록 박스 "통과 — 통과" | **PASS** | `047-06-validate-PASS2-nocoat-100g.png` |

- 통과2 = 코팅을 안 걸면 얇은 종이도 허용 → RULE_TYPE.02 금지형의 **오차단 0 구조**(손님이 유광/무광을 적극 선택했을 때만 발동) 실화면 실증.

## 안전 준수
- HUNI_ADMIN_* 읽기 탐색만 · **저장/삭제/일시중지 버튼 미클릭**(③은 reload 로 폐기) · validate 는 읽기 Ajax.
- 스크린샷에 비밀값 비노출(로그인 자격증명·env 값 미포함).
- console 오류 0 · validate POST 500 무발생.
