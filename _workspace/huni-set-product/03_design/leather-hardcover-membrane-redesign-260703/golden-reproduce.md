# 072 면지 통합 재설계 — 골든 재현(무손상 입증) DRY-RUN

> §23 · 2026-07-03 · 라이브 엔진 `evaluate_set_price` 실호출(webadmin `/simulate-set/` · `HUNI_ADMIN_*`).
> 재현 스크립트: `_repro.py`(구조 diff) · `_repro2.py`(내지 manual/derived 대비). 라이브 읽기전용.

## 1. 참 골든 = 현재 라이브(실 시뮬레이터 경로)

내지 284는 `semi_role_cd` 없음 → 실 시뮬레이터에서 `is_inner=false` → **manual 출력매수**(기여 0). 따라서 현재 라이브 셋트가 = **셋트공식 PRF_HC_MUSEON_SET 단독**.

| copies | final_price | set_contrib | 내지 기여 | 면지 기여(074/075/076) |
|---|---|---|---|---|
| 1 | **34,100** | 34,100 | 0 | 0 / 0 / 0 |
| 10 | **159,100** | 159,100 | 0 | 0 / 0 / 0 |
| 100 | **796,900** | 796,900 | 0 | 0 / 0 / 0 |

§34 handoff·scoreboard-sets.csv(796,900·members_priced=0)와 일치.

## 2. 재설계 후 상태 재현

### 2-a. 구조 diff (면지 3멤버 → 1멤버 → 0멤버, 모두 동일)
`_repro.py PRD_000072` — 멤버 payload만 바꿔 라이브 재계산:

| 케이스 | 멤버 구성 | copies 1 / 10 / 100 |
|---|---|---|
| BASELINE | 073+284+074+075+076 | 34,284 / 160,944 / 815,338 (derived 참고치) |
| REDESIGN | 073+284+074 | 34,284 / 160,944 / 815,338 |
| SANITY | 073+284 (면지 0) | 34,284 / 160,944 / 815,338 |

→ 면지 멤버 3→1→0 전부 **동일** = 면지 기여 정확히 0(구조 무손상). ※이 표는 내지 derived(pages=24) 가정치 — 절대비교 목적, 값 동일성이 핵심.

### 2-b. 실 시뮬레이터 경로(내지 manual) 재설계 후 = 참 골든 불변
`_repro2.py` + 단일 면지멤버 074에 자재 MAT_382 명시 선택(재설계 후 실제 UI 상태):

| copies | 재설계 후 final | 면지074 기여 | 판정 |
|---|---|---|---|
| 1 | **34,100** | 0 | = 참 골든 ✓ |
| 10 | **159,100** | 0 | = 참 골든 ✓ |
| 100 | **796,900** | 0 | = 참 골든 ✓ |

## 3. 무손상 판정

- **final_price 불변**: 34,100 / 159,100 / 796,900 (재설계 전후 동일).
- **면지 기여 = 0**(공식 없음 → 자재 유무·색 선택 무관). 3→1 통합 가격영향 0.
- **셋트공식 자재 미종속**(use_dims=[min_qty]) → 부모 면지자재 은퇴가 셋트가 무영향.
- **이중합산 0**: 면지비 이중 계상 없음(골든 불변이 증명).
- **PRICE≠0**: 전 케이스 final>0.

→ **S4(가격 e2e) 무손상 통과 예상.** 실 게이트는 `hsp-set-gate-validation`이 evaluate_set_price 독립 재계산 + DRY-RUN으로 재판정.

## 4. 게이트 인계 체크리스트
- [ ] apply.sql 롤백전용 DRY-RUN(트랜잭션 BEGIN…ROLLBACK) 멱등·트리거 통과 확인.
- [ ] 재설계 후 라이브 골든 34,100/159,100/796,900 재계산 일치.
- [ ] 074 자재 드롭다운(화/블/그) 렌더·fn_chk_opt_item_ref 위반 0.
- [ ] 인간 승인 후 webadmin 실화면(제외 0·PRICE≠0·판형 자동선택).
