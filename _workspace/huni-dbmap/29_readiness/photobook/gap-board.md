# 포토북 — 갭 보드 (round-19 · 미적재/오적재/가격 미구성)

> 2026-07-03 · `dbm-readiness-auditor`. 라이브 읽기전용 실측 근거. **실 COMMIT/배선 = 인간 승인 후 기존 라운드.**

## 요약

- **가격 silent-0 / 미적재 셀 = 0건.** base 11행 + per-page 4행 전부 권위 verbatim 적재·골든 4/4 오차0.
- **미달분은 전부 가격 데이터 밖** — ② 차원환원 배선(결정), 코드트랙 2, 데이터 미세갭 1, 구조 note 1.
- **가격으로 닫을 COMMIT 필요분 = 없음.**

## 갭 목록

| # | 유형 | 항목 | 재현/증거 | 돈영향 | 처리·라우팅 |
|---|------|------|-----------|--------|------------|
| G1 | **② 차원환원 미배선** | 표지타입 opt_cd(하드/레더/소프트)→표지자재·표지멤버 `t_prd_product_option_items` **0건** | `SELECT * FROM t_prd_product_option_items WHERE prd_cd='PRD_000100'` → 0행. 옵션(OPV_000484~486)은 있으나 ref_dim 미해소 | 없음(가격은 opt_cd text로 계산됨) · **생산전달 영향**(선택 표지→어느 소재/멤버 불명) | **결정 필요** → §34 거버넌스/§23 셋트. 셋트 표지 택1을 (a)자재 드롭다운 배선 (b)멤버 활성 중 택. 단독 데이터 적재 아님 |
| G2 | **코드트랙(셋트 UI)** | STAT-SET-SIZCD — 셋트 시뮬 UI가 siz_cd 미전파 → webadmin 초기 0원 | set_selections에 siz_cd 주면 정상 계산(Q5). 미전파 시 base 0 | 없음(엔진 정상) · UX | **§6 위젯 / DEV-REQUEST**(엽서북 094 동근·공통) |
| G3 | **코드트랙(위젯 계약)** | OI-PAGE — 편집기 page count→내지 qty(copies×⌈(page−24)/2⌉) 계약 미구현. 서버 'derived' 모드는 페이지→qty 미계산 | qty_mode='manual'·qty=13 → A4하드50P=23800 OK / 'derived'·pages=50 → included=False·증분0 | 없음(데이터 완료) · **위젯 미구현시 페이지 증분 누락** | **§6 위젯**. manual 계약 채택(memory OI-PAGE) |
| G4 | **데이터 미세갭** | 소프트커버 page_rule 4~14(권위) 미차등 — 라이브 24~150 단일(하드 기준) | `t_prd_product_page_rules` PRD_000100 1행 24/150/2. 소프트 하한 4 미반영 | 미미(base 24P는 소프트 4P여도 적용·증분만 영향) | 선택 COMMIT. 셋트라 부모 단일 page_rule — 소프트 차등 필요시 제약/멤버 검토 |
| G5 | **구조 note** | 표지 멤버 5(102 하드/103 아트250/105 레더하드/106 레더/107 소프트) vs 표지타입 옵션 3. 레더하드=레더·아트250=소프트 중복 구조 | `t_prd_products` PRD_000101~107 + `t_prd_product_materials` USAGE.02 5행 | 없음 | 잉여 멤버 정리 여지 → §34 거버넌스(선택·현행 무해) |

## 견적가능 폐루프 결론

- **③ 가격 = GO** — 재적재 불필요. 되돌리지 말 것(2026-06-29 COMMIT: PRF_PHOTOBOOK_FIXED/INNER·COMP_BASE 11·COMP_PAGE 4·OPT_000079 표지타입).
- **견적가능 = 🟡 조건부 GO** — 데이터 미달 0. 조건 = ① G1 표지타입 배선 결정 ② G2·G3 코드트랙(위젯).
- **다음 닫을 것:** G1 결정(표지 택1 생산전달 모델) → 확정 시 §34/§23. 코드 2건은 위젯 하네스(§6) 큐.
