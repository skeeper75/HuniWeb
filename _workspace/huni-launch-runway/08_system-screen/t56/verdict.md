# t56 verdict — 오픈일정 S7-A · 역할별 Todo 재판정

브랜치 `WT-role-todo-rejudge` · 워크트리 `.claude/worktrees/t56` · 분기점 `c53fe59c`(=`WT-unlisted-rejudge` 끝, t51 merged.csv·CONTRACT.md 포함).
읽기전용 — DB write 0 · 라이브 COMMIT 0 · MES 저장소 쓰기 0 · 날짜·작업량 추정 0. **재배정은 전부 제안까지이고 결정은 지니.**

## 0. 범위

카드 본문 ①②④⑤ 수행. ③(위젯 남은 일 5행 역방향 확인)은 리드 lane-1 통지(260919)로 **t58(lane-4)로 이관** — t56 은 하지 않는다.
다만 착수 직후 ③을 일부 실측한 뒤 이관 통지를 받았으므로, 버리지 않고 §6 에 t58 인계 메모로 남긴다.

## 0-A. 재작업 기록 (260919 · 리드 lane-1 PASS + 재작업 2건)

초판(커밋 `41d20930`) 뒤 리드 판정 PASS. 이어서 두 건을 고쳤다 — **규칙은 `judge.py` 에서 고치고 735행을 전부 재생성했다(수기 패치 0).**

1. **페이지빌더 담당 = 김동학(쇼핑개발)** — 지니 결정 260919. 초판이 「자사 관리도구」로 보아 서희항으로 돌린 판단은 **철회**한다(CARDS-S §1 A5 그대로). `SITE_OWNER['pagebuilder']` 한 줄을 바꿔 전 행 재판정.
   - `STD-SYS-021`(페이지빌더) 재배정→서희항 ⇒ **담당맞음(김동학)** · `STD-CAT-007`·`STD-CAT-012` 분할 ⇒ **담당맞음(김동학)** · `STD-ADP-015` 분할 ⇒ **담당맞음(김동학)**(조회·편집이 같은 사람에게 모임).
   - `STD-CAT-019`(인쇄 가이드 11종)·`STD-INF-007`(가이드북 화면)은 시스템 경계가 한 사람으로 모여도 **원고(최숙진)와 화면(김동학)은 다른 일**이라 분할을 유지한다 — 제안 담당만 서희항→김동학으로 바꿨다.
   - `STD-ADC-014`(인쇄 가이드 콘텐츠 관리)는 pagebuilder 가 아니라 webadmin(`s3_guide.py`)이라 서희항 그대로.
2. **STD-SYS-008 근거 범위 한정** — 리드 지적대로 `shopby_sync.py` 는 열어 보면 메인이미지 동기화 파일이다. 재확인 결과 그 파일이 미는 축은 **메인이미지(`:118 sync_main_images`)와 전시 `frontDisplayYn`(`:315 sync_front_display`) 둘뿐**이고 공통 클라이언트는 `shopby_client.py` 다. 시작가·옵션 등 나머지 상품 축의 push 코드는 없다(시작가는 스킨이 `GET /api/w/v1/catalog` 를 당겨가는 pull — `STD-CAT-043`). **결론(동기화 코드가 전부 webadmin 에 살고 스킨 코드 0)은 유지**하고, `rejudge.csv` 의 `basis` 에 범위 한정과 추가 path:line 을 적었다.

## 0-B. 보정 묶음 (260919 · 리드 lane-1 · 지니 나머지 역할 확정)

지니가 260919 에 나머지 역할을 확정해 `judge.py` 상단 책임 경계에 반영하고 **735행을 다시 재생성**했다(수기 패치 0).

> · **신우진 = 프로젝트 총괄 PM** — CTO 와 기술 적합성 논의 · 기초자료 정리 · 프로젝트 전체 프로세스 정리 · 일정관리. **만들지 않는다.**
> · **최숙진** = ① 가이드북 콘텐츠 ② 상세페이지 탭 콘텐츠 ③ 후니프린팅 정책(엑셀) 정리 ④ 내부 프로세스 정의(→ CTO 와 생산 연동에 필요한 내용 정리) ⑤ 테스트 시나리오별 내부팀 역할 배정. **도구·화면은 최숙진 몫이 아니고 그 안의 내용이 최숙진 몫.**

**1) 「PM 은 만들지 않는다」 게이트(`_pm_guard`)** — 구현·설정 행을 신우진에게 **보내는** 재배정 제안을 막는다. 결정과 구현이 한 행에 섞인 경우의 「구현 담당 + 신우진」 **분할은 유지**(거기서 신우진은 만드는 쪽이 아니라 결정하는 쪽).
리드가 지목한 3행 + 같은 규칙으로 돌아간 1행을 잡았다:

| row_id | 전 | 후 | 근거 |
|---|---|---|---|
| STD-SYS-017 데이터 백업·복구 절차 | 김동학→신우진 | **재배정→서희항** | 관리서버·DB 영역(T1 인프라와 같은 손) |
| STD-SYS-019 반응형/모바일 대응 | 김동학→신우진 | **담당맞음(김동학)** | 스킨 전 화면 품질축 |
| STD-SYS-022 구→신규 URL 리다이렉트 | 김동학→신우진 | **담당맞음(김동학)** | 스킨 라우팅·도메인 |
| T5-3 법정 표기 단일화 + 증빙 목업 제거 | 김동학→신우진·최숙진 | **분할 김동학+최숙진** | 표기 내용=최숙진 / 화면 수정=김동학 |

게이트 적용 후 **「구현 행을 신우진으로 보내는 재배정」은 0건**이다(남은 신우진 지목 16행은 전부 결정+구현 분할).

**2) STD-ADC-014 인쇄 가이드 콘텐츠 관리 — 재배정(서희항) ⇒ 분할(서희항+최숙진).** 리드가 t59 근거를 전달했고 **직접 열어 재확인**했다:
- `raw/webadmin/webadmin/config/urls.py:73-74` — `admin/guide-file-md/`(상품별 작업가이드 파일 관리) 화면 실재.
- 같은 파일 `:81-83` — `guide_save` 주석 「prd_cd 스코프 부분 upsert(**표시명·태그·순서·비고만 갱신**, 원본파일명·키는 불변)」. 도구가 받는 입력이 곧 콘텐츠다.
- `_workspace/_foundation/live-snapshot/snap_20260827_1422/t_prd_guide_files.csv` **504행 · 상품 88종**(`check_guide.py` 재집계): `orig_file_nm`·`file_key`·`disp_seq` 는 **전건 채움**인데 `guide_nm`·`tags`·`note` 는 **전건(504/504) 공란**. → **도구는 있고 내용이 비었다.**
- [원장 결함 기록] 이 행의 `check_method` 는 「샵바이 셀러어드민에서 화면이 열리면 완료」인데 `evidence` 는 webadmin 이다 — 확인처 불일치. `rejudge.csv` note 에 적었고 교정은 원장 쪽 몫이다.

**3) 「도구=개발 / 내용=최숙진」 일관 적용(`CONTENT_SPLIT`)** — 같은 규칙이 걸려야 하는 콘텐츠 행을 손으로 골라 분할했다(정규식 금지 — 과발화 방지). `STD-CAT-019`·`STD-INF-007` 은 이미 분할이라 유지.

| row_id | 분할 제안 |
|---|---|
| STD-CAT-007 상품 상세페이지 본문 | 김동학(화면) + 최숙진(본문 콘텐츠·역할②) |
| STD-CAT-009 후가공 용어 툴팁 | 김동학(툴팁) + 최숙진(용어 설명) |
| STD-CAT-012 재작업 불가 고지 | 김동학(guide-data.ts 스텁) + 최숙진(고지 문구) |
| STD-CAT-034 상세페이지 탭 6종 자동 등록 | 서희항(자동등록 도구) + 김동학(표시) + 최숙진(탭 콘텐츠) |
| STD-INF-005 공지사항 고객 화면 | 김동학(화면) + 최숙진(공지 작성·게시) |
| STD-OPT-057 규격 가이드 모달 | 김동학(모달) + 최숙진(사이즈 안내 내용) |
| STD-OPT-058 주문가능 자재 목록 모달 | 김동학(모달) + 최숙진(자재 안내 내용) |
| STD-ADC-014 인쇄 가이드 콘텐츠 관리 | 서희항(관리 화면) + 최숙진(표시명·태그·비고) |

t59 의 신규 제안 21행(NEW-*)은 **넣지 않았다** — `rejudge.csv` 는 735행 1:1 원장이다.

## 1. 산출물

| 파일 | 내용 |
|---|---|
| `rejudge.csv` | 735행 전건 재판정(판정·제안담당·근거등급·근거·evidence) |
| `todo-by-owner.md` | 담당자별 Todo — 한 장 요약 → 트랙 → 행(Top→Down) |
| `reassign-proposal.md` | 재배정 제안표(이동 흐름 + 행 단위 제안) |
| `lead-check.md` | ② 리드 1차 후보 17행 대조 + 김동학 T2·T4 41행 전수 |
| `judge.py` · `render.py` · `verify.py` · `join.py` · `xref.py` · `dump.py` · `show.py` · `sig.py` · `merged_stats.py` | 판정·산출·검산 스크립트(결정론) |
| `verify-result.txt` | 검산 11게이트 출력 |

## 2. 행수·분포 (검산 출력 그대로)

- 입력: `07_rebaseline/S/S5-plan/plan-rows.csv` **735행** · `08_system-screen/t51/merged.csv` **747행**
- 출력 `rejudge.csv` **735행**(입력과 1:1)
- 남은 일(status≠작동/완료) **620행** / 남은 일 아님 115행

| 판정 | 행수 |
|---|---|
| 담당맞음 | 456 |
| 재배정(제안) | 134 |
| 분할(경계 걸침·행을 나눠야 함) | 44 |
| provided(샵바이·MES·PitStop 제공 — 개발 일 아님) | 37 |
| 불필요 | 1 |
| 미정(외부·대표 담당 또는 실측 전) | 63 |

| 근거등급 | 행수 | 뜻 |
|---|---|---|
| merged-owner_side | 366 | t51 merged.csv 가 그 기능이 사는 코드 쪽(`owner_side`/`system`)을 이미 적어 둔 행 |
| evidence-path | 177 | plan-rows `evidence` 가 저장소·호스트를 직접 가리키는 행 |
| rule-track | 99 | 위가 없어 트랙·수기 판단만으로 판정 — **약한 근거** |
| rule-step | 85 | 위가 없어 CARDS-S §1 단계 기본 담당으로만 판정 — **약한 근거** |
| manual-read | 8 | 내가 그 evidence 파일을 열어 확인한 행(아래 §5) |

**약한 근거(rule-*) 184행 = 전체의 25%.** 이 행들은 제안이지 판정이 아니다. 실측 없이 배정을 바꾸면 안 된다.

## 3. 담당자별 (남은 일 기준)

| 담당 | 현재 행 | 남은 일 | 담당맞음 | 재배정 | 분할 | 제공/불필요 | 미정 | 넘어올 제안 |
|---|---|---|---|---|---|---|---|---|
| 서희항 | 202 | 144 | 113 | 11 | 11 | 9 | 0 | 57 |
| 김동학 | 244 | 193 | 127 | 45 | 17 | 23 | 3 | 43 |
| 최숙진 | 153 | 150 | 104 | 31 | 10 | 6 | 2 | 65 |
| 신우진 | 69 | 66 | 19 | 40 | 4 | 0 | 3 | 16 |

재배정 이동 흐름 상위(사내 4담당, 남은 일·완료 구분 없이 재배정 판정 전체): 김동학→최숙진 27 · 신우진→김동학 22 · 최숙진→서희항 20 · 김동학→서희항 19 · 신우진→최숙진 12 · 최숙진→김동학 11 · 서희항→최숙진 8 · 신우진→서희항 7 (전체 표는 `reassign-proposal.md`). **구현 행을 신우진으로 보내는 재배정은 0건**(§0-B 1).

## 4. 카드 질문별 답

### ① 전 735행 5분류 재판정 — 완료
`rejudge.csv`. 판정 규칙은 `judge.py` 상단 docstring(책임 경계 + 사는 곳→담당 표 + 단계 기본 담당 표)에 전부 적었다.

### ② 김동학 앞 244행이 정말 쇼핑몰 스킨 일인가 — **아니다. 244행 중 88행이 스킨 밖·경계다**

- 244행 판정: 담당맞음 153 · 재배정 47 · 분할 18 · provided 23 · 미정 3 → **스킨 밖·경계 걸침 = 88행(36%)**
- 리드가 지목한 **T2·T4 41행**: 담당맞음 18 · 재배정 16 · 분할 3 · provided 4 → **23행(56%)이 스킨 밖**
- 리드 1차 후보 17행 대조: **16행 확인 · 1행 어긋남**(재배정 13 · provided 3 / 어긋남 1 = `STD-ADP-015`).
- **`STD-ADP-015`(상품 상세페이지 콘텐츠 편집)는 어긋남** — 페이지빌더 담당이 김동학으로 확정(지니 260919)되면서 조회(스킨)·편집(페이지빌더)이 같은 사람에게 모였다. **김동학 담당이 맞다.**
- 리드가 「경계 걸침」으로 본 **STD-SYS-009(가격 계산 결과 카트 주입 브리지)는 어긋남** — merged `t48:210` 이 `owner_side=huni-mall`·status 완료로 적어 두었다. 스킨 쪽에 살고 이미 동작하므로 **김동학 담당이 맞고 남은 일이 아니다**.
- 리드 목록에 없던 **추가 7행**을 새로 찾았다: `STD-CAT-033`(webadmin+widget) · `STD-CAT-041`(셀러어드민 집계) · `STD-CAT-043`(시작가 이관·분할) · `STD-SHP-008`(택배 배송=샵바이 제공·작동) · `STD-SYS-039`(웹훅 등록 화면=셀러어드민) · `STD-SYS-041`(huni_token 확정=결정+구현 분할) · `STD-SYS-045`(서버키 헤더=스킨+위젯 분할).
- 리드가 못 읽은 트랙도 봤다: T3 132행(담당맞음 99 · 재배정 10 · 분할 13 · provided 10) · **T5 41행(담당맞음 13 · 재배정 18 · 분할 1 · provided 9 — 여기가 가장 심하다. 셀러어드민 통계 STD-FIN-001~006·011, 감사로그 STD-SYS-003, 배송비·알림 설정이 쇼핑개발에 걸려 있다)** · T6 7 · T7 14(재배정 2) · T1 9(전부 담당맞음).

### ④-1 신우진(PM)이 구현 행을 들었는가 — **그렇다. 69행 중 43행이 재배정 대상(남은 일 기준 40행)**

PM 이 맞는 행은 19행(선행 입력 BLK-* · 결정 안건 · 리허설). 나머지는 실제 구현·설정 행이다.
- **김동학(스킨)으로 22행**: `STD-PAY-001`(신용카드 결제) · `STD-PAY-010`(B2B 후불) · `STD-ORD-015/017`(주문서 옵션) · `STD-MYP-004/005/019`(옵션보관함·마이페이지) · `STD-CLM-018/021` · `STD-SYS-012`(본인인증) · `STD-PRM-011/016` · `STD-INF-001~004` · `F4-7/F4-8`(Vercel) 등.
- **서희항(위젯·webadmin)으로 7행** — 그중 위젯·에디터 축 4행: `STD-OPT-053`(옵션보관함 저장) · `STD-OPT-054`(견적서 발급) · `STD-ART-016`(PitStop 연동) · `STD-ART-023`(에디터 작업물 저장).
- **최숙진(운영·셀러어드민)으로 12행**, 서희항·최숙진 양쪽 2행: `STD-FIN-015~018`(구IA 상품통계) · `STD-B2B-013/014` · `STD-PAY-006/029` · `STD-SHP-011` 등.
- 카드가 지목한 **T3-5**(「범위 결정 후 스킨 버튼·문의 라우트 구현」)는 한 행에 결정과 구현이 섞였다 → **분할**(결정=신우진 / 구현=김동학).

### ④-2 최숙진 T3 32행 — **18행 담당맞음, 14행이 아니다**

담당맞음 18 · 재배정 6 · 분할 4 · provided 4.
- 스킨 개발로 넘길 것: `STD-PAY-002`(실시간 계좌이체) · `STD-PAY-015/017/018`(증빙 화면) · `STD-ORD-025`(구매확정) · `STD-CLM-004`.
- 콘텐츠/구현이 한 행에 섞인 것(분할): `STD-CAT-019`(인쇄 가이드 11종) · `STD-INF-007`(가이드북 화면) — 콘텐츠=최숙진 / 화면·스텁=김동학.
- 셀러어드민 제공이라 개발 일 아님: `STD-PAY-003/028`(무통장·가상계좌) 등 4행.

### ⑤ 산출 — 완료
`todo-by-owner.md`(Top→Down: 담당자 한 장 요약 → 트랙 → 행) · `reassign-proposal.md`(재배정 제안표).

## 5. 파일을 직접 열어 확인한 행 (manual-read 8행)

| row_id | 연 파일 | 확인한 것 | 판정 |
|---|---|---|---|
| STD-SYS-008 | `raw/webadmin/webadmin/catalog/shopby_sync.py:118`(메인이미지)·`:315`(전시 frontDisplayYn) · `catalog/shopby_client.py:1-16` | **범위 한정**: 이 파일이 미는 축은 이미지·전시 둘뿐, 시작가는 스킨이 당겨가는 pull. 그래도 존재하는 동기화 코드는 전부 webadmin 안이고 스킨 코드 0 | 재배정→서희항 |
| STD-ADO-002 | `raw/webadmin/webadmin/catalog/models.py:1078-1093` | `TOrdOrders`(t_ord_orders)·`TOrdArtworks` 가 webadmin 모델 | 재배정→서희항 |
| STD-ADO-003 | `raw/webadmin/webadmin/catalog/models.py:1085` | `ord_sts`(주문상태) 컬럼이 webadmin 쪽 | 재배정→서희항 |
| STD-MFG-001 | `raw/webadmin/webadmin/config/urls.py:268` | `api/w/v1/order/register` 수신 엔드포인트 실재(주석 「쇼핑몰이 주문 생성 직후 부른다」) | 담당맞음(서희항) |
| STD-ADP-015 | `huni-skin-shopby/src/app/(main)/product/[slug]/page.tsx:49-56` | 스킨은 `fetchPublishedDetailTabs` 로 발행된 탭을 읽고, 편집은 pagebuilder 쪽. **페이지빌더=김동학 확정**이라 한 사람 몫 | 담당맞음(김동학) |
| STD-MYP-003 | `huni-skin-shopby/src/components/product/configurator-actions.tsx:170-176` | 스킨 버튼에 `onClick` 없음(데드 버튼) | 분할 |
| STD-PAY-013 | `huni-skin-shopby/src/lib/api/widget-order.ts:26-35` | `amountToOrderCnt`(10원×수량)는 스킨 코드, 총액 권위는 위젯 handoff | 분할 |
| STD-PAY-031 | 같음 | 표시↔청구 정합은 스킨·위젯 두 쪽 합의 행 | 분할 |

**§5 밖의 직접 확인 1건**: `STD-ADC-014` — `raw/webadmin/webadmin/config/urls.py:73-74`·`:81-83` 와 `snap_20260827_1422/t_prd_guide_files.csv`(504행/88상품 재집계)를 열어 「도구 있음·내용 공란」을 확인했다(§0-B 2). `judged_by` 는 `merged-owner_side` 로 두었다 — 판정을 민 것은 merged 원장이고, 직접 확인은 분할 근거를 보강한 것이다.

## 6. t58(③ 역방향 확인) 인계 메모 — 이관 전에 이미 실측한 것

`merged.csv` 747행의 `system=widget` 은 **133행**이고 그중 **완료 128 · 진행 2 · 미확인 3**이다.
→ 「위젯 남은 일 5행」은 **merged.csv 화면 원장 기준으로는 정확**하다(진행 2 = `t49:259` 게시 위젯 기본값 미지정 점검·`t49:316` 게시 위젯 가격 전수 대조 / 미확인 3 = `t49:215` 신규몰 도메인 허용 사이트 등록·`t49:318`·`t49:319` 미리보기 업로드 presign). 그 밖에 `t50:99`(edicus→widget project_id 저장, 미확인)가 위젯에 닿는다.
다만 이 수치는 **「위젯 화면·SDK 가 이미 다 만들어져 있다」**는 뜻이지 위젯 축 오픈 작업이 5건이라는 뜻이 아니다 — 위젯이 쓰는 데이터·가격·파이프라인 잔업은 plan-rows 쪽(서희항 T2 41행 남은 일 · 최숙진 T2 데이터 충전 · T4 80행)에 있다. t58 이 저장소 실독으로 확정할 것.

## 7. 한계 (그대로 읽을 것)

1. **약한 근거 184행(25%)** — `rule-step`·`rule-track` 은 단계 기본 담당표로만 판정했다. 신우진·김동학 재배정에 특히 몰려 있다. 실측 후 확정해야 한다.
2. **evidence 를 실제로 열어 확인한 행은 9행**이다 — `judged_by=manual-read` 8행(§5) + `STD-ADC-014`(§0-B 2 · urls.py 와 라이브 스냅샷 재집계). 나머지 726행은 원장(t51 `owner_side`)과 evidence 문자열을 근거로 했다. 카드가 요구한 「evidence 를 열어 판정」을 전 행에 적용하지 못했다.
3. ~~pagebuilder 담당 미확정~~ → **해소(260919)**. 지니 결정으로 페이지빌더 = 김동학(쇼핑개발). §0-A 1 참조.
4. **작업량·기간 숫자는 산출하지 않았다.** 카드 금지 조항대로 행수만 셌다. 행수는 일의 크기가 아니다.
5. `07_rebaseline/S/CARDS-S.md` 는 이 브랜치에 없고 메인 체크아웃(미트래킹)에만 있어 거기서 읽었다(그 문서의 §0-B 트랙표·§1 28구간 기본 담당표). 브랜치 간 내용이 다를 수 있다.

## 8. 검산 (실행한 명령과 출력)

```
$ python3 verify.py
PASS V1 입력 plan-rows 735행 실측 735
PASS V2 입력 merged 747행 실측 747
PASS V3 출력 행수 = 입력 행수 735 vs 735
PASS V4 row_id 집합 동일
PASS V5 판정값이 5분류(+불필요) 안에만
PASS V6 근거등급이 정의된 5종 안에만
PASS V7 판정 미기입 0건
PASS V8 owner_now 가 원장 owner_name 과 전건 일치
PASS V9 status 가 원장과 전건 일치
PASS V10 remaining 은 status 로만 파생
PASS V11 재배정·분할 행에 근거 문자열 존재
실패 0
```

DB write 0 · 라이브 COMMIT 0 · 외부 저장소 쓰기 0 · 읽기 전용 탐색만 수행.
