# Huni-Shopby-Commerce — CHANGELOG

라이브 DB 상품/가격 → Shopby(NHN Commerce) 장바구니→주문 종단 통합 설계 하네스. (CLAUDE.md §24)

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-29 | 미지정 상품 배정 + **신규 제약 실측** — 사용자 요청(종이슬로건·지그재그엽서를 엽서/카드에 연결) 처리 중 **지그재그엽서의 기존 배정(접지카드)이 소실된 것을 발견**(3회 재조회 동일·`updateYmdt` 2026-07-29 00:21:11 갱신 → 읽기 오류 아닌 실제 해제). 원인=`엽서/카드`(1169134)는 자식 4개를 가진 **부모 노드**라 상품 직접 배정 불가인데 어드민에서 부모에 붙이려는 시도가 있었고, **부모 배정 시도가 기존 배정까지 해제**시킴 → 제약 #9로 문서화(제약 #1의 부작용). 사용자 결정에 따라 API로 재배정+검증: 지그재그엽서→`접지카드`(1272773, 라이브 분류와 일치)·종이슬로건→`엽서`(1169135). PATCH 204 후 즉시 재조회 + 20초 후 재확인으로 **지속성까지 검증**. `category-proposal.tsv` 의 해당 2건을 애매/확실 → **확정**(사용자 결정 근거 기재)으로 갱신. 전수 재검증: 판매중 217·배정 **216**·미지정 1(엽서캘린더)·목표불일치 0. | 13_product-load | 사용자 요청: 종이슬로건·지그재그엽서를 엽서/카드에 연결 |
| 2026-07-29 | 표기 통일 완료 — 사용자가 Shopby에서 `하드커버`→`하드커버책자`(1168124)·`데스크 소품`→`데스크소품`(1168165) 개명(`폰케이스&액세서리`는 원래 동명이라 변경 없음). 전후 트리 diff로 **구조 무변경 확인**(56경로·70노드 유지, 이름만 2건 변경) — 삭제/재생성이 아니므로 상품 연결 손실 없음. `build-mapping.py` 재생성 → 자동 매칭 210→**211건**, `--reconcile` **0건**(개명은 번호 불변). 전수 재검증: 판매중 217·배정 215·미지정 2·목표불일치 0. **표기 통일 트랙 종료** — 라이브 2레벨 이름이 Shopby와 전부 대응(자동 매칭 181→192→203→210→211로 수렴). 잔여 미매칭 6건은 표기 문제가 아니라 ①라이브에서 1레벨에만 귀속(레더링바인더·맥세이프 스마트톡·종이슬로건) ②라이브에 카테고리 자체가 없음(하드커버책자·레더하드커버책자·엽서캘린더) — 라이브 DB 분류 작업 영역. | 13_product-load | 사용자: 하드커버책자·데스크소품·폰케이스&액세서리 완료 |
| 2026-07-29 | 카테고리 매핑 정밀화 + 선행 보고 오류 정정 — 사용자 지적("데스크소품은 없고 데코소품은 있는 듯")을 계기로 부모 노드까지 포함해 재대조: **Shopby에도 `데스크 소품`(1168165)·`폰케이스&액세서리`(1168234)가 라이브와 동명으로 존재**하며 각각 자식 1개를 가진 부모라 말단 집합에서 빠졌던 것 — 직전 보고의 "Shopby에만 데스크/사무용품·폰 스트랩"은 **말단만 비교한 데서 온 착시**였고 개명이 불필요했음(보고 정정). 참고로 `데스크소품`(문구 하위)과 `데코소품`(라이프 하위)은 서로 다른 갈래의 별개 카테고리로 양쪽 모두 존재. **단일자식 부모 폴백** 규칙을 `build-mapping.py` 에 추가(라이브명이 Shopby 부모와 일치 + 자식 정확히 1개 → 그 유일 자식에 배정, 모호성 없음) → 자동 매칭 **203→210건**. **★신규 실측 제약**: `PATCH /products/{no}` 가 **200/204 를 반환하고도 반영되지 않는 사례 관측**(폰 스트랩 2건이 assign-log 상 OK 였으나 실제 displayCategories 빈 배열; 재시도 시 204로 정상 반영) → `--reconcile` 에 **PATCH 직후 GET 재조회 검증** 추가(미반영 시 ⚠️ 표기·재실행으로 자동 재시도). 전수 재검증: 판매중 217·배정 215·미지정 2(종이슬로건·엽서캘린더)·**목표불일치 0**. 표기 통일 잔여는 `하드커버책자`↔`하드커버` 1쌍(3건)뿐. | 13_product-load | 사용자 지적: 데스크소품/데코소품 확인 |
| 2026-07-28 | 표기 통일 2차 — 사용자가 `페브릭`→`패브릭`(Shopby 에코백/파우치/포스터 3개) · `전단지/리플랫`→`전단지/리플렛`(라이브 CAT_000058) 수정. 양쪽 재조회로 통일 확인. `build-mapping.py` 재생성 → **자동 매칭 192→203건**(+11). `--reconcile` 결과 **재배정 0건** — 개명은 카테고리 번호를 바꾸지 않으므로(1168282·1143015 불변) 상품은 이미 올바른 위치에 있었고, 실질 이득은 향후 매핑 자동화(수작업 제안표 불필요). 최종: 판매중 217·배정 215·미지정 2 유지. 잔여 표기차 3쌍(데스크소품↔데스크/사무용품·하드커버책자↔하드커버·폰케이스&액세서리↔폰 스트랩) 통일 시 미매칭 14→2건 예상. | 13_product-load | 사용자: 표기 통일 수정완료 |
| 2026-07-28 | 카테고리 구조 정비 — 라이브↔Shopby 트리 전수 대조: **1레벨 12개 이름 완전 일치**, 2레벨 완전일치 27·띄어쓰기차 20·실질차 라이브9/Shopby6, 라이브 3레벨 3개는 상품 0건 빈 껍데기, 1레벨 직접귀속 상품 중 순수 1레벨만은 3건뿐(Shopby 제약 영향 미미) → **라이브를 SOT로 Shopby를 맞추는** 방향 확정. **전시카테고리 생성/개명 API 부재 확정**(전 스펙 전수 검색 — `get-display-categories`·`get-display-categories-tree` 등 조회만 존재) → 어드민 수동 작업 필요. 사용자가 어드민에서 `엽서/카드` 하위 말단 3개 추가(`접지카드`1272773·`엽서북`1272776·`포토카드`1272781) + `타이백 파우치`→`타이벡 파우치` 개명(1168256 번호 불변) 수행. 이후 `build-mapping.py` 재생성(매칭 181→**192**) + 신설 `assign-categories.py --reconcile`(현재 배정↔목표 대조 후 차이만 PATCH)로 **6건 재배정 성공**(엽서북·접지카드3·포토카드2). 최종: 판매중 217·중복 0·카테고리 배정 **215**·미지정 2(종이슬로건·엽서캘린더). 잔여 표기차(페브릭/패브릭·리플랫/리플렛·데스크소품·하드커버책자·폰케이스&액세서리) 통일 시 미매칭 25건 추가 자동해소 가능. | 13_product-load | 사용자 요청: 라이브·Shopby 카테고리 구조 대조 후 정리 방향 확정 → 엽서/카드 하위 추가·표기 통일 |
| 2026-07-28 | ★상품 카탈로그 적재 — 라이브 실사용 상품 **217건(완제품 202+기성 15)** Shopby 등록 완료·카테고리 **214건 배정**(미지정 3=분류 판단 보류). 조인키=`productManagementCd`==`prd_cd`. 본보기=프리미엄엽서(132773412) 형태 복제(SINGLE·DEFAULT·salePrice 100·옵션1행 addPrice 0·deliveryTemplate 1001962), 16필드 전량 일치 검증. **실측 Shopby 제약 7건 확정**: ①말단 카테고리에만 등록 가능(부모 배정 시 INVALID_DISPLAY_CATEGORY) ②`/products/search`가 pageSize 무시·페이지당 20건(totalCount 기준 순회 필수) ③전시카테고리 응답 depth5에 말단번호 중복 반복(부모집합 계산 시 dedupe 필수) ④`immediateDiscountUnitType` 등록 필수 ⑤상품 하드삭제 API 부재(STOP+display:false가 최선) ⑥사후 수정은 `PATCH /products/{no}`(displayCategoryNos·mallProductImages 지원) ⑦이미지는 productImages[]로 등록 가능(mainYn=대표·외부 URL 가능)이나 **라이브 DB에 이미지 소스 부재**(vc_ 테이블 image_url 0건). **사고·수습**: ②를 놓쳐 배치 재실행 시 중복 71건 생성 → 전수 재조회로 적발 → 원본(작은 번호) 보존·중복분만 판매중지(71/71 성공, 브리지 상품 132773412 보호 확인) → 페이지네이션 수정·재발방지 주석. 최종 검증: 판매중 217·고유코드 217·중복 0. 산출=`13_product-load/`(build-mapping·register·dedupe·assign-categories + 로그·제안표·README). | 13_product-load | 사용자 요청: 라이브DB 상품을 카테고리 맞춰 Shopby 등록·이미지 API 가능여부 확인 |
| 2026-07-28 | ★★H4 관문 해소 — **옵션 addPrice 쓰기 실동작 확인**. NHN 1:1 회신(server-api 인증 2방식: ① Authorization+systemKey ② systemKey+mallKey)을 근거로 5방식 읽기 프로브 실측 → **systemKey + `Authorization: Bearer <SHOPBY_SERVER_ACCESS_TOKEN>` 만 200**(`GET /categories`·`GET /products/132773412/options`), 나머지 4방식 전부 400/401. **직전 가설(앱 #2096·#2087 심사 미승인이 원인) 반증** — 원인은 인증 헤더 누락이었고 `.env.local` 기존 토큰으로 즉시 통과. `mallNo` 쿼리 부착이 오히려 `APEC0001` 원인이었음(우리 추측 우회였음) → 제거. `mallKey=huniprinting48`(몰 이름)은 오답(`A0002`) — 앱 설치 시 발급되는 별도 키로 추정하나 D방식이면 불필요. **쓰기 실측(계약 §4 절차: 백업→PUT→검증→복구)**: 1차 시도는 `NCPE0002`("options에 DEFAULT 값 설정 불가")로 무손상 거부 — GET의 `type:"DEFAULT"`를 PUT에 그대로 되돌린 우리 실수(PUT 입력에서 DEFAULT=옵션 미사용 금지값) → `COMBINATION` 교정 후 **PUT 200 · 견적가행(optionNo=533996163·addPrice=1000) 주입 확인 · 복구 PUT 200 · 원상복구 확인**. 첫 옵션 `addPrice=0` 불변식 준수(견적가는 order=2 별행). **★신규 실측: optionNo 재발급 조건** — 1차에서 번호가 전량 갱신(454659462→533996162)된 것은 `DEFAULT→COMBINATION` **유형 전환 1회성** 현상이며, 유형 동일 상태의 멱등 재-PUT에서는 **번호 유지 확인**(533996165 불변) → `mallOptionNo` 지정 시 제자리 수정 의미론 성립 = **기존 카트 라인 optionNo 무효화 위험 없음 → A안 구조 유효**. 부수효과: 132773412 옵션 유형이 `DEFAULT→COMBINATION`으로 잔존(API로 DEFAULT 환원 불가·테스트 상품·인간 승인 후 진행). server.mjs 인증 교정(mallNo 쿼리 제거·Authorization 추가·토큰 마스킹) + 부팅 검증(HTTP 200). **미해결(설계 리스크)**: NHN이 "고객 입력 기반 동적 가격 산정은 미지원·커스텀 불가, 옵션 사전 전수등록 필요"라고 명시 → 기술적 가능≠정책적 승인. NHN 역질문 2건(견적엔진 정의·구매시점 가격변동 사유) 답신이 다음 관문. | 11_simulator/server.mjs·CHANGELOG·HANDOFF | NHN 1:1 회신 수령 → 인증 실측 → 쓰기 실측(각 단계 인간 승인) |
| 2026-07-16 | Track H 실행 검증 — IP 등록(112.221.164.197) 완료로 server-api IP 관문 통과. 백업 읽기를 shop-api로 우회(server-api GET은 mallNo doc-divergent), 백업 동작 확인(옵션1·addPrice0). H4 쓰기(PUT /products/options)는 `APEC0001 필드명:mallNo`로 계속 거부 — storefront mallNo(81683)≠앱스코프 mallNo 추정 + 셀러어드민 앱 2종(#2096·#2087) 심사대기·판매불가 상태 = server-api 카탈로그 쓰기 권한 미승인. **PUT 전량 400(쓰기 전 거부)로 카탈로그 무손상 확인·쓰기 재잠금**. H4 잔여 관문=앱 심사 승인+정확한 앱 mallNo. mallNo 자동조회(shop-api /malls) 로직 추가. | 11_simulator·README | 사용자: IP 등록함→실행 검증 |
| 2026-07-16 | ★시뮬레이터 Track H 확장 — 후니 가격시뮬레이터→Shopby 브리지. 사전 분석(dynamic workflow 4에이전트→12_sim-bridge/01~04): 후니 webadmin 위젯 런타임 API 5종 라이브 실증(widgets·price·validate·handoff·verify 5/5 200)·put-product-options 쓰기 계약·시뮬레이터 갭·종단 설계. 구현: server.mjs에 huniCall(읽기·site_key 서버주입·마스킹)+shopbyServerCall(systemKey·쓰기 2중가드=arming AND 화이트리스트)+라우트 8종(huni-widgets/validate/price/handoff/verify + sync-backup/option/verify/inspection-check/restore). UI에 Track H(H1~H4)+무손실 3차 배지+옵션 원상복구 버튼. **H1~H3 실동작 검증 완료**(브라우저: final_price 14,283 서버계산·HMAC 왕복 valid=true·후니 디자인 적용). **H4(옵션 쓰기)는 코드 완성했으나 Shopby server-api IP 허용목록에 현재 IP(112.221.164.197) 미등록으로 실행 대기** — 등록 위치=워크스페이스>셀러(service admin 페이지엔 IP 필드 미노출·프로덕션 보안설정이라 미강행). 상품 불일치 직시: 게시 위젯=프리미엄엽서 1건, 쓰기 대상도 프리미엄엽서(132773412)로 정합. | 11_simulator·12_sim-bridge | 사용자 요청: 가격시뮬레이터 선택옵션·가격을 카트로 넘겨 주문까지 확인 |
| 2026-07-16 | ★실API 인터랙티브 시뮬레이터 구축 — `11_simulator/`(server.mjs + 해와바다 DS UI). 로그인→상품→옵션·견적→원고(참조 문자열)→카트(담기/조회/calculate/validate)→주문서(생성/조회/calculate)를 버튼으로 실호출, 우측 "API 상세뷰"에 operationId·요청/응답(마스킹)·DB 기록 주석 표시. 결제/클레임은 서버 코드 BLOCKED 정규식 원천 차단·정리 버튼(카트 삭제→count:0). 카테고리 매칭 검증 화면: Shopby 67 ↔ 라이브 72 실조인(EXACT 37·CODE_ONLY 24=공백차이·NONE 6). E2E 스모크+headless 브라우저 검증 완료(95,000원 서버 확정 일치). 디자인=후니프린팅 DS(claude.ai/design cbd48dae · 퍼플 #5538B6·Noto Sans KR·타이트자간, 오연결이었던 해와바다 ec7226cc에서 교체). | 11_simulator/ | 사용자 요청: 전 과정 인터랙션 시뮬레이터+실UI/UX+API 상세뷰 |
| 2026-07-16 | ★2R 실행 완료(dynamic workflow·7 에이전트) — **게이트 GO-CONDITIONAL**(FAIL 0). 실API 실증 13호출 전부 200(로그인→카트→calculate→validate→주문서, 결제 미호출·카트 정리 count=0). 핵심 확증: ① 가격=카트 라인에 단가 필드 없음·`buyAmt=salePrice+addPrice` 서버 산출(95000 실측)→final_price는 옵션 addPrice 등록이 유일 경로 ② 원고=파일 첨부 자리 0건→`optionInputs[].inputValue` 참조 문자열 ③ 카테고리=Shopby `managementCode`==라이브 `cat_cd` 항등 조인 확증(I-CAT-1 해소). codex xhigh 13건→수렴 9. 조건=문서 shape 교정 7건(F-01/04/07/08/09/11/13, architect 라우팅)+인간 승인 큐 7건(I-PRICE-1 복수옵션 별행 심사=런칭 관문 등). 산출=06_scope~10_gate2 15+파일. | 06~10 전 디렉토리 | 사용자 요청 2R 실행(ultracode·codex xhigh) |
| 2026-07-16 | 2R 범위 확장 — ① 실API 실증 트랙(2R-P1b): `.env.local SHOPBY_*` 자격증명으로 테스트 회원 로그인→카트→주문서 생성까지 실호출·필드 대조(결제 금지·잔여물 정리·비밀값 비노출), Shopby 어드민 등록 카테고리 실조회 인벤토리. ② 2R-P3에 카테고리 매칭 명세(Shopby 어드민 카테고리↔라이브DB 상품) 추가. 안전규칙 "장바구니 submit 금지"→"결제 직전까지 허용" 예외 갱신. | 오케스트레이터·rules | 사용자 지시: 문서-only 검증 금지·실제 API 확인, 카테고리 매칭 고려 (호출범위=결제 직전까지 승인) |
| 2026-07-16 | ★2라운드 착수 — 하네스 확장: `hsb-artwork-flow-designer` 에이전트 + `hsb-domain-boundary-design`·`hsb-artwork-flow-design` 스킬 신설, 오케스트레이터에 2R Phase(P0 인벤토리→P1 커머스후반∥원고→P2 경계→P3 명세→P4 codex xhigh→P5 게이트) 추가. 범위=분석/설계 전용(구현 없음): 인쇄 영역↔쇼핑몰 영역 소유권 분할, 위젯 옵션→원고(업로드/Edicus)→견적→카트→주문→결제→배송 종단, Shopby 전달값 명세(필드 단위)+Shopby 필요 DB 정보 명세(양방향). 적대검증=codex-cli xhigh. 산출 디렉토리 06_scope~10_gate2 신설. | 오케스트레이터·에이전트·스킬·rules | 사용자 요청: 전체 산출물 확인+인쇄/쇼핑몰 영역 분리+주문→배송 설계(ultracode) |
| 2026-06-25 | ★재게이트 R2 — **GO (CONDITIONAL)**. 직전 NO-GO(R1)의 8 FAIL 드라이버(X03 salePrice=0 불변식·X04 put-product-options full shape·X05 server API systemKey 인증분리·X07 recurringPaymentDelivery·X08 addressRequest·X09 reserve 필수9·X10 guest-token POST+password·X11 클레임 추적)가 설계 본문에 전부 보정 반영됨을 게이트가 스펙 라인 직접 재대조해 verified(반증 0). SB1~SB7 단일 FAIL 0·신규 결함 0. 라이브 토대 재실측 104(공식78∪직접단가26)·골든 PRD_000016(공식 PRF_DGP_A·옵션그룹8)·라인 가격필드 0건 드리프트 0. CONDITIONAL 사유=I-PRICE-1/2/3 BLOCKED(상품심사·정산권위·동시성/클레임추적, 실 구현 전 인간 승인·갭필 선결). | 05_gate/gate-verdict·e2e-golden-trace·remediation-spec | 재게이트(직전 NO-GO 항목 해소 확인): architect 보정 루프 후 8 교정 CLOSED |
| 2026-06-25 | ★선행 토대 큐레이터 추가 — `hsb-foundation-curator` + `hsb-foundation` 스킬. Phase 1에 후니 측 선행 토대(① webadmin 상품+구성요소 선택→evaluate_price→final_price 가격계산 흐름 ② 라이브 DB 적재 현황·결함/갭)를 기존 §13/§14/§21/§7 산출 재사용으로 못박는 단계 신설. 브리지를 토대+커머스 종합 단계로 재배선(5→6 에이전트). 워크스페이스 `00_foundation/` 추가. | hsb-foundation-curator·orchestrator·CLAUDE.md §24 | 사용자 피드백: webadmin 상품+구성요소 선택→가격계산·라이브 DB 적재 현황이 Shopby 브리지의 선행 리서치여야 함 |
| 2026-06-25 | 하네스 초기 구성 — 5 에이전트(hsb-commerce-researcher·hsb-product-bridge-analyst·hsb-integration-architect·hsb-codex-verifier·hsb-integration-gate) + 6 스킬 + SB1~SB7 게이트. Shopby=커머스 백엔드 확정(`shopby-excluded-from-scope` 결정 갱신). 범위=리서치+통합설계(구현은 §6 위임)·문서 권위+라이브 갭필·브리지 전략은 하네스 권고. | 전체 | 사용자 요청: 라이브DB 상품/가격을 카트에 전달, 추후 위젯으로 주문 완료 흐름 구성 |

| 2026-09-15 | 프린트머니(선불 충전금) 분석·설계 — Shopby 예치금 부재 확인, 적립금 재해석+충전권 상품 방안 A 권고, Server API 매핑·실측 V1~V8·구현 M0~M6 | `17_prepaid-money/` (SPEC-HARVEST·design md/html, `docs/huni/prepaid-money-design-260915.html`) | 지니 요청(lead 세션) |
| 2026-09-16 | 프린트머니 리포트 피드백 반영 — 용어 외부포인트, 토스 가상계좌 직접 발급 확정(§14-0, 무통장+PayAction 은 대안 기록), 개발 주체 김동학→서희항·지니 + webadmin 분할 3자 논의(§16), 이관 잔액 별도 관리(§15) | `17_prepaid-money/` · `docs/huni/prepaid-money-design-260915.html` | 지니·김동학 피드백 |
