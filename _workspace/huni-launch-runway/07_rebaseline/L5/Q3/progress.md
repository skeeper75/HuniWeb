# Q3 진행 기록 — 개발 전달 문서 3건 · 무통장 운영 절차 · 생성기 필터

> lane `sync-huniweb` · 2026-09-02 · 카드 `L0/CARDS-Q.md` §Q3 · lead `lead-peta [a760e0]`
> [HARD] 코드 수정 0 · 커밋 0 · 라이브 쓰기 0 · 셀러어드민 접속 0회 · 주문/입금확인/폼제출 0

---

## 1. 산출물

| 파일 | 무엇 | 담당(수신자) |
|---|---|---|
| `DEV-REQUEST-01-huni-item.md` | `huni_token` → `huni_item` 계약 정합 | 쇼핑개발 |
| `DEV-REQUEST-02-order-register.md` | `POST /api/w/v1/order/register` 실호출 배선 | 쇼핑개발 |
| `DEV-REQUEST-03-webhook-consumer.md` | 샵바이 웹훅 소비자(방어형) | 인쇄개발 |
| `ops-bank-transfer.md` | 무통장입금 운영 절차 1장 | 실무진 |
| `../../L6/build_artifact.py` | 「무통장 선검증 가능」 비고 필터 1개 (5곳 수정) | lead |

### 관련 — 중복 작성하지 않은 것

price-setup 개발 4건은 **링크만** 둔다(카드 지시).
→ `_workspace/price-setup/STATUS-260901.md` **§5 개발 전달 4건**
① 셋트 제본비 미청구(`set_procs` 미전송) ② 고객이 고른 크기가 속 구성품에 안 내려감(`set_selections` 빈 값) ③ 품목사이즈 전용 판형 자동 미선택 ④ 550x375 판형 미배선 잔여.
**전부 주문화면·가격엔진 코드**라 이 3건과 수신자는 겹치나 사안이 다르다. 다시 쓰지 않았다.

---

## 2. 완료조건 대조표

| # | 완료조건 | 판정 | 근거 |
|---|---|---|---|
| ① | 전달 문서 3건 전부 **file:line 근거 · 영향 std_id** | **PASS** | 아래 §2-1 |
| ② | 운영 절차 근거 **전건 N1 문서 줄** | **PASS** | 아래 §2-2 |
| ③ | 생성기 재실행 **exit 0 · 회귀 0** | **PASS** | 아래 §2-3 |
| ④ | 역방향: **M2 I-4/I-5 서술과 다른 점 목록**(0이면 명시) | **PASS — 다른 점 3건** | 아래 §2-4 |

### 2-1. ① file:line 근거 · 영향 std_id

기계 집계(`grep -oE '[A-Za-z0-9_./-]+\.(py|ts|tsx|html|yml|md|mdx|txt):[0-9]+'` · `grep -oE 'STD-[A-Z]{3}-[0-9]{3}' | sort -u`):

| 문서 | file:line | 영향 std_id(고유) |
|---|---:|---:|
| DEV-REQUEST-01 | 14 | **6** |
| DEV-REQUEST-02 | 15 | **9** |
| DEV-REQUEST-03 | 8 ⁽¹⁾ | **7** |

⁽¹⁾ DEV-REQUEST-03 은 `shopby_hook.py` 한 파일 안을 `:73-78` 형태의 **줄만 표기**한 칸이 6개 더 있다(표 머리에 파일명이 이미 있어 반복하지 않았다). 실제 줄 근거 수는 14.

**형식 준수**: 3건 전부 「증상 · 재현 · 원인 file:line · 수정 방향 · 확인 방법 · 영향 std_id」 6절 + 「아직 확인 못 한 것」 1절.

**shopby_hook.py 줄번호는 이번에 원본 재확인해 교정했다**(§2-4 ③).

### 2-2. ② 운영 절차 근거

`ops-bank-transfer.md` 의 주장별 근거 — **전건이 N1 문서**이고, N1 이 인용한 매뉴얼 줄까지 함께 적었다.

| 절 | 주장 | N1 근거 |
|---|---|---|
| §1 | 무통장만 사람이 누른다 / 가상계좌·에스크로는 PG 자동 | `shopby-order-state-machine.md` **T4·T5** · `selleradmin-ops.md` **§1.1 ②** (`order/deposit-wait.mdx:11`, `:28-30`) |
| §2 ① | 입금대기 주문 메뉴 위치 | `selleradmin-ops.md` **§1.1 ②** |
| §2 ③ | 입금확인은 **입금대기 목록에서만** · 결제완료로만 | `shopby-order-state-machine.md` **T4 · B2** · `selleradmin-ops.md` **§1.2 「예외 둘」**(`order/list.mdx:76-78`) |
| §3 | 그 뒤 상태 버튼 금지 · 취소권 경계 | `selleradmin-ops.md` **§1.2 [HARD]**(`sdk:755-759`, `:895-898`, `:925-928`, `:1398-1400`) · `shopby-order-state-machine.md` **§취소 경계표**(`sdk:929-936`) |
| §4-1 | 입금기한 초과 자동취소 = `CANCEL_NO_REFUND` · 3~7일 권장 | `shopby-order-state-machine.md` **C1**(`claim-order/cancel/condition.mdx` 1행 · `order/deposit-wait.mdx` ②) · `sdk:937-940` |
| §4-1 미확인 | 입금기한 기본값·설정 위치 | `shopby-order-state-machine.md` **§미확인 9** |
| §4-2 | 입금대기부터 재고 차감 · 금액÷10 이 재고를 먹는다 | `shopby-order-state-machine.md` **§4-2**(`order/deposit-wait.mdx` §Callout) · **`:274`** |
| §4-3 | 알림·영수증·통계 수량 노출 전수표 | `headless-boundary.md` **§A-2-0**(`sdk:949-951`,`:1035`,`:1044-1048`) · **§A-2-1**(`sdk:1412-1415`) · 알림 항목표(`sdk:1419`) |

**근거 없는 문장 0.** 「미확인」으로 남긴 것 1건(입금기한 기본값) — 추정으로 메우지 않았다.

### 2-3. ③ 생성기 exit 0 · 회귀 0

**수정 5곳**(`L6/build_artifact.py`) — 「필터 1개」를 위한 최소 배선:

| # | 자리 | 무엇 |
|---|---|---|
| 1 | 원장 row dict | `"note": g(r, "비고", 300)` 추가 |
| 2 | `const F` | `bank: false` 추가 |
| 3 | HTML 칩 바 | `<button id="f-bank">무통장 선검증 가능</button>` 추가 |
| 4 | 토글 배선 | `['money','order','unchecked']` → `+ 'bank'` |
| 5 | `filtered()` | `if (F.bank && !(r.note \|\| '').includes('무통장 선검증 가능')) return false;` |

**실행 · 검산**

```
L=L5/P1/unified-ledger-v2.csv
python3 L6/build_artifact.py --ledger $L --out <scratch>/new.html   → exit 0
```

생성기 자체 검산 전건 통과 — 검산 ②(원장 504 = HTML ledger 504) · L6b(block 키 504/504 · 분포 일치 `{작업 항목 437 · 차단(외부) 8 · 오픈 무관 50 · 미판정 9}`) · ⑥(원장 밖 기능 문자열 0 · 날것 `<br>` 0) · CDN 화이트리스트 3건.

**회귀 0 — 수정 전/후 산출 HTML 기계 비교**

| 항목 | 결과 |
|---|---|
| `DATA` 최상위 키 집합 | **동일** |
| 원장 행 수 | 504 → **504** |
| 원장 항목 키 | **`note` 1개만 추가** · 제거 0 |
| 기존 키 값 전건 | **504행 전부 동일** |
| `gates`·`scen`·`xp`·`blockers`·`edgeKinds`·`criticalPath`·`close` | **전부 동일** |
| `DATA` 줄 제외 본문 diff | **4곳** — 추가한 칩 버튼 1 · `F` 1 · 토글 배선 1 · `filtered()` 1 (+ 생성 시각 1줄) |

**현재 매칭 0건이 정상이다** — v2 원장에는 「무통장 선검증 가능」 비고가 아직 없다. Q1 의 v3 원장이 그 비고를 채우면 그때부터 걸린다. 필터는 **빈 결과로 조용히 동작**한다(예외 없음).

**쓰기 범위 준수**: `--out` 을 스크래치로 돌려 `L6/runway-dashboard.html`·`runway-data.json` 은 **건드리지 않았다.** 생성기가 `REBASE/LEDGER.md` 를 무조건 덮어쓰므로 실행 전 백업 → 실행 후 **원본 복원**(diff 동일 확인). 커밋 0.

### 2-4. ④ 역방향 — M2 I-4/I-5 서술과 다른 점

**서술이 뒤집힌 것은 0건이다.** I-4·I-5 의 사실 판정(호출부 0건 · 소비자 0 · 계약 내용)은 이번에 재확인해 **전건 그대로 성립**했다.

다만 **M2 를 그대로 옮기지 않고 달라진 것 3건**을 적는다.

| # | 다른 점 | M2 서술 | 이번 확인 | 성격 |
|---|---|---|---|---|
| ① | **호출측 원장 행이 없다** | I-4 는 「자사몰 몫의 필수 호출」이라고만 적음 | `L5/P1/unified-ledger-v2.csv` **504행 전수**에서 `order/register`·`S2S` 언급 행은 `STD-MFG-001`(인쇄개발·**수신측**)·`STD-ORD-029`(재견적) **2건뿐**. **호출측(쇼핑개발) 행이 없다** | **원장 갭 — 신규 발견.** 「10/6 쇼핑개발 174건」에 오픈 최대 블로커가 안 세어져 있다 → Q1 반영 대상 |
| ② | **`huni_item` 이 두 요청의 공통 키다** | I-4 와 I-2(=Q0)를 별개 접점으로 서술 | `item_id` 경로를 고르면 **서버키 발급이 두 요청의 공통 선행조건**이 된다(`widget_api.py:4210-4223`). DEV-REQUEST-01 §4-2 ↔ 02 §4-5 를 상호 참조로 묶었다 | 통합 — 새 사실 아님 |
| ③ | **`shopby_hook.py` 줄번호 6곳 오기** | I-5 근거: 상태값 4종 `:44-47` · 응답 규약 `:85-96` · 저장 본문 `:110-133` · 시크릿 `:77-82` | 원본 재확인 — 상태값 4종 **`:43-46`** · `405` **`:93`** · `404`(`raise Http404`) **`:95`** · `_secret_ok` **`:73-78`** · `create` **`:124`** · `500` **`:125-129`** · `_unparsed` **`:108`** · `_truncated` **`:110`** | **줄번호 교정**(대개 ±1~2). 서술 내용은 전부 맞다 |

> ③ 은 **개발자가 그 줄을 열었을 때 다른 게 나오면 문서 신뢰가 깎이는** 종류라 교정했다. M2 원본은 고치지 않았다(쓰기 범위 밖) — lead 판단 대상.

---

## 3. 이번에 기계로 재확인한 사실 (전달 문서의 토대)

| 확인 | 명령 | 결과 |
|---|---|---|
| 자사몰이 등록된 라벨을 안 쓴다 | `grep -rn "huni_item" src` | **0건** |
| 자사몰이 없는 라벨을 쓴다 | `grep -rn "huni_token" src` | **8건** |
| 주문등록 호출부 부재 | `grep -rn "order/register" src` | **0건** |
| 서버키 헤더 부재 | `grep -rn "X-Huni-Server-Key" src` | **0건** |
| 자사몰에 웹훅 없음 | `grep -rn "webhook" src` | **0건** |
| 웹훅 소비자 부재 | `grep -rn "TOrdWebhooks" raw/webadmin/webadmin/` | **2건**(정의 `models.py:1123` · 생성 `shopby_hook.py:124`) — **읽는 곳 0** |

코드 4곳은 원문을 직접 열어 인용문까지 대조했다 — `huni-widget.tsx:69-71` · `requote.ts:72-75` · `cart-page.tsx:118-119`·`:172-175` · `widget-order.ts:106-111` · `route.ts:42-48`.

---

## 4. 넘기는 판단 (lead 결정 대상)

| # | 판단 | 왜 내가 못 정하나 |
|---|---|---|
| 1 | **원장에 「자사몰 order/register 호출」 행 신설** (§2-4 ①) | 원장은 Q1 소유. 제안 문구는 `DEV-REQUEST-02` §6 ★ 에 적어 뒀다 |
| 2 | **`token` 경로 vs `item_id` 경로** 선택 | 서버키 발급 선행 여부가 갈린다. 쇼핑개발+PM 결정 |
| 3 | **서버키가 이미 발급됐는지** 확인 | 발급 전에 헤더를 실으면 결제 경로 3개가 한꺼번에 `403` 으로 막힌다 — 확인 없이 착수하면 위험 |
| 4 | **M2 `interface-6.md` I-5 근거 줄번호 교정** (§2-4 ③) | M2 는 쓰기 범위 밖 |
| 5 | **무통장 입금기한 현재 설정값** 확인 | 30일보다 길면 원고가 먼저 사라진다. 셀러어드민 화면 필요(레인은 접속 금지) |

---

## 5. 하지 않은 것 (범위 준수)

- 코드 수정 **0** — 전달 문서만 썼다
- 커밋 **0** · 라이브 쓰기 **0** · 셀러어드민 접속 **0회** · 주문/입금확인/폼제출 **0**
- `L6/runway-dashboard.html`·`runway-data.json` **미변경**(스크래치로 빌드)
- `LEDGER.md` **원본 복원 확인**
- price-setup 개발 4건 **재작성 안 함**(링크만)
- M2·N1·N3 원본 **미수정**

---

## 완료

완료조건 ①~④ 전건 PASS. 카드 §Q3 할 일 1~3 종료.
