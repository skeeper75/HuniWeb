# L1 progress — 표준 기능목록 신규 수립

- 카드: **t28 (L1)** · 분류 C(설계) · 선행 없음
- 세션: `plan-huniweb` · 작성 2026-09-02
- 워크트리: **미사용**(lead 지시). primary checkout `_workspace/huni-launch-runway/07_rebaseline/L1/` 안에만 기록.
- 커밋·푸시: **하지 않음**(lead 소관). DB write 0 · 코드 수정 0 · 읽기전용 조사만 수행.

## 산출물

| 파일 | 내용 |
|---|---|
| `standard-feature-canon.csv` | 표준 기능목록 **353행** (열 7종: std_id·대분류·중분류·기능·인쇄특수여부·필수도·근거출처) |
| `README.md` | 분류 체계 근거 서술 (§2 16 대분류 축, §3 인쇄특수 판정기준, §4 필수도 기준, §5 근거 원본 7종 + 미참조 선언) |
| `recheck-20.txt` | 무작위 20행 근거 재대조 실행 로그 |

## 완료조건 충족 근거

### ① csv 행 ≥ 250 → **충족 (353행)**

```
$ python3 -c "import csv;print(len(list(csv.DictReader(open('standard-feature-canon.csv',encoding='utf-8')))))"
353
```

대분류별 분포: 옵션·견적 55 / 운영자·상품가격 33 / 원고·파일 28 / 장바구니·주문 27 / 운영자·주문운영 24 /
상품·카탈로그 22 / 시스템·플랫폼 22 / 클레임·CS 20 / 회원·인증 20 / 결제 18 / 배송 16 / 마이페이지 16 /
프로모션 14 / 운영자·회원CS 14 / B2B 12 / 정산·통계 12.

### ② 모든 행에 근거출처 채워짐(빈칸 0) → **충족**

```
근거출처 빈칸: 0
결측/열수이상 행: 0
중복 std_id: []
```

추가로 **인용된 377개 참조 전부를 기계 검증**했다 — 인용 파일 존재 여부 + `#앵커`의 실재(`[ID]` 문자열) 대조:

```
총 인용 참조: 377
존재하지 않는 파일: []
앵커 불일치: 0
```

### ③ 무작위 20행 근거 재대조 불일치 0 → **충족 (20/20 PASS)**

seed 20260902 표본 20행에 대해 인용 파일·앵커·절 헤더를 원문에서 재확인. 전체 로그는 `recheck-20.txt`.

```
일치 20 /20 · 불일치 0
```

### ④ `L1/README.md`에 분류 체계 근거 서술 → **충족**

README §2가 분류 축을 서술한다. 축은 임의 분할이 아니라 (a) **주문 1건이 지나가는 경로**
(카탈로그→옵션·견적→원고·파일→주문→결제→배송→클레임)와 (b) 그것을 떠받치는 **고객 계정 축·운영자 축**을 따랐고,
각 대분류마다 "왜 독립 축인가"를 근거 파일과 함께 적었다.

## 카드 [HARD] 준수 확인

| 금지 사항 | 준수 |
|---|---|
| 260616 IA 엑셀을 분모로 참조 | **미참조** — `docs/huni/*.xlsx` 를 이 카드에서 한 번도 열지 않았다 |
| launch-scope 162기능을 분모로 참조 | **미참조** — `_workspace/huni-launch-scope/` 미열람 |
| runway 410항목 원장 참조 | **미참조** — `01_scope/ledger.json` 미열람 |
| 코드 수정 · DB write | **0건** — 읽기 전용 조사만 |
| 워크트리 사용 · 커밋 · `git add -A` | **미실행** |

## 정직한 잔여 위험 (lead 판단 필요)

1. **`print-quote/04_design/ia.md` 계보 문제.** 관리자 화면 기능(운영자·상품가격 / 운영자·주문운영 / 운영자·회원CS /
   정산·통계 영역 다수)은 경쟁사 라이브로 관측할 수 없어 이 문서를 보조 근거로 썼다. 이 문서는 후니 IA 계보(`huni-ia-master` 112건)
   이므로 **완전한 독립 수립은 아니다**. L4에서 `new` 판정 시, 근거출처가 `ia.md` 인 행은 「독립 발견」으로 과대평가하지 말 것.
   고객 경로 축(카탈로그·옵션·견적·원고·주문·결제·배송·클레임 = 206행)은 경쟁사 3사 실측 + 인쇄 도메인 KB + Shopby 계약이 주근거라
   이 오염에서 자유롭다.
2. **경쟁사 캡처 신선도.** `order-flow-steps.md` 캡처 일자는 2026-08-01. 한 달 경과분 변경은 반영되지 않았다.
3. **필수도(must/should/could)는 표준 기능목록 기준**이지 후니 10/6 오픈 범위 판정이 아니다. 오픈 범위는 L5 소관.
4. **행 353건은 "기능 단위" 판단이 들어간 수치**다. 예컨대 후가공 10종을 10행으로 폈고 결제수단 10종을 10행으로 폈다.
   L4 매핑 시 legacy 항목과 1:N으로 붙는 것이 정상이며, 행수 차이를 곧바로 누락으로 읽지 말 것.

## 상태

**완료.** lead 가 증거를 읽고 다음을 지시할 때까지 대기한다.
