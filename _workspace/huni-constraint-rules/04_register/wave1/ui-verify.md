# wave-1 UI 실화면 4항 검증 (129 폼보드 · 130 포맥스보드)

> §31 Phase 5 · 2026-07-02 14:25~14:28 gstack 캡처(registrar) + 오케스트레이터 증거 확인.
> registrar 에이전트가 캡처 완료 후 최종 문서 작성 직전 API 연결 오류로 중단 → 오케스트레이터가 스크린샷·라이브 재실측으로 판정 확정.

## 판정 요약: 양 상품 4항 전부 PASS

| 항 | PRD_000129 폼보드 | PRD_000130 포맥스보드 | 증거 |
|----|-------------------|----------------------|------|
| ① 역파싱(빌더로 열림) | PASS — 규칙이 폼빌더 편집 폼으로 열림(raw JSON 폴백 아님). 조건=자재 드롭다운·결과=사이즈 드롭다운 | PASS 동일 | `129-02-edit-builder-parsed.png`·`130-02-edit-builder-parsed.png` |
| ② 표시(읽히는 형태) | PASS — 목록에 규칙명("[제약조건데모] A3 폼보드(화이트) 5mm 는 A3 크기 전용")·유형(필수동반)·오류문구(쉬운 한국어+대안 안내)·표시순서·사용여부 표시, 4건 전부 | PASS 동일(4건) | `129-01-constraints-list.png`·`130-01-constraints-list.png` |
| ③ 조정 가능 | PASS — 결과 사이즈를 A3→A2로 UI에서 변경 가능 확인 후 저장 없이 이탈(수정 취소) | PASS 동일 | `129-03-adjust-A3-to-A2-unsaved.png`·`130-03-adjust-A3-to-A2-unsaved.png` |
| ④ 동작(validate 미리보기) | PASS — 막힘: A3 폼보드(화이트) 5mm + A2 크기 → "막힘 — 이 자재는 A3 크기 전용입니다. 크기를 A3로 선택해 주세요." / 통과: A3 자재 + A3 크기 | PASS 동일 | `129-04/05`·`130-04/05` validate 스크린샷 |

- 구 R_DEMO_MATSIZ: 등록 규칙 목록에 미표시(활성 4건만) — 논리삭제 반영 확인(목록 스크린샷).
- 저장/삭제 버튼 클릭 0(읽기 탐색 + validate Ajax만). 스크린샷 내 비밀값 노출 없음.
