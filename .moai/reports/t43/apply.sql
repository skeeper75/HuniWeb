-- t43 apply — PRD_000286 하드커버 링책자-내지 A4 사이즈 교정
-- ============================================================================
-- [HARD] 실행 금지. 리드 지시(지니 최종 확인) 전까지 이 파일을 psql 에 넣지 않는다.
--        DRY-RUN 은 dryrun.sql 이 따로 있다(스스로 ROLLBACK).
-- 근거: plate_authority_check.md §2(판형 게이트 통과) · §3(정본 판정) · §4(실화면 시뮬)
-- 효과: A4 판수 1 → 2. 100매 기준 73,073원 → 56,537원 (과다청구 16,536원 해소)
-- 되돌리기: rollback.sql
-- ============================================================================
--
-- ▸ 정본 코드 선택 (한 줄만 바꾸면 대안으로 전환된다)
--     SIZ_000252  A4세로 (work 213x303)  ← 권장. 형제 5상품(중철·무선·PUR·레더하드커버·포토북) 공통
--     SIZ_000258  A4     (work 210x297)  ← t43 카드 원문안. 가격 결과는 동일(56,537)
--   둘 다 판수 2 로 권위(판걸이수 r58 = 2판)와 일치한다. 갈리는 건 컨벤션뿐이다.
--
-- ▸ 왜 UPDATE 1 인가
--   PK 는 (prd_cd, siz_cd) 이고 t_prd_product_sizes 를 참조하는 FK 는 0건이다(실측).
--   PRD_000286 은 SIZ_000252 행을 아직 갖고 있지 않아 PK 충돌도 없다.
--   따라서 행을 지우고 새로 넣을 필요 없이 siz_cd 한 칼럼만 옮기면 되고,
--   disp_seq·dflt_yn·reg_dt 가 그대로 보존돼 rollback 이 완전 대칭이 된다.

BEGIN;

UPDATE t_prd_product_sizes
   SET siz_cd = 'SIZ_000252',
       upd_dt = now()
 WHERE prd_cd = 'PRD_000286'
   AND siz_cd = 'SIZ_000172'
   AND del_yn = 'N';
-- 기대: UPDATE 1

COMMIT;
