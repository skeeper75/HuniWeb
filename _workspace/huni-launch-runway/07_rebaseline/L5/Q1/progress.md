# Q1 진행 — 무통장 전제로 원장·게이트·가능성 v3

> lane `run-huniweb` · 2026-09-02(D-34) · 카드 `L0/CARDS-Q.md §Q1`
> 기계 검증 출력 전문: `L5/Q1/_verify-q1.txt`

## 산출물

| 파일 | 무엇 |
|---|---|
| `unified-ledger-v3.csv` · `.json` | 504행 · 16열(v2 동일) · 무통장 표기 + lead 결정 + partial 「안 되는 부분」 |
| `gates-v2.md` | G0~G5 · G1 이탈+1 · G2 ③④⑦ 재작성 · G3 이탈+1 · G4 PG 관통으로 확정 |
| `feasibility-verdict-v3.md` | 시나리오 D 판정 + A/B/C/D 병용표 + 게이트 판정 v3 |
| `build_v3.py` | 원장 v3 생성기(재실행 가능 · exit 0) |
| `_verify-q1.txt` | 위 표의 근거가 되는 기계 출력 |

## 완료조건 대조표

| # | 완료조건 | 판정 | 확인처 |
|---|---|---|---|
| ① | 차단 8행 전건 「무통장 선검증 가능/불가/미확인」 표기 | **충족** — 8/8 · 누락 0 (불가 6 · 가능(부분) 1 · 미확인 1) | `_verify-q1.txt` 「차단 8행 무통장 표기」 · 원장 v3 `비고` 열의 `무통장 선검증:` |
| ② | G1~G4 갱신 조건 전건 확인처 명시 · 금지어 0 | **충족** — `[확인처]` 26개 · 금지어 0 | `_verify-q1.txt` 「gates-v2 금지어」 · `gates-v2.md` §3~§6 |
| ③ | 시나리오 D 판정 + 조건 | **충족** — 「조건부 가능」 + 조건 D-1~D-5(그중 미확인 2 · 결정 필요 2 · PM 1) | `feasibility-verdict-v3.md` §3 |
| ④ | partial 85행 비고 빈칸 0 | **충족** — 85/85 에 「안 되는 부분:」 한 줄. **단 38행은 내용이 「미확인」** | `_verify-q1.txt` 「partial」 줄 |
| ⑤ | lead 결정 6건 반영 흔적(스크립트) | **5건 충족 · 1건 미반영(사유 명시)** | 아래 표 · `build_v3.py` 의 `LEAD_NOTES`·`EVIDENCE_FIX`·`X_TRACK_PENDING` |
| ⑥ | 역방향: v2 대비 뒤집은 것 목록 | **충족** | 아래 §역방향 |

### ⑤ lead 결정 6건 — 하나씩

| # | 결정 | 반영 | 어디에 |
|---|---|---|---|
| 1 | `STD-ORD-001`·`003` 비고 「구현 완료·미검증·계약 드리프트」 | ✅ 2행 | `build_v3.py` `LEAD_NOTES` · 원장 v3 해당 행 비고 |
| 2 | `STD-SYS-009` 동형 | ✅ 1행 | 같은 곳 (v2 의 「구현 완료·미검증」에 계약 드리프트 근거를 덧붙임) |
| 3 | 관리코드 중복 71건 「완료(라이브 실측 09-02)」 | ✅ 2행 | `X-SHOPBY-HYGIENE-01` 이 매핑된 `STD-ADP-013`·`STD-SYS-008` 비고 |
| 4 | `X-NHN-REPLY-01` 근거 `CHANGELOG.md:13` | ❌ **미반영** | **이 원장 504행에 `X-NHN-REPLY-01` 매핑 행이 0건**(기계 확인). 값은 `L3/legacy-normalized.json[323]`·`L3/id-preservation-map.csv:325` 에 있고, lead 가 말한 「최종 원장 합침」이 아직 없었다. `build_v3.py` `X_TRACK_PENDING` 에 정정문을 적어 두었고 재실행하면 그대로 출력된다 |
| 5 | `STD-MFG-099` 근거 `:1226`/`:1252` | ✅ 1행 | 근거 열의 `widget_api.py:3388` → `widget_api.py:1226,:1252` 치환 + 비고에 정정 기록 |
| 6 | partial 85행 「안 되는 부분」 1줄 | ✅ 85행 | 위 ④ |

## 역방향 — v2 대비 뒤집은 것

**원장**: **값을 뒤집은 행은 0이다.** `상태`·`오픈차단여부`·`담당` 변경 0행(기계 확인). 차단 8행은 무통장이 열려도 **여전히 차단(외부)** 이다 — 6행은 PG·구 DB 종속이 그대로고, `STD-FIN-008` 은 정산 권위 실증만 앞당겨질 뿐 「PG 정산 대사」 자체는 PG 승인 후에 닫힌다.
바뀐 것은 `비고` 95행 · `근거` 1행 · `done_criteria` 76행이며, `done_criteria` 76행은 v2 가 「남은 부분 미기재」라고 적어 둔 문장이 **더 이상 참이 아니게 된** 행만 고쳤다.

**게이트**(`gates-v2.md` §8 전문): 8건 — G1 이탈+1 · G2 ③④⑦ 재작성 · G3 이탈+1 및 표현 교체 · G4 ① 성격 확정 · G5 진입 경로 추가. **날짜는 하나도 안 바꿨다.**

**가능성**(`feasibility-verdict-v3.md` §6): 2건 — ① **G1 「가능」 → 「조건부」**(빈칸은 0 이지만 38행이 미확인) ② **시나리오 D 신설**(PG 승인 없이 여는 경로가 문서상 성립).

## 미확인 (정직 기록)

1. **무통장 개통 자체가 문서로 확인되지 않았다** — 근거는 「지니 09-02 16:4x 통보」 한 줄. 샵바이 서면 회신·설정 화면 캡처 0건.
2. **partial 38행의 「안 되는 부분」** — 근거·비고 어디에도 결손 표기가 없어 「미확인」으로 적었다. 만들어 쓰지 않았다.
3. **`STD-MYP-007`(프린팅머니 충전)이 무통장으로 검증 가능한지** — 충전 경로 코드가 no-op 스텁이라 관측 대상 자체가 없고, 무통장 충전에 대한 문서 근거 0건.
4. **웹훅 URL 이 샵바이에 등록돼 있는지**(A-3) · **`CHANGE_ORDER_STATUS` 상태값 목록**(B-1g) — G2 ④ 의 선행 2건.
5. **입금기한 기본값**(D-1) · **재고 자동 재충전 주기와 자동취소분 복구 여부**(D-2).
6. **연휴 공식 확인** — 특일 정보 API `530`, **4회째**. 재시도하지 않았다.

## 이 카드가 하지 않은 것 [HARD 기록]

- **라이브 쓰기 0건** — 주문·입금확인·설정 변경·폼 제출 0. **셀러어드민 접속 0회**(보류 중).
- **라이브 DB 조회 0건** — 이 카드는 파일만 읽었다.
- **커밋 0건.** 쓰기는 `L5/Q1/` 안에만 했다(파일 6개).
- **v2·v1 파일 무수정** — `L5/P1/`·`L5/O2/` 는 손대지 않았다.
- **시간 추정 0건.**

---

## 후속 반영 (lead 지시 09-02 17:0x · Q2·Q3 결과)

| # | 지시 | 반영 | 어디에 |
|---|---|---|---|
| 1 | A-13 실측(297건 전건 게스트 구매 가능) | ✅ | 원장 `STD-MEM-014`·`STD-ORD-007`·`STD-ORD-022` 비고 · `gates-v2.md` §0·G3-5 · `feasibility-verdict-v3.md` §2·§6 |
| 2 | A-5 실측(`viewShopSpecification=false` · `pgType=null` · `cashReceipt=false`) | ✅ | 원장 `STD-PAY-018`·`STD-SYS-004`·`STD-PAY-015`·`STD-MYP-015` 비고 · 같은 문서들 |
| 3 | 시나리오 재분류(무통장 23 / PG 필요 4)를 게이트에 분배 | ✅ | `gates-v2.md` G3 ① · G4 ① |
| 4 | 절차서 단계 8 정지 유력 → G2 ③④ 전제 | ✅ | `gates-v2.md` G2 ③④ · 「미실행」과 「미구현으로 확인됨」을 구분해 적게 했다 |
| 5 | 원장 갭 — `order/register` 호출측 행 신설 | ✅ **신설 `STD-ORD-030`** | 중복 판단 근거를 비고에 남겼고, `STD-ORD-028`·`029`·`STD-MFG-001` 에도 「호출측은 STD-ORD-030」이라고 적었다 |
| 6 | M2 `interface-6.md` I-5 줄번호 오기(:44-47→:43-46 등) | **해당 없음** | 기존 504행의 `근거` 열에서 `interface-6.md` 를 인용한 행이 **0건**이고, 인용하는 행은 이번에 신설한 `STD-ORD-030` **1건뿐**이다(기계 확인 · `_verify-q1.txt`). 그 1건은 **I-4 를 절 단위로만** 인용해 줄번호를 쓰지 않으므로 I-5 줄번호 교정의 대상이 아니다 |

### ★ 행수가 바뀌었다 — 504 → **505**

신설 1행(`STD-ORD-030`) 때문이다. **L6 생성기·대시보드·엑셀의 「504」는 재실행이 필요하다.**
상태 분포도 `todo 188 → 189`, 오픈차단여부 `작업 항목 437 → 438` 로 바뀐다.
`build_v3.py` 는 재실행할 때마다 이 문장을 출력한다.

### 상태값 이견 1건 (정직 기록)

신설 행의 상태를 lead 는 `todo`, `L5/Q3/DEV-REQUEST-02 §6` 은 `new` 로 제안했다.
**`todo` 를 택했다** — 주문 흐름 자체는 서 있고 이 호출만 빠진 상태라서다. 비고에 두 제안을 다 적어 두었다.

---

**완료** — 완료조건 ①~④·⑥ 충족 · ⑤ 는 6건 중 5건 반영(1건은 원장에 행이 없어 미반영·사유 명시). 라이브 쓰기 0 · 셀러어드민 접속 0 · 커밋 0.
