# §26 아크릴 가격격자 적재 무결성 진단 — 2026-06-27

권위=인쇄상품가격표 260527 "아크릴" 시트(`06_extract/price-acrylic-price-l1.csv`). 라이브=Railway `t_prc_component_prices`. 읽기전용 실측.

## 1. 판정: I-게이트 NO-GO (미적재 141셀)

권위 격자는 **꽉 찬 대칭 매트릭스**(sparse 아님):
| 자재 | 구성요소/자재 | 권위 격자 | 라이브 | 미적재 | 정합불일치 | 잉여 |
|---|---|---|---|---|---|---|
| 투명아크릴3T | COMP_ACRYL_CLEAR3T / MAT_000043 | 196 (14×14, 20~200²) | 113 | **83** | 0 | 0 |
| 투명아크릴1.5T | COMP_ACRYL_CLEAR3T / MAT_000042 | 81 (9×9, 20~100²) | 52 | **29** | 0 | 0 |
| 미러아크릴3T | COMP_ACRYL_MIRROR3T / NULL | 81 (9×9, 20~100²) | 52 | **29** | 0 | 0 |
| **합계** | | **358** | 217 | **141** | **0** | **0** |

## 2. 결함 본질
- **유형①(미적재 셀)만 발생** — 141셀. 정합불일치(유형③)·잉여 0 → **라이브에 있는 217셀은 전부 권위 verbatim 일치**(데이터 신뢰).
- 빠진 셀 = 주로 **대칭쌍 누락**(예: (60,20) 있는데 (20,60) 없음). 권위 면적가는 가로↔세로 대칭이나 라이브가 한쪽만 적재.
- **돈 영향:** 엔진 `match_component`는 (siz_width, siz_height) 정확매칭(ceiling tier 독립 선택) → 손님이 빈 셀 크기 선택 시 `no_tier_row` → 해당 구성요소 미합산 → **견적 0/결손**. 특히 **자유치수 상품(146 아크릴키링 등)**이 전 범위 입력 가능 → 선재 결함.

## 3. 교정 명세 (단계2 §7 교정적재)
- 141 미적재 셀 INSERT — 단가=권위 verbatim, min_qty(CLEAR3T=1·MIRROR3T=NULL) 기존 형태 보존, 신규 mint 0.
- SQL: `_foundation/remediation/acryl-grid-fill-{fix,dryrun,undo}.sql`. DRY-RUN 통과(3격자 196/81/81·대칭 완전·미적재 0).
- 적재 후 재실측 → I-게이트 GO 예상 → 단계4(157~162 면적공식 바인딩) 안전.

## 4. 검증 방법(생성≠검증)
권위 358셀을 CSV에서 파싱(`/tmp/acryl_diff.py`) → 라이브 217셀 덤프와 (comp,mat,w,h) 키 대조. 0 mismatch는 라이브 적재 정확성을, 141 missing은 적재 누락을 각각 입증.
