# t9 명세 — 셋트 책자 판형 잔여 교정 (2026-09-04)

카드 t9 의 ①②③ 은 이미 라이브에 적용됐다(`premise_recheck.md`).
이 문서는 **재실측으로 남은 잔여 5건**의 명세다. 전부 **지니 승인 후에만** 적용한다.
DB 직접 write 0 · 적용 경로는 webadmin UI 우선.

우선순위: R1(돈) > R2·R3(위생) > R4(위생) > R5·R6(권위 조회 필요 — 실무진 확인 전 적용 금지)

---

## R1 [High · 돈] SIZ_000641(480x320) 용지비가 316x467 복사본 — 4.09% 과소청구

### 근거

`siz641_vs_499.txt` — `COMP_PAPER` 단가행 7건 전건 대조:

| 자재 | 480x320 단가 | 316x467 단가 | 차이 | 면적비례 기대값 |
|---|---:|---:|---:|---:|
| MAT_000074 백색모조지 220g | 70.64 | 70.64 | **0.00** | 73.53 |
| MAT_000080 아트지 200g | 62.20 | 62.20 | **0.00** | 64.74 |
| MAT_000081 아트지 250g | 77.75 | 77.75 | **0.00** | 80.93 |
| MAT_000082 아트지 300g | 93.30 | 93.30 | **0.00** | 97.11 |
| MAT_000091 스노우지 250g | 77.75 | 77.75 | **0.00** | 80.93 |
| MAT_000092 스노우지 300g | 93.30 | 93.30 | **0.00** | 97.11 |
| MAT_000109 몽블랑 240g | 142.20 | 142.20 | **0.00** | 148.01 |

- 480x320 = 153,600mm² vs 316x467 = 147,572mm² → **면적 +4.085%**
- 7행 전건 차이 0.00 = 값을 그대로 복사한 흔적 (`reg_dt` 2026-09-01 00:17)
- 대조군: 같은 시기 신설된 SIZ_000640(550x375)은 46.65 → 67.16 으로 **면적비례(+39.8%)보다
  약간 높게(+43.96%) 산정**돼 있다. 640 은 값을 정했고 641 은 복사했다는 방증.

### 영향 범위

무선책자-표지(PRD_000290) · PUR책자-표지(PRD_000292) 의 **A4 표지(SIZ_000633)** 선택 건.
부수 100 기준 표지 용지비 7,064원 → 면적비례 7,353원, **건당 289원 과소**.

### 조치안 (택1 — 실무진 판단 필요)

- **(권장) A안 — 면적비례 재산정.** 7행을 `316x467단가 × 1.04085` 로 갱신(소수 2자리 반올림).
  SIZ_000640 이 이미 면적비례+α 로 잡혀 있어 일관된다.
- B안 — 권위 확인 후 실측 단가 적재. 단 「판걸이수」 시트에 480x320 은 **어휘 자체가 없다**
  (`plate_inventory.py`: 권위 판형 = 316x467·330x660·330x540·330x470·304x629). R5 와 함께 판단.

⚠ 어느 안이든 **가격 변경이므로 지니 승인 필수.** 이 카드에서는 명세까지만.

---

## R2 [Medium · 위생 · 가격영향 0] 중철책자-표지를 권위 정합 SIZ_000631 로 재배선

### 근거

- 권위 r60: 중철책자 표지 A5 = 재단 296x210 · 블리드 3.0 → 작업 **302x216**
- 라이브 PRD_000288 연결: **SIZ_000251 (작업 300x214 · 재단 NULL · 여백 NULL)**
- 권위 정합본 **SIZ_000631**(296x210 / 302x216 / 여백 3)은 260824 에 이미 생성됐으나 **연결 0건**

### 가격영향 = 0 (실증)

`fn_calc_pansu('SIZ_000499', ·)` → SIZ_000251 = **2**, SIZ_000631 = **2** (dryrun.log BEFORE 절)

### 조치

`t_prd_product_sizes` 에서 PRD_000288 → SIZ_000631 추가(dflt Y·disp_seq 1),
SIZ_000251 논리삭제(`del_yn='Y'` · 물리삭제 금지).
**적용 경로 = webadmin 상품정보 > 사이즈 탭.** SQL 은 DRY-RUN 검증용(`dryrun.sql`)일 뿐이다.

DRY-RUN 결과: `dryrun.log` — INSERT 1 / UPDATE 1 / 살아있는 행 2건 / ROLLBACK 후 원상 확인.

---

## R3 [Medium · 위생 · 가격영향 0] 중철책자-표지 A4 도 재단·여백 NULL

- 라이브 SIZ_000181 = 작업 426x303 · **재단 NULL · 여백 NULL**
- 권위 r61: 재단 420x297 · 블리드 3.0 → 작업 426x303 (작업치수는 일치)
- 권위 정합 값을 이미 가진 사이즈가 **존재한다**: `SIZ_000054`(재단 420x297 / 작업 426x303 /
  여백 3) — 다만 이름이 `420x297mm` 이고 현재 `PRD_000048 접지리플렛`에 연결돼 있다.

### 조치안 (택1)

- **(권장) A안 — SIZ_000181 에 재단·여백만 채운다**(420x297 / 3mm). 작업치수 불변 → 판걸이 불변(1),
  연결 변경 없음, 부작용 최소. search-before-mint 원칙에도 부합(신규 mint 0).
- B안 — SIZ_000054 재사용. 다만 이름이 상품맥락을 안 담아 표지 목록에서 혼동 소지.
- C안 — `중철책자 표지A4` 신설. 631 과 대칭은 좋으나 mint 1건 추가.

---

## R4 [Low · 위생] PUR책자 부모의 SIZ_000475(330x660) 판형행 재단 NULL

- `premise_q2_plate_detail.txt`: PRD_000070 살아있는 판형 2행 —
  SIZ_000499(dflt **Y**) · SIZ_000475 330x660(dflt N, **cut_width/cut_height NULL**)
- 실제 자동선택은 `one_default` 로 SIZ_000499 가 잡히므로 **현재 가격 영향 없음**(실측 확인)
- 그러나 330x660 은 권위 판형 어휘에 있는 실판형이다. 재단치수 NULL 은 t43 SIZ_000172 와
  같은 결함 계열이라, 향후 이 판형이 선택되는 순간 판걸이 오산의 씨앗이 된다.

### 조치

SIZ_000475 에 재단·여백 채움(여백 5mm 사방 → 재단 320x650). **판형 마스터 수정이라
다른 상품에도 파급**되므로 영향 범위 조사 후 별건 처리 권고 — 이 카드에서 단독 적용 금지.

---

## R5 [보류 · 권위 조회 필요] 무선/PUR 표지A4 — 권위 판형과 라이브 판형 불일치

- 권위 r63: 판형 **316x467(국4절)**. 그러나 작업 460x303 은 316x467 에서 **판걸이 0** (실측).
- 재단 그대로(454x297)면 316x467 실영역 306x457 에 회전 배치로 **1장 성립**(457−454 = 3mm 여유).
- 라이브는 신설 판형 **480x320(SIZ_000641)** 으로 우회했고, 480x320 은 **권위 어휘에 없다**.
- 권위 어휘 중 **330x660(3절)이면 판걸이 1 로 성립**한다 → search-before-mint 위반 후보.

### 확인이 필요한 질문 (실무진)

1. 무선/PUR A4 표지는 실제로 어느 인쇄기(국4절/3절)에 거는가?
2. 표지에 블리드 3mm 를 실제로 주는가, 재단 그대로 앉히는가? (권위 6열 판걸이가 비어 있음)
3. 480x320 은 실재하는 판형인가, 계산을 맞추려 만든 가상 판형인가?

**답이 나오기 전에는 R1 도 확정할 수 없다**(단가의 분모가 판형이므로).

---

## R6 [보류 · 권위 자체 모순] 하드커버무선 표지A4 — 권위 판형이 기하학적으로 불가능

- 권위 r65: 재단 532x355 · 블리드 0 · 판형 **330x660(3절)**
- 실영역 320x650 에 532x355 는 **정방향·90° 회전 모두 불가** → `fn_calc_pansu` = **0** (실측)
- 라이브는 **550x375(SIZ_000640)** 신설로 해소(판걸이 1). 550x375 역시 권위 어휘에 없다.
- 카드가 제안한 365x542 도 권위 어휘 밖이며, 이미 550x375 로 해소됐으므로 **신설 불필요**.

### 조치

권위 시트 r65 의 판형 지정이 오기인지 실무진 확인. 550x375 가 실판형이면 권위 시트에
역반영을 요청한다(라이브를 권위에 맞추는 방향의 예외 — 권위 쪽이 틀린 경우).

---

## 이 카드에서 다루지 않는 것 (W-2 소관)

`premise_q6_photobook.txt` — 구성요소/공식 0건이라 판형으로 안 풀리는 구성원:

| 상품 | 사이즈 | 판형 | 공식 |
|---|---:|---:|---:|
| PRD_000074 하드커버책자-면지 | 0 | 0 | 0 |
| PRD_000102 포토북-하드커버 | 0 | 0 | 0 |
| PRD_000104 포토북-면지 | 0 | 0 | 0 |
| PRD_000105 포토북-레더하드커버 | 0 | 0 | 0 |
| PRD_000107 포토북-소프트커버 | 0 | 0 | 0 |

카드가 명시한 트윈링071·하드커버링082·레더링바인더088·엽서북094 도 동일하게 범위 밖이다.
추가로 화면에서 확인된 `셋트공식(제본) 제본비 싸바리바인더 — 해당 없음 · 0원` 도
셋트공식 단가행 문제라 이 카드 밖이다.
