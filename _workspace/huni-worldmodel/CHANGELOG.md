# Huni-WorldModel 트랙 — 변경 이력

> 재시작 포인터=`HANDOFF.md`. 이 파일은 **무엇이 언제 왜 바뀌었는가**의 원장.
> [HARD] `05_gate/*` · `04_design/evaluation-rubric.md` · `03_problem/problem-ledger.md` 는 **판정 기록**이라 사후 수정하지 않는다. 아래 이력에서 그 파일들이 "생성" 이후 등장하지 않는 것이 정상이다.

---

## 2026-08-15 ~ 08-16 · 트랙 개설부터 SPEC 완성·게이트 재심까지 (세션 1)

`source_session_id: 250a060e-6d22-495b-851e-f6cf0ed0ba4d`

### 1. 리서치·진단 (에이전트 15 병렬)

- `01_research/R1~R7` 신설 — 뉴로심볼릭 이론 / 월드모델 계보 / **이주환 로컬 9문서 정독** / 이주환 웹 보강 / 온톨로지·GraphRAG / CPQ·CSP / 에이전틱 커머스.
- `02_diagnosis/D1~D8` 신설 — `models.py` 도메인 모델 / 가격엔진 / 옵션 캐스케이드 / **현행 뉴로 계층(`assistant*.py`)** / 운영 암묵지 / 에디터·원고 경계 / 선행 하네스 수확 / 라이브 스키마 실측.
- **전제 검증**: "이주환 = 뉴로심볼릭 월드모델 창업자" → **근거 확인됨**(저서 ISBN 979-11-93229-48-4). 단 *"창시"* 대상은 뉴로심볼릭·월드모델이 아니라 **ESTC 프레임워크·월드 모델 캔버스**. 원문 1,252쪽 미대조는 한계로 기록.

### 2. 문제 확정

- `03_problem/problem-ledger.md` 신설 — 문제 **26항** · 인과(근본 4 → 중간 5 → 증상 11).
- **핵심 판정: `PARTIALLY_CONFIRMED`** — *"월드모델이 없다"는 REFUTED*. 결정론 전이함수 **4개 실재**(`evaluate_price`·`_sim_disallowed`·`fn_best_plate`/`fn_calc_pansu`·`delete_usage`). 정확한 명제는 **"월드모델이 1축·부분·비결합 상태"**.
- 발표 서사 §5 교정: *"지식이 없다"가 아니라 **"지식은 있는데 그것으로 미래를 굴려보는 층이 없다"***.

### 3. 평가 루브릭 사전선언 [핵심 방법론]

- `04_design/evaluation-rubric.md` v1.0 신설 — **설계안이 존재하기 전에** 규칙 18개 선언(BLOCKER 5 = 27.8%, 상한 1/3 준수).
- 문서 4행에 *"작성 시점에 설계안은 존재하지 않는다. 작성자는 어떤 설계안도 읽지 않았다"* 명기 = **역산 불가능성 담보**.
- 반증 입증책임 5요건 + **기본값 = 각하** · 판정 3등급 · 오판 감사 의무 포함.

### 4. 설계

- `design-A/B/C` 3안 독립 생성 → 3렌즈(구현가능성·가격정확성·운영부담) 심사 → **세 렌즈 모두 C 승** → `design-FINAL.md` 합성.
- 채택: **「계측 롤아웃 루프」** — 기존 결정론 엔진 6종을 무수정 import 해 부작용 0 전이함수로 묶고, 선택 **전에** 후보를 굴려 비교. 빌려 쓴 정의가 여전히 참인지 **매 실행 기계 게이트로 계측**.
- A 탈락: 효과 1행을 등록할 **화면이 존재하지 않는다**(제약 커버리지 11.8% 가 그 증거). B 탈락: 파생이 `use_dims` 선언 시에만 작동하는데 **돈이 가장 크게 새는 P-05 가 정확히 `use_dims` 빈 케이스**.

### 5. 게이트 (원 라운드)

- `05_gate/gate-report.md` — **CONDITIONAL_GO**. codex-cli xhigh 3렌즈 + Claude 3렌즈 = 제기 19 → 심리 18(각하 1) → 병합 14 → **확정 위반 5**.
- **오판 감사: 정밀도 0.39** (임계 0.5 미달). 오판 12건 중 **11건이 규칙 매핑 오류(FP-4)**, 근거 인용 원본 실재 확인률 **100%**.
- 진단: *"검증자들은 코드 사실을 정확히 짚었으나 규칙 매핑에서 틀렸다"* → **교정 대상은 설계가 아니라 루브릭 커버리지와 검증 프롬프트**. 루브릭 개정안 ①②③ 등재.
- 필수 교정 FIX-1~FIX-10 확정.

### 6. 보충 검증 1차

- `coverage-supplement.md` — 원 라운드에서 **인용 0회로 미검증**이던 `RULE-03`(BLOCKER · 설계의 중심 논지)·`RULE-17` 을 독립 검증 → **둘 다 위반 없음**. BLOCKER 커버리지 80% → **100%**.
- **`/validate` 부작용 실측 = 있음(확정)** — 공통 게이트 `_gate` 에서 `widget_api.py:135` `t_wgt_widgets` UPDATE + `:195-196` 캐시 incr. → **FIX-4 의 "라이브 단독 오라클" 경로 폐기, clone DB 단일 경로로 확정.**

### 7. 교정 반영

- `design-FINAL.md` 804 → **1,046행** (34개소). FIX-1~10 전건.
- SPEC 3종 동기화 — **불일치 6건 교정**. 최대 실질은 `spec.md` §2.3 이 **FIX-4 반전 *전* 문안**을 담고 있던 것.

### 8. 보충 검증 2차

- `coverage-supplement-2.md` — 부분검증으로 남은 `RULE-10`·`RULE-18` 독립 검증 → **둘 다 위반 확정**(CS2-01·CS2-02). 부분검증 2 → **0**.
- **설계 자기 주장 반박**: `:281` *"미표기 0건 · F1~F20 외 도입 없음"* 의 **후단이 지지되지 않음**.
- 정밀도 1.00 이지만 검증자가 스스로 *"1.00 은 루브릭 건전성의 증거가 아니다"* 로 경계. 루브릭 개정안 **④** 등재.

### 9. 거버넌스 규칙 승격

- `.claude/rules/moai/core/adversarial-verification-governance.md` **332행 신설** — 기존 `verification-claim-integrity.md`(주장자의 입증책임)와 **짝**을 이루는 반증자의 입증책임 규율.
- `zone-registry.md` 40,254자 → **38,592자** 분할(`-design-mirror` 22건 · `-ci-protocol` 18건 이동, 유실 0) + 신규 조항 **`CONST-V3R6-002~018` 17건** 등재.

### 10. G6 clone 실현성

- `g6-clone-feasibility.md` — **FEASIBLE**. 슈퍼유저 권한 · SELECT 불가 테이블 0/154 · 확장 `plpgsql` 하나 · large object 0 · **37MB**(D8 의 27MB 에서 증가) · `upd_dt` **46/46 실재**.
- 로컬 `pg_dump` 18.3 있음, 서버는 @15/@16 → `brew install postgresql@18` 로 해소. **단 `pg_dump` 0회 실행 — "차단요인 부재의 실측"이지 "성공 실행의 관측"이 아니다.**

### 11. SPEC 보강 — ESTC 3단 축

- `spec.md` **§11 자율성 경계 선언** 신설(11.0~11.8) — 루브릭 18규칙에 **자율성 스펙트럼을 재는 규칙이 없다**는 공백 자체를 기록.
- 현재 자율 레벨을 **룰베이스 ~ 플로우 가이드**로 단정(세미 오토노머스 아님) + 상향 조건 (a)~(d) 명시.
- `AC-AUT-01~05` 신설 — 특히 **자동 확정 경로 0건**과 **`soft_prefs` 가 정렬 키 ④ 에만** 을 기계 확인.

### 12. 게이트 재심

- `gate-rehearing.md` — 재심 범위를 루브릭 §3.1 대로 **"교정 후 해당 규칙만"** 8규칙으로 고정. codex 3/3 실구동.
- **CLOSED 5** (RULE-05·09·12·13·14) / **STILL_OPEN 3** / **`amend_cleared = false`**.
- **핵심 발견 — `soft_prefs.axis` 가 열린 `str`**: 원 지적 A7(`selections`)은 닫혔으나 같은 `violation_test ①` 이 `soft_prefs.axis`·`budget_krw` 에서 재실패. 워크스루의 `axis:"mat_grade"` 는 **12축 어디에도 없고 문서 전체에서 한 곳에만 등장**했으며 Actor LLM → 심볼릭 Scorer ④ 까지 도달했다.
  → **"경계 안에서의 자율"인데 경계 자체에 구멍이 하나 있었다.** SPEC §11.4 의 ② 축 판정("충족(강함)")이 흔들린 지점.
- 정밀도 **1.00**(P=3) · **codex 오판 0건**(원 라운드 FP-4 8건 대비). 단 *"원인은 검증자 우수함이 아니라 좁혀진 라운드 설계"* 로 자기 경계.

### 13. R-02 · R-18 교정

- `design-FINAL.md` 1,046 → **1,123행**.
- **`PREF_AXES` 화이트리스트 신설** — `siz_cd`·`mat_cd`·`proc_cd`·`bdl_qty`·`print_opt_cd` 5축, 전부 `DIM_META` 실재 키(**새 어휘 0건**). 정의처 1곳. `plt_siz_cd` 제외 사유 = *"생산 개념 · 사용자 선택 대상 아님"*.
- fail-closed **11 → 12지점**. 워크스루 `mat_grade` → **`mat_cd`**.
- `spec.md` **§11.4-a** 신설 — ② 축 판정이 **흔들렸다 복구된 경위를 보존**. *"원래부터 강했다"로 고쳐 쓰지 않는다.* 방어선이 **(가) 위치 + (나) 정의역 두 겹**이 되어 복원.
- `§11.6 (e)` 신설 — `PREF_AXES` 화이트리스트 유지가 자율 레벨 상향 조건.
- R-18: `§4.0` 에 **F21 `projection map`** · **F22 `payable_parity`** 2행 추가 + F18 범위 문언 정정.

### 14. 요구사항 추적성

- `acceptance.md` 468 → **709행**. **§H 커버리지 표** 신설 — 요구사항 **64건 전건 등장**.
- **총수 정정: 58 → 64.** `[UX]-\d{3}` 정규식이 **`E-`/`S-`/`O-` 9건을 구조적으로 못 봤다.** 그중 **`S-005.1`(전제 센서 통과해야 롤아웃)은 검증 AC 가 0건**이었다.
- 미커버 **18.8% → 6.3%**. `AC-TR-01~07` 신설(미룬 것 0건). 신설 전/후 **두 표를 모두 보존** — 후자만 남기면 *"원래 커버돼 있었다"* 로 읽힌다. 간접 9건 **합산 금지** 명시.
- 미커버 4건은 **S1 착수를 막지 않음**(단정) — 사유가 검증 설계 결손이 아니라 **범위 선언 자체**. 단 *"위반 없음으로 쓰지 않는다"* [HARD] 박음.

### 15. 잔여 3건 정리

- `design-FINAL.md` 1,123 → **1,203행** · SPEC 3종 → **2,068행**.
- **① `intent.py` 모순** → **갈래 (B) 이연 선택**(범위 확대 회피). 근거 4항 전부 기존 선언. 대가(S1~S2 동안 `PREF_AXES` 기계 미검증)를 4곳에 명시. `AC-TR-08/09` 는 **미사용 예약**.
- **② 유령 ID 2건 정정** — `U-002.9~X-002.11` · `U-002.4~X-002.8`. 남은 `U-004` 는 **정규식 아티팩트**(실건수 0).
- **③ R-10 명세 반영** — 읽기전용 SELECT 로 `rule_cd ↔ rule_nm` 역인덱스, 해소 실패 시 `UNKNOWN_RULE_ID` + `sim_escalation` **fail-closed**(조용한 폴백 금지). fail-closed **12 → 13지점**. `AC-TR-10` 신설. **구현은 S2**.
- **FU-6·FU-7 신설** — `esc_kind` 채번 대기 / 재심 전제(`Scorer` S1 포함) ↔ 산출물 목록 어긋남.

### 16. 아티팩트

- `06_artifact/worldmodel-story.html` — 발행(비공개). v1 → **v2 시각 중심 재설계**.
- **mermaid 3 → 10개**, CSS 게이지 5개, `<details>` 접기 9곳, 산문→표 전환 11곳.
- **버그 수정**: `<pre class="mermaid">` 안의 `<br/>` 가 DOM 요소로 먹혀 줄바꿈이 소실됐다. `&lt;br/&gt;` 이스케이프로 해소(68개 전건 생존 확인). **`mermaid.parse()` 는 통과하므로 파서로 안 잡히는 버그.**

---

## 미실행 (다음 세션 이후)

- **S1 구현** — 산출물 6개. 한 줄도 시작하지 않음.
- **clone 리허설** — `pg_dump` 0회.
- **루브릭 개정 ①~⑤ 심의** — v1.0 → v1.1.
- **R-10 구현** (S2) · **FU-5 `dir` 의미 확정** (M3 전) · **FU-6 `esc_kind` 채번** (M2-c).
- **R-02·R-18 교정의 게이트 재심** — 현재 "교정 후 충족"은 자기평가이며 재심 권한은 게이트에 있다.
