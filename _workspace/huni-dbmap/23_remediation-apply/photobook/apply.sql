-- =====================================================================
-- 포토북 라이브 정정 적재본 (apply.sql) · round-13 / 2026-06-14
-- 정답 권위: 원본 엑셀 L1 최신 24_master-extract-260610/photobook-l1.csv
-- [HARD] 실제 COMMIT은 인간 승인 후 실행. 파일은 보유하되 자동 실행 금지.
-- 멱등: WHERE 현재값 가드 / ON CONFLICT → 재실행 시 0행 또는 동일결과.
-- 비파괴: DDL/DELETE 없음. 가격·자재정리·페이지차등은 컨펌대기(주석 보존).
-- 타임스탬프: INSERT reg_dt=now() / UPDATE upd_dt=now() (note 칸 있는 테이블만 변경이력).
-- 디지털인쇄 apply.sql 양식 + APPLIED.md MINOR(값 실제 다를 때만 가드) 계승.
-- =====================================================================

BEGIN;

-- =====================================================================
-- [즉시적용 1 · W-01] 무광코팅 공정 연결 (MISSING → 연결)
-- 종이표지(아트250+무광코팅)의 코팅을 공정으로 분리. 책자 6종(PRD_000068~072·082)은
-- 이미 PROC_000015 무광을 공정으로 분리 연결했으나 포토북만 자재명에 평면화돼 빠짐.
-- search-before-mint: PROC_000015 무광(부모 PROC_000013 코팅) 마스터 실재 → 신규 mint 0.
-- 근거: extraction-plan §3 · 실무진 확정 Q9(코팅=공정) · 책자 family 정합 live-diff §6.2.
-- 주의: t_prd_product_processes 에 note 컬럼 없음 → 변경이력=reg_dt=now() 만.
-- 멱등: ON CONFLICT (prd_cd,proc_cd) DO NOTHING → 재실행 0행.
-- =====================================================================
INSERT INTO t_prd_product_processes (prd_cd, proc_cd, mand_proc_yn, disp_seq, reg_dt, del_yn)
VALUES
  ('PRD_000100','PROC_000015','N',2,now(),'N')   -- 무광(표지 코팅·선택공정)
ON CONFLICT (prd_cd, proc_cd) DO NOTHING;

-- =====================================================================
-- [즉시적용 2 · W-02] PUR제본 필수공정 켜기 (mand_proc_yn N → Y)
-- 제본은 본질적 필수 공정(포토북은 PUR 단일·택일 불요). v03 15시트 필수여부 빈값이
-- 그대로 전파돼 N으로 적재된 것. excl_group 불요.
-- 근거: SDI 398(제본=필수) · loadlogic §3 · round-12 CONFLICT-PB-4.
-- 멱등: WHERE … mand_proc_yn='N' → 이미 Y면 0행(APPLIED.md MINOR 완전 무동작 멱등).
-- =====================================================================
UPDATE t_prd_product_processes
   SET mand_proc_yn='Y', upd_dt=now()
 WHERE prd_cd='PRD_000100'
   AND proc_cd='PROC_000020'
   AND mand_proc_yn='N';   -- 값가드: 실제 N일 때만 변경(재실행 무동작)

-- =====================================================================
-- 위까지가 즉시적용분. 아래는 모두 컨펌대기 — 주석 처리(실행 안 함).
-- =====================================================================

COMMIT;   -- [HARD] 인간 승인 후에만 실제 실행.

-- =====================================================================
-- ============== 컨펌대기 블록 (실행 금지 · 설계 보존용) ==============
-- =====================================================================

-- --------------------------------------------------------------------
-- [컨펌대기 W-03] 페이지·책등 표지타입별 차등 (Q-PB-1)
-- L1 책등 칸: 하드/레더=10/12/14/16, 소프트=4/6/8/10/12/14 → 표지타입별 차등.
-- 라이브 t_prd_product_page_rules PK=prd_cd(상품당 1행) → 한 테이블에 차등 못 담음.
-- 현행 24/150/2 는 하드 기준 → 소프트 페이지 범위가 다르면 옵션/sub_prd 설계 필요(cpq-plan §2).
-- (가) 현행 1행 유지 시: 변경 없음(하드 기준 공유).
-- (나) 표지타입별 차등 필요 시(컨펌 후): note 에 차등 명세 기록 예시:
-- UPDATE t_prd_product_page_rules
--    SET note=COALESCE(note,'')||' | 정정 2026-06-14: 표지타입별 책등 차등(하드 10/12/14/16·소프트 4/6/8/10/12/14·L1 책등칸)',
--        upd_dt=now()
--  WHERE prd_cd='PRD_000100';
-- (단 실제 차등 적재는 CPQ 옵션 레이어로 — apply 아님)

-- --------------------------------------------------------------------
-- [컨펌대기 W-04] 아트250 자재 정리 — 코팅 분리 + 중복 슬러그 통합 (Q-PB-2)
-- MAT_000250 "아트250+무광코팅"(.01·1상품) · MAT_000260 "아트250 + 무광코팅"(.01·7상품) 중복.
-- MAT_000172 "하드커버전용지+무광코팅"(.01·0연결) 미사용.
-- 코팅은 W-01 공정으로 분리했으므로 자재명도 코팅 제거 검토. 단 mat_nm 변경/통합은 횡단(8상품) 영향.
-- → ddl-proposer / 자재정리 트랙. 본 apply 에서 자재명 변경 안 함(오염 방지).
-- (참고 — 컨펌 후 코팅 제거 예시, 횡단 영향 확인 후):
-- UPDATE t_mat_materials SET mat_nm='아트250', upd_dt=now()
--  WHERE mat_cd='MAT_000250' AND mat_nm='아트250+무광코팅';
-- (260 으로 통합 시 PRD_000100 의 250 연결을 260 으로 재연결 — search-before-mint)

-- --------------------------------------------------------------------
-- [컨펌대기 W-05] 가격 전체 적재 (Q-PB-3) — round-2/16 가격 트랙 책임
-- L1: 가격_기본(24P) size×표지타입 매트릭스(활성 11조합·소프트10x10 공란=비활성) + 가격_추가(2P)당.
-- 라이브 t_prd_product_prices·t_prd_product_price_formulas 모두 0행.
-- 목표 테이블 = 고정가 직접단가 t_prd_product_prices(SDI 409) vs SKU별 t_prd_template_prices(Phase11).
-- → load_master 가격 함수 부재. 가격 적재는 dbm-price-formula(round-2) + 인간 컨펌(목표 테이블).
-- (본 정정 트랙은 가격 적재 안 함 — 기초데이터+CPQ 중심·가격사슬 현황만 보고)

-- --------------------------------------------------------------------
-- [컨펌대기 W-06] 표지 펼침 작업사이즈 귀속 (Q-PB-4)
-- 펼침 siz 마스터 실재(SIZ_000272/273/277/278/279/280/281·work만·cut 빈값) · t_prd_product_sizes 미연결.
-- (가) 미연결 유지(생산 작업용·고객 미선택·견적밖) — 현행 정상 가능.
-- (나) 표지 sub_prd(102/103/105~107)에 펼침 siz 연결(생산 BOM 완성도) — 컨펌 후.
-- INSERT INTO t_prd_product_sizes (prd_cd, siz_cd, ...) VALUES ('PRD_000102','SIZ_000272',...) ON CONFLICT DO NOTHING;
