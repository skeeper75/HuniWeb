# 엽서 상품군 — 견적가능 Q-게이트 판정 (S5)

> 작성 2026-06-15 · round-19 · `dbm-readiness-auditor`. 대상 016/017/018 엽서(PRF_DGP_A).
> 견적가능 = **①UI 견적 + ②생산정보(차원환원) + ③가격 계산** 셋 다 성립([[dbmap-goal-ui-quote-mes]]). 하나라도 미달 = NO-GO(거짓 GO 금지).
> 모든 판정 = 본 세션 라이브 읽기전용 실측 인용. "보인다" 금지.

---

## 0. 결론

| 출력 | 상태 | 근거 요약 |
|------|:--:|----------|
| **① UI 견적**(옵션 선택) | **PASS** | 016=4그룹/29옵션/29아이템, 017=4/8, 018=4/15 실적재. 손님이 인쇄·종이·모서리·후가공(017은 코팅)을 화면에서 선택 가능 |
| **② 생산정보**(차원환원) | **PASS** | 3상품 전 option_items의 ref_dim_cd가 라이브 차원행으로 100% 해소(트리거 predicate 위반 0/52). 선택→자재/공정/도수 BOM 환원 성립 |
| **③ 가격 계산**(사슬) | **CONDITIONAL-PASS** | 3상품 PRF_DGP_A 바인딩·29 구성요소→단가행 사슬 단절 0·대표 케이스 보정0 재현. **단 후가공 가격유형 오적재(D-1b·🔴)로 후가공 포함 견적은 과대** |

### 견적가능 종합 = **GO (조건부)**

- **무옵션/기본 견적**(인쇄+종이+모서리, 후가공 미선택)은 **완전 견적가능**: ①②③ 모두 성립, C1=44,330·C2=141,650 라이브 단가만으로 정확 재현.
- **후가공 포함 견적**은 ①②는 성립하나 ③에서 D-1b 오적재로 가격이 비합리적으로 과대(오시1줄 100매 → 1,000,000원). → **후가공 포함 시는 가격 NO-GO**, 정정(round-18/webadmin Phase11) 후 닫힘.
- 거짓 GO 방지: 엽서를 "완전 견적가능"이라 하지 않고 **"기본옵션 견적가능 GO + 후가공 가격 정정 1건 잔존"**으로 정직 판정.

---

## 1. Q1 컬럼 커버리지 — **PASS**

- `column-readiness.md` §1에 44 의미컬럼 전수 ✅/🟡/❌ 분류. **빈칸 0**.
- 집계: ✅ 34 · 🟡 7 · ❌ 0. 엽서 미해당 컬럼(별색·박·접지)은 "제외확정 ✅"로 명시(은폐 0).
- **PASS**(빈칸 0 기준 충족).

## 2. Q2 기초데이터 완전성 — **PASS**

라이브 실측(2026-06-15):

| 차원 | 016 | 017 | 018 | 판정 |
|------|:--:|:--:|:--:|:--:|
| siz (사이즈) | 7 | 7 | 5 | ✅ |
| plate (출력판형) | 1(SIZ_000499) | 1 | 1 | ✅ |
| mat (종이·USAGE.07) | 21 | 2 | 7 | ✅ |
| proc (공정) | 6 | 4 | 6 | ✅ |
| print_option (도수) | 2 | 2 | 2 | ✅ |
| clr (도수 색수) | CLR_000005/001 참조 실재 | 〃 | 〃 | ✅ |

- 필요 차원(siz·plate·mat·proc·도수) 전부 행 존재. **PASS**.

## 3. Q3 ①UI + ②차원환원 — **PASS**

### ① UI (option_groups/options/option_items 적재)
| 상품 | groups | options | items | 그룹 구성 |
|------|:--:|:--:|:--:|------|
| 016 | 4 | 29 | 29 | 인쇄(SEL.01 택1)·종이(택1·21종)·모서리(택1)·후가공(SEL.02 택N max4) |
| 017 | 4 | 9 | 8 | 인쇄·종이·**코팅**(택1)·모서리 |
| 018 | 4 | 15 | 15 | 인쇄·종이·모서리·후가공 |

- 손님이 화면에서 고를 항목(도수·종이·모서리·후가공·코팅)이 전부 옵션화됨. **① PASS**.

### ② 차원환원 (ref_dim_cd → 라이브 차원행 해소 = 생산정보)
트리거 `fn_chk_opt_item_ref`의 정확 predicate(.03은 mat_cd AND usage_cd 동시·.06은 opt_id::int)로 전수 해소 검사:

| 상품 | ref_dim 분포 | 미해소(위반) |
|------|------|:--:|
| 016 | .03=21 · .04=6 · .06=2 (계 29) | **0** |
| 017 | .03=2 · .04=4 · .06=2 (계 8) | **0** |
| 018 | .03=7 · .04=6 · .06=2 (계 15) | **0** |

- 전 52 option_item이 라이브 차원행으로 100% 환원 → 손님 선택이 자재(mat_cd+usage_cd)·공정(proc_cd)·도수(print_option opt_id)로 BOM 변환 가능 = **생산정보 전달 성립**. MES_ITEM_CD 의존 아님(설계 제1원칙 충족).
- 고아 option_item(삭제된 옵션/그룹 가리킴) = **0**.
- (주의: groups→options→items를 prd_cd 없이 조인하면 132/61 같은 fan-out 허수 발생. 진실 = option_items 직접 카운트 + ref_key2 포함 predicate. 본 판정은 진실값 사용.)
- **② PASS**.

### templates/constraints
- 016 addons 5(봉투 SKU 전부 del_yn=N 유효). 017/018 addons 0. 봉투는 본체 견적에 선택적 추가상품이라 본체 견적가능에 무영향.
- constraints: 3상품 전부 0행(글로벌 10행 중 엽서 미해당). PRF_DGP_A는 후가공이 독립 택N(SEL_TYPE.02)이라 하드 캐스케이드 제약 불요 → constraint 0이 정당(NO-GO 사유 아님).

**Q3 = PASS** (①②, 트리거 위반 0).

## 4. Q4 ③가격사슬 — **CONDITIONAL-PASS**

라이브 사슬 실측:
1. **바인딩**: `t_prd_product_price_formulas` — 016/017/018 → **PRF_DGP_A**(apply_bgn=2026-06-01·적재 2026-06-07). ✅
2. **배선**: `t_prc_formula_components` — PRF_DGP_A에 **29 구성요소**(인쇄 디지털 S1/S2·별색 10·코팅 2·용지·후가공 13·모서리 2·...) addtn_yn=Y. ✅
3. **단가행**: `t_prc_component_prices` — 전 구성요소 단가행 보유(COMP_PRINT_DIGITAL_S1=212행·COMP_PAPER=49·COMP_COAT_*=92·후가공 각 9~23). **사슬 단절 0**. ✅

→ 기본옵션(인쇄+종이+모서리) 사슬 완결. **하지만 🔴 D-1b**: 후가공 13 comp(오시/미싱/가변)가 `prc_typ_cd=PRICE_TYPE.01`(단가형). 라이브 단가행은 가격표 "합가(구간고정총액)"값(오시1줄: 1매=5000·100매=10000·300매=15000). 단가형으로 ×수량하면 100매=10000×100=1,000,000원(과대 990,000). 가격표 권위 = ⓒ 구간고정총액형(수량 무관 1회 부과)([[PRF_DGP_A/d1-plate-conversion]] §5). **값은 정확·prc_typ 메타만 오적재**.

- 기본옵션 견적 = **PASS** / 후가공 포함 견적 = **FAIL(가격 과대)**. → **CONDITIONAL-PASS**.

## 5. Q5 견적 시뮬레이션 — **PASS(기본) / FAIL(후가공)**

### C1 — 016·100×150·단면4도·아트지300g(MAT_000082)·무코팅·직각·100매 (보정 하드코딩 0)
- 차원환원: 도수=opt_id1(단면 CLR_000005) / 종이=MAT_000082·USAGE.07 / 모서리=PROC_000027(직각)
- D-1 출력판형: 작업사이즈 SIZ_000003 → note 전지 316×467 → **SIZ_000499**
- 가격(라이브 component_prices 조회):
  - 인쇄 COMP_PRINT_DIGITAL_S1 siz499·CLR_000005·min_qty=100 → **350.00**/장 ×100 = 35,000
  - 용지 COMP_PAPER siz499·MAT_000082 → **93.30**/장 ×100 = 9,330
  - 모서리직각 → 0
  - **= 44,330원** → PRF_DGP_A 골든 C1 일치 ✅
- C2(500매): 인쇄 min_qty=500=190.00×500=95,000 + 용지 93.30×500=46,650 = **141,650원** → 골든 C2 일치 ✅
- **기본옵션 시뮬 = PASS**(라이브 단가만으로 재현, 보정 하드코딩 0).

### C3 — 위 + 오시1줄 추가 (후가공)
- 차원환원: 오시=PROC_000029(✅ 환원). 단 **줄수(1줄)는 보존 안 됨(GAP-PARAM)** → 어느 줄수 단가를 쓸지 옵션값으로 식별 불가.
- 가격: COMP_PP_CREASE_1L 100매 단가행=10,000. 현 prc_typ=.01(단가형) → 엔진 ×100 = **1,000,000원**(비합리). ⓒ 정정 시 = 10,000(1회) → 총액 104,330원(가격표 정합).
- **후가공 시뮬 = FAIL**(D-1b 오적재로 990,000원 과대).

## 6. Q6 정직 갭 — **PASS**

미달 항목 전부 차단유형+라우팅으로 분류(§7). over/under-report 0:
- ②차원환원을 "132 item·일부 미해소"로 과대보고하지 않음(fan-out 허수 제거 후 진실 52 item·위반 0).
- ③가격을 "완전 GO"로 과대보고하지 않음(후가공 D-1b 명시·기본만 GO).
- 책자 stale("BLOCKED-5"·"DRY-RUN 4 item") 그대로 인용하지 않고 라이브 재실측으로 정정.

---

## 7. 차단 항목 + 라우팅 (S1)

| ID | 차단 항목 | 차단유형 | 영향 | 라우팅 대상 | 비고 |
|----|-----------|----------|------|------------|------|
| **D-1b** | 후가공 13 comp prc_typ=.01(단가형) 오적재 → ⓒ 구간고정총액형이어야 | 가격유형 메타 오적재(🔴 돈-크리티컬) | 후가공 포함 견적 과대(예 +990,000) | **round-18 `dbm-price-engine-verifier` → webadmin Phase11**(PRICE_TYPE.03 신설 or .02+엔진규칙 + 합가형 명세 보강) | 값 정확·메타만 정정·인간 승인. [[PRF_DGP_A]] §5 확정 |
| **GAP-PARAM** | 후가공 줄수(오시/미싱)·개수(가변) 0~3 보존처(ref_param_json) 부재 | 그릇(param 슬롯) 부재 | 줄수별 단가 분기 불가(현재 단일 단가) | **`dbm-ddl-proposer`**(ALTER 제안) | C31~34 🟡. 책자 C32와 동류 구조 한계 |
| **C38-ENV** | 봉투 addon 017/018 미적재(016만 5종) | CPQ 추가상품 부분 적재 | 017/018 봉투 선택 불가(본체 견적 무영향) | **round-6 `dbm-option-mapper`**(017/018 봉투 addon 적재) | 016 패턴 복제 |
| **C39-ENVPRICE** | 봉투 추가가격 보관처(t_prd_templates.price 컬럼) 부재 | 가격 그릇 미정 | 봉투가 견적 미반영 | **round-16/ddl-proposer**(가격엔진 연계 결정) | CONFIRM-3 |
| **C11** | plate output_file_typ 일부 NULL | 일부 NULL | 견적 영향 경미(생산메타) | (보류·Low) | — |
| **016-DFLT** | PRD_000016 plate dflt_plt_yn=N(타 2상품 Y) | 일관성 경미 | 단일 plate라 매칭 무영향 | (보류·경미·H-3) | CONFIRM-016-DFLT |

> ✅ 요소(기초데이터·CPQ 옵션·기본 가격사슬)는 이미 라이브 적재됨 → 적재 단계(S2~S4) 스킵. 라우팅은 ❌/🟡 **D-1b·GAP-PARAM·C38/C39**만.

---

## 8. 다음 행동 (견적가능 폐루프 닫기)

| 우선 | 행동 | 라운드 | 닫히는 것 |
|:--:|------|------|----------|
| **High** | 후가공 prc_typ 정정(ⓒ 구간고정총액형)·합가형 엔진규칙 명세 | round-18 → webadmin Phase11 (인간 승인) | ③ 후가공 포함 견적 → 완전 견적가능 GO |
| High | 후가공 줄수/개수 param 그릇(ref_param_json) DDL 제안 | `dbm-ddl-proposer` (인간 승인) | C31~34 🟡→✅, 줄수별 단가 분기 |
| Medium | 017/018 봉투 addon 적재(016 패턴) + 봉투가격 보관처 결정 | round-6 + round-16 | C38/C39 🟡→✅(추가상품 완전) |
| Low | 016 plate dflt_plt_yn=Y 정정·output_file_typ NULL 보강 | round-13 (경미) | C10/C11 일관성 |

**폐루프**: D-1b 정정 적용 후 C3(후가공) 재시뮬 → 104,330원(가격표 정합) 확인되면 엽서 = **완전 견적가능 GO**. 현재는 **기본옵션 견적가능 GO + 후가공 가격 정정 1건 잔존**.
