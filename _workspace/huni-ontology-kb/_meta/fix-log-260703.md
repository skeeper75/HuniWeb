# 결함 교정 이력 (fix-log) — Huni-Ontology-KB

> append-only 라운드별 교정 원장. 해소 판정은 생성자가 하지 않는다(생성≠검증) — 다음 라운드 검증가(okb-adversarial-gate) 몫.
> 이 파일은 "무엇을·왜·어떻게 고쳤나 + 재실측 증거"만 기록한다.

---

## R1 — 2026-07-03 (검증 라운드 1 결함 보드 교정 · builder 역할)

입력 결함 보드: 7건(중복 id 2쌍 병합 = 실질 고유 7건). 재빌드 결과 **hard=0·멱등 True·nodes=204·edges=420**(직전 425 → 중복 5 dedup).
빌드 리포트=`05_verification/build-report-20260703.md`. 스크립트=`04_graph/build_graph.py`(교정본·재실행 가능).

### 교정 결함 (fixed)

| ID | 라우팅 | 무엇을 | 어떻게(파일) | 재실측 증거 |
|----|--------|--------|--------------|-------------|
| **V1-01A** | curator+builder | 016·027·046 노드 + 팩이 인용한 정체/결함진단 파일 경로 접두사 오류(`print-kb/wiki/17_correctness/` = dead-path·bare `17_correctness/` = rootless) | 노드 3건 source_file → `_workspace/huni-dbmap/17_correctness/digital-print/`(product-016·027=product-identity.md·046=correction-manifest.md). 027 본문 "위키" 문구도 "huni-dbmap round-13 결함진단 문서"로 정정. 팩 §1 line 39(print-kb/wiki 접두사) + bare `17_correctness/digital-print/` 8곳(§3·§5) → `_workspace/huni-dbmap/…` 절대경로. **부수 정정:** src_id `SR-3-wikibase`(016)·`SR-pack-dp`(046 이 1건)는 "wiki/pack" 전제를 담아 실제 dbmap round-13 파일과 모순 → 027과 동일하게 `SR-13-identity`로 통일(3건 동일 소스). | `sqlite3 graph.db "SELECT node_id,source_file FROM source WHERE source_file LIKE '%17_correctness%'"` → 3건 전부 huni-dbmap 접두사. dead-path/rootless 잔재 `grep -c` = 0(노드·팩). 실파일 실재 확인(`ls huni-dbmap/17_correctness/digital-print/`). L-17 통과(hard=0). |
| **V1-01B/V1-02A** | curator+builder | 오염 차단 lint(O2/I-6)가 `blocklist.md` 부재로 공허 통과 + `load_blocklist()`가 부재 시 빈 set 반환(조용한 PASS 위장) | (a) `05_verification/blocklist.md` 실체화 — source-registry §8 STALE 목록을 HARD(src_id 5·path 5)/ADVISORY(src_id 2·path 2)/수동판정(경로 불가) 3구역으로 분류. (b) `load_blocklist()` 재작성: present 플래그·src_id+source_file 경로 부분포함 대조(우회 방지). **부재 시 하드 FAIL**(build). 리포트 I-6 라인=로드 원천 수 표기(공허 0 위장 제거). | blocklist rename 테스트 → `hard=1`·리포트 "게이트 원천 부재 = 하드 FAIL". 복구 → `hard=0`·"원천 실재 — hard src_id 5·path 5·advisory 2·2 로드". 현 정본 STALE 인용 0(회귀 방어). |
| **V1-02B** | architect+builder | lint 24종 중 6종(L-1·L-12·L-13·L-16·L-18·O3) 코드 전무. 특히 L-18은 graph-build §5.4 I-4 하드인데 file-format §5.3 소프트로 명세 자체 불일치 | architect: file-format-spec §5.3 L-18 → §5.1 구조 하드로 이동(graph-build와 통일). builder: **L-18 하드 구현**(option_refs 타깃이 부모 product 차원[uses_material·has_process·has_size·has_print_option]에 실재·has_option_group 역참조). 회귀 방어 가드 추가: L-1(하드·파일명↔id)·L-12/L-13/L-16(소프트)·O3(소프트 프록시·위키 승계라벨). | hard=0(L-18/L-1 현 데이터 위반 0 — 016/041 부모정합 수동 확인 일치). soft: L-12 8건(산문 raw 가격/수량 = 정당 신호)·나머지 0. |
| **V1-03A** | builder(+architect 규약) | 같은 유형(option_group)을 016은 `t_.../PRD_000016` 앵커, 041은 `anchor=none`(사유)로 다르게 모델링 | 041 옵션그룹 3개 앵커 → `t_prd_product_option_groups/PRD_000041`(016 방식 통일). 복합키는 sources locator에 전사 유지. 섹션 노트 갱신. ontology-schema §1.0에 **junction 앵커 규약** 명문화(부모측 앵커 + sources 복합키 확증). | `sqlite3 … WHERE id LIKE 'optgroup-041-%'` → 3건 전부 `t_prd_product_option_groups/PRD_000041`. L-17 통과(PRD_000041 스냅샷 실재·hard=0). |
| **V1-03B** | builder | 본문 `[[ ]]` 중복 언급 시 동일 references 엣지 2개 생성 → edges.jsonl 425 vs sqlite 420(ⓑ≠ⓒ)·리포트 과다계상 | `build_graph.py` 엣지 추출 후 (src,rel,dst) 전역 dedup 추가(직렬화·sqlite·리포트 전부 유일 엣지). | `wc -l edges.jsonl`=420 == `SELECT count(*) FROM edge`=420. 중복 (src,rel,dst) = 0. 리포트 "엣지 420". |
| **V1-04B** | architect | O4(index 등재)가 file-format §5.2=하드, graph-build §5=미포함, 코드=소프트 3원 불일치 | file-format-spec §5.2(하드) → §5.3(소프트)로 이동(발견성 규칙이지 무결성/오염 아님·graph-build I-set 미포함과 통일). 코드는 이미 soft(불변). | 세 원천 소프트로 통일. 리포트 O4 = 소프트 경고(현 데이터 0). |
| **V1-05B** | builder | 리포트 비고 "상품 노드 미집필 → Phase 4 대기"가 이미 8상품 집필 완료와 모순 | `report()` 비고 문구 갱신 — 현행 소프트 고아 = ①파일럿 미사용 축 항목 ②floating GAP(어떤 노드도 미링크). floating GAP은 연결 완전성(축5) 라운드 재판정 명시. | 리포트 비고 = 실상태 반영(node type product=8 실측). |

### 스키마 변경(실질/문서)
- `file-format-spec.md` v1.0.2: L-18 소프트→하드(§5.1)·O4 하드→소프트(§5.3)·O2 경로대조+부재 하드FAIL·O3 소프트 프록시 정직표기.
- `graph-build-spec.md` v1.0.2: L-18 하드 재확인·O4 I-set 제외(소프트) 명시·I-6 부재 하드FAIL+경로대조.
- `ontology-schema.md` v1.0.2: §1.0 junction·복합키 앵커 규약 추가.

### skipped / 보류
- 없음(전 7건 fix안대로 교정). O3은 fix안대로 "회귀 방어용" 소프트 프록시로 구현(전체 DROP-리스트 배선은 미완 — 검증가 O3 수동판정이 1차, 스키마에 정직 표기).

### 판단·근거 남김(임의 대체 아님)
- **src_id 정정(V1-01A 부수):** fix안은 source_file 접두사만 명시했으나, `SR-3-wikibase`/`SR-pack-dp`가 "wiki/pack" 전제를 담아 교정된 dbmap 경로와 모순되므로 동일 소스를 쓰는 027(SR-13-identity)에 통일했다(3건 동일 파일·동일 주장). 임의 신설 아님·기존 src_id 재사용.
- **V1-03A 방향:** fix안 2안 중 권장(016 방식 통일)을 채택. 041 `none`은 L-17 부모 미검증을 정직 회피한 것이나 016과 불일치가 결함의 본질 → 부모측 앵커+sources 복합키 확증으로 통일(스키마 규약화로 회귀 방지).

### open questions(검증가·architect 판단 요청)
- 팩 §3.5 `15_domain-spec/digital-print/column-dictionary.md`도 rootless(해당 경로 `ls` 부재) — 17_correctness 밖이라 이번 스코프 제외. 실제 위치 확인 후 curator 경로 정정 필요(V1-01A와 동류 잠재 결함).
- L-12 8건(산문 raw 가격/수량)은 소프트 정당 신호 — 스크립트 전사표/스칼라 prop+src_id 또는 `lint-allow` 태그로 정리할지 집필 정책 확정 필요(decision/gap 노드의 역사적 금액 서술 포함).

---

## R2 — 2026-07-03 (검증 라운드 2 결함 보드 교정 · builder 역할)

입력 결함 보드: 7건(중복 id 2쌍 — V2-01·V2-02가 각각 architect/builder 2건씩 · 실질 고유 7건). 재빌드 결과 **hard=0·멱등 True·nodes=204·edges=430**(직전 420 → R2 배선 +10).
빌드 리포트=`05_verification/build-report-20260703.md`. 재실측 스크립트=`05_verification/scripts/verify_r2_wiring_260703.py`(라이브↔그래프 전수 정합·재실행 가능).
★재실측 선행: 배선 전 라이브 스냅샷 직접 재측정으로 6결함 근거 전건 확증(016=7공정·041=5공정·085 상품바인딩 0·016 addon tmpl=005/006/009/038/039) — 추정 배선 금지.

### 교정 결함 (fixed)

| ID | 라우팅 | 무엇을 | 어떻게(파일) | 재실측 증거 |
|----|--------|--------|--------------|-------------|
| **V2-02(High·builder)** | builder | gap-016-process-nodes 색인('해소')·본문('미민팅·배선 대기')·gap노드(gap_what '배선 불가') 3소 불일치 + '미민팅' 사유가 반증가능 거짓(4공정 이미 verified 민팅) | **배선 완성 branch 채택**: 016 has_process 3→7(027/028/031/032 추가·라이브 부합)·corner/vartext/varimg optgroup option_refs 배선. gap-016-process-nodes **제거**(대상 소멸·아래 판단근거)·gaps.md 색인·본문·axis 주석 통일. | `verify_r2_wiring_260703.py`: 016 graph has_process==live 7공정. sqlite: gap-016-process-nodes count=0·optgroup option_refs 배선 확인. hard=0(L-18 부모정합 통과). |
| **V2-01(Med·builder)** | builder | 016 corner/varimg/vartext optgroup의 option_refs 0건(옵션→공정 경로 016만 끊김·형제 027/033 전수 배선) | V2-02와 동시 해소 — 세 optgroup에 option_refs(027/028·031·032) 배선. L-18 정합 위해 has_process 선행(부모 차원 실재). | sqlite optgroup-016-corner→027/028·vartext→031·varimg→032 배선. |
| **V2-03(Med·builder)** | builder | 041 has_process→PROC_000085가 환각(085 라이브 상품바인딩 0)·라이브 031/032 누락·optgroup-041-finish도 085 참조 | 041 has_process 085 제거·031/032 추가(라이브 부합·형제 패턴). optgroup-041-finish option_refs 085→031/032. "부모접기" 폐기(085=라이브 부재값). | `verify_r2_wiring`: 041 graph==live 5공정·has_process→085=0·live 085바인딩=0. |
| **V2-05(Low·curator)** | curator | 팩 §3.12 '핵심사실' 016 봉투 addon tmpl=010/011(stale)·라이브=038/039 | 팩 §3.12 tmpl 005/006/009/038/039로 갱신 + STALE 함정에 'tmpl_cd 010/011=교정 전 값' 등재. | 라이브 t_prd_product_addons PRD_000016 tmpl=005/006/009/038/039(재실측). |
| **V2-06(Low·builder)** | builder | 인간 종합 리포트 엣지 425 stale(결정론=420)·§8 gap 해소 거짓확정 | §1/§4 엣지 425→**430**(R2 실측·손계산 아님·결정론 리포트 전사)·해시 갱신·§2.2 헤더·§8 gap-016-process-nodes→GAP_016_material·§9 addon 부분해소. | 결정론 리포트 `build-report-20260703.md` 엣지 430·has_process 39·option_refs 75·references 84와 동기. |
| **V2-01(Low·architect)** | architect | L-7(updated 필수) 유령 규칙 — 선언만·강제 0(§5 규칙행 부재·빌더 미파싱/미직렬화) | file-format-spec v1.0.3: §2.1 필수 YAML에서 updated 제거(필수=5필드 명시)·§2.3 선택(권장·file-node)으로 이설·유령 '(L-7)' 라벨 삭제. **강등(옵션 b) 채택**(강제=옵션 a는 block-node 196 전수 FAIL로 부적절). | grep 'L-7' = 규칙행 0(정상)·§2.1 필수=id·type·anchor·badge·sources 5. 빌드 hard=0(회귀 없음). |
| **V2-02(Low·architect)** | architect | graph-build §3.1 블록 파싱 서술('파일 frontmatter 상속 + `- 출처:`·`- 연결:` 파싱')이 구현과 불일치(빌더=영문키·상속없음) | graph-build-spec v1.0.3: §3.1을 실제 구현대로 정정(파싱 영문키 `- type:`/`- src:`/`- rel:`/`- anchor:` 등 명기·파일 frontmatter 상속 없음·한글 라벨 드롭 경고). 엄격 파싱=회귀 안전으로 명세 확정. | 빌더 parse_block 키 분기 대조(코드=영문키). 빌드 hard=0. |

### 스키마 변경(실질/문서)
- `file-format-spec.md` v1.0.3: `updated` 필수→선택(권장) 강등·유령 L-7 라벨 제거(§2.1·§2.3). 실질 변경(필수 필드 집합 축소 — 단 강제 규칙은 원래도 없었으므로 빌더 코드 무영향).
- `graph-build-spec.md` v1.0.3: §3.1 블록 파싱 서술 정정(문서만·빌더 코드 무수정).

### 판단·근거 남김(임의 대체 아님)
- **V2-02 gap 노드 '제거' vs fix안 'gap_what 편집':** fix안은 배선 완성 branch를 명시적으로 제시하되 "gap_what '미민팅' 사유 삭제"로 노드 유지를 암시했다. 그러나 배선을 완성하면 gap의 전 주제(4공정 미민팅·미배선)가 실제로 소멸하므로, type=gap·badge=unknown 노드를 남기고 gap_what을 '배선 완료'로 고치면 **gap이면서 gap 아님**이 되어 V2-02와 동일 결함류(색인/본문/노드 상태 모순)를 재생산한다. 그래서 노드를 제거해 3소(색인·본문·노드)를 '공백 없음·전수 배선' 상태로 통일했다. 이는 fix안의 '배선 완성' branch의 직접 귀결이지 임의 대체 아님. ★'해소' 자기선언 아님 — 데이터를 라이브 부합으로 교정한 결과 gap이 사라진 것이며, 해소 판정(V2-02 CLOSED)은 다음 라운드 검증가 몫(생성≠검증). HTML 주석으로 제거 사유·근거 남김.
- **V2-03 옵션 1(라이브 부합) vs 옵션 2(부모접기 정책 명문화):** 자식 031/032가 R1 260703 축 승격으로 이미 verified 민팅됐고 라이브 041 공정=004/029/030/031/032라, 라이브 부합 직접 배선(옵션 1)이 환각(085 부재값 배선)을 제거하는 정답. 옵션 2(부모접기+085 양면라벨)는 라이브에 없는 관계를 유지해 fn_chk_opt_item_ref/L-18 부모정합도 깨지므로 미채택.
- **V2-04(자재 커버리지) 처리:** 라우팅=architect. GAP 노드 신설(fix안 옵션)을 채택 — 17자재 전수 민팅은 architect 완전성 정책 결정 대상이라 builder 단독 mint 대신 **정직 선언(GAP_016_material)**으로 조용한 누락을 명시하고 정책을 gap_fill_from에 상신. (아래 skipped 참조 — 스키마 완전성 기준 명문화는 architect 상신)

### skipped / 보류
- **V2-04 스키마 완전성 기준 명문화(architect 상신):** fix안 후단 "'대표만' 파일럿 정책이면 위키계승 규칙에 그래프 완전성 기준 명문화"는 파일럿 자재 subset 정책을 정할 **architect 결정**(대표 subset 유지 vs 전수 민팅)이라 builder가 스키마에 규약을 못박지 않고 GAP_016_material·gap_fill_from + 이 상신으로 남긴다(임의 정책 확정 금지). 실 결정 시 wiki-inheritance-map/graph-build §5에 "비대표 축멤버 subset은 커버리지 GAP 선언 필수" 규약 추가 권고.

### open questions(검증가·architect 판단 요청)
- **PROC_000085 소프트 고아 경계:** 085 has_process 배선 제거로 085는 라이브 실재하나 상품 미바인딩 공정(가격 proc_grp로만 쓰임)이 됐다. 현재 material-MAT_000105 산문 [[axis/processes#PROC_000085]] 참조가 우연히 degree 1을 줘 고아 회피 중 — 이는 노트 위치가 MAT_000105 블록에 귀속된 부수효과(spurious). 085를 축 문서 노드로 유지할지/031·032에 derived_from(parent) 엣지로 정식 연결할지 architect 판단.
- **041 사이즈 013/014 둘 다 dflt_yn=Y** 잔존 open_question(데이터 품질·라이브 소관·V2 범위 밖).
