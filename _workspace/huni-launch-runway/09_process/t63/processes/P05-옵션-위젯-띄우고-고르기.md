# P05 · 옵션 위젯 띄우고 고르기

> t63 프로세스 정리 B — 탐색~결제 · L1 **옵션·견적** · L2 옵션위젯 · 제약

## 1. 한줄정의 · 시작/끝 · 등장인물

**한줄정의** — 상품 상세에 위젯이 마운트되고, 손님이 옵션을 고를 때마다 하위 선택지와 불가 조합이 갱신된다.

- **시작**: 상품 상세에서 주문옵션 위젯이 마운트된다
- **끝**: 손님이 고를 수 있는 사양 축이 모두 열린다(P06·P07 로 이어짐)

| 등장인물 | 무엇인가 |
|---|---|
| 고객 | 몰을 쓰는 손님(회원·비회원) |
| huni-mall | 신규 독립몰(headless · Next.js 15 App Router) |
| 위젯 | 상품 상세에 마운트되는 인쇄 주문옵션·견적 위젯 |
| webadmin | 후니 자체 관리서버(Django) — 상품·옵션·가격·위젯빌더 |

**가로지르는 곳**
- P14 — 확정 사양의 주문항목 변환(STD-ORD-028)이 이 위젯의 출력을 받는다
- t65 — 제약규칙 등록·관리(webadmin 폼빌더)는 운영 쪽

## 2. 흐름 (sequence)

```mermaid
sequenceDiagram
    actor C as 고객
    participant M as huni-mall
    participant G as 위젯
    participant W as webadmin
    M->>G: 위젯 마운트(격리 렌더 · 상품코드 전달)
    G->>W: 정규화 옵션 데이터 계약 조회
    W-->>G: 옵션 그룹·항목·제약·차원
    G-->>C: 옵션 컴포넌트 렌더(셀렉트·라디오·체크·수치·이미지)
    C->>G: 상위 옵션 선택
    G->>W: 하위 목록·제약 재평가
    W-->>G: 가능 선택지 / 불가 사유
    G-->>C: 하위 목록 갱신 · 선택 요약 패널
```

## 3. 갈림길 (flowchart · 실패·비회원·취소 분기)

```mermaid
flowchart TD
    A[위젯 마운트] --> B[정규화 옵션 계약 로드]
    B --> C[옵션 컴포넌트 렌더]
    C --> D[손님이 옵션 선택]
    D --> E{제약 규칙 평가}
    E -- 불가 조합 --> E1[차단 + 사유 표시]
    E -- 가능 --> F[하위 목록 캐스케이드 갱신]
    F --> G{후가공 토글}
    G -- 켬 --> G1[하위 선택지 노출]
    G -- 끔 --> H[선택 요약 패널 갱신]
    G1 --> H
    E1 --> D
    H --> I[사양 축 입력으로]
```

## 4. 단계표

| # | 단계 | 시스템 | 담당자 | 원장 row_id | 상태 | 근거 |
|---|---|---|---|---|---|---|
| 1 | 상품 상세 내 주문옵션 위젯 마운트(격리 렌더) | 미확인 | 서희항 | `STD-OPT-001` | 작동 | 「미확인」 |
| 2 | 정규화 옵션 데이터 계약 기반 위젯 구동 | 미확인 | 서희항 | `STD-OPT-002` | 작동 | 「미확인」 |
| 3 | 옵션 컴포넌트 타입 다형 렌더(셀렉트·라디오·체크·수치입력·이미지선택) | 미확인 | 서희항 | `STD-OPT-003` | 작동 | 「미확인」 |
| 4 | 옵션 그룹 접기/펼치기·진행 스텝 표시 | 미확인 | 서희항 | `STD-OPT-006` | 부분 | 「미확인」 |
| 5 | 옵션 선택 캐스케이드(상위 선택이 하위 목록 갱신) | 미확인 | 서희항 | `STD-OPT-004` | 부분 | raw/webadmin/webadmin/config/urls.py:249 (선행 카드 증거 · 실재 확인) |
| 6 | 토글형 후가공 섹션(켜야 하위 선택지 노출) | 미확인 | 서희항 | `STD-OPT-005` | 부분 | 「미확인」 |
| 7 | 옵션 조합 제약 규칙 평가(불가 조합 차단·사유 표시) | 미확인 | 서희항 | `STD-OPT-037` | 부분 | raw/webadmin/webadmin/config/urls.py:371 (선행 카드 증거 · 실재 확인) |
| 8 | 자재→가능 인쇄방식 게이팅 | 미확인 | 서희항 | `STD-OPT-038` | 부분 | raw/webadmin/webadmin/config/urls.py:371 (선행 카드 증거 · 실재 확인) |
| 9 | 공정 택일 그룹(상호배타) 처리 | 미확인 | 서희항 | `STD-OPT-039` | 부분 | raw/webadmin/webadmin/config/urls.py:371 (선행 카드 증거 · 실재 확인) |
| 10 | 필수 동반 옵션 강제(예 인쇄없음 시 후가공 필수) | 미확인 | 서희항 | `STD-OPT-040` | 부분 | 「미확인」 |
| 11 | 수량 최소/최대/증분 검증 | 미확인 | 서희항 | `STD-OPT-041` | 작동 | 「미확인」 |
| 12 | 사이즈 범위·비율 검증(매트릭스 밖 사이즈 차단) | 미확인 | 서희항 | `STD-OPT-042` | 부분 | 「미확인」 |
| 13 | 선택 요약 패널(현재 사양 한눈에) | 미확인 | 서희항 | `STD-OPT-007` | 작동 | 「미확인」 |

## 5. 빠진 곳

**나 행은 있는데 코드 0** — 9건

| row_id | 내용 | 진단 |
|---|---|---|
| `STD-OPT-001` | 상품 상세 내 주문옵션 위젯 마운트(격리 렌더) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| `STD-OPT-002` | 정규화 옵션 데이터 계약 기반 위젯 구동 | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| `STD-OPT-003` | 옵션 컴포넌트 타입 다형 렌더(셀렉트·라디오·체크·수치입력·이미지선택) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| `STD-OPT-006` | 옵션 그룹 접기/펼치기·진행 스텝 표시 | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| `STD-OPT-005` | 토글형 후가공 섹션(켜야 하위 선택지 노출) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| `STD-OPT-040` | 필수 동반 옵션 강제(예 인쇄없음 시 후가공 필수) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| `STD-OPT-041` | 수량 최소/최대/증분 검증 | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| `STD-OPT-042` | 사이즈 범위·비율 검증(매트릭스 밖 사이즈 차단) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| `STD-OPT-007` | 선택 요약 패널(현재 사양 한눈에) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |

**다 코드는 있는데 연결 안 됨** — 4건

| row_id | 내용 | 진단 |
|---|---|---|
| `STD-OPT-004` | 옵션 선택 캐스케이드(상위 선택이 하위 목록 갱신) | 코드는 있는데 원장 상태가 「부분」다 |
| `STD-OPT-037` | 옵션 조합 제약 규칙 평가(불가 조합 차단·사유 표시) | 코드는 있는데 원장 상태가 「부분」다 |
| `STD-OPT-038` | 자재→가능 인쇄방식 게이팅 | 코드는 있는데 원장 상태가 「부분」다 |
| `STD-OPT-039` | 공정 택일 그룹(상호배타) 처리 | 코드는 있는데 원장 상태가 「부분」다 |

## 6. 채우는 방법

| 누가 | 무엇을 | 선행 |
|---|---|---|
| 서희항 | 상품 상세 내 주문옵션 위젯 마운트(격리 렌더) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| 서희항 | 정규화 옵션 데이터 계약 기반 위젯 구동 | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| 서희항 | 옵션 컴포넌트 타입 다형 렌더(셀렉트·라디오·체크·수치입력·이미지선택) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| 서희항 | 옵션 그룹 접기/펼치기·진행 스텝 표시 | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| 서희항 | 옵션 선택 캐스케이드(상위 선택이 하위 목록 갱신) | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 토글형 후가공 섹션(켜야 하위 선택지 노출) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| 서희항 | 옵션 조합 제약 규칙 평가(불가 조합 차단·사유 표시) | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 자재→가능 인쇄방식 게이팅 | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 공정 택일 그룹(상호배타) 처리 | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 필수 동반 옵션 강제(예 인쇄없음 시 후가공 필수) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| 서희항 | 수량 최소/최대/증분 검증 | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| 서희항 | 사이즈 범위·비율 검증(매트릭스 밖 사이즈 차단) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| 서희항 | 선택 요약 패널(현재 사양 한눈에) | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |

## 7. 확인 못 한 것

제약 규칙이 위젯(클라이언트)에서 평가되는지 webadmin(서버)에서 평가되는지 실행 경로를 끝까지 따라가지 않았다.

---
> 이 문서의 상태·근거는 원장 `08_system-screen/t56/rejudge.csv` 와 이 카드의 증거 수집본에서 기계로 채웠다. 「미확인」은 코드·원장·명세를 뒤졌으나 **찾지 못했다**는 뜻이지, 없다는 뜻이 아니다.
