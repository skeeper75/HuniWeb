# M3 재사용 목록 — `_workspace/huni-dbmap` 조사 (2026-08-29)

> M3 = 권위 가격표 셀 좌표 → 라이브 차원 코드 번역기 (`SPEC-PRICEGRID-001`).
> 조사 범위 919 md / 45 디렉터리. **읽기만 했고 아무것도 고치지 않았다.**

---

## 0. 결론 세 줄

1. **파트 2·3(축 라벨 → 차원 코드 · 묶음 분해)은 이미 두껍게 있다.** 16 상품군 전수 설계 + 독립 검증 게이트 16종.
2. **파트 1(권위 블록 → `comp_cd`)의 일반 규칙은 이 트리에 없다.** 있는 것은 16시트분 손확정 정답표와 그걸 하드코딩한 스크립트뿐이다. **새로 만들어야 한다.**
3. **[HARD] 설계 문서 전부가 옛 권위(260527) 위에 있다.** 다만 **추출은 260822 로 다시 돌려 놨고**, 그 산출물이 내가 새로 만든 파서보다 낫다(§4).

---

## 1. [HARD] 판본 함정 — 재사용 전 반드시 읽을 것

```
md 919개 중  260822 언급   0건
             260527 언급  88건
             260702/705    28건
```

**설계·매핑·게이트 문서 전량이 260527 기준이다.** 셀 좌표·블록 범위·행수 주장(예: 명함포토카드 R1:R134, 코팅 A1:E56)은 260822 에서 **재확인 없이 신뢰하면 안 된다.**

재사용 규칙: **규칙과 판정 프레임은 승계하고, 좌표와 개수는 260822 로 재실측한다.**

추가 stale 표식:

| 대상 | 상태 |
|---|---|
| `02_mapping/` (round-2) | 게이트 항목 이름이 「P2 stale 차단」. `frm_typ_cd` 존재 가정 · 6/8차원 표기는 **폐기**, 차원 배치표·T-규칙은 승계 |
| `18_schema-change/impact-diagnosis.md` | 스스로 「stale 진단」이라 명시 |
| 라이브 `min_qty` | `_gate/cutting-drilling-gate.md` 확정 — 시트 300/500/1000 ↔ 라이브 200/300/400. **수량 tier 를 라이브에서 역산하면 틀린다** |
| `00_schema/price-engine-ddl.md` | 구버전 `table-spec_260602.html` 사용 금지 명시 |

---

## 2. 파트별 재사용 목록

### 파트 3 — 묶음 라벨 분해 (`/`·`,` 로 묶인 동가군)

| 경로 | 내용 | 범위 | 검증 주장 |
|---|---|---|---|
| `20_price-import/sticker/sticker-material-axis.md` | **원본 규칙.** 가격표 3그룹을 3 mat_cd 로 collapse 한 것을 개별 소재로 분해 | 스티커 | 라이브 `t_mat_materials` 실측 |
| `20_price-import/namecard-photocard/namecard-photocard-decomposition.md` §3·B09 | 「금/은/먹유광·청박·적박·동박」 = 일반박 6종 동가 → `_STD` / 홀로그램·트윙클 → `_HOLO` | 명함포토카드 | round-trip 14셀 100% — **단 §5 경고** |
| `20_price-import/plywood-domusong/…-decomposition.md` §7 | 소재 2축 → 6 개별분해. 「원본 셀 370 ↔ 분해행 1110(셀당 3소재), 1110÷3=370 복원」 | 합판도무송 | 셀 카운트 |
| `20_price-import/folding/folding-decomposition.md` | 헤더 `/` 6개 → `proc_cd` 개별분해(480행), 라이브 collapse(336행) 병기 | 접지옵션 | 셀 카운트 |
| `28_price-arbitration/_price-queue-closeout/phase-a-pricetable-recheck.md` | 셀 좌표까지 인용해 **「`/` 헤더 = 저자가 의도적으로 같은 가격 소재를 그룹화」를 권위로 확정** | 3건 | 「권위(HARD)=가격표 엑셀 직접 열람 · 라이브는 오라클이지 권위 아님」 |

### 파트 2 — 축 라벨 → 차원 코드 (패밀리 비종속 일반 규칙)

| 경로 | 내용 |
|---|---|
| `02_mapping/mapping-spec-price.md` §1 | **시트 × 차원 배치표** + **T-1~T-10 변환 규칙**. T-1 `band_header_path` 를 `>` 로 split(외밴드=comp·내밴드=옵션) · T-3 단/양면=별도 comp(`_S1/_S2`) · T-4 면적매트릭스 가로×세로→`siz_cd` · T-7 수량행→`min_qty` 상향개방 · T-10 한 셀 2종소재 동일단가→대표 mat+note |
| `02_mapping/schema-fitgap-price.md` §2b | **실무진 메모 해석표**(「용지 포함」·「20장1세트」→`bdl_qty`·「종이+동판+박가공비」→합가) + siz 41종·mat 12종 **실코드 재사용 목록** |
| `33_silsa-price-quote/component-inventory.md` (2026-06-17, 트리 최신) | 라이브 `t_prc_component_prices` 컬럼 전수 = 13차원 + `dim_vals`. 8 구성요소별 실제 `use_dims`/`prc_typ`/배선/행수 + 재현 쿼리. 「줄수·개수는 `dim_vals` jsonb」·「별색=공정이라 `clr_cd` 공백」 |
| `20_price-import/coating/coating-structure.md` §4 | **판형 ↔ `siz_cd` round-trip**: 국4절=`SIZ_000499` · 3절=`SIZ_000077`. 라벨은 불일치하나 단가 왕복으로 매핑 확정 |
| `05_method/F2-price-sheet-structures.md` | 가격표 15 단가시트 블록 인벤토리 · 밴드헤더 깊이 · 수량축 정체 · 차원 귀속 후보. 9게이트 15/15 PASS |
| `_gate/envelope-gate.md` | 「10차원」 표기 보정 → 실제 자연키 차원 **8개** + `comp_cd` + `apply_ymd` |

### 파트 1 — 권위 블록 → `comp_cd` **(일반 규칙 없음)**

없는 것을 먼저 적는다. **「임의의 (sheet, block) 에서 `comp_cd` 를 도출하는 규칙」은 이 트리에 존재하지 않는다.** 실제 코드는 `NAMECARD_PROD = {"B01":"STD", ...}` 식 **블록ID 하드코딩 딕셔너리**다.

대신 공짜로 얻는 것 셋:

| 순위 | 경로 | 무엇 |
|---|---|---|
| 1 | `22_price-load-prereq/prereq-dependency-map.md` §1 | **시트를 행 키로 삼은 유일한 표** — 시트 × `frm_cd` × 배선 × 바인딩 `prd_cd` × `comp_cd` × 차원 × 단가행 상태 × 단절. 11시트 커버. 라이브 실측(2026-06-13) |
| 2 | `26_price-engine-verify/_class-map.md` (2026-06-14) + `30_batch/classification.csv` | **역방향 앵커** — 「이 comp 가 어느 공식·상품에 붙어 있나」. 시트→comp 후보를 상품 바인딩으로 교차검증하는 오라클. 「184상품(75%) 가격 미구성」으로 빈칸 크기도 알려 줌 |
| 3 | `20_price-import/<16 family>/*-decomposition.md` §1 + `_gate/<family>-gate.md` | **16시트분 손확정 정답 셋 + 각 항목에 독립 검증 판정.** 규칙은 아니지만 **규칙을 귀납할 학습셋으로 가장 크고 신뢰도 라벨이 붙어 있다** |
| 4 | `26_price-engine-verify/_binding-overview/binding-product-price-matrix.md` | 「상품 존재? × 가격표 존재?」 2×2 **판정 스키마** — 매칭 실패를 A/B/C/D(연결만 하면 됨 / 가격 없음 / 고아 / 둘 다 없음)로 분류 |

---

## 3. 이미 변환을 수행하는 스크립트

| 경로 | 한 줄 |
|---|---|
| **`02_mapping/scripts/transform_price_sheets.py`** (1,543줄) | L1 CSV → `component_prices` long-format. **세 파트를 한 번에 수행하는 유일한 실행물** — 시트별 `transform_*()` 12개 + 파트1 블록→comp 딕셔너리 + 파트2 라벨→코드 딕셔너리 + 파트3 `leaf.split("/")`. **새 생성기의 baseline이자, 무엇이 하드코딩이고 무엇이 규칙인지 가르는 기준선** |
| `02_mapping/scripts/gen_foil_load.py` | 박 등급 A~E 를 **DB 에 안 싣고** 조인 소거로 (가로,세로,수량)→가격 환원. 「엑셀 편의축 제거」 참고 구현 |
| `06_extract/scripts/extract_price_sheets.py` · `verify_price_sheets.py` | 가격표 → 무손실 L1 CSV + 9게이트 검증 |
| **`_scripts/run_extract_price_260822.py`** | 위 추출기를 **현재 권위 260822_1** 로 재실행한 러너 (원본 sha256 기록) |

---

## 4. [HARD] 내가 만든 파서는 기존 L1 추출본의 열등한 재구현이다

`24_price-extract-260822/` 에 **현재 권위 기준** L1 이 16시트 7,355행으로 이미 있다.

```
price-digital-print-price-l1.csv
  sheet, block_id, block_title, row_seq, col, cell_ref, row_key,
  band_header_path, value, cell_meta_json

  디지털인쇄비, B01, …, B2, , "흑백(1도) > 단면", 흑백(1도), {"merged_range":"B2:C2"}

-meta.csv → {"title":…, "r_title":1, "row_range":[1,60],
             "col_range":["A","O"], "header_rows":[2,3], "data_start_row":4}
```

| | 내 `table_parse.py` | 기존 L1 (260822) |
|---|---|---|
| 권위 판본 | 260822 | **260822** |
| 다단 헤더 경로 | `col_group` — **이번 턴에 내가 추가** | **`band_header_path` (`>` 구분) — 이미 있음** |
| 하위표 식별자 | 없음 (감사 D2) | **`block_id`** |
| 셀 좌표 추적 | 없음 | **`cell_ref`** |
| 병합·색상 | 없음 | `merged_range` · `fill_rgb` |
| 블록 기하 | 없음 | `header_rows` · `data_start_row` · `row_range` |
| 소비 규칙 | 없음 | **T-1 이 `band_header_path` 를 `>` 로 split 하도록 이미 설계돼 있음** |

⇒ **M3 의 입력측은 `table_parse.py` 가 아니라 `24_price-extract-260822/*-l1.csv` 로 잡는다.**
`table_parse.py` 는 `table_diff.py` 의 입력으로만 계속 쓴다(권위↔라이브 대조는 그 위에서 네 차례 튜닝돼 GAP 20셀까지 좁혀져 있고, 이번 `col_group` 추가로 회귀 0 확인).

---

## 5. 재사용 시 반드시 함께 읽을 경고

- **`_gate/namecard-photocard-gate.md` 평결**: 「CONDITIONAL-GO … round-trip 은 무손실(14/14) 이나 **빌더가 줄곧 주장한 27 구성요소는 라이브 실측상 28개**(분모 오류)이고, **import.xlsx 의 PEARL/WHITE 소재 전개에 정합 결함 2건**」. 즉 이 문서의 14셀 100% 는 검증됐지만 **소재 전개 자체는 P5 🔴 FAIL** 이다. 설계는 승계하되 산출물은 재검한다.
- **`29_readiness/_price-remediation-queue.md`**: 횡단 결함 3종(`prc_typ` 오적재 22 comp · 배선 단절 · GAP-PARAM). 파트 1 로 comp 를 정한 뒤 **왜 값이 그래도 안 나오는지**의 목록.

---

## 6. 무관 확인 (누락 아님)

`00_schema` · `01_excel`(단 `price-sheet-scope.md` 는 16 단가시트 범위 확정으로 유용) · `03_validation` · `04_audit`/`04_review` · `07_domain` · `08_remediation` · `09_load` · `10_configurator`/`13_admin-ui-spec` · `11_ddl_proposals` · `12_coverage` · `14_change-tracking`/`26_change-tracking-260702` · `15_domain-spec`/`16_mapping-research`/`17_correctness`/`19_grid-binding`(상품마스터 렌즈 — 가격표 셀 좌표 아님) · `18_schema-change` · `23_remediation-apply` · `24_master-extract-*` · `25_load-readiness-scan` · `27_competitor-benchmark` · `29_readiness` · `31_acrylic-price-link` · `32_axis-staged-load`(자재·사이즈 축은 파트 2 간접 참고) · `33_silsa-price-quote`(단 `component-inventory.md` 는 §2) · `34_naming-standardization` · `35_category-map` · `platesize-remediation`

---

## 7. 미검증 (이 조사가 하지 않은 것)

- **각 문서의 내용 정확성은 재실측하지 않았다.** 인용한 검증 주장은 그 문서가 스스로 적은 것이고, 260527 기준이다.
- L1 추출본 7,355행의 **무손실 여부를 260822 원본으로 재확인하지 않았다.** `_price-extract-summary.json` 이 roundtrip 을 기록한다고 보고받았으나 그 파일의 구조를 직접 읽지 못했다.
- `transform_price_sheets.py` 1,543줄을 **실행해 보지 않았다.** 딕셔너리 구조는 보고받은 것이다.
