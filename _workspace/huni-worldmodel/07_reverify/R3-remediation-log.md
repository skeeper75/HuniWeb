# R3 반영 원장 — SPEC-WORLDMODEL-001 (권고 W-1~W-10)

> 작성 2026-08-16 · 트랙 `huni-worldmodel/07_reverify` · 카드 SPEC-WORLDMODEL-001 R3 재검증 · 칼럼 `plan`
> 입력: `R3-adversarial-review.md`(§⑥ 권고 W-1~W-10) · `R3-mechanical-scan.md`(인용 좌표 실측·추적성 재집계·코드 앵커)
> 대상: `.moai/specs/SPEC-WORLDMODEL-001/{spec,plan,acceptance}.md` + `_workspace/huni-worldmodel/04_design/design-FINAL.md`
>
> [HARD] **판정 기록은 한 글자도 수정하지 않았다** — `05_gate/*`(`gate-report.md`·`gate-rehearing.md`·`coverage-supplement*.md`·`g6-clone-feasibility.md`) 및 `04_design/evaluation-rubric.md`. R3 보고서 2종도 입력이므로 수정하지 않았다. 이 세션의 쓰기는 위 4개 문서 + 본 원장뿐이다.
> [HARD] **라이브 DB 미접속 · `raw/webadmin/**` 읽기 전용**(`wc -l`·`sed` 만 사용). 커밋하지 않았다(sync 칼럼 소관). `git add -A` 를 쓰지 않았다.
> [HARD] **관측하지 않은 것을 통과로 쓰지 않았다.** 아래 "반영하지 않은 것", "미교정 좌표 처리 방침", "Gaps" 절이 그 경계다.

---

## ① 권고별 반영 (W-1 ~ W-10 전건)

### 우선순위 High (W-1 ~ W-5)

| 권고 | 대상 파일:절 | 무엇을 어떻게 고쳤는가 | 근거 |
|---|---|---|---|
| **W-1** [수정] | `spec.md` §8 후속 안건 **FU-7 행** | *"다음 재심 라운드에서 (i)·(ii)를 게이트가 판정한다"* 를 **판정 완료 기록**으로 교체했다 — (i) `loop.Scorer` 는 S1 산출물이 **아님**, (ii) R-02 의 S1 차단 근거는 **유지되지 않으며 올바른 차단 지점은 M3**. 안건을 **종결**로 표기하고, 재심 판정문은 **수정하지 않고 "다음 라운드 판정으로 갱신"** 임을 명시했다. R-02 반영을 되돌리지 않는다는 [HARD] 를 유지했다 | R3 재판정 §4.1~4.2 · 원본 3처 축자 일치(`design-FINAL.md` §7 S1 · `spec.md` U-010.1 · `plan.md` M1.1) · AC-TR-06 분모 6 |
| **W-2** [수정] | `plan.md` M1.0′ **[처방 — P-6]** + `acceptance.md` **AC-M0-12 [왜 필요한가]** | 두 곳의 근거 문장(*"S1 범위에 `loop.Scorer`(F6)가 포함되고 `soft_prefs` 는 Scorer ④(`design-FINAL.md:488`)의 입력이므로 …"*)을 **차단 지점 S1 → M3** 로 교체했다. 정정 문언은 두 곳 **동일** — *"`soft_prefs` 는 `Intent` 필드이고 그 생성원 Actor LLM 은 X-010.2 가 S1 에서 제외하므로, 이 결손이 실제 위험이 되는 지점은 M3 다. 문서 반영은 이미 완료(AC-M0-12 7항)이며, 구현 대조는 M3(AC-AUT-03 ③) 소관이다."* **P-6 자체는 충족 상태로 유지·삭제하지 않았다.** 인용 좌표 `:488` 은 **절 번호 참조(§4.6 Scorer)** 로 교체했다 | R3 재판정 §4.2 (근거 2중 실패) · `spec.md:656` (S1 은 고객 발화 미입력) |
| **W-3** [보완] | `plan.md` M1.0′ **[처방 보강 — P-5]**(신설 3차 블록) + P-5 표 판정 기준 셀 + `plan.md` 미해결 12번 + `acceptance.md` **AC-AUT-03 실행 절**(신설 [실행 시점] 블록) | AC-AUT-03 **①(Scorer 정렬 키 정적 구조 대조)도 M3 이연 대상에 편입**했다. 근거 문장은 §H.4 ① 갈래 (B) 와 **축자 동형**으로 썼다 — *"① 은 `simcore/` 의 Scorer 구현 실재를 전제하는데 `loop.Scorer` 는 U-010.1 의 S1 6산출물에 없다. 따라서 ①②③ 전건을 M3 에서 실행하며, S1 에서 AC-AUT-03 를 '통과'로 쓰지 않고 미검증으로 표기한다."* P-5 판정 기준 셀에 **"S1 판정은 AC-AUT-02 단독으로 낸다"** 를 명시해 내부 정합을 맞췄다 | R3 재판정 §5 관찰 2 · `acceptance.md:571` 의 ① 실행 정의 · 대가 기록은 `spec.md:662`·`acceptance.md:716` 범위 확장 |
| **W-4** [수정] **★이번 반영의 핵심** | `plan.md`(M1.0′ 현황 갱신 블록 · 미해결 6번) · `spec.md`(§9.1 표 + 하단 [HARD] 2곳) · `acceptance.md`(§F 착수 차단 조건 3항 + **AC-M0-10 현 상태 신설**) | *"해제됐다"* 를 실제 상태로 **강등**했다. 정정 문언(4곳 동일) — ***"차단 요인 부재가 전수 실측으로 확인됐다(FEASIBLE). 그러나 AC-M0-10 이 요구하는 생성·폐기·대조·리플레이 각 1회의 실증은 아직 없다 — 리허설 1회가 남았고, 그때까지 P-1~P-4 는 미충족이다."*** `AC-M0-10` 에 **항별 판정(P-1~P-4 전건 미충족 + 각 사유)** 을 신설했다. `plan.md` 가 이미 *"리허설 1회가 남은 공백"* 이라 정확히 적은 것과 문장 강도를 일치시켰다 | R3 재판정 §4.3 · `g6-clone-feasibility.md:292`·`:311`(자기 자백 2회) · `acceptance.md:119` 기대 문언 |
| **W-5** [추가] | `plan.md` M1.0′ — **P-0 clone 리허설 1회** 신설 (표 아래 전용 절) | S1 을 막는 유일 조건을 **명시적 선행 작업 단위**로 승격했다. 절차 5항을 `g6-clone-feasibility.md:300`·`:301`·`:302`·`:303` 그대로 적었다 — ① `brew install postgresql@18` ② `pg_dump`(읽기전용) → 로컬 `createdb`/`pg_restore` ③ **동일성 검사 분모 = `t_*` 46개**(`bak_*` 97 · Django 10 제외 + **제외 근거 한 쌍 기록**) ④ **시점 규율**(*"clone 생성 직후 · 리플레이 이전 · 덤프 시각 기준 라이브 스냅샷 대비"* — `fn_upd_dt` 트리거 45개) ⑤ `dropdb`. **사용자 승인 필요를 [HARD] 로 명기**했다. P-0 ↔ P-1~P-4 대응도 명시했다 | R3 재판정 §⑥ W-5 · `g6-clone-feasibility.md:279`·`:300`~`:303` |

### 우선순위 Medium (W-6 ~ W-8)

| 권고 | 대상 파일:절 | 무엇을 어떻게 고쳤는가 | 근거 |
|---|---|---|---|
| **W-6** [보완] | `design-FINAL.md` 본문 4곳 — ★R-02 절 · §4.0 **F18 행** · **F21 행** · **F22 행** | 구 판본(1,046행) 좌표 4건을 **절 번호 참조로 교체**하고, 각 자리에 *"구 인용 `:NNN` 은 구 판본 좌표이며 현행 `:NNN` 은 <다른 문단>"* 을 병기했다 — `:488`→**§4.6 Scorer** · `:740`→**§8 RULE-05 행** · `:459`→**§4.5** · `:725`→**§7 게이트표 G10 행**. 판정 기록 인용(`gate-rehearing.md:NNN` 등)은 **손대지 않았다** | R3 재판정 §5 관찰 1(표 형태로 진타겟 수동 확정) · `design-FINAL.md:50` 좌표 규약 |
| **W-7** [보완] | `design-FINAL.md` §7 **S1** — `★ W-7` 문단 신설 | `loop.Scorer`(F6)와 `Intent`/`IntentValidator`(F7·F9)의 **S1 지위를 표로 명시**했다(둘 다 S1 산출물 아님 · 실제 배정 = S2 이후 / S3). 그리고 **G9 가 S1 에서 "루프 경로"를 무엇으로 실행하는지 1문단으로 규정**했다 — ③`Scorer`·④추천이 S1 에 없으므로 (가) 루프 경로는 **①`frontier` → ②`rollout` → ⑤관측** 으로 실행하고 ③④는 **미실행으로 명시 기록**하며, 분모 *"골든 의도 20건"* 은 S1 에서 **골든 `selections` 상태 20건**을 뜻함(스키마 검증 없는 직접 상태 투입)을 명시했다 | R3 재판정 §5 관찰 4 + FU-7 뿌리 · `spec.md:656` · `design-FINAL.md` §7 S3 배정 |
| **W-8** [확장] | `design-FINAL.md` §4.1 — 스키마 주석 + 3분류표 **(다) 행** + `★ W-8` 문단 신설 + **§8-1 12번 행 병기** / `spec.md` §3.2 **U-002.9** 말미 | `soft_prefs[].w` 를 **(다) 축 아닌 스칼라**로 편입하고 값 도메인을 닫았다 — **`0.0 ≤ w ≤ 1.0` 유한 실수 · `NaN`/`inf` 는 스키마 위반(되묻기 fail-closed)**. 근거(`pref_score` 가 정렬 키 ④ 에 들어가므로 비유한 값은 **G3/RULE-17 정렬 결정론**을 깬다)를 함께 적었다. **`IntentValidator` 기존 1행에 편입 · §8-1 12번 행에 병기**해 **평가 지점 수를 늘리지 않았다**(13지점 불변) | R3 재판정 §2.1 잠재 등재 1건(선제 해소) · `dir` 은 이미 리터럴로 닫혀 결손은 `w` 1건뿐 |

### 우선순위 Low (W-9 · W-10)

| 권고 | 대상 파일:절 | 무엇을 어떻게 고쳤는가 | 근거 |
|---|---|---|---|
| **W-9** [확장] | `spec.md` §8 — 개정안 ② 사례표에 **②-사례5·6·7 신설** | R3 재판정 §5 관찰 1·3·5 를 개정안 ②(신설 RULE-19 문서 내부 정합)의 **추가 실측 사례**로 등재했다 — 사례5(구 판본 좌표 4건 · W-6 으로 교정 완료) · 사례6(AC-M0-10 두 강도 서술 병존 · W-4 로 교정 완료) · 사례7(AC-M0-12 3항 기대값 이동 · **교정 대상 아님, 인접 사례 등재만**). **등재만 하고 등급은 내지 않았다.** 개정안 ② 가 **4개 라운드 누적 사례를 가진 유일한 축**이라는 심의 우선순위 근거를 명시했다 | R3 재판정 §3.1 · §5 |
| **W-10** [개선] | `spec.md` §8 후속 안건 **FU-9 신설** (+ `plan.md` 후속 안건 FU-6 포인터) | 문서 교정이 아니라 **라운드 설계 사안**이므로 후속 안건으로 등재했다 — ① 조건 ⑧ 심의(최소 개정안 ②④) **선행**, ② 라운드 범위를 *"지정 N규칙의 지정 `violation_test` 적용"* 으로 고정, ③ **범위 밖 실체 결함은 별도 트레이로 수집**(억지 매핑 금지 · 버리기 금지). 정밀도 변동(`0.39` → `1.00` → `1.00` → 정의 불가)이 **설계 품질이 아니라 프롬프트 범위의 함수**라는 진단을 함께 적었다. **게이트·루브릭 트랙 소관이며 본 SPEC 이 실행할 수 없음**을 명시했다 | R3 재판정 §3.2 · 세 라운드 연속 자백 |

---

## ② 인용 좌표 교정 — **수동 확정분만** (규율 2 준수)

[HARD] **후보 좌표를 확정처럼 써넣지 않았다.** 아래는 R3 기계 실측이 **원문 대사로 수동 확정**한 것만이다. 스크립트가 제시한 "현재 위치 후보"는 **하나도 채택하지 않았다.**

### ②-1 교정한 것 (수동 확정)

| # | 위치 | 구 인용 | 처리 | 근거 |
|---|---|---|---|---|
| 1 | `spec.md` §2.3 고정 선언 | `design-FINAL.md:17` | → **§0 고정 선언(현행 76-77행)** | 스캔 사례표 — *"사이드카 저장소만"* 문구 현재 76-77행 |
| 2 | `spec.md` §3.2 U-002.5 | `design-FINAL.md:324`·`:789`·`:791` | → **§4.1 ★FIX-1 부기(356-357행)** · **§9 주장 1(926행)** | 스캔 사례표 3건 — 현행 `:324` 는 Intent `provenance` 스키마, `:789`·`:791` 은 §7 헤딩 |
| 3 | `spec.md` §3.4 U-004.4 | `widget_api.py:1148` | → **`:1150`** | 스캔이 py 인용 48행을 전수 대사해 **진짜 드리프트로 확정한 유일한 py 좌표** (`VAT_RATE = Decimal("0.1")` 정의는 1150행) |
| 4 | `spec.md` §7 채무표 R-18 행 | `:740` · `:281` | → **§8 RULE-05/RULE-18 행(871·884행)** · **§4.0 자기 선언(301행)** | 스캔 사례표 + 암묵 인용 귀속표 |
| 5 | `acceptance.md` AC-TR-01 | `widget_api.py:1148` | → **`:1150`** | 위 3과 동일 |
| 6 | `acceptance.md` AC-STR-03 | ``:740``(암묵 · 파일 미명시) | → **`design-FINAL.md` §8 RULE-05/RULE-18 행(871·884행)** | 스캔 UNRESOLVED 귀속표 1건 |
| 7 | `plan.md` M0.4-b | `:306` · `:622` | **병기**(치환 아님) → 현행 **§4.3 성질 1(450행)** · **§5.6-8번(903행)** | 스캔 MISSING/MISMATCH 재판정표 2건 |
| 8 | `plan.md` M0.5-a | `:389` · `:601` | **병기** → 현행 **§4.7 ★FIX-6 `esc_kind` 7종(639행)** | 스캔 UNRESOLVED 귀속표 2건 |
| 9 | `plan.md` M0.5-b | `:393-399` · `:565-567` | **병기** → 현행 **656-658행** · **647-649행**(둘 다 §4.7) | 스캔 UNRESOLVED 귀속표 2건 |
| 10 | `plan.md` M0.5-e | `:51` · `:598` | **병기** → 현행 **§4.3 ★FIX-7 폴백 사다리(454행)** · **§8 RULE-11 행(877행)** | 스캔 UNRESOLVED 귀속표 2건 |
| 11 | `plan.md` M0.7-c | `spec.md:393`·`:395`·`:181` | → **`spec.md:529`(RK-11)** · **`spec.md:162`(U-007.3)** | 스캔 수동 확인 plan→spec 3건 |
| 12 | `plan.md` M0.7-f | `spec.md:489` | → **`spec.md:86`(U-001.5)·`:124`(U-004.5)** | 스캔 MISMATCH 재판정 — LINE_DRIFT(수동) |
| 13 | `design-FINAL.md` 본문 4곳 | `:488`·`:740`·`:459`·`:725` | → **절 번호 참조**(W-6) | R3 재판정 §5 관찰 1 표 |

**[중요] 7~10 은 치환이 아니라 병기다.** `plan.md` 머리의 [HARD] 좌표 규약이 *"M0 의 FIX 작업 행에 적힌 좌표 = 개정 전(804행) 판본 좌표이며, 그 행들은 '어디를 고칠 것인가'를 지목하므로 교정 대상 좌표를 보존한다"* 로 못 박고 있다. 치환하면 그 규약을 깬다. 따라서 **구 좌표를 보존한 채 "[좌표 귀속] 개정 전 판본 좌표이며 현행 대응 위치는 …" 을 병기**해, 규약 보존과 귀속 해소를 동시에 달성했다.

### ②-2 교정하지 **않은** 것 (진타겟 특정 불가 — 지어내지 않음)

| 위치 | 인용 | 왜 고치지 않았는가 |
|---|---|---|
| `plan.md` M0.7-f | `spec.md:178` | 인용 문언 `PICK→PR→RG` 가 현행 `spec.md` 전체에 **0건**(전수 grep). 개정으로 문언 자체가 변형된 것으로 보이며(U-001.5 로 흡수 `[추정]`) **진타겟을 특정할 수 없다.** 좌표를 지어내지 않고 **미해소로 남긴다는 사실을 그 자리에 명시**했다 |

---

## ③ `sql/33_fn_best_plate.sql` 인용 3건 — 규율 3 처리 결과

**결론: 이 3건은 `sql/33_fn_best_plate.sql` 인용이 아니었다. 스캔의 파일 귀속 오류이며, `spec.md` 는 수정하지 않았다.**

기계 실측 §④ 6b 는 *"SPEC(`spec.md:323`)의 `:114`·`:115`·`:170-174` 인용은 60행 파일이라 줄 부재"* 로 적었다. 원본을 직접 열어 대조한 결과:

| 실측 | 명령 | 관측 |
|---|---|---|
| `spec.md:323` 의 실제 문언 | `sed -n '323p' spec.md` | `- **source**: `R7:71`, `:114`, `:115`, `:170-174`` — **RULE-17(재현 결정론)의 source 행**이며, 선행 토큰이 **`R7:71`** 이다. 뒤따르는 bare `:NNN` 은 **`R7-agentic-commerce.md` 의 연속 인용**이다 |
| `R7` 행수 | `wc -l R7-agentic-commerce.md` | **213행** — `:170-174` 실재 |
| `R7:71`·`:114`·`:115`·`:170-174` 내용 | `sed -n '71p;114p;115p;170,174p'` | 71행 = *"재현성 붕괴 · τ-bench pass^1/pass^8 · 상태 기반 채점"* · 114-115행 = *"결정론은 모델이 아니라 엔진 쪽에서 확보해야 한다"* / *"τ-bench 는 대화가 아니라 최종 DB 상태를 비교"* · 170-174행 = *"골든 케이스 × k회 재실행 → 최종 상태 완전일치율"*. **전건이 RULE-17 의 norm·violation_test 와 축자 대응한다** |
| `sql/33` 의 실제 SPEC 인용 | `grep -n '33_fn_best_plate' spec.md` | `spec.md:28`(파일 참조, 줄 없음) · **`spec.md:318`(RULE-16 source) = `sql/33_fn_best_plate.sql:48`** |
| `sql/33:48` 실재 | `wc -l` + `sed -n '44,52p'` | 60행 파일이며 **48행 실재** — `WHERE pp.prd_cd = p_prd_cd AND pp.del_yn = 'N'` (판면효율 계산 블록). RULE-16(*"원천 없는 결과축 도입 금지"* — 판면효율 원천)의 근거로 **정합** |

**판정**: 스캔의 정규식이 직전에 본 파일 경로(`sql/33_fn_best_plate.sql`, `spec.md:318`)를 다음 `source:` 행의 bare `:NNN` 에 **carry-over** 시킨 파일 귀속 오류다. `sql/33` 관련 실제 인용(`:48`)은 **좌표·내용 모두 정확**하다. 따라서 **갱신할 좌표가 없고, 지어낼 좌표도 없다.** `spec.md:323` 은 원본 그대로 둔다.

---

## ④ 반영하지 **않은** 것과 그 사유

| # | 항목 | 왜 반영하지 않았는가 |
|---|---|---|
| 1 | **W-7 의 "목록에 명시 추가" 갈래** | W-7 은 *"'S1 에 넣지 않는 것' 목록에 `loop.Scorer` 와 `Intent`/`IntentValidator` 를 **명시 추가하거나**, 추가하지 않는다면 G9 실행을 1문단으로 규정"* 의 **택일**을 제시했다. **후자를 택했다.** 이유: 그 목록은 `spec.md` **X-010.2** 와 대응하고 그것이 **`AC-TR-06` 의 "금지 심볼 5종" 분모**다. 항목을 더하면 분모가 함께 움직여 **범위 확대**가 되는데, W-7 자신이 *"U-010.1 의 6산출물 목록과 AC-TR-06 분모 6 은 바꾸지 않는다"* 를 요구했다. 대신 **지위 명시 문단 + G9 실행 규정**을 함께 넣어 W-7 의 두 요구를 모두 충족시켰다(목록은 불변) |
| 2 | **`acceptance.md` 에 `w` 도메인 AC 신설** | W-8 은 `design-FINAL.md` §4.1 만 대상으로 했다. AC 를 신설하면 §H 커버리지 집계(직접 51 · 간접 9 · 미커버 4 · 총계 64)와 AC-M0-12/AC-AUT-03 분모가 움직인다. **문서 내부 정합만 확보**하는 선에서 `spec.md` U-002.9 말미에 **기존 ID 안의 문장으로만** 추가했다(새 요구사항 ID 0건 · 분모 64 불변) |
| 3 | **`evaluation-rubric.md` 개정** | 어느 권고에서도 대상이 아니다. 판정받는 쪽이 기준을 고치면 사전선언이 무의미해진다. 개정안 ②의 사례 3건은 **등재**이며 개정이 아니다 |
| 4 | **재심 판정문(`gate-rehearing.md`) 전제 수정** | 판정 기록이므로 사후 변경 대상이 아니다. W-1 반영은 그 전제를 **덮어쓰지 않고 "다음 라운드 판정으로 갱신"** 하는 형식으로 썼다 |
| 5 | **`STILL_OPEN` / 채무 상태의 해제** | R-10 은 여전히 **S2 착수 전 · 구현 미착수**이고, `STILL_OPEN` 해제 권한은 게이트에 있다. 상태 문언을 건드리지 않았다 |
| 6 | **`status:` frontmatter 전이** | 지시대로 `draft` 유지. 단계 전이는 lead 판단이다 |
| 7 | **커밋** | sync 칼럼 소관이므로 커밋하지 않았다 |

---

## ⑤ 미교정 좌표 74건 처리 방침

**방침: 손대지 않고 후속 안건으로 등재했다.** 등재처는 **`spec.md` §8 FU-8**(본문) + **`plan.md` 후속 안건 FU-6**(포인터, 중복 등재 아님).

- **규모**: 기계 실측이 SPEC 3종 인용 **586건**을 전수 추출해 `LINE_DRIFT` **157건**(design-FINAL 대상 **84건**이 최대 군집)을 집계했고, 그중 **수동 확정은 design-FINAL 13건**(사례표 6 + 암묵 인용 7)이다. 잔여 **71건 + 기타 = 약 74건**이 미교정이다.
- **왜 손대지 않는가**: 스캔의 "현재 위치 후보"는 **토큰·문구 부분문자열 매칭 결과**이지 원문 대사로 확정된 값이 아니다. 스캔 자신이 Gaps 1·3 에서 *"md 대상 LINE_DRIFT 의 오탐률은 미검증"*·*"LINE_DRIFT 표의 '현재 위치 후보'는 후보일 뿐, 수동 확정 열에 있는 값만 확정"* 이라 못 박았다. py 대상은 전수 대사 결과 **오탐 95.8%**(48행 중 진짜 2행)로 실측됐다 — **후보를 확정처럼 써넣으면 정확한 좌표를 틀린 좌표로 바꾸는 역효과**가 난다.
- **근본 해소 방향**: 개별 좌표 추적이 아니라 **좌표계를 절 번호 참조로 통일**하는 것이다(`design-FINAL.md:50` 이 이미 정한 방식). 문서가 다음 개정에서 1,203행을 넘으면 좌표계가 셋이 되므로(재판정 §⑧ 잔여위험 4), 그 전에 통일하지 않으면 이 유형은 계속 늘어난다. **문서 전반 개정이므로 별도 안건**이다.

---

## ⑥ 무엇을 실행해 무엇을 관측했는가 (증거)

| # | 명령 | 관측 |
|---|---|---|
| 1 | `git status --short \| head -50` | 다른 세션의 변경분 존재 확인(`.claude/**` 대량 삭제·수정 · `.codex/config.toml` 등). **본 카드는 그 경로를 건드리지 않았고 커밋도 하지 않았다** |
| 2 | `wc -l raw/webadmin/sql/33_fn_best_plate.sql` | `60` — 스캔 보고와 일치 |
| 3 | `sed -n '44,52p' raw/webadmin/sql/33_fn_best_plate.sql` | 48행 실재(판면적 계산 블록). `spec.md:318` 의 `:48` 인용 정합 |
| 4 | `wc -l _workspace/huni-worldmodel/01_research/R7-agentic-commerce.md` | `213` — `:114`·`:115`·`:170-174` 전건 실재 |
| 5 | `sed -n '71p;114p;115p;170,174p' …/R7-agentic-commerce.md` | 전건이 RULE-17 문언과 축자 대응 → **③절 판정의 근거** |
| 6 | `grep -n ':488\|`:740`\|`:459`\|`:725`' design-FINAL.md` | W-6 대상 4곳의 현행 행번호 확정(295 · 298 · 299 · 372) |
| 7 | `grep -n '^#\{2,4\} ' design-FINAL.md` | 절 번호 ↔ 행번호 대조표 확보(§4.5=573 · §4.6=606 · §7 게이트표=835 · §8=863) → W-6 의 절 번호 참조가 **실재 절을 가리키는지 확인** |
| 8 | `sed -n '854,858p;871p' design-FINAL.md` | `:725`→§7 G10 행(856) · `:740`→§8 RULE-05 행(871) 진타겟 확인 |
| 9 | `grep -n '해제' spec.md plan.md acceptance.md` (반영 후) | 잔여 매치 2건은 **모두 "개정 전 문언은 …였다" 인용 문맥**이며 주장 문언으로 남은 것 0건 |
| 10 | `grep -n 'widget_api.py:1148' …` (반영 후) | 잔여 매치 1건은 **FU-8 의 교정 이력 서술 문맥**뿐 |
| 11 | `sed -n '5,7p' spec.md` · `head -1 plan.md acceptance.md` | `status: draft` 유지 확인 · `updated: 2026-08-16`(= 오늘) 확인 · **plan.md·acceptance.md 는 frontmatter 자체가 없음** 확인 |
| 12 | `ls -lT _workspace/huni-worldmodel/05_gate/ …/evaluation-rubric.md …/R3-{adversarial-review,mechanical-scan}.md` | **판정 기록 무수정 실측** — `05_gate/*` 전건(`gate-report.md` 8/15 16:11 · `gate-rehearing.md` 8/16 00:14 · `coverage-supplement*.md` 8/15 · `g6-clone-feasibility.md` 8/15 19:30 · 렌즈 산출물 6종 8/15~16) · `evaluation-rubric.md`(8/15 15:00) · R3 입력 2종(8/16 17:46·18:02) **모두 이번 세션 편집 시각(18:09~18:13) 이전 mtime 으로 남아 있다 = 손대지 않았다.** ※ `_workspace/**` 와 `.moai/specs/**` 는 git 미추적(`??`)이라 `git diff` 로는 대조할 수 없어 mtime 을 증거로 삼았다 |

**`updated:` frontmatter 처리**: SPEC 3종 중 frontmatter 를 가진 것은 **`spec.md` 뿐**이며, 그 값은 이미 **`updated: 2026-08-16`**(= 본 반영일)이므로 **변경할 것이 없었다.** `plan.md`·`acceptance.md` 는 frontmatter 블록이 존재하지 않는다(둘 다 `# ` 제목으로 시작). 없는 필드를 만들지 않았다. `status:` 는 지시대로 `draft` 유지.

---

## ⑦ Gaps — 이 반영이 하지 못한 것

1. **구현 검증이 아니다.** `_workspace/huni-simcore/` 는 실재하지 않는다. W-7·W-8 의 반영은 전부 **명세 문언의 반영**이며 구현·측정의 통과가 아니다. R-02/S3 · R-10/S2 · P-7/S1 구현 트리거는 **그대로 살아 있다**.
2. **라이브 DB 를 조회하지 않았다.** `rule_nm` 중복 실재 여부, `nm` 이 `"제약"` 으로 떨어지는 행 수, A5 오차 규모는 **여전히 미측정**이다. 미측정을 통과의 증거로도 결함으로도 쓰지 않았다.
3. **미교정 좌표 74건은 검증하지 않았다.** 후보 좌표의 정오를 판정하지 않았고, **미교정이라는 사실만 등재**했다(§⑤). 그 74건 중 실제로 몇 건이 진짜 드리프트인지는 **모른다**.
4. **W-9 의 사례 등재는 등급이 아니다.** 사례5·6·7 은 루브릭 대응 규칙이 없어 **위반/비위반 어느 등급도 부여하지 않았다.** 등재가 곧 판정이 아니다.
5. **W-10 은 실행하지 않았다.** 라운드 프롬프트 설계는 게이트·루브릭 트랙 소관이며, 본 카드는 **후속 안건 등재까지만** 했다.
6. **반영의 정합성을 독립 검증하지 않았다.** 이 원장은 **반영자 자신의 기록**이다. 반영이 R3 권고를 정확히 구현했는지, 그리고 이번 편집이 새 내부 불일치를 만들지 않았는지는 **검증측(게이트·다음 라운드) 소관**이다 — 생성 ≠ 검증.
7. **단일 판정자·단일 반영자 라운드다.** codex 를 붙이지 않았다. 특히 §④-1(W-7 택일 판단)과 §③(스캔 파일 귀속 오류 판정)은 **반영자 1인의 해석**이며 독립 렌즈가 갈릴 수 있다.

---

Sources: 웹 검색을 사용하지 않았다. 본 원장의 모든 근거는 저장소 내부 `파일경로:라인` 이며 외부 URL 인용이 없으므로 검증된 URL 목록 절을 두지 않는다.
