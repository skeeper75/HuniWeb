# t66 — 지금 바로 할 수 있는 일 (결정·외부회신 없이)

네 카드의 빠진 곳 525건 중 **선행이 비었거나 「없음 — 바로 착수 가능」이라고 적힌 것**만 골랐다. 종류 「라(결정 미정)」는 성격상 제외했고, 선행 문장에 외부·회신·계약·구매·심사·스펙·승인·결정이 들어간 줄도 뺐다. **고른 규칙은 `build.py:build_quickwins` 한 곳에 있다.**

**133건** — 담당자별로 아래와 같다. 각 줄의 문장은 네 카드가 쓴 그대로다.

| 담당 | 건수 |
|---|---:|
| 서희항 | 50 |
| 최숙진 | 30 |
| 김동학 | 24 |
| 미정 | 20 |
| 최숙진+서희항 | 5 |
| 대표 | 1 |
| 신우진(PM) | 1 |
| 신우진(PM)+최숙진 | 1 |
| 최숙진+김동학 | 1 |
| **합계** | **133** |

> 주의 — 「선행이 없다」는 **그 카드가 선행 칸을 비웠다**는 뜻이다. 일의 크기를 뜻하지 않고, 일정·작업량 추정은 하지 않는다.

---

## 서희항 — 50건

| # | 카드·프로세스 | 종류 | 할 일 | 관련 row_id |
|---:|---|---|---|---|
| 1 | `t64` P02 원고승격 · 보관 | 나 | `STD-MFG-007` — 보류(HOLD) 주문 목록 화면. 상태를 만드는 코드는 있는데 그 상태를 보는 화면이 없다 | STD-MFG-007 |
| 2 | `t64` P04 원고보완 · 재업로드 | 가 | 서명 링크 만료·재발급 경로 — 7일이 지나거나 이미 쓴 링크로 고객이 들어왔을 때 무엇을 보여 주고 누가 재발급하는지가 행으로 없다. `STD-MFG-054` 는 발급만 말한다 | STD-MFG-054 |
| 3 | `t64` P04 원고보완 · 재업로드 | 가 | 재업로드 화면의 비로그인 접근 정책 — 비회원·로그아웃 고객이 링크로 들어오는 경로가 행으로 없다 | — |
| 4 | `t64` P04 원고보완 · 재업로드 | 나 | `STD-MFG-054`·`055`·`056`·`057`·`016`·`125`·`126` · `STD-ART-018` · `STD-ADO-009` — 9행 전부 코드 0. | STD-MFG-054;STD-ART-018;STD-ADO-009 |
| 5 | `t64` P04 원고보완 · 재업로드 | 다 | 위젯 업로드 컴포넌트 — 최초 업로드용으로 살아 있다(`STD-ART-001` 작동). 재사용 배선만 남았다 | STD-ART-001 |
| 6 | `t64` P05 접수파일등록 · 조판 | 가 | 우리 판걸이 값 → MES 조판 전달 경로 — `STD-ADO-014` 는 "산출"까지만 말한다. 그 값이 MES-1 payload 에 실리는지, MES 가 다시 계산하는지가 행으로 없다. 같은 수를 두 곳에서 계산하면 반드시 어긋난다 | STD-ADO-014 |
| 7 | `t64` P05 접수파일등록 · 조판 | 다 | 판걸이 계산(`STD-ADO-014` 작동) — webadmin 가격엔진 안에서만 쓰인다. 생산 쪽으로 나가는 통로가 없다 | STD-ADO-014 |
| 8 | `t64` P06 MES주문접수전송 | 가 | 품목코드 미매핑 주문의 처리 — 289 중 15만 채워진 상태에서 나머지 주문이 들어오면 무엇을 하는지(보류? 거부? 기본값?)가 행으로 없다. `STD-MFG-010` 은 적재만 말한다 | STD-MFG-010 |
| 9 | `t64` P06 MES주문접수전송 | 나 | `STD-MFG-013`·`014`·`018`·`019`·`020`·`021`·`012`·`131` · `STD-ADO-015` — 9행. 그중 `STD-MFG-020`(SQS)은 환경변수까지만 준비돼 있다. | STD-MFG-013;STD-ADO-015;STD-MFG-020 |
| 10 | `t64` P06 MES주문접수전송 | 다 | `MES_ITEM_CD` 컬럼 + 중복검증 폼 — 입력 UI 는 상품 편집 화면에 실재한다(`admin.py:2710`). 값이 289 중 15 뿐이고, 목록 화면이 없어 현황이 안 보인다 | — |
| 11 | `t64` P06 MES주문접수전송 | 다 | `handoff_id` — 발급·로그·정리까지 살아 있다(`widget_api.py:3932`·`3959`·`3996`·`4726`·`5268`). MES 로 나가는 자리가 없다 | — |
| 12 | `t64` P07 생산공정진행 | 가 | `mand_proc_yn` NULL 교정 — 책자 5상품의 필수공정여부가 전건 NULL 이라는 별도 카드(t3)가 있으나, 그것이 생산 라우트에 미치는 영향은 행이 없다 | — |
| 13 | `t64` P09 생산상태 · 역방향동기화 | 가 | 상태 단위 전환(주문 → 주문상품옵션) — `STD-MFG-023` 은 "단위는 주문상품옵션"이라고 선언만 한다. 지금 `ord_sts` 가 주문 단위라는 사실과 그 마이그레이션이 행으로 없다. 한 주문에 명함+스티커가 담기면 지금 구조로는 상태가 하나다 | STD-MFG-023 |
| 14 | `t64` P09 생산상태 · 역방향동기화 | 가 | 보정 폴링(대사) — 설계 §9.1 3중 방어의 ③ — 30분 주기로 최근 N일 주문을 훑는 것. `STD-MFG-021`(DLQ)과 별개인데 행이 없다 | STD-MFG-021 |
| 15 | `t64` P09 생산상태 · 역방향동기화 | 가 | TO-1 주문 조회 재확인(fail-closed) — 설계 §9.2 의 핵심 원칙("웹훅을 믿지 않고 조회로 재확인")인데 행이 없다 | — |
| 16 | `t64` P09 생산상태 · 역방향동기화 | 나 | `STD-MFG-025`·`027`·`030`·`034`·`018`·`136` — 6행. | STD-MFG-025 |
| 17 | `t64` P09 생산상태 · 역방향동기화 | 다 | 상태 상수 — 넷(`REGISTERED`/`PROMOTING`/`PROMOTED`/`HOLD`)이 실제로 돌고 있다. 그 뒤 아홉 칸이 비어 있다 | — |
| 18 | `t64` P09 생산상태 · 역방향동기화 | 다 | `ORD_OWNED` 가드 — 승격이 자기 범위 밖 상태를 덮어쓰지 않게 막는 장치가 이미 있다(`artwork_promote.py:56`). 뒤 상태가 생기면 그대로 쓰인다 | — |
| 19 | `t64` P09 생산상태 · 역방향동기화 | 다 | 샵바이 API 클라이언트 — `catalog/shopby_client.py` 가 있고 상품 계열 호출이 돈다. 주문·배송·클레임 계열만 안 쓰인다 | — |
| 20 | `t64` P10 포장 · 출고 · 송장 | 가 | 송장 정정 경로 — 명세에 `PUT /orders/update-invoices` 가 있는데 잘못 찍은 송장을 고치는 업무 흐름이 행으로 없다 | — |
| 21 | `t64` P11 배송추적 · 배송지변경 | 가 | 출고 전/후 판정 기준 — `STD-MFG-017` 은 "출고 전 한정"이라고만 한다. 어느 상태까지가 출고 전인지(`MES_SENT`? `IN_PRODUCTION`? `PRODUCED`?)가 행으로 없다 | STD-MFG-017 |
| 22 | `t64` P11 배송추적 · 배송지변경 | 나 | `STD-MFG-017` — MES-4 통보. 이 프로세스의 t64 담당 행 전부(1행) | STD-MFG-017 |
| 23 | `t64` P11 배송추적 · 배송지변경 | 다 | 웹훅 수신함 — `UPDATE_RECEIVER` 가 들어오면 저장은 된다. 읽는 쪽이 없다 | — |
| 24 | `t64` P12 재제작 · 반송 · 재출고 | 가 | 재제작 시 원고 회차 — 파일 오류로 재제작하면 새 회차인가 같은 회차인가? `STD-MFG-008`(회차 규약)과 이 프로세스가 연결된 행이 없다 | STD-MFG-008 |
| 25 | `t64` P12 재제작 · 반송 · 재출고 | 다 | 클레임 API 명세 — `claim.mdx` 에 취소·반품·교환·수거·승인·철회가 전부 문서화돼 있다(`:32`·`:72`·`:171`·`:216`·`:239`·`:292`·`:362`·`:381`·`:439`). 호출 코드 0건 | — |
| 26 | `t64` P13 생산구간 · 주문취소 | 가 | 취소 경합 알림 채널 — `STD-MFG-033` 은 "에스컬레이션"이라고만 한다. 누구에게 어떻게 알리는지가 행으로 없다 | STD-MFG-033 |
| 27 | `t64` P13 생산구간 · 주문취소 | 다 | 클레임 API 명세 — 9개 엔드포인트가 전부 문서화돼 있다(P12 §5 다 참조) | — |
| 28 | `t64` P13 생산구간 · 주문취소 | 다 | `ORD_OWNED` 가드 — 승격이 취소된 주문을 되돌리지 않게 막는 장치가 있다(`artwork_promote.py:56`) — 경합 방어의 선례가 이미 코드에 있다 | — |
| 29 | `t64` P14 운영자 · 주문운영 | 가 | 주문번호로 사양·원고를 여는 우리 화면 — `STD-ADO-002` 는 "주문 상세 조회(사양·파일·금액)"라고 하나로 묶어 말한다. 실제로는 금액(샵바이)과 사양·파일(우리)이 다른 화면이고, 우리 쪽 화면이 통째로 없다. 별도 행으로 서야 한다 | STD-ADO-002 |
| 30 | `t64` P14 운영자 · 주문운영 | 가 | 세 시스템을 잇는 운영자 진입점 — 운영자가 주문번호 하나로 샵바이·우리·MES 를 오가는 방법이 행으로 없다. `STD-MFG-024`(3시스템 매핑)는 데이터 얘기지 화면 얘기가 아니다 | STD-MFG-024 |
| 31 | `t64` P14 운영자 · 주문운영 | 나 | `STD-MFG-007` — 보류 주문 목록 화면 | STD-MFG-007 |
| 32 | `t64` P14 운영자 · 주문운영 | 나 | `STD-ADO-002`(우리 몫) — 주문 단위 사양·원고 조회 화면 | STD-ADO-002 |
| 33 | `t64` P14 운영자 · 주문운영 | 다 | Django admin 자동등록 — `catalog/admin.py:2626` 이 앱 모델을 전부 자동 등록하는데 `TOrd*` 계열은 등록되지 않았다 | — |
| 34 | `t64` P15 생산현황 · 산출데이터 | 가 | 산출 데이터의 원천 주문 단위 — 주문 단위인가 주문상품옵션 단위인가 작업 단위인가. `STD-MFG-023`(상태 단위)과 이어지는데 행이 없다 | STD-MFG-023 |
| 35 | `t64` P15 생산현황 · 산출데이터 | 가 | 우리 대시보드 ↔ MES 현황표의 경계 — `STD-MFG-131`(우리)과 `094`(MES)가 비슷한 것을 각자 만든다. 무엇을 어디서 보는지가 행으로 없다 | STD-MFG-131 |
| 36 | `t65` P12 기초데이터 마스터 등록·관리 | 다 | 여섯 화면이 각자 살아 있어 「이 코드를 쓰는 상품이 몇 건인가」를 한 화면에서 못 본다. 사용처 조회는 가격 구성요소 쪽에만 있다(`price-viewer/comp/{comp_cd}/usage/`). | G-t65-035 |
| 37 | `t65` P13 판형·제본·공정 규칙 관리 | 다 | 공정 택일그룹 모델이 제거되고 옵션그룹으로 흡수됐는데(`catalog/admin.py:1595` 「(제거됨 — Phase 7/07-08) 공정 택일그룹 모델: 옵션그룹으로 흡수·DROP」), 흡수 뒤 기존 택일 제약이 옵션그룹에서 같은 효과를 내는지 실화면으로 확인된 기록을 찾지 못했다. 원장 상태도 「구현-미검증」이다. | G-t65-036 |
| 38 | `t65` P13 판형·제본·공정 규칙 관리 | 다 | 책등 계산(`catalog/spine_calc.py`)이 구현돼 있으나 「구현-미검증」이다. 책등폭 → 표지 크기 → 그보다 큰 판형 선택이라는 규칙이 실제 상품에서 재현되는지 확인이 남았다. | G-t65-037 |
| 39 | `t65` P16 옵션그룹 구성·차원 연결·무결성 검증 | 다 | 옵션그룹 CRUD(`config/urls.py:356-360`)와 유효성 검증(`:373`)이 구현돼 있으나 둘 다 원장 상태가 「구현-미검증」이다. 검증이 무엇을 잡고 무엇을 놓치는지 실화면 기록을 찾지 못했다. | G-t65-045 |
| 40 | `t65` P16 옵션그룹 구성·차원 연결·무결성 검증 | 나 | 유효성 검증 결과를 정기적으로 돌려 보는 절차가 없다. 지금은 사람이 생각날 때 누른다. | G-t65-047 |
| 41 | `t65` P17 제약규칙 등록·시뮬레이션 | 다 | 제약규칙 관리(`config/urls.py:371` JSONLogic)와 모델(`catalog/models.py:801`), 시뮬레이션(`config/urls.py:169` `price_sim_constraints` — 「제약 cascade: 현재 선택 → 각 차원 불가값(비활성)」)이 다 구현돼 있는데 둘 다 「구현-미검증」이다. 만든 규칙이 위젯에서 같은 결과를 내는지 실화면 기록이 없다. | G-t65-048 |
| 42 | `t65` P17 제약규칙 등록·시뮬레이션 | 나 | 제약규칙 전수 목록과 그 규칙이 붙은 상품 수를 한 화면에서 보는 곳이 없다. | G-t65-050 |
| 43 | `t65` P18 가격공식·구성요소 설계·바인딩 | 다 | 단가유형 설정이 「구현-미검증」이다 — `catalog/admin.py:2816` 이 「Phase 11: 사용차원 순서지정 위젯 + 단가유형 그룹제한(basecodes prc_typ_cd)」 으로 기록돼 있으나 실제 계산에서 단가형/합가형이 갈리는 것을 확인한 기록을 찾지 못했다. | G-t65-051 |
| 44 | `t65` P18 가격공식·구성요소 설계·바인딩 | 나 | 구성요소가 어떤 상품군에 유효한지(배선 유효성)를 검사하는 장치가 없다. 엉뚱한 상품군에 구성요소가 붙어도 화면은 조용하다. | G-t65-053 |
| 45 | `t65` P19 단가행 적재·할인·이력 관리 | 다 | 단가행 일괄 등록과 면적 매트릭스가 둘 다 「구현-미검증」이다. `price_views.py:1650` 이 「그리드에 보이는 행이 곧 이 구성요소의 전체 단가표(엑셀처럼)」 라고 적어 둔 대로 동작하는지, 붙여넣기 실패가 조용히 넘어가지 않는지 확인 기록을 찾지 못했다. | G-t65-054 |
| 46 | `t65` P19 단가행 적재·할인·이력 관리 | 나 | 적재된 격자에 **빈 셀이 남았는지** 한 화면에서 보는 장치가 없다. 빈 셀은 계산에서 그 조합만 조용히 빠지므로 「가격이 이상하다」가 뒤늦게 발견된다. 가격테이블 무결성 하네스가 따로 도는 이유다. | G-t65-055 |
| 47 | `t65` P20 가격 검산·기본사양가 진단·게시 위젯 전수 대조 | 다 | 원장 행 이름은 「가격 미산출(0원) 상품 진단 리포트」인데 그 근거가 가리키는 `admin/start-price/` 화면의 실제 이름은 **「상품 › 상품별 기본사양가」** 다(운영자 매뉴얼 `tools/manual_content.py:696`·화면 title `catalog/start_price_views.py:57`). 이 화면은 진단 리포트가 아니라 **자사몰 목록에 나갈 대표가를 관리하는 화면**이고, 자동/수기/없음(실패) 탭과 실패사유 열이 0원 진단 구실을 겸한다. 매뉴얼은 기본사양가가 「예전 이름 시작가 · API 필드 `start_price` · 최저가가 아니다」 라고 못 박아 두었다(`manual_content.py:77`) — 원장 행 이름으로는 이 화면을 찾을 수 없다. | G-t65-057 |
| 48 | `t65` P32 원고 저장소·디자인 에디터 연동 | 다 | 업로드 presign(`config/urls.py:251`·`catalog/s3_artwork.py:208-280`)과 확정 승격(`catalog/artwork_promote.py:1`)이 구현돼 있으나 「구현-미검증」이다. 주문 확정 시 임시→확정 승격이 빠짐없이 도는지 확인 기록을 찾지 못했다. | G-t65-093 |
| 49 | `t65` P32 원고 저장소·디자인 에디터 연동 | 나 | 원고 보관 기간·용량 정책이 없다. 인쇄 원고는 크고 오래 남아 저장 비용이 계속 는다. | G-t65-095 |
| 50 | `t65` P35 장애 모니터링·백업복구·서버 환경 | 나 | DB 스냅샷·복구 리허설 기록이 없다. 백업이 돌고 있는지, 돌아도 되돌릴 수 있는지 확인된 적이 없다 — 「되돌릴 수 있다」는 해 본 적이 있어야 사실이다. | G-t65-105 |

## 최숙진 — 30건

| # | 카드·프로세스 | 종류 | 할 일 | 관련 row_id |
|---:|---|---|---|---|
| 1 | `t64` P05 접수파일등록 · 조판 | 나 | `STD-MFG-058`·`062`·`064`·`072`~`081`·`092` — 13행. MES 쪽 개발 범위이며 MES 저장소에 HuniWeb 관련 코드가 0건이다. | STD-MFG-058 |
| 2 | `t64` P06 MES주문접수전송 | 다 | MES 쪽 주문 수신부 — `BackOffice.Server/CRT.EasyMES.V2.IF/IOrderService.cs:58` `COrderByExcel` 등 기존 채널(Cafe24·엑셀) 주문 수신은 있다. 후니 전용 입구만 없다 | — |
| 3 | `t64` P07 생산공정진행 | 가 | 우리 공정 마스터 ↔ MES 라우트 정합 — webadmin 에 `t_proc_processes`·`t_prd_product_processes` 가 살아서 돌고 있고, MES 에 라우트를 새로 만든다. 두 개의 공정 정의가 생긴다 — 어느 쪽이 정본인지, 어떻게 동기화하는지 행이 없다. 이 프로세스에서 가장 큰 공백이다 | — |
| 4 | `t64` P07 생산공정진행 | 나 | `STD-MFG-084`~`091`·`093` · `STD-ADO-012`·`013`·`016` — 12행. 전부 MES 쪽. | STD-MFG-084;STD-ADO-012 |
| 5 | `t64` P07 생산공정진행 | 다 | MES 공정 실적 등록 — 바코드 기반 공정상태 갱신 인터페이스가 MES 에 이미 있다. 후니 작업이 그 안에 안 들어갈 뿐 | — |
| 6 | `t64` P08 외주발주 · 입고 | 가 | 어떤 공정이 상시 외주인가 — `STD-MFG-101` 은 책자만 말한다. 나머지 상품군의 외주 여부가 행으로 없다 | STD-MFG-101 |
| 7 | `t64` P08 외주발주 · 입고 | 가 | 외주 단가·발주 금액 — 정산(`STD-ADO-019`)이 월 마감을 말하는데 단가 그릇이 행으로 없다 | STD-ADO-019 |
| 8 | `t64` P08 외주발주 · 입고 | 가 | 외주 리드타임 → 출고예정일 반영 — 외주가 끼면 출고예정일(`STD-MFG-065`)이 달라진다. 그 연결이 행으로 없다 | STD-MFG-065 |
| 9 | `t64` P08 외주발주 · 입고 | 나 | 8행 전부. | STD-MFG-101;STD-MFG-100;STD-ADO-017;STD-MFG-102;STD-MFG-103;STD-ADO-018;STD-MFG-104;STD-ADO-019 |
| 10 | `t64` P10 포장 · 출고 · 송장 | 나 | `STD-MFG-106`~`118`(13행) · `028`·`029`·`114` · `STD-ADO-016` — 17행. | STD-MFG-106;STD-ADO-016 |
| 11 | `t64` P10 포장 · 출고 · 송장 | 다 | MES 바코드 실적 등록 — `IManufactureService.cs:173`·`:179` 가 있다. 후니 작업이 안 들어갈 뿐 | — |
| 12 | `t64` P10 포장 · 출고 · 송장 | 다 | huni-mall 출고완료 배지 — 라벨 매핑만 있다(`mypage-shared.tsx:161`). 그 상태를 만드는 쪽이 없다 | — |
| 13 | `t64` P12 재제작 · 반송 · 재출고 | 가 | 귀책 판단 기준 — `STD-CLM-011`(귀책 구분)은 t65 에 있으나, 생산 쪽에서 불량을 판정하는 기준(어디까지가 불량인가)이 행으로 없다 | STD-CLM-011 |
| 14 | `t64` P12 재제작 · 반송 · 재출고 | 가 | 재출고 배송비 부담 — `STD-MFG-122` 는 메모필드만 말한다. 실제 금액 정산이 행으로 없다 | STD-MFG-122 |
| 15 | `t64` P12 재제작 · 반송 · 재출고 | 나 | `STD-ADO-011` · `STD-MFG-119`~`124` — 7행 전부. | STD-ADO-011;STD-MFG-119 |
| 16 | `t64` P14 운영자 · 주문운영 | 가 | 구 사이트 화면과의 대응표 — `STD-ADO-025`·`026` 이 「미실측」인 채로 남아 있다. 새 몰에서 그 기능이 어디로 갔는지 대응 행이 없다 | STD-ADO-025 |
| 17 | `t64` P14 운영자 · 주문운영 | 나 | `STD-ADO-006` — 작업지시서 출력(MES) | STD-ADO-006 |
| 18 | `t64` P15 생산현황 · 산출데이터 | 가 | 현황표의 「당일」 기준 — `STD-MFG-094` 가 당일발주·당일생산·당일출고를 말하는데 하루의 경계(자정? 업무 시작?)가 행으로 없다. 리드타임(`096`) 계산의 전제다 | STD-MFG-094 |
| 19 | `t64` P15 생산현황 · 산출데이터 | 가 | 자재 마스터 — `STD-MFG-098` 은 입·출고만 말한다. 자재 자체는 webadmin 에 `t_mat_*` 로 이미 있다(자재 마스터 화면 `/admin/master/mat/`). 그 둘의 관계가 행으로 없다 | STD-MFG-098 |
| 20 | `t64` P15 생산현황 · 산출데이터 | 나 | `STD-MFG-094`·`095`·`096`·`098`·`132`·`131` — 6행 전부. | STD-MFG-094 |
| 21 | `t64` P16 오프라인주문 · 증빙 | 가 | 거래처 마스터 — 6행이 전부 "거래처별"·"업체별"인데 거래처를 등록·관리하는 행이 없다. B2B 대분류(t62 `STD-B2B` 15행)에 회원 축 거래처가 있을 수 있으나 생산 축 거래처와 같은 것인지 확인되지 않았다 | — |
| 22 | `t64` P16 오프라인주문 · 증빙 | 가 | 오프라인 주문의 가격 산출 — `STD-MFG-133` 은 "금액 직접 입력"이라고 한다. 우리 가격엔진을 쓰지 않는다는 뜻인데, 그 결정과 그로 인한 불일치 관리가 행으로 없다 | STD-MFG-133 |
| 23 | `t64` P16 오프라인주문 · 증빙 | 가 | OEM 3사 연동 규격 — `STD-MFG-071` 은 IMPORT 라고만 한다. 3사 각각의 포맷·주기·인증이 행으로 없다 | STD-MFG-071 |
| 24 | `t64` P16 오프라인주문 · 증빙 | 나 | `STD-MFG-069`·`070`·`071`·`133`·`134`·`135` · `STD-ADO-024` — 7행 전부. | STD-MFG-069;STD-ADO-024 |
| 25 | `t64` P16 오프라인주문 · 증빙 | 다 | MES 엑셀 주문 수신 — `COrderByExcel`(`IOrderService.cs:58`)이 이미 있다 — 기존 채널(Cafe24 등)용. 후니 거래처 포맷으로 쓸 수 있는지는 미확인 | — |
| 26 | `t64` P17 생산 · 파일알림발송 | 가 | 경계 위반 감시 — `STD-MFG-130` 은 "경계 준수"라고 선언만 한다. 샵바이가 실제로 무엇을 보내고 있는지 확인하는 행이 없다 — 확인 없이는 중복인지 공백인지 알 수 없다 | STD-MFG-130 |
| 27 | `t65` P22 고객군·상품군 판매 통계 | 나 | 상품군 통계 4행이 모두 「미실측」이다 — 셀러어드민 통계 메뉴를 열어 대체 가능 여부를 판정한 사람이 아직 없다. | G-t65-063 |
| 28 | `t65` P26 관리자 계정·권한·감사로그 | 나 | 셀러어드민에 권한그룹이 0개다(마스터 1명뿐). 실무진이 각자 계정으로 들어가려면 그룹부터 만들어야 하는데, 지금은 한 계정을 나눠 쓰는 상태로 보인다 — 그러면 감사로그가 누가 했는지 구분하지 못한다. | G-t65-074 |
| 29 | `t65` P27 몰 기초정보·도메인·발송메일 설정 | 나 | 사업자 정보가 통째로 빈칸이다 — 회사명·사업자등록번호·통신판매업 신고번호·주소·업종/업태·개인정보보호책임자가 모두 비어 있고 대표자명이 「대표 운영자」로 남아 있다. API `serviceBasicInfo` 의 `companyName` 도 빈 문자열이다. 법적 표기 의무라 런칭 차단 사유다. | G-t65-077 |
| 30 | `t65` P28 알림(SMS·알림톡) 설정·발송 | 나 | SMS·알림톡 사용설정이 둘 다 「사용 안 함」이고, 주문배송 항목도 전부 「사용 안 함」이다. 스위치만 켜면 되는 일과 템플릿을 만들어야 하는 일이 섞여 있다. | G-t65-081 |

## 김동학 — 24건

| # | 카드·프로세스 | 종류 | 할 일 | 관련 row_id |
|---:|---|---|---|---|
| 1 | `t62` P04 계정 찾기(아이디·비밀번호) | 나 | 화면·폼은 있으나 두 기능 모두 "준비 중입니다" 토스트로 끝난다. 샵바이 shop API 에는 `/profile/find-id`·`/profile/find-password`·`/profile/password/sending-email-with-url` 이 모두 있다 — 배선만 남았다. | G-t62-007 |
| 2 | `t62` P05 회원정보 변경·탈퇴 | 다 | 비밀번호 재확인 게이트(`/profile/check-password`)가 샵바이 스펙에는 있으나 `/mypage/account` 진입에 걸려 있는지 확인하지 못했다. | G-t62-008 |
| 3 | `t62` P22 마케팅 발송·수신동의 관리 | 다 | `STD-PRM-014` 는 `작동` 판정이지만, 가입 폼이 수집한 `mkt.sms`·`mkt.email` 이 가입 요청에 실리지 않는다(`signup-form.tsx:51,135`·`api/auth/signup/route.ts` 본문 필드 5개). 가입 시점 동의가 저장되지 않는다. | G-t62-052 |
| 4 | `t64` P11 배송추적 · 배송지변경 | 가 | 택배사 추적 링크 — `STD-SHP-014` 는 "고객 배송 조회(택배사 추적)"라고 하는데 huni-mall 은 번호를 글자로만 보여 준다. 클릭해서 택배사로 가는 것이 별도 행으로 없다 — 「부분」 판정 안에 묻혀 있다 | STD-SHP-014 |
| 5 | `t64` P11 배송추적 · 배송지변경 | 가 | 주문 건별 배송지 변경 화면 — `STD-SHP-015` 는 "배송지 변경/추가"인데 실제로 있는 것은 주소록이다. 이미 낸 주문의 배송지를 바꾸는 화면이 행으로 구분돼 있지 않다 | STD-SHP-015 |
| 6 | `t64` P11 배송추적 · 배송지변경 | 다 | 송장 표시 — huni-mall 이 이미 보여 준다. 그 값을 채우는 쪽(P10 TO-3)이 없어서 늘 빈칸이다 | — |
| 7 | `t64` P12 재제작 · 반송 · 재출고 | 다 | huni-mall 클레임 표시 — `src/lib/api/hooks/use-my-orders.ts:26`·`:335-340` 에서 `claimStatusTypeLabel`·`cancelDoneCnt`·`returnDoneCnt`·`exchangeDoneCnt` 를 표시용으로 다룬다. 신청·승인 호출은 0건 | — |
| 8 | `t65` P01 주문 취소 신청·승인 | 나 | huni-mall 에 취소 신청 화면·호출이 없다 — `src/app/(main)` 하위 라우트는 cart·checkout·company·guest-order·guide·location·mypage·notice·order-complete·privacy·search·shop·terms 13개뿐이고 claim/cancel 라우트가 없다. 마이페이지 컴포넌트 18개에도 클레임 섹션이 없다. 샵바이 Shop API 쪽은 `POST /profile\|guest/orders/{orderNo}/claims/cancel` 과 `POST /profile\|guest/order-options/{orderOptionNo}/claims/cancel` 이 회원·비회원 대칭으로 다 있다. | G-t65-001 |
| 9 | `t65` P01 주문 취소 신청·승인 | 다 | `mypage-shared.tsx:165-166` 의 `cancel_req`/`cancel_done` 은 「주문취소」 글자가 붙은 **상태 칩**이고, 그 카드 전체가 주문 상세를 여는 버튼이라 취소 신청 동작이 걸려 있지 않다. `use-my-orders.ts:117-123` 의 취소처리중·반품처리중·교환대기중·교환처리중·취소완료·반품완료·교환완료 매핑도 표시 전용이다 — 읽기는 되는데 쓰기가 없다. | G-t65-002 |
| 10 | `t65` P02 반품·교환 신청·처리 | 나 | huni-mall 에 반품·교환 신청 화면이 없다. 샵바이 Shop API 는 `POST /profile\|guest/order-options/{orderOptionNo}/claims/return` (단일)·`POST /profile\|guest/claims/return`(복수)·`POST /profile\|guest/order-options/{orderOptionNo}/claims/exchange` 가 모두 열려 있다. | G-t65-004 |
| 11 | `t65` P02 반품·교환 신청·처리 | 다 | 반품·교환 **정책 문구**는 이미 고객에게 보이는데(`product-sections.tsx:227-228` 의 「교환/반품」 탭 — "개별 제작 상품 특성상 제작 접수 이후 단순 변심에 의한 취소·교환·반품은 어렵습니다. 제품 불량·인쇄 오류·파손·오염 시 수령 후 7일 이내 접수해 주세요.") 그 문구가 가리키는 접수 창구가 없다. 문구는 상품 상세 컴포넌트에 하드코딩돼 있어 운영자가 고칠 수도 없다. | G-t65-005 |
| 12 | `t65` P03 환불 예상금액 산정·환불계좌·지급 | 나 | 환불 예상금액 화면과 환불계좌 입력 화면이 huni-mall 에 없다. 샵바이 Shop API 는 `GET /profile\|guest/order-options/{no}/claims/estimate`(단일)·`POST /profile\|guest/claims/estimate`(복수)·`PUT /profile\|guest/claims/{claimNo}/account` 가 다 있다. | G-t65-007 |
| 13 | `t65` P04 클레임 접수·사유분류·귀책판정·승인거부·철회 | 나 | 클레임 목록·상세 화면이 huni-mall 에 없다 — `GET /profile/claims`·`GET /profile/claims/{claimNo}/result`·`GET /guest/claims/{claimNo}/result` 는 열려 있다. | G-t65-011 |
| 14 | `t65` P06 1:1 문의·상품 Q&A 응대 | 나 | huni-mall 에 문의 라우트가 없다 — `src/app/(main)` 하위 13개 라우트에 inquiry·qna 가 없고 마이페이지 컴포넌트 18개에도 문의 섹션이 없다. 샵바이는 양쪽이 다 열려 있다: 상품문의 `GET/POST /products/{productNo}/inquiries`·`GET /profile/product-inquiries`, 1:1문의 `GET/POST /inquiries`·`GET /inquiries/{inquiryNo}`·`GET /inquiries/{inquiryNo}/file`, 운영자 답변 `POST /inquiry/{inquiryNo}/reply`·`POST /inquiries/{inquiryNo}/answer`. | G-t65-016 |
| 15 | `t65` P09 공지사항 등록·열람 | 다 | 공지 본문이 `notice/[articleNo]/page.tsx:69` 에서 필터 없이 렌더된다 — 운영자가 넣은 HTML 이 그대로 실행된다. 같은 문제가 상품설명(`configurator.tsx:77`)에도 있고 P36 에서 함께 다룬다. | G-t65-025 |
| 16 | `t65` P27 몰 기초정보·도메인·발송메일 설정 | 다 | 도메인이 세 갈래다 — PC 웹 `shopby.huniprinting.com`, 모바일 웹 `shoby.huniprinting.com`, SNS 콜백 PC `shoby.huniprinting.co.kr`. `shopby` 와 `shoby` 철자가 갈려 있어 SNS 로그인 콜백이 어느 쪽으로 돌아오는지 불안정하다. | G-t65-078 |
| 17 | `t65` P31 가격 계산 결과 카트 주입 브리지 | 나 | 담기 전달값에 토큰·원고 저장키·프로젝트 식별자가 평문으로 실린다(`ARCHITECTURE-v2.md` §10 A-13·§3.2·§8 I-2). 서명 검증도 없다 — 고객이 값을 바꿔 담으면 가격이 바뀔 수 있다는 뜻이다. | G-t65-090 |
| 18 | `t65` P34 고객 유입·전환 품질(분석·SEO·반응형·리다이렉트) | 나 | 분석 도구가 없다 — `src` 전역에서 `gtag\|GA4\|googletagmanager\|analytics` grep 이 한 건도 안 나온다. 오픈 뒤 무엇이 안 되는지 숫자로 말할 근거가 안 쌓인다. | G-t65-099 |
| 19 | `t65` P34 고객 유입·전환 품질(분석·SEO·반응형·리다이렉트) | 나 | `sitemap\|robots` grep 도 무결과다. 검색 색인을 통제하지 못한다. | G-t65-100 |
| 20 | `t65` P35 장애 모니터링·백업복구·서버 환경 | 나 | 몰에 전역 오류 화면이 없다 — `error.tsx`·`not-found.tsx`·`global-error.tsx` 가 0개다. 고객이 Next 기본 404/500 을 그대로 본다. 포크에는 세 파일이 다 있다. | G-t65-103 |
| 21 | `t65` P36 보안 강화(프록시 가드·XSS·CSP·개인정보) | 나 | `/api/shopby/[...path]/route.ts:20-30` 이 인증도 경로 제한도 없이 파트너 비밀 토큰을 붙여 중계한다. 감사 보고서가 C1(Critical)·H1 로 잡아 둔 항목이고, 포크에는 `shopby-proxy-guard.ts` 의 default-deny 구현이 이미 있다. | G-t65-107 |
| 22 | `t65` P36 보안 강화(프록시 가드·XSS·CSP·개인정보) | 나 | `notice/[articleNo]/page.tsx:69` 와 `configurator.tsx:77` 이 `dangerouslySetInnerHTML` 로 본문을 그대로 렌더한다. 운영자 계정이 하나 뚫리면 저장형 XSS 로 번진다. 포크에 `sanitize.ts` 가 있다. | G-t65-108 |
| 23 | `t65` P36 보안 강화(프록시 가드·XSS·CSP·개인정보) | 나 | `next.config.ts:1-17` 에 `headers()` 가 없어 보안 응답 헤더·CSP 가 하나도 안 나간다. 포크는 CSP 를 Report-Only 로 먼저 켰다. | G-t65-109 |
| 24 | `t65` P37 코드베이스 정비·포크 처리·CI·문서 | 다 | 환경변수가 세 곳에서 어긋난다 — 코드가 참조하는 키(`HUNI_WIDGET_SITE_KEY`·`SHOPBY_NOTICE_BOARD_NO`)가 `.env.example`(21키)에 없고, `vercel-env-push.sh` 는 쓰지 않는 `GRAPHQL` 2종을 밀고, `DEMO_AUTH_*` 는 아무도 안 쓴다. | G-t65-112 |

## 미정 — 20건

| # | 카드·프로세스 | 종류 | 할 일 | 관련 row_id |
|---:|---|---|---|---|
| 1 | `t64` P02 원고승격 · 보관 | 가 | order 버킷 보존기간 확정 — 설계 정본 §12-9 의 ★ 미결(제안 1년)인데 원장 행이 없다 | — |
| 2 | `t64` P02 원고승격 · 보관 | 다 | 승격 전체 — P01 이 안 불리면 이 프로세스가 통째로 실행되지 않는다. 승격은 `order/register` 요청 안에서 수행된다(크론·워커가 없어 lazy 패턴) | — |
| 3 | `t64` P02 원고승격 · 보관 | 다 | 스윕 — 5분 스로틀 스윕도 요청에 얹혀 있다. 요청이 0이면 스윕도 0이다 | — |
| 4 | `t64` P05 접수파일등록 · 조판 | 가 | 조판 실패·재조판 경로 — 조판이 안 되는 원고(크기 초과·도수 불일치)가 나왔을 때 어디로 가는지 행이 없다 | — |
| 5 | `t64` P05 접수파일등록 · 조판 | 다 | MES 생산계획·공정 모듈 — `BackOffice.Server/CRT.EasyMES.V2.IF/IManufactureService.cs` 에 생산계획·작업순서(`UManufactPlanPrintWorkSeq:64`)·공정 테이블(`T_MNF_MANUFACT_PLAN_PRCS`)이 이미 있다. 후니 주문이 들어오는 입구가 없을 뿐이다 | — |
| 6 | `t64` P06 MES주문접수전송 | 다 | MES 쪽 SQS — `Framework/CRT.Framework.AWS/SqsClient.cs` 가 실제로 구현돼 있다(로그·ECount 연동용). 후니 큐와 연결돼 있지 않을 뿐 | — |
| 7 | `t64` P07 생산공정진행 | 가 | 공정 완료 신호의 우리 쪽 반영 — 팀이 MES 에 완료를 찍으면 그게 고객 화면까지 오는가? P09(FR-1)는 접수완료/생산대기/생산중/생산완료 4개만 말한다. 공정 단위 진행은 고객에게 안 간다는 결정인지 누락인지 행이 없다 | — |
| 8 | `t64` P09 생산상태 · 역방향동기화 | 다 | 웹훅 수신함 — `shopby_hook.py` 가 원문 저장 후 즉시 200 을 준다(설계대로). 읽는 쪽이 없다 | — |
| 9 | `t64` P10 포장 · 출고 · 송장 | 다 | 샵바이 배송 API 명세 — 전부 확인됐다(위 15번). 부르는 코드만 없다 | — |
| 10 | `t64` P11 배송추적 · 배송지변경 | 다 | 주소록 — 살아 있다. 주문 건별 변경과 이어져 있지 않다 | — |
| 11 | `t64` P12 재제작 · 반송 · 재출고 | 다 | 웹훅 수신함 — 반품·교환 이벤트가 오면 저장은 된다 | — |
| 12 | `t64` P13 생산구간 · 주문취소 | 다 | 웹훅 수신함 — `CHANGE_ORDER_STATUS` 가 오면 `claimStatusType` 까지 파싱해 저장한다 → `catalog/shopby_hook.py:112-119`. 분기하는 쪽이 없다 | — |
| 13 | `t64` P14 운영자 · 주문운영 | 다 | `t_ord_orders`·`t_ord_artworks` — 데이터는 쌓인다(P01·P02). 볼 화면이 없어 DB 조회로만 확인 가능하다 | — |
| 14 | `t64` P14 운영자 · 주문운영 | 다 | webadmin 상품 뷰어·가격 뷰어 — 운영 화면이 상품 축으로는 잘 돼 있다(product-viewer·price-viewer·price-simulator). 주문 축이 없다 | — |
| 15 | `t64` P15 생산현황 · 산출데이터 | 다 | webadmin 자재 마스터 — 화면이 있다(`/admin/master/mat/`). 생산 입·출고와 이어져 있지 않다 | — |
| 16 | `t64` P15 생산현황 · 산출데이터 | 다 | webadmin 사용자 권한 — Django 권한 체계가 깔려 있다. 7역할 구분은 없다 | — |
| 17 | `t64` P15 생산현황 · 산출데이터 | 다 | MES 생산계획 테이블 — `T_MNF_MANUFACT_PLAN_PRCS` 등이 있다. 후니 작업이 없어 집계할 것이 없다 | — |
| 18 | `t64` P16 오프라인주문 · 증빙 | 가 | 오프라인 주문의 원고 경로 — 웹 주문은 tmp→order 승격(P02)을 탄다. 오프라인은 원고가 어디서 오는지 행이 없다 | — |
| 19 | `t64` P16 오프라인주문 · 증빙 | 다 | 우리 가격엔진 — 가격 시뮬레이터·가격 뷰어가 살아 있다. 오프라인 주문이 "금액 직접 입력"이라 쓰이지 않는다 | — |
| 20 | `t64` P17 생산 · 파일알림발송 | 가 | 발송 이력·실패 재발송 — 알림이 실패했을 때 무엇이 남고 누가 다시 보내는지 행이 없다 | — |

## 최숙진+서희항 — 5건

| # | 카드·프로세스 | 종류 | 할 일 | 관련 row_id |
|---:|---|---|---|---|
| 1 | `t62` P29 인쇄 가이드 콘텐츠 운영 | 다 | webadmin 에 업로드·측정·저장·삭제가 모두 있으나(`catalog/guide_views.py`), 이것이 huni-mall `/guide` 화면과 실제로 이어져 있는지 확인하지 못했다 — 판정도 `구현-미검증` 이다. | G-t62-071 |
| 2 | `t64` P03 파일검판 · 프리플라이트 | 가 | 검사 결과 보관 스키마(DDL) — `STD-MFG-052`·`STD-ART-017` 은 "보관한다"·"상태 관리한다"고만 하고, 어떤 테이블을 새로 파는지는 행이 없다 | STD-MFG-052;STD-ART-017 |
| 3 | `t64` P10 포장 · 출고 · 송장 | 가 | 분할 출고 시 샵바이 반영 규칙 — `STD-SHP-011`(분할배송)과 `STD-MFG-114`(다중 송장)가 만나는 지점 — 주문 일부만 나갔을 때 샵바이 주문상태를 어떻게 쓰는지 행이 없다 | STD-SHP-011;STD-MFG-114 |
| 4 | `t64` P13 생산구간 · 주문취소 | 나 | `STD-MFG-026`·`031`·`032`·`033`·`015` · `STD-ADO-023`(호출부) — 6행. | STD-MFG-026;STD-ADO-023 |
| 5 | `t64` P17 생산 · 파일알림발송 | 나 | `STD-MFG-125`·`126`·`127`·`128`·`129`·`130` · `STD-ADO-005` — 7행. | STD-MFG-125;STD-ADO-005 |

## 대표 — 1건

| # | 카드·프로세스 | 종류 | 할 일 | 관련 row_id |
|---:|---|---|---|---|
| 1 | `t64` P03 파일검판 · 프리플라이트 | 가 | 바이러스 검사 엔진 선정 — `docs/artwork-scan-clamav-vs-eset.md` 로 비교는 돼 있는데 선정·조달 행이 없다(PitStop 은 `BLK-S2-4` 가 있다) | BLK-S2-4 |

## 신우진(PM) — 1건

| # | 카드·프로세스 | 종류 | 할 일 | 관련 row_id |
|---:|---|---|---|---|
| 1 | `t64` P13 생산구간 · 주문취소 | 가 | 부분 취소·수량 변경 — 설계 §12-10 이 "1차 미지원 제안"이라 했다. 미지원을 명시적으로 결정한 행이 없다 | — |

## 신우진(PM)+최숙진 — 1건

| # | 카드·프로세스 | 종류 | 할 일 | 관련 row_id |
|---:|---|---|---|---|
| 1 | `t64` P17 생산 · 파일알림발송 | 가 | 발송 채널 선택 — 설계 §12-7 이 "샵바이 알림톡 vs 자체 비즈메시지"를 미결로 남겼다. 채널 결정 행이 없다 — 이게 정해져야 2번을 시작할 수 있다 | — |

## 최숙진+김동학 — 1건

| # | 카드·프로세스 | 종류 | 할 일 | 관련 row_id |
|---:|---|---|---|---|
| 1 | `t62` P24 거래처 단가·대량견적 | 나 | 대량견적 요청 폼·견적서 양식 모두 코드를 찾지 못했다. | G-t62-059 |
