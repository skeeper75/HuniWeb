# M3 — L0 지도 교정 지점

> 작성 2026-09-02 · lane `sync-huniweb`
> 대상: `_workspace/huni-launch-runway/07_rebaseline/L0/ARCHITECTURE.md` (lead 초안) 및 **카드 M3 본문 자체**
> **[HARD] 초안을 존중하지 않는다.** 내 축(생산·공정·MES·프리플라이트·배송)에서 틀린 곳을 지목하고 교정본을 낸다.

---

## ★ C-0 — 카드 M3 본문의 사실 오류 (가장 먼저 고쳐야 할 것)

### 카드가 쓴 것

> 「**L1 353행에 생산축이 통째로 빠져 있다.**」

### 실제 (L1 `standard-feature-canon.csv` 실측 2026-09-02)

**통째로 빠져 있지 않다. 23행이 이미 있다.**

| L1 위치 | 행 | 내용 |
|---|---:|---|
| `운영자·주문운영 › 생산` | **5** | `STD-ADO-012` 인쇄 공정 상태 트래킹 · `-013` 작업 큐/일정 배정 · `-014` 판걸이/임포지션 정보 산출 · `-015` 생산 지시 데이터 연계(JDF/XJDF) · `-016` 바코드/라벨 출력 |
| `운영자·주문운영 › 검수` | **4** | `STD-ADO-008` 파일 확인 처리(검판 판정) · `-009` 재업로드 요청 발송 · `-010` 검수 게이트 관리(G1 파일·G3 가공·G4 출고) · `-011` 불량 판정·재작업 지시 |
| `운영자·주문운영 › 외주` | **3** | `STD-ADO-017` 외주 발주 등록·관리 · `-018` 외주 입고 전수검수 기록 · `-019` 외주 정산(월 마감) |
| `운영자·주문운영 › 배송` | **2** | `STD-ADO-020` 송장 등록·일괄 업로드 · `-021` 출고 처리·배송중 전이 |
| `원고·파일 › 검판` | **10** | `STD-ART-008`~`-018` (포맷·재단선/블리드·CMYK·dpi·폰트·실측 사이즈·별색/오버프린트·리포트 노출·**PitStop 연동**·게이트 상태·재업로드 요청) |
| `원고·파일 › 생산연계` | **1** | `STD-ART-028` 생산용 원고 자동 다운로드/전달 |
| **합계** | **25** | — |

### 정확한 진술로 교정

> **L1 에 생산축이 25행 있으나 ① 대분류가 없어 흩어져 있고 ② 세분이 거칠며 ③ MES 인계·상태왕복·공정라우트·포장출고 4개 축이 실제로 빠져 있다.**

빠진 4축의 실체:

| 빠진 축 | L1 에 있나 | M3 가 채운 행수 |
|---|---|---:|
| **MES 인계**(SF-1·승격·WCF·품목코드 매핑) | ❌ 0행 | 21 |
| **상태왕복**(FR-1~7 ↔ TO-2~6·취소 경합 CAS) | ❌ 0행 | 14 |
| **공정라우트**(18케이스·팀·interlock·산출데이터) | ❌ 0행 (`STD-ADO-012` 「공정 상태 트래킹」 1줄이 전부) | 18 |
| **포장·출고**(1차/박스포장·바코드·명세서 3종) | ❌ 0행 (`STD-ADO-020/021` 은 송장·전이만) | 13 |

**왜 중요한가**: 「통째로 빠졌다」로 적으면 편입 시 **기존 25행과 중복 신설**하게 된다. 실제로 필요한 것은 **신설 + 기존행 보강**이다(§C-1 매핑).

---

## C-1 — L1 편입 시 중복 방지 매핑 (lead 가 합칠 때 쓸 표)

M3 의 135행 중 **기존 L1 행과 겹치는 12건**. 신설하지 말고 **기존행을 보강**하거나 **M3 행을 하위 세분으로 붙인다.**

| M3 행 | 겹치는 L1 행 | 관계 | 권고 |
|---|---|---|---|
| `STD-MFG-041` PitStop 프리플라이트 | `STD-ART-016` 자동검판 도구 연동(PitStop 등) | **동일** | L1 유지 · MFG-041 삭제 또는 하위 세분 표기 |
| `STD-MFG-042` 재단여백 | `STD-ART-009` 재단선/블리드 3mm 확보 검사 | 동일(M3 가 A/B 분기 추가) | **L1 보강** — 「+ 없음=자동요청 / 부족=담당자」 |
| `STD-MFG-043` 해상도 | `STD-ART-011` 해상도(dpi) 검사 | 동일 | L1 보강 |
| `STD-MFG-044` 폰트 | `STD-ART-012` 폰트 아웃라인 여부 검사 | **다름** — L1 은 아웃라인, M3 는 미포함 열림실패 | **둘 다 유지** |
| `STD-MFG-045` 색공간·별색 | `STD-ART-010` + `STD-ART-014` | 동일 | L1 유지 |
| `STD-MFG-046` 페이지수 대조 | — | **없음** | **신설** |
| `STD-MFG-047` 인쇄 크기 대조 | `STD-ART-013` 주문 사양과 파일 실측 사이즈 대조 | 동일 | L1 유지 |
| `STD-MFG-049` 3갈래 분기 | `STD-ART-017` 검판 게이트 통과/보류 상태 관리 | 부분(L1 은 2갈래) | **L1 보강 → 3갈래** |
| `STD-MFG-054~056` 재업로드 링크·화면·회차 | `STD-ART-018` 재업로드 요청 발송 + `STD-ART-007` 재업로드·버전 이력 | 부분 | L1 보강 + M3 세분 신설 |
| `STD-MFG-063` 파일 확인 처리 | `STD-ADO-008` 파일 확인 처리(검판 판정) | 동일 | L1 유지 |
| `STD-MFG-091` 팀별 다운로드 권한 | `STD-ART-028` 생산용 원고 자동 다운로드/전달 | 부분 | **L1 보강** — 「제작중 and 생산다운여부 Y」 조건 명시 |
| `STD-MFG-111` 송장 출력 | `STD-ADO-020` 송장 등록·일괄 업로드 | 부분(방향이 반대) | **둘 다 유지** — L1 은 등록, M3 는 MES 발 출력 |

**나머지 123행은 신설 대상이다.**

### C-1.1 L1 기존행 중 **재판정이 필요한 것 1건**

`STD-ADO-015` **생산 지시 데이터 연계(JDF/XJDF)** — 이 프로젝트의 MES 연계는 **JDF/XJDF 가 아니라 WCF(SOAP)** 로 확정돼 있다(`aws-architecture-huni.pdf` p.4·p.7 · `order-to-mes-process.md` §8.3). **근거출처를 확인하고 정정하거나, 별개 항목(인쇄 표준 연계)임을 명시해야 한다.** 지금 상태로 두면 「JDF 를 만들어야 하나」로 잘못 읽힌다.

---

## C-2 — ARCHITECTURE.md §1 「시스템 5개」 → **7개**

| 교정 | 내용 |
|---|---|
| **누락 S6** | **MES** — 기존 · EC2 Windows · SQL Server + WCF + WinForms. **생산·작업지시·공정·출고·송장의 원장.** 유지(신규로 대체 안 함). 소유: PM + MES 담당 개발자 |
| **누락 S7** | **PitStop Server** — EC2 Windows · **미도입**. PDF 프리플라이트. 로드맵 4단계 |
| 근거 | `aws-architecture-huni.pdf` p.4 표 · p.5 §2 · `order-to-mes-process.md` §1 |

**왜 치명적인가**: MES 가 시스템 목록에 없으면 **역할 경계(§7)에 MES 담당 개발자 칸이 안 생긴다.** 실제로 §7 의 3인 구도(쇼핑개발·인쇄개발·PM)에는 **MES 화면 신규 작업(FR-3/4/5 버튼)을 할 사람이 없다.**

---

## C-3 — ARCHITECTURE.md §3 종단 흐름도가 **주문 확정에서 끝난다**

초안 mermaid 의 마지막 노드는 `ORD[/api/w/v1/order/register] → S3DB`. **그 뒤 7단계가 없다.**

교정본: `production-chain.md` §2 의 13단계 mermaid 로 대체한다. 추가되는 것 —
승격(6) → 웹훅(7) → 바이러스검사+프리플라이트(8·9) → **MES 접수 WCF(10)** → MES 버킷 복사(11) → 상태·송장 회신(12) → Shopby 반영(13) → 고객 배송조회.

---

## C-4 — ARCHITECTURE.md §5 「데이터 저장소 4곳」 교정

| 초안 | 교정 |
|---|---|
| 「**S3 원고 버킷**」 1개 | **S3 버킷 3개**: `tmp`(주문 전·30일 자동삭제) / `order`(웹↔MES 인수인계) / **`MES 버킷`**(MES 자기 키 체계). 근거 `order-to-mes-process.md` §4 |
| MES SQL Server 없음 | **추가**: MES SQL Server — 생산·작업지시·출고의 원장. **직접 접근 금지**(WCF 경유 필수). 근거 `aws-architecture-huni.pdf` p.11 §6 |
| Amazon SQS 없음 | **추가**: SQS(주문 큐 / 프리플라이트 큐) + DLQ. 미구성 |

**tmp 30일 자동삭제가 만드는 위험**을 §5 에 명시할 것: 가상계좌 입금이 늦으면 **입금 전에 원고가 사라진다.** 그래서 **승격(파일 지키기)과 접수(생산 시작)를 분리**했다 — 입금대기 상태에서도 파일은 order 버킷으로 즉시 승격하고 MES 전송만 막는다. 추가 안전장치로 **입금기한을 tmp 수명보다 짧게**(권고 3~7일) 설정해야 한다.

---

## C-5 — ARCHITECTURE.md §6 「API 표면」에 **결제 이후 인터페이스 24건이 없다**

초안은 Shopby 31 + 위젯 27 + Next 7 + webadmin 151 만 센다. **결제 이후 5방향 24건이 통째로 빠져 있다.**

| 방향 | 건수 | 상태 |
|---|---:|---|
| 자사몰 → 우리 (SF-1 주문 등록) | **1** | ✅ 라이브 |
| Shopby → 우리 (웹훅 SB-1~4) | **4** | ⚠ 수신함만 |
| 우리 → Shopby (TO-1~7) | **7** | ❌ 엔드포인트만 확정 |
| 우리 → MES (MES-1~5 · WCF) | **5** | ❌ 미구현 |
| MES → 우리 (FR-1~7) | **7** | ❌ 미구현 · **정의서 미작성** |
| **합계** | **24** | — |

근거: `order-to-mes-process.md` §8.

---

## C-6 — ARCHITECTURE.md §7 역할 경계에 **MES 축이 없다**

초안의 3구도(쇼핑개발 ↔ 계약 ↔ 인쇄개발 + PM)에 **네 번째 축**을 추가해야 한다.

```
쇼핑개발 ↔ [계약] ↔ 인쇄개발 ↔ [계약: 인터페이스 12건] ↔ MES 담당
```

**MES 쪽 신규 작업(다른 사람의 일)**: FR-3 취소 버튼 · FR-4 재업로드 요청 버튼 · FR-5 취소요청 승인/거부 — **MES WinForms 에 버튼과 사유 입력이 새로 생겨야 한다.**
**리드타임 경고**: MES 는 상대가 있어 **스펙 협의 리드타임이 길다** → 설계문서가 「승격과 **병행 착수**」를 명시했다. L5 일정에 반드시 반영할 것.

---

## C-7 — ARCHITECTURE.md §8 결함표에 **생산축 결함 5건 추가**

| # | 결함 | 근거 | 왜 중요한가 |
|---|---|---|---|
| **A-7** | **MES 인계 관이 없다** — 결제된 주문이 생산에 도달하지 못한다 | 코드 grep `wcf/soap/zeep` **0건** | 결제 이후 절반이 통째로 끊긴다. **런칭 블로커** |
| **A-8** | **MES 품목코드 매핑 5.6%** (15 / 269 활성상품) | 라이브 실측 2026-09-02 · 설계문서 08-18 도 15개 → **2주간 진전 0** | 매핑 없으면 접수 payload 를 못 만든다. A-7 의 선행 |
| **A-9** | **웹훅 수신함에 소비자가 없다** — 원문만 쌓이고 아무도 읽지 않는다 | `shopby_hook.py` 존재 · 소비자 grep 0건 · `t_ord_webhooks` **0행** | 취소·입금완료가 처리되지 않는다 |
| **A-10** | **공정라우트가 데이터가 아니다** — 18케이스가 PDF 안에만 있다 | 라이브 라우트 테이블 0개 · `disp_seq` 는 **표시순서** | interlock·리드타임·완료예정일 산정이 전부 막힌다 |
| **A-11** | **SF-1·승격이 실사용 0건** — 코드는 라이브인데 한 번도 안 돌았다 | `t_ord_orders`·`t_ord_artworks` 각 **0행** | 「라이브」와 「검증됨」을 구분해야 한다 |

---

## C-8 — ARCHITECTURE.md §9 미확인 목록 보강

초안의 「shopby webhook 의 실제 수신 이벤트 미확인」에 **실측 사실 추가**:
> `t_ord_webhooks` **0행**(2026-09-02) — 웹훅 URL 이 Shopby 에 **아직 등록되지 않았거나** 이벤트가 발생하지 않았다. 설계문서 §11 3단계도 「키는 대표님이 보유」로 남아 있다. **판정은 M2 축의 몫.**

추가할 미확인 (내 축):

| # | 미확인 |
|---|---|
| M3-U1 | MES 실제 상태 코드·WCF 메서드/필드 스펙 (설계문서가 스스로 **추정**이라 명시) |
| M3-U2 | 배송사(택배사) 계약·API·`deliveryCompanyType` 코드값 — 문서·코드 전무 |
| M3-U3 | MES 안에 공정라우트 그릇이 이미 있는가 (있으면 중복 구축 회피) |
| M3-U4 | PitStop 라이선스 보유 여부 · n8n 호스팅 위치 |
| M3-U5 | 22 접수파일 등록 / 26 출고예정일 / 28 의뢰서출력이 **MES 화면인지 신규 화면인지** — 범위 미결 |
| M3-U6 | 오프라인 주문·OEM 주문이 신규 시스템 범위인지 |

---

## C-9 — 「살아남는 부분」 목록에 대한 내 축의 확인

초안이 「교정 대상 아님」으로 남긴 항목 중 내 축과 닿는 것:

| 항목 | 내 판정 |
|---|---|
| §2 가격 브리지 `orderCnt = 총액÷10` | **생산축에 영향 없음** — MES 로 넘어가는 것은 payload 의 `total`/`supply`/`vat` 이고 orderCnt 는 Shopby 결제 표현일 뿐이다. 다만 **금액 대조**(§9.2.1)에는 영향이 있으니 M2 가 판정 |
| §5 저장소 목록 | **불완전** — C-4 로 교정 |
| §6 API 표면 개수 | **불완전** — C-5 로 교정 |
| A-6 테이블 수 불일치(44 vs 60/120) | **내 축에서 일부 해소**: `t_ord_orders`·`t_ord_artworks`·`t_ord_webhooks` 3개가 2026-08 이후 신설된 것이라 44 기준선이 낡았다. 전면 재실측 필요 |

---

## C-10 — lead 가 합칠 때 확인할 것 (M1·M2 와의 접합)

| 접합 | 확인할 것 |
|---|---|
| **M1 ↔ M3** | M1 의 `production-payload.md` 가 아직 없어 **내가 `widget_api.py:3388` payload 를 직접 읽었다**(`mes-handoff.md` §2). M1 산출이 나오면 **키 단위로 대조**할 것. 특히 `options`·`combo`·`proc_sels`·`addons[].components` 4개가 생산 정보의 본체다 |
| **M1 ↔ M3 (파일)** | ⚠ **payload 의 `files[].key` 는 tmp 버킷 키라 MES 가 못 가져간다.** MES 파일 목록의 출처는 `t_ord_artworks` 다(`models.py:1086`). M1 이 「payload 에 파일이 있다」로만 적으면 **틀린 인계 설계가 나온다** |
| **M2 ↔ M3** | 접점 6 중 **`shopby webhook`** 이 내 축과 겹친다. 수신함은 있고 소비자가 없다는 사실(A-9)을 M2 의 접점 항목에 반영할 것 |
| **M2 ↔ M3 (돈)** | Shopby 쿠폰·적립금·배송비가 「10원 상품 × N」과 충돌하면 **MES-1 로 넘어가는 금액도 틀어진다.** M2 의 `promotion-conflict.md` 판정이 내 축의 전제다 |
| **L1 편입** | C-0·C-1 을 반드시 먼저 읽을 것. 「통째로 빠졌다」 전제로 편입하면 12건이 중복 신설된다 |
