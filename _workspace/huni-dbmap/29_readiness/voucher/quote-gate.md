# 상품권 상품군 — 견적가능 Q-게이트 판정 (S5)

> 작성 2026-06-15 · round-19 · `dbm-readiness-auditor`. 대상 041/042 쿠폰·상품권(PRF_DGP_A).
> 견적가능 = **①UI 견적 + ②생산정보(차원환원) + ③가격 계산** 셋 다 성립([[dbmap-goal-ui-quote-mes]]). 하나라도 미달 = NO-GO(거짓 GO 금지).
> 모든 판정 = 본 세션 라이브 읽기전용 실측 인용(2026-06-15). "보인다" 금지.
> 선례 = `29_readiness/postcard/quote-gate.md`. 상품권 고유(수량단위·042 박·미싱/가변 핵심·고아 카테고리)는 독립 실측.

---

## 0. 결론

| 출력 | 상태 | 근거 요약 |
|------|:--:|----------|
| **① UI 견적**(옵션 선택) | **PASS** | 041=3그룹/10옵션아이템, 042=4그룹/22아이템 실적재. 손님이 인쇄·종이·후가공(미싱/오시/가변)·(042는 박칼라)을 화면에서 선택 가능 |
| **② 생산정보**(차원환원) | **PASS** | 041/042 전 option_items의 ref_dim_cd가 라이브 차원행으로 100% 해소(미해소 0/32·고아 0). 선택→자재/공정/도수 BOM 환원 성립. 042 박 8색도 PROC_000037~044 전수 환원 |
| **③ 가격 계산**(사슬) | **CONDITIONAL-PASS / 042박 FAIL** | 041/042 PRF_DGP_A 바인딩·기본부(인쇄+종이) 사슬 단절 0·라이브 단가만으로 재현. **단 ① 후가공 prc_typ 오적재(D-1b·🔴 미싱/가변=상품권 핵심) ② 042 박 comp 0개(🔴 신규 단절)** |

### 견적가능 종합 = **GO (조건부·잔존 2건)**

- **기본 견적**(인쇄+종이, 후가공·박 미선택) = 041·042 모두 **완전 견적가능**: ①②③ 성립, 라이브 단가만으로 재현(보정 하드코딩 0).
- **041 후가공 포함**(미싱/오시/가변) = ①② 성립·③은 D-1b 오적재로 과대 → **D-1b 정정 후 GO**.
- **042 박 포함** = ①②는 성립하나 ③에서 **PRF_DGP_A에 박 가격 comp 0개** → 가격 산출 불가 → **박 comp 신설 전까지 NO-GO**.
- 거짓 GO 방지: 상품권을 "완전 견적가능"이라 하지 않고 **"기본옵션 견적가능 GO + 잔존 2건(D-1b 후가공 가격·042 박 가격사슬 신설)"**으로 정직 판정. 엽서보다 잔존 1건(042 박) 많다.

---

## 1. Q1 컬럼 커버리지 — **PASS**

- `column-readiness.md` §1에 44 의미컬럼 전수 ✅/🟡/❌ 분류. **빈칸 0**.
- 집계: ✅ 32 · 🟡 9 · ❌ 0. 상품권 미해당 컬럼(별색·코팅·모서리·봉투)은 "제외확정/상품권 고유 ✅"로 명시(은폐 0).
- **PASS**(빈칸 0 기준 충족).

## 2. Q2 기초데이터 완전성 — **PASS**

라이브 실측(2026-06-15):

| 차원 | 041 | 042 | 판정 |
|------|:--:|:--:|:--:|
| siz (사이즈) | 2(013/014) | 2 | ✅ |
| plate (출력판형) | 1(SIZ_000499) | 1 | ✅ |
| mat (종이·USAGE.07) | 4 | 8 | ✅ |
| proc (공정) | 4(오시/미싱/가변2) | 12(+박 8) | ✅ |
| print_option (도수) | 2 | 2 | ✅ |
| clr (도수 색수) | CLR_000005/001 참조 실재 | 〃 | ✅ |

- 필요 차원(siz·plate·mat·proc·도수) 전부 행 존재. 042 박 공정 8종도 차원행 실재. **PASS**.

## 3. Q3 ①UI + ②차원환원 — **PASS**

### ① UI (option_groups/options/option_items 적재)
| 상품 | groups | items | 그룹 구성(sel_typ) |
|------|:--:|:--:|------|
| 041 | 3 | 10 | 인쇄(SEL.01 택1·2) · 종이(SEL.01 택1·4) · 후가공(SEL.02 택N max4·4) |
| 042 | 4 | 22 | 인쇄(택1·2) · 종이(택1·8) · 후가공(택N max4·4) · **박칼라(SEL.01 max1 mand=N·8+박없음)** |

- 손님이 화면에서 고를 항목(도수·종이·후가공[미싱/오시/가변]·042 박칼라)이 전부 옵션화됨. 박없음=null 선택지 정상. **① PASS**.

### ② 차원환원 (ref_dim_cd → 라이브 차원행 해소 = 생산정보)
트리거 `fn_chk_opt_item_ref` predicate(.03=mat_cd AND usage_cd 동시·.04=proc_cd·.06=opt_id::int)로 전수 해소 검사(prd_cd 직접 카운트·fan-out 허수 제거):

| 상품 | ref_dim 분포 | 차원행 해소 | 미해소(위반) |
|------|------|------|:--:|
| 041 | .03=4 · .04=4 · .06=2 (계 10) | .03 4/4·.04 4/4·.06 2/2 | **0** |
| 042 | .03=8 · .04=12(후가공4+박8) · .06=2 (계 22) | .03 8/8·.04 12/12·.06 2/2 | **0** |

- 전 32 option_item이 라이브 차원행으로 100% 환원 → 손님 선택이 자재(mat_cd+usage_cd)·공정(proc_cd·박 8색 포함)·도수(opt_id)로 BOM 변환 가능 = **생산정보 전달 성립**. MES_ITEM_CD 의존 아님(설계 제1원칙 충족).
- 고아 option_item(삭제된 옵션/그룹 가리킴) = **0**.
- ★042 박칼라 8색이 PROC_000037~044로 100% 환원됨이 핵심 검증(생산 BOM에 박종류 전달됨). 가격은 별개(Q4).
- **② PASS**.

### templates/constraints
- 041/042 addons 0 (상품권=봉투 추가상품 없음·정상·견적 본체 무영향).
- constraints 041/042 0행. PRF_DGP_A는 후가공이 독립 택N(SEL_TYPE.02)·박칼라 단일택1이라 하드 캐스케이드 제약 불요 → constraint 0 정당(NO-GO 사유 아님).

**Q3 = PASS** (①②, 트리거 위반 0/32).

## 4. Q4 ③가격사슬 — **CONDITIONAL-PASS (기본 GO / 041후가공 정정필요 / 042박 단절)**

라이브 사슬 실측:
1. **바인딩**: `t_prd_product_price_formulas` — 041/042 → **PRF_DGP_A**(apply_bgn 2026-06-01). ✅
2. **배선**: `t_prc_formula_components` — PRF_DGP_A에 **29 구성요소** addtn_yn=Y(인쇄2·별색10·코팅2·용지·오시3·미싱3·가변6·모서리2). ✅
3. **단가행**: `t_prc_component_prices` — 인쇄(siz499·CLR·수량구간)·용지(siz499·mat)·후가공(수량구간) 전부 단가행 보유. **기본부 사슬 단절 0**. ✅

### 결함 ① D-1b (엽서와 동형·상품권은 핵심공정 영향)
- 오시/미싱/가변 12 comp가 `prc_typ_cd=PRICE_TYPE.01`(단가형). 라이브 단가행 = 가격표 "합가(구간고정총액)"값(미싱1줄 실측: 100매=10000·300매=15000·1000매=25000). 단가형 ×수량 시 미싱1줄 100매=10000×100=**1,000,000원**(과대 990,000). 가격표 권위 = ⓒ 구간고정총액형(수량 무관 1회 부과)([[PRF_DGP_A/d1-plate-conversion]] §5). **값 정확·prc_typ 메타만 오적재**.
- ★상품권은 미싱(절취선)·가변(넘버링)이 핵심 공정이라 거의 모든 실주문이 후가공 포함 → D-1b 영향이 엽서보다 크다.

### 결함 ② 042 박 가격사슬 단절 (🔴 신규·엽서엔 없음)
- 042 박 8색(PROC_000037~044)은 ①UI·②차원환원까지 완비됐으나 **PRF_DGP_A 29 comp에 박(foil) 가격 comp가 0개**. 박 comp는 DB에 명함전용(COMP_NAMECARD_FOIL_*)으로만 존재 = PRF_DGP_A 미배선.
- → 042 박 선택 시 가격에 박값이 0으로 누락(침묵 미반영) = 가격사슬 단절. **박 comp 신설+배선+단가 적재 전까지 042 박 포함 견적 가격 불가**.

- 기본옵션 = **PASS** / 041 후가공 = **FAIL(D-1b 과대)** / 042 박 = **FAIL(comp 0개 단절)**. → **CONDITIONAL-PASS**.

## 5. Q5 견적 시뮬레이션 — **PASS(기본) / FAIL(041후가공·042박)**

### C1 — 041·148×68·단면4도·백색모조지100g(MAT_000072)·무후가공·100매 (보정 하드코딩 0)
- 차원환원: 도수=opt_id1(단면 CLR_000005) / 종이=MAT_000072·USAGE.07
- D-1 출력판형: 작업사이즈 SIZ_000013 → note 전지 316×467 → **SIZ_000499**
- 가격(라이브 component_prices 조회):
  - 인쇄 COMP_PRINT_DIGITAL_S1 siz499·CLR_000005·min_qty=100 → **350.00**/장 ×100 = 35,000
  - 용지 COMP_PAPER siz499·MAT_000072 → **30.73**/장 ×100 = 3,073
  - **= 38,073원** (라이브 단가만으로 산출·보정 하드코딩 0) ✅
- **기본옵션 시뮬 = PASS**.

### C2 — 041·위 + 미싱1줄 추가 (상품권 핵심)
- 차원환원: 미싱=PROC_000030(✅ 환원). 단 **줄수(1줄)는 보존 안 됨(GAP-PARAM)**.
- 가격: COMP_PP_PERF_1L 100매 단가행=10,000. 현 prc_typ=.01(단가형) → 엔진 ×100 = **1,000,000원**(비합리). ⓒ 정정 시 = 10,000(1회) → 총액 48,073원(가격표 정합).
- **미싱 시뮬 = FAIL**(D-1b 오적재로 990,000 과대). 상품권 핵심 공정이라 거의 전 주문 영향.

### C3 — 042·박(금유광) 추가
- 차원환원: 박=PROC_000038(✅ 환원).
- 가격: **PRF_DGP_A에 박 comp 0개** → 박값 조회 대상 자체 없음 → 침묵 누락(박값 0). **가격 산출 불가(단절)**.
- **042 박 시뮬 = FAIL**(가격사슬 신설 필요).

## 6. Q6 정직 갭 — **PASS**

미달 항목 전부 차단유형+라우팅으로 분류(§7). over/under-report 0:
- ②차원환원을 fan-out 허수로 과대보고하지 않음(prd_cd 직접 카운트·진실 32 item·위반 0).
- ③가격을 "완전 GO"로 과대보고하지 않음(D-1b + 042박 단절 명시·기본만 GO).
- 엽서 선례를 그대로 베끼지 않고 상품권 고유(수량 12·042 박·미싱핵심·모서리 없음·고아 카테고리)를 라이브로 독립 확인.

---

## 7. 차단 항목 + 라우팅 (S1)

| ID | 차단 항목 | 차단유형 | 영향 | 라우팅 대상 | 비고 |
|----|-----------|----------|------|------------|------|
| **D-1b** | 후가공 12 comp(오시/미싱/가변) prc_typ=.01 오적재 → ⓒ 구간고정총액형이어야 | 가격유형 메타 오적재(🔴 돈-크리티컬) | 후가공 포함 견적 과대(미싱1줄 100매 +990,000)·**상품권 핵심공정이라 거의 전 주문 영향** | **round-18 `dbm-price-engine-verifier` → webadmin Phase11**(PRICE_TYPE.03 신설 or .02+엔진규칙 "구간총액 그대로") | 엽서 D-1b와 동일 사실·값 정확·메타만 정정·인간 승인. [[PRF_DGP_A/d1-plate-conversion]] §5 |
| **042-FOIL** | 042 박 8색 UI·차원환원 완비·**PRF_DGP_A에 박 가격 comp 0개**(명함전용만 존재) | 가격사슬 단절(🔴 신규·돈-크리티컬) | 042 박 선택 시 박값 침묵 누락(가격 불가) | **round-16 `dbm-price-import-builder`**(박 comp 신설·formula_components 배선·박색별 단가 적재) + **round-2 `dbm-price-formula`**(박칼라 가격구조 결정: 박색별 단가/면적등급/셋업비) | ★엽서엔 없던 상품권 042 고유. 가격표 박 시트 권위 대조 필요. C-06(부모 박 PROC_000033 연결)도 함께 정리 |
| **GAP-PARAM** | 후가공 줄수(오시/미싱)·개수(가변) 0~3 보존처(ref_param_json) 부재 | 그릇(param 슬롯) 부재 | 줄수/개수별 단가 분기 불가(현재 단일 단가) | **`dbm-ddl-proposer`**(ALTER 제안) | C31~34 🟡. 상품권은 미싱/가변 핵심이라 우선순위 ↑. 엽서 GAP-PARAM과 동류 |
| **C-05/F-ID-3** | 041/042 잉여 고아 카테고리 CAT_000295(upr=NULL)에 이중 오연결(정상 노드 063/064는 실재) | 카테고리 오연결 | 견적 본체 무영향(정상 노드 실재) | **round-13 `dbm-correctness-auditor`**(고아 295 논리정리·이중연결 해소) | 정상 분류 이미 있음·search-before-mint |
| **C-06** | 042 박 부모 PROC_000033 미연결(8색만 직접 연결) | 공정 위상 부분 | 차원환원엔 무영향(8색 전수 해소) | round-13 + 042-FOIL과 동반 | 가격 comp 신설 시 함께 결정 |
| **C11** | plate output_file_typ 일부 NULL | 일부 NULL | 견적 영향 경미(생산메타) | (보류·Low) | — |

> ✅ 요소(기초데이터·CPQ 옵션·기본 가격사슬)는 이미 라이브 적재됨 → 적재 단계(S2~S4) 스킵. 라우팅은 ❌/🟡 **D-1b·042-FOIL·GAP-PARAM·C-05/C-06**만.

---

## 8. 다음 행동 (견적가능 폐루프 닫기)

| 우선 | 행동 | 라운드 | 닫히는 것 |
|:--:|------|------|----------|
| **High** | 후가공 prc_typ 정정(ⓒ 구간고정총액형)·합가형 엔진규칙 명세 | round-18 → webadmin Phase11 (인간 승인) | ③ 041/042 후가공 포함 견적 정상화(상품권 핵심공정) |
| **High** | ★042 박 가격구조 결정 + 박 comp 신설·PRF_DGP_A 배선·박색별 단가 적재 | round-2 `dbm-price-formula` + round-16 `dbm-price-import-builder` (인간 승인) | ③ 042 박 포함 견적 가능(현재 침묵 누락) |
| Medium | 후가공/박 줄수·개수 param 그릇(ref_param_json) DDL 제안 | `dbm-ddl-proposer` (인간 승인) | C31~34 🟡→✅, 줄수/개수별 단가 분기 |
| Low | 041/042 고아 카테고리 CAT_000295 논리정리·이중연결 해소 + 박 부모 PROC_000033 연결 | round-13 (경미) | C1/C-05/C-06 정합 |
| Low | plate output_file_typ NULL 보강 | round-13 (경미) | C11 |

**폐루프**: ① D-1b 정정 적용 후 C2(미싱) 재시뮬 → 48,073원(가격표 정합) 확인 + ② 042 박 comp 적재 후 C3(박) 재시뮬 가격 산출 확인되면 상품권 = **완전 견적가능 GO**. 현재는 **기본옵션 견적가능 GO + 후가공 가격 정정(D-1b·041/042 공통) + 042 박 가격사슬 신설(042 고유) 2건 잔존**.
