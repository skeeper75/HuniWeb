# N1·N2·N3 lead 검산 + 합침 (2026-09-02 · lead-peta [a760e0])

세 카드 모두 `progress.md` 에 「완료」를 적었다. lead 규칙대로 lane 주장을 믿지 않고 **산출 파일을 직접 세어** 판정했다.

## 1. lead 재실측 — lane 주장 검증

| 카드 | lane 주장 | lead 재현 (명령 · 관측) | 판정 |
|---|---|---|---|
| N2 | v2 501행 · std_id 중복 0 · L1 353 원문 보존 · 근거 빈칸 0 | `python3 _work/verify_v2.py` 재실행 → V-1~V-14 **전부 PASS**. 독립 카운트: 행 501 · 유니크 501 · 대분류 18 · 빈칸 0 · L1 353 ⊂ v2 = True · 신규 148 | ✅ 확인 |
| N2 | 무작위 15건 재대조 불일치 0 · 신규 148행 근거 파일 실재 | `recheck15.py` → 「표본 15 · 불일치 0」 · `recheck_all.py` → 「전수 148행 · 근거 미실재 0」 | ✅ 확인 |
| N2 | legacy 716 = 귀속 652 + 미귀속 64(X 55 · P 9) | verify V-13/V-14 재실행 동일 | ✅ 확인 |
| N3 | 초안 오류 잔존 0 · 교정 41건 반영 | `_mechanical-check.txt` 12항 잔존 0 · 19항 반영 ≥1 → PASS | ✅ 확인 |
| N3 | 위젯 API 22 · assistant 5 | `grep -c 'path("api/w/v1' raw/webadmin/catalog/urls.py` → **22** · `api/assistant` → **5** | ✅ 확인 |
| N3 | §11 「주문 이후」는 N1 대기로 비워 둠 | `grep -c 'N1 대기' ARCHITECTURE-v2.md` → **10** | ✅ 확인 (합침 대상) |
| N1 | webhook 이벤트 전수 33종 | `docs/shopby/shopby-api/workspace-server-public.yml` :624-637 · :782-796 대문자 토큰 유니크 → **33** (샵바이 24 + `GD_*` 9) | ✅ 확인 |
| N1 | 상태 매트릭스 13행×10조작 · 미확인 47건 | 상태머신 §3 실재 · open-questions A14+B12+C3+D5(+D-6)+E6+F3+G4 = 47 | ✅ 확인 |
| N1 | 완료조건 ④ 셀러어드민 실화면 **미충족** | 자격증명 부재로 접속 0회 — lane 이 정직하게 미충족 표기 | ✅ 정직 판정 인정 |

**세 카드 전부 GO.** N1 ④ 미충족은 lane 과실이 아니라 **자격증명이 저장소에 없기 때문**이다 — 지니가 줘야 닫힌다(§4).

## 2. N2 가 lead 에 넘긴 판단 5건 — 확정

| # | 판단 | 결정 | 근거 |
|---|---|---|---|
| 1 | `mapping.csv` 갱신(재귀속 56 + 재매핑 권고 4) | **O3 가 통합 원장에서 수행** — 상태(done/partial/todo)는 §5 공통규칙(done=L2 file:line 필수) 그대로 | 매핑 재판정은 원장 병합과 한 번에 해야 3분할 경계가 안 흔들린다 |
| 2 | `STD-ART-004`·`005` `new` 뒤집기 | **`partial` 로 뒤집는다** · 매핑 `F-052`·`IA-052`·`SCOPE-052` | legacy 052 「D1 파일 업로드」의 하위 변형. 1:N 정상(§4-2). 「신규 개발」로 읽히면 이미 있는 업로드를 또 만든다 |
| 3 | `new` 7건 「실물 근거 있음」 표시 | **채택** — 상태는 `new` 유지, `비고=실물있음(M3 행)` 열 추가. 단 `STD-ART-016`→`MFG-041`, `STD-ADO-011`→`MFG-097` 은 **삭제 흡수** | N2 §3 그대로 |
| 4 | 정보·콘텐츠 이관 3행(`CLM-018`·`CAT-019`·`CAT-020`) | **채택** — O3 원장 병합 시 한 번에 | L4 3분할 경계는 O3 에서 통합되므로 문제 없음 |
| 5 | `STD-ADO-015` JDF/XJDF | **「후니 미채택」 표기 · 구현 축은 `STD-MFG-013`(WCF)** | N3 A-22 와 일치 |

## 3. 합침 — N1 → N3 §11

N3 v2 §11 여덟 소절은 전부 「N1 대기」다. N1 산출 6종이 그 자리를 채운다. **O3 가 병합**하되(`ARCHITECTURE-v3.md` 가 아니라 v2 §11 을 N1 산출 링크+요지로 채운 `ARCHITECTURE-v2.md` 갱신), N1 이 카드 명세 정정으로 남긴 2건은 **반드시 반영**한다:
- **고정 IP 는 아웃바운드 제약**(후니→샵바이 server API), 인바운드 웹훅 URL 제약은 미확인. v2 가 인바운드처럼 적었다면 정정.
- 취소요청 가능 구간 = **상품준비중·배송준비중 둘뿐**, 배송중부터는 반품 경로.

## 4. N 이 드러낸 사실 — 전략에 그대로 올라가는 것

1. **결제승인 현재 단계** — 문서로는 확인되지 않았으나(N1 C-1) **지니가 09-02 12:58 확인: 「심사 중 · 신청서 제출함」**. 이니시스 가맹점관리자 추가 작업은 실무진이 진행 중(개발 불가 영역). C-1 은 「지니 1차 지식으로 닫힘 · 캡처는 실무진 완료 후」로 재분류.
   **셀러어드민 계정은 환경변수에 있다**(지니 확인) → N1 ④ 미충족과 A 14건은 레인이 **읽기전용**으로 닫을 수 있다 → O1 에 배정.
2. **결제수단 결정 기록이 없다**(N1 D-1). 원천사 심사가 네이버페이 3~4주·카카오 2주·PAYCO 10~12영업일 → **D-34 에서 가장 먼저 걸리는 외부 의존.**
3. **웹훅 정책 7건 전부 문서 공백**(N1 B-1) · 등록 API 없음 · 재발송 API 없음 · 실패조회 7일 창 → NHN 문의 필수. `CHANGE_ORDER_STATUS` 상태값 목록(B-1g)이 없으면 결제→생산 전환 코드를 못 짠다.
4. **셀러어드민·이니시스 가맹점관리자 자격증명이 저장소에 없다**(N1 A·F) → 미확인 14+3건이 여기 묶여 있다.
5. **분모 확정 501행** · legacy 716 전건 귀속 판정 완료(652 귀속 · 64 X/P 트랙).
6. **지도 v2 완성**(§11 만 합침 대기) · 「차단」은 A-8 MES 품목코드 · A-21 세금계산서 수량표기 둘뿐.

## 5. 다음 카드로 넘기는 것

- **O1 테스트 시나리오**: N1 상태머신 §3 매트릭스 + `nextActions`/`cancelable` 플래그(런타임 교차검증 수단) + G-1·G-2 는 **두 분기 다 써라**.
- **O2 L5**: 임계경로의 외부 의존 = PG(결제수단 심사) · MES(품목코드·스펙) · NHN 회신(웹훅 정책). 세 개가 「차단」 후보 전부다.
- **O3 원장·닫기팩**: §2 결정 5건 적용 · N1 open-questions 47 을 「닫는 수단별 실행팩」으로.

## 6. lead 자기 기록

- 이 판정에서 lead 가 새로 조사한 것은 없다. 재실행한 스크립트와 grep 만 있다.
- `07_rebaseline/` 전체가 **git 미추적**이다(`git status` `??`). 커밋은 지니 지시 시.
- lane 상태(ListAgents 12:58): `sync-huniweb [941347]` idle · `plan-huniweb [5d5bad]`·`run-huniweb [a067ab]` busy(마무리 중으로 추정 — progress 는 완료).
