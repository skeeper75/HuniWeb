# P12 · 에디터로 디자인하기

> t63 프로세스 정리 B — 탐색~결제 · L1 **원고·파일** · L2 에디터

## 1. 한줄정의 · 시작/끝 · 등장인물

**한줄정의** — 원고가 없는 손님이 에디쿠스 에디터에 들어가 템플릿을 골라 직접 만들고 인쇄용 파일로 내보낸다.

- **시작**: 손님이 「디자인하기」로 에디터에 진입한다
- **끝**: 에디터 산출물이 인쇄용 파일로 변환되어 주문에 붙는다

| 등장인물 | 무엇인가 |
|---|---|
| 고객 | 몰을 쓰는 손님(회원·비회원) |
| huni-mall | 신규 독립몰(headless · Next.js 15 App Router) |
| 에디쿠스 | 온라인 디자인 에디터(Edicus) |
| webadmin | 후니 자체 관리서버(Django) — 상품·옵션·가격·위젯빌더 |

**가로지르는 곳**
- P04 — 디자인 상품에서 고른 디자인이 여기 템플릿으로 들어온다
- P06 — 사이즈·페이지 선택과 캔버스를 맞춘다
- P03 — 템플릿/샘플 다운로드(STD-CAT-020)와 같은 자산

## 2. 흐름 (sequence)

```mermaid
sequenceDiagram
    actor C as 고객
    participant M as huni-mall
    participant E as 에디쿠스
    participant W as webadmin
    C->>M: 에디터 진입(상품코드 + 선택 옵션)
    M->>W: 옵션 조합에 연결된 템플릿 조회
    alt 템플릿 없음
        W-->>M: 「옵션 조합에 연결된 편집기 템플릿 없음」
        M-->>C: 오류 — 진입 불가
    else 템플릿 있음
        W-->>M: 템플릿 식별자
        M->>E: 에디터 기동(템플릿 · 사이즈 · 페이지)
        E-->>C: 캔버스
        C->>E: 편집 · 저장
        E->>M: 산출물 반환
        M->>M: 인쇄용 파일 변환 · 주문 연결
    end
```

## 3. 갈림길 (flowchart · 실패·비회원·취소 분기)

```mermaid
flowchart TD
    A[에디터 진입] --> B{옵션 조합에 템플릿이 연결됐나}
    B -- 아니오 --> B1[「편집기 템플릿 없음」 오류 — 연결 점검 대상]
    B -- 예 --> C[템플릿 선택·불러오기]
    C --> D[캔버스 편집]
    D --> E{사이즈·페이지가 바뀌면}
    E --> F[캔버스 동기화]
    F --> D
    D --> G[작업물 저장 — 나중에 재편집]
    G --> H[인쇄용 파일 변환]
    H --> I[주문에 연결]
    B1 --> J[삭제된 사이즈 코드가 물린 템플릿 전수조사]
```

## 4. 단계표

| # | 단계 | 시스템 | 담당자 | 원장 row_id | 상태 | 근거 |
|---|---|---|---|---|---|---|
| 1 | 온라인 디자인 에디터 진입 | widget | 서희항 | `STD-ART-021` | 부분 | raw/webadmin/webadmin/catalog/static/catalog/widget.js:368-395 |
| 2 | 「옵션 조합에 연결된 편집기 템플릿 없음」 오류 — 에디쿠스 템플릿 연결 점검 | webadmin | 서희항 | `STD-ART-030` | 미착수 | raw/webadmin/webadmin/catalog/edicus_views.py:327-419 · raw/webadmin/tools/audit_edicus_coverage.py:1-20 |
| 3 | 에디터 템플릿 선택·불러오기 | widget | 서희항 | `STD-ART-022` | 작동 | raw/webadmin/webadmin/catalog/static/catalog/widget.js:634 |
| 4 | 삭제된 사이즈 코드가 연결된 템플릿 전수조사(800여 개 중 노출 대상 선별) | webadmin | 서희항 | `STD-ART-031` | 미착수 | raw/webadmin/webadmin/catalog/edicus_views.py:327-419 · raw/webadmin/tools/audit_edicus_coverage.py:82-232 |
| 5 | 에디쿠스 테스트용 단순 상품(메모패드 등) 제공 | webadmin | 서희항 | `STD-ART-032` | 미착수 | 「미확인」 — 찾지 못했다(webadmin/catalog·tools·.planning 전수 grep 메모패드/memo pad/테스트용 단순 상품 — 메모패드는 실사고 재현 사례로만 등장하고 에디쿠스 테스트 전용 상품을 의도적으로 제공하는 기능/문서는 없음) |
| 6 | 선택 옵션(사이즈·페이지)과 에디터 캔버스 동기화 | widget | 서희항 | `STD-ART-025` | 부분 | raw/webadmin/webadmin/catalog/static/catalog/widget.js:378-394 |
| 7 | 에디터 작업물 저장·재편집 | widget | 서희항 | `STD-ART-023` | 미착수 | raw/webadmin/webadmin/catalog/static/catalog/widget.js:613-615 · raw/webadmin/webadmin/catalog/static/catalog/widget.js:625-634 |
| 8 | 에디터 산출물 인쇄용 파일 변환·주문 연결 | widget | 서희항 | `STD-ART-024` | 부분 | raw/webadmin/webadmin/catalog/static/catalog/widget.js:1004 |
| 9 | 에디터 산출물 변환 실패 처리 | huni-mall | 서희항 | — | **원장 행 없음** | 「미확인」 |

## 5. 빠진 곳

**가 원장에 행 없음** — 1건

| row_id | 내용 | 진단 |
|---|---|---|
| — | 에디터 산출물 변환 실패 처리 | 인쇄용 파일 변환이 실패했을 때의 손님 안내·재시도가 원장에 없다 |

**나 행은 있는데 코드 0** — 1건

| row_id | 내용 | 진단 |
|---|---|---|
| `STD-ART-032` | 에디쿠스 테스트용 단순 상품(메모패드 등) 제공 | CLAUDE.md·STATE.md 의 메모패드 언급은 전부 STD-ART-030/031 사고 재현·감사 도입 계기이지 테스트용 단순 상품 제공 자체는 별도 산출물로 존재하지 않음. |

**다 코드는 있는데 연결 안 됨** — 6건

| row_id | 내용 | 진단 |
|---|---|---|
| `STD-ART-021` | 온라인 디자인 에디터 진입 | _onEditor 가 huni:editor(cancelable) 발화 후 /api/w/v1/editor/resolve 호출 → 오버레이로 편집기 진입시키는 전체 흐름. |
| `STD-ART-030` | 「옵션 조합에 연결된 편집기 템플릿 없음」 오류 — 에디쿠스 템플릿 연결 점검 | gap_report()가 편집기 버튼은 켜졌는데 템플릿 0행([C]류 |
| `STD-ART-031` | 삭제된 사이즈 코드가 연결된 템플릿 전수조사(800여 개 중 노출 대상 선별) | gap_report()의 dead(죽은 행 = 템플릿이 가리키는 값을 고객이 못 고름 |
| `STD-ART-025` | 선택 옵션(사이즈·페이지)과 에디터 캔버스 동기화 | _onEditor 가 매번 selections/sel_opts/proc_sels 를 모아 서버 resolve 를 다시 불러 ps_cd/tmpl_uri 를 재산출 — 옵션 변경 시 편집기 규격이 재동기화됨. |
| `STD-ART-023` | 에디터 작업물 저장·재편집 | sdk.on(save) 가 setEditorResult 로 prjid 저장(613-615) 이후 재진입 시 prev.prjid 있으면 openProject 로 이어 편집(631-632) — 저장·재편집 확인. |
| `STD-ART-024` | 에디터 산출물 인쇄용 파일 변환·주문 연결 | handoff payload 의 editors[]에 prjid 가 실려 주문에 연결되나(구조는 확인) Edicus 쪽 실제 인쇄용 고해상 파일 렌더링(definitive_order)은 edicus_lookup.py 주석상 미배선 — 주문 연결은 부분 |

## 6. 채우는 방법

| 누가 | 무엇을 | 선행 |
|---|---|---|
| 서희항 | 에디터 산출물 변환 실패 처리 | 원장 735행에 행을 새로 섞지 않는다 — 이 제안으로만 남긴다 |
| 서희항 | 온라인 디자인 에디터 진입 | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 「옵션 조합에 연결된 편집기 템플릿 없음」 오류 — 에디쿠스 템플릿 연결 점검 | 코드는 있는데 원장 상태가 「미착수」다 |
| 서희항 | 삭제된 사이즈 코드가 연결된 템플릿 전수조사(800여 개 중 노출 대상 선별) | 코드는 있는데 원장 상태가 「미착수」다 |
| 서희항 | 에디쿠스 테스트용 단순 상품(메모패드 등) 제공 | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| 서희항 | 선택 옵션(사이즈·페이지)과 에디터 캔버스 동기화 | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 에디터 작업물 저장·재편집 | 코드는 있는데 원장 상태가 「미착수」다 |
| 서희항 | 에디터 산출물 인쇄용 파일 변환·주문 연결 | 코드는 있는데 원장 상태가 「부분」다 |

## 7. 확인 못 한 것

삭제된 사이즈 코드가 연결된 템플릿이 800여 개 중 몇 개인지는 선행 관측이고 이 카드에서 재실측하지 않았다.

---
> 이 문서의 상태·근거는 원장 `08_system-screen/t56/rejudge.csv` 와 이 카드의 증거 수집본에서 기계로 채웠다. 「미확인」은 코드·원장·명세를 뒤졌으나 **찾지 못했다**는 뜻이지, 없다는 뜻이 아니다.
