# 포토북 — 교정 매니페스트 (correction-manifest, round-13 C4)

> **작성** 2026-06-11 · round-13. live-diff의 각 발견을 CORRECT/MIS-LOADED/MISSING/EXTRA/AMBIGUOUS로 분류 + why(oracle 근거 + 적재로직 원인) + how(비파괴 교정 제안) + 심각도 + 라우팅. 빈칸 0.
>
> **[HARD] 비파괴:** 교정은 **제안**까지. COMMIT/DDL/DELETE 절대 없음. EXTRA/REMOVED=논리삭제 제안. **search-before-mint**(기존 행 재사용 우선). 실 교정=round-5/6 + 인간 승인.
>
> **[HARD] v03 directive:** 모든 MIS-LOADED의 진원은 상류 v03 정규화. load_master는 충실 전파기. 정답=상품마스터 L1.

---

## 0. 분류 분포

| 분류 | 건수 | ID |
|------|:--:|----|
| CORRECT | 9 | PB-OK-1 ~ PB-OK-9 |
| MIS-LOADED | 4 | PB-C1·PB-C2·PB-C3·PB-C4 |
| MISSING | 2 | PB-M1·PB-M2 |
| AMBIGUOUS/GAP | 2 | PB-A1·PB-A2 |
| EXTRA | 0 | — |
| **합계 발견** | **17** | (CORRECT 9 + 결함 8) |

> **[F-PB-1 재분류]** 종전 PB-M3(소프트 page "4~14" MISSING) → **PB-A2(엑셀 공란 GAP·AMBIGUOUS)**. "4~14"는 엑셀에 없는 추정값이라 정답값에서 삭제, 라이브 24/150/2는 오적재 아님(엑셀 유일 page 행 충실 적재). MISSING 3→2·AMBIGUOUS 1→2.

---

## 1. MIS-LOADED (4) — 라이브가 틀림, 교정 대상

### PB-C1 — 레더 자재유형 혼재 (.01 종이 + .08 실사소재, 정답 .06 가죽) 🔴

| 필드 | 내용 |
|------|------|
| **상품·속성** | PRD_000100 · 자재(USAGE.02 표지 레더) |
| **라이브 현재값** | 포토북 연결 레더 2건의 자재유형 오분류: MAT_000006 레더하드커버(**.01 종이**·note "포토북 표지 자재") + MAT_000186 레더(화이트)(**.08 실사소재**). 별도 .06 가죽 행(MAT_000008/173/174/175) 4행 고아(포토북용 아님 — note 책자/base) |
| **정답값** | 포토북 연결행(MAT_000006·MAT_000186)의 자재유형 = **MAT_TYPE.06 가죽**(usage=USAGE.02 표지). 연결 자체는 정상 |
| **why** | **[F-PB-3 보정]** 레더=가죽은 `MAT_TYP_OVERRIDE`(load_master L116~121, "레더하드커버 A/A4/A5"=가죽) + 라이브 .06 가죽행 실재(MAT_000008/173~175 mat_typ=.06)로 입증(SDI 453은 "레더 용도=표지"까지·자재유형 단정은 override/라이브 근거). **적재로직 원인:** override가 슬러그 "레더하드커버 A/A4/A5"(173/174/175)만 가죽화했으나 포토북이 연결한 건 "레더하드커버"(006, 접미 A 없음)+"레더(화이트)"(186). 006은 override 미적용→자재구분 원천 .01 종이, 186은 ENUM_ALIAS "실사"→.08. **진원=v03가 레더를 중복 슬러그로 갈라놓음** |
| **how (비파괴)** | **[F-PB-2 보정] prd_cd 재연결 아님 — 자재유형(mat_typ_cd)만 교정.** validator note 전수 재실측: 포토북이 연결한 **MAT_000006 note="포토북 표지 자재"·MAT_000186**은 이미 포토북 전용 연결행이고 틀린 건 mat_typ뿐(.01·.08→.06). 반면 가죽 고아행은 포토북용 아님 — **MAT_000008 note="책자 표지 자재"**·**MAT_000173 note="IMPORT 약어/base 기준 root"**(이름만 정합·실제 책자/base). 따라서 정답 교정 = `UPDATE t_mat_materials SET mat_typ_cd='MAT_TYPE.06' WHERE mat_cd IN ('MAT_000006','MAT_000186')`(**자재유형만**, prd_cd 재연결·논리삭제 불요 — 연결행이 이미 포토북용). 가죽 고아행(008/173~175)은 재연결 대상 **아님**(별도 중복정리 사안). **단 MAT_000186은 6상품 횡단 연결이라 .06 일괄 교정 영향 확인 후 — 컨펌-Q1** |
| **심각도** | **High** (자재유형=생산 BOM·라우팅 근간. round-12·책자 BK-4와 통합 결정 가능) |
| **라우팅** | 🔴 컨펌-Q1 → load-execution(mat_typ_cd UPDATE) |

### PB-C2 — 아트250+무광코팅 복합표기 평면화 + 자재 중복

| 필드 | 내용 |
|------|------|
| **상품·속성** | PRD_000100 · 자재(USAGE.02 아트250) + 공정(무광코팅) |
| **라이브 현재값** | MAT_000250 "아트250+무광코팅"(.01 종이) 단일 자재 — **코팅 공정 미분리**. + 중복 자재 MAT_000260 "아트250 + 무광코팅"(공백차, 7상품 연결)·MAT_000172 미사용 |
| **정답값** | 자재 = **아트250(.01 종이, 코팅 제거명)** + 공정 = **무광코팅 PROC_000015**(별 연결) |
| **why** | **SDI 348(복합표기 단일 자재 ✗·코팅 분해)·Q9(코팅=공정)**. **family 불일치 결정 증거:** 책자류(PRD_000068~072·082)는 동일 무광코팅을 PROC_000015 공정으로 **분리 연결**(live-diff §6.2). 포토북만 자재명에 평면화. **적재로직 원인:** load_master는 자재명 그대로 전파(L236), 코팅 분해 로직 없음. v03 복합표기·중복 슬러그 진원 |
| **how (비파괴)** | ① 자재명 정정 제안: MAT_000250 mat_nm "아트250+무광코팅"→"아트250"(또는 신규 "아트250" 자재로 재연결) ② 코팅 공정 추가: `INSERT t_prd_product_processes (PRD_000100, PROC_000015, mand=N)`(무광) ③ 중복 250/260 통합(search-before-mint: 7상품 쓰는 260로 통일 검토). **컨펌-Q2(코팅 분해 일관성)** |
| **심각도** | Medium-High (Q9 family 통일·가격엔진 코팅 component 영향) |
| **라우팅** | 🔴 컨펌-Q2 → ddl-proposer(자재정리) + load-execution(코팅 연결) |

### PB-C3 — PUR 제본 mand_proc_yn=N (정답 Y)

| 필드 | 내용 |
|------|------|
| **상품·속성** | PRD_000100 · 공정(PUR 제본 필수성) |
| **라이브 현재값** | PROC_000020 PUR mand_proc_yn=**N** |
| **정답값** | mand_proc_yn=**Y**(제본=본질적 필수 공정·PUR 단일·택일 불요) |
| **why** | SDI 398(제본=필수)·loadspec K-3. **적재로직 원인:** `load_rel_processes` L416 `_yn(필수공정여부)` — v03 15시트 필수여부가 빈값/N → `_yn`이 N으로 적재(L80: 빈값→N). 진원=v03 필수여부 미기입 |
| **how (비파괴)** | `UPDATE t_prd_product_processes SET mand_proc_yn='Y' WHERE prd_cd='PRD_000100' AND proc_cd='PROC_000020'`. excl_group 불요(PUR 단일) |
| **심각도** | Low-Medium (단일 공정이라 런타임 영향 작으나 의미상 교정) |
| **라우팅** | 교정 직접(단순 UPDATE·컨펌 불요) → load-execution |

### PB-C4 — qty_unit_typ_cd 적재경로 불명 (참고)

| 필드 | 내용 |
|------|------|
| **상품·속성** | PRD_000100 · 수량단위 |
| **라이브 현재값** | qty_unit_typ_cd=QTY_UNIT.03 권 |
| **정답값** | 권(맞음) — 단 load_master는 **NULL 강제**(L269 주석: 시트10에 원천 컬럼 없음) |
| **why** | load_master가 NULL 적재했는데 라이브엔 .03 존재 = **별도 세션 backfill**(적재경로 불명). 값 자체는 정답이나 **출처가 load_master 아님** = finding(어떻게 들어왔는지 모름) |
| **how** | 값 정답이라 교정 불요. **적재경로 불명을 기록**(향후 재적재 시 NULL로 덮어쓰지 않도록 주의 — load_master --all 재실행하면 NULL로 회귀) |
| **심각도** | Low (값 정합·경로만 불명) |
| **라우팅** | 컨펌(경로 확인) — 정보 보존 |

---

## 2. MISSING (2) — 있어야 하는데 라이브에 없음

### PB-M1 — 가격 전체 미적재 (가장 영향 큰 미적재)

| 필드 | 내용 |
|------|------|
| **상품·속성** | PRD_000100 · 가격(C37 base 24P + C38 per-page 2P당) |
| **라이브 현재값** | `t_prd_product_prices` 0행·`t_prd_product_price_formulas` 0행 |
| **정답값** | size×표지타입 매트릭스 base(11 활성조합) + per-page 증분. **고정가형**(SDI 409) |
| **why** | load_master에 가격 적재 함수 **부재**(MASTERS/RELATIONS 목록에 없음). 가격=round-2 별 트랙인데 **포토북은 한 번도 실행 안 됨**. **round-14 경고:** Phase11이 `t_prd_template_prices`(SKU 직접단가)·`PRICE_TYPE`(단가/합가형)·component_prices proc/opt 차원 신설 — round-12 mapping-final의 `t_prd_product_prices` 목표는 고정가형엔 여전히 유효하나 SKU별 단가면 template_prices가 적합 |
| **how (비파괴)** | round-2 가격엔진(`dbm-price-formula`)로 적재. 활성 11조합 base + per-page. 목표 테이블=고정가 직접단가 `t_prd_product_prices` 우선(SDI 409), 단 SKU(size×표지타입)별이면 `t_prd_template_prices` 검토. **컨펌-Q3(가격 목표 테이블)** |
| **심각도** | **High** (가격 없으면 견적 불가·포토북 핵심 가치) |
| **라우팅** | 🔴 컨펌-Q3 → round-2 dbm-price-formula |

### PB-M2 — 무광코팅 공정 미연결 (PB-C2와 연동)

| 필드 | 내용 |
|------|------|
| **상품·속성** | PRD_000100 · 공정(표지 무광코팅) |
| **라이브 현재값** | 코팅 공정 연결 0행(자재명에 평면화) |
| **정답값** | PROC_000015 무광(부모 PROC_000013 코팅) 연결, 표지(하드/소프트) |
| **why** | Q9(코팅=공정)·책자류 정합(live-diff §6.2). PB-C2의 공정 측 — 자재 평면화의 이면 |
| **how** | `INSERT t_prd_product_processes (PRD_000100, PROC_000015, mand=N, disp_seq)`. search-before-mint: PROC_000015 무광 기존 존재(신규 mint 불요) |
| **심각도** | Medium (PB-C2와 통합 교정) |
| **라우팅** | 🔴 컨펌-Q2(PB-C2와 동일) → load-execution |

### PB-A2 — 소프트/레더 page_rule 엑셀 공란 (GAP) **[F-PB-1 재분류: MISSING→AMBIGUOUS/GAP]**

| 필드 | 내용 |
|------|------|
| **상품·속성** | PRD_000100 · page_rule(표지타입별 차등) |
| **라이브 현재값** | 24/150/2 단일 행(하드 기준) |
| **정답값** | **불확실(GAP)** — **엑셀 L1 직접 파싱 결과 page 값은 하드커버 행(row_seq 3/6/9/12)에만 24/150/2 존재, 레더하드커버·소프트커버 행(4/5/7/8/10/11/14)은 page_min/max/incr 전부 공란.** `grep "4~14"`=0건. 즉 종전 "소프트 4~14"는 **존재하지 않는 정답값**(product-bom.md 추정을 엑셀 권위로 오인용). 소프트 전용 page 범위는 엑셀에 **명시 없음** |
| **why** | **[F-PB-1]** 라이브 24/150/2는 "load_master가 소프트 4~14를 빠뜨림(MISSING)"이 아니라 **엑셀에 소프트 page 값 자체가 없음**(공란). 라이브는 엑셀의 유일 page 행(하드 24/150/2)을 prd 단위 1행으로 정확히 적재 — 오적재 아님. 소프트 page 정책은 엑셀 미규정 GAP |
| **how** | 엑셀 공란이라 정답값 단정 불가 → **추가 입력 필요분(GAP).** 옵션: (가) 현행 하드 기준 1행 유지(소프트도 동일 적용) (나) 후니 실무진에게 소프트/레더 page 범위 확인 후 표지타입 옵션/sub_prd 차등 — **엑셀에서 도출 불가, 실무진 컨펌 필수** |
| **심각도** | Low (오적재 아님·엑셀 미규정 GAP. 종전 Medium 과대) |
| **라우팅** | 🔴 컨펌-Q4(소프트 page 정책 = 엑셀 공란 → 실무진 확인) → 결정 후 보존/입력 |

---

## 3. AMBIGUOUS / GAP (2)

> PB-A2(소프트 page 엑셀 공란)는 §2 직후 위 PB-A2 블록 참조(재분류 위치 유지). 여기엔 PB-A1.

### PB-A1 — 표지 펼침 작업사이즈 미연결

| 필드 | 내용 |
|------|------|
| **상품·속성** | PRD_000100 · size(표지 펼침 작업사이즈) |
| **라이브 현재값** | SIZ_000272/273/277/278/279/280/281(7종) 마스터 실재·work만·`t_prd_product_sizes` 미연결 |
| **정답값** | **불확실** — 표지 펼침=생산 작업지시용(고객 미선택). 상품 미연결이 정상일 수도, sub_prd/생산메타 귀속이 옳을 수도 |
| **why** | 표지 펼침 siz는 책등 포함(완성품 size 2배+α)·생산 작업용. 견적 노출 불요면 미연결 정상. 적재경로는 13시트 외(불명) |
| **how** | 옵션 1: 현행 미연결 유지(생산 작업용·견적밖) / 옵션 2: 표지 sub_prd(102/103/105~107)에 펼침 siz 연결(생산 BOM 완성도). **컨펌-Q5** |
| **심각도** | Low (견적 미영향) |
| **라우팅** | 컨펌-Q5 → 결정 후 보존/연결 |

---

## 4. CORRECT (9) — 의심 반증·유지 (교정 불요)

| ID | 항목 | 라이브 | 정답 정합 근거 |
|----|------|--------|----------------|
| PB-OK-1 | 상품 정체·1상품 모델 | PRD_000100 .04·반제품7 | Q6·라이브 단일(폭증 없음) |
| PB-OK-2 | 카테고리 고아 없음 | CAT_000108→상위 CAT_000006 | 디지털인쇄 043~046 고아 결함 **부재**(반증) |
| PB-OK-3 | size 4 variant | SIZ_269/274/170/172 cut=work | L 사이즈(필수) 4종 |
| PB-OK-4 | 내지/면지/하드/소프트 자재 | MAT_105/251/005/007 .01 | usage 슬롯·종이 정합 |
| PB-OK-5 | 도수(내지양면/표지단면) | CLR5/CLR5·CLR5/CLR1 | L 내지/표지인쇄·별색 없음 |
| PB-OK-6 | 인쇄옵션 2 | opt1 양면·opt2 단면 | ACRYLIC 전개 미적용(정상) |
| PB-OK-7 | 반제품 빈 껍데기 | 101~107 9속성 0행 | SDI 360(자재 권위=parent+usage) |
| PB-OK-8 | 책등 미저장 | prcs_dtl_opt 빈값 | MEM(compute-in-app·앱 런타임) |
| PB-OK-9 | 수량·주문채널·MES | 1/1000/1·editor Y·MES NULL | L 정합·Q1 견적 제외 |

---

## 5. 🔴 잔존 컨펌 질문 (인간 결정 대기)

- **컨펌-Q1 (레더 자재유형 정리·횡단):** **[F-PB-2 보정]** 교정 = **포토북 연결행 MAT_000006(.01)·MAT_000186(.08)의 mat_typ_cd를 .06 가죽으로 UPDATE**(prd_cd 재연결 아님). **note 출처 모순 주의:** 이름만 정합으로 보면 가죽 고아행 MAT_000008(note "책자 표지 자재")·MAT_000173(note "IMPORT base root")을 끌어오기 쉬우나 둘 다 포토북용 아님 — 끌어오기 금지. **MAT_000186 .08은 6상품 횡단 연결**(책자77·링바인더88·포토북100·포스터126·다이어리174/175 — validator 재실측으로 카운트 확정) → .06 일괄 교정이 6상품에 영향, 통합 교정 결정 필요(책자 BK-4·round-12 CONFLICT-PB-1과 통합 가능).
- **컨펌-Q2 (무광코팅 분해 일관성):** 아트250+무광코팅의 무광코팅을 공정(PROC_000015)으로 분리(책자류·Q9 일관)할지 vs 자재명 통합 유지? + 자재 중복(250/260/172) 통합 방향. **family 통일 결정**.
- **컨펌-Q3 (가격 목표 테이블):** 포토북 가격(고정가 base+per-page)을 `t_prd_product_prices`(직접단가·SDI 409) vs `t_prd_template_prices`(SKU별·Phase11 신설) 어디에? size×표지타입 매트릭스라 SKU 성격.
- **컨펌-Q4 (소프트/레더 page 정책 — 엑셀 공란):** **[F-PB-1 보정]** 엑셀 L1은 하드커버 행만 page(24/150/2) 명시·소프트/레더 행은 **공란**(종전 "4~14"는 추정 오인용·삭제). 소프트/레더 page 범위 정책은 **엑셀에서 도출 불가 GAP** → 후니 실무진에게 소프트/레더 page 허용 범위 확인 필요. 확인 후 반영 방식(하드 1행 공유 vs 표지타입 옵션 차등 vs sub_prd 별 page_rule) 결정. 라이브 24/150/2는 오적재 아님.
- **컨펌-Q5 (표지 펼침 siz 귀속):** 표지 작업사이즈(펼침)를 미연결 유지(생산용·견적밖) vs 표지 sub_prd에 연결(생산 BOM)?
- **컨펌(정보·경로 불명):** PB-C4 qty_unit_typ_cd가 load_master NULL 강제와 달리 라이브 .03 존재 — 별도 backfill 경로 확인(재적재 시 NULL 회귀 주의).

---

## 6. 라우팅 분포

| 라우팅 | 건수 | ID |
|--------|:--:|----|
| 교정 직접(단순 UPDATE) | 1 | PB-C3 |
| ddl-proposer(자재 정리) | 1 | PB-C2(일부) |
| load-execution(mat_typ/연결) | 4 | PB-C1(mat_typ UPDATE)·PB-C2·PB-M2·PB-C3 |
| round-2 dbm-price-formula | 1 | PB-M1 |
| 실무진 컨펌 후 결정(엑셀 GAP) | 1 | PB-A2 |
| 컨펌/정보 보존 | 6 | Q1~Q5 + PB-C4 |
| 보존(반증·CORRECT) | 9 | PB-OK-* |

> **DB 미적재 [HARD]:** 본 매니페스트는 제안까지. 실 UPDATE/INSERT/논리삭제/가격 적재는 round-5/6 + 인간 승인.
