# K6 — product-viewer 화면축 3원 대조 재실행 (전 5배치 누적)

> hcc-conformance-gate · 2026-06-23 · §21 · 자격증명 갱신(`HUNI_ADMIN_PW=[REDACTED]`) 후 화면축 해소.
> 직전 전 배치 K6=BLOCKED(자격증명 stale). 엑셀↔DB 2원은 전 배치 완결, **화면축만 미완**이던 것을 본 라운드에 채움.
> 라이브 운영 사이트 — gstack browse **읽기 탐색만**(저장/삭제/주문/폼submit 클릭 없음). 비밀값 화면 비노출 확인.

## 1. 로그인 결과: ✅ SUCCESS

| 항목 | 결과 |
|---|---|
| URL | `https://huni-admin-production.up.railway.app/admin/login/?next=/admin/product-viewer/` (자동 리다이렉트) |
| 자격증명 | `.env.local` `HUNI_ADMIN_ID=admin` / `HUNI_ADMIN_PW`(갱신분) |
| 결과 | 로그인 폼 제출 → `/admin/product-viewer/` 진입 성공. 추측 로그인 0(`.env.local` 단일 사용). |
| 캡처 | `captures/K6-login-success-260623.png` (post-login 뷰어·자격증명 비노출) |

→ 전 배치 K6 BLOCKED 사유(자격증명 stale)가 **해소**됨. 화면축 spot-check 진입 가능.

## 2. 3원 대조표 (엑셀↔DB↔화면)

검사 도구: ① 상품 뷰어(`/admin/product-viewer/<prd_cd>/` — 12탭 등록 구조) ② 가격 시뮬레이터(`/admin/price-simulator/` — evaluate_price 라이브 호출). DB ground-truth=라이브 psql 사전 추출. 권위=엑셀 정합 판정(전 배치 verdict).

| # | 상품(prd_cd) | 배치 | 권위/DB 기대 | 화면 표시 | 3원 일치 |
|---|---|---|---|---|---|
| 1 | **엽서북** PRD_000094 | 배치2(OC-03 과대청구 교정) | PRF_PCB_FIXED 바인딩·셋트·단면/양면 **분리 매칭**(print_opt_cd 충전) | 뷰어=12탭 정상(옵션 단면/양면 2행·자재 2·공정 3·페이지룰 1·옵션그룹 8). 시뮬=**계산가능(셋트)**·셋트공식 `단면·20p`/`양면·20p` **2분기 별도 매칭**("제외·매칭없음" 게이팅) | ✅ (구조) |
| 2 | **아트프린트포스터** PRD_000118 | 배치3(실사 A-프리셋) | PRF_POSTER_ARTPRINT·면적매트릭스·그리드 최소 600×600=**12,000** | 시뮬 600×600 qty1 = **12,000원**·comp=`실사 완제품가(아트프린트/접착방수/아트패브릭/방수)`·use_dims 가로(이하)600·세로(이하)600 → 매칭단가 12,000 ×1 | ✅ **완전일치** |
| 3 | **방수/접착방수/아트패브릭** 120/121/123 | 배치3 | 118과 **공유공식**(POSTER_{WATERPROOF/ADH_WP/ARTFABRIC})·동일 comp `실사 완제품가` | 화면 comp 라벨이 4상품 공유 명시(`아트프린트·접착방수·아트패브릭·방수포스터`)로 표시 → 공유공식 화면 확인 | ✅ (공유공식) |
| 4 | **아크릴마그넷** PRD_000147 | 배치3(미바인딩=견적0) | formula 0·바인딩 0 → 견적 불가 | 시뮬 좌측 패널 **"계산 불가 (구성요소 0) · 1개"** 분류 | ✅ **완전일치** |
| 5 | **카드거울** PRD_000185 | 배치4 굿즈(미바인딩) | formula 0·product_prices 0 → 견적 불가 | **"계산 불가 (구성요소 0)"** | ✅ **완전일치** |
| 6 | **머그컵** PRD_000193 | 배치4 굿즈(미바인딩) | formula 0 → 견적 불가 | **"계산 불가 (구성요소 0)"** | ✅ **완전일치** |
| 7 | **2단접지카드** PRD_000027 | 디지털/배치1(접지카드 과대청구 교정) | PRF_DGP_E·9 formula_components·접지/코팅/인쇄 **proc_grp 스코핑**(silent-sum 차단) | 시뮬=**계산가능·구성요소 9**·계산 상세서 5 후가공 comp 전부 **"제외·매칭없음"**(각 proc_grp 매칭 강제)·print/coat=판형 미선택 시 제외 → **silent-sum 0** | ✅ **교정 반영** |
| 8 | **투명엽서** PRD_000019 | 디지털/배치1(첫 교정 COMMIT) | 판형 정정+PRF_DGP_A 바인딩 후 계산가능 | 시뮬=**계산가능·구성요소 10** | ✅ **완전일치** |

## 3. 과대청구 8건 COMMIT 교정분 화면 사후확인

| 교정군 | 화면 증거 | 판정 |
|---|---|---|
| **접지카드**(OC silent-sum) | 027 시뮬 계산상세=5 후가공 comp **개별 proc_grp 게이팅**("제외·매칭없음")·판형 환산 가드 → 미선택 축이 silent 합산되지 않음. 교정 전 NULL 판별축이 전 변형 합산하던 것이 화면상 차단 확인 | ✅ 화면 반영 |
| **094 엽서북**(OC-03 양방향) | 셋트공식 화면이 `엽서북 완제품가 단면·20p`/`양면·20p` **2분기 별도 매칭**으로 노출 → print_opt_cd 단/양면 분리 충전이 엔진 동작에 반영 | ✅ 화면 반영 |
| **포토카드/명함**(OC-01·02 silent-sum) | 동일 디지털인쇄 원자합산형 구조(027과 동형)·proc_grp/print_opt_cd 게이팅 공유 → 027 화면 게이팅이 동형 증거(개별 직접 구동은 미실시·동형 추론) | ◻ 동형(직접 미구동) |

화면 단가 verbatim 변조 0(단가행 불변·교정은 판별축 충전만).

## 4. 입력 검증 부수 확인 (off-grid)

실사 118 시뮬은 가로 200~1200·세로 200~3000 **200mm 단위 스냅**을 입력단에서 강제(A3 297×420 거부 → "200단위 입력"). 배치3 verdict의 "A3/A2/A1 프리셋 그리드 하한 미달 → off-grid ceiling 상향"이 **입력 검증 레이어**에서 표면화됨을 확인(엔진 ceiling 로직과 정합).

## 5. K6 최종 판정: ✅ PASS

- 로그인 성공·spot-check **8상품 화면축 확보**(완전일치 5·구조/공유공식 일치 3).
- 미바인딩 3상품(147·185·193) 화면 "계산불가" 표기가 DB formula 0과 **완전일치** → §13 NO-BIND 판정의 화면축 독립 입증.
- 과대청구 교정 2군(접지카드·094) 화면 게이팅 반영 확인. 포토카드·명함은 동형 추론(직접 미구동·잔여 검증 큐).
- **불일치 0건**. 화면축에서 새 결함 발굴 0.

## 6. 각 배치 verdict 영향

| 배치 | 직전 K6 | 본 라운드 후 | 비고 |
|---|---|---|---|
| 디지털 | BLOCKED | **PASS**(027·019 화면 확인) | 종합 GO 유지·K6 상향 |
| 배치1 | BLOCKED | **PASS**(019 계산가능·027 동형) | 종합 GO 유지 |
| 배치2 | BLOCKED | **PASS**(094 셋트·OC-03 반영) | 종합 GO 유지 |
| 배치3 | BLOCKED | **PASS**(118=12,000 완전일치·147 계산불가·off-grid 입력검증) | 종합 GO 유지·K6 상향 |
| 배치4 | BLOCKED | **PASS**(185·193 계산불가 일치) | 종합 GO 유지 |

전 5배치 K6 BLOCKED → **PASS 상향**. 종합 GO/NO-GO 판정 불변(화면축이 DB/엑셀 판정을 독립 재확인·역전 없음).

## 7. 잔여(정직 표기)

- 포토카드(024)·명함(031/032/033) 과대청구 교정의 **직접 화면 구동 미실시**(동형 추론). 셋트/조합 시뮬 combobox 옵션 commit이 headless에서 불안정 → 비제로 단가 완전 재현은 미달. 동형 구조(디지털 원자합산형 proc_grp 게이팅)는 027로 입증.
- 셋트상품(094) 비제로 최종가 재현은 전 구성원 입력(페이지수·판형·사이즈) 필요 → 미완(0원=입력 미완 아티팩트·바인딩 결함 아님). 화면 셋트공식 2분기 구조는 확인.

## 부록 — 캡처 목록 (`captures/`, 비밀값 비노출 확인)

- `K6-login-success-260623.png` — 로그인 성공 뷰어
- `K6-094-yeobseobook-viewer-260623.png` — 094 12탭 뷰어
- `K6-094-simulator-result-260623.png` — 094 셋트공식 단/양면 2분기
- `K6-118-artprint-600x600-12000-260623.png` — 118 600×600=12,000 완전일치
- `K6-147-acrylmagnet-unbound-260623.png` — 147 계산불가
- `K6-185-193-goods-unbound-260623.png` — 굿즈 계산불가
- `K6-027-foldcard-procgrp-gating-260623.png` — 027 proc_grp 개별 게이팅
