# 사이즈 축(t_siz_sizes) 4축 검수·정리 리포트 — 파일럿

생성: 2026-06-19 / 검수가: hbd-dedup-analyst / 방법론: hbd-dedup-analysis
입력: Phase 1 캐시(index.csv 520 · authority.csv 1039 · live.csv 520) — 엑셀 원본 미접근
정규화 규칙 재사용: `_workspace/huni-dbmap/04_audit/size-parity.md`

## 판정 요약 — **PASS (NO-OP)**

**4축 검수 결과 통합/신규적재/오적재교정 대상 0건.** 모든 표시·내부값 충돌은
④ 의미구분 보존 가드를 통과(형상·입수·반칼·판형/임포지션·작업전용·가격종속).
clean merge group = **0** (양 멤버가 같은 역할·형상·비가격종속인 그룹 없음).

| 축 | 발견(원시) | 정당 구분(④ 가드) | 실 정리대상 |
|---|:---:|:---:|:---:|
| ① 표시↔내부 정합(오적재) | 7 후보 | 7 (inch 6 + 판형 1) | **0** |
| ② 표시 중복 | 29 그룹 | 29 | **0** |
| ③ 내부값 중복 | 29 그룹 | 29 | **0** |
| ④ 의미구분 보존(가드) | — | 적용 | — |
| 신규 적재(new-load) | 0 | — | **0** |

BLOCKED(가격종속·정리 보류) = 84행 · 논리삭제(현황만) = 65행.

## canonical 정의

`canonical = (semantic_axis, norm_w, norm_h, unit)`
- 치수 추출 우선순위: cut(재단=최종납품) → work(작업) → 라벨 dim. 단위 mm 통일(cm 라벨 ×10 환산).
- semantic_axis: `작업+재단 보유`(491) / `치수미상/라벨형`(29, 실제 대부분 cut-only) — **단, 충돌 판정 시 라이브 메타(impos_yn·note·plate 바인딩)로 판형 sub-axis를 추가 분리**(index의 2분류만으로는 판형≠본체사이즈를 못 가름).

## ② 표시 중복 / ③ 내부값 중복 — 29 그룹 전부 정당 구분

같은 치수(예: 30×30이 4 siz_cd)가 여러 코드인 이유는 전부 정당한 sub-axis:

| 구분 sub-axis | 예시 | 근거 |
|---|---|---|
| **형상(shape)** | `원형30x30`/`사각30x30`/`정사각30x30mm(2EA)` vs `30x30` | round-23 [HARD] 형상=칼틀(도무송), 같은치수 가공별 가격 분리. SIZ_000505/000423/000216 vs 000330 |
| **입수/팩(pack)** | `100x100mm(50장)`/`(20장)`/`(40장)`/`(100장)` vs `100x100` | 입수=주문/가격 단위, siz_nm에 baked. SIZ_000083/099/100/101 vs 000113 |
| **반칼/완칼** | `A4(210x297mm) 반칼`(SIZ_000520) vs `A4 (210X297)`(SIZ_000050) | round-23 의도적 분리(반칼전용 채번) |
| **판형/임포지션** | `A5 (148X210)`(impos·판걸이=4·전지=316x467·plate=9) vs `A5`(본체·pd=Y) | 판형 역할 행은 출력판형/임포지션 메타 보유 — 통합 시 메타 파손 |
| **작업전용(plate/work-only)** | `230x330`(cut공백·plate=1) vs `A4용(230x330mm)`(cut보유) | plate 바인딩 작업사이즈 역할 |

★ 30개 그룹에서 "plain(무플래그) 멤버 ≤1 + 나머지 전부 가드플래그" → 통합 짝이 성립하지 않음.

## ① 표시↔내부 정합(오적재) — 0건

- **inch×mm 이중라벨 6건**(`8x8(203x203mm)`·`5x5(127x127mm)` 등): 라벨=inch, 내부=mm 정확. **정합(오적재 아님)** — 사진사이즈 관용 이중표기.
- **SIZ_000499 `316x467`**: 라벨=전지 출력판형명, 내부 306x457=인쇄가능 작업영역(impos_yn=Y·plate=32). **의도적 판형 모델링**(오적재 아님). 단 비-inch 유일 라벨↔내부 차이라 컨펌 큐에 표기(가격종속 Y라 어차피 BLOCKED).

## BLOCKED 목록 — price_dependent=Y 84행

component_prices가 siz_cd를 참조(면적매트릭스 922행 포함). 라이브 직접 통합/삭제 시
가격사슬 CASCADE 위험 → **적재 매핑 비대상·escalate**. 안전 교정 경로는 경로 Y(개발자 재적재)뿐.
(del_yn=Y와 1행 중복.)

## 논리삭제 65행 (del_yn='Y')

이미 논리삭제 — 현황 파악만, 재처리 대상 아님. 권위=[[dbmap-del-yn-soft-delete-authority]].

## 컨펌 큐

1. **SIZ_000499 `316x467`**(라벨=전지명·내부=작업영역 306x457): 판형 의도 모델링이 맞는지 확인(BLOCKED라 실행영향 없음·문서 정합용).
2. (참고) shape/pack/반칼 분리는 round-23 [HARD]로 이미 확정 — 재확인 불요.

## 결론(1차)

사이즈 축은 표시중복·내부값중복이 다수 보이지만 **전부 형상·입수·반칼·판형·가격종속의
정당한 의미구분**으로, 4축 검수상 무손실 정리(통합)·신규적재 대상이 0건이다. → **PASS (NO-OP)**.
억지 통합은 round-23에서 의도적으로 분리한 가공/형상/가격 차원을 파손하므로 금지.

---

# 재판정 (Phase 3 — codex divergence 해소 · 라이브 확정)

생성: 2026-06-19 / hbd-dedup-analyst / 입력: reconcile.md(codex gpt-5.5 적대검토) + 라이브 t_* 직접 재실측

## 1차 오류 자인 [HARD]

**1차 PASS(NO-OP)는 틀렸다.** 1차 리포트가 "29 내부값 충돌그룹 전부 정당"으로
**멤버별 가드 플래그 실재성을 1:1 대조하지 않고 일괄 흡수**해, 가드가 0인 진짜 중복
(SIZ_000104/105)을 가렸다. codex 2차 교차검증이 이 false-negative를 적발했고
(환각 0건·인용 8 siz_cd 전부 라이브 실재), 본 재판정에서 **라이브 직접 재실측으로 확정**했다.
교훈: 충돌그룹 keep 판정은 멤버별 가드(형상/입수/반칼/판형/단위) 실재를 1:1 대조해야 한다.

## D-1 [HIGH] SIZ_000104 / SIZ_000105 — 진짜 중복 **확정 (라이브 merge GO)**

라이브 재실측(t_siz_sizes·t_prd_product_sizes·t_prc_component_prices·option_items·plate·constraints):

| 검사 | SIZ_000104 | SIZ_000105 | 판정 |
|---|---|---|---|
| siz_nm | `165x115mm(10장)` | `165x115mm(10장)` | 동일 |
| work/cut w×h | 165×115 / 165×115 | 165×115 / 165×115 | 동일 |
| impos_yn·note·del_yn | N·null·N | N·null·N | 동일 |
| reg_dt | 2026-06-03 12:46:02 | 2026-06-03 12:46:02 | 동일(동시 생성) |
| price_dependent (cp_rows) | **N (0)** | **N (0)** | CASCADE 무관 |
| 구분축(형상/입수/반칼/판형/단위) | 없음 | 없음 | 가드 0 |
| 상품바인딩 | PRD_000004 카드봉투 (dflt_yn=Y·seq=1) | PRD_000004 카드봉투 (dflt_yn=Y·seq=1) | **동일 상품·이중 default** |
| option_items·plate·template·constraint 참조 | 0 | 0 | 안전 |

**★결정적 근거: PRD_000004(카드봉투)의 전 사이즈 = SIZ_000104, SIZ_000105 둘뿐인데
둘 다 `165x115mm(10장)`·둘 다 dflt_yn='Y'·disp_seq=1.** 즉 같은 사이즈가 사이즈 선택기에
2번 노출되고 둘 다 기본값으로 찍힌 **순수 표시·내부값 중복(데이터 결함)**. 색(화이트/블랙)
인코딩 아님(siz_nm은 치수). pd=N·외부 참조 0 → **CASCADE 무관·라이브 안전 merge 가능**.

- **정본 = SIZ_000104** (낮은 번호=먼저 등록 우선·reg_dt 동일이라 번호가 tiebreaker).
- **멤버 = SIZ_000105** → 논리삭제(del_yn='Y') + PRD_000004→SIZ_000105 바인딩 제거
  (SIZ_000104가 이미 PRD_000004에 바인딩돼 있어 무손실·이중 default도 해소).

## D-2 [LOW] A3 SIZ_000174 / SIZ_000315 — **BLOCKED 확정 (경로 Y)**

라이브: 174 `A3(297x420mm)`(prd 22·cp 12) · 315 `A3`(prd 1=PRD_000134·cp 18). 동일 내부값
297×420·plate0·판형 가드 부재. **둘 다 pd=Y(component_prices 참조 174→12·315→18행)** →
라이브 직접 merge 시 가격사슬 CASCADE 위험 → **BLOCKED**. 근본 교정=경로 Y(개발자 v03 재적재). 컨펌 큐.

## D-3 [LOW] A2 SIZ_000197 / SIZ_000317 — **BLOCKED 확정 (경로 Y)**

D-2 동형. 197 `A2(420x594mm)`(prd 19·cp 12) · 317 `A2`(prd 1=PRD_000134·cp 12). 동일 420×594·
plate0·가드 부재. **둘 다 pd=Y** → 라이브 merge BLOCKED·경로 Y. 컨펌 큐.

## 재판정 종합

| 항목 | 1차 | 재판정 | 실행 |
|---|---|---|---|
| 판정 | PASS(NO-OP) | **NO-OP 불승인** | — |
| 라이브 안전 merge | 0 | **1 (D-1)** | apply-plan.md |
| BLOCKED(pd=Y·경로 Y) | 84 | 84(+D-2/D-3 명시) | 컨펌 큐 |
| 합의 고신뢰 항목 | — | inch 이중라벨·SIZ_000499·del_yn·형상/입수/반칼 | 재검토 불요 |

## 컨펌 큐 (인간 승인 대상)

1. **D-1 SIZ_000105 merge → SIZ_000104** (apply-plan.md 명세·라이브 안전·delta 예측 제시) — 승인 후 hbd-load-execution 위임.
2. **D-2/D-3 A3·A2 plain twins** (pd=Y BLOCKED) — 경로 Y(개발자 v03 재적재)로만 교정. 실행영향 0·문서 정합.
3. **SIZ_000499** 판형 의도 모델링(BLOCKED) — 1차 그대로 확인 큐.
4. (잔존) 나머지 28 충돌그룹 멤버별 가드 1:1 재검 — D-1 교훈 반영 다음 라운드 권고.
