# 디자인캘린더 — 정규화·오염 방지 가드 (normalization-guard)

> **작성** 2026-06-14 · round-13. 사용자 기준 6번 = "정정으로 들어가는 모든 값이 임의 판단 아닌 화이트리스트 안에 있음을 입증".
> **3중 화이트리스트:** ① table-spec 도메인값(`docs/huni/table-spec_260610.html`) ② webadmin sql/ FK·제약 ③ 260610 L1 엑셀 명시값.
> **목표:** 화이트리스트 밖 값 0 보장. apply.sql이 적재/변경하는 모든 코드값을 3축으로 검증.

---

## 1. 즉시적용분 적재값 화이트리스트 입증 (apply.sql 실행분)

| 적재 컬럼 | 적재값 | ① table-spec 도메인 | ② webadmin FK/제약 | ③ 260610 L1 엑셀 | 화이트리스트 안? |
|-----------|--------|---------------------|--------------------|--------------------|:--:|
| `t_prd_products.editor_yn` (108/109/111/112) | 'Y' | YN 플래그(Y/N) | char(1)·YN 도메인 | 디자인보유 ●(AC열 FF31859B)·디자인=에디터 surface | ✅ |
| `editor_yn` (110 엽서) | (변경 안 함) | — | — | 엑셀 디자인보유 ● 없음 → 제외 | ✅ (보수적 제외) |
| `t_prd_products."MES_ITEM_CD"` (108) | '007-0001' | MES 품목코드(자유) | varchar·UNIQUE(중복 시 제약위반) | C3 MES ITEM_CD=007-0001 | ✅ |
| `"MES_ITEM_CD"` (109~112) | '007-0002'~'007-0005' | 〃 | 〃·1:1(중복 없음 입증) | C3 007-0002~5 | ✅ |
| `upd_dt` | now() | 타임스탬프 | nullable timestamp | — (변경이력) | ✅ |
| `prd_cd` | PRD_000108~112 | 상품코드 | FK→`t_prd_products` 실재 | L1 prd_nm 매칭 | ✅ |

> **화이트리스트 밖 값 = 0.** 즉시적용분이 쓰는 값(editor_yn='Y'·MES='007-0001~5')은 전부 엑셀 명시값 + 라이브 도메인 안. **신규 코드/FK mint 0.** MES UNIQUE 충돌 없음 입증(5상품 1:1·아래 SELECT).

**라이브 입증 SELECT (재현):**
```sql
-- editor_yn 현재 N·디자인보유 4상품(엽서 제외) 정확 매핑
SELECT prd_cd, prd_nm, editor_yn FROM t_prd_products
 WHERE prd_cd IN ('PRD_000108','PRD_000109','PRD_000110','PRD_000111','PRD_000112');
-- → 전부 editor_yn=N. UPDATE 대상은 디자인보유 ● 4상품(110 제외)
-- MES UNIQUE 충돌 점검(007-0001~5가 이미 다른 상품에 쓰이는지)
SELECT "MES_ITEM_CD", count(*) FROM t_prd_products
 WHERE "MES_ITEM_CD" IN ('007-0001','007-0002','007-0003','007-0004','007-0005') GROUP BY "MES_ITEM_CD";
-- → 0행(충돌 없음 → UNIQUE 안전)
```

---

## 2. 신규적재/컨펌대기분 적재 후보값 화이트리스트 (실행 전 사전 검증)

| 적재 컬럼 | 후보값 | ① table-spec | ② webadmin FK | ③ 260610 L1 | 안? | 비고 |
|-----------|--------|--------------|---------------|--------------|:--:|------|
| `t_prd_product_prices.unit_price` (D-03) | 4000~24000 | 가격(numeric) | nullable numeric | 엑셀 가격 4000/6500/9700/9900/10400/24000 | 🟡 | **그릇 단일가 vs 다가 미스매치**(PK=prd_cd+apply_ymd) → Q-DC-A |
| `t_prd_product_materials.mat_cd` (D-04 우드거치대) | MAT_000034 | 자재 코드 | FK→`t_mat_materials` 실재(MAT_TYPE.07) | 추가상품=우드거치대 4000 | ✅ | 기존 자재 재사용(mint 0·search-before-mint) |
| `usage_cd` (D-04) | USAGE.07 | 용도 코드 | FK·캘린더 종이 .07 공통 | — | ✅ | 라이브 캘린더 자재 패턴(.07) 일관 |
| `t_prd_product_processes.proc_cd` (D-05 삼각대거치) | (신규 PROC) | 공정 코드 | **FK 대상 부재**(거치/삼각대/세움 0건) | 캘린더가공=삼각대(그레이/블랙) | ❌ | **삼각대거치 공정 마스터 mint 선행**(Q-DC-B·ddl-proposer) |
| `del_yn` 논리삭제 (D-05/07/08 자재) | 'Y' | YN | char·del_dt 동반 | — | ✅ | hard-delete 아님·provenance 보존 |
| `t_prd_product_categories.cat_cd` (D-09/10) | CAT_000113·CAT_000116 | 카테고리 코드 | FK→`t_cat_categories` 실재(미연결 빈 노드) | 엑셀 구분=탁상형/벽걸이(전용노드와 충돌) | 🟡 | 전용노드 실재하나 엑셀 구분과 충돌 → Q-DC-D |
| 사이즈/페이지별 다가 (D-03) | — | — | **적재처 칸 부재**(단일가 그릇) | 가격 다수 | ❌ | **Q-DC-A 그릇 결정 선행** |

> **신규적재/컨펌대기 화이트리스트 밖(❌) 2건:** 삼각대거치 공정 마스터(부재→mint 결정)·사이즈/페이지별 다가 적재처(단일가 그릇 한계). 이 둘은 임의 mint/억지 삽입 금지 — apply.sql에서 주석 처리(실행 안 함). **🟡 2건**(고정가 단일가·전용노드 카테고리)은 모델 결정 후.

---

## 3. 오염 방지 — 무엇을 하지 않았나 (round-13 교훈 반영)

| 오염 패턴 | 본 정정이 회피한 방식 |
|-----------|----------------------|
| 색/형상/사이즈를 자재로 오적재 (MAT_TYPE.08~10 오염) | 삼각대 거치·링칼라는 자재 아닌 **공정**(거치/제본)으로 분리(D-05/D-08) — 자재 테이블 정화 방향 |
| 별색을 도수로 오적재 | 캘린더는 별색 없음(OM-5 무관)·도수=print_options 유지 |
| 신규 자재 코드 남발 | 우드거치대=기존 MAT_000034 **재사용**(search-before-mint), 신규 mint 0 |
| 신규 카테고리 코드 남발 | 미니/와이드 전용노드=기존 CAT_000113/116 **재사용**(mint 0), 단 엑셀 구분과 충돌이라 컨펌 |
| 자재행 hard-delete | 삼각대/링 잉여 자재는 **논리삭제(del_yn='Y'+del_dt)** — DELETE 안 함 |
| 적재처 없는 값 억지 삽입 | 삼각대거치 공정·사이즈별 다가는 적재처 부재로 **주석 보류**(컨펌) — NULL 강제/엉뚱한 칸 삽입 안 함 |
| 별 상품 폭증 | 디자인캘린더=같은 5상품 surface(별 prd_cd mint 0)·CAT_000118 빈 노드 강제 연결 안 함 |
| MES 임의값 | 엑셀 명시 007-0001~5만·UNIQUE 충돌 입증 후(중복 없음) |

---

## 4. 가드 결론

- **즉시적용분(editor_yn 4상품·MES 5상품): 화이트리스트 밖 값 0.** 전 값이 엑셀 명시(260610 L1) + 라이브 도메인 안. MES UNIQUE 충돌 0 입증. 멱등 DRY-RUN 통과(제약 위반 0·2회차 UPDATE 0).
- **신규적재/컨펌대기분: 화이트리스트 밖 2건(삼각대거치 공정·다가 적재처)을 정직하게 보류** — 억지 삽입 0.
- 임의 판단 0: 모든 적재값이 ① 엑셀 명시(260610 L1) ② 라이브 FK 대상 실재 ③ table-spec 도메인 중 셋을 동시 충족하거나, 못 충족 시 보류.
