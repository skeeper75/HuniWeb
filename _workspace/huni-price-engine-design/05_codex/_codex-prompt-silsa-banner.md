너는 후니프린팅 **실사·현수막(면적매트릭스형) 가격엔진 설계**의 **독립 외부 검토자**다. 아래 설계 산출물을 읽고, 다른 누구의 판정도 참고하지 말고 **너 자신의 독립 판정**을 내려라. 근거와 함께, 모르면 "모른다"고 명시하라. 추측은 추측이라고 표시.

## 배경: 후니 가격엔진 구조 (사실)
- 후니 라이브 가격엔진 `evaluate_price`는 4단 구조: 공식(price_formulas) → 공식-구성요소 배선(formula_components) → 구성요소(price_components, prc_typ=단가형/합가형) → 단가행(component_prices, 차원 매트릭스).
- 엔진 동작(엔진 계약):
  - 공식 가격 = Σ(배선된 구성요소 subtotal). frm_typ는 참조 안 함(분기 없음·항상 합산).
  - **단가형(PRICE_TYPE.01)**: subtotal = unit_price × qty.
  - **합가형(PRICE_TYPE.02)**: subtotal = (unit_price ÷ min_qty) × qty. min_qty=NULL(또는 ≤0)이면 ÷ 오류(ValueError).
  - 단가행 매칭: 선택값(selections)이 그 단가행의 차원(use_dims/NON_QTY_DIMS·예 siz_width·siz_height·siz_cd·mat_cd·proc_cd·opt_cd·min_qty)과 전부 일치해야 매칭. 어떤 차원 컬럼이 단가행에서 NULL이면 그 차원은 "무엇이든 매칭"으로 통과(와일드카드).
  - 한 공식에 여러 구성요소가 배선되면 매칭된 것들이 전부 합산됨. addtn_yn=Y는 가산 비목. 엔진은 addtn_yn을 읽지 않고, 매칭된 모든 구성요소를 합산한다(매칭 0건이면 자연 제외).
  - 면적 차원(siz_width·siz_height)은 '이하' 상한 ceiling 매칭(off-grid 주문값 → 한 단계 큰 격자). 주문값 > 최대 임계면 ERR_ABOVE_MAX.
  - 수량 차원(min_qty)은 '이상' 하한 매칭(주문수량 ≥ min_qty인 행 중 '최대' min_qty 선택). 면적 차원의 '이하' ceiling과 방향이 반대다. 주문수량 < 최소 min_qty면 ERR_BELOW_MIN.
  - 비수량 차원(siz_cd·mat_cd·proc_cd·opt_cd)은 정확매칭.

## 검토 대상 설계 산출물 (이것만이 증거 — 워크디렉토리=프로젝트 루트)
아래 5개 파일을 직접 읽어라:
- `_workspace/huni-price-engine-design/03_design/engine-design-silsa-banner.md` — 실사·현수막 3계산방식(면적매트릭스·고정가·수량구간) 완제품 가격공식+구성요소+바인딩+G-S1 후가공 배선 설계.
- `_workspace/huni-price-engine-design/03_design/golden-cases-silsa-banner.md` — 대표 케이스 + 기대 골든값(단가=가격표 verbatim).
- `_workspace/huni-price-engine-design/01_formula/formula-map-silsa-banner.md` — 상품→구성요소→계산방식 지도.
- `_workspace/huni-price-engine-design/02_benchmark/absorption-candidates-silsa-banner.md` — 경쟁사(RedPrinting BN/WowPress) 흡수 후보 C-SB1~C-SB7.
- `_workspace/huni-price-engine-design/03_design/design-decisions.md` — 설계 결정·흡수·컨펌큐(파일 안의 "실사·현수막 절" DS-1~DS-6·Q-SB-*만 보라).

## 너의 독립 판정 질문 (각각 근거 + 자기 판정 GO/CONDITIONAL/FAIL)

### Q1. 3계산방식 분류 건전성 — 면적/고정가/수량구간 3분류가 옳은가?
- 설계는 실사·현수막 28상품을 ① 면적매트릭스형(use_dims=[siz_width,siz_height]) ② 고정가형(use_dims=[siz_cd] 또는 [siz_cd,min_qty]) ③ 수량구간형(use_dims=[siz_cd,min_qty]·미니류) 셋으로 나눈다.
- 이 3분류가 별 엔진 분기가 아니라 본체 comp 1개의 use_dims 차이일 뿐이라는 설계 논리가 건전한가? 잘못 분류된 상품이나 빠진 계산방식이 있나?
- 각 계산방식에서 본체 comp가 단가행 1행을 정확히 매칭하는가, 아니면 차원 미스매치(use_dims와 단가행 차원 불일치)로 매칭 실패/오매칭/다중매칭(ERR_AMBIGUOUS)이 생기나?

### Q2. ★돈크리티컬 — 면적단가가 1장당가인가 묶음총액인가·×qty 폭발 분석 (직접 계산해보라)
- 본체 면적 comp(예 COMP_POSTER_CANVAS_FABRIC)는 **prc_typ=단가형(.01)**이고 단가행 min_qty가 NULL 또는 1이라고 설계가 주장한다.
- 단가형 엔진식은 `subtotal = unit × qty`다(÷min_qty 미발생).
- golden GC-S1(캔버스 600×1800, unit=37,800)을 손님이 **qty=10** 주문하면 결과가 얼마인가? **직접 곱해보라.** 기대 골든(378,000)과 일치하는가, 아니면 ×qty 만큼 과대청구되는가?
- ★핵심 비교: 디지털인쇄 파일럿에선 단가가 "100매 1세트 총액"(예 명함 3500=100매 총액)인데 prc_typ가 잘못 설정돼 qty=100을 곱하면 350,000으로 ×100 폭발했다. **실사·현수막 면적단가(37,800)는 "1장당 완제품가"인가, 아니면 "묶음/구간 총액"인가?** 가격표 매트릭스 셀(=[가로×세로] 1셀)의 의미를 설계 근거로 판단하라. 단가 의미가 1장당가라면 ×qty가 정답이고, 묶음총액이라면 폭발이다.
- 면적 본체가 전건 .01 단가형이라면, 디지털(묶음총액 단가가 .01로 잘못 적재됨)과 같은 ×qty 결함이 실사·현수막에 있는가 없는가?

### Q3. ★수량구간형 min_qty 밴드 — 방향과 ×qty 정합 (직접 계산해보라)
- 미니배너(COMP_POSTER_MINI_BANNER) SIZ_000028 단가행: min_qty 4=6,500 / 19=4,900 / 49=4,200 / 99=3,500 / 10000=2,800 (수량 ↑ 개당가 ↓).
- 손님이 **30개** 주문하면 어느 min_qty 행이 선택되는가? (min_qty는 '이상' 하한·주문수량 이상인 행 중 '최대' min_qty) → 직접 판정하고 subtotal을 계산하라. 기대 골든(147,000)과 맞는가?
- 손님이 **100개** 주문하면 어느 행인가? 골든은 350,000(min_qty=99·개당 3,500)이라 주장한다. 100<10000이라 10000 행이 아니라 99 행이 맞는가? 직접 판정하라.
- 이 수량밴드 단가가 "수량구간별 개당가"인지 "구간 총액"인지 판단하라. 개당가라면 ×qty가 정답·폭발 없음. 또 이 본체 내장 수량할인이 t_dsc 별도 구간할인과 이중 적용되면 어떤 문제가 생기는지 설계가 잡았는가?

### Q4. ★G-S1 후가공 배선 + silent 이중합산 위험 (구조 분석·핵심)
- 설계는 28 PRF_POSTER 공식이 전부 본체 comp 1개뿐(후가공 미배선)이고, 후가공 comp(타공/큐방/끈/봉미싱/거치/오시/미싱/별색 등)는 라이브에 실재하나 공식에 배선되지 않아 **현 견적 시 후가공 선택해도 가격 반영 0**이라 진단한다.
- ★배너 전용 후가공(PUNCH_4/6/8·QBANG_4·STRING_4·BONGSEW·CUTEDGE·DTAPE·STAND)은 **use_dims=[](빈 배열)·전 차원 컬럼 NULL = 판별차원 전무**라고 설계가 진단한다.
- 질문: 이런 판별차원 없는 후가공 comp를 한 공식에 그냥 배선하면 어떻게 되는가? 예를 들어 PUNCH_4(3,000)·PUNCH_6(4,000)·PUNCH_8(5,000) 셋을 현수막 공식에 배선하면, 손님이 "타공 6개" 하나만 골라도 셋 다 매칭되어 합산되는가? **직접 추론하라.** 본체 8,000짜리 현수막에 타공 셋 다 배선하면 최종이 얼마가 되는지 계산하라. 이것이 silent 이중합산(과청구) 위험인가?
- 설계는 배선 전 "판별차원 충전(택1 타공은 1 comp+opt_cd로 통합·택N은 각 opt_cd 충전)을 절대 선결"로 요구한다. 이 가드가 타당한가, 아니면 과잉/누락인가? 공통 후가공(오시·미싱·별색·우드행거·린넨마감 등 proc_cd/opt_cd/siz_cd 판별차원 보유)은 배선 안전한가?
- ★후가공/거치 가산이 "1주문건당 1회"인지 "×수량"인지(예 현수막 10장 주문 시 거치대 25,000을 ×10 할 것인가)를 설계가 컨펌큐로 정직하게 남겼는가, 아니면 추측으로 적재했는가? 이 돈크리티컬 미확정 처리가 정직한가?

### Q5. 경쟁사 흡수 overfit/답습 여부 (신규 가격축 0건)
- absorption-candidates-silsa-banner의 C-SB1(면적=2축 구간 매트릭스+ceiling·면적함수 부결)·C-SB2(자유사이즈→nonspec_incr 스냅+범위제한)·C-SB3(소재=별 가격축 아님·소재별 공식이 동형 단가표 공유·byte-identical 결합)·C-SB4(거치대=별 고정가 부속·면적차원 오모델 금지)·C-SB5(소재→강제 후가공/포장=CPQ 제약)·C-SB6(인쇄방식=자재행 분기·그룹핑 슬롯 신설 부결)·C-SB7(부자재 수량=dim_vals.개수) 흡수가 답습(경쟁사 베끼기·overfit)인가, 아니면 후니에 정당한가?
- 신규 가격축/테이블을 남발하나, 아니면 기존 그릇(siz_width/height 매트릭스·nonspec_incr·소재별 공식·고정가 comp·round-6 CPQ 제약·dim_vals)으로 닫히는가? 특히 RedPrinting `real_price` 전용 면적엔진·면적함수(㎡당 단가)·자재 그룹핑 슬롯을 후니에 신설하지 않고 부결한 것이 타당한가?
- 신규 가격축 신설이 필요하다고 보는가(rpmeta 같은 다른 판정과 무관하게 너의 독립 판단)? 필요 없다면 "없음"이라고 명시.

### Q6. 골든 재현 (허용오차 0)
- golden-cases-silsa-banner의 GC-S1~GC-S12가 설계 공식을 그대로 엔진에 태웠을 때 재현되는가? 직접 계산하라:
  - GC-S1 캔버스 600×1800 ×10 = ? (개당 37,800 기대 378,000)
  - GC-S3 린넨 600×600 ×1 = 17,000 / 린넨 600×1800 = 32,400 (W=가로 앞·H=세로 뒤 축 권위·비대칭 셀)
  - GC-S5 off-grid 현수막 650×650 → ceiling 셀 (격자에 650 없음·한 단계 큰 구간)
  - GC-S7 미니배너 30개 = 147,000(min_qty=19)·100개 = 350,000(min_qty=99)
  - GC-S10 ★배너 타공 6개: 미충전 배선 시(silent 합산) vs 판별차원 충전 후(정답) — 둘 다 계산하라.
- ★W×H 축 권위: 설계는 siz_width=가로(앞)·siz_height=세로(뒤)이고 **work사이즈(블리드) 기준 룩업은 절대 금물**이라 한다. 비대칭 셀(린넨 600×600=17,000 vs 600×1800=32,400)에서 축이 뒤바뀌면(1800×600) 틀린 셀을 룩업하는가? 이 축 권위가 골든 재현에 맞는가?
- 골든값 출처가 "라이브 단가행 verbatim(가격표260527)"이고 "설계가 만든 값"이 아닌가(순환참조 0)? 불일치가 나온다면 진원이 "설계 골든값 자체가 틀림"인지 "라이브 구조 결함(G-S1 후가공 미배선·배너 후가공 판별차원 미충전·prc_typ)"인지 구분하라.

## 출력 형식
- 각 Q에 GO/CONDITIONAL/FAIL 자기 판정 + 근거(어느 파일·어느 케이스·구체 수치 계산).
- ×qty 과청구·silent 이중합산을 발견하면 명확히 명시(구체 수치 계산 포함). 발견 못 하면 "없음"이라고 명시.
- 마지막에 종합 판정(이 실사·현수막 설계를 그대로 적용해도 되는가)과 가장 위험한 결함/우려 1~3개(없으면 "차단 결함 0").
- 디지털인쇄 파일럿(단가=묶음총액)과 실사·현수막(단가=?)의 결정적 차이를 너의 독립 판단으로 확인/반박하라.
- 모르는 것은 "모른다"고. 추측은 추측이라고 표시.
