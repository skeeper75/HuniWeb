# 디지털인쇄 — 정답 추출규칙 (round-13 C2)

> **작성** 2026-06-10 · round-13. 설명 한국어, 식별자/컬럼/코드/SQL 영어.
>
> **목적:** 디지털인쇄 파일럿 7상품 × 5속성축(size·자재·공정·도수·인쇄옵션)을 엑셀 L1(1차 권위)+스키마 의도+확정 도메인에 비춰 **어떻게 추출·변환해 어느 t_*에 넣어야 정확한지** 확정. load_master 실제 규칙(C1)과 나란히 둔다(실제 vs 정답).
>
> **권위 순서[HARD]:** 엑셀 L1 > 적재 SQL/seed > 스키마 의도 > round-11/12 확정. 라이브는 피고.

---

## 0. 파일럿 7상품 (round-11 BOM이 고른 대표 — 단, 디지털인쇄 시트는 실제 36상품)

| prd_cd | prd_nm | 가격공식(라이브) | 비고 |
|--------|--------|------------------|------|
| PRD_000016 | 프리미엄엽서 | PRF_DGP_A | 별색/코팅/커팅 **없음**(엑셀 빈값) |
| PRD_000024 | 포토카드 | PRF_PHOTOCARD_FIXED | 코팅·화이트별색 엑셀 존재 |
| PRD_000027 | 2단접지카드 | PRF_DGP_E | 접지·박·박칼라·가변 존재 |
| PRD_000031 | 프리미엄명함 | PRF_NAMECARD_FIXED | 박·형압·박칼라·가변 존재 |
| PRD_000041 | 스탠다드 쿠폰/상품권 | PRF_DGP_A | 오시·미싱 존재 |
| PRD_000043 | 인쇄배경지(OPP봉투타입) | PRF_DGP_C | 커팅 22행·접지 존재 |
| PRD_000047 | 소량전단지 | PRF_DGP_D | 코팅·접지 존재 |

> **[HARD] 핵심 발견 — round-12 "7상품" 전제 오류:** 디지털인쇄 시트는 **36 고유 상품**. 별색/코팅/커팅은 옵션이 아니라 **별도 상품(SKU)으로 분리**(투명엽서 PRD_000019·화이트인쇄엽서 PRD_000020·코팅엽서 PRD_000017·모양엽서 PRD_000023 등, 전부 라이브 실재). round-11 BOM의 "엽서=별색+코팅+커팅 다 함"은 도메인 추론이며 **엑셀 실측과 불일치**. 추출규칙은 상품별로 엑셀이 실제 가진 값만 따른다.

---

## 1. 속성축별 정답 추출규칙 (전 상품 공통 규칙 + 상품별 인스턴스)

### 축 ①: 사이즈 (size) → `t_siz_sizes` + `t_prd_product_sizes`

**정답 규칙:**
- 엑셀 출처: `사이즈(필수)`(C5, "73 x 98 mm") + `파일사양_작업사이즈`(C8) + `파일사양_재단사이즈`(C9).
- 변환: "73 x 98 mm"→cut_width=73, cut_height=98 / 작업="75 x 100"→work_width=75, work_height=100. C5=C9 동일값(완성치수). 한 siz_cd 행에 work+cut 공존.
- 목표: `t_siz_sizes.cut_width/cut_height/work_width/work_height` + `t_prd_product_sizes`(prd_cd+siz_cd).
- oracle 근거: column-dictionary C5/C8/C9 ✅ · load_master.py:194-207(마스터)·307-315(관계) · fix_size_dims.py(NULL 보정).
- **사이즈 행수 = 엑셀 그 상품의 `사이즈(필수)` nonblank 행수**(세로 캐스케이드의 사이즈 슬롯만, 옵션 캐스케이드 행 제외).

**상품별 정답:**

| 상품 | 엑셀 사이즈 nonblank | 정답 siz 행수 | 비고 |
|------|---------------------|---------------|------|
| PRD_000016 엽서 | 73x98·98x98·100x150·135x135·95x210·110x170·148x210 = **7** | 7 | round-12 "13종" 주장은 오류 |
| PRD_000024 포토카드 | 1종 | 1 | — |
| PRD_000027 접지카드 | 100x150·150x100·135x135·110x170·170x110 = 5 | 5 | 접지 사이즈선택 캐스케이드 별도 |
| PRD_000031 명함 | 90x50·90x55·86x52 = 3 | 3 | — |
| PRD_000041 상품권 | 2종 | 2 | — |
| PRD_000043 배경지 | 6종 | 6 | — |
| PRD_000047 전단지 | 4종 | 4 | — |

### 축 ②: 자재 (material) → `t_mat_materials` + `t_prd_product_materials`

**정답 규칙:**
- 엑셀 출처: `종이(필수)`(C16, "아트250"·"*별도설정"). 디지털인쇄 유일 본체 자재.
- 변환: 종이명+평량→mat_nm. `*별도설정`=공통풀 IMPORT(빈값 아님). 별 시트 `14_상품별자재`가 실제 (상품,자재,용도) 연결 권위.
- usage_cd: `14_상품별자재.용도` 그대로. **디지털인쇄 종이는 용도 빈값→USAGE.07 공통**(load_master.py:324 default·seed USAGE.07="공통" 확정).
- 목표: `t_prd_product_materials`(prd_cd+mat_cd+**usage_cd=USAGE.07**+dep_proc_cd).
- oracle 근거: column-dictionary C16 ✅ · seed line 258(USAGE.07=공통) · load_master.py:318-333 · **round-12 R12-2가 ".01 본체→.07 공통" 정정한 것이 맞음**(단 "라이브 권위"가 아니라 load_master default 규칙이 근거).

> **도메인 주의:** mapping-info.md·product-bom.md는 "usage=본체(USAGE.01)"라 적었으나 **엑셀 용도 컬럼이 비어 load_master가 USAGE.07로 적재**. 정답=USAGE.07(공통). round-11 산출의 "본체" 표기는 도메인 의도이고 적재 현실은 공통 — 의미상 낱장은 본체이나 코드값은 공통. 이건 결함 아님(정당 default).

### 축 ③: 공정 (process) → `t_proc_processes` + `t_prd_product_processes`

**정답 규칙:**
- 엑셀 출처: 후가공 컬럼군 — `코팅`(C23)·`커팅`(C24)·`접지`(C25)·`모서리`(C30)·`오시`(C31)·`미싱`(C32)·`가변텍스트`(C33)·`가변이미지`(C34)·`박/형압 가공`(C35)·`박칼라`(C37)·별색(C18~22).
- 변환(정답): 각 공정 = `t_proc_processes` **부모 공정 1행** + 색상/형상/단수 = `prcs_dtl_opt` param 또는 자식 공정 연결.
  - **코팅(C23):** 부모 PROC_000013 코팅 + 자식 유광014/무광015. **정답=부모 PROC_000013 연결 + prcs_dtl_opt(광택/면)**, 자식만 거는 건 부모 누락 불완전.
  - **박(C35,C37):** 부모 PROC_000033 박 연결 + 박칼라(C37)는 자식 PROC_000034~049 연결 또는 prcs_dtl_opt(박색). **부모 박 행이 있어야 "박 공정 있음"이 성립.**
  - **형압(C35):** PROC_000050 형압 + prcs_dtl_opt(음각/양각).
  - **접지(C25):** PROC_000056 접지 family + 자식(2단059…6단오시073) + prcs_dtl_opt(단수/방향).
  - **커팅(C24):** PROC_000053 완칼 + prcs_dtl_opt(형상). size축 drop 금지(OM-7).
  - **별색(C18~22):** PROC_000007 별색인쇄 root + 자식 008~012 + prcs_dtl_opt(면). **print_side 아님.**
  - **모서리(C30):** 직각=PROC_000027/둥근=PROC_000028(귀돌이 026 자식). 오시=029·미싱=030·가변텍스트=031·가변이미지=032.
- 목표: `t_prd_product_processes`(prd_cd+proc_cd+mand_proc_yn).
- oracle 근거: column-dictionary C18~37 · product-bom.md 횡단 인벤토리 · mapping-final C18~37 ✅ · 라이브 proc 마스터 실측(PROC_000033 박+자식 034~049 family).

**상품별 정답 공정(엑셀 nonblank 기준):**

| 상품 | 엑셀이 가진 공정 | 정답 proc 행 |
|------|------------------|---------------|
| PRD_000016 엽서 | 모서리(직각/둥근)·오시·미싱·가변텍스트·가변이미지 | PROC_000027,028,029,030,031,032 (별색/코팅/커팅 **없음**) |
| PRD_000024 포토카드 | 코팅·화이트별색·모서리 | PROC_000013+014/015·PROC_000008·027/028 |
| PRD_000027 접지카드 | **접지·박+박칼라·가변** | **PROC_000056(접지)+자식**·**PROC_000033(박)+박칼라자식**·031/032 |
| PRD_000031 명함 | **박+형압+박칼라·모서리·가변** | **PROC_000033(박)·PROC_000050(형압)**+박칼라자식·027/028·031/032 |
| PRD_000041 상품권 | 오시·미싱·가변·박+박칼라 | 029·030·031/032·PROC_000033+자식 |
| PRD_000043 배경지 | **커팅 22행·접지** | **PROC_000053(완칼)+형상 param·PROC_000056(접지)** |
| PRD_000047 전단지 | 코팅·접지·가변 | PROC_000013+014/015·PROC_000056·031/032 |

### 축 ④: 도수 (color-count) → `t_prd_product_print_options.front/back_colrcnt_cd`

**정답 규칙:**
- 엑셀 출처: `인쇄(옵션)`(C17, "단면"/"양면").
- 변환: 단면→front=CLR_000005(4도)·back=CLR_000001(0도 인쇄안함) / 양면→both CLR_000005. **별색은 여기 넣지 않음(공정).**
- 목표: `t_prd_product_print_options`(prd_cd+opt_id+print_side+front_colrcnt_cd+back_colrcnt_cd).
- oracle 근거: column-dictionary C17 ✅ · mapping-final C17 라이브 실측 · load_master.py:352-382.
- **모든 디지털인쇄 상품 = 단면/양면 2 옵션**(엑셀 인쇄 컬럼).

### 축 ⑤: 인쇄옵션(plate/판형) → `t_prd_product_plate_sizes`

**정답 규칙:**
- 엑셀 출처: `파일사양_출력용지규격`(C10, "316x467") + `파일사양_출력파일`(C12, PDF/AI).
- 변환(정답): 출력용지규격 치수→**OUTPUT_PAPER_TYPE 코드**(316x467→국전계열 `.01`·기타→`.03`). 치수를 코드로 분류(load_master는 무조건 `.03` 강제 = 오류, §C1 L-B).
- 목표: `t_prd_product_plate_sizes`(prd_cd+siz_cd+output_paper_typ_cd+output_file_typ).
- oracle 근거: column-dictionary C10 ✅ · seed OUTPUT_PAPER_TYPE.01=국전계열/.03=기타 · 메모리 platesize-is-output-paper(국4절=국전계열).
- **정답 = `.01 국전계열`**(316x467은 국전 4절). 라이브 엽서 plate가 `.01`인 것은 정답과 일치(단 load_master는 이 값을 못 만듦 → 후속 손작업 산물).

---

## 2. 미저장(의도) — 추출규칙 N/A

| 컬럼 | 사유 |
|------|------|
| C6 판수(판걸이수) | 앱 임포지션 런타임 계산(메모리 compute-in-app). t_* 매핑 금지 |
| C11 파일명약어·C13 폴더 | Q1 견적 제외(생산 메타). 적재 비대상 |
| C36 박 크기 | 면적→등급 앱 계산. prcs_dtl_opt 입력UX만 |
| C2 ID·C39 추가가격·C41 파일명규칙·C42~44 | 보조키/빈값/footnote 무시 |

---

### 축 ⑥(포장재 전용): 봉투 세트 + 카테고리 → `t_prd_product_addons`/`t_prd_product_sets` + `t_prd_product_categories`

> round-13 C-ID(product-identity §F-ID-1)로 추가된 축. **배경지(043/044/045)는 포장 세트** — 5속성축만으로는 정체(세트성)가 안 잡힌다. 라벨/택(046)·일반 인쇄물은 봉투 세트 없음.

**정답 규칙 — 봉투 세트:**
- 엑셀 출처: `추가상품(옵션)_추가상품`(C38) 자유텍스트 — "봉투 ★사이즈선택 : 76x100"(사이즈 매칭 신호) + "OPP접착봉투 80x120 mm 50장"(구체 봉투 SKU). 배경지 6 사이즈 각각에 매칭 봉투.
- 변환(정답): 봉투 SKU명→`t_prd_templates.tmpl_cd`(base_prd_cd=봉투 상품) 연결 + 배경지 prd_cd→`t_prd_product_addons`(prd_cd+tmpl_cd). 사이즈선택 캐스케이드(배경지 siz_cd ↔ 봉투 siz)는 CPQ template_selections/constraint. **사이트가 "세트"로 판매**(goods_view_102)하므로 `t_prd_product_sets`(prd_cd=배경지, sub_prd_cd=봉투, 사이즈매칭) 모델도 정체 부합.
- 목표: search-before-mint — 봉투 template은 라이브에 **이미 실재**(TMPL-000004~000009: OPP접착/비접착봉투·카드봉투·트레싱지봉투). 신규 mint 불요, 재사용 연결.
- oracle 근거: site `goods_view_102.html`(배경지+봉투/케이스 세트) · product-master:380~388(012 포장 봉투/케이스) · schema-intent:399(봉투=별 SKU template) · 라이브 t_prd_templates 실측.
- **🔴 Q-ID-A:** addon(tmpl_cd) vs sets(sub_prd_cd) vs CPQ 중 적재 모델 컨펌(product-identity §3).

**정답 규칙 — 카테고리:**
- 엑셀 출처: MES prefix(012=포장) + `구분`(배경지).
- 변환(정답): 배경지(043/044/045)·라벨택(046)은 **CAT_000012 포장 하위**에 연결돼야. 현 CAT_000296(배경지)는 upr_cat_cd=NULL 고아 → CAT_000012를 상위로 연결하거나 배경지를 012 하위 노드로 재배치.
- oracle 근거: product-master:82·172·380 · 라이브 카테고리 트리 실측(CAT_000012 포장 lvl1 실재).

**상품별 정답(축⑥):**

| 상품 | 봉투 세트 | 카테고리(정답) |
|------|-----------|----------------|
| PRD_000043 배경지OPP | 6 사이즈×매칭 OPP봉투(TMPL 재사용) | CAT_000012 포장 하위 |
| PRD_000044 배경지케이스 | 2 사이즈×매칭 PP투명케이스 | CAT_000012 포장 하위 |
| PRD_000045 헤더택 | 봉투 매칭 | CAT_000012 포장 하위 |
| PRD_000046 라벨/택 | 없음(단품) | CAT_000012 포장(라벨/포장스티커 CAT_000283) 하위 |
| 엽서·포카·접지·명함·상품권·전단 | 봉투 addon(선택, 세트 아님) | 각 일반 인쇄물 카테고리(정합) |

---

## 3. 커버리지 (M1·K1)

- **5 속성축 × 핵심 7 대표상품 = 35 셀** 전수 추출규칙 확정(§1 축①~⑤).
- **축⑥(포장 세트+카테고리)** = 배경지 3상품+라벨택 1상품에 추가(정체 기반). 일반 인쇄물은 N/A(세트 아님) 사유 명기.
- N/A(미저장) 사유 명기(§2). **빈칸 0.**
