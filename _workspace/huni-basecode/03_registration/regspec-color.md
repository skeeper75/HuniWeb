# 등록 명세 — ③ 도수 (`t_clr_color_counts`) 🟢

> **하네스** hbg Phase 3 설계가 · 2차 회차. **작성** 2026-06-18.
> **입력:** `02_diagnosis/diagnosis-color.md`(🟢 결함 0) · `01_authority/axis-authority-color.md`.
> **[HARD] 명세 ≠ 적용.** 실 COMMIT = dbmap 위임. **결론: 등록/교정/축이동 0 — 정상 폐쇄 SEED·라우팅 빈칸.**

---

## 0. 한 줄 평결

**도수 축 결함 0 → 등록 명세 0건.** `t_clr_color_counts` = CLR_000001~005 5행 폐쇄 SEED(채널수 0~4), 라이브 = 권위 정답과 byte 일치. 별색은 공정 family(PROC_000007·008~012)로 올바로 분리(도수칸 혼입 0). **신규 등록·교정·축이동·소프트삭제 전부 0.** 빈 라우팅을 정직 명시한다.

---

## 1. 등록 명세 단위 (빈 라우팅)

| 명세 단위 | 내용 |
|-----------|------|
| **대상 t_* + 코드값** | `t_clr_color_counts` — **5행 폐쇄 SEED**(CLR_000001 인쇄안함·002 1도·003 2도·004 3도·005 CMYK 4도). 신규 발급 사실상 없음(채널수 물리 상한 0~4). |
| **올바른 의미** | 잉크 채널수 0~4. 별색=공정(PROC_000007 family·clr_cd=NULL)으로 분리(정상). |
| **search-before-mint** | **신규 코드행 0** — SEED 폐쇄·라이브 5행 정답과 1:1 일치. 잉크색·별색은 도수 아님(채널수 ≠ 색). |
| **FK 위상** | 신규 0 — 적재순서 무관. |
| **적재경로** | (해당 없음) — 신규 발급 시에만 catalog Django `tclrcolorcounts__add`. 본 회차 입력 행 0. |
| **영향분석** | 변경 0 → 가격사슬·BOM·백필·롤백 전부 무영향. |

---

## 2. 자재 잉크색 수신 여부 (AX-1 — 도수 부적합 재확인)

| 항목 | 판정 |
|------|------|
| 자재 .10 잉크색 7행 = 도수 축 수신? | **수신 0** — 도수=CMYK 채널수 SEED이므로 잉크색 7종(만년스탬프 잉크색) 부적합. |
| 정답 귀속 | **주문 옵션(CPQ option) 확정**(AX-1·2026-06-18 사용자 컨펌·`_registration-master.md §4.1`). 도수/별색 아님. |
| 본 도수 진단의 역할 | 잉크색이 도수 칸으로 새지 않았음을 재확인(별색 혼입 0·SEED 무변형 라이브 실측). 도수 축에서 처리할 라우팅 0. |

> **★1차 자재 산출 정합:** 잉크색 옵션 축이동은 `regspec-material.md §4.4`(④→옵션 확정)에서 등록 명세화됨. 도수 축은 **목적지가 아님**을 입증하는 역할만(중복 명세 금지).

---

## 3. del_yn / use_yn 권위 적용

- t_clr_color_counts 5행 전건 del_yn='N'·use_yn='Y'(소프트삭제 0·정상). 정리 대상 0.
- del_yn 권위([[dbmap-del-yn-soft-delete-authority]]) 적용 불요(변경 0).

---

## 4. dbmap 인용 (재제안 금지)

- **round-22 ③도수 🟢** 판정 = 라이브 재확인 종결(별색 분리 정상·잉크색 유입 목적지 아님). 재진단·재제안 0.

---

## 5. hbg-validator 통지

- **도수 축 = 등록 명세 0건**(정상 폐쇄 SEED). 검증 포인트: ① 라이브 5행 byte 일치(즉시 어긋남 게이트 통과·6행 이상/별색 혼입 0) ② 잉크색이 도수 목적지가 아님(AX-1 옵션 확정·자재 산출 정합) ③ 변경 0 → 영향분석 N/A.
- **escalate:** 없음.
