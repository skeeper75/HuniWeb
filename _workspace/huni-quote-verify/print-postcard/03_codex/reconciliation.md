# 프린트엽서 — Claude ↔ Codex Reconciliation

> **hqv-codex-cross-verifier** · 2026-06-18 · `print-postcard`
> codex 가용성 = **AVAILABLE (gpt-5.5)** · 판정 = **NO** (work-spec 기반 독립).
> ★ [HARD] **Claude측 판정(`02_verify/verdict-claude.md`)이 아직 부재** — Claude 검증가(hqv-quote-verifier)가 병렬 실행 중. 본 문서는 **codex 단독 결과 정리 + reconcile 가능 범위**까지. **최종 reconcile은 오케스트레이터(메인)가 두 판정을 합쳐 수행.**
> ★ Codex 주장 = 외부 의견·가설(환각 경계). 라이브/권위 엑셀 직접 재실측 전엔 사실 채택 금지.

---

## 0. reconcile 상태

| 항목 | 상태 |
|------|------|
| Codex 판정 | ✅ 완료 (NO·gpt-5.5·exit 0) |
| Claude 판정 (`02_verify/verdict-claude.md`) | ⏳ **부재** (병렬 실행 중·생성 전) |
| 본 에이전트 reconcile | codex 단독 정리 + work-spec 기준 정합 점검까지 |
| 최종 reconcile | **오케스트레이터 위임** (Claude 판정 도착 후 합산) |

> codex 미가용 폴백 아님 — codex는 정상 응답. 단지 reconcile 상대인 Claude 판정이 아직 디스크에 없음(독립 병렬 설계의 정상 상태).

---

## 1. Codex ↔ work-spec(생성자 분해) 정합 점검

Claude 판정이 없으므로, codex 판정을 **work-spec(생성자)의 결함 보드**와 대조해 codex가 독립적으로 같은 결함에 도달했는지(수렴) / 새로 발굴했는지를 정리한다. ★ work-spec은 생성자 주장(dodge-hunt 대상)이지 권위가 아님 — 일치해도 둘 다 같은 자료를 봤으니 "라이브 재실측으로 확정 필요"는 동일하게 남는다.

| 결함 ID (work-spec) | work-spec 주장 | Codex 독립 판정 | 수렴? |
|------|------|------|:--:|
| **D-COLOR** (도수축 드롭·🔴High) | 인쇄비 흑백값만 적재·use_dims 색 없음→칼라 무반영 | **동일 결함 독립 확정** (축3 "가장 크게 깨짐"·칼라 200매 11,250원 과소산출 수치화) | ✅ **강한 수렴** |
| **D-BIND-투명** (투명엽서 바인딩 결손) | PRD_000019 PRF_DGP_A scope인데 라이브 NONE | codex 프롬프트에 **미포함** (대표 PRD_000016만 분해 제시) → codex 판정 없음 | — (범위 밖) |
| **V-4** (유광코팅 0행·고아) | COMP_COAT_GLOSSY 단가행 0 | **동일 결함 독립 확정** (축2 "고아/미적재 결함"·조건부=UI에서 막혀 있으면 영향 없음 단서) | ✅ 수렴 |
| **V-5/D-5** (S1/S2 양키 이중) | 동일 SIZ 양키 동시 제공 시 이중합산 | **위험으로 인정** (단 "확정 결함까지는 아니고 방어장치 없음"·호출이 둘 중 하나만 주는지 모름) | 🟡 부분 수렴(codex가 더 신중) |
| 후가공 이중매칭 (D-1/2 참고) | 줄수/개수 동시매칭 이중합산 | **위험 인정** (배타 매칭 보장 필요·줄수/개수 분리 여부 불명) | 🟡 부분 수렴 |

### Codex 단독 발굴 (work-spec 명시 결함 외 — "미검증 가설"로 분류)
| # | Codex 추가 지적 | 분류 |
|---|------|------|
| C-1 | **손지율 +5장 적용 위치** — 엔진이 전 comp에 같은 수량(출력매수+5) 넘기면 인쇄비 오산. (산식 자체는 권위 일치=결함 아님) | 구현 위험 가설 (엔진 코드 실측 필요) |
| C-2 | **판걸이수 앱 룩업 ↔ 가격표 동기화 의존** — 틀리면 인쇄비·용지비 연쇄 오산 | 운영 위험 가설 |
| C-3 | **별색 중복 적용 규칙** — 별색이 도수축 일부이면서 별색인쇄비 comp로도 존재 → 디지털인쇄비+별색인쇄비 동시/추가 규칙 명확화 필요 | 설계 모호 가설 (라이브 규칙 실측 필요) |

> C-1~C-3은 **codex 단독 주장 = 미검증 가설.** 채택 전 라이브 엔진(pricing.py evaluate_price)·라이브 데이터 직접 재실측 필요. 특히 C-3(별색 중복)은 work-spec이 "별색=별도 항목 정당"으로 본 것과 결이 다르므로 divergence 후보 — Claude 판정·라이브 규칙으로 판별 요.

---

## 2. 합의/불일치 (Claude 부재 → 잠정)

### 강한 합의 후보 (codex ↔ work-spec 수렴 → Claude 도착 시 3자 합의 가능성 높음)
- **D-COLOR (도수축 드롭) = 1순위 돈크리티컬 결함.** codex가 독립적으로 같은 결론+차액 11,250원 수치화. **단 [HARD] 골든 PC-2/PC-4 쌍을 라이브 evaluate_price로 재계산해 칼라·흑백 둘 다 8,750 산출하는지 직접 실측해야 확정** (codex·work-spec 둘 다 명세 기반 추론·라이브 미확인).
- **V-4 (유광코팅 0행).** codex 독립 확정. 단 codex 단서: "UI에서 유광 선택이 막혀 있으면 영향 없음" → Q-유광선택가능여부 컨펌 필요.

### 잠정 divergence / 미확정
- **별색 중복 (C-3)**: codex는 "디지털+별색 동시적용 규칙 모호" 지적, work-spec은 "별색=정당 별도 항목." → 라이브 규칙 실측 + Claude 판정으로 판별.
- **S1/S2 이중 (V-5)**: work-spec=🟡Med 위험, codex="확정 아님·방어장치 없음." 둘 다 라이브 호출측 단일키 보장 여부 재실측 큐로 일치.

---

## 3. 미검증 가설 분류 (★ 라이브 재실측 전 사실 채택 금지)

| 가설 | 출처 | 검증 방법 (오케스트레이터/Claude) |
|------|------|------|
| 도수축 드롭→칼라 무반영 (final 11,250원 과소) | codex+work-spec 수렴 | 라이브 evaluate_price PC-2(칼라)/PC-4(흑백) 재계산 → 둘 다 8,750이면 확정 |
| 유광코팅 0행→0원 침묵 | codex+work-spec | 라이브 t_prc_component_prices COMP_COAT_GLOSSY count=0 재실측 |
| S1/S2 양키 이중합산 | codex+work-spec | 라이브 호출 normalize selections에서 plt_siz/siz 단일키 보장 여부 |
| 별색 디지털+별색인쇄비 중복 적용 (C-3) | **codex 단독** | 라이브 별색 선택 시 두 comp 동시 매칭 여부 |
| 손지율 전 comp 일괄 적용 오산 (C-1) | **codex 단독** | pricing.py 수량 전달 로직 실측 |
| 판걸이수 룩업 비동기 (C-2) | **codex 단독** | 앱 판걸이수 테이블 ↔ 가격표 동기화 점검 |
| 비명시 수량행 구간룩업 정책 | codex "모른다"+work-spec Q-PANSU-LOOKUP | 라이브 엔진 구간룩업 규칙 실측 |

---

## 4. 종합 (codex 단독 단계)

- **Codex 독립 판정 = NO.** 핵심 = 도수축 드롭(축3·돈크리티컬·11,250원 과소산출). work-spec 1순위 결함과 **독립 수렴** → 신뢰도 상향 신호.
- **단 [HARD] 이는 명세 기반 추론.** codex도 work-spec도 라이브 evaluate_price를 직접 호출하지 않음 → **확정은 Claude 검증가의 라이브 재계산(PC-2/PC-4 쌍) + psql 재실측에 의존.**
- **codex 가치 입증**: 단순 동의가 아니라 ① 권위 칼라 열 명시 재구성 ② 차액 11,250원 수치화 ③ 추가 가설 3건(손지율 전파·판걸이수 동기화·별색 중복) 발굴 — 단일 모델 blind-spot 축소에 기여.
- **다음**: Claude 판정 도착 시 오케스트레이터가 codex-NO ↔ Claude 판정 3자(work-spec 포함) reconcile. divergence 후보 = C-3(별색 중복). 합의 후보 = D-COLOR·V-4.
