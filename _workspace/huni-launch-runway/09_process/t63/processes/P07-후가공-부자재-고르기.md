# P07 · 후가공·부자재 고르기

> t63 프로세스 정리 B — 탐색~결제 · L1 **옵션·견적** · L2 후가공 · 부자재

## 1. 한줄정의 · 시작/끝 · 등장인물

**한줄정의** — 손님이 코팅·박·형압·오시·도무송 같은 후가공과 케이스·포장 같은 부자재를 더한다.

- **시작**: 후가공 섹션을 켠다
- **끝**: 후가공·부자재가 확정되어 견적에 반영된다(P08 로 이어짐)

| 등장인물 | 무엇인가 |
|---|---|
| 고객 | 몰을 쓰는 손님(회원·비회원) |
| 위젯 | 상품 상세에 마운트되는 인쇄 주문옵션·견적 위젯 |
| webadmin | 후니 자체 관리서버(Django) — 상품·옵션·가격·위젯빌더 |

**가로지르는 곳**
- t64 — 선택된 공정은 MES 공정 전개의 입력이 된다

## 2. 흐름 (sequence)

```mermaid
sequenceDiagram
    actor C as 고객
    participant G as 위젯
    participant W as webadmin
    C->>G: 후가공 토글 켬
    G->>W: 해당 상품의 공정 목록·택일 그룹 조회
    W-->>G: 가능 공정 + 상호배타 규칙
    C->>G: 코팅·박·형압·오시·미싱·타공·귀돌이·도무송 선택
    G->>W: 공정별 하위 축(색상·면·크기·개수) 조회
    W-->>G: 하위 선택지
    C->>G: 부자재·추가상품 템플릿 선택
    G-->>C: 후가공·부자재 확정
```

## 3. 갈림길 (flowchart · 실패·비회원·취소 분기)

```mermaid
flowchart TD
    A[후가공 토글] --> B{공정 택일 그룹인가}
    B -- 예 --> B1[상호배타 — 하나만]
    B -- 아니오 --> C[복수 선택 가능]
    B1 --> D[공정별 하위 축 입력]
    C --> D
    D --> E{기본제공 부자재 규칙}
    E -- 있음 --> E1[기본제공 표시]
    E -- 없음 --> F[부자재 직접 선택]
    E1 --> F
    F --> G[추가상품 템플릿 묶음]
    G --> H[견적 반영]
```

## 4. 단계표

| # | 단계 | 시스템 | 담당자 | 원장 row_id | 상태 | 근거 |
|---|---|---|---|---|---|---|
| 1 | 코팅 선택(무광/유광 × 단면/양면) | widget | 서희항 | `STD-OPT-024` | 부분 | raw/webadmin/webadmin/catalog/static/catalog/widget_renderer.js:696 · raw/webadmin/webadmin/catalog/static/catalog/widget_renderer.js:3995 |
| 2 | 박 선택(색상 다종 × 면 × 크기 × 내용같음/틀림) | webadmin | 서희항 | `STD-OPT-025` | 부분 | raw/webadmin/webadmin/catalog/models.py:589-608 |
| 3 | 형압(양각/음각) 선택 | webadmin | 서희항 | `STD-OPT-026` | 부분 | raw/webadmin/webadmin/catalog/models.py:596-601 |
| 4 | 오시 선택(줄 수 × 방향) | webadmin | 서희항 | `STD-OPT-027` | 부분 | raw/webadmin/webadmin/catalog/models.py:589-624 |
| 5 | 미싱 선택 | webadmin | 서희항 | `STD-OPT-028` | 부분 | raw/webadmin/webadmin/catalog/models.py:589-624 |
| 6 | 타공 선택(직경 × 개수 × 위치) | webadmin | 서희항 | `STD-OPT-029` | 부분 | raw/webadmin/webadmin/catalog/models.py:589-624 |
| 7 | 귀돌이/라운딩 선택(반경 × 모서리 위치) | webadmin | 서희항 | `STD-OPT-030` | 부분 | raw/webadmin/webadmin/catalog/models.py:589-624 |
| 8 | 도무송(전체/부분·모양·개수) 선택 | webadmin | 서희항 | `STD-OPT-031` | 작동 | raw/webadmin/webadmin/catalog/models.py:589-624 · raw/webadmin/webadmin/catalog/views.py:2610 |
| 9 | 넘버링(일반/난수 × 자릿수 × 개수) 선택 | webadmin | 서희항 | `STD-OPT-032` | 미착수 | 「미확인」 — 찾지 못했다(raw/webadmin/webadmin/catalog/*.py 및 static/catalog/*.js 전체에서 넘버링 문자열 grep 0건) |
| 10 | 부분UV/에폭시 선택 | webadmin | 서희항 | `STD-OPT-033` | 부분 | raw/webadmin/webadmin/catalog/models.py:589-624 |
| 11 | 부자재 선택(케이스·폴리백·포장) | webadmin | 서희항 | `STD-OPT-034` | 작동 | raw/webadmin/webadmin/catalog/models.py:281-294 |
| 12 | 상품별 기본제공 부자재 규칙 표시 | webadmin | 서희항 | `STD-OPT-035` | 미착수 | 「미확인」 — 찾지 못했다(models.py TPrdProductAddons에 기본제공/자동포함 여부 컬럼 없음 · widget_renderer.js ADDON dflt_val 외 기본제공 규칙 표시 로직 없음) |
| 13 | 추가상품(악세사리) 템플릿 묶음 선택 | widget | 서희항 | `STD-OPT-036` | 작동 | raw/webadmin/webadmin/catalog/models.py:281-294 · raw/webadmin/webadmin/catalog/static/catalog/widget_renderer.js:851 |

## 5. 빠진 곳

**나 행은 있는데 코드 0** — 2건

| row_id | 내용 | 진단 |
|---|---|---|
| `STD-OPT-032` | 넘버링(일반/난수 × 자릿수 × 개수) 선택 | 코드에서 넘버링 관련 문자열을 찾지 못했다 — scope.csv 상태도 미착수와 일치. |
| `STD-OPT-035` | 상품별 기본제공 부자재 규칙 표시 | 상품별 기본제공 부자재를 명시적으로 표시하는 규칙/UI 코드를 찾지 못했다. |

**다 코드는 있는데 연결 안 됨** — 9건

| row_id | 내용 | 진단 |
|---|---|---|
| `STD-OPT-024` | 코팅 선택(무광/유광 × 단면/양면) | 무광코팅 라벨과 무광/유광 동시선택 금지 제약 코멘트로 코팅 선택 UI는 확인되나 |
| `STD-OPT-025` | 박 선택(색상 다종 × 면 × 크기 × 내용같음/틀림) | TProcProcesses.side_sel_yn(면별선택)+swatch_color/swatch_img_key가 박(색상·면·크기) 공정의 일반 프레임을 제공하나 전용 박 모델은 없다(공정마스터 데이터로 표현). |
| `STD-OPT-026` | 형압(양각/음각) 선택 | TProcProcesses.side_sel_yn 도움말이 예: 박·형압으로 형압을 명시하며 같은 일반 공정 프레임을 공유한다. |
| `STD-OPT-027` | 오시 선택(줄 수 × 방향) | 오시(줄 수×방향)는 TProcProcesses의 prcs_dtl_opt(공정상세옵션 JSON)로 일반화되어 표현되며 전용 모델·전용 코드 라벨은 찾지 못했다. |
| `STD-OPT-028` | 미싱 선택 | 미싱도 오시와 같은 TProcProcesses+prcs_dtl_opt 일반 프레임으로 표현되며 코드 상 미싱 문자열 자체는 찾지 못했다(데이터 등록 항목으로 추정). |
| `STD-OPT-029` | 타공 선택(직경 × 개수 × 위치) | 타공(직경×개수×위치)도 같은 prcs_dtl_opt 일반 프레임. widget_renderer.js에 직경 병기 로직(공정 판정용 |
| `STD-OPT-030` | 귀돌이/라운딩 선택(반경 × 모서리 위치) | 귀돌이/라운딩(반경×모서리위치)도 같은 prcs_dtl_opt 일반 프레임으로 구현 추정되나 전용 코드 문자열은 찾지 못했다. |
| `STD-OPT-031` | 도무송(전체/부분·모양·개수) 선택 | 도무송은 상품명(합판도무송스티커)으로만 코드에서 확인되고 |
| `STD-OPT-033` | 부분UV/에폭시 선택 | 부분UV/에폭시도 같은 prcs_dtl_opt 일반 프레임으로 추정되며 전용 코드 문자열은 찾지 못했다(X-GAP-UVFORM-01 선행 조사도 적재경로 미상으로 기록). |

## 6. 채우는 방법

| 누가 | 무엇을 | 선행 |
|---|---|---|
| 서희항 | 코팅 선택(무광/유광 × 단면/양면) | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 박 선택(색상 다종 × 면 × 크기 × 내용같음/틀림) | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 형압(양각/음각) 선택 | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 오시 선택(줄 수 × 방향) | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 미싱 선택 | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 타공 선택(직경 × 개수 × 위치) | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 귀돌이/라운딩 선택(반경 × 모서리 위치) | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 도무송(전체/부분·모양·개수) 선택 | 코드는 있는데 원장 상태가 「작동」다 |
| 서희항 | 넘버링(일반/난수 × 자릿수 × 개수) 선택 | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |
| 서희항 | 부분UV/에폭시 선택 | 코드는 있는데 원장 상태가 「부분」다 |
| 서희항 | 상품별 기본제공 부자재 규칙 표시 | 코드·명세를 뒤졌으나 구현을 찾지 못했다 |

## 7. 확인 못 한 것

넘버링(STD-OPT-032)은 원장 상태가 미착수인데 라이브 위젯에 노출되는지 확인하지 않았다.

---
> 이 문서의 상태·근거는 원장 `08_system-screen/t56/rejudge.csv` 와 이 카드의 증거 수집본에서 기계로 채웠다. 「미확인」은 코드·원장·명세를 뒤졌으나 **찾지 못했다**는 뜻이지, 없다는 뜻이 아니다.
