# 082 하드커버 링책자 — 면지 통합 재설계 골든 재현(무손상 입증) DRY-RUN

> §23 · 2026-07-03 · 라이브 엔진 `evaluate_set_price` 실호출(`_workspace/_foundation/batch` lib_huni·`.env.local`). 라이브 읽기전용.
> 072/077 파일럿 동형 전파. 재현 = 072 `_repro.py PRD_000082`(구조 diff·3조건) + `set_full_scan.py PRD_000082`(하네스 골든).

## 1. 현행 골든 = 라이브 실측 (재설계 전)

082 셋트공식 = **PRF_HC_TWINRING_SET** → 단일 comp **COMP_BIND_HC_TWINRING**
use_dims=`["proc_cd", "min_qty", "proc_grp:PROC_000017"]` (**mat_cd 없음 = 자재 미종속**·라이브 실측).
면지 멤버 084/085/086/087 = 공식 없음(fml=0) → 기여 **정확히 0**.

### 1-a. 하네스 골든 스캐너 (`set_full_scan.py` · COPIES=100 · 내지 derived24) — 권위 스캔
| prd | final | set_eval | 멤버 기여 |
|---|---|---|---|
| PRD_000082 | **818,438** | 800,000 | 083:0 · 286(내지):18,438 · 084:0 · 085:0 · 086:0 · 087:0 |

- final 818,438 = set_eval(800,000·트윈링 제본비 COMP_BIND_HC_TWINRING) + 내지 286(18,438). 표지 083·면지 084/085/086/087 = 전부 0.
- ※ `set_full_scan.py` GOLDEN 딕셔너리의 44123은 **qty1·30p 조건의 동작화 골든(스테일 엔트리)** — 스캐너 COPIES=100 조건과 달라 `!=gold` 플래그가 뜨는 건 조건 차이이지 결함 아님(077의 51146과 동일 성격). 무손상 판정은 **동일 조건 전후 대조**로 한다.

### 1-b. 3조건 구조 diff (`_repro.py PRD_000082` · 셋트공식 미발현 순수 멤버합) — 면지 collapse 불변 실증
| 케이스 | 멤버 | copies1 | copies10 | copies100 |
|---|---|---|---|---|
| BASELINE | 083+286+084+085+086+087 | 184 | 1,844 | 18,438 |
| REDESIGN | 083+286+084 | 184 | 1,844 | 18,438 |
| SANITY | 083+286 (면지 0) | 184 | 1,844 | 18,438 |

- 면지 멤버 4→1→0 전 케이스에서 멤버합 **완전 동일** → 면지 084/085/086/087 기여 정확히 0.
- (이 repro는 proc 미선택으로 셋트공식 미발현·순수 멤버합만 비교 — collapse 불변의 자. 셋트공식 발현 골든은 §1-a 818,438.)

## 2. 재설계 후 = 참 골든 불변 (구조 증명)

재설계 SQL이 건드리는 것: ① 084 리네임 ② 084에 자재4+옵션 추가 ③ 부모 082 USAGE.03 자재4+OPT_066 은퇴 ④ 085/086/087 셋트링크 은퇴.
**건드리지 않는 것**: 셋트공식 PRF_HC_TWINRING_SET·COMP_BIND_HC_TWINRING·**USAGE.07 링자재(013/014/015)**·내지 286·표지 083.

| 조건 | BASELINE(면지4) | REDESIGN(면지1=084) | SANITY(면지0) | 판정 |
|---|---|---|---|---|
| copies1 순수멤버합 | 184 | **184** | 184 | 불변 ✓ |
| copies100 순수멤버합 | 18,438 | **18,438** | 18,438 | 불변 ✓ |
| 하네스 스캔 copies100 (set_eval 800,000 발현) | 818,438 | **818,438(예상)** | 818,438 | 불변 ✓ |

→ 면지 멤버는 공식 없음 → 자재 유무·색/인쇄 선택 무관 기여 0. 4→1 통합 가격영향 0.

## 3. 인쇄면지(MAT_385 / 087) 기여 0 — 확인 [HARD·과제 명시]

- BASELINE `_repro.py`에서 멤버 087(하드커버 링책자-면지(인쇄면지)) contribution=**0**(copies 1/10/100 전부).
- 재설계 후: 084에 MAT_385(인쇄면지) 자재를 추가하고 "인쇄"(OPV_443) 옵션으로 택1 가능하나, **면지 멤버 084는 공식 없음(fml=0)** → MAT_385(인쇄면지) 선택 시에도 evaluate_price 기여 **정확히 0**.
- 인쇄면지 인쇄비 실현은 무공식 → 별개 D-3 트랙(§18). 본 재설계는 **무손상 보존만**(인쇄비 실현 아님). → `blocked-board-082.csv` 등재.

## 4. USAGE.07 링자재 불가침·가격 보존 [HARD·과제 명시]

- 부모 082 USAGE.07 링자재 = **MAT_013(화이트링)·MAT_014(블랙링)·MAT_015(링메탈링)** — apply-082.sql은 `usage_cd='USAGE.03'` 가드로 은퇴 대상을 면지자재로만 한정 → **링자재는 SQL 어디서도 미참조**(은퇴/이관 0).
- DRY-RUN 사후검사: `082 USAGE.07 링 활성 = 3`(불변·PASS1/PASS2 동일). 링자재 유지.
- set_eval=800,000(COMP_BIND_HC_TWINRING·proc 기반)은 링자재 미변경 + 자재 미종속(use_dims에 mat_cd 없음)이므로 트리비얼하게 보존. **링 가격 기여 무영향 확인.**

## 5. 부모 082 면지자재/옵션 은퇴 무손상 (구조적 증명)

- 셋트공식 comp **COMP_BIND_HC_TWINRING** use_dims=`["proc_cd","min_qty","proc_grp:PROC_000017"]` — siz_cd/print_opt_cd/bdl_qty/**mat_cd 없음**. `set_selections_for(082)` = 빈 셀렉션 → 부모 082 USAGE.03 면지자재·OPT_066은 셋트공식 입력에 **전혀 안 들어감**.
- 하네스 스캔 golden 818,438 = set_eval(800,000·COMP_BIND@proc) + 내지 286(18,438). 둘 다 부모 082 면지자재·면지옵션 미참조 → 은퇴 가격중립.
- **이중합산 0** — 면지비 어디에도 이중 계상 없음(면지=제본비 포함 도메인·기여 0). 골든 불변이 증명.

## 6. 롤백전용 DRY-RUN 실증 (BEGIN…ROLLBACK · 2026-07-03 · 라이브)

`apply-082.sql` 2회 연속 apply 후 상태 검사 → ROLLBACK. 결과:
- pass1: UPDATE1(리네임) / INSERT 4·1·4·4(자재·그룹·옵션·아이템) / UPDATE 4·4·1·4·3·3(부모 은퇴+멤버 은퇴+use_yn) — 11스텝 정상.
- **pass2 멱등**: 리네임 UPDATE 0·부모은퇴 UPDATE 0·ON CONFLICT INSERT delta 0 → 2회차 상태 변화 없음.
- **트리거 fn_chk_opt_item_ref 통과**(에러 0) — 자재[2] 선행 → 옵션아이템[5]/OPV_443→MAT_385 참조 무결.
- 사후 상태: 084='하드커버 링책자-면지'·자재4·옵션4·아이템4 활성 / 082 USAGE.03=0·OPT_066=0(은퇴) / **082 USAGE.07 링=3(불가침 유지)** / 활성멤버=083·286·084 / 085·086·087 use_yn=N.
- ROLLBACK 후 비영속(이전상태 완전 복원: 084~087 del_yn=N·부모 자재/옵션 활성).

## 7. 무손상 판정

- **final_price 불변**: 순수멤버합 전 조건 BASELINE=REDESIGN=SANITY 동일(면지 기여 0)·하네스 골든 818,438 예상 불변.
- **셋트공식 자재 미종속**(COMP_BIND_HC_TWINRING use_dims에 mat_cd 없음) → 부모 면지자재/옵션 은퇴 가격중립.
- **USAGE.07 링 보존**: SQL 미참조·DRY-RUN 활성 3 유지·set_eval 무영향.
- **인쇄면지(MAT_385) 기여 0**: 084 무공식 → 인쇄 선택 시에도 0(D-3 후속).
- **이중합산 0**·**PRICE≠0**(818,438).

→ **S4(가격 e2e) 무손상 통과 예상.** 실 게이트는 `hsp-set-gate-validation`이 evaluate_set_price 독립 재계산 + DRY-RUN으로 재판정.

## 8. 게이트 인계 체크리스트
- [x] apply-082.sql 롤백전용 DRY-RUN 멱등·트리거 통과 확인(§6).
- [x] USAGE.07 링자재 3종 활성 유지(불가침) 확인(§4·§6).
- [x] 인쇄면지 MAT_385/087 기여 0 확인(§3).
- [ ] 재설계 후 라이브 골든(하네스 스캔 818,438) 재계산 일치 — 게이트가 커밋 DRY-RUN 상태에서 set_full_scan 재실행으로 확인.
- [ ] 084 자재 드롭다운(화/블/그/인쇄) 렌더·fn_chk_opt_item_ref 위반 0.
- [ ] 인간 승인 후 webadmin 실화면(제외 0·PRICE≠0·판형 자동선택·085/086/087 미노출).
