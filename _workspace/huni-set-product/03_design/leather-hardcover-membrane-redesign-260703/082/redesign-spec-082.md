# 082 하드커버 링책자 — 면지 통합 재설계 명세 (072/077 파일럿 동형 전파)

> §23 Huni-Set-Product · hsp-set-designer · 2026-07-03 · **DB 미적재(설계·적재본까지)**
> 파일럿 072/077(COMMIT 완료) → 082 동형 전파. 라이브 실측 읽기전용(`.env.local RAILWAY_DB_*`) + 라이브 엔진 골든 재현.
> 근거: `03_design/leather-hardcover-membrane-redesign-260703/redesign-spec.md`(072 계열 §7 전파표·082 행).

---

## 0. 결론 요약 (TL;DR)

- **082는 072/077의 4-멤버 변형**(면지 4개=화/블/그/**인쇄**). 면지색 이중표현(옵션 OPT_066 + 빈 멤버 084/085/086/087) → 면지 1멤버(084) 통합·085/086/087 은퇴·자재4+옵션을 부모 082→084로 이관·부모 은퇴.
- **가격 무손상(라이브 엔진 실증)** — 면지 멤버 084~087 = 공식 없음(fml=0) → evaluate_set_price 기여 **정확히 0**. 셋트공식 COMP_BIND_HC_TWINRING use_dims=`["proc_cd","min_qty","proc_grp:PROC_000017"]` 자재 미종속. 하네스 골든 = **818,438**(= set_eval 800,000 + 내지 286 18,438) → 재설계 후 **동일**(4→1→0 전 케이스 불변, `golden-reproduce-082.md`).
- **★특이점 2건**:
  1. **인쇄면지 MAT_385(4번째 택1)** — 084에 이관·"인쇄"(OPV_443) 옵션 발현. 단 084 무공식이라 인쇄 선택 시에도 기여 0(무공식·D-3 후속). 본 재설계는 **무손상 보존만**.
  2. **★★USAGE.07 링자재 MAT_013/014/015 = 불가침** — 면지가 아닌 표지/바인딩(링) 자재. apply-082.sql은 `usage_cd='USAGE.03'` 가드로 링자재를 **절대 미참조**(은퇴/이관 0). 부모에 그대로 유지.
- 적재본 = `apply-082.sql`(8스텝+선택 내지블록) + `undo-082.sql`. 전부 멱등·기존 참조(**신규 mint 0·물리 DELETE 0**).

---

## 1. 현재 구조 (라이브 실측 · 2026-07-03)

| 항목 | 실측값 |
|---|---|
| 082 셋트공식 | PRF_HC_TWINRING_SET ("하드커버트윈링 제본 합산가"·링 전용 신설·무선 PRF_HC_MUSEON_SET 재사용 금지) |
| 셋트 comp | COMP_BIND_HC_TWINRING (PROC_000024·6밴드) use_dims=`["proc_cd","min_qty","proc_grp:PROC_000017"]`·단가행 6·**자재 미종속** |
| 셋트 멤버(del_yn=N) | 083 표지(disp1·qty1·min1/max1·SEMI_ROLE.02) · 286 내지(disp2·min8/max100/incr2) · 084/085/086/087 면지(disp3~6·qty1·min/max/incr NULL·SEMI_ROLE.03) |
| 084~087 삼각확인 | mat=0·siz=0·prt=0·plt=0·fml=0 (전부 완전 빈 껍데기) |
| 084~087 이름 | 084="…-면지(화이트면지)"·085="(블랙면지)"·086="(그레이면지)"·087="**(인쇄면지)**" · 전부 PRD_TYPE.02 반제품·SEMI_ROLE.03 |
| 부모 082 면지자재(USAGE.03) | MAT_382(화이트면지·dflt Y)·MAT_383(블랙면지)·MAT_384(그레이면지)·**MAT_385(인쇄면지)** |
| 부모 082 면지옵션 | OPT_066 "면지색"(SEL_TYPE.01·1~1·mand Y) → OPV_440 화/441 블/442 그/**443 인쇄** → 아이템 ref OPT_REF_DIM.03 MAT_382/383/384/**385** |
| 부모 082 **USAGE.07 링자재** | **MAT_013(화이트링)·MAT_014(블랙링)·MAT_015(링메탈링)** — 표지/바인딩(링) 자재·**면지 재설계 불가침** |

- 084~087은 **부모 082 전용 구성원**(다른 셋트 미참조·실측) + **옵션아이템 ref_key1 역참조 0**(실측) → 은퇴 안전.
- 내지 286은 완전 구축(mat9/siz3/prt2/plt1/fml1·PRF_DGP_INNER) — 면지 재설계 무관·미변경.

## 2. 목표 모델 [HARD · 072/077 동형·단 4택1]

082 = **표지083 + 내지286 + 면지(1개=084)**. 면지색 화/블/그/**인쇄** = **084 내부 택1**.

1. **면지 멤버 084 통합** — "하드커버 링책자-면지"로 리네이밍. 085/086/087은 셋트에서 은퇴(`t_prd_product_sets.del_yn=Y` + 상품마스터 use_yn=N).
2. **자재 이관** — 부모 USAGE.03 MAT_382/383/384/**385** → 084에 적재(dflt=화이트 MAT_382). 구성원 용지 select 드롭다운 = 색/인쇄 택1 실동작 수단.
3. **면지색 옵션그룹 이관** — 부모 OPT_066(+옵션 OPV_440~**443** + 아이템)를 084로 이동(동일코드 재사용·PK=prd_cd 스코프). **★fn_chk_opt_item_ref: OPT_REF_DIM.03 아이템(OPV_443→MAT_385 포함)은 참조자재가 같은 prd_cd(084)에 실재해야 함 → 자재 이관[2] 후 옵션 이관[3~5] 순서 [HARD].**
4. **부모 082 은퇴** — 이관 완료 후 OPT_066 및 USAGE.03 면지자재 4종 논리삭제(del_yn=Y). 셋트공식 자재 미종속 → 가격 무영향. **★USAGE.07 링자재(013/014/015)는 미변경(불가침).**

### 옵션그룹 이관 실효성 주의 (072/077과 동일)
현 셋트 시뮬레이터는 구성원 옵션그룹 미렌더(`_set_members_meta` opt_groups 미동봉) → 이관 OPT_066은 시뮬레이터에서 **휴면**(색/인쇄 택1은 자재 드롭다운으로 발현). 옵션그룹 이관 자체는 가격/계산 무손상(evaluate_set_price 제약 미참조). 구성원 옵션그룹 렌더는 072 §6 DEV-REQUEST D-1과 동일 후속 트랙.

## 3. 가격 정합·무손상

`evaluate_set_price` = Σ 구성원 evaluate_price + 셋트공식(copies) + 할인. 면지 멤버 = 공식 없음 → 기여 0.
- COMP_BIND_HC_TWINRING use_dims=`["proc_cd","min_qty","proc_grp:PROC_000017"]` → **mat_cd 없음·자재 미종속**·`set_selections_for(082)`=빈 셀렉션 → 부모 면지자재/옵션 은퇴 가격중립.
- **★USAGE.07 링 보존**: SQL 미참조·자재 미종속 → set_eval=800,000 트리비얼 보존(DRY-RUN 링 활성 3 유지).
- **인쇄면지 MAT_385 기여 0**: 084 무공식 → 인쇄 선택 시에도 0(D-3 후속·무손상 보존만).
- **이중합산 0** — 면지비 어디에도 이중 계상 없음(골든 불변이 증명).
- 상세 골든(818,438 불변·인쇄면지 0·USAGE.07 보존) = `golden-reproduce-082.md`.

## 4. 내지 개수필드(min/max/incr) 정정 — 선택(072/077과 동형)

내지 286 현재 min8/max100/incr2 = 페이지수 오등록(page_rule 부모 082 별도 보유). 정정안 = min1/max1/incr NULL(무의미한 "+내지 추가" 버튼 제거·가격중립). **파일럿 미포함**(주석 선택블록) — 내지 페이지가격 실현(semi_role.01)과 묶어야 코히런트 → §23-inner 별도 트랙(D-2).

## 5. search-before-mint / 경계 준수

- **신규 mint 0** — 면지 멤버 084·자재 MAT_382/383/384/385·옵션 OPT_066/OPV_440~443·셋트공식 전부 라이브 기존 참조. 재사용만. **물리 DELETE 0**(은퇴=del_yn 논리·undo의 신규삽입분 DELETE만 물리).
- **경계(옵션오염 방지)** — 084엔 자기 면지자재(USAGE.03)만. **082 부모의 USAGE.07 링자재(표지/바인딩)는 084로 끌어오지 않음**(경계 준수). 셋트공식 공유(PRF_HC_TWINRING_SET·링 전용)는 자재 미종속이라 오염 없음. ★S8 오염 방지: 무선 PRF_HC_MUSEON_SET과 comp 분리(링=COMP_BIND_HC_TWINRING 단독)로 책자별 제본 comp 경계 유지.
- **product-type-board** — 084(PRD_TYPE.02 반제품·SEMI_ROLE.03 면지) 유지. 구성원 전부 반제품.

## 6. 적재 순서 [HARD] (트리거 fn_chk_opt_item_ref)

자재[2] → 옵션그룹[3] → 옵션[4] → 옵션아이템[5] → 부모옵션 은퇴[6] → 부모자재 은퇴(USAGE.03만)[7] → 빈멤버 은퇴[8].
`apply-082.sql` DRY-RUN(BEGIN…ROLLBACK) 실증: 멱등(2회 delta 0)·트리거 통과(OPV_443→MAT_385 포함)·활성멤버 083/286/084·부모 USAGE.03=0·**USAGE.07=3(불가침)**·OPT_066=0·085/086/087 use_yn=N.

## 7. 차단분 (blocked-board-082.csv)

**082 면지통합 차단 0** — 전 구성원·자재·옵션·공식 라이브 기존 참조(신규 mint 불요). 후속 트랙(면지 통합 블로커 아님):
- D-1 구성원 옵션그룹 렌더 · D-2 내지 페이지가격(§23-inner) · **D-3 인쇄면지(MAT_385) 인쇄비 실현**(082/088 특이·§18).

## 8. 다음 단계
codex 독립 2차(reconcile) → S1~S8 게이트(evaluate_set_price 재계산·커밋 DRY-RUN에서 set_full_scan 818,438 재확인·USAGE.07 링 보존·인쇄면지 0) → 인간 승인 → load-executor COMMIT → webadmin 실화면(제외0·PRICE≠0·084 화/블/그/인쇄 드롭다운 기본화이트·085/086/087 미노출·링자재 유지).
