-- =====================================================================
-- 스티커 라이브 정정 적재본 (apply.sql) · round-13 / 2026-06-14
-- 정답 권위: 원본 엑셀 L1 최신 24_master-extract-260610/sticker-l1.csv (E1~E5 GO)
-- [HARD] 실제 COMMIT은 인간 승인 후 실행. 파일은 보유하되 자동 실행 금지.
-- 멱등: WHERE 현재값 조건 / ON CONFLICT DO NOTHING|UPDATE → 재실행 시 0행 또는 동일결과.
-- 비파괴: COMMIT/DDL/DELETE 없음. EXTRA=hard-delete 아닌 논리삭제(use_yn='N'+note).
-- 타임스탬프: INSERT reg_dt=now() / UPDATE upd_dt=now() + note 변경이력.
-- 디지털인쇄 패턴(23_remediation-apply/digital-print/apply.sql) 계승.
-- =====================================================================

BEGIN;

-- =====================================================================
-- [즉시적용 S-01] 점착지 자재유형 정정 — 3종 .01(종이)→.11(스티커)
-- 같은 스티커 점착물인데 일부가 MAT_TYPE.01(종이)로 갈려 들어옴(v03 05시트 자재구분 혼재).
-- 명백한 점착지 3종만 즉시적용: 비코팅스티커(084)·미색스티커(242)·투명전용지(243).
-- 타투전용지(167)는 전사지라 종이 가능 → 컨펌(Q-ST-F)으로 분리(여기 미포함).
-- 멱등: WHERE mat_typ_cd='MAT_TYPE.01' → 이미 .11이면 0행.
-- 근거: 라이브 실측(.01/.11 혼재) + table-spec MAT_TYPE.11=스티커 + 동질 점착지 .11 다수 존재.
-- =====================================================================

UPDATE t_mat_materials
   SET mat_typ_cd='MAT_TYPE.11', upd_dt=now(),
       note=COALESCE(note,'')||' | 정정 2026-06-14: 자재유형 종이(.01)→스티커(.11) 점착지'
 WHERE mat_cd IN ('MAT_000084','MAT_000242','MAT_000243')
   AND mat_typ_cd='MAT_TYPE.01';   -- 멱등: 이미 .11이면 0행

-- =====================================================================
-- [즉시적용 S-02] 063 반칼팬시투명스티커 화이트인쇄 공정 연결 (MISSING 보충)
-- 투명 베이스는 화이트 underbase 도메인 필수. 053/054/056엔 있고 063만 누락(v03 15시트 결함).
-- L1 063 `별색인쇄(옵션)_화이트`=`화이트인쇄(단면)` 실재. PROC_000008 마스터 실재(search-before-mint).
-- 멱등: ON CONFLICT (prd_cd,proc_cd) DO NOTHING.
-- 주의: t_prd_product_processes 에 note 컬럼 없음 → 변경이력은 reg_dt=now()로 추적.
-- mand_proc_yn/disp_seq = 형제(053/054/056) 패턴 그대로 계승: mand='N'·disp_seq=2(커팅 뒤).
--   (커팅 필수성 Y 전환은 별건 Q-ST-G — 여기서 선점하지 않음. 063 현재 커팅=PROC_000055 seq=1.)
-- =====================================================================

INSERT INTO t_prd_product_processes (prd_cd, proc_cd, mand_proc_yn, disp_seq, reg_dt, del_yn)
VALUES ('PRD_000063','PROC_000008','N',2,now(),'N')
ON CONFLICT (prd_cd, proc_cd) DO NOTHING;

-- =====================================================================
-- [즉시적용 S-03] 066 빈 옵션그룹 OPT-000004 "원형" 논리삭제 (EXTRA 정리)
-- option_items 0행 빈 껍데기(round-9 admin/round-6 CPQ 파일럿 잔재·2026-06-10).
-- 066 형상은 이미 size(Q7 칼틀)로 표현 → 옵션그룹 중복. hard-delete 금지 → use_yn='N'+del_yn='Y'.
-- 멱등: WHERE use_yn='Y' → 이미 N이면 0행.
-- =====================================================================

UPDATE t_prd_product_option_groups
   SET use_yn='N', del_yn='Y', del_dt=now(), upd_dt=now(),
       note=COALESCE(note,'')||' | 정정 2026-06-14: 빈 옵션그룹 잔재 논리삭제(items 0행·형상=size로 표현)'
 WHERE prd_cd='PRD_000066' AND opt_grp_cd='OPT-000004'
   AND use_yn='Y';   -- 멱등: 이미 N이면 0행

-- =====================================================================
-- 위까지가 즉시적용분(자재유형 3종·063 화이트·066 빈 옵션그룹).
-- 아래는 모두 컨펌대기/신규적재 — 주석 처리(실행 안 함).
-- =====================================================================

COMMIT;   -- [HARD] 인간 승인 후에만 실제 실행.

-- =====================================================================
-- ============== 신규적재/컨펌대기 블록 (실행 금지 · 설계 보존용) ==============
-- =====================================================================

-- --------------------------------------------------------------------
-- [신규적재 S-04] 카테고리 16상품 정상노드 재연결 (search-before-mint·신규 mint 0)
-- 16상품 전부 root CAT_000002(스티커·lvl1)에만 연결. 정상노드 030~047 전부 실재.
-- PK=(prd_cd,cat_cd) 복합 → cat_cd UPDATE 불가 → 정상노드 INSERT + root행 논리강등(main_cat_yn='N').
-- root(CAT_000002)는 고아 아닌 정상 lvl1 → DELETE 안 함·보조 연결로 강등.
-- 상품명 1:1 매칭 확정(2026-06-14 실측): 052→030·053→031·054→032·055→034·056→035·057→036·
--   058→038·059→039·060→040·061→041·062→042·063→043·064→033·065→047·066→044·067→046.
-- --------------------------------------------------------------------
-- INSERT INTO t_prd_product_categories (prd_cd, cat_cd, main_cat_yn, disp_seq, note, reg_dt, upd_dt)
-- VALUES
--   ('PRD_000052','CAT_000030','Y',1,'정정 2026-06-14: root→정상노드 반칼 자유형 스티커',now(),now()),
--   ('PRD_000053','CAT_000031','Y',1,'정정 2026-06-14: root→정상노드 반칼 자유형 투명스티커',now(),now()),
--   ('PRD_000054','CAT_000032','Y',1,'정정 2026-06-14: root→정상노드 반칼 자유형 홀로그램스티커',now(),now()),
--   ('PRD_000055','CAT_000034','Y',1,'정정 2026-06-14: root→정상노드 낱장 자유형 스티커',now(),now()),
--   ('PRD_000056','CAT_000035','Y',1,'정정 2026-06-14: root→정상노드 낱장 자유형 투명스티커',now(),now()),
--   ('PRD_000057','CAT_000036','Y',1,'정정 2026-06-14: root→정상노드 대형 자유형 스티커',now(),now()),
--   ('PRD_000058','CAT_000038','Y',1,'정정 2026-06-14: root→정상노드 반칼 원형 스티커',now(),now()),
--   ('PRD_000059','CAT_000039','Y',1,'정정 2026-06-14: root→정상노드 반칼 정사각 스티커',now(),now()),
--   ('PRD_000060','CAT_000040','Y',1,'정정 2026-06-14: root→정상노드 반칼직사각스티커',now(),now()),
--   ('PRD_000061','CAT_000041','Y',1,'정정 2026-06-14: root→정상노드 반칼띠지스티커',now(),now()),
--   ('PRD_000062','CAT_000042','Y',1,'정정 2026-06-14: root→정상노드 반칼팬시스티커',now(),now()),
--   ('PRD_000063','CAT_000043','Y',1,'정정 2026-06-14: root→정상노드 반칼팬시투명스티커',now(),now()),
--   ('PRD_000064','CAT_000033','Y',1,'정정 2026-06-14: root→정상노드 소량 자유형 스티커',now(),now()),
--   ('PRD_000065','CAT_000047','Y',1,'정정 2026-06-14: root→정상노드 스티커팩',now(),now()),
--   ('PRD_000066','CAT_000044','Y',1,'정정 2026-06-14: root→정상노드 합판도무송스티커',now(),now()),
--   ('PRD_000067','CAT_000046','Y',1,'정정 2026-06-14: root→정상노드 타투스티커',now(),now())
-- ON CONFLICT (prd_cd,cat_cd) DO UPDATE SET main_cat_yn='Y', upd_dt=now(), note=EXCLUDED.note;
-- -- root 행 보조강등(논리·hard-delete 아님):
-- UPDATE t_prd_product_categories
--    SET main_cat_yn='N', upd_dt=now(),
--        note=COALESCE(note,'')||' | 정정 2026-06-14: 정상노드로 메인 이전(root 보조)'
--  WHERE prd_cd BETWEEN 'PRD_000052' AND 'PRD_000067'
--    AND cat_cd='CAT_000002' AND main_cat_yn='Y';

-- --------------------------------------------------------------------
-- [신규적재 S-05 / 컨펌 Q-ST-B] 조각수 적재 — 저장처(ref_param_json) 부재
-- L1 조각수 실재: 052(*최대20/40)·055/056/057(5~10조각)·064(*1조각). 라이브 0행(066만 형상별 EA).
-- Q8★=묶음수+조각수 둘 다. 공정 prcs_dtl_opt.조각수 상품 레벨 저장처 부재(OM-7).
-- → bundle_qty 부분 적재는 가능하나 *최대N(상한 범위) 의미를 단일 bundle로 표현 곤란. ddl-proposer 선결.
-- (보류 — Q-ST-B 컨펌 + ref_param_json 신설 후)

-- --------------------------------------------------------------------
-- [컨펌 Q-ST-A·CONFLICT-1] 코팅 8상품 — 공정(Q9) vs 자재(라이브/L1/가격) 충돌
-- 라이브: 052·058·059·060·061·062·064·066 에 무광/유광코팅스티커(MAT_000155/156) 자재.
-- L1: 종이칸에 무광/유광코팅스티커 자재 variant로 인코딩(코팅 옵션칸 전 행 빈값). round-11 §44 자재 정당.
-- Q9★: 코팅=공정. → 충돌 미해소. 공정 전환 시(컨펌 후):
-- INSERT INTO t_prd_product_processes (prd_cd, proc_cd, mand_proc_yn, disp_seq, reg_dt, del_yn)
-- SELECT prd_cd,'PROC_000013','N',8,now(),'N' FROM (VALUES
--   ('PRD_000052'),('PRD_000058'),('PRD_000059'),('PRD_000060'),
--   ('PRD_000061'),('PRD_000062'),('PRD_000064'),('PRD_000066')) v(prd_cd)
-- ON CONFLICT (prd_cd,proc_cd) DO NOTHING;
-- + 자재 MAT_000155/156 연결 정리(비코팅 분해) + 가격엔진 코팅 단가 반영. [HARD] 컨펌 필수.

-- --------------------------------------------------------------------
-- [컨펌 Q-ST-C] 규격형 058~062 형상 저장처 — size/공정/상품명 중 결정
-- 형상(원형/정사각/직사각/띠지/팬시)이 어디에도 없음. PROC_000054 반칼(모양 param) 교체 또는
-- 합판식 siz_nm 인코딩 통일 또는 현행 유지. ref_param_json(OM-7) 선결. (보류)

-- --------------------------------------------------------------------
-- [컨펌 Q-ST-D] 055/057 자재명 절단 — "유포지"→"유포지+엠보코팅"
-- mat_nm 변경=멱등키 영향. 자재명 복원 vs 별 mat_cd 신설. (보류)

-- --------------------------------------------------------------------
-- [컨펌 Q-ST-E] 065 스티커팩 세트 구성 — 구성품 불명(L1 미명시)
-- t_prd_product_sets 하위 정의 vs 단일 시트 상품. 팩 구성 데이터 필요. (보류)

-- --------------------------------------------------------------------
-- [컨펌 Q-ST-MES] MES_ITEM_CD — 16상품 NULL vs L1 002-0001~0016
-- load_master 의도(MES 중복 UNIQUE 회피). 중복 정리 후 재적재 vs NULL 유지. (정책·보류)

-- --------------------------------------------------------------------
-- [컨펌 Q-ST-F] 타투전용지(MAT_000167) 자재유형 — 종이(.01) vs 스티커(.11)
-- 전사지=종이 가능(전사 인쇄). S-01 점착지 3종과 분리. 표본 확인 후. (보류)

-- --------------------------------------------------------------------
-- [컨펌 Q-ST-G] 커팅 필수성 mand_proc_yn — 커팅=정체(Y) vs 라이브 전부 N
-- 반칼/완칼이 상품명에 인코딩=필수 주장 vs 옵션 인쇄/별색은 N 정당. 공정별 정책. (보류)
