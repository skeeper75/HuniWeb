# 책자(booklet) — 정규화·오염 방지 가드 (normalization-guard)

> **작성** 2026-06-14 · round-13. 사용자 기준 6번 = "정정으로 들어가는 모든 값이 임의 판단 아닌 화이트리스트 안에 있음을 입증".
> **3중 화이트리스트:** ① table-spec 도메인값(`docs/huni/table-spec_260610.html`) ② webadmin sql/ FK·제약 ③ 260610 L1 엑셀 명시값(`24_master-extract-260610/booklet-l1.csv`).
> **목표:** 화이트리스트 밖 값 0 보장. apply.sql이 적재/변경하는 모든 코드값을 3축으로 검증.

---

## 1. 즉시적용분 적재값 화이트리스트 입증 (apply.sql 실행분)

| 적재 컬럼 | 적재값 | ① table-spec 도메인 | ② webadmin FK/제약 | ③ 260610 L1 엑셀 | 안? |
|-----------|--------|---------------------|--------------------|--------------------|:--:|
| `t_prd_product_categories.cat_cd` (068) | CAT_000100 중철책자 | 카테고리 코드 | FK→`t_cat_categories`(실재·upr=CAT_000006·L2) | 상품명=중철책자 | ✅ |
| `cat_cd` (069) | CAT_000101 무선책자 | 카테고리 | FK 실재(upr=006·L2) | 무선책자 | ✅ |
| `cat_cd` (070) | CAT_000102 PUR책자 | 카테고리 | FK 실재(upr=006·L2) | PUR책자 | ✅ |
| `cat_cd` (071) | CAT_000103 트윈링책자 | 카테고리 | FK 실재(upr=006·L2) | 트윈링책자 | ✅ |
| `cat_cd` (077) | CAT_000106 레더하드커버책자 | 카테고리 | FK 실재(upr=CAT_000104·L3) | 레더 하드커버책자 | ✅ |
| `cat_cd` (082) | CAT_000107 하드커버링책자 | 카테고리 | FK 실재(upr=104·L3) | 하드커버 링책자 | ✅ |
| `cat_cd` (088) | CAT_000131 레더링바인더 | 카테고리 | FK 실재(upr=CAT_000130·L3) | 레더 링바인더 | ✅ |
| `main_cat_yn` | 'Y' / 'N' | YN 플래그(Y/N) | NOT NULL·YN 도메인 | — (메타) | ✅ |
| `prd_cd` | PRD_000068~097 | 상품코드 | FK→`t_prd_products` 실재 | L1 prd_nm 매칭 | ✅ |
| `t_prd_product_materials.del_yn` (097/078) | 'Y' | YN 플래그 | YN 도메인·del_dt 동반 | — (논리삭제) | ✅ |
| `mat_cd` 대상 (097) | MAT_000073 백색모조120 | 자재 코드 | FK→`t_mat_materials` 실재 | 내지=백색모조120 | ✅ |
| `mat_cd` 대상 (078) | MAT_000105 몽블랑130g | 자재 코드 | FK 실재 | (반제품 잡음·삭제 대상) | ✅ |
| `reg_dt`/`upd_dt`/`del_dt` | now() | 타임스탬프 | NOT NULL(reg_dt) | — | ✅ |
| `note` | "정정 2026-06-14: …" | 자유 메모(nullable) | nullable text | — (이력) | ✅ |

> **화이트리스트 밖 값 = 0.** 즉시적용분이 쓰는 카테고리 코드(CAT_000100/101/102/103/106/107/131)는 전부 라이브 카테고리 마스터에 이미 실재(2026-06-14 psql 실측: 전용 잎노드 7개 모두 상품 0=고아). 신규 카테고리 코드 mint 0 — search-before-mint 준수. 자재행은 신규 적재가 아닌 **논리삭제**(del_yn)라 코드값 mint 0.

**라이브 실재 입증 SELECT (재현):**
```sql
SELECT cat_cd, cat_nm, COALESCE(upr_cat_cd,'NULL'), cat_lvl,
       (SELECT count(*) FROM t_prd_product_categories pc WHERE pc.cat_cd=cc.cat_cd) cnt
  FROM t_cat_categories cc
 WHERE cat_cd IN ('CAT_000100','CAT_000101','CAT_000102','CAT_000103','CAT_000106','CAT_000107','CAT_000131');
-- → 100 중철책자|006|2|0 · 101 무선책자|006|2|0 · 102 PUR책자|006|2|0 · 103 트윈링책자|006|2|0
--    106 레더하드커버책자|104|3|0 · 107 하드커버링책자|104|3|0 · 131 레더링바인더|130|3|0  (전부 고아)
```

---

## 2. 컨펌대기분 적재 후보값 화이트리스트 (실행 전 사전 검증)

| 적재 컬럼 | 후보값 | ① table-spec | ② webadmin FK | ③ 260610 L1 | 안? | 비고 |
|-----------|--------|--------------|---------------|--------------|:--:|------|
| `t_prd_product_processes.proc_cd` (088 후공정) | PROC_000076 수축포장 | 공정 코드 | FK→`t_proc_processes` 실재 | 088 개별포장 빈값 | 🟡 | **존재 여부 미확정(Q-BK-D)** — L1 빈값이라 적재 근거 약함 |
| `t_prd_product_plate_sizes.output_paper_typ_cd` (책자) | OUTPUT_PAPER_TYPE.* | 출력용지유형 코드 | FK/제약 | L1 폴더(책자/디지털/실사/특수인쇄) | 🟡 | **폴더→코드 매핑 미확정(Q-BK-C)** |
| `t_prd_product_page_rules` (097 제거) | — | — | **del_yn 컬럼 부재** | L1 page 빈값 | ❌ | **논리삭제 불가** — DELETE/NULL화 결정(Q-BK-B) |
| `t_cat_categories.use_yn` (CAT_000297) | 'N' | YN 플래그 | YN 도메인·use_yn 보유 | (상품 아님·잡음) | ✅ | 상품 0이라 안전·디지털 296/295 동형 |

> **컨펌대기 화이트리스트 점검:** BK-5(CAT_000297 use_yn='N')는 코드값·연결 모두 화이트리스트 안(상품 0). BK-6/BK-7은 코드값 자체는 실재하나 **적재 근거(L1)가 빈값**이라 임의 적재 금지(🟡 보류). BK-8은 **적재처(del_yn) 부재**라 ❌ 보류. 어느 것도 억지 삽입 0.

---

## 3. 오염 방지 — 무엇을 하지 않았나 (round-13 교훈 반영)

| 오염 패턴 | 본 정정이 회피한 방식 |
|-----------|----------------------|
| 색/형상/사이즈를 자재로 오적재 (MAT_TYPE.08~10 오염) | 책자는 형상 오염 없음. 레더 자재유형(.08→.06)은 레더 BATCH-4가 이미 정정(077·088 자동 정합) — 본 산출은 재정정 안 함(이중 적용 회피) |
| 별색을 도수로 오적재 | 박칼라 8종=공정(PROC_000037~044) 유지(Q2 ★ 정합)·도수 분리 — 미오염 |
| 신규 카테고리 코드 남발 | 전용 잎노드 100/101/102/103/106/107/131 **재사용**(search-before-mint), 신규 mint 0 |
| 고아·잉여행 hard-delete | 카테고리=논리강등(main_cat_yn='N')·자재=논리삭제(del_yn='Y')·고아노드=use_yn='N' — DELETE 안 함 |
| 반제품에 자재 강제 보존 | 반제품 078 자재는 빈 껍데기가 정답(Q3) → 잡음 자재 논리삭제(완제품 077 자재 보존) |
| 적재처 없는 값 억지 삽입 | page_rule 제거(del_yn 부재)·plate 폴더·088 후공정은 근거/적재처 부족으로 **주석 보류**(컨펌) — NULL 강제 안 함 |

---

## 4. 가드 결론

- **즉시적용분(BK-CAT 7상품 재연결·BK-4·BK-1·BK-2): 화이트리스트 밖 값 0.** 전 코드값 3중 화이트리스트 안 + 라이브 실재 입증 + 멱등 DRY-RUN 통과(2026-06-14 BEGIN…ROLLBACK: INSERT 0 7·UPDATE 4/3/1/1/2·제약 위반 0).
- **컨펌대기분: 화이트리스트/적재처 부족 3건(BK-6 후공정·BK-7 폴더·BK-8 page_rule 제거)을 정직하게 보류** — 억지 삽입 0.
- 임의 판단 0: 모든 적재값이 ① 엑셀 명시(260610 L1) ② 라이브 FK 대상 실재 ③ table-spec 도메인 중 셋을 동시 충족하거나, 못 충족 시 보류.
