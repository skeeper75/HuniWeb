# 문구(stationery) — 라이브 정정 실무진 정리표 (manifest-worksheet)

> **작성** 2026-06-14 · round-13 라이브 정정 트랙 (260610 L1 기준).
> **정답 권위:** 원본 엑셀 L1 최신 `24_master-extract-260610/stationery-l1.csv` (byte동일·유효). v03/load_master 권위 배제.
> **이 표는 비전문가 실무진용** — 코드값은 전부 한글 병기, 영문 분류어("MIS-LOADED" 등) 사용 안 함.
> **라이브 실측 갱신(2026-06-14):** 기존 `17_correctness/stationery` manifest의 일부 값이 stale → 직접 psql 재측정으로 확정. **가장 큰 정정 = 레더(ST-04): round-13은 "실사소재(.08)로 잘못 등록"이라 했으나, 라이브 실측 결과 이미 가죽(.06)으로 고쳐져 있음**(레더 BATCH-4 선적용분 반영) → "고칠 것 없음"으로 격하.
> **디지털인쇄 GO 산출 양식 표준 동일(5종).**

---

## 1. 처리구분 4분류 (정직하게)

| 처리구분 | 뜻 | 본 시트 건수 |
|----------|----|:--:|
| **즉시적용** | 사람 확인 없이 단순 UPDATE/INSERT로 바로 고칠 수 있는 것 | 2 |
| **신규적재** | 연결행을 새로 만들어야 하나, 공정 마스터 신설·세트 모델 결정이 얽혀 설계가 필요한 것 | 4 |
| **컨펌대기** | 사람이 결정해야 하는 것 (질문 명시) | 9 |
| **고칠 것 없음** | 라이브가 이미 정답(정정 불요) | 4 |

> 즉시적용 2 + 신규적재 4 + 컨펌대기 9 + 고칠 것 없음 4 = 19건(라이브 재측정으로 ST-04가 AMBIGUOUS→고칠것없음 이동, ST-13b 가격 항목 분리 보강).

---

## 2. 정리표

| ID | 상품명 | 무엇이 틀렸나 (쉬운 한국어) | 어떻게 고치나 | 영향 범위 | 처리구분 | 근거 |
|----|--------|---------------------------|---------------|-----------|----------|------|
| **W-ST-01** | 만년다이어리 4종 · 먼슬리플래너 | 5개 상품이 가짜 카테고리 "플래너"(어디에도 안 붙은 떠 있는 칸)에 묶여 있음. 진짜 자리인 "문구 > 만년다이어리(커버타입별)/먼슬리플래너"는 이미 만들어져 비어 있는데도 안 씀 | 진짜 문구 자리에 새로 연결 + 가짜 "플래너" 칸 연결은 끔(논리정리). **단 진짜 칸의 이름순≠상품번호순이라 커버타입 의미로 짝지어야 함**(소프트→만년다이어리(소프트커버)칸, 하드→하드커버칸 등) | 상품 5개 카테고리 연결 | **즉시적용**(매칭 컨펌 후) | 엑셀 문구코드(008) + 라이브 진짜 노드 5개 실재 |
| **W-ST-02** | 먼슬리·스프링노트·스프링수첩·메모패드·중철노트 · 떡메모지 | 종이(백모조)가 용도 칸 없이 "공통"으로 들어감. 진짜 용도는 "내지"여야 함 | 종이 용도를 "내지"로 고침. 떡메모지는 "공통"으로 중복 들어간 줄을 끔(논리정리) | 상품 6개 종이 용도 | **즉시적용** | 도메인 정답 내지(USAGE.01) + 라이브 .07 공통 실측 |
| **W-ST-03** | 만년다이어리·노트류 표지 | 표지 종이 이름이 "아트250 + 무광코팅"으로 종이+코팅이 한 칸에 뭉쳐 있음. 코팅은 이미 공정으로 따로 붙어 있어 의미가 겹침 | 종이 이름을 "아트250"만으로 정리(코팅 떼냄). 코팅은 이미 공정에 붙어 있어 중복 제거. 단 가격 영향 확인 동반 | 표지 자재 8상품 | **컨펌대기** | 라이브 평면화 + 코팅 공정 별도 연결 실측 |
| **W-ST-04** | 만년다이어리 레더하드·레더소프트 | (정정) 기존 분석은 "레더가 실사소재로 잘못 등록"이라 했으나, **라이브 실측 결과 이미 가죽으로 고쳐져 있음**(레더 일괄 교정 선적용) | 고칠 것 없음 (이미 정답) | — | **고칠 것 없음** | 라이브 실측 MAT_000186=가죽(.06) |
| **W-ST-05** | 떡메모지 · 메모패드 | 같은 "떡제본"인데 떡메모지는 "선택"·메모패드는 "필수"로 값이 엇갈림(둘 중 하나는 잘못) | 떡제본이 필수인지/선택인지 정한 뒤 둘 다 같은 값으로 통일 | 떡제본 2상품 | **컨펌대기** | 라이브 097=N·179=Y 불일치 실측 |
| **W-ST-06** | 만년다이어리 소프트·레더소프트 · 먼슬리 | "미싱제본"(실로 박는 소프트커버 제본)이 하나도 안 들어감. DB 제본 종류 8가지에 "미싱제본" 자체가 없음 | "미싱제본" 공정을 새로 만들어야 함(기존 미싱류 2개는 제본이 아니라 후가공/접지라 못 씀). 만든 뒤 3상품에 연결 | 상품 3개 제본 + 공정 마스터 신설 | **신규적재** | 엑셀 `*출력+미싱` + 라이브 제본 8종에 미싱제본 부재 |
| **W-ST-07** | 떡메모지 | "페이지 3/3/3" 값이 들어가 있으나 떡메모지는 낱장이라 페이지 개념이 무의미. 진짜 주문 축은 묶음수(50/100장1권) | 페이지 3/3/3을 정리(없애기)할지, 장수3 의미로 둘지 결정 | 떡메모지 1상품 | **컨펌대기** | 라이브 3/3/3 실측 + 묶음수 실재 |
| **W-ST-08** | 만년다이어리 하드·레더하드 | "면지"(하드커버 안쪽 종이)가 안 들어감 | 면지 종이를 연결(화이트/블랙/그레이 면지 줄이 이미 DB에 있어 그걸 씀). 색 3종을 옵션으로 둘지 결정 | 상품 2개 자재 | **신규적재** | 엑셀 `하드커버(면지?)` + 라이브 면지 마스터 실재 |
| **W-ST-09** | 스프링노트 · 스프링수첩 | "실버링"(트윈링 링)이 안 들어감 | 실버링을 부속 자재로 연결(링 실버링 줄이 이미 DB에 있어 그걸 씀)할지, 링 색 옵션으로 둘지 결정 | 상품 2개 자재 | **컨펌대기** | 엑셀 `실버링` + 라이브 링 실버링 마스터 실재 |
| **W-ST-10** | 만년다이어리 소프트 | "PVC커버"(투명 겉커버)가 안 들어감 | PVC커버를 투명커버 자재로 연결할지, 견적 밖 메모로 둘지 결정 (엑셀에 메모 형태로만 있음) | 상품 1개 자재 | **컨펌대기** | 엑셀 메모 `*PVC커버` |
| **W-ST-11** | 스프링류·메모패드 등 | "합지보드"(뒷판) 등 부속 자재가 일부 안 들어감 | 합지보드를 부속 자재로 등록할지 결정 | 상품 다수 자재 | **컨펌대기** | 엑셀 메모 `*합지보드` |
| **W-ST-12** | 만년다이어리 하드·레더하드 | 하드커버는 "표지 하드보드(반제품) + 본체" 세트인데 세트 연결이 안 들어감 | 표지 반제품을 세트로 묶을지, 표지 자재만으로 둘지 결정(책자 하드커버와 같은 방식) | 상품 2개 세트 | **컨펌대기** | 생산구조 B세트 + 라이브 세트 0행 |
| **W-ST-13a** | 만년다이어리 소프트·레더소프트 등 | 일부 내지 종이가 안 들어감(공통풀 처리) | 내지 종이를 명시 연결 검토 | 상품 일부 자재 | **신규적재** | 엑셀 내지 일부 빈값 |
| **W-ST-13b** | 만년다이어리 레더소프트 | 공정이 통째로 0줄(코팅·제본·포장 전무). 레더 특수인쇄+미싱제본이어야 함 | 미싱제본(W-ST-06 신설 후)+레더 특수인쇄 연결 | 상품 1개 공정 | **신규적재** | 라이브 175 공정 0행 |
| **W-ST-14** | 전 문구 상품 | 가격이 하나도 안 들어감. 엑셀에는 고정가(9000/12000/4500…)와 떡메모지 가격표가 있음 | 고정가·떡메모지 매트릭스를 가격 트랙(round-2 양식)으로 적재 | 전 상품 가격 | **컨펌대기**(가격 트랙) | 엑셀 가격 명시 + 라이브 0행 |
| **W-ST-15** | 떡메모지 | 묶음수 "50권"·"100권"이 둘 다 "기본값"으로 표시됨(택1인데 기본이 2개) | 사이즈별로 기본이 갈리면 그대로, 아니면 1개만 기본으로 | 떡메모지 묶음수 | **컨펌대기** | 라이브 둘 다 dflt=Y 실측 |
| **W-ST-16** | 전 문구 상품 | 내지/표지 "폴더"(디지털인쇄/특수인쇄)가 출력용지 칸에 비어 있음 | 출력용지규격으로 적재할지, 생산 라우팅 메모로 둘지 결정 | 전 상품 판형 | **컨펌대기** | 엑셀 폴더 + 라이브 출력용지 NULL |
| **W-ST-17** | 메모패드(내지커스텀) 준비중 | 준비중 상품이라 자재/공정/가격이 비어 있음 | 고칠 것 없음 (준비중 정직 표기·완성 후 재분석) | — | **고칠 것 없음** | 엑셀 노란배경 신규 |
| **W-ST-18** | 떡메모지 → 떡메모지-내지 | 떡메모지가 내지 반제품과 세트로 묶여 있음 | 고칠 것 없음 (정답) | — | **고칠 것 없음** | 라이브 세트(097→098) 정합 |
| **W-ST-19** | 상품 채번·수량·제본·인쇄면 | 상품 12행·수량 min/max·트윈링/중철/떡 제본·단양면 인쇄면은 값이 정답 | 고칠 것 없음 (값 정답) | — | **고칠 것 없음** | 값 정답(엑셀 정합) |

---

## 3. 처리구분 분포

| 처리구분 | 건수 | ID |
|----------|:--:|------|
| **즉시적용** | 2 | W-ST-01(상품 5개 카테고리 재연결·매칭 컨펌 후)·W-ST-02(종이 용도 6상품) |
| **신규적재** | 4 | W-ST-06(미싱제본 신설)·W-ST-08(면지)·W-ST-13a(내지)·W-ST-13b(레더소프트 공정) |
| **컨펌대기** | 9 | W-ST-03·05·07·09·10·11·12·14·15·16 (중 일부 중복 — 아래 컨펌 목록) |
| **고칠 것 없음** | 4 | W-ST-04(레더 이미 정답)·17(준비중)·18(떡메모 세트)·19(값 정답) |

> **즉시적용 2건의 실체:** ① 카테고리 재연결 = 만년다이어리 4 + 먼슬리 = 5상품을 정상 노드(CAT_000119~123)로 연결 + 가짜 고아 300 논리정리. **단 노드 이름순≠prd_cd순(소프트172→121·하드173→122·레더하드174→120·레더소프트175→119·먼슬리176→123)이라 매칭 컨펌 후 실행**(Q-ST-B). ② 종이 용도 .07→.01 UPDATE(6상품) + 떡메모 .07 중복행 논리삭제. **둘 다 화이트리스트 안·라이브 실재 입증.**

---

## 4. 6대 기준 자가 점검표

| # | 사용자 기준 | 본 산출 충족 방식 |
|---|-------------|-------------------|
| 1 | 코드 숨긴 실무진 정리표 | 본 worksheet — 코드값 전부 한글 병기, 영문 분류어 0, 처리구분 한국어 4분류 |
| 2 | 멱등·안전 기술 적재본 | `apply.sql` — BEGIN…COMMIT(인간 승인 후), WHERE 현재값 가드/ON CONFLICT, 재실행 0행 |
| 3 | 롤백 | `rollback.sql` — 적용 전 원문 복원 |
| 4 | 정규화·오염 방지 | `normalization-guard.md` — 3중 화이트리스트(table-spec 도메인·webadmin FK·260610 L1)·밖 값 0 |
| 5 | CPQ option/template/constraint 반영 | `cpq-plan.md` — 커버타입·면지색·링색·떡메모 묶음수를 option_groups/constraints로 설계 |
| 6 | 타임스탬프·변경이력 추적 | `apply.sql` — INSERT=reg_dt=now()·UPDATE=upd_dt=now()·note(있는 테이블)에 "정정 2026-06-14: X→Y" |

---

## 5. 미해소 컨펌 (인간 결정 필요) — 디지털 Q와 일괄 후보 표기

- **Q-ST-A [핵심·신규]** 미싱제본 — 라이브 제본 8종(중철/무선/PUR/트윈링/떡/하드커버무선/하드커버트윈링/레이플랫)에 미싱제본 부재. 기존 미싱류 2개(PROC_000030 미싱=후가공 줄수·PROC_000074 6단미싱접지=접지 자식)는 제본 family 아님. (a)신규 자식 "미싱제본" mint (b)PROC_000030 후가공 재해석 (c)무선/중철 변형 중?
- **Q-ST-B [핵심·즉시적용 선행]** 카테고리 재연결 매칭 — 노드 이름순≠prd_cd순. 커버타입 의미 매칭(172→121·173→122·174→120·175→119·176→123)으로 재연결 + 고아 300 논리정리. (a)의미 매칭 재연결(권장) (b)고아 300 upr만 008로 보수. **디지털 Q-ID-B와 일괄.**
- **Q-ST-C [=Q-BK-C]** 내지/표지 폴더를 출력용지규격으로 적재 vs 생산 라우팅 메타(견적밖)?
- **Q-ST-E [=Q9]** 표지 `아트250 + 무광코팅`을 자재(아트250)+공정(무광)으로 분해? 가격 영향 검증 동반. **횡단 코팅 family 통일.**
- **Q-ST-F** 떡제본이 필수(Y 통일)인가 선택(N 통일)인가?
- **Q-ST-G [=Q-BK-B]** 떡메모지 page 3/3/3 정리 vs 장수3 유지?
- **Q-ST-H** 만년다이어리 하드/레더하드 면지(화이트/블랙/그레이) 연결, 색 3종=옵션 variant?
- **Q-ST-I [=BK-3]** 실버링=링 부속 자재(MAT_000016) vs 트윈링 링색 param?
- **Q-ST-J** PVC커버(메모 힌트)=투명커버 자재 vs 견적밖?
- **Q-ST-K [=ST2-8]** 합지보드/하드보드=부속 자재 등록?
- **Q-ST-L** 만년다이어리 하드/레더하드=B세트(표지 sub_prd+sets) vs parent 자재만?
- **Q-ST-M** 떡메모지 묶음수 50/100 둘 다 기본 — size별 정합이면 유지, 아니면 1개만?
- **Q-ST-N [가격 트랙]** 문구 고정가·떡메모지 매트릭스 가격은 round-2 트랙 — 본 라운드는 경로만 명시.

> **일괄 결정 후보:** Q-ST-C=Q-BK-C·Q-ST-E=Q9·Q-ST-G=Q-BK-B·Q-ST-I=BK-3·Q-ST-B=디지털 Q-ID-B. **문구 고유 핵심**=Q-ST-A(미싱제본 신설)·Q-ST-F(떡제본 mand).
