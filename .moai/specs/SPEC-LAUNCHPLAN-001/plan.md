# SPEC-LAUNCHPLAN-001 — 구현 계획

> 순서 원칙: **되돌리기 어려운 결정을 먼저** 둔다. §A~§C 가 바뀌면 나머지가 전부 다시 쓰인다.
> §F 이후는 기계적 조립이다. 검토자는 §A~§C 에 시간을 쓰면 된다.

---

## A. Tier 판정 — Tier M 유지 (리드 판정 ③ 승인 완료)

### A-1. 초판(0.1.0)의 근거가 틀렸다 — 정정

초판은 「≥3 마일스톤 AND ≥10 파일」을 Tier 기준으로 인용했다. **그것은 Tier 정의가 아니라
`CLAUDE.md` §4 의 `manager-lead` 라우팅 조건이다.** 잘못된 인용이었으므로 철회하고,
SSOT 인 `spec-workflow.md` § SPEC Complexity Tier 의 **LOC × 파일 2축** 표로 교체한다.

### A-2. 2축 실측·추정

| 축 | 값 | Tier 구간 |
|---|---|---|
| 산출 파일 수 | **5~6** — D1 HTML · D2 빌더(.py) · D2 산출(.xlsx) · D3(.mjs) · 중간 상태판 CSV · 리뷰 노트 | L 문턱(파일 > 15) 미달. M 구간(5~15) |
| 산출 LOC 추정 | **약 2,600~3,400줄** — D1 HTML 이 13섹션 + 7트랙 × 최상위 3~5행 + 상세 접힘 + mermaid 3종으로 1,800~2,400줄, D3 가 **14 검사**(§A AC-LP-001~014)로 400~600줄, D2 확장분 200~300줄, 상태판 CSV 40행 | M 구간 |
| 마일스톤 수 | **5** (M0 상태판 · M1 데이터 조립 · M2 HTML · M3 엑셀 · M4 검증) | Tier 축 아님 |

**결론: Tier M 유지.** 두 축 모두 M 구간이다.

### A-3. [HARD] 예산 예외 선언 — 리드 판정 ③ 승인 기록

리드 판정: **Tier M 유지 + 선언된 예외 승인**, 조건 (a) 예외를 이 절에 기록 ·
(b) 재감사가 「검사 강도 유지」를 별도 판정해 PASS. **(b) 는 통과**했다 — 승격 4건 전부
진짜 게이트, 대리 지표 9건 → 2건, 분할 반대 논거 타당 판정. 아래가 (a) 기록이다.

| 항목 | 값 |
|---|---|
| **초과 축** | **REQ 한 축만.** REQ 18 (예산 16 · **+2**) |
| **준수 축** | **AC 16 = 예산 16.** 강도 중립 병합 3건으로 19 → 16 |
| **초과 사유** | 지니 추가 요건 2건이 각각 요구사항 1개를 신설 — REQ-LP-017(판매 준비 프로세스 절) · REQ-LP-018(ego-browser 판정 규율). 둘 다 `[HARD]` 지시이며 기존 조항에 얹으면 과묶음이 된다 |
| **REQ 축을 줄일 수 없는 이유** | 강도 중립 병합 경로가 **없다**. 유일 후보 REQ-LP-015+016 은 016 의 과묶음을 재발시키고, 001+002 는 Ubiquitous 와 Unwanted 를 섞어 GEARS 를 깬다 |
| **병합 거부한 검사 목록** | **0건.** 초판에 있던 거부 3건은 전부 강도 중립으로 재판정되어 수행됐다(구 AC-004→003(b) / 구 016+017→015 / 구 019→007(d)) — 셋 다 같은 파서 패스·같은 실패면을 공유하고 소조건 단위 위반 목록이 진단 해상도를 보존한다 |
| **분할을 택하지 않은 이유** | 쪼개면 두 SPEC 이 같은 HTML 을 공동 집필하고 D3 계약이 두 `acceptance.md` 로 나뉘어 **병합 파일의 소유자가 사라진다**(감사 타당 판정) |

두 축 초과와 한 축 초과는 승인 무게가 다르므로, 예외 범위를 **REQ 한 축**으로 좁혀 둔다.

## B. 되돌리기 어려운 설계 결정 — 여기서 갈린다

### B-1. 산출물 경로와 디렉터리 신설 (run 단계 선행 작업)

| 산출 | 경로 | 비고 |
|---|---|---|
| D1 | `docs/huni/후니프린팅_오픈계획서_260917.html` | 워크트리 상대경로 |
| M0 상태판 | `_workspace/…/S/S5-plan/status-28.csv` | 28구간 통합 상태판 |
| D2 | `_workspace/…/S/S5-plan/build_xlsx_v4_plan.py` + 생성 `.xlsx` | 기존 R5 빌더 확장 |
| D3 | `_workspace/…/S/S5-plan/verify_plan.mjs` | Node + jsdom |
| 리뷰 노트 | `_workspace/…/S/S5-plan/review-notes.md` | AC-LP-016 산출 |

**[HARD] run 단계의 첫 작업은 `_workspace/huni-launch-runway/07_rebaseline/S/S5-plan/`
디렉터리 신설이다.** 워크트리에는 `S/` 자체가 없다(git 미추적). 입력 `S/` 와 이름이 겹치지
않도록 산출은 전부 `S5-plan` 하위에 둔다.

입력은 메인 체크아웃 절대경로로 **읽기 전용** 접근:
`/Users/innojini/Dev/HuniWeb/_workspace/huni-launch-runway/07_rebaseline/{R,S}/…`

### B-2. 문서 골격 — S0 §6 + **선언된 이탈 3건** (초판의 「그대로 채택」은 사실이 아니었다)

초판 §B-2 는 「S0 §6 을 그대로 채택」이라고 적었으나 실제로는 개수만 11로 맞았고 내용이
달랐다. 감사에서 지적된 대로 정정한다 — 이제 REQ-LP-005 가 이탈 3건을 **명시적으로 선언**한다:

| 이탈 | 내용 | 사유 |
|---|---|---|
| ①(추가) | §5 시스템 배치도 — S0 §6 에 없음 | T1 이 첫 마일스톤이면 「무엇이 어디로 옮겨지나」가 그림 없이 안 읽힘 |
| ②(추가) | §6 판매 준비 프로세스 — S0 §6 에 없음 | 지니 확정 260917. 실무진 일상 질문에 답하는 절 |
| ③(밀림) | RACI·의사소통 = ⑫, 부록 = ⑬ | 신설 2건으로 두 칸 밀림 |

**복원 1건**: 초판이 떨어뜨렸던 **의사소통 주기**(S0 가 필수로 판정)를 ⑫ 에 되살렸다.
섹션 개수만 맞추고 필수 요소를 잃는 것이 초판의 실수였다.

대안(트랙을 최상위로 올리고 프로세스를 부록으로 내리는 안)은 **기각** — CTO 가 구간으로
사고한다는 것이 이번 재작성의 근거이고, 트랙은 구간 위에 얹히는 2차 구조다.

### B-3. 척추를 두 층으로 — CTO 10구간 위에 28구간을 접는다

재작성의 근거가 「CTO 가 구간으로 사고한다」인데 척추를 28구간으로 잡으면 **2.8배로 잘게
쪼갠 것**이고, 그 사실이 기록되지 않으면 읽는 사람의 언어가 사라진다. 그래서 상위 = CTO
10구간, 하위 = 28구간으로 매핑한다(REQ-LP-007 · `id="map-cto10"`).

CTO 문서가 5분 안에 읽히는 이유는 구간마다 붙은 **영향 한 문장**이다(「결제는 됐는데 무엇을
만들지 모르는 주문이 됩니다」). 그 문장을 매핑 블록의 필수 칸으로 계약했다.

### B-4. 그림 4종 + 신설 도식 2종의 기술 선택

| 그림 | 방식 | 이유 |
|---|---|---|
| 스윔레인 · 시스템 배치도 · 옵션↔가격 도식 | mermaid | 레인·노드·핸드오프 표현이 손 SVG 보다 유지보수가 싸고 D3 가 파싱으로 검증 가능 |
| 마일스톤 차트 · 임계경로 | HTML/CSS 레일 | 2~4주에 1개꼴 4~6개 — 간트가 아니라 체크포인트. mermaid gantt 는 날짜를 못 박는 인상을 줘 P-1 과 충돌 |
| 메뉴 지도 | HTML 표 | 37행 × 갭 표기 — 그림보다 표가 읽힌다. **이 표가 말하는 깊이는 「메뉴 렌더 확인」까지다** — 「전수 실측」이 아니다 |
| 매뉴얼 요소 대조 — 요약 층 | HTML 표(집계) + 목록 2건 | 화면 단위 집계 · 결과 4종 분포 · **불일치 목록** · **`쓰기경로-dev환경필요` 목록**. 뒤 두 목록은 접지 않는다(고칠 것 / T1 진입 조건) |
| 매뉴얼 요소 대조 — 전건 층 | `<details>` 접힘 부록 + CSV | 수백 행이 본문에 오면 §6 이 행 목록이 되어 반려 원인이 §7 에서 §6 으로 이사한다(AC-LP-016(e) 사람 판정과 충돌). 매트릭스는 전건 그대로 만들되 **싣는 자리**를 나눈다 |

mermaid CDN 1건 + 웹폰트 CDN 1건은 REQ-LP-001 의 허용 예외다. 그 외 외부 참조 0.

### B-5. `data-role` / `data-step` / `data-std` / `data-owner` / `data-work` 속성 계약

체크리스트의 기계 검증은 이 다섯 속성에 전적으로 의존한다. 표 셀 텍스트 파싱 대안은 **기각** —
서식이 섞이면 파서가 문서 디자인에 결합된다.

**소유·분류는 칸이 아니라 속성으로 싣는다**(지니 정정 260917) — 여섯 칸 계약을 깨지 않기
위해서다. `data-owner` ∈ {`nhn`(NHN 제공 — Shopby SaaS·셀러어드민·주문/결제/회원 원장·
Shop/Server API·NCPPay), `huni`(후니 개발 — 스킨·webadmin·위젯·가격엔진·DB),
`ext`(외부 벤더 — 이니시스 PG·토스페이먼츠·MES 벤더·구 사이트 운영사·인프라팀·NHN 계약 창구)} ·
`data-work` ∈ {`config`(설정/확인), `dev`(개발/수정), `wait`(회신·계약·심사 대기)}.
**[HARD] `nhn` × `dev` 금지 · `ext` × `dev` 금지 · `wait` 은 `ext` 전용** — 우리가 고칠 수
있는 대상이 아닌 것을 「개발/수정」으로 적으면 임계경로가 부풀려진다(AC-LP-006(e)). `nhn` 은
**이미 제공되는 것**(설정만 하면 됨) · `ext` 는 **아직 오지 않은 것**(기다려야 함)이고,
`ext` × `wait` 집합은 §8 선행/외부 대기 항목(AC-LP-012(b))과 **같은 집합**이다.

```html
<tr data-role="top" data-step="C4" data-std="STD-MFG-030;STD-MFG-136"
    data-owner="huni" data-work="dev">
  <td>서희항</td><td class="duedate">2026-09-24</td>
  <td class="evidence">raw/webadmin/webadmin/catalog/artwork_promote.py:198</td>
  <td>webadmin 관리자 메뉴에서 「보류 주문」 목록을 열어 행이 보이는지 확인한다</td>
  <td>D-P1</td><td>없음</td>
</tr>

<tr data-role="top" data-step="D5" data-std="STD-PAY-011"
    data-owner="ext" data-work="wait">
  <td>외부(토스페이먼츠)</td><td class="duedate">2026-09-26</td>
  <td class="evidence">https://huniprinting.co.kr/admin/pay@2026-09-17</td>
  <td>계약 회신 메일을 열어 심사 상태 화면에서 승인 여부를 확인한다</td>
  <td>—</td><td>대기</td>
</tr>
```

**행 수 상한이 계약의 핵심이다.** S0 §6-5 는 트랙당 3~5항목이다. 상한이 없으면 트랙 제목을
씌운 300행 표가 모든 기계 검사를 통과하고, 그것은 9/16 에 반려된 물건과 구조적으로 같다.
654행 원장은 `data-std` **다중값**으로 최상위 행에 매단다 — 행을 늘려서 담지 않는다.
세부는 `data-role="detail"` 로 접거나 부록으로 내린다.

### B-6. 목표일 칸 — 바꿔치기 철회

S0 §6-5 의 원문 계약은 「담당·완료 증거·**마감**·상태·블로커」다. 초판은 마감→체크 방법,
블로커→선행으로 바꿔치기하고 그 사실을 적지 않았다. 마감(목표일)을 여섯 번째 칸으로
**복원**한다. P-1 과 충돌하지 않는다 — P-1 이 금지한 것은 **오픈일 판정**이지 작업 항목의
목표일이 아니다. 성공 정의가 「자기 일을 체크하고 일정을 잡는다」인 이상 날짜 칸 없는 표로는
성공 기준을 만족할 수 없다.

### B-7. 「5분 테스트」를 기계 검사로 환원하지 않는다

가장 중요한 성공 기준(SC-1)이 유일하게 자동화 불가다. 대리 지표(섹션 수·글자 수·읽기 시간
추정)로 바꾸려는 유혹이 run 단계에 생길 수 있는데, 그것이 바로 9/16 반려를 만든 사고방식이다.
AC-LP-016 은 **사람 2인의 서면 판정 5문항**으로만 닫힌다 — 그 중 (c)는 「체크 방법 문장대로
실제로 따라 해 보기」이고 (d)는 「이 문서는 10/6 에 된다고 말하고 있나?」이며 (e)는 「옵션을
바꾸면 어느 화면에서 고치나」다.

### B-8. `작동` 은 화면으로만 — ego-browser 판정 규율

[HARD] **「전건 재실측」의 뜻은 「28구간을 모두 한 번씩 본다」가 아니라 「`작동` 으로 적을 행은
전부 화면 근거를 갖는다」다**(spec.md §H 해설 · AC-LP-007(d) 가 검사하는 것도 그것뿐이다).
요소 수준의 1:1 대조는 M1.5 매뉴얼 요소 대조가 별도로 맡는다.

[HARD] 코드·DB 근거만으로 `작동` 을 적지 않는다(REQ-LP-018). 라이브 판정은 ego-browser
space **20** 재사용(p1 신규몰 · p2 webadmin · p3 셀러어드민 · p4 구 사이트), 자기 탭만,
새 space 금지, 읽기 전용 탐색(저장·주문·결제·삭제·발송 버튼 0).

**승계의 한계를 명시한다**: 오늘 입력 조사(S1~S4)는 코드·문서만 본 것이다. 따라서 `작동`
표기는 **9/16 라이브 실측 승계분에 한정**되고, 입력 카드가 `구현-미검증` 으로 적은 행을
화면 확인 없이 승격시킬 수 없다. 미관측은 부재의 증거가 아니지만 **작동의 증거도 아니다**.

## C. 데이터 조립 — 무엇을 어디서 가져오는가

입력 **4카드 전건 도착**(S1·S2·S3·S4) + 골격 권위 S0 + 원장 R2 + 결정 R3 + CTO 문서 =
**8개 원본**. 미해소 `[NEEDS CLARIFICATION]` 마커 없음.

| 문서 섹션 | 출처(읽기 전용 절대경로 기준) |
|---|---|
| §4 스윔레인 · CTO 10구간 매핑 | `docs/huni/후니-주문흐름-장바구니에서-MES까지_서희항_260908.html` |
| §5 배치도 · §7 T1 | `S/S4-infra/migration-plan.md` |
| §6 판매 준비 프로세스 | `huni-product-lifecycle.md` §1·§2 · `huni-pricing-engine-map.md` · `widget-contract-260829.md` · `huni-widget-flow/02_mermaid` · `R/R1b/evidence/menu-map.csv`(37행) · **`S/S5-live/menu-map-260917.csv`(37행 · 오늘자)** · **`S/S5-live/option-price-trace.md`(mermaid 실재 — 도식 실입력)** · `R/R1b/findings.md` §4 · webadmin 매뉴얼 2종 |
| §4 스윔레인 검증 · §7 T3 | **`S/S5-live/mall-flow-260917.md`**(신규몰 p1 실측) |
| §7 T4 | `S/S2-pipeline/pipeline-status.csv`(30행) + `findings.md`(D-P1~D-P9) |
| §7 T3·T6 | `S/S3-migration/migration-status.md` |
| §7 T2·T5 · §2 범위 | `R/R2/unified-ledger-v4.csv`(654행) + `R/R3/decisions-for-pm.md`(30건) |
| §7 T3 스킨측 | `S/S1-skin/api-wiring.csv`(84행 · `page.tsx` 39개 전건) + `findings.md` |
| §13 부록 | `R/R2/unified-ledger-v4.csv` 링크 |
| M0 재실측 | **S5 도착 완료** — `S/S5-live/`(2026-09-17 01:30~01:33 KST · p2/p1 · 쓰기 0). 입력 **5/5 완결** |

### C-1. S1 실측이 바꾸는 것 (T3 구성)

배선 현황 **84행 = wired Y 53 · N 13 · stub 15 · n/a 3**.

- **Shopby 호출 배선이 완료된 것 4건** — B1 상품찾기 · B4 장바구니 · B5 가입/로그인(소셜 포함) ·
  D2 마이페이지 7메뉴. 확정 전제 P-5 가 실측으로 확인됐다. 이 넷은 T3 에서 **검증 대상**으로
  분류한다. 단 「배선 완료」는 **호출 배선**에 한정이며 각 구간에 잔재(N·stub 행)가 남아 있다
  — 잔재는 별도 행으로 유지하고 완료로 흡수하지 않는다.
- **개발 잔여 7항** — 이는 **28행(N 13 + stub 15)의 분류 묶음**이며, 「7항뿐」이 아니다.
  28행 **전건**이 체크리스트(최상위 행 또는 `detail` 행)에 매달린다. 7버킷 밖에 남는 것으로
  최소 4행이 확인됐다 — C7 공정단계 표시 · D2 옵션보관함류 · D4 사업자정보 보관 ·
  D2 `useMe`→`useProfile`.

  | # | 잔여 묶음 | 구간 |
  |---|---|---|
  | ① | `order/register`·`X-Huni-Server-Key`·`handoff/verify` 호출 **0건** | C1(호출측) |
  | ② | 취소·반품교환·1:1문의·Q&A·FAQ 전부 없음 | D3 |
  | ③ | 카드/계좌이체 탭만 렌더·제출 시 차단 — `payType` 이 `"ACCOUNT"` 상수 고정(`use-order-sheet.ts:346-372`) · 현금영수증·세금계산서 폼값 미전송 | B6 |
  | ④ | 증빙이 정적 표 | D4 |
  | ⑤ | 가입 약관동의가 Shopby 로 미전달 — **법정 항목 · 오픈 차단** | B5 |
  | ⑥ | 홈 더미 · 찾기 스텁 · 비회원 완료화면 | A5·B1·B7 |
  | ⑦ | 레거시 mock 레이어(실소비 5 · 죽은 20) + 무인증 프록시 삭제 | E2 |

- **임계경로 교정 (초판 과장 철회)**: 초판은 「24행이 **반일** 재실측으로 **닫힌다**」고 적었다.
  「반일」은 **원천 어디에도 없는 창작 수치**이고, 이 SPEC 이 금지하는 부풀린 수치(P-7)의
  거울상이다. 「닫힌다」도 틀렸다 — `구현-미검증`은 *못 봤다*이지 *보면 된다*가 아니며,
  재실측이 결함을 드러내면 **새 개발 부하가 생긴다**. 「계정 1건**만**」도 두 번째 선행을
  지웠다. 정정 문언:

  > `구현-미검증` 32행 중 **24행은 개발 부하가 아니라 재실측 대기**다. 선행은 둘 —
  > ① 이메일형 테스트 회원 계정 확보 ② 리다이렉트 교정 선후 결정. **재실측 결과에 따라
  > 잔여 개발이 추가될 수 있다.** 소요 추정은 적지 않는다.

- **로그인 리다이렉트 (초판 단정 철회)**: 초판은 「종결」로 적었으나 S4 와 충돌한다. 두 줄로
  분리한다 —
  - **코드 원인 = 확정.** `vercel.app` 문자열은 코드에 없고, 실제 구조는 `proxy.ts:59` 의
    `new URL("/login", nextUrl)` 이 배포 origin 을 잡는 것이다. 교정 = 절대 origin
    (`AUTH_URL`/Host). **T1 체크리스트에 Lightsail 이전 시 재검증 행**으로 넣는다.
  - **Vercel 대시보드 리다이렉트 유무 = 별건·미확인.** S4 §1 은 「미검증」, F4-7 은 「PM 이
    확인해 결정」이 정식 단계이고 Vercel 접근이 미확보다. **F4-7 PM 확인을 유지**한다.

### C-2. S5 실측(2026-09-17 01:30~01:33)이 바꾸는 것

읽기 전용 · 쓰기 0 · p2(webadmin)/p1(신규몰). partA/B/C raw 로그 동반.

- **webadmin 34경로 전부 렌더**, 헤드라인 수치가 9/16 과 **전건 일치** — 판매중 292 ·
  게시 193 / 중단 4 / 위젯 없음 58 · 기본사양가 자동 189 / 수기 1 / 없음 2. 매뉴얼 갭 **3건**
  불변(컴포넌트 16종 표기 · 구성템플릿 조합 명칭 · M-1 템플릿 직접단가 패널 미기술).
- **갭 수는 판독 기준에 따라 갈린다(N-1)** — 원장 CSV 갭 열은 13(실질 텍스트) / 11(비결함 2건
  제외) / 5(★) 로 읽히고, 오늘자는 매뉴얼 갭 3건이다. **어느 판독으로도 단일 상수가 없으므로
  계획서는 수를 적되 판정 기준과 원천을 함께 적는다**(REQ-LP-017 · AC-LP-014(c)).
  「37」도 원천이 사이드바 33 + 비사이드바 4 로 나누므로 전수라 부를 때 내역 한 줄을 단다.
- **옵션↔가격 종단 3상품**: 프리미엄명함 **4,950원 정상** / **아크릴키링 0원**(시뮬레이터·진단
  모두 — 사이즈·고리 **기본값 부재**. 단가행 196/31 은 **존재**) / 무선책자 진단 **23,760**
  (표지 「본품 사이즈 미선택」 제외).
- **`option-price-trace.md` 에 mermaid 연결 도식이 실재** → REQ-LP-017 도식의 실입력이다.
  처음부터 그리지 않고 이것을 승계한다.
- **신규몰 p1**: 상세 2종 위젯 Shadow DOM 렌더 + 가격 표시(7,700 / 44,130) · 담기·바로구매는
  필수 입력(원고·에디터·옵션) 전 비활성 · **카테고리 카드 전건 「10원~」**(기본사양가 미반영
  **추정** — S5 는 「결함인지 미연동 정상 상태인지 **화면만으로 판정 불가**」로 기록) ·
  홈 영문 더미 · 주문서 진입은 「결제하기」 버튼뿐이라 HARD 규칙상 **미클릭**(B6·B7 은
  9/16 승계로 판정).

**T2 체크리스트에 실제 행으로 넣을 것** — 아크릴키링 0원은 **데이터 부재가 아니라 재게시
미실행**이고, 카테고리 카드 「10원~」도 같은 뿌리(기본사양가 미반영)로 보인다. 두 행 모두
근거 = S5 경로 + 실측 일시(`URL@일시`).

## D. 트랙별 구성 방침 (T1~T7)

| 트랙 | 구간 | 문서에서의 형태 | 주의 |
|---|---|---|---|
| T1 인프라 이전 | E2 + F1~F4 | **첫 마일스톤**. 4단계 + 진입조건 9항 + 위험 상위 5 + 로그인 리다이렉트 절대 origin 재검증 | 되돌릴 수 없는 2단계(Railway·Vercel 종료)는 오픈 테스트 통과 전 금지 |
| T2 상품·가격·위젯 | A1~A4·B2·B3 | 실무운영 중심. §6 판매 준비 프로세스와 짝. **실제 행 2건 확정** — ① 아크릴키링 기본값(사이즈·고리) 지정 후 **재게시**(단가행은 존재, 0원은 재게시 미실행) ② 카테고리 카드 「10원~」 = 기본사양가 미반영(같은 뿌리로 추정 — 확정 전 검증 행) | DB 반입 창 동안 가격·상품·위젯 편집 **전면 정지** — T1 과의 충돌을 문서에 드러냄 |
| T3 쇼핑몰 주문·결제·회원 | A5·B1·B4~B7·D2·D3 + 회원 이관 6단계 | 검증 대상 4건 / 개발 잔여 28행(7묶음) / 이관 6단계 | 담당 미확정 표기. ⑤ 약관동의 = 법정·오픈 차단. P-6 이 「구 ASP 미참고」 기록을 덮어씀을 한 줄로 |
| T4 주문 수신·원고·생산 | C1~C7 | **3단**: 설계 결정 주(D-P1~D-P9) → 구현 → 종단 테스트 | 상태는 실측값 그대로(C1·C2·C3수신=구현-미검증 / 나머지=없음). PitStop 은 조달 리드타임이 임계경로 |
| T5 운영 설정·알림·CS·증빙 | D1·D4·E1·E3 | 실무운영 체크리스트 | PG 심사 4요건을 행으로 |
| T6 프린팅머니 | D5 | 이관 6단계 **+ 6단계 밖 외부 대기**(토스 계약 · Shopby 엔터프라이즈 플랜) | 6단계 하위 외부 의존 2건과 6단계 밖 대기를 **계층 분리**. 담당 세 기록 불일치 → `미확정` |
| T7 테스트·리허설·Go/No-Go·컷오버 | E4 | §9~§11 과 짝 | 결정권자 1인 실명 |

## E. 위험과 완화

| # | 위험 | 완화 |
|---|---|---|
| K1 | 재작성이 또 「행 목록」이 된다 | 트랙당 최상위 3~5 상한(REQ-LP-009) + §1·§4·§5·§6 을 체크리스트 앞에 배치 |
| K2 | 기계 검사만 통과하고 사람은 못 읽는다 | AC-LP-016 이 독립 기준. §A·§B PASS 는 필요조건이지 충분조건이 아님을 DoD 에 명시 |
| K3 | `구현-미검증` 24행을 개발 부하로 계상해 임계경로가 부풀려진다 | §C-1 정정 문언대로 「재실측 대기 + 선행 2건 + 결과에 따라 개발 추가 가능」. 소요 추정 금지 |
| K4 | 654행을 본문에 다시 끌어온다 | 부록 링크 1건 + `data-std` 다중값(AC-LP-008) |
| K5 | 「10/6 가능」이 문장으로 새어 들어온다 | AC-LP-002(c) 구조 검사(술어+날짜 동일 문장) + AC-LP-016(d) 사람 판정 |
| K6 | 조판이 트랙으로 승격된다 | AC-LP-002(d) — 출현 1회 상한 |
| K7 | D2 확장이 기존 654행 시트를 깨뜨린다 | AC-LP-015(c) — 654 행수 대조 |
| K8 | 화면 확인 없이 `작동` 으로 승격된다 | AC-LP-007(d) — `작동` 행 전부 `URL@일시` + 일시 ≥ 2026-09-16 |
| K9 | M0 재실측이 라이브에 쓰기를 남긴다 | 읽기 전용 탐색만. space 20 재사용·자기 탭만. DoD §E-5 가 변경 파일 목록으로 확인 |
| K10 | 매뉴얼 요소 대조 수백 행이 §6 본문에 실려 **반려 원인이 §7 에서 §6 으로 이사한다** | 층 분리(본문=요약 층 / 부록=`<details>` 전건 층 + CSV) + AC-LP-016(e) 사람 판정이 「옵션을 바꾸면 어느 화면에서 고치나」 한 문장으로 잡는다 |
| K11 | 층을 나눈 대가로 **요약과 전건이 어긋난다**(본문 12건 · 부록 340행) | AC-LP-014(e)(i) 3항 사슬 검사 — 원고 grep 분모 == 부록/CSV 행 수 == 요약 층 집계 수 |
| K12 | 요소 대조 중 쓰기 버튼을 눌러 라이브를 바꾼다 | 쓰기 경로는 판정하지 않고 `쓰기경로-dev환경필요` 로 둔다(M1.5-2). 결과 값 집합 4종이 그 우회로를 없앤다 |

## F. 마일스톤 (우선순위 순 · 기간 추정 없음)

### M0 — 28구간 통합 상태판 (Priority High · **첫 마일스톤**)

S1~S5 의 delta 가 아직 합쳐지지 않았다. **그 합침 자체가 이 SPEC 의 첫 마일스톤**이고,
체크리스트·마일스톤·임계경로가 **전부 이 상태판에서 파생**된다.

#### M0 선행조건 — 「전건 재실측」은 오늘 완료 가능하지 않다

M0 를 완료 가능한 것처럼 선언하면 안 된다. 세 가지가 전건 재실측을 막는다:

| 차단 | 내용 | 근거 |
|---|---|---|
| 회원 화면 24행 | 이메일형 테스트 계정 미확보 — 현재 아이디형만 있어 **비로그인 진행**, 회원 흐름 미검증 | 미해결 입력 S1-1(차단 Y) · S5 §7 재확인 |
| F1~F4 | 첫 Lightsail 배포 **미실행** — 관측할 화면이 존재하지 않는다 | S4 런북 §0 |
| p3 셀러어드민 · p4 구 사이트 | S5 가 사용하지 않음 — D1·D3·E1·E3 근거 공백 | S5 는 p2/p1 만 |

#### M0 3분할

1. `S/S5-plan/` 디렉터리 신설.
2. **① 즉시 가능(p1 신규몰 · p2 webadmin)** — S5 로 사실상 완료. 아래 4 에서 소비만 한다.
3. **② 계정 확보 후(회원 화면 24행)** — S1-1 해소를 선행으로 걸고, 확보 전에는 상태판에
   `재실측 대기(선행: 테스트 계정)` 로 둔다. **재실측 결과에 따라 잔여 개발이 추가될 수 있다.**
4. **③ 관측 불가 · 설계 판정(F1~F4 · p3 · p4)** — 화면이 없거나 미접근이므로 `없음`/`미착수`
   로 두고 **`작동` 표기를 금지**한다. REQ-LP-018 이 이미 이 경로를 막는다.
5. **S5 실입력 소비** — `S/S5-live/{findings.md, menu-map-260917.csv, option-price-trace.md,
   mall-flow-260917.md, partA/B/C-raw.log}`(2026-09-17 01:30~01:33 KST · p2/p1 · 쓰기 0).
   `menu-map-260917.csv` 37행 → §6 메뉴 지도, `option-price-trace.md`(mermaid 실재) → §6 도식,
   `mall-flow-260917.md` → §4 스윔레인 검증.
6. 산출: `S/S5-plan/status-28.csv` — 열 = `구간 · 시스템 · 담당 · status · 10/6 필수여부 ·
   근거 · 남은 일`. 32행(28 + F1~F4). 각 행에 위 ①②③ 중 어느 분할인지 표기.
7. **`작동` 표기는 화면 관측 근거를 가진 행에만** 준다(REQ-LP-018). B6·B7 은 오늘 근거가
   없고(주문서 진입이 「결제하기」 버튼뿐이라 HARD 규칙상 미클릭), 9/16 실측 승계로 통과한다 —
   승계 조항이 정확히 이 상황을 덮는다.

### M1 — 데이터 조립 (Priority High · M0 의존)

1. M0 상태판을 분모로 삼아, 입력 8원본의 세부 행을 `data-step` × 트랙 × `std_id` 로 정규화한
   중간 CSV 생성(`S/S5-plan/plan-rows.csv`).
2. 트랙당 **최상위 3~5행**을 선정하고 나머지를 `detail` 로 강등. `std_id` 는 최상위 행에
   다중값으로 매단다.
3. 결정 30건 → 구간 귀속 + 「오픈 전 반드시 정해야 하는 것」 추림.
4. 미해결 입력 35건(research.md §4) 중 차단 24건을 §8 외부 대기 항목으로 배치.

### M1.5 — 매뉴얼 요소 대조 (Priority High · M1 의존 · **M2 선행**)

§G 판매 준비 프로세스가 전제하는 실측의 깊이는 **매뉴얼 원고 요소와의 1:1 대조**다(지니 결정
260917 · REQ-LP-017). 메뉴 지도의 「렌더 확인」으로는 그 깊이가 나오지 않으므로 별도 마일스톤을
둔다.

[HARD] **run 레인이 p1·p3 재실측을 마친 뒤 같은 p2 드라이버로 순차 수행한다 — 동시 실행이
아니다.** ego-browser space 20 의 p2 탭 하나를 공유하므로 병렬로 돌리면 서로의 페이지 상태를
덮는다.

1. **분모 산출** — `raw/webadmin/tools/manual_content.py`(1,468행)·`widget_manual_content.py`
   (1,117행)의 `SCREENS` / `MODEL_ADMIN_SCREENS` 를 열거해 **화면별 요소·버튼·패널 전건**을 뽑는다.
   [HARD] 이때 **무엇을 요소 1건으로 세는가**(산출 규칙)를 먼저 문장으로 고정하고, 그것을
   재현하는 **명령 한 줄**을 함께 남긴다 — D1 이 그 규칙과 명령을 선언하고 D3 가 그 명령을
   실행해 분모를 다시 계산한다(AC-LP-014(e)(i)). **분모를 어디에도 상수로 적지 않는다.**
2. **실측** — 읽기 경로(탭·필터·검색·상세·미리보기·진단·다운로드 열기)는 p2 에서 **실제로 눌러**
   판정한다. 쓰기 경로(등록·수정·삭제·게시·저장)는 **라이브에서 누르지 않고**
   `쓰기경로-dev환경필요` 로 둔다.
3. **결과 값은 4종뿐** — `동작확인(URL@일시)` · `불일치(URL@일시+차이)` · `쓰기경로-dev환경필요` ·
   `미실측(사유)`. 그 밖의 값을 만들지 않는다. `미실측` 은 사유 없이 남기지 않는다.
4. **산출**: `_workspace/huni-launch-runway/07_rebaseline/S/S5-plan/manual-element-matrix.csv`
   — 열 = `화면 · 매뉴얼 원문 위치(파일:줄) · 요소 · 경로 종류(읽기/쓰기) · 실측 결과`.
5. **집계** — 화면 단위 요소 수와 결과 4종 분포, 그리고 **불일치 행 목록**·
   **`쓰기경로-dev환경필요` 행 목록**을 따로 뽑는다. 이 둘이 M2 에서 §6 본문 요약 층에 들어간다.
6. **T1 연결** — `쓰기경로-dev환경필요` 집합은 dev 환경이 서기 전에는 판정 불가라는 뜻이므로,
   T1 오픈 테스트 진입 조건과의 연결을 한 줄로 적는다(M2 §6 · T1 블록 양쪽).

### M2 — D1 HTML 작성 (Priority High)

1. 13섹션 스캐폴드 + 앵커 `sec-01`~`sec-13`.
2. §1 요약(6요소·700자) · §2 범위 두 표 · §3 마일스톤 4~6 · §4 스윔레인 + CTO 10구간 매핑 ·
   §5 배치도(노드 ≤12) · §6 판매 준비 프로세스(메뉴 지도 37 + 옵션↔가격 도식 + 매뉴얼 요소 대조).
   - **§6 매뉴얼 요소 대조는 두 층으로 싣는다** — 본문 `id="map-manual-elements"` 에 **요약 층**
     (화면 단위 집계 · 결과 4종 분포 · **불일치 목록** · **`쓰기경로-dev환경필요` 목록** +
     T1 진입 조건 연결 문장), 부록 `id="manual-element-matrix"` 에 `<details>` 접힘 **전건 층**
     (+ CSV 경로 명시). 본문 요약 층의 집계 수는 M1.5 산출 CSV 에서 **계산해서** 넣는다 —
     손으로 적으면 AC-LP-014(e)(i) 사슬이 깨진다.
   - 메뉴 지도 옆 문언은 「전수 실측」이 아니라 **「메뉴 렌더 확인 + 요소 대조 결과」**로 쓴다.
   - **§6 블록 순서 = 메뉴 지도 → 옵션↔가격 도식 → 매뉴얼 요소 대조 요약 층**. 도식이
     매트릭스보다 **앞**이다 — 도식이 AC-LP-016(e)(「옵션을 바꾸면 어느 화면에서 고치나」)를
     직접 떠받치는 블록이라 뒤로 밀리면 사람 판정이 먼저 깨진다(§H R-3).
3. §7 트랙 7블록 + 역할별 진입점 5블록 — M1 의 중간 CSV 를 그대로 렌더(손으로 다시 적지 않는다).
4. §8 임계경로 · §9 Go/No-Go · §10 컷오버 · §11 하이퍼케어 · §12 RACI·의사소통 · §13 부록.

### M3 — D2 엑셀 재생성 (Priority Medium)

1. `R/R5/build_xlsx_v4.py` 를 `S/S5-plan/build_xlsx_v4_plan.py` 로 확장 복제.
2. 기존 654행 시트 보존 + 상태판·트랙·체크리스트 시트 추가(D1 행 수와 일치).

### M4 — D3 검증 + 판정 (Priority Medium)

1. `verify_plan.mjs` 작성 — acceptance §A 의 **AC-LP-001~014** 구현, 종료 코드 이진.
2. 음성 대조 실행(다크 규칙 주입 복사본 → 종료 코드 1)으로 스크립트 자체 검증(AC-LP-015(d)).
2-1. **AC-LP-014(e)(i) 사슬 검사 — 명령은 D3 의 4번째 입력 인자로 받는다.** [HARD] D3 는
   **문서가 적어 준 문자열을 실행하지 않는다**. run 레인이 분모 산출 명령을 **인자로** 넘기고,
   D3 는 ① 「D1 이 선언한 명령 == 인자로 받은 명령」을 **문자열 비교**해 일치를 확인한 뒤
   ② **자기가** 그 명령을 실행해 분모를 계산하고 ③ 부록/CSV 행 수 · 요약 층 집계 수와 세 값이
   같은지 대조한다. 실행 주체가 **피검사 문서가 아니라 호출자**다 — 검사 대상이 검사 방법을
   정하면 D3 는 자기정합만 확인하게 되고(감사 독립성 붕괴), 문서 문자열을 그대로 실행하면
   검증 스크립트에 **코드 주입면**이 열린다. **분모를 스크립트에 하드코딩하지도 않는다**
   (하드코딩하는 순간 이 검사가 상수 검사로 퇴화한다).
3. D2 빌더 실행 + 654 행수 대조(AC-LP-015(c)).
4. §A·§B 전부 PASS 후 AC-LP-016 사람 판정 2인 × **5문항** → `review-notes.md`.

## G. 피해야 할 패턴

- 체크리스트를 표가 아닌 산문으로 쓰기 — 여섯 칸 계약이 깨진다.
- 트랙 최상위 행을 6개 이상 만들기 — 상한이 있는 이유가 반려 재현 방지다.
- `data-role`/`data-step`/`data-std` 를 생략하고 「나중에 붙이기」 — D3 가 전부 FAIL 한다.
- 기계 검사 통과를 5분 테스트 통과로 보고하기 — verification-claim-integrity 위반.
- 코드·DB 근거만으로 `작동` 적기 — 미관측은 작동의 증거가 아니다.
- 원천에 없는 소요 추정치 만들어 쓰기 — 「반일」 같은 창작 수치가 P-7 의 거울상이다.
- 결정 안건에 「기본값(제안)」을 결정으로 승격하기 — 제안은 제안으로 표기한다.

## H. 잔존 기술부채 (기록 본체는 `progress.md` — 여기서는 하중 변화만)

- **R-3**(§6 블록 순서 계약 없음) — **하중이 커졌다**. §6 이 2블록에서 **3블록**이 되면서
  매뉴얼 요소 대조가 앞에 오면 옵션↔가격 도식이 뒤로 밀릴 위험이 생겼다. 도식은 이 절의 존재
  이유(AC-LP-016(e))를 직접 떠받치는 블록이라, 밀리면 사람 판정이 먼저 깨진다. 여전히 MINOR 로
  두되(감사 회차 소진 — 감사받지 않은 계약 변경을 더 얹지 않는다), M2 에서 **도식을 매트릭스
  앞에 배치**하는 것으로 운영상 회피한다.
- **R-2**(선언 갭 수 == 표기 갭 수) · **m-8** — 변화 없음.

## I. 상호 참조

- `spec.md` §2 확정 전제 · §3-0 예산 초과 선언 · §3 요구사항 18항 · §4 Out of Scope
- `acceptance.md` §A D3 판정 · §B 별도 판정 · §C 사람 판정 · §E DoD
- `research.md` — 입력 실측 요약과 미해결 입력 통합표 35건
