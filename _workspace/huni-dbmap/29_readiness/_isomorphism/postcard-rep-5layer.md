# 엽서 대표 5레이어 종단 + 이진 게이트 (S0~S5)

> 작성 2026-06-15 · round-21 1차 사이클 · `dbm-readiness-auditor`(게이트 전담, DB 읽기전용).
> 대상 = **대표 프리미엄엽서(PRD_000016) 주축 + 코팅엽서(PRD_000017) 코팅 보완 + 스탠다드엽서(PRD_000018) 부분집합 참조**.
> 5레이어 = 토대 §6-2(① 구성요소 BOM · ② 옵션 CPQ · ③ 템플릿 · ④ 제약 · ⑤ 가격). 게이트 = 견적가능 Q1~Q6 + webadmin 가시성 + 이진 GO/NO-GO.
> 모든 판정 = 본 세션 라이브 읽기전용 실측 인용. "보인다" 금지. 보정 하드코딩 0([HARD]).

---

## 0. 결론 (이진 게이트 요약)

| 게이트 | 판정 | 한 줄 근거 |
|--------|:--:|----------|
| Q1 컬럼 커버리지 | **GO** | 44 의미컬럼 빈칸 0(round-19 column-readiness §1 재활용·라이브 재확인) |
| Q2 기초 완전성 | **GO** | siz/mat/proc/plate/print_opt 016=7/21/6/1/2 전부 행 존재 |
| Q3 ①UI+②차원환원 | **GO** | 016 4그룹/29옵션/29아이템·ref_dim 미해소 0/29(.03·.04·.06 전수 해소) |
| Q4 ③가격사슬 | **CONDITIONAL-GO** | PRF_DGP_A 29 comp·사슬 단절 0. **단 D-1b: 후가공 14 comp prc_typ=.01 오적재** |
| Q5 견적 시뮬(다양 조합) | **GO(기본·도수·코팅) / NO-GO(후가공)** | **[보정]** 출력매수 변환 적용 재산출(C1=14,679·C2=37,844·C5=30,612·C6=55,879). excel↔live 교차 일치(다른 출처). 후가공=D-1b로 계산불가 분리 |
| Q6 정직 갭 | **GO** | 미달 전부 차단유형+라우팅 분류(§5). **1차 순환 골든·출력매수 누락 결함 정직 정정** |
| webadmin 가시성(§9-4) | **GO** | option_groups opt_grp_nm=한국어(인쇄/종이/모서리/후가공)·note=쉬운 한국어 라벨 실재 |

### 대표 종단 = **GO (조건부)** — 기본·도수·코팅 견적가능, 최종가 골든 부재 + 후가공 D-1b 잔존

- **[보정 closeout]** 1차 산출은 V3 REFUTED(순환 골든·출력매수 변환 누락)였다. 보정 후: 출력매수 변환(`ceil(주문수량/판걸이수)`, 판걸이수 100×150=8) 명시 적용 → 인쇄·용지비가 excel 단가표 ↔ live 단가행 **교차 일치**(같은 출처 자기참조 아님). 기본·도수·코팅 견적 = GO.
- **거짓 GO 방지(보정판)**: ① 후가공은 D-1b로 "계산불가" 분리(임의값 금지). ② 디지털인쇄비형은 가격표에 **최종가 known값이 없음** → 최종가 골든 부재로 정직 분리(인간 승인 큐). 검증은 출력매수 공식 정합 + excel↔live 교차 일치로 수행.
- 판정 = **"기본·도수·코팅 옵션 견적가능 GO(출력매수 변환 적용·교차검증) + 최종가 골든 부재(분리) + 후가공 D-1b 1정정 잔존"**.

---

## 1. 5레이어 라이브 readiness 매트릭스 (실측 2026-06-15)

> ✅적재완료·정합 / 🟡부분(일부 상품·param 부재·메타 오적재) / ❌미적재·그릇부재. 빈칸 0.

### ① 구성요소 BOM (t_mat_*·t_proc_*·t_prd_products)

| 요소 | 016 | 017 | 018 | 판정 | 증거(라이브) |
|------|:--:|:--:|:--:|:--:|------|
| products 정체 | 프리미엄엽서 PRD_TYPE.01 | 코팅엽서 .04 | 스탠다드엽서 .04 | ✅ | `t_prd_products` min/max/incr=15/10000/15·QTY_UNIT.02(건수)·upload Y·editor N |
| 카테고리 | CAT_000001 | 〃 | 〃 | ✅ | `t_prd_product_categories` 엽서 귀속 |
| siz(사이즈) | 7 | 7 | 5 | ✅ | `t_prd_product_sizes` |
| mat(종이·USAGE.07) | 21 | 2 | 7 | ✅ | `t_prd_product_materials` |
| proc(공정) | 6 | 4 | 6 | ✅ | `t_prd_product_processes`(016=모서리2+오시/미싱/가변4 / 017=코팅2+모서리2) |
| plate(출력판형) | 1 SIZ_000499 | 1 | 1 | ✅ | `t_prd_product_plate_sizes` OUTPUT_PAPER_TYPE.01·국4절 316×467 |
| print_opt(도수) | 2 | 2 | 2 | ✅ | `t_prd_product_print_options`(단면/양면) |

> **레이어① = ✅** 전 요소 행 존재. 016 plate `dflt_plt_yn=N`(017/018=Y)·output_file_typ NULL은 경미(단일 plate라 매칭 무영향·§5 H-3).

### ② 옵션 CPQ (t_prd_product_option_groups/options/option_items)

| 요소 | 016 | 017 | 018 | 판정 |
|------|:--:|:--:|:--:|:--:|
| option_groups | 4 | 4 | 4 | ✅ |
| options | 29 | 9 | 15 | ✅ |
| option_items(ref_dim) | .03=21·.04=6·.06=2 (29) | .03=2·.04=4·.06=2 (8) | .03=7·.04=6·.06=2 (15) | ✅ |
| **②차원환원 미해소** | **0** | (017 동형) | (018 동형) | ✅ |

미해소 검사(트리거 `fn_chk_opt_item_ref` predicate 그대로·016 실측):
- `.03`(종이): `NOT EXISTS(mat_cd AND usage_cd 동시매칭)` → **0**
- `.04`(공정): `NOT EXISTS(proc_cd 매칭)` → **0**
- `.06`(도수): `NOT EXISTS(opt_id::int 매칭)` → **0**

> **레이어② = ✅** 손님 선택(도수·종이·모서리·후가공)이 자재(mat_cd+usage_cd)·공정(proc_cd)·도수(opt_id)로 100% BOM 환원 = ②생산정보 전달 성립. MES_ITEM_CD 의존 아님(설계 제1원칙).

### ③ 템플릿 (t_prd_templates·t_prd_product_addons)

| 요소 | 016 | 017 | 018 | 판정 |
|------|:--:|:--:|:--:|:--:|
| addon(봉투 SKU) | 5 | 0 | 0 | 🟡 |

016 addon 5 = TMPL-000005(OPP접착봉투 110x160 50장)·006(OPP비접착)·009(트레싱지 160x110 20장)·010(카드봉투화이트 165x115 50장)·011(카드봉투블랙). `t_prd_templates` 정체 명칭 실재(staff-recognizable).

> **레이어③ = 🟡** 016만 봉투 세트 적재. 017/018 미적재(round-6 라우팅). 봉투는 본체 견적에 선택적 추가상품 → 본체 견적가능엔 무영향(견적 본체 GO 불변).

### ④ 제약조건 (t_prd_product_constraints)

| 016 | 017 | 018 | 판정 |
|:--:|:--:|:--:|:--:|
| 0 | 0 | 0 | ➖ |

> **레이어④ = ➖(정당 불요)** PRF_DGP_A는 후가공이 독립 택N(SEL_TYPE.02)·모서리 택1이라 잘못된 조합을 차단할 하드 캐스케이드 제약 불요. constraint 0이 정당(NO-GO 사유 아님·은폐 아님).

### ⑤ 가격 (t_prc_* 4단)

| 단 | 016/017/018 | 판정 | 증거 |
|----|:--:|:--:|------|
| 1 바인딩 | PRF_DGP_A(apply_bgn 2026-06-01) | ✅ | `t_prd_product_price_formulas` 3상품 동일 공식 |
| 2 배선 formula_components | 29 comp | ✅ | `t_prc_formula_components` frm_cd=PRF_DGP_A |
| 3 단가행 component_prices | 사슬 단절 0 | ✅ | 29 comp 전부 component_prices 보유(broken=0) |
| 4 prc_typ 메타 | **29 comp 전부 PRICE_TYPE.01** | 🟡(D-1b) | 후가공 14 comp는 구간고정총액형이어야(가격표 권위) |

> **레이어⑤ = 🟡(D-1b)** 사슬 자체는 완결(단절 0). 후가공 14 comp(COMP_PP_*) prc_typ 메타만 오적재 → 후가공 포함 견적 과대.

---

## 2. Q4·Q5 가격 게이트 상세 — 출력매수 변환 적용 재산출 (보정·V3 FAIL 반영)

> **[보정 이력] 1차 산출(C1=44,330·C2=141,650)은 V3 REFUTED(F-1 BLOCKER)였다 — 두 결함:**
> 1. **순환 골든**: 골든값이 round-18 손계산(라이브行×주문수량)을 운반 = 검증값·골든이 같은 출처(자기참조). "보정0 일치"는 허위였다.
> 2. **출력매수 변환 누락**: 계산공식집초안 B5 `출력매수 = ceil(주문수량 / 판걸이수)`, B6 `인쇄비 = [수량행단가]×출력매수`. component_prices min_qty 축 = **출력매수**(라이브 note "출력매수≥N" 실측 확정). 1차는 주문수량을 단가행에 직결 → 인쇄비 약 3배 과대.
>
> 본 §2는 출력매수 변환을 명시 적용하고, 골든을 **가격표 엑셀 component 단가표에서 독립 추적**한 값으로 재산출한다(라이브 단가행과 다른 출처). 보정 하드코딩 0.

### 2-1. 출력매수 변환 — 권위·실측

| 항목 | 값 | 출처(독립) |
|------|------|----------|
| 공식 | `출력매수 = ceil(주문수량 / 판걸이수)` · `인쇄비 = [출력매수 수량행단가] × 출력매수` · `용지비 = [기준단가] × (출력매수 + 5장 손지율)` | 계산공식집초안 R5·R6·R9 |
| 판걸이수(100×150mm 엽서) | **8** | 가격표 `판걸이수` 시트 R4 실측(73×98=18·98×98=12·135×135=6·110×170=4·148×210=4) |
| min_qty 축 의미 | **출력매수**(주문수량 아님) | 라이브 note "디지털인쇄 출력비(국4절)/칼라(CMYK)/단면 **출력매수≥N**" + 가격표 `디지털인쇄비` 행축 "수량(국4절)" 3중 |

### 2-2. 독립 골든 (가격표 엑셀 직접 인용)

- **인쇄단가** = 가격표 `디지털인쇄비` 시트 칼라(CMYK)·단면 열 [출력매수→단가] (R4~R51 직접 인용): 출력매수 10→1000·35→700·60→500·100→350 (라이브 COMP_PRINT_DIGITAL_S1 siz499 CLR_000005 단가와 **교차 일치**·다른 출처).
- **용지단가** = 가격표 `출력소재(IMPORT)` 시트 아트지300g `가격(국4절)` = **93.3** (R13 직접 인용·라이브 COMP_PAPER 93.30과 교차 일치).
- **최종가 골든 부재**: 디지털인쇄비형(원자합산형 build-up)은 가격표에 **component 단가표만** 있고 "주문 N매 엽서 최종가" known값이 **없다**. → 최종가 골든은 **부재로 정직 분리**(인간 승인 큐 "권위/골든값 부재"·§5). 검증은 (a) 출력매수 변환 공식 정합 + (b) excel 단가표 ↔ live 단가행 교차 일치(다른 출처)로 수행.

### 2-3. 견적 시뮬레이션 재산출 (출력매수 변환 적용)

> 인쇄단가·용지단가는 excel(`디지털인쇄비`·`출력소재`)에서 룩업 = 골든. live 단가행은 교차검증용(별 출처).

| 케이스 | 조합 | 출력매수 | 인쇄비(excel) | 용지비(excel) | 합계 | live 교차 | 판정 |
|--------|------|:--:|------|------|:--:|:--:|:--:|
| **C1** | 016·100×150·단면4도·아트지300·무코팅·직각·100매 | ceil(100/8)=**13** → 구간≤13=10행 단가1000 | 1000×13=13,000 | 93.3×(13+5)=1,679 | **14,679** | live 1000(≥10) 일치 | ✅ |
| **C2** | C1·500매 | ceil(500/8)=**63** → 구간≤63=60행 단가500 | 500×63=31,500 | 93.3×(63+5)=6,344 | **37,844** | live 500(≥60) 일치 | ✅ |
| **C5** | C1·300매 | ceil(300/8)=**38** → 구간≤38=35행 단가700 | 700×38=26,600 | 93.3×(38+5)=4,012 | **30,612** | live 700(≥35) 일치 | ✅ |
| **C6** | C1·1000매 | ceil(1000/8)=**125** → 구간≤125=100행 단가350 | 350×125=43,750 | 93.3×(125+5)=12,129 | **55,879** | live 350(≥100) 일치 | ✅ |
| **C-DUP(양면)** | 016·양면4도·100매 | 13 | 단면13,000 + 뒷면 S2 단가(출력매수 13행)×13 | +용지 | (도수축 가산 성립) | S2 단가행 실재(출력매수축) | ✅(구조) |
| **C-COAT** | 017·무광코팅 단면·100매·아트250 | 13 | +코팅 COMP_COAT_MATTE 출력매수13행 단가×13 | — | (코팅축 가산 성립) | 코팅 단가행 출력매수축 실재 | ✅(구조) |
| **C3(후가공)** | C1 + 오시1줄 100매 | — | — | — | **계산불가(D-1b)** | — | ❌ |
| **C4(off-grid 250)** | C1 + 오시1줄 250매 | — | — | — | **계산불가(D-1b)** | — | ❌ |

> **다양 조합 커버:** 수량구간 4종(C1 100·C5 300·C2 500·C6 1000)으로 출력매수 변환·구간 룩업·손지율을 교차 입증 + 도수(C-DUP)·코팅(C-COAT) 구조 정합. 인쇄·용지비 **출력매수 변환 적용** 후 excel↔live **교차 일치**(같은 출처 자기참조 아님).
> **후가공(C3·C4)은 D-1b 결함이라 "계산불가"로 분리** — 임의 0원·임의 값 금지(보정 하드코딩 금지 [HARD]). D-1b 정정 전까지 후가공 포함 견적은 미증명.

### 2-4. D-1b 사실 (라이브 실측·1차와 동일·재현됨)

후가공 14 comp(COMP_PP_CORNER_RIGHT/ROUND·CREASE/PERF/VARTEXT/VARIMG) 전부 `PRICE_TYPE.01`. 단조성 실측(COMP_PP_PERF_1L): `1=5000·100=10000·300=15000·1000=25000·5000=105000` = 수량↑ 시 비감소 = 구간고정총액형 시그니처. 가격표 `인쇄후가공` "합가" 명시와 정합 → 메타 오적재 확정(값 정확). CORNER_RIGHT(0원) 제외 → 정정대상 13.

---

## 3. 게이트별 이진 판정 + 증거

| 게이트 | GO/NO-GO | 증거(라이브 수치) |
|--------|:--:|------|
| **Q1** | GO | 44 의미컬럼 ✅34/🟡7/❌0(round-19 §1, 라이브 재확인 BOM·CPQ·가격 일치) |
| **Q2** | GO | 016 siz7·mat21·proc6·plate1·print_opt2 전부 행 존재 |
| **Q3** | GO | 016 4그룹/29옵션/29아이템·미해소 0/29(.03·.04·.06 전수) |
| **Q4** | CONDITIONAL-GO | 29 comp·broken 0. 후가공 14 comp prc_typ=.01 오적재(D-1b) |
| **Q5** | GO(기본/도수/코팅) · NO-GO(후가공) · 최종가골든 부재(분리) | **[보정]** 출력매수 변환 적용: C1=14,679·C5=30,612·C2=37,844·C6=55,879(excel↔live 교차 일치) / 후가공 D-1b 계산불가 / 최종가 known값 가격표 부재 |
| **Q6** | GO | §5 차단 전부 분류. **1차 순환 골든·출력매수 변환 누락 결함 정직 정정**(V3 FAIL 반영)·fan-out 허수(132/61) 제거 진실 52 item |
| **webadmin 가시성** | GO | option_groups opt_grp_nm 한국어(인쇄/종이/모서리/후가공)·note 쉬운 한국어("종이(자재) 택1 필수…21종 라이브 materials") 실재 = §9-1 3축 충족 |

> webadmin 가시성 = 데이터 측 실측(option_groups note·nm)으로 입증. 라이브 admin product-viewer 자격증명(`.env.local HUNI_ADMIN_*`) 보유·round-19 admin-manual 재대조에서 0 드리프트 확인됨(브라우저 탐색 생략·데이터 측 충분). 코드만 뜨고 명칭 없는 §9-1 위반 = 없음.

---

## 4. NO-GO 항목 결함 라우팅

| ID | 결함 | 게이트 | 라우팅 | 닫히는 것 |
|----|------|:--:|------|----------|
| **D-1b** | 후가공 13 comp(CORNER_RIGHT 제외) prc_typ=.01 → 구간고정총액형(PRICE_TYPE.03 신설)이어야 | Q4/Q5 | **round-13 정정(prc_typ UPDATE) + webadmin Phase11(엔진 .03 분기)** | 후가공 포함 견적 → 완전 GO. 엽서+상품권 동시(PRF_DGP_A 공유) |
| **GAP-PARAM** | 후가공 줄수/개수(오시/미싱 1~3줄·가변 1~3개) ref_param_json 부재 | Q4 🟡 | **dbm-ddl-proposer**(ALTER ref_param_json jsonb) | C31~34 🟡→✅·줄수별 단가 분기 |
| **C38-ENV** | 봉투 addon 017/018 미적재(016만 5종) | ③🟡 | **round-6 dbm-option-mapper**(016 패턴 복제) | 017/018 봉투 추가상품(본체 무영향) |
| **C39-ENVPRICE** | 봉투 추가가격 보관처 미정 | ③🟡 | **round-16/ddl-proposer** | 봉투가 견적 반영 |
| **016-DFLT** | 016 plate dflt_plt_yn=N(타 2상품 Y) | ①경미 | (보류·Low) | 일관성(단일 plate라 매칭 무영향) |
| **C11** | plate output_file_typ NULL(3상품) | ①경미 | (보류·Low) | 생산메타(견적 무관) |

> ✅ 요소(BOM·CPQ·기본 가격사슬·코팅)는 이미 적재 → S2~S4 적재 스킵(중복 적재 금지). 라우팅은 D-1b·GAP-PARAM·봉투만.

---

## 5. 폐루프 (RESOLVED 판정 기준)

> **후가공 축 확정(라이브 note 실측):** COMP_PP_CREASE_1L note = "오시/1줄 **제작수량≥N** (합가)" → 후가공 단가행 축 = **제작수량(주문수량)**, 인쇄/용지의 출력매수 축과 다르다(계산공식집초안 R10 `후가공비=[제작수량행]`과 정합). 즉 후가공은 주문수량 구간으로 룩업한 총액(합가)이고 ×주문수량을 하면 안 된다(= D-1b 결함의 본질). **인쇄·용지=출력매수축 / 후가공=제작수량축**으로 축이 갈린다(혼동 금지).

D-1b 정정(ⓒ-2 PRICE_TYPE.03 신설 + 엔진 .03 해석) 후:
- [ ] C3 재시뮬(016·오시1줄·100매): 후가공 = 제작수량≥100 구간 총액 10,000(1회·×수량 안 함). 현 `10,000×100=1,000,000` → `10,000`. 총액 = base 14,679(출력매수 변환) + 후가공 10,000(제작수량축 총액) = **24,679**.
- [ ] C4(250매·off-grid): 후가공 ⓒ=구간고정총액 ≠ 명세식 ÷mq×qty → ⓒ만 단조 정합.
- [ ] 인쇄·용지비 = 출력매수 변환 적용·excel↔live 교차 일치([HARD] 보정 하드코딩 0).
- 충족 시 D-1b 그룹① RESOLVED → 엽서 = **완전 견적가능 GO**(단 최종가 골든 부재는 잔존·인간 승인 큐).

> 현재 = **기본·도수·코팅 옵션 견적가능 GO(출력매수 변환 적용·교차검증) + 최종가 골든 부재(분리) + 후가공 D-1b 1정정 잔존**. 실 정정/COMMIT = 인간 승인(엔진 evaluate_price 미구현이라 실청구 위험 0·안전 큐 적재).
