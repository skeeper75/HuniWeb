# P1 진행 — 미판정 143 판정 · done_criteria 504 · 오픈차단 등급 · 가능성 v2

> 2026-09-02 · lane `run-huniweb` · 카드 `L0/CARDS-P.md §P1`
> 워크트리 미사용 · `L5/P1/` 안에만 씀 · **커밋·푸시 0** (`git status` 로 확인 — 기존 추적 파일 수정 0)

---

## 완료조건 대조표

| # | 완료조건 | 결과 | 확인 방법 (재현 가능) |
|---|---|---|---|
| ① | 미판정 잔여 건수 명시 · 0 이 아니면 전건 사유 | ✅ **9건 · 전건 사유 있음** | `python3 L5/P1/build_v2.py` → `미판정 9` · `judged-143.md §3` 표 9행 · 기계 검사 「전건 사유 보유 True」 |
| ② | `done_criteria` 빈칸 0 · 금지어 0 (기계) | ✅ **빈칸 0 · 금지어 0** | 위 스크립트 검증부. 금지어 = 검토한다/정리한다/준비한다/보완한다/정상 동작/완료한다 |
| ③ | `done` 전건 `file:line` | ✅ **done 99 / file:line 없음 0** | 정규식 `[\w./-]+\.(py\|ts\|tsx\|js\|json\|sql):\d+` 로 `근거` 열 전수 |
| ④ | `오픈차단여부` 빈칸 0 | ✅ **빈칸 0 · 허용값 외 0** | 값 4종만: 차단(외부) 8 · 작업 항목 437 · 오픈 무관 50 · 미판정 9 |
| ⑤ | 가능성 v2 에 MID 신청 마감일(영업일 역산) 명시 | ✅ **9/07(월)** | `feasibility-verdict-v2.md §2-2` 역산표(2가정 × 4경로) · §2-3 한 줄 |
| ⑥ | 역방향 — O2 판정 중 뒤집은 것 목록 | ✅ **4건** | `feasibility-verdict-v2.md §5` 표 + 「뒤집지 않고 확인한 것」 4건 |

**6/6 통과.**

---

## 산출물

| 파일 | 내용 |
|---|---|
| `unified-ledger-v2.csv` · `.json` | 504행 · `상태`·`done_criteria` 채움 + **`오픈차단여부` 열 신설**(`상태` 바로 뒤) |
| `judged-143.md` | 143행 행별 판정 근거표 · 수동 대체 50건 · 미판정 9건 사유 |
| `feasibility-verdict-v2.md` | 게이트 판정 v2 · MID 역산 · 뒤집은 것 4건 · 틀릴 수 있는 곳 5건 |
| `load-final.md` | 담당×상태 재집계 · 오픈차단여부 교차 · lead 「함정」 재판정 |
| `build_v2.py` · `judgments.py` · `gen_judged.py` | 재실행 생성기 (`python3 L5/P1/build_v2.py` → 검증까지 한 번에) |

**L6 를 위해**: `build_v2.py` 는 입력 경로가 파일 상단 `SRC` 한 줄이다. 출력 스키마는 기존 15열 + `오픈차단여부` 1열이라 **L6 생성기는 열 매핑에 1행만 더하면 된다.**

---

## 한 것 (카드 할 일 1~5)

### 1. 미판정 143행 판정 — 4분류

| 상태 | 건수 |
|---|---:|
| `done` | 14 |
| `partial` | 7 |
| `todo` | 55 |
| `new` | 58 |
| `미판정`(잔여) | 9 |

**권위 순서**: ① `order-to-mes-process.md §11` 구현 체크리스트(`[x]`/`[ ]`) → ② 코드 실측 → ③ `auto-vs-human.md` 분계표.
`done` 14건 전건에 `file:line` 을 실측으로 붙였고, **「코드 경로 존재 = done, 운영 성공 아님」을 비고에 그대로 적었다**(카드 지시).
생산·공정 중 `auto-vs-human.md` 의 🧑 경로 + ✅현행인 **50건**은 「10/6 범위 수동 대체」로 표기했다.

### 2. `done_criteria` 504행 전건

O2 템플릿(`L5/O2/done-criteria-templates.md`) 상태별 뼈대 5종 × 대분류 확인처 15종을 기계 적용.
돈·주문 행(`돈여부=Y` 또는 `주문여부=Y`, 268행)은 §3 강화 규칙 — **숫자를 넣었다.** 두 갈래로 나눴다:

- 고객에게 금액이 표시되는 계열 → 「표시된 금액이 권위값과 **원 단위까지** 같은 것이 1건 확인된다」
- 운영자가 값을 넣는 계열 → 「저장한 값이 라이브 DB 1행으로 조회되고, 그 값이 가격 계산 결과에 원 단위까지 반영된다」

### 3. `오픈차단여부` 열 신설

lead 차단 최종 규칙(`LEAD-VERDICT-O.md §3`) — **「우리 손 밖 + 오픈을 못 연다」 둘 다** 만족하는 것만 `차단(외부)`.

| 값 | 건수 | 내용 |
|---|---:|---|
| `차단(외부)` | **8** | PG 승인 6(`STD-PAY-001`·`008`·`011`·`SYS-004`·`MYP-007`·`FIN-008`) + 구 DB 이관 2(`STD-MEM-020`·`STD-MYP-009`) |
| `작업 항목` | 437 | 기본값. NHN·EDICUS·배송사 회신 대기도 여기(lead 결정) |
| `오픈 무관` | 50 | 생산·공정 수동 대체 |
| `미판정` | 9 | |

`STD-SYS-009` 는 lead 결정대로 **`done` 유지 + 비고 「구현 완료·미검증 — 검증은 `STD-PAY-013`/`STD-FIN-008`/`X-NHN-REVIEW-01`」**.

### 4. 가능성 재판정 v2

**G5 「불가」 → 「조건부」.** ★ MID 신청 마감 **9/07(월)** (보수 가정 · 계좌이체 최초심사 포함 13영업일).
카드 재심사(+7~10영업일) 발생 조건은 **「미확인」으로 표시**했다(§2-5).

### 5. 부하 최종

미판정 143 → 9. **10/6 오픈에 실제로 걸리는 일감 = 인쇄개발 144 · 쇼핑개발 174** (`오픈 무관` 50 제외).

---

## ★ 역방향 — 초안·상위 판정을 뒤집은 것

### O2 가능성 판정 (4건 · `feasibility-verdict-v2.md §5`)

| # | v1 | v2 |
|---|---|---|
| 1 | G5 **불가** | G5 **조건부** |
| 2 | 「승인 예정일이 어디에도 없다」 | 역산 가능 — **9/07(월)** |
| 3 | G1 **조건부**(`done_criteria` 빈칸이 이탈조건) | G1 **가능**(504행 전건 채움) |
| 4 | PG 리드타임을 단일 구간(3~5영업일)으로 읽음 | **직렬 2단계 합산 6~13영업일** |

### 카드의 ★ 사실 문장도 좁힌다

카드 ★: 「MID 추가 **3~5영업일**」. 이것은 `payment-approval-flow.md §1-1` 의 **[4] 심사 구간만** 가리킨다.
그 뒤 **[6] 지불수단 신청 → [7] 원천사 심사 3~5영업일**이 직렬로 붙는다. **기본 결제수단까지 6~13영업일이다.**
「3~4주는 간편결제에만 해당」은 참이지만, **기본 수단도 1주가 아니라 2~3주로 봐야 한다.**

### lead 의 「함정」 재판정 (`load-final.md §4`)

lead: 「인쇄개발 실제 부하는 이 표보다 크다」 → **절반만 맞다.** 부하는 커졌으나(39→148) **그중 50건이 오픈 무관**이라 오픈에 걸리는 무게는 쇼핑개발(174)이 더 크다.

### 카드의 「차단 후보」 목록을 좁혔다

카드 §3: 차단 후보 = 구DB 이관 · PG · **NHN · EDICUS · 배송사**.
lead 차단 최종(§3)이 **NHN·EDICUS 를 명시적으로 「작업 항목」으로 강등**했으므로 그 규칙을 따랐다 → `차단(외부)` 8건에 NHN·EDICUS·배송사 없음.
간편결제 2건(`STD-PAY-004` 네이버페이 · `STD-PAY-005` 카카오페이)도 `작업 항목` 으로 두고 **비고에 외부 리드타임을 적었다** — 축소 시나리오 A 를 PM 이 고르면 오픈을 안 막기 때문.

### 원장 근거 오기 1건

`STD-MFG-099` 의 근거 `widget_api.py:3388 case_cnt` → **오기.** 실제는 `widget_api.py:1226 case_cnt_rule` · `:1252 parse_case_cnt`. 정정해 v2 에 반영.

---

## 못 한 것 · 미확인

| # | 항목 | 왜 |
|---|---|---|
| 1 | 미판정 4건(`STD-CAT-024`·`CAT-026`·`ADP-034`·`ADP-035`) | **셀러어드민 접속 보류** — 카드 [HARD] 지시대로 접속하지 않았다 |
| 2 | 미판정 5건(`STD-MFG-059`·`060`·`061`·`090`·`133`) | `auto-vs-human.md §5` A-3·A-4·A-5·A-6 승계. MES 소스 없음 · PDF 색 판독 불가 · PM 범위 미결 |
| 3 | **연휴 공식 확인** | 특일 정보 API `530` — **3회째 실패**(O2 · lead · P1 2026-09-02). 역산 (나)의 연휴 날짜는 **추정**이다 |
| 4 | MID **신청 여부** | v1 §3-(1)은 「신청서 제출함」, 09-02 15시 확인은 「추가 발급만 남았다」 — 두 진술이 갈린다. `feasibility-verdict-v2.md §2-4` |
| 5 | 카드 재심사 발생 조건 | 입점 당시 판매상품 범주 미확인. §2-5 |
| 6 | `partial` 76행의 「안 되는 부분」 | 원장에 기록이 없다(비고가 전부 `R1 · L4 판정 승계`). `done_criteria` 에 **「남은 부분 미기재 — 전 항목 1회 점검해 적는다」**로 정직하게 적었다 |

**3·4·5 는 한 장의 캡처로 두 개가 닫힌다** — 이니시스 가맹점관리자 `변경/추가 → 8. 서비스 신청현황`.

---

## lead 에게 넘기는 판단 2건

| # | 판단 | P1 의견 |
|---|---|---|
| 1 | `partial` 76행의 「남은 부분 미기재」를 G1(9/11) 이탈조건에 넣을 것인가 | **넣기를 권한다.** 지금 `done_criteria` 는 「점검해서 적어라」까지만 말한다 — 진짜 완료조건은 그 뒤에 온다 |
| 2 | 카드의 ★ PG 사실 문장(「MID 추가 3~5영업일」)을 **6~13영업일**로 고쳐 다음 카드에 물릴 것인가 | **고치기를 권한다.** 그대로 물리면 다음 레인이 마감일을 1주 늦게 잡는다 |

---

## 완료

**멈춘다.** lead 가 P1 v2 원장으로 L6 생성기를 재실행하면 된다.
