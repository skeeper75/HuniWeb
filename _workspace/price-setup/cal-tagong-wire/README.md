# 캘린더 타공+끈 배선 — `110` 엽서캘린더 · `111` 벽걸이캘린더 (2026-08-30)

승인자: 지니 (「타공+끈은 합가로 본다」 · 「캘린더 전용 구성요소 신설」)
선행 조사: `../calendar-structure-audit/` · `../cal-tagong-audit/`

---

## 1. 무엇을 바꿨나

| # | 대상 | 변경 |
|---|---|---|
| 1 | **`COMP_CUT_PERF_CAL`** 「캘린더 타공가공비 (타공+끈·합가)」 | **신규 생성** — 후가공비 · **고정금액** · 차원 `proc_cd, min_qty, proc_grp:PROC_000079`(타공) |
| 2 | 위 구성요소 단가 | **2행** — `PROC_000126`(1구 6mm) **1,000** · `PROC_000127`(2구 6mm) **1,500** |
| 3 | 공식 `PRF_DGP_INNER` (110) | `COMP_CUT_PERF_CAL` **배선 추가** |
| 4 | 공식 `PRF_DGP_CAL_WIDE` (111) | `COMP_CUT_PERF_CAL` **배선 추가** |

기존 `COMP_CUT_PERF_1H6`(헤더택 타공비) **무변경**. 다른 구성요소·단가 **무변경**.

## 2. 권위

상품마스터 260822_1 「캘린더」 시트 **「캘린더가공(필수)」** 블록 (S 캘린더가공 / T 추가가격):

| 캘린더가공 | 추가가격 | 층 |
|---|---:|---|
| 고리형트윈링제본 | 2,000 | ① 인쇄가공비 → `COMP_BIND_CAL_WALL` (기존) |
| **1구타공+끈** (엽서캘린더) | **1,000** | ② **추가가공 — 이번 배선** |
| **2구타공+끈** (벽걸이캘린더) | **1,500** | ② **추가가공 — 이번 배선** |
| 우드거치대 | 4,000 | ③ 부속 → 추가상품 addon (기존) |
| 가공없음(재단만) / 제본없음(재단만) | – | 0 |

## 3. 왜 전용 구성요소인가

기존 `COMP_CUT_PERF_1H6`(타공비 6mm)는 **헤더택 값**이고 캘린더와 다르다:

| | 1구 | 2구 |
|---|---:|---:|
| 헤더택 `COMP_CUT_PERF_1H6` | 2,000 | 4,000 |
| **캘린더 `COMP_CUT_PERF_CAL`** | **1,000** | **1,500** |

그리고 `110` 엽서캘린더와 `045` 인쇄헤더택은 **타공 공정(`PROC_000126`)도 판형(`SIZ_000499`)도 같아**
한 표에 담으면 구분할 축이 없다. 그래서 표를 나눴다(지니 판단).

두 comp 가 같은 공정을 쓰지만 **서로 다른 공식에만 붙어** 충돌하지 않는다 —
실계산으로 확인했다(§5: `045` 무변화).

## 4. [HARD] 실행 중 잡은 결함 — 단가유형을 잘못 골랐다

처음에 **합가형(`PRICE_TYPE.02`)** 으로 만들었더니 `110` 이 100장에 **+100,000** 이 붙었다
(1,000 × 100). 즉시 코드로 원인을 확인했다 — `pricing.py` `component_subtotal`:

> **합가형**: unit_price(**구간 총액**) ÷ 구간 `min_qty` = 장당가, **× 수량**
> **고정금액**: unit_price 그대로 — **수량 무관(곱셈 없음)**

`min_qty=1` 행에 1,000 을 넣으면 「1장에 총 1,000」 = 장당 1,000 이 되어 수량만큼 곱해진다.
기존 헤더택 타공비가 flat 처럼 보인 것은 `min_qty=100` 구간에 2,000 이 들어 있어
2,000÷100 × 100 = 2,000 이 된 것이지, 합가형이 정액이어서가 아니다.

⇒ **권위의 「수량 무관 정액」은 `고정금액(PRICE_TYPE.03)` 이다.** 유형을 바꿔 해소했다.

> 지니의 「합가로 본다」는 **엑셀의 합가 개념**(항목을 묶은 값)이고,
> 라이브 `PRICE_TYPE` 분류로는 **고정금액**이 대응한다. 두 낱말이 같은 뜻이 아니다.

**교훈**: 단가유형은 이름이 아니라 **`component_subtotal` 의 계산식으로 고른다.**
구성요소 비고에 이 이력을 남겼다.

## 5. 검증 — 실계산 (`after/verify-sim.json`)

**10케이스 전수 · 의도한 2건만 움직였다.**

| 케이스 | 전 | 후 | 차이 |
|---|---:|---:|---:|
| 110 기본 | 17,214 | 17,214 | +0 |
| **110 1구타공** | 17,214 | **18,214** | **+1,000** |
| 111 기본 | 33,129 | 33,129 | +0 |
| **111 2구타공** | 33,129 | **34,629** | **+1,500** |
| 111 제본(세로형좌철) | 233,129 | 233,129 | +0 |
| 112 와이드벽걸이 | 68,608 | 68,608 | +0 |
| **045 헤더택 타공 1구** | 11,645 | 11,645 | +0 |
| **045 헤더택 타공 2구** | 13,645 | 13,645 | +0 |
| 287 중철책자-내지 | 40,922 | 40,922 | +0 |
| 311 트윈링책자-내지 | 0 | 0 | +0 |

**수량 무관 확인** (110 · 타공 선택 vs 미선택 차이):

| 수량 | 1 | 10 | 100 | 500 |
|---|---:|---:|---:|---:|
| 차이 | **+1,000** | **+1,000** | **+1,000** | **+1,000** |

정액으로 정확히 작동한다.

## 6. 증거

| 경로 | 내용 |
|---|---|
| `before/baseline-sim.json` | 배선 전 10케이스 |
| `before/01-newcomp-ready.png` · `02-inner-ready.png` | 구성요소 생성 직전 · 공식 저장 직전 |
| `after/grid-COMP_CUT_PERF_CAL.json` | 신규 단가 2행 (고정금액) |
| `after/detail-110.json` · `detail-111.json` | 변경 후 구성요소 구성 |
| `after/verify-sim.json` | 10케이스 + 수량 무관 4케이스 |
| `after/01-diagram-111.png` | 111 가격 구조 |

## 7. 되돌리는 법

1. `PRF_DGP_INNER` · `PRF_DGP_CAL_WIDE` 에서 「캘린더 타공가공비」 행을 삭제하고 저장.
2. 필요하면 `COMP_CUT_PERF_CAL` 을 `use_yn=N` 으로 은퇴(단가 2행은 보존).

기존 구성요소·단가는 손대지 않았으므로 값 복원은 필요 없다.

## 8. 남은 것

- **가격표 「커팅타공」 `D42` 「헤더택 / 벽걸이캘린더」 라벨**은 여전히 미해소다.
  그 옆 「타공(합가)」 표는 2구 4,000 인데 이번에 쓴 상품마스터 값은 1,500 이다.
  **상품마스터가 상품별로 명시한 값**을 택했고(지니 판정), `D42` 는 두 표 사이에 병합 없이 떠 있어
  어느 표의 라벨인지 문서만으로 정해지지 않는다. 실무진 확인 시 이 선택을 재검토한다.
- `112` 와이드벽걸이캘린더는 타공 공정이 없어 대상이 아니다(권위에도 타공 옵션 없음).
