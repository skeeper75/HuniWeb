# PRD_072 하드커버책자 셋트 — GO분 실행 인계서 (handoff-spec)

> §34 Huni-Load-Governance · Phase 5 · hlg-governance-gate · 2026-07-03
> 상위 게이트: `gate-report.md`(LG1~LG7 전부 GO) · 판정: **옵션 NO-OP(처분 0)** → 실행 큐 최소.
> [HARD] 본 게이트는 DB 미적재·COMMIT 범위 밖. 아래는 인간 승인 후 기존 트랙에 위임할 방향만 명기.

---

## 1. 즉시 실행 큐 — **없음 (처분 0건)**

072 셋트 옵션 레이어는 전건 KEEP. RETIRE/MOVE/EXTEND/BLOCKED 처분 0건 → **COMMIT할 옵션 처분 SQL
없음**. 별도 적재 트랙(§7/§31/§17) 위임 항목 없음. 현 라이브 상태 무손상 유지가 정답.

- undo 방향: N/A(변경 없음).
- webadmin 실화면 확인 의무: 처분 COMMIT이 0이므로 본 옵션 거버넌스에 실화면 확인 트리거 없음.
  (단 아래 §2 관찰 큐가 향후 실 변경으로 전환되면, **그 위임 트랙이 COMMIT 전 webadmin 실화면
  "제외 0·PRICE≠0" 확인 [HARD] 의무를 진다** — §1 도메인 규칙.)

## 2. 관찰 큐 라우팅 (지금 처분 금지 · 후속 트랙 위임)

### O-2 — 부모 072 print_opt (규범 §3-4 tension) → 규범 §1 + §23
- **현상(라이브 실측)**: 부모 072가 POPT_001/002 보유(del=N), 내지 284도 동일 보유. 규범 §3-4는 셋트 부모
  도수/인쇄옵션 "금지" 문언.
- **판정**: literal tension이나 **가격 무해**(796,900이 순수 COVERBIND tier·print_opt 구동 추가청구 0).
  siz_cd 셋트 UI 전파([[postcard-book-sim-convergence-260702]])와 동형(부모=손님선택 전파 계층) 가설 유력.
- **위임**:
  1. **규범 큐레이터(§1 / hlg-vessel-norm-curator)** — vessel-norm §3-4에 "셋트 부모 = 손님선택 UI 전파
     계층" 예외조항 보강 여부 결정(규범 문서 갱신·라이브 변경 아님).
  2. **§23 셋트 트랙** — 셋트 UI에서 단면/양면을 부모 레벨로 골라 내지로 전파하는 코드 계약 확인.
- **금지**: 부모 print_opt 즉시 제거(손님 단면/양면 선택 불가 = 매출 차단·오차단 0 위반). BLOCKED 용어
  부적용(print_opt=기준정보 레이어·옵션 아님).

### O-1 — 면지 색 이중표현(옵션 + 빈 멤버 074/075/076) → §23 셋트 트랙
- **현상(라이브 실측)**: 면지색 옵션 3옵션(→MAT_382/383/384 생산전달)과 멤버 074/075/076(materials/proc/
  sizes/plate/print/formula 전부 0·완전 빈 껍데기)이 모두 "면지 색"을 표현. 멤버 3종 선택 기여 0.
- **판정**: 현재 무해(멤버 무가격). 향후 면지 자재에 단가 붙으면 멤버 3종 합산 latent 위험.
- **위임**: **§23 셋트 설계 트랙** 택일 — ① 옵션 선택색만 멤버로 포함하는 조건부 구성(옵션↔멤버 연결) or
  ② 멤버 3종 제거하고 색은 옵션 전담. 실 COMMIT 시 §7 dbmap(t_prd_product_sets del_yn 논리삭제).
- **금지**: 지금 멤버 삭제(동작 검증 셋트·§23 PRICE≠0 골든 무손상 보장 전까지). 옵션 KEEP×3 불변.

### OPT_064 — work-order 전달경로 검증 → webadmin 후속 확인 큐
- **현상**: 라이브 DB에서 옵션 ref→자재 환원(item .03=MAT_382~384)은 확인됨. 최종 소비점(작업지시서/SKU
  코드에 면지색이 실제 전달되는지)은 DB로 확인 불가.
- **위임**: webadmin 주문/작업지시 코드 경로 읽기 확인(HUNI_ADMIN_* 읽기 탐색·저장/삭제 금지). U-3
  전달경로 실증. 환각 아님·기각 아님·KEEP 판정 불변.

### §23-inner — 내지 284 contribution=0 (교차참조 · 본 §34 유발 아님)
- **현상**: evaluate_set_price에서 내지 284 기여 0(내지 페이지곱 비용 미반영). 현재 072가 부모 COVERBIND
  tier로만 가격 산출.
- **판정**: §23의 **기존 pending**("072 내지 반제품 승격 = 그릇 GO·적재 대기") — 본 옵션 거버넌스가 유발한
  것이 아니다. §34 잣대(PRICE≠0·이중합산 0)는 충족하므로 **§34 NO-GO 아님**.
- **위임**: §23 내지 승격 적재 트랙(내지 페이지 기여 완전성=§23 골든 소관). 본 게이트는 교차참조만.

## 3. 인간 승인 필요 항목

| 항목 | 승인 대상 | 트랙 |
|------|-----------|------|
| O-2 규범 §3-4 예외조항 보강 | 규범 문서 갱신(라이브 변경 아님) | §1 hlg-vessel-norm-curator |
| O-2 셋트 UI print_opt 전파 계약 | 코드 계약 확인(변경 시 별도) | §23 / §6 |
| O-1 면지 멤버 구조 택일 | t_prd_product_sets 변경(택일 후) | §23 → §7 dbmap COMMIT |
| OPT_064 work-order 경로 | webadmin 읽기 확인 | webadmin 후속 |

→ 위 어느 항목이든 실 라이브 변경으로 전환되면 **위임 트랙이 COMMIT 전 webadmin 실화면(제외 0·PRICE≠0·
판형 자동선택) 확인 [HARD]** — 본 §34 게이트는 그 의무를 위임 트랙에 인계한다.

## 4. 건드리지 말 것 (동작 검증·무손상 확정)

- 면지색 옵션 OPT_000064 및 3옵션(화이트/블랙/그레이) — KEEP 확정, 삭제/이관 금지.
- 072 셋트 구성(t_prd_product_sets 5구성원) — 현 배치 무손상(evaluate_set_price 34,100/159,100/796,900).
- 부모 072 자재 MAT_382/383/384 — 옵션 ref 백킹(fn_chk_opt_item_ref)·삭제 시 트리거 위반.
