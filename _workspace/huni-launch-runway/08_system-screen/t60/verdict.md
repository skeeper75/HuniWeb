# t60 verdict — 오픈일정 S9-A · 서희항 CTO 역할 축별 Todo 수립

브랜치 `WT-cto-role-todo` · 워크트리 `.claude/worktrees/t60` · 분기점 `90be92fb`(main)
병합: `WT-ops-role-todo`(t59 `02dbfb14` · t56@`5bb97cfd`+t57+t58 포함) → `WT-role-todo-rejudge`(t56 최신 `a83dc44b`), 둘 다 `--no-ff`.

읽기전용 — DB write 0 · 라이브 DB 접속 0 · 라이브 화면 접속 0 · webadmin·MES·huni-mall 저장소 쓰기 0 · 날짜 추정 0 · 작업량(사람·일) 숫자 0.
**재배정·신규 행은 전부 제안이고 결정은 지니다. t56 원장(`t56/rejudge.csv`)은 수정하지 않았다.**

## 1. 산출물

| 파일 | 내용 |
|---|---|
| `cto-todo.md` | **본문** — 한 장 요약 → 6축 → 축마다 ㉮원장 행 · ㉯원장에 없는 일 · ㉰선후 관계표 → ㉱먼저 할 순서 → 미확인 |
| `axis-rows.csv` | ㉮ 행 원장 **186행**(축·근거강도·prereq·evidence·배분 사유 13열) |
| `lead-recount.md` | 리드 「241행·181」 대조 — **재현 실패**와 그 탐색 과정 |
| `inputs-check.md` | 카드 지정 입력 6종 + 절대경로 5종 실재 확인 · 열지 않은 것 명시 |
| `scan.py`·`build_axis.py`·`prereq.py`·`axis_prereq.py`·`gap_scan.py`·`edges_out.py`·`piecanvas_scan.py`·`count_probe.py`·`count_probe2.py`·`dump.py`·`dump2.py` | 결정론 스캔·배분·선후 실측·공백 탐색 |
| `verify.py` · `verify-result.txt` | 검산 **51게이트 · 실패 0** |

## 2. 행수·분포

- 입력: `plan-rows.csv` **735행** · `t56/rejudge.csv` **735행**(G1·G2) · `t51/merged.csv` **747행**
- **서희항 몫 246행 · 남은 일 186행**(G3) — 리드 표기 241/181 은 **재현 실패**(§4-1)
- 산출 `axis-rows.csv` **186행 · 미배치 0**(G4·G5)

| 축 | 이름 | 행 | 선행 없음 | 외부 선행 토큰 |
|---|---|---:|---:|---:|
| ① | 위젯·가격 | 39 | 34 | 3 |
| ② | 에디터·원고 | 30 | 12 | 11 |
| ③ | API·SDK(huni-mall 이음매) | 18 | 8 | 12 |
| ④ | 생산연동(PitStop·MES) | **50** | **3** | **55** |
| ⑤ | 인프라 이전(Lightsail) | 19 | 1 | 9 |
| ⑥ | webadmin 운영화면·회원/정산 이관 **(신설 제안)** | 30 | 18 | 6 |
| | **합계** | **186** | **76** | **96** |

**근거강도**(G9·G10): 강함 132(`merged-owner_side` 66 · `evidence-path` 59 · `manual-read` 7) ·
**약함 54 = 29%**(`rule-step` 42 · `rule-track` 12). 약한 54행은 **배정이 아니라 제안**이다.

**신규 제안 4건**(G40 · 원장 row_id 충돌 0): `NEW-S1` 허용사이트 등록 수단 · `NEW-S2` handoff/verify 재검증 계약 정의서 ·
`NEW-S3` 「우리가 제공할 API 정의서」 · `NEW-S4` Edicus 파일 검수 스킵 여부.
**결정 안건 신설 0건** — 이 카드는 `screens.csv` 를 만들지 않으므로 계약 보충 4 해당 없음.
다만 **기존 결정 안건 1건의 제목 정정을 제안**한다(§4-4).

## 3. 리드 초안 5축에 대한 판정 — 2곳 고침

| | 리드 초안 | 판정 | 근거 |
|---|---|---|---|
| 고침 1 | ⑤ 인프라에 `STD-ADP` 11행 | **① 로 옮김** | 11행의 evidence 가 전건 webadmin 기준정보·가격 라우트(`urls.py:356-360`·`:371`·`:144-148`·`:320-330`)다. Lightsail 을 가리키는 행 **0**. 인프라 행은 전건 `migration-plan.md:36-69` — **두 집합이 섞이지 않는다** |
| 고침 2 | 5축이 전부 | **⑥ 신설 제안** | 186행 중 **30행**이 5축 어디에도 안 들어간다(webadmin 운영화면 · 구 사이트 이관 · 알림톡). 억지로 넣으면 그 축의 선후 구조가 오염된다 |
| 유지 | ①②③④ 뼈대 | 유지 | 단 `STD-MFG` 65행은 ④ 한 축이 아니다 — 8행이 ③ 주문 수신 계약, 6행이 ② 원고 승격 |

리드 카드 축별 숫자 **6종은 정확히 일치**한다(`STD-ADP` 11 · `STD-ADO` 6 · `STD-CAT` 8 · `F1·F2·F3` 15 · `STD-SYS` 7 · `BLK` 3).
어긋난 `STD-ART` 20 · `STD-MFG` 62 는 **`provided` 8행을 뺀 수**임을 확인했다(G17·G18).

## 4. 카드 질문별 답

### 4-1. 분모 — **리드의 241/181 을 재현하지 못했다**

내 집계 246/186. 기준 3종 × 배제 규칙 5종의 **모든 부분집합 48조합**을 돌렸으나 241/181 은 나오지 않았다(`count_probe2.py`).
차이는 3~5행이고, **총계−남은일 = 60 이 세 판본 모두 같다** → 같은 모양의 집합을 거의 같은 규칙으로 센 것이 맞다.
**이 문서는 재현되는 수(246/186)로 갔다.** 상세는 `lead-recount.md`.

### 4-2. ㉮ 축별 행 전수 수집 — 완료

186행 전건을 축에 배분했고 규칙을 `build_axis.py` 상단에 정본으로 적었다(미배치 0 · G5).
**evidence 실독은 축 판단이 갈리는 자리에 집중**했다 — 186행 전건의 evidence 파일을 열지는 않았다(§5-6).

### 4-3. ㉯ 원장에 없는 일 — **4건. ①③④ 에 하나씩, ⑤⑥ 은 0건**

| 제안 | 축 | 돌린 패턴 · 결과 |
|---|---|---|
| `NEW-S1` 허용사이트(도메인·site_key) 등록 수단 | ① | `허용\s*(사이트\|도메인)\|allow_domains\|site[_ ]?key\|사이트\s*키` → **적중 3행 전부 다른 일** |
| `NEW-S2` handoff/verify 재검증 계약 정의서 | ③ | `handoff\|재검증\|verify` → **적중 3행 전부 다른 일**. t48:192 는 `plan_row_id=NEW` |
| `NEW-S3` 「우리가 제공할 API 정의서」 | ③ | `API\s*4종\|정의서\|인터페이스\s*규격\|API\s*계약` → **적중 0행** |
| `NEW-S4` Edicus 파일 검수 스킵 여부 | ④ | `검수\s*스킵\|편집기.*검수\|Edicus.*검수\|에디쿠스.*검수` → **적중 0행**(t59 가 독립으로 같은 결론) |

**⑤는 0건** — 런북(`migration-plan.md`)이 단계·담당·선행·완료확인·롤백·위험 6열을 다 채웠고 원장 행이 1:1 로 문다.
**⑥도 0건** — 후보였던 「구 DB 접근」은 `BLK-S3-1` 로 원장에 실재한다(신규가 아니라 **간선 제안**).

`NEW-S1`은 **내가 다시 열어 확인**했다(계약 보충 4): `TWgtSites` Django admin **0건**(G32) ·
`allow_domains` 를 **쓰는 코드는 테스트 픽스처 2곳뿐, 운영 도구·화면 0곳**(G33) ·
발급 도구는 `site_key` 만 UPDATE(`tools/issue_site_key.py:35-37`) · 미설정이면 Origin 검사가 **꺼진다**(`widget_api.py:710` G34).

### 4-4. ④ `STD-ART-034` 결정 사슬 — **서희항 쪽에서 끝까지 폈고, 안건 제목의 오류를 찾았다**

t59 가 편 것은 **위쪽 절반**(최숙진이 무엇을 정의해야 하는가)이다. 이 카드는 **뿌리와 아래쪽**을 이었다.

- **사실 정정 제안** — 원장 `STD-ART-034` 제목은 「PitStop 연동 방식 결정(**핫폴더 vs CLI**)」인데(G37),
  t57 이 선례를 실독하고 **「「핫폴더 vs CLI」가 아니다 — 열림PnP 는 둘 다 만들었고 둘 다 CLI 다」**라고 적었다(G36 · `t57:305-309`).
  실제 갈래는 **A(큐+DB) vs B(파일+JSON)**. → **제목을 고치지 않으면 없는 선택지를 놓고 회의한다.**
  (제목 교정은 **제안**이다. 원장은 건드리지 않았다.)
- **아래쪽 전개** — 뿌리 아래 **서희항 구현 행 최소 18행**(`STD-ART-016` · `STD-MFG-036`~`-040`·`-044`·`-046`·`-049`~`-053` · `STD-ART-008`~`-015`)이
  A/B 에 따라 **큐 소비자 ↔ 폴더 감시자로 구현 형태가 통째로 갈린다**.
- **원장은 이 사슬을 `STD-ART-034 → STD-SYS-031` 한 줄로만 묶어 두었다.** 나머지 간선은 전부 제안이라고 표에 적었다.
- **결정 주체를 고르지 않았고, A/B 중 무엇이 낫다고도 하지 않았다** — 선례 저장소를 이 카드가 열지 않았다(§5-4).
- **`T4-3` 상충 미해소** — t50 대표(채훈희) ↔ t56 서희항+신우진(`rule-track` 약함). t57 대로 **병기만** 했다.

### 4-5. ③ 김동학 호출부와 맞물리는 계약 — **path:line 으로 못 박았다**

제공자(webadmin) `api/w/v1` 라우트 **22개 실재**(G20). 카드가 지목한 넷 전부 있다(G19). 호출자(huni-mall) 실측:

| 계약 | 제공자 | 호출자 현재 | 게이트 |
|---|---|---|---|
| C-1 `order/register` | `urls.py:268` | **0건** | G21 |
| C-2 `handoff/verify` | `urls.py:256` | **주석 2줄뿐 · 실호출 0** | G23 |
| C-3 `handoff/requote` | `urls.py:257` | 프록시·헬퍼 있음 · **호출처는 `cart-page.tsx` 뿐 → 결제 직전 재견적 0** | G24 |
| C-4 `X-Huni-Server-Key` | `F1-2`·`BLK-S2-1` | **0건** | G22 |
| C-5 `GET /api/w/v1/catalog` 시작가 | `urls.py:248` | 샵바이 `salePrice` 사용 · 미이관 | — |

**핵심은 「김동학이 늦다」가 아니다.** `BLK-S1-5`(`order/register` 계약 — 엔드포인트·페이로드·서버키)가 **미착수**이고
담당이 **서희항+신우진**이다. **계약서가 없으면 부를 수 없다.** t61(김동학 5축)의 ① 호출부와 맞물리는 지점이 이 표다.
**t61 산출은 보지 않았고, 양쪽 저장소를 내가 직접 연 것만 적었다.**

### 4-6. ⑤ 이전 순서와 「오픈 테스트는 Lightsail 위에서」 — 완료 (+ 리드 보강 3건 반영)

`CARDS-S.md` §0-A 원문 확인(G38). 런북 F3-11 이 **「되돌릴 수 없는 단계 — 오픈 테스트 통과 후에만」**이라고 스스로 적었고(G39),
원장 `T1-3` 제목도 같은 말을 단다. → **순서 = F1 → F2 → F3-1~10 → 〔오픈 테스트〕 → F3-11 → F3-12.**
⑤의 진짜 뿌리는 **외부(인프라팀) OIDC·시크릿·SSH 터널 회신** 하나다 — 서희항이 할 수 있는 건 요청뿐이다.

**(a) 통합 대상 4개와 담당 구분** — 인프라 구축 자체·webadmin·DB = **서희항**(= 이 축 19행) /
huni-mall·페이지빌더 = **김동학**(`F4-2`~`F4-6`·`T1-4` · ⑤ 에 안 들어옴).
「파트너사」 정정(지니 확정 260919 · 리드 전달)이 **원장에 미치는 영향을 실측**했다:
`EXT-PIECANVAS` 를 `prereq` 로 단 행 **0**(G48 — 토큰은 `LEDGER.md:106` 에 정의만 돼 있고 안 쓰인다) ·
페이지빌더 언급 행 **2행 전건 김동학**(G49 — 정정과 이미 일치). → **원장 선후 구조는 안 바뀐다.**
바뀌는 것은 **판정 문서**다 — `pie-canvas-model.md:9-10` 「벤더」(G50) · `t49/progress.md:51-52` 「파트너사 소유 SaaS · 코어 수정 통제 밖」·「NO-GO(대외배포)」가
**상대가 통제 밖 외부라는 전제** 위에 있다. **이 카드는 t49 를 재판정하지 않았다**(범위 밖) — 사실만 적었다.

**(b) 나가는 간선** — ⑤ 인프라 행을 `prereq` 로 단 행은 원장 전체에 **17행**인데(G41),
**김동학에게 나가는 것은 `F4-4`(DATABASE_URL 목적지 결정) ← `F2-1` 단 하나**다(G42·G43).
나머지 16행은 ⑤ 자기 안이거나 최숙진·신우진이다. → **원장은 「서희항 인프라가 김동학 이전을 문다」를 거의 기록하지 않는다.**
간선 제안 3건을 ㉰ 표에 추가했다: `F1` 계열 → `F4` 계열(`F4-2` 가 「webadmin 것을 본떠」 만든다고 적었는데 `prereq` 는 `F4-1` 뿐 · G44) ·
⑤ → 페이지빌더 이전(**행 자체가 없다** · G47) · 〔오픈 테스트〕 → `F3-11`.

**(c) 오픈 테스트 시점에 무엇이 올라와 있어야 하나** — 「구 환경 정지」 행 둘이 거꾸로 답을 준다.

| 대상 | 어디까지 | 근거 강도 |
|---|---|---|
| webadmin + DB | **`F3-10` 까지** | 런북 위험란·`T1-3` 제목에만 명시. **`F3-11.prereq` 에 「오픈 테스트」가 없다**(G46) |
| huni-mall | **`F4-6` 까지** | **`F4-8.prereq` = 「F4-6 + 오픈 테스트」 — 735행 중 「오픈 테스트」를 `prereq` 로 단 유일한 행**(G45) |
| 페이지빌더 | **찾지 못했다** | 전수 검색 **적중 0행**(G47). 「필요 없다」가 아니라 「원장이 말하지 않는다」 |

**테스트 시점에 구 환경(Railway·Vercel)은 아직 살아 있다** — `F3-11`·`F4-8` 이 둘 다 테스트 뒤다. 되돌릴 곳이 남아 있다는 뜻이다.

### 4-7. ④ 「세 저장소 전부 코드 0」 — **맞다. 내가 직접 돌려서 확인했다**

webadmin PitStop 0(G25) · MES 전송 코드 0(G26) · MES 저장소 PitStop 0(G28) · shopby 0(G29) · huni-mall 0(G30).
**정확히는 「전송·연동 코드가 0이고, MES 품목코드 매핑 그릇은 webadmin 에 8곳 있다」**(G27 — 전부 필드·안내·중복검증).
MES 저장소의 유일한 외부 커머스 연동은 **Cafe24**(`Cafe24Interface.Token/Common/Cafe24AuthClient.cs:13`)다 — 오픈 분모 밖 선례.

### 4-8. ㉱ 먼저 할 순서 — 선후로 세웠다(날짜 0)

**0순위 셋은 전부 「글 쓰기」와 「독촉」이고 코딩이 아니다.** 그리고 셋 다 서희항 말고 할 사람이 없다.

1. `BLK-S1-5` + `NEW-S2` + `NEW-S3` — 계약을 글로 고정(③ 전부가 여기 걸림 · 외부 선행 0)
2. `STD-ART-034` 제목 정정 후 결정 테이블 상정(④ 50행의 뿌리)
3. 외부(인프라팀) 회신 독촉(⑤ → 오픈 테스트 전체)

그다음 1순위 = 선행 없는 축 병렬(① 34행 · ⑤ F1→F2-2 리허설 · ② 템플릿 21건 충전 · ⑥ 18행),
2순위 = 뿌리가 풀린 뒤(④ 검사군·MES 전송 · ③ 구현 · ⑥ 알림톡·이관), 말단 = **F3-11**(비가역).

**④가 가장 큰 덩어리(50행)지만 먼저 할 일이 아니다** — 선행 없는 행이 3행뿐이고 외부 선행이 55개다.

## 5. 미확인 (그대로 읽을 것)

1. **리드의 241/181 을 재현하지 못했다.** 48조합 전수 탐색 실패. 어느 3~5행이 다른지 특정하지 못했다.
2. **「원장에 없다」는 전부 「이 패턴으로 찾지 못했다」**다. 돌린 정규식을 `gap_scan.py` 에 남겼다.
3. **F3-7 의 「스킨 하드코딩 3곳」과 내 실측이 다르다** — `huni-admin.printly.co.kr` 은 huni-mall 에 6곳이고 그중 **기능 코드는 2곳**(`src/lib/printly/huni.ts:10` · `src/app/api/printly/requote/route.ts:15`), 나머지는 주석이다. CNAME 으로 호스트명이 유지되면 발화하지 않는 항목이라 **판정하지 않았다**.
4. **선례 저장소 `CRT.DigitalEdit.V2` 를 열지 않았다.** ④의 「둘 다 CLI 다」는 t57 인용이고, 그 근거 `Program.cs:636` 을 내가 확인하지 않았다. **A/B 판단을 하지 않는 이유다.**
5. **`provided` 8행을 ④에 넣었다.** 작업량으로 세면 안 된다. 리드 숫자와의 차이는 전부 여기서 나온다.
6. **㉮의 evidence 실독은 축 판단이 갈리는 자리에 집중**했다 — 186행 전건의 evidence 파일을 열지는 않았다.
7. **선후 관계표에서 「간선 제안」이라 적은 것은 원장에 없다.** 원장 `prereq` 가 실제로 가진 것만 「`prereq` 실재」로 적었다.
8. **라이브 접속 0.** `STD-MFG-010` 의 「269개 중 15개 매핑」은 t56 evidence 안의 **260902 실측 인용**이며 현재값이 아니다.
9. **⑥ 축은 제안이다.** 5축으로 가기로 하면 30행을 어디에 넣을지 다시 정해야 하고, 그때 치르는 대가를 본문 §7 에 적어 두었다.
10. **`docs/huni/` 의 회의록·엑셀을 열지 않았다.** 본문에 나오는 `후니정기미팅 정리260915.html` 는 t56 evidence 안의 인용이며 내가 확인한 것이 아니다.
11. **「파트너사 = 김동학 대표」는 리드 lane-1 이 전달한 지니 확정(260919)이다.** 지니 발언 원문을 내가 확인한 것이 아니다. 내가 확인한 것은 기존 문서의 표기와 그 토큰이 `prereq` 에서 0회 쓰인다는 사실뿐이다.
12. **오픈 테스트에 페이지빌더가 필요한지 답하지 못했다.** 이전 행이 0행이다. 상세페이지 탭이 발행본에 의존하므로(`STD-CAT-034`) 무관하다고 보기는 어렵다.
13. **t61(김동학 5축) 산출을 보지 않았다.** ⑤에 적은 김동학 쪽 행(`F4`·`T1-4`)은 내가 `plan-rows.csv` 에서 직접 읽은 것이다.

## 6. 검산 (`python3 verify.py` 출력 그대로 · `verify-result.txt`)

```
PASS G1 입력 plan-rows 735행 — 735
PASS G2 입력 t56 rejudge 735행 — 735
PASS G3 서희항 몫 246 · 남은 일 186 — (246, 186)
PASS G4 axis-rows 186행 — 186
PASS G5 미배치 0건 — 0
PASS G6 축별 행수 39/30/18/50/19/30 — {'1': 39, '2': 30, '3': 18, '4': 50, '5': 19, '6': 30}
PASS G7 row_id 중복 0 — 186
PASS G8 축 행 전건 t56 원장 실재
PASS G9 judged_by 66/59/7/42/12 — {'evidence-path': 59, 'merged-owner_side': 66, 'rule-step': 42, 'rule-track': 12, 'manual-read': 7}
PASS G10 약한 근거 54행 = 29% — (54, 29)
PASS G11 축별 선행없음 34/12/8/3/1/18 — {'1': 34, '2': 12, '3': 8, '4': 3, '5': 1, '6': 18}
PASS G12 축별 외부 선행 3/11/12/55/9/6 — {'1': 3, '2': 11, '3': 12, '4': 55, '5': 9, '6': 6}
PASS G13 ④ 외의 다섯 축 외부 선행 합 41 — 41
PASS G14 ④ EXT-PITSTOP 24 · EXT-MES 20 · EXT-NHN 9 — {'EXT-PITSTOP': 24, 'EXT-MES': 20, 'EXT-NHN': 9, 'EXT-PG': 1}
PASS G15 ④ 외 다섯 축 선행없음 합 73 — 73
PASS G16 provided 8행 전건 ④ — 8
PASS G17 STD-ART 남은 27 · provided 7 → 20 (리드 표기와 일치) — (27, 7)
PASS G18 STD-MFG 남은 63 · provided 1 → 62 (리드 표기와 일치) — (63, 1)
PASS G19 제공자 라우트 5종 실재(urls.py)
PASS G20 api/w/v1 라우트 22개 — 22
PASS G21 huni-mall order/register 호출 0건 — 0
PASS G22 huni-mall 서버키 헤더 0건 — 0
PASS G23 huni-mall handoff/verify = 주석 2줄뿐(실호출 0) — 2
PASS G24 requote 헬퍼 호출처 = cart-page.tsx 뿐 — ['/Users/innojini/Dev/huni-skin-shopby/src/components/cart/cart-page.tsx']
PASS G25 webadmin PitStop 0건
PASS G26 webadmin MES 전송 코드 0건
PASS G27 webadmin(앱) mes_item_cd 8곳 — 전부 그릇(필드·안내·중복검증) — 8
PASS G28 MES 저장소 PitStop 0건
PASS G29 MES 저장소 shopby 0건
PASS G30 huni-mall PitStop/MES 0건
PASS G31 Edicus miss 합계 21 · blocked 1상품 — (21, ['/products/PRD_000226'])
PASS G32 TWgtSites Django admin 미등록(0건)
PASS G33 allow_domains 쓰기 = 테스트 픽스처 2곳뿐 · 운영 도구·화면 0곳 — ['tests/test_catalog_include_unpublished.py', 'tests/test_prd_sellable_gate.py']
PASS G33b allow_domains 등장 21곳(저장소 전체) — 21
PASS G34 widget_api.py:710 「미설정=통과」 원문 실재
PASS G35 인용 경로 22개 전건 실재 — 없음 0
PASS G36 t57 「핫폴더 vs CLI 가 아니다」 원문 실재
PASS G37 t56 원장 STD-ART-034 제목에 「핫폴더 vs CLI」 실재(정정 대상)
PASS G38 CARDS-S §0-A 「오픈 테스트는 Lightsail 위에서」 실재
PASS G39 런북 F3-11 「되돌릴 수 없는 단계 — 오픈 테스트 통과 후에만」 실재
PASS G40 신규 제안 4건 row_id 원장 충돌 0 — 충돌 0
PASS G41 ⑤ 인프라 행을 prereq 로 단 행 17 — 17
PASS G42 ⑤ → 김동학 나가는 원장 간선은 F4-4 하나뿐 — ['F4-4']
PASS G43 F4-4.prereq 에 F2-1 실재 — F2-1
PASS G44 F4-2.prereq 에 F1 계열 없음(간선 제안이 맞다) — F4-1
PASS G45 prereq 에 「오픈 테스트」가 적힌 행은 F4-8 하나뿐 — ['F4-8']
PASS G46 F3-11 은 prereq 에 오픈테스트 없음 · 같은 조건이 T1-3 제목에만 있다 — F3-10 안정 + vc_ 웹팀 확인
PASS G47 페이지빌더 Lightsail 이전 행 0 (찾지 못했다) — 0
PASS G48 EXT-PIECANVAS 를 prereq 로 단 행 0 — 정정이 원장 선후를 바꾸지 않는다
PASS G49 페이지빌더 언급 행 2 · 전건 김동학(정정과 이미 일치) — [('STD-ADP-015', '김동학'), ('STD-SYS-021', '김동학')]
PASS G50 pie-canvas-model.md 가 「벤더」로 적고 있다(정정 대상 표기 실재)

실패 0
```

**검산 중 본문을 3곳 고쳤다** — 처음 쓴 「⑤ 외 선행없음 67」(→73) · 「mes_item_cd 5곳」(→8) ·
「allow_domains UPDATE 0건」(→쓰기 2곳은 테스트 픽스처 · 운영 도구·화면 0곳). **게이트를 숫자에 맞추지 않고 본문을 실측에 맞췄다.**
