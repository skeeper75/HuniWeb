# t57 verdict — 오픈일정 S7-B 시스템 연결 설계서 (Top→Down)

카드 **t57**(class C · SPEC 없음 · 조사·문서 · 읽기전용) · 리드 lane-1 [5fdcce] 디스패치 · 260919
워크트리 `.claude/worktrees/t57` · 브랜치 **`WT-connect-design-doc`**

> **개정 2판 (담당 열 반영)** — 초판(`11b9399b`)은 리드 PASS 를 받았고 담당 열을 비워 두었다.
> t56 재작업(`5bb97cfd`)이 닫혀 **L1 에 담당 열을 넣고 t58(`e023dd87`) 발견 3건을 접었다.**
> 2판에서 달라진 것만 보려면 **§12~§15** 를 읽으면 된다. §1~§11 은 초판 내용이며 수치 변경은 없다.

---

## 0. 이 카드가 낸 것

| 파일 | 내용 |
|---|---|
| `t57/connect-design.html` | **주 산출** — 드릴다운 HTML 1본(L0→L1→L2→L3 · 이음매 10개 접기/펼치기 · 라이트·다크) |
| `t57/connect-design.md` | md 짝(같은 내용 · 텍스트 정본) |
| `t57/verify.py` | 9게이트 검산기(읽기전용) |
| `t57/seams.py` · `t57/seams.json` | t51 원장에서 뜬 이음매(integrate) 센서스 — L1 수치의 출처 |
| `t57/pick.py` | 이음매별 원장 행 조회기(조사용) |
| `t57/verdict.md` | 이 문서(완료 신호) |

`screens.csv` **0행** · `*-decisions.md` **신규 0건**. 이 카드는 원장을 만들지 않는다 —
t51 원장 747행과 t53·t54·t55 verdict 를 **읽어서 설계 문서로 조립**하는 카드다(계약 보충 4: 이중계상 금지).

## 1. 워크트리·머지

지시대로 t55 워크트리에서 나와(`ExitWorktree keep` — **t55 는 미푸시 유일본이라 지우지 않았다**)
t57 워크트리를 만들고 브랜치를 `WT-connect-design-doc` 으로 바꾼 뒤 네 브랜치를 순서대로 `--no-ff` 머지했다.

| 커밋 | 병합 브랜치 | 들어온 것 |
|---|---|---|
| `2116530c` | `WT-unlisted-rejudge` | t48~t52 (t51 원장 747행 + t52 재판정) |
| `c0a83095` | `WT-pitstop-precedent` | t53 verdict |
| `3179805a` | `WT-mes-intake-seam` | t54 verdict + parts 5종 |
| `153628fc` | `WT-framework-compare` | t55 verdict + facts.csv |

충돌 0건(각 브랜치가 자기 카드 폴더만 건드린다).

## 2. 문서의 틀 — 9/16 「무엇을 이야기하는 문서인지 모르겠다」에 대한 답

원인은 데이터가 아니라 **틀**이었다(`07_rebaseline/S/CARDS-S.md:3`). 기능 행 목록만 있고
① 전체 그림 ② 프로세스 ③ 자기 할 일을 볼 자리가 없었다. 이 문서는 네 층으로 내려간다.

| 층 | 무엇 | 답하는 질문 |
|---|---|---|
| **L0** | 전체 흐름 1장 + CTO 10구간 대조표 | 「시스템이 서로 어떻게 이어지나」 |
| **L1** | 이음매 10 × 상태·미결·결정 주체 + 결정 사슬 | 「무엇을 정해야 하나」 |
| **L2** | 이음매별 재사용 / 신규 / 베끼면 안 되는 것 / 조용히 깨지는 계약 | 「어떻게 만드나」 |
| **L3** | 근거 부록 + 인용 승격표 + 미확인 8건 | 「이거 믿어도 되나」 |

**L0 은 지니 260919 확정을 그대로 척추로 삼는다 — 위젯이 몸통이다.**
쇼핑몰 페이지에 임베드된 위젯이 상품선택→가격계산→에디터→파일업로드→PitStop/MES 연계를 처리하고,
쇼핑몰은 그 값을 받아 카트·주문·결제를 한다.

**CTO 의 10구간 사고 틀에 맞췄다**(`docs/huni/후니-주문흐름-장바구니에서-MES까지_서희항_260908.html`).
L0 에 구간 대조표를 두어 CTO 가 자기 문서의 번호로 이 문서를 읽을 수 있게 했다.

## 3. L1 이음매 10 — 결과

| # | 이음매 | CTO 구간 | 상태 |
|---|---|---|---|
| ① | 위젯 → 쇼핑몰 (handoff) | 02 | 있음 |
| ② | 쇼핑몰 → 샵바이 (카트·주문·결제) | 03·04 | 부분 |
| ③ | 샵바이 → webadmin 주문 등록 | 05 | 부분 |
| ④ | 샵바이 웹훅 → webadmin | 07 | 부분(저장만) |
| ⑤ | 원고 승격 (S3 tmp → order) | 06 | 있음 |
| ⑥ | 원고 → PitStop 작업 지시 | 08 | **신규** |
| ⑦ | 샵바이 → MES 주문 수신 | 09 | **수신 신규 · 변환 재사용** |
| ⑧ | PitStop → MES 결과 인계 | 09 | **신규 · 선례 0** |
| ⑨ | Edicus 렌더 → MES | — | 있음 |
| ⑩ | MES → 샵바이 상태·송장 회신 | 10 | **미확인 이음매** |

카드가 지정한 최소 8개에 **③ 샵바이→webadmin 주문 등록**과 **⑤ 원고 승격**을 더했다 —
CTO 10구간의 05·06 에 해당하고 원장 근거가 있어서다. **MES→NAS→생산**은 있음(오픈 분모 밖)으로 부수 기재.

**「있는 줄 알았는데 없는 것」 = MES WebApi 주문 API** 를 L1 에 별도 블록으로 명시했다 —
Swagger 가 있어 「붙이면 되는 기존 API」로 읽히지만 ① 솔루션 빌드 대상이 아니고 ② 호출 SP 가 DB 에 0건이며
③ 주문 아이템이 저장되지 않고 ④ CRUD 6개가 전부 익명이다.

**결정 사슬**: `STD-ART-034`(연동방식)가 뿌리이고 그 아래 `035`(범위)→`033`(일정)이 달린다.
7건 중 **결정 주체가 정해진 것은 4건**(대표 채훈희 2 · PM 신우진 2)이고 **3건이 미정**이다.
**뿌리인 034 의 결정 주체가 미정**인 것이 ⑥·⑧ 작업량을 낼 수 없는 직접 원인이다.

## 4. 이 카드가 새로 적발한 것 — 금액 표현 단위가 세 문서에서 다르다

세 카드 어디에도 없던 발견이다. 원장 행 `t48:209` 의 evidence 를 직접 열어 확인하다 나왔다.

| 출처 | 적힌 것 |
|---|---|
| CTO 9/8 문서 | **1원**짜리 상품 × 금액만큼의 수량 (8/26 샵바이 회신으로 확정) |
| 원장 `t48:209` | 「**10원×수량**」 모델 정합 검증 — 미착수 |
| **스킨 실코드** | `"판매가 10원 상품 × 수량(=금액÷10)" 으로 결제 금액을 표현한다(후니 §11 확정)` · `후니 가격엔진이 청구액 1원 단위를 절삭 → 항상 10원 배수라 정확히 표현된다` — `/Users/innojini/Dev/huni-skin-shopby/src/lib/api/widget-order.ts:22-23,29-30` |

**돌고 있는 코드는 10원이다.** 원장 행이 evidence 로 댄 `README.md:70` 에는 숫자가 없고
「표시↔청구 불일치(별도 과제)」만 있다 — 직접 열어 확인했다(`:70-71`).

**판정하지 않는다.** 어느 쪽이 맞는지는 결정 안건으로 올렸다. 다만 10원 단가가
「가격엔진이 1원 단위를 절삭해 항상 10원 배수」라는 **전제 위에 서 있고**, 그 전제가 깨지는 상품이
하나라도 있으면 **청구액이 조용히 어긋난다**는 사실은 L2-① 에 적었다.

## 5. 리드 정정 3건 — 직접 재확인, 셋 다 리드가 옳다

계약 보충 4(교차 사실은 인용하는 쪽이 직접 `path:line` 확인)에 따라 세 건 모두 직접 열었다.

| # | 원 문서 | 정정 | 재확인 |
|---|---|---|---|
| 1 | t53 §3-7 `SQSMonitorService/Program.cs:120-121` | → **`:125-126`** | `:125` 가 `savePdfFileDirectory = Path.Combine(savePdfFileDirectory`, `:126` 이 이어지는 인자. `:120-121` 은 `{`·`try` 뿐 — **채택** |
| 2 | t55 F-50 `packages.config` md5 동일 **8** | → **9** | `md5 -q Framework/*/packages.config \| sort \| uniq -c` → `9 078435635886cf070ccb2b271e298a0c` — **채택** |
| 3 | t55 §2-B `grep "CRT\." Pitstop/*.cs` **0건** | → **4건** | 4건 전부 `namespace CRT.Framework.Pitstop` **선언**(`EnfocusReport.cs:9`·`PitStopConfiguration.cs:7`·`VariableSet.cs:11`·`PitStopEnums.cs:7`). **수치는 채택**하되 원 결론(「CRT 타입 **참조** 0」)은 유지 — 선언은 참조가 아니다 |

3번은 **정정이 결론을 뒤집지 않는 사례**로 문서에 그대로 적었다. 수치는 고치되 「753줄만 옮기면 된다」는 판단은 그대로다.

## 6. 인용 승격표 — t54 함정에 대한 구조적 대응

t54 §1-(1)이 짚은 것: **이 저장소들에서 `path:line` 축약 인용은 위험하다**
(`Program.cs` 17개 · `OrderDac.cs` 4개 · 열림에는 `ProgramOld.cs` 죽은 사본 두 벌).

본문은 읽기 쉽게 축약을 쓰되 **L3 에 승격표를 두어 28건 전부를 전체경로로 회수**했고,
검산기 게이트 **C3b** 가 ① 승격된 전체경로가 실재하는지 ② 본문 축약이 하나도 빠짐없이 회수됐는지를
기계 대조한다. 결과 **회수 못 한 축약 0건**.

`AppArguments.cs` 는 A판·B판의 작업 식별 규약이 서로 달라(숫자 jobId 접두 ↔ 파일명 `yyMMdd_코드`)
승격표에서 **둘을 갈라 두었다** — 섞어 인용하면 결론이 뒤집힌다.

## 7. 검산 — 실행한 명령과 출력

```
$ python3 t57/verify.py
PASS C1 네 층 구조 — md 4/4 · html 4/4
PASS C2 이음매 10 전수 — md 누락 0 · html 누락 0 · 상태값 이탈 0
PASS C3 인용 실재 — 유일해소·통과 21 / 축약(승격표로 회수) 40 / 초과·부존재 0
PASS C3b 인용 승격표 — 승격 28건 · 실재하지 않는 경로 0 · 회수 못 한 축약 0
PASS C4 원장 수치 — 행 747 · 시스템 8개 대조 전건 일치
PASS C5 담당 배정 0 — 배정 문구 0건 (담당 열은 t56 결과 전까지 비운다)
PASS C6 날짜·작업량 추정 0 — 추정 표현 0건 []
PASS C7 자격증명 전사 0 — 의심 토큰 0건 (키 이름·파일 위치만 적는다)
PASS C8 md↔html 정합 — 불일치 0

총 9/9 게이트 통과
```

게이트 뜻: **C1** 네 층이 md·html 양쪽에 실재 · **C2** 이음매 10개 전수 수록 + 상태값이 4값 이탈 0 ·
**C3** 유일 해소되는 인용의 줄번호가 파일 길이 안에 있음(**추정 해소를 쓰지 않는다** — t54 교훈:
추정 해소는 없는 결함을 만들어 낸다) · **C3b** 축약 전건이 승격표로 회수 · **C4** 원장 수치를
`merged.csv` 에서 **다시 세어** 대조 · **C5** 담당 배정 문구 0 · **C6** 날짜·작업량 추정 0 ·
**C7** 자격증명 값 전사 0 · **C8** md↔html 핵심 수치·경고 정합.

```
$ python3 t57/seams.py        # L1 수치의 출처 — integrate 229행의 이음매 센서스
huni-mall <-> shopby  n=104 · mes <-> shopby  n=11 (전건 미착수·owner_side=미정) ·
pitstop <-> pitstop  n=6 · mes <-> pitstop  n=1 · edicus <-> mes  n=1 · s3 <-> webadmin  n=6 …
```

그 밖에 직접 실행한 확인 명령:
```
sed -n '115,130p' <열림>/CRT.Yeolim.SQSMonitorService/Program.cs          # 리드 정정 1
md5 -q <후니MES>/Framework/*/packages.config | sort | uniq -c              # 리드 정정 2 → 9
grep -n "CRT\." <열림>/CRT.Framework.Pitstop/*.cs                          # 리드 정정 3 → 4건(전부 namespace)
sed -n '60,80p' /Users/innojini/Dev/huni-skin-shopby/README.md             # :70-71 알려진 이슈 확인
grep -rn "10원\|1원\|unitPrice" .../src/lib/api/widget-order.ts            # :22-23,29-30 → 10원 확정
```

## 8. 계약 대응

| 조항 | 처리 |
|---|---|
| 보충 1 (오픈 분모 밖) | 해당 없음 — `screens.csv` 0행. MES 기존 연동(카페24·우커머스·성원·이카운트·NAS)은 L1·L2 에서 **「오픈 분모 밖 기존 자산」으로 명시**하고 선례로만 인용 |
| 보충 2 (`config` 판정) | 해당 없음 |
| 보충 3 (`work_type` 축) | 해당 없음. 단 「`status=완료` 는 코드가 있다이지 오픈 시나리오에서 돈다가 아니다」를 L3 에 [HARD] 로 적었다 |
| 보충 4 (결정·관리 안건은 행이 아니다) | 결정 안건을 **행으로 만들지 않고** L1 결정 사슬에 순서로만 그렸다. 원본 좌표는 `../t50/pitstop-decisions.md`(7) · `../t50/edicus-decisions.md`(2) |
| 레인 간 교차 사실 | 리드 정정 3건 + 스킨 README·`widget-order.ts` + 열림·MES 인용을 **직접 열어 확인**했다(§5·§7) |
| `[HARD]` 열림 코드 | 「후니 status 의 근거가 아니다 · pitstop 23행은 미착수/미확인 그대로」를 md·html 양쪽 상단에 박았다 |

## 9. 확인 못 한 것 — 미확인 8건

| # | 미확인 | 왜 |
|---|---|---|
| 1 | 금액 표현 단위 정본(1원 vs 10원) | 세 문서가 다르다 — 결정 안건 |
| 2 | 자사몰이 결제 직후 주문등록 창구를 실제로 부르는가 | 라이브 미확인 |
| 3 | `USP_ORD_ORDER_FILES_D` 의 `FILE_TYP` 필터 유무 | 라이브 SP 본문 미접속 |
| 4 | 샵바이의 무엇이 `USR_ORD_CD`/`MARKET_ORD_ITEM_NO` 가 되는가 | 스펙에도 코드에도 없다 |
| 5 | PitStop Server 23 라이선스·동시성·사양 | Enfocus 문서 영역 |
| 6 | 후니 워커 서버에 PitStop 이 같이 얹히는가 | 서버 실사 필요 |
| 7 | MES → webadmin 상태 push 경로 | 세 카드 모두 미실측 |
| 8 | `CommandHandler.UpdateTracking` 빈 본문이 의도인지 미구현인지 | 코드로 알 수 없다 |

**「없다」는 전부 「찾지 못했다」다.** 각 카드 verdict 에 돌린 grep 패턴이 적혀 있고,
패턴 밖 구현이 있으면 이 판정은 틀린다.

## 10. 잔여 위험 · 다음 카드로 넘길 것

1. **담당 열이 비어 있다.** S7-A(t56 · lane-2 [591296])가 담당 판정을 내면 L1 표에 열을 추가해야 문서가 완성된다.
   현재 적힌 「결정 주체」는 9/17 원장 `owner_name` 값이고 **작업 담당과 다른 축**이다.
2. **금액 단위 정본**(§4)은 결정이 나면 L2-① 과 원장 `t48:209`·`STD-PAY-031` 을 함께 고쳐야 한다.
3. **프린팅머니 10행 재기준**은 L3 에 기록만 했다. 실제 행 교체는 리드 브리프 §8 의 P1 카드 몫이다.
4. **이 문서는 코드를 읽었을 뿐 CTO·MES 담당(서희항 대표)과 대조하지 않았다.** 정확성은 그 확인을 받아야 확정된다.
5. **⑩ MES→샵바이 회신**이 가장 얇은 자리다. 세 카드가 모두 이 방향을 실측하지 않았으므로
   다음 라운드에서 **MES→webadmin push 경로 전용 조사**가 필요하다.

## 11. 금지 준수

DB write 0 · 라이브 DB 접속 0 · 라이브 COMMIT 0 · 세 대상 저장소(`CRT.DigitalEdit.V2` ·
`TS.BackOffice.Huni` · `huni-skin-shopby`) **쓰기 0 · 빌드 0 · 실행 0 · git 조작 0** ·
자격증명 값 전사 0(키 이름·파일 위치만) · **날짜 추정 0** · 작업량(일수) 추정 0 · **푸시 0** ·
`PRINTMONEY-BRIEF.md` 읽기만(미트래킹 유일본 — 워크트리로 복사 0) ·
t55 워크트리 보존(`ExitWorktree keep`) · 담당 배정 0.

---

# 2판 — 담당 열 반영 (t56 `5bb97cfd` · t58 `e023dd87`)

## 12. 담당 열 — 넣었다

입력 = `.claude/worktrees/t56` · 브랜치 `WT-role-todo-rejudge` · 커밋 `5bb97cfd` · `t56/rejudge.csv` **735행**.
**직접 열어 확인**했다(계약 보충 4). 이음매별 원장 행 → `plan_row_id` → t56 판정을 모으는 조회기를 새로 냈다
(`t57/owners.py` · 산출 `t57/owners.json`).

| # | 이음매 | 담당 | 분모 | 근거 |
|---|---|---|---:|---|
| ① | 위젯 → 쇼핑몰 | **서희항**(위젯·API) + **김동학**(스킨 호출부) | 4/4 | `manual-read`·`merged-owner_side` |
| ② | 쇼핑몰 → 샵바이 | **김동학** | 98/102 | `merged-owner_side` |
| ③ | 샵바이 → webadmin 주문등록 | **서희항** | 5/6 | `merged-owner_side` |
| ④ | 샵바이 웹훅 → webadmin | **서희항** | 2/2 | `merged-owner_side` |
| ⑤ | 원고 승격 | **서희항** | 5/6 | `merged-owner_side` |
| ⑥ | 원고 → PitStop | **서희항** | 5/6 | `merged-owner_side` |
| ⑦ | 샵바이 → MES | **미정 + 최숙진** ⚠ | **1/11** | 10행이 `plan_row_id=NEW` — t56 이 판정할 행이 없다 |
| ⑧ | PitStop → MES | **서희항** ⚠ | 1/1 | 이음매 전체가 원장 1행뿐 |
| ⑨ | Edicus 렌더 → MES | **외부(상대측 회신 대기)** ⚠ | 1/1 | 4경계 밖 — 담당이 아니라 **상태** |
| ⑩ | MES → 샵바이 상태·송장 | **서희항** | 6/6 | 그중 **4행이 김동학→서희항 재배정 제안** |

**리드 지적 ①(약한 근거 25%) 처리**: t56 `judged_by` 분포를 직접 세었다 —
`merged-owner_side` 366 · `evidence-path` 177 · **`rule-track` 95 + `rule-step` 89 = 184(25%)** · `manual-read` 8.
**이음매 담당은 대부분 강한 근거에서 나왔고**, 얇은 세 칸(⑦·⑧·⑨)에만 ⚠ 와 분모를 붙여
**「배정이 아니라 제안」**으로 명시했다. 근거 강도를 문서에 그대로 노출한다.

**4경계 이탈 2건**(`미정` · `외부(상대측 회신 대기)`)은 **사람이 아니라 상태**이므로 그대로 두었다 —
사람 이름을 채우면 없는 배정을 만드는 것이 된다.

## 13. 결정 사슬 — t56 이 뿌리를 풀지 못했고, 상충 2건이 새로 드러났다

결정 사슬 9건 중 **7건이 `rule-track`/`rule-step`(약한 근거)** 이다. 이유가 있다 —
결정 안건은 화면도 기능도 없어 `merged-owner_side`(코드가 사는 쪽)로 판정할 수가 없다.

| 안건 | t50(9/17 원장) | t56 | 결과 |
|---|---|---|---|
| `STD-ART-034` 연동방식 **(사슬 뿌리)** | 미정 | 미정 ⚠`rule-track` | **여전히 미정 — t56 이 해소하지 못했다** |
| `STD-ART-033` 일정 상정 | 미정 | 미정 ⚠ | 미정 |
| `STD-ART-035` 범위 산정 | 미정 | 미정 | 미정 |
| `STD-MYP-024` 연동 담당 | 미정 | 미정 ⚠ | 미정 (선행 `STD-ART-016` → 서희항 재배정) |
| `BLK-S2-4` 구매·라이선스 | 대표(채훈희) | 대표(구매) ⚠ | 일치 |
| **`T4-3` 파일 검수 경로** | **대표(채훈희)** | **서희항+신우진** ⚠ | **상충 — 병기** |
| `STD-ADO-010` 검수 게이트 | 미정(최숙진 추정) | 최숙진 ⚠`rule-step` | 추정이 확인됨(약한 근거) |
| `STD-MYP-031` 보관함 저장 시점 | 신우진(PM) | 신우진 ⚠ | 일치 |
| **`STD-OPT-053` 개발 주체** | **신우진(PM)** | **서희항 재배정** ⚠ | **상충 — 병기** |

**상충 2건을 이 문서가 닫지 않았다.** 둘 다 t56 근거가 `rule-track` 이라 9/17 원장 `owner_name` 을
덮을 만큼 강하지 않다. 두 판본을 병기하고 **지니 확정이 필요한 자리**로 표시했다.

## 14. t58 발견 3건 — 직접 재확인 후 접음

리드가 알려 준 t58(`e023dd87`) 3건을 전부 **직접 열어 확인**하고 반영했다.

| 발견 | 이 세션 재확인 | 접은 곳 |
|---|---|---|
| **PitStop 코드가 webadmin 에도 0** | `raw/webadmin` 의 `*.py`·`*.js`·`*.html` 전수 grep **0건**. 등장은 `docs/**` 7 · `.planning/**` 5 · `sql/88_ord_artworks.sql` 뿐 — **직접 실행 확인** | L2-⑧ — **세 저장소 전부 0** |
| **handoff 는 우리 쪽 완료 · 부르는 스킨 쪽 3행 미착수** | `t48:192`(`NEW`) · `t48:193`(`STD-ORD-030`) · `t48:239`(`STD-ORD-029`) 전부 `미착수`. 셋 다 담당 **김동학**(t56). 추가로 `verify` 응답에 `handoff_id` 없음 확인 — `widget_api.py:5006-5007` 반환이 `{ok, valid, payload\|reason}` 뿐 | L2-① — 「완료 4행을 오픈 준비 완료로 읽으면 안 된다」 |
| **`allow_domains` 미설정 = Origin 통과인데 등록 수단 없음** | `_origin_allowed` docstring 「미설정=통과」 + `if not doms: return True`(`widget_api.py:709-711`) · `tools/issue_site_key.py:35-37` UPDATE 가 `site_key`·`upd_dt` 만 씀 — 둘 다 직접 확인 | L2-① 조용히 깨지는 계약(신규) |

세 번째는 **초판에 없던 「조용히 깨지는 계약」이 하나 더 늘어난 것**이다 —
예외가 나지 않고 Origin 검사만 꺼진다.

## 15. 검산 게이트 C5 교체 — 기록

**초판 C5 = 「담당 배정 문구 0」**이었다(t56 대기 중이라 배정 자체를 금지하는 게이트).
담당 열이 들어오면 **그 게이트가 그대로 FAIL 한다.** 그래서 교체했고, 교체 사실을 여기 남긴다.

**새 C5 = 담당 열 정합** — 세 가지를 본다:
① 이음매 10행 전건에 담당 값이 있는가 ② 값이 지니 확정 4경계(서희항·김동학·최숙진·신우진) +
허용 상태어(`미정`·`외부(상대측 회신 대기)`·`대표(구매)`) 안인가
③ **t56 `rejudge.csv` 가 실제로 그 값을 판정했는가**(문서가 담당을 지어내지 않았는가).

**게이트가 장식이 아님을 음성 테스트로 확인했다**(스크래치패드 사본으로 수행 · 원본 무수정):

```
OK   경계 밖 사람 이름 주입: FAIL C5 — ② 담당 칸에서 경계 안 값을 못 찾음: '**박철수**'
OK   담당 칸 비우기:        FAIL C5 — ② 담당 칸에서 경계 안 값을 못 찾음: ' '
OK   원복 후:               PASS C5
C5 음성 테스트 통과 — 게이트가 실제로 잡는다
```

**검산기 자체 결함 2건도 이 과정에서 잡아 고쳤다**(정직하게 남긴다):
- **C5 초안**의 담당 토큰 정규식 `[가-힣()·\s]{2,20}` 이 「서희항(위젯·API)」를 한 덩어리로 물어
  **정상 문서를 FAIL** 시켰다 → 허용값 부분일치로 바꿨다.
- **C6** 의 D-day 패턴이 **`STD-ORD-030` 의 꼬리**에 걸려 오탐 4건을 냈다
  → 앞에 영숫자가 오면 제외하도록 고쳤다. **둘 다 문서가 아니라 게이트가 틀린 것이었다.**

## 16. 2판 검산 — 실행한 명령과 출력

```
$ python3 t57/verify.py
PASS C1 네 층 구조 — md 4/4 · html 4/4
PASS C2 이음매 10 전수 — md 누락 0 · html 누락 0 · 상태값 이탈 0
PASS C3 인용 실재 — 유일해소·통과 23 / 축약(승격표로 회수) 42 / 초과·부존재 0
PASS C3b 인용 승격표 — 승격 29건 · 실재하지 않는 경로 0 · 회수 못 한 축약 0
PASS C4 원장 수치 — 행 747 · 시스템 8개 대조 전건 일치
PASS C5 담당 열 정합 — 이음매 10/10 · 등장 담당 ['김동학','미정','서희항','외부(상대측 회신 대기)','최숙진']
                       · 4경계+상태어 이탈 0 · t56 미근거 0
PASS C6 날짜·작업량 추정 0 — 추정 표현 0건 []
PASS C7 자격증명 전사 0 — 의심 토큰 0건 (키 이름·파일 위치만 적는다)
PASS C8 md↔html 정합 — 불일치 0

총 9/9 게이트 통과
```

```
$ python3 t57/owners.py        # 이음매별 담당 — L1 담당 열의 출처
t56 원장 735행 · 판정 분포 {'담당맞음': 457, '재배정': 141, '미정': 63, 'provided': 37, '분할': 36, '불필요': 1}
```

리드가 알려 준 t56 집계(담당맞음 457 · 재배정 141 · 분할 36 · 미정 63 · provided 37 · 불필요 1)와
**독립 재집계가 전건 일치**했다.

## 17. 2판 기준 미확인 — 8건 → **12건**

초판 8건에 4건이 늘었다(전부 담당·t58 관련):

| # | 미확인 | 왜 |
|---|---|---|
| 9 | ⑦ 샵바이→MES 의 이음매 담당 | 11행 중 10행이 `NEW` — t56 이 판정할 원장 행이 없다 |
| 10 | `T4-3`·`STD-OPT-053` 의 담당 | t50 ↔ t56 상충 · t56 근거가 `rule-track`(약함) |
| 11 | ⑨ 의 「외부(상대측 회신 대기)」가 누구인가 | 상대측 회신 전 |
| 12 | `allow_domains` 가 신규몰에 실제로 채워지는가 | 채울 수단(화면·도구)이 없어 직접 DB UPDATE 외 경로 미확인 |

## 18. 2판 잔여 위험 — 지니 결정이 필요한 것 3가지

1. **금액 표현 단위 정본**(초판 §4) — 1원 vs 10원. t56 도 `STD-PAY-031` 을 `manual-read` 로
   같은 파일(`widget-order.ts:26-35`)을 읽어 판정했다 — **두 레인이 독립적으로 같은 자리에 닿았다.**
2. **담당 상충 2건**(`T4-3` · `STD-OPT-053`) — 병기해 두었고 확정이 필요하다.
3. **`STD-ART-034`(연동방식)** — 사슬의 뿌리이며 t56 이 풀지 못했다. **결정 주체부터 정해야 한다.**

## 19. 2판 산출 변경

| 파일 | 변경 |
|---|---|
| `t57/connect-design.md` · `.html` | L1 담당 열 + 「담당 열을 읽는 법」 · 결정 사슬 t56 대조 · L2-① t58 3건 · L2-⑧ 세 저장소 0 · L3 입력 카드 2행 추가 · 승격표 1건 추가 · 미확인 12 · 「담당 열 — 채워졌다」로 교체 |
| `t57/owners.py` · `owners.json` | **신규** — 이음매별 담당 조회기 |
| `t57/verify.py` | C5 교체(배정금지 → 담당 정합) · C6 D-day 오탐 수정 |
| `t57/verdict.md` | 이 2판 절(§12~§19) |

**금지 준수(2판)**: t56·t58 워크트리 **읽기만**(git 조작 0 · 쓰기 0) · `raw/webadmin` **읽기만**(쓰기 0) ·
DB write 0 · 라이브 접속 0 · 날짜·작업량 추정 0 · **담당을 스스로 배정하지 않음**(t56 판정 전재) ·
상충 임의 종결 0 · 푸시 0.
