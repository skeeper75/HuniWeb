# L6 progress — 엑셀 5시트 생성기 + 인터랙티브 아티팩트 생성기 + LEDGER.md

> L6 → **L6b**(생성기 보강: `오픈차단여부` 반영) 순서로 이어진다. L6b 는 이 파일 아래쪽.

카드: `L0/CARDS-P.md` §L6 · 세션 `sync-huniweb` · 2026-09-02

## 완료

완료조건 ①~⑦ 전건 PASS. 아래 대조표에 증거를 적었다.

## 산출물

| 산출 | 경로 | 크기 |
|---|---|---|
| 엑셀 생성기 | `L6/build_xlsx.py` | 실행 시 검산 8건 출력 · 실패 시 exit 1 |
| 아티팩트 생성기 | `L6/build_artifact.py` | 실행 시 검산 9건 출력 · 실패 시 exit 1 |
| 엑셀 5시트 | `docs/huni/후니프린팅_통합IA_일정_역할분담_260902.xlsx` | 64KB |
| 대시보드 | `L6/runway-dashboard.html` | 277KB (단일 파일) |
| 대시보드 데이터 | `L6/runway-data.json` | 283KB |
| 원장 요약 | `07_rebaseline/LEDGER.md` | 5.4KB |
| 사용법 | `L6/README.md` | 108줄 |
| 렌더 캡처 | `L6/shots/` | 8장 |

## 완료조건 대조표

| # | 조건 | 판정 | 증거 |
|---|---|---|---|
| ① | xlsx 5시트 전부 존재 · 시트명 · 열 구조 260616 과 일치(openpyxl 기계 대조) | **PASS** | `build_xlsx.py` 검산 ① — 시트명 `['00_읽는법','01_역할정의','02_IA마스터','03_페이즈일정','04_진행현황']` 원본과 동일 · `02_IA마스터` 헤더 11열 문자열 동일 · `00`3열 `01`6열 `03`5열 일치 |
| ② | 아티팩트 항목 수 = 원장 행 수(504) | **PASS** | `build_artifact.py` 검산 ② — HTML 안 `const DATA` 를 다시 JSON 파싱해 `ledger` 504 확인. 브라우저 실측 `#t-ledger tbody tr` = **504** · 카운터 「504 / 504행」 |
| ③ | 라이트/다크 양쪽 렌더 캡처 2장 | **PASS** | `shots/01-light.png`(bg `rgb(255,255,255)`) · `shots/02-dark.png`. 추가 `03-dep-mermaid` `04-gates` `07-scen-dark` `08-dep-dark` |
| ④ | 체크박스 새로고침 후 유지(캡처) | **PASS** | 체크 0 → 앞 3행 클릭 → `localStorage['huni-runway-checks']` = `{"STD-CAT-001":true,"STD-CAT-002":true,"F-088;IA-088;SCOPE-088":true}` → 새로고침 후 `input:checked` = **3** · KPI 「내가 닫은 것 3」. `shots/05-check-before.png`(0) · `shots/06-check-after-reload.png`(3) |
| ⑤ | mermaid `<br/>` 이스케이프 검증 통과 | **PASS** | 검산 ⑤ — 소스형 `<br/>` 17회 · 비정규 `<br…>` 0 · 미이스케이프 `&` 0 · 라벨 따옴표 균형 OK. 주입형 `<pre>` 안 `&lt;br/&gt;` 17회 · 날것 `<br>` **0**. 브라우저 콘솔 에러 **0** · `pre.mermaid svg` 1개 렌더 |
| ⑥ | 원장에 없는 항목 창작 0(기계) | **PASS** | 두 생성기 공통 — `기능` 문자열 전건이 원장 유래(원장 밖 0건) · 엑셀 비고에 `std_id` 없는 행 0건 · 02 데이터행 504 = 원장 504 |
| ⑦ | 재실행 명령 1줄로 재생성 확인 | **PASS** | 산출 4종을 **삭제하고** README 의 한 줄을 실행 → 4종 전부 재생성 · 두 생성기 exit 0 · 검산 전건 ✓ (아래 § 재실행 실증) |

## 재실행 실증

```bash
rm -f L6/runway-dashboard.html L6/runway-data.json LEDGER.md docs/huni/…_260902.xlsx
L=L5/unified-ledger-assigned.csv && python3 L6/build_xlsx.py --ledger $L && python3 L6/build_artifact.py --ledger $L
```

결과: `exit 0 / 0` · 재생성 `xlsx 64,027B · html 277,725B · json 282,839B · LEDGER.md 5,377B` · 검산 17줄 전부 ✓.
**P1 이 v2 원장을 내면 `$L` 만 `L5/P1/unified-ledger-v2.csv` 로 바꾼다.**

## 260616 원본 무수정

`docs/huni/후니프린팅_통합IA_일정_역할분담_260616.xlsx` mtime = **Jun 16 08:50** (작업 전후 동일).
생성기가 이 파일을 여는 곳은 검산(시트명·열 구조 대조) 한 곳뿐이고 `openpyxl.load_workbook` 읽기만 한다.
`git status` 상 수정(M) 항목 0건 — 새로 만든 것만 있다.

## 260616 양식과 의도적으로 다른 3가지 (전부 「지어내지 않기」 때문)

| # | 다른 점 | 이유 |
|---|---|---|
| 1 | `02_IA마스터` 의 **우선순위 · 개발규모 · Phase** 3열이 전건 「미판정」 | 원장에 그 세 축이 **없다**. 채우려면 지어내야 한다. P1 이 「오픈차단여부」를 신설하면 우선순위가 매핑 가능해진다 |
| 2 | `04_진행현황` 첫 집계 축을 **우선순위 → 진행상태** | 위와 같은 이유. 카드 §L6-1 도 「담당×상태 집계」를 지시 |
| 3 | `03_페이즈일정` 이 Phase 1~3 → **게이트 G0~G5** | 카드 §L6-1 지시. 내용은 `L5/O2/gates.md` 를 기계 파싱(게이트 6 · 진입 5 · 이탈 **31**) |

세 가지 다 `00_읽는법` 시트와 `L6/README.md` 에 적었다.

## 판정하지 않은 것 (미확인)

- **`done_criteria` 504행 전건 빈칸.** 원장 현재 상태 그대로다. 대시보드는 이 행을 「완료조건 미기재」 배지로 표시하고 엑셀은 「미판정」으로 둔다. **P1 이 채운다.**
- **상태 미판정 143행**(생산·공정 127 포함). 그대로 실었다. P1 판정 대상.
- **`03_페이즈일정` 의 담당 배정**은 이탈 조건 문장의 키워드 규칙표(`GATE_OWNER_RULES`)로 넣은 것이다. 사람이 검토한 배정이 아니다 — 조건 원문과 「확인처」는 `L5/O2/gates.md` 가 권위다.
- **G3·G4 의 「목표」 칸**에 `★초안 9/23 → 9/22 …` 가 들어간다. `gates.md` §2 한눈에 블록이 그 두 줄에 목표 대신 변경 사유를 적었기 때문이고, 원문 그대로다.
- **G5 「목표」가 `—`** — `gates.md` §2 의 G5 줄에 `─` 뒤 설명이 없다. 지어내지 않았다.
- **캡처는 로컬 http 서버(127.0.0.1:8791) 기준**이다. `file://` 은 브라우저가 막는다. 서버는 검증 후 종료했다.

## 작업 중 잡은 결함 4건 (전부 실측으로 발견)

| # | 증상 | 원인 | 고친 것 |
|---|---|---|---|
| 1 | 게이트 목표 칸에 「1. G0 이탈 조건 6개 전부 충족」이 들어감 · 이탈 조건 31→28 누락 · 백틱 안 단어 증발(「원장 전건에 **가 채워졌다**」) | 파서가 진입 조건 첫 줄을 목표로 집었고, `**이탈 조건 (= …)**` 형태를 못 잡았고, 백틱을 내용째 지웠다 | 목표는 §2 한눈에 블록에서 · 이탈 헤더는 정규식 · 백틱은 기호만 벗김 |
| 2 | mermaid 콘솔 에러 20건 `translate(undefined, NaN)` | 숨은 탭에서 도식을 그렸다(폭·높이 0) + mermaid 자동실행이 켜져 있었다 | `startOnLoad:false` 를 즉시 끄고, 탭이 열릴 때만 · `offsetParent` 있을 때만 렌더 |
| 3 | 도식 라벨이 줄바꿈 없이 붙음(「이니시스 PG차단」) | `<pre class="mermaid">` 안 날것 `<br/>` 를 **HTML 파서가 진짜 BR 태그로 먹어** `textContent` 에서 사라짐 | `&lt;br/&gt;` 로 이스케이프 주입 + 검산에 주입형 검사 추가. **카드가 짚은 그 항목이 실제로 재현됐다** |
| 4 | 원장 표 열 폭 붕괴(「영역」이 한 글자씩 세로로 흐름) | `table-layout` 미지정 | 8열 폭 고정 |

부수 함정 2건(다음 세션용): `file://` 스킴은 gstack 이 막는다 → 로컬 http 서버. 캡처는 `--viewport` 없으면 **세로 82,729px** 전체 페이지가 나온다.

## 하지 않은 것

- 커밋·푸시 **0건**(카드 지시).
- `L6/` · `07_rebaseline/LEDGER.md` · `docs/huni/…_260902.xlsx` **밖에는 아무것도 쓰지 않았다.**
- 원장 내용은 **한 글자도 바꾸지 않았다** — 읽기만 했다.

---

# L6b — 생성기 보강: `오픈차단여부` 열 반영

카드: `L0/CARDS-P.md` §L6b · 세션 `sync-huniweb` · 2026-09-02 · 원장 `L5/P1/unified-ledger-v2.csv`(16열 · 504행)

## 완료

완료조건 ①~⑤ 전건 PASS. 기존 ①~⑦ 회귀 0.

## 무엇을 고쳤나

| # | 파일 | 고친 것 |
|---|---|---|
| ① | `build_xlsx.py` | `02_IA마스터` **우선순위 ← `오픈차단여부`** (값 그대로 · 변환표 없음). `block_of()` 는 v1 원장(열 없음)이면 「미판정」을 낸다 — **하위호환** |
| ① | `build_xlsx.py` | `04_진행현황` 에 **⑤ 오픈차단여부 × 담당** · **⑥ 차단(외부) 전건** 두 표 추가 |
| ① | `build_xlsx.py` | `00_읽는법` 「우선순위」 행을 새 뜻으로 고쳐 씀 |
| ② | `build_artifact.py` | 원장 항목에 **`block` 키** 추가 · 원장 탭 **「차단 등급」 필터** · KPI **「차단(외부) 8」·「오픈 무관 50」** · 표에 **차단 열** 신설 · 차단 8행 **왼쪽 빨간 띠 + 「차단」 배지** |
| ② | `build_artifact.py` | `LEDGER.md` 에 **오픈차단여부 × 담당** 표 + **차단(외부) 전건** 표 추가 |
| ③ | — | 두 생성기를 v2 원장으로 재실행 · 캡처 10장 갱신 |
| ④ | — | 이 절 |

## 오픈차단여부 분포 (v2 원장 · 기계 집계)

| 담당 | 차단(외부) | 작업 항목 | 오픈 무관 | 미판정 | 합계 |
|---|---|---|---|---|---|
| PM | 2 | 26 | 0 | 0 | 28 |
| PM(작성)+쇼핑개발 | 0 | 4 | 0 | 0 | 4 |
| PM(정책)+인쇄개발(구현) | 0 | 1 | 0 | 0 | 1 |
| 쇼핑개발 | 6 | 203 | 0 | 2 | 211 |
| 인쇄개발 | 0 | 203 | 50 | 7 | 260 |
| **합계** | **8** | **437** | **50** | **9** | **504** |

차단(외부) 8행 — `STD-PAY-001` 신용카드 결제(PG) · `STD-PAY-008` 복합결제 · `STD-PAY-011` PG 리다이렉트·확정 콜백 · `STD-MEM-020` 기존 회원 데이터 이관 · `STD-MYP-007` 프린팅머니 충전 · `STD-MYP-009` 기존 프린트머니 잔액 이관 · `STD-FIN-008` PG 정산 대사 · `STD-SYS-004` PG 연동 설정.
**8행 중 6행이 PG, 2행이 구 DB 이관이다.** 전건 돈여부 Y.

## 완료조건 대조표 (L6b)

| # | 조건 | 판정 | 증거 |
|---|---|---|---|
| ① | xlsx 우선순위 빈칸 0(= 오픈차단여부 값) | **PASS** | `build_xlsx.py` 검산 L6b — 빈칸 **0건** · 504행 값이 원장 `오픈차단여부` 와 **전건 일치** · 분포 `{'작업 항목': 437, '차단(외부)': 8, '오픈 무관': 50, '미판정': 9}` |
| ② | 대시보드 `block` 키 504행 | **PASS** | `build_artifact.py` 검산 L6b — HTML 안 `DATA.ledger` 재파싱 `block` 보유 **504/504** · 분포가 원장과 일치. 브라우저 실측 `tr.blocked` **8행** · KPI 「차단(외부) 8」 |
| ③ | 재실행 exit 0 | **PASS** | 산출 4종 삭제 후 v2 원장으로 재실행 → `xlsx=0 artifact=0` · 검산 20줄 전부 ✓ · ✗ **0건** |
| ④ | 캡처 2장 | **PASS** | `shots/01-light.png` · `02-dark.png` 갱신. 추가 `09-blocked-filter.png`(차단 필터 8/504) · `10-blocked-dark.png` |
| ⑤ | 기존 ①~⑦ 회귀 없음(기계) | **PASS** | 같은 재실행 로그에서 — 시트명 5종 ✓ · 11열 ✓ · 00/01/03 열 수 ✓ · 데이터행 504=504 ✓ · 원장 밖 기능 문자열 0 ✓ · 비고 std_id 누락 0 ✓ · 대시보드 항목 504 ✓ · mermaid `&lt;br/&gt;` 17회·날것 0 ✓ · CDN 화이트리스트 3건 ✓ · 콘솔 에러 **0** · 체크 3건 새로고침 유지 ✓ |

## 재실행 실증

```bash
rm -f L6/runway-dashboard.html L6/runway-data.json LEDGER.md docs/huni/…_260902.xlsx
L=L5/P1/unified-ledger-v2.csv && python3 L6/build_xlsx.py --ledger $L && python3 L6/build_artifact.py --ledger $L
```

`exit 0 / 0` · 재생성 `xlsx 77,607B · html 415,508B · json 420,824B · LEDGER.md 6,779B`.
v1 대비 커진 것은 v2 원장이 `done_criteria` 504행을 채워 왔기 때문이다.

## 260616 원본 무수정 · 쓰기 범위

`docs/huni/…_260616.xlsx` mtime = **Jun 16 08:50** (변화 없음). 커밋 **0건**.
쓴 곳: `L6/` · `docs/huni/…_260902.xlsx` · `07_rebaseline/LEDGER.md`.
**LEDGER.md 는 `build_artifact.py` 가 소유·재생성하는 파일이라 재실행이 불가피하게 덮어쓴다** — 카드의 쓰기 범위 문구(`L6/` 와 xlsx)보다 한 파일 넓다. L6 카드가 이 파일을 L6 산출로 지정했기 때문이며, 내용은 전부 원장 기계 집계다.

## 판정하지 않은 것 (미확인)

- **`개발규모`·`Phase` 2열은 여전히 미판정.** v2 원장에도 그 두 축은 없다. 지어내지 않았다.
- **`오픈차단여부` 미판정 9행**(쇼핑개발 2 · 인쇄개발 7)을 그대로 실었다. 우선순위 열에도 「미판정」으로 나온다.
- **차단 등급의 옳고 그름은 판정하지 않았다.** P1 이 매긴 값을 그대로 옮겼을 뿐이다. 등급 근거는 `L5/P1/judged-143.md` · `feasibility-verdict-v2.md` 가 권위다.
- **KPI 「차단(외부)」의 뜻이 바뀌었다** — L6 에서는 의존 그래프의 외부 노드 수(14)였고, L6b 부터는 **원장 차단 행 수(8)** 다. 그래프 쪽 14는 「의존·임계경로」 탭의 표에 그대로 남아 있고 제목을 「차단 등급 외부 의존 (의존 그래프 노드)」로 구분했다.
