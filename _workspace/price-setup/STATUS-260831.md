# 가격 배선 교정 — 세션 정리와 다음 세션 지침 (2026-08-31)

> **다음 세션은 이 파일부터 읽는다.** `STATUS-260830-4.md` 는 이 파일로 대체된다
> (거기 §5 「다음에 할 일」은 상당수 처리됐다 — §3 참조).

---

## 1. [HARD] 작업 규약 — 이번 세션에서 지니가 지적한 것

**이 셋을 먼저 읽는다. 어겨서 라이브를 원복한 일이 실제로 있었다.**

1. **손댈 화면은 「가격공식 · 가격구성요소 · 가격뷰어」 셋뿐이다.**
   **상품뷰어와 위젯은 실무진의 영역** — 조회는 하되 **절대 쓰지 않는다.**
2. **지니가 사실을 말한 것과 고치라고 지시한 것은 다르다.**
   범위 밖 사실은 **보고까지가 내 일**이다.
3. **범위 밖 교정안을 `AskUserQuestion` 선택지로 올리지 않는다.**
   올리는 순간 범위 확장이 승인처럼 보인다.
   (`STATUS-260830-4.md` §6-3 — 이번에 어겨 `015a51c0` → `8df90a5b` 로 원복했다.)

이전 세션 규약도 그대로 유효하다:
4. **조사 순서: 상품뷰어 → 가격구성요소/공식/뷰어 → 위젯.**
5. **한 층 고치면 다시 재본 뒤** 다음 층을 판단한다.
6. **원본(권위 엑셀 셀 + pricing.py) 먼저 읽는다.** 재질문 금지.

## 2. [HARD] 이 세션이 라이브에 바꾼 것

| # | 커밋 | 대상 | 변경 | 검증 |
|---|---|---|---|---|
| 1 | `efedcbf5` | 미니접지카드 `PRD_000028` | 가격공식 연결에 `PRF_DGP_E_FOIL`(08-30 적용) 행 추가 | 박 선택 시 +66,000 · 비박 무변화 |
| 2 | `f049c3d8` | `COMP_CUT_FULL_DIECUT` | 차원에 `proc_cd`+`proc_grp:PROC_000121` 추가 · 72행 충전 · `PRF_DGP_C` 배선 | 044 커팅비 +18,000 회수 · 046/051 과대청구 해소 |
| 3 | `4ebba1dd` | `COMP_CUT_PERF_1H6` | 18행 `proc_cd` 4mm→6mm(`PROC_000126`) | 045 구수1 2,000 / 구수2 4,000 |
| 4 | `2e9d14d0` | `COMP_ACC_ENV_ETC` | **4행 추가**(투명케이스 3 · 고무판 1) | `009`·`011` 0원 → 권위값 |
| 5 | `4f1686c1` | `COMP_CUT_PERF_CAL` | **신설** + `110`·`111` 배선 | 타공 +1,000/+1,500 |
| 6 | `67d06703` | 캘린더 가격축 | **`opt_cd` 전환** · `COMP_BIND_CAL_WALL_OPT` 신설 · `PRF_DGP_CAL_WIDE` 제본비 교체 | 위젯 「가공」에서 가격 붙음 |
| — | `ba2b9fc0` | 엽서캘린더 `110` | 타공비 배선 **철회**(권위 재해석) | 원복 확인 |
| — | `8df90a5b` | 옵션 기본여부 | **범위 위반 철회** | 원복 확인 |

**단가 값 삭제·수정 0건**(4·5·6의 신규 행 제외). 위젯 설정 무변경(`upd_dt` 대조 확인).

## 3. `STATUS-260830-4.md` §5 처리 결과

| 항목 | 결과 |
|---|---|
| 미니접지카드 `028` 박 | ✅ 완료(#1) |
| 인쇄배경지(커팅형) `044` 커팅비 | ✅ 완료(#2) — 판별축 선행 |
| 엽서캘린더 `110` 타공비 | ✅ 완료(#5·#6) |
| 포토카드 `024`·`025` 귀돌이비 | ✅ **결함 아님** 확정 — 권위 §8 세트 고정가뿐 |
| 투명명함 `039` 별색인쇄비 | ✅ **결함 아님** 확정 — 13,500 포함가 |
| 하드커버무선 / 레더링바인더 | ⏸ 미착수 |
| 셋트 6종 + 트윈링책자 `071` | ⏸ 미착수 — **전용 도구 필요**(§5-D) |

## 4. 새로 적발한 것 (미해결)

### 4.1 게시 완제품 0원 — 5건 (§상세 `optgroup-sweep/`)

| 상품 | 원인 | 영역 |
|---|---|---|
| 포토카드 `024` · 투명포토카드 `025` | `bdl_qty`·`opt_cd` 축이 상품에 없음 | 상품뷰어 |
| 화이트인쇄명함 `040` | 옵션그룹이 앞면/뒷면 별색을 안 보냄 | 상품뷰어 |
| 아크릴쉐이커코롯토 `226` | 권위 가격축이 **글리터**인데 라이브는 사이즈 축 · **블랙글리터 단가 권위에도 없음** | 상품뷰어+실무진 |
| 아크릴포카코롯토 `165` | `_TBD` placeholder 0행 — **결함 아님**(실무진 대기) | — |

> 도메인 §7: 0원이면 **0원으로 노출되고 0원으로 주문된다.**

### 4.2 현수막 `138`·`139` — 가공 + 부자재 동시 선택 불가

권위 「포스터사인」은 **가공옵션**과 **추가옵션**을 별개 표로 두고 「추가없음 0」이 있어 **독립·가산**이다.
라이브는 둘을 `opt_cd` 스칼라 축 하나에 합쳐 **항상 한쪽만 매칭**된다(예: 아일렛 3,000 + 끈 4,000 중 하나만).
**축 설계 문제 — 가격층만으로는 못 푼다.**

### 4.3 캘린더 — 가격은 전부 정상, 상품뷰어만 남음

`CALENDAR-AUDIT-260831.md` §5 참조. 4건 모두 **실무진 영역**:
`111`·`110` 「가공」 필수인데 기본값 없음 · `112` 「제본없음(재단만)」 옵션 부재 ·
「디자인캘린더(가격포함)」 시트 5종의 라이브 대응 없음.

### 4.4 타공 계열 잔여

- 벽걸이캘린더 2구 값이 **1,500**(상품마스터 T23)인지 **4,000**(가격표 「타공(합가)」)인지 —
  `D42` 「헤더택 / 벽걸이캘린더」 라벨이 두 표 사이에 병합 없이 떠 있다. **상품마스터 값을 택했다**(지니 판정).
- `COMP_CUT_FULL_PERF_1H6` · `2H6` — 각 9행인데 **쓰는 공식 0개**(미배선).
- 썬캡 `051` 전용 36행 도달 불가(상품에 완칼커팅 공정 미등록 · 미출시).
- 라벨/택 `046` 완칼커팅이 선택(N) — 권위 §2 는 커팅비 무조건 포함, 형제 `023` 은 필수(Y).

## 5. 다음에 할 일 (우선순위)

### A. 범위 안 — 승인만 있으면 바로 (가격층)

1. **`COMP_CUT_FULL_PERF_1H6` · `2H6` 배선** — 단가 9행씩 있는데 공식 0개. 권위 대조 후 판단.
2. **`165` 아크릴포카코롯토 `_TBD` 재확인** — 권위가 `260822_1` 로 갱신됐으니 단가가 들어왔는지.
3. **`COMP_FOLD_LEAF_3FOLD` 비고 미갱신 1건**(데이터 무변경).

### B. 권위·실무진 확인이 먼저

4. 캘린더 타공 `D42` 라벨 귀속(§4.4).
5. `226` 블랙글리터 단가.
6. 하드커버무선 / 레더링바인더 — 레더링바인더 공식 비고가 비어 근거가 약하다.

### C. 상품뷰어·위젯 영역 — **보고만 한다**

`024`·`025`·`040`·`226` 0원 / 캘린더 4건 / 현수막 축 분리 / `046` 필수화 / `051` 공정 등록.

### D. 별도 트랙 — 전용 도구 필요

**셋트 7종**(`068`·`069`·`070`·`071`·`082`·`094`·`204`)이 전수 실계산에서 0원으로 나왔다.
다만 **셋트는 `/simulate/` 가 아니라 `/simulate-set/`** 을 쓴다 — 내 도구가 부적절했을 가능성이 크다.
결함으로 단정하지 말고 **전용 검사부터** 한다. 반제품 `310`·`311` 도 같은 이유로 재확인 대상.

## 6. 도구 — 다음 세션이 바로 쓸 것

```
_workspace/price-setup/foil-mini-wire/sim.py       # 시뮬 실호출(결정론). Cloudflare UA 포함
_workspace/price-setup/optgroup-sweep/sim-all.json # 292종 전수 실계산 스냅샷
_workspace/price-setup/CALENDAR-AUDIT-260831.md    # 캘린더 전수 + 기술지식 T-1~T-8
```

**기술 함정 8가지는 `CALENDAR-AUDIT-260831.md` §8 에 정리돼 있다 — 반드시 읽는다.**
특히 T-1(단가유형은 `component_subtotal` 계산식으로 고른다) · T-2(차원·행은 한 호흡) ·
T-3(옵션축이면 0원 옵션도 행 필요).

## 7. 전수 조사 재현

```bash
B=~/.claude/skills/gstack/browse/dist/browse
set -a; . ./.env.local; set +a
$B goto https://huni-admin.printly.co.kr/admin/          # 로그인
$B cookies                                               # sessionid·csrftoken → curl

/admin/widget-manager/tree/                              # 상품 분모 292
/admin/price-viewer/{PRD}/sim-meta/                      # 차원·옵션그룹·공정·추가상품
/admin/price-viewer/{PRD}/detail/                        # 가격 소스·구성요소
/admin/price-viewer/comp/{COMP}/grid/                    # 단가표(차원·행)
/admin/price-viewer/comp/{COMP}/usage/                   # 이 구성요소를 쓰는 공식·상품
/admin/price-formula/{FRM}/products/                     # 이 공식을 쓰는 상품
/admin/widget-builder/widgets/{PRD}/                     # 위젯 목록
/admin/widget-builder/w/{WGT}/load/                      # 위젯 cfg (★분모 아님 — T-6)
/admin/widget-builder/?prd={PRD}&wgt={WGT}               # 위젯 실화면(.hw-root)
```

**권위 엑셀은 매번 절대경로로 최신본 확인**:
`ls -la /Users/innojini/Dev/HuniWeb/docs/huni/*.xlsx` → 현재 **`260822_1`**

---

## 8. 다음 세션 시작점 (붙여넣기용)

✂──── 여기부터 복사 ────✂

ultrathink. 가격 배선 교정 이어서.

전제 검증:
1. `_workspace/price-setup/STATUS-260831.md` 를 먼저 정독한다.
   §1 작업 규약 3항(상품뷰어·위젯은 실무진 영역 — 절대 쓰지 않는다)을 특히 본다.
2. `_workspace/price-setup/CALENDAR-AUDIT-260831.md` §8 기술지식 T-1~T-8 을 읽는다.
   특히 T-1(단가유형은 pricing.py component_subtotal 계산식으로 고른다).
3. 권위 최신본을 절대경로로 재확인:
   `ls -la /Users/innojini/Dev/HuniWeb/docs/huni/*.xlsx`
4. [HARD] 손댈 화면은 가격공식·가격구성요소·가격뷰어뿐이다.
   범위 밖 사실은 보고만 하고, 교정안을 선택지로 올리지 않는다.

실행: `COMP_CUT_FULL_PERF_1H6` · `COMP_CUT_FULL_PERF_2H6` 을 확인한다.
 각 9행 단가가 있는데 쓰는 공식이 0개다. 권위 가격표 「커팅타공」 커팅(완칼) 표와
 대조해 어느 상품이 이 표를 써야 하는지 판정하고, 배선 여부를 보고한다.
 배선 전 `_workspace/price-setup/foil-mini-wire/sim.py` 로 기준선을 뜬다.

후속: 셋트 7종(068·069·070·071·082·094·204) 전용 검사 —
 `/admin/price-viewer/{PRD}/simulate-set/` 경로를 쓴다. 앞선 0원 관측은
 `/simulate/` 로 부른 결과라 도구 오호출일 가능성이 크다. 결함으로 단정하지 않는다.

✂──── 여기까지 복사 ────✂
